-- =============================================================================
-- Interview Prep — Generated Frontend Seed Data (JavaScript + React)
-- AUTO-GENERATED by scripts/generate-seed.mjs from scripts/seed-data/**/*.json.
-- Do not hand-edit this file — edit the JSON source files and regenerate:
--   node scripts/generate-seed.mjs
--
-- Idempotent: upserts by slug, safe to re-run. Run AFTER supabase/schema.sql
-- and supabase/seed.sql (languages/topics must already exist).
-- =============================================================================

insert into public.questions (
  language_id, topic_id, slug, question, answer, example, code, code_language, difficulty, tags, is_frequently_asked, display_order
)
select l.id, t.id, v.slug, v.question, v.answer, v.example, v.code, v.code_language, v.difficulty, v.tags, v.is_frequently_asked, v.display_order
from (values
('javascript','async-javascript','javascript-async-javascript-101','How do you create a Promise manually using the Promise constructor?','You pass an executor function to new Promise((resolve, reject) => {...}); call resolve(value) when the async operation succeeds, or reject(error) when it fails. The executor runs synchronously and immediately, but resolve/reject are typically called later, once some asynchronous work (like a timer or I/O callback) completes.',null,'const wait = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
await wait(1000);','javascript','easy',ARRAY['javascript','async']::text[],true,101),
('javascript','async-javascript','javascript-async-javascript-102','What''s a common pitfall when chaining multiple .then() calls?','Forgetting to return a value (or a promise) from inside a .then() callback silently breaks the chain — subsequent .then() calls receive undefined instead of the expected data, since each .then() implicitly wraps whatever its callback returns in a resolved promise. Another common pitfall is nesting .then() calls instead of chaining them flatly, which recreates the callback-hell problem promises were meant to solve.',null,null,null,'medium',ARRAY['javascript','async','promises']::text[],false,102),
('javascript','async-javascript','javascript-async-javascript-103','Should you use try/catch or .catch() to handle errors in an async function?','Both work, but they suit different situations: try/catch around await calls handles errors the same way as synchronous code and is usually more readable for sequential logic. .catch() attached directly to a promise chain is useful when you want to handle errors for a specific async call without wrapping the whole function, or when working outside an async function entirely.',null,'async function load() {
  try {
    const data = await fetchData();
  } catch (err) {
    console.error(''failed'', err);
  }
}','javascript','medium',ARRAY['javascript','async','error-handling']::text[],true,103),
('javascript','async-javascript','javascript-async-javascript-104','What does Promise.any() do, and how does it differ from Promise.race()?','Promise.any() resolves as soon as any one of the input promises fulfills, and only rejects if all of them reject (with an AggregateError containing all the individual errors). Promise.race() settles on whichever promise finishes first, whether it fulfills or rejects — so race can reject even if a later promise would have succeeded, while any() specifically waits for a success.',null,null,null,'hard',ARRAY['javascript','async','promises']::text[],false,104),
('javascript','async-javascript','javascript-async-javascript-105','What is ''promisifying'' a callback-based function, and how would you do it manually?','Promisifying wraps an older Node-style callback API (fn(args, (err, result) => {...})) in a function that returns a Promise instead, so it can be used with async/await. You wrap the call in new Promise((resolve, reject) => {...}), calling reject(err) if the callback receives an error and resolve(result) otherwise.',null,'function promisify(fn) {
  return (...args) => new Promise((resolve, reject) => {
    fn(...args, (err, result) => err ? reject(err) : resolve(result));
  });
}','javascript','medium',ARRAY['javascript','async','node']::text[],false,105),
('javascript','async-javascript','javascript-async-javascript-106','What does for await...of do, and when do you need it instead of a regular for...of?','for await...of iterates over an async iterable (an object whose Symbol.asyncIterator returns items wrapped in promises), automatically awaiting each yielded value before the loop body runs. It''s needed when iterating over something like a paginated API, a readable stream, or an async generator, where each ''next'' value itself takes time to produce.',null,null,null,'hard',ARRAY['javascript','async','iterators']::text[],false,106),
('javascript','async-javascript','javascript-async-javascript-107','How were generators used to manage asynchronous flow before async/await existed?','Generator functions (function*) can pause execution at a yield and be resumed later, so libraries like co wrapped a generator in a driver function that would yield a promise, wait for it to resolve, then resume the generator with the resolved value — effectively hand-rolling what async/await now does natively. async/await is often described as syntactic sugar over this exact generator-plus-promise-driver pattern.',null,null,null,'hard',ARRAY['javascript','async','generators']::text[],false,107),
('javascript','async-javascript','javascript-async-javascript-108','What is the practical difference between setTimeout and setInterval?','setTimeout schedules a callback to run once after a delay. setInterval schedules a callback to run repeatedly every given interval until explicitly stopped with clearInterval — but if the callback itself takes longer than the interval, calls can queue up or overlap in confusing ways, which is why a self-rescheduling setTimeout is often preferred for reliable repeated polling.',null,null,null,'easy',ARRAY['javascript','async','timers']::text[],false,108),
('javascript','async-javascript','javascript-async-javascript-109','What does queueMicrotask() do?','queueMicrotask() schedules a callback to run in the microtask queue, the same queue used for resolved Promise .then() callbacks — meaning it runs after the current synchronous code finishes but before the next macrotask (like a setTimeout callback or UI render). It''s useful when you want promise-like ''run soon but not synchronously'' timing without actually creating a Promise object.',null,'queueMicrotask(() => console.log(''microtask''));
console.log(''sync''); // logs: sync, then microtask','javascript','medium',ARRAY['javascript','async','event-loop']::text[],false,109),
('javascript','async-javascript','javascript-async-javascript-110','What causes a race condition in async JavaScript code, and how can you guard against one?','A race condition occurs when multiple async operations that read/write shared state can complete in an unpredictable order, causing the final result to depend on timing rather than logic — a classic example is a search input firing multiple requests where an older, slower response overwrites a newer one. Guards include ignoring stale responses (tracking a request ID/token and discarding out-of-order results) or cancelling the previous request before starting a new one.',null,null,null,'hard',ARRAY['javascript','async']::text[],true,110),
('javascript','async-javascript','javascript-async-javascript-111','How do you cancel an in-flight fetch request?','Create an AbortController, pass its .signal to fetch''s options, and call controller.abort() to cancel the request — fetch then rejects with an AbortError. This is commonly used to cancel a previous request when a new one starts (e.g. in a search-as-you-type feature) to avoid race conditions and wasted bandwidth.',null,'const controller = new AbortController();
fetch(url, { signal: controller.signal });
controller.abort(); // cancels the request','javascript','medium',ARRAY['javascript','async','fetch']::text[],false,111),
('javascript','async-javascript','javascript-async-javascript-112','What''s the difference between running two async operations sequentially vs in parallel?','Sequential execution (await a(); await b();) waits for each to finish before starting the next, so total time is the sum of both — necessary when b depends on a''s result. Parallel execution (await Promise.all([a(), b()]), or starting both calls before awaiting either) runs them concurrently, so total time is roughly the slower of the two — appropriate when the operations are independent.',null,'// Sequential: slower
const a = await fetchA(); const b = await fetchB();
// Parallel: faster
const [a2, b2] = await Promise.all([fetchA(), fetchB()]);','javascript','medium',ARRAY['javascript','async']::text[],true,112),
('javascript','async-javascript','javascript-async-javascript-113','What does an async function actually return if you don''t write an explicit return statement?','An async function always returns a Promise, even without an explicit return — if there''s no return statement, it implicitly returns a Promise that resolves to undefined once the function body finishes executing. If you return a plain value, it''s automatically wrapped in a resolved Promise; if you return a promise, the outer promise adopts its state.',null,null,null,'easy',ARRAY['javascript','async']::text[],false,113),
('javascript','async-javascript','javascript-async-javascript-114','What is an ''unhandled promise rejection'' and why is it a problem?','It occurs when a Promise rejects but no .catch() handler (or try/catch around an await) is attached to observe the rejection, meaning the error is silently swallowed instead of surfacing. In Node.js this can even crash the process depending on version/configuration, and in browsers it logs a console warning — either way it usually indicates a missing error-handling path in the code.',null,null,null,'medium',ARRAY['javascript','async','error-handling']::text[],false,114),
('javascript','async-javascript','javascript-async-javascript-115','How does an error thrown deep inside an await chain propagate to the caller?','If any awaited call inside an async function throws (or its promise rejects), that rejection propagates up and effectively ''throws'' at the await point, which can be caught by a surrounding try/catch in that same function. If uncaught locally, the async function''s own returned promise rejects, and the error continues propagating to whatever calls that function and awaits (or catches) it.',null,null,null,'medium',ARRAY['javascript','async','error-handling']::text[],false,115),
('javascript','async-javascript','javascript-async-javascript-116','How would you limit the number of concurrent async requests running at once?','A common pattern is a simple concurrency-limited queue: maintain a counter of in-flight requests, and only start a new one from a work queue when the counter is below a max limit, decrementing and pulling the next item when one finishes. Libraries like p-limit implement this, but it can be hand-rolled with a loop of workers each pulling from a shared queue.',null,null,null,'medium',ARRAY['javascript','async','performance']::text[],false,116),
('javascript','async-javascript','javascript-async-javascript-117','How would you implement retry logic with exponential backoff for a flaky async call?','Wrap the call in a loop (or recursive function) that catches failures, and on each retry waits progressively longer (e.g. doubling the delay each time, often with some random jitter added) before trying again, up to a maximum number of attempts. This avoids hammering a struggling service with immediate retries while still recovering automatically from transient failures.',null,'async function retry(fn, attempts = 3, delay = 500) {
  for (let i = 0; i < attempts; i++) {
    try { return await fn(); }
    catch (err) { if (i === attempts - 1) throw err; await new Promise(r => setTimeout(r, delay * 2 ** i)); }
  }
}','javascript','hard',ARRAY['javascript','async','resilience']::text[],false,117),
('javascript','async-javascript','javascript-async-javascript-118','What do Promise.resolve() and Promise.reject() do?','Promise.resolve(value) returns a Promise that''s already fulfilled with the given value (or, if given a thenable/promise, adopts its eventual state). Promise.reject(error) returns a Promise that''s already rejected with the given error. They''re useful for normalizing a value into a Promise or short-circuiting a chain with an immediate outcome.',null,'Promise.resolve(42).then(v => console.log(v)); // 42
Promise.reject(''oops'').catch(e => console.log(e)); // oops','javascript','easy',ARRAY['javascript','async','promises']::text[],false,118),
('javascript','async-javascript','javascript-async-javascript-119','What does it mean to ''compose'' async functions, and how would you do it?','Composing async functions means chaining several async steps together so the output of one feeds into the next, typically written as a sequence of awaited calls inside a single async function, or via a reduce over an array of async functions that each await the previous result. It keeps multi-step async pipelines (validate, then transform, then save) readable as straight-line code instead of nested callbacks.',null,null,null,'medium',ARRAY['javascript','async']::text[],false,119),
('javascript','async-javascript','javascript-async-javascript-120','What''s the difference between blocking and non-blocking I/O, and where does JavaScript fit?','Blocking I/O halts the entire thread until an operation (like a file read or network call) completes, wasting time waiting. Non-blocking I/O lets the thread continue doing other work while the operation happens in the background, invoking a callback (or resolving a promise) when it''s done. JavaScript''s single-threaded runtime relies heavily on non-blocking I/O, via the event loop, to stay responsive despite having only one thread of execution.',null,null,null,'medium',ARRAY['javascript','async','event-loop']::text[],false,120),
('javascript','async-javascript','javascript-async-javascript-121','How do timers interact with the event loop''s phases?','A timer callback (from setTimeout/setInterval) isn''t guaranteed to run at exactly its specified delay — it''s only guaranteed to run no earlier than that delay, since it must wait for the call stack to be empty and for its turn in the timer phase of the event loop. If synchronous code or other queued work is still executing, a timer''s callback can fire noticeably later than requested.',null,null,null,'medium',ARRAY['javascript','async','event-loop']::text[],false,121),
('javascript','async-javascript','javascript-async-javascript-122','Why is requestAnimationFrame preferred over setTimeout for animations?','requestAnimationFrame schedules a callback to run right before the browser''s next repaint, syncing your updates with the display''s actual refresh rate (typically 60fps) and automatically pausing when the tab is inactive to save resources. setTimeout has no awareness of the rendering pipeline, so animations driven by it can appear janky or keep running (wasting battery) in a backgrounded tab.',null,null,null,'medium',ARRAY['javascript','async','performance']::text[],false,122),
('javascript','async-javascript','javascript-async-javascript-123','What problem do Web Workers solve, and what are their limitations?','Web Workers run JavaScript on a separate background thread, letting you offload CPU-intensive work (like heavy computation or data processing) without blocking the main thread and freezing the UI. Their main limitation is that they don''t share memory with the main thread — communication happens via message passing (postMessage), and they can''t directly access the DOM.',null,null,null,'hard',ARRAY['javascript','async','performance']::text[],false,123),
('javascript','async-javascript','javascript-async-javascript-124','Why doesn''t array.forEach(async (item) => { await doSomething(item); }) wait for all the async work to finish?','forEach doesn''t know or care what its callback returns — it calls the callback for each item and moves on immediately, ignoring the Promise each async callback returns. This means code after the forEach call runs before any of the async operations actually complete, which is a very common source of bugs; using a for...of loop with await, or Promise.all with map, are the correct alternatives.',null,'// Wrong: doesn''t wait
items.forEach(async (item) => await save(item));
// Correct: waits for all
await Promise.all(items.map(item => save(item)));','javascript','medium',ARRAY['javascript','async']::text[],true,124),
('javascript','async-javascript','javascript-async-javascript-125','When would you prefer explicit .then() chaining over async/await?','then() chaining can be more natural for simple, single-step transformations of a promise''s result, or in contexts that can''t be async functions. For anything with sequential logic, conditionals, or loops, async/await is almost always more readable since it lets asynchronous code be structured like ordinary synchronous code.',null,null,null,'easy',ARRAY['javascript','async']::text[],false,125),
('javascript','async-javascript','javascript-async-javascript-126','How can async code create a deadlock-like situation, even though JS is single-threaded?','It happens when two async operations end up waiting on each other in a cycle — for example, two mutually-dependent promises where each awaits a resolution that depends on the other completing first, or a lock/mutex-style pattern used to serialize access to a shared resource where a lock is acquired but never released due to an unhandled error path. It''s less common than true multi-threaded deadlocks but can still hang a specific async flow indefinitely.',null,null,null,'medium',ARRAY['javascript','async']::text[],false,126),
('javascript','async-javascript','javascript-async-javascript-127','Conceptually, how does streaming a response differ from awaiting the whole response at once?','Streaming processes data in chunks as they arrive (e.g. via a ReadableStream reader or an async iterator over response.body), letting you start working with the beginning of a large payload before the rest has finished downloading. Awaiting the whole response (like response.json()) buffers everything into memory first and only resolves once the entire payload has arrived, which is simpler but less memory-efficient for large responses.',null,null,null,'medium',ARRAY['javascript','async','streams']::text[],false,127),
('javascript','async-javascript','javascript-async-javascript-128','How would you implement a debounced async search that avoids showing stale results?','Debounce the trigger (wait for a pause in typing before firing the request) to avoid firing a request per keystroke, and separately guard against out-of-order responses — e.g. by tagging each request with an incrementing ID and only applying the response if its ID matches the latest request, or by aborting the previous request with AbortController before starting a new one.',null,'let latestId = 0;
async function search(query) {
  const id = ++latestId;
  const results = await fetchResults(query);
  if (id === latestId) render(results);
}','javascript','hard',ARRAY['javascript','async','performance']::text[],false,128),
('javascript','async-javascript','javascript-async-javascript-129','What is an async generator function and when would you use one?','An async generator (async function*) combines generators and promises — each yielded value can itself be awaited, and it''s consumed with for await...of. It''s useful for representing a sequence of asynchronously-produced values, like paginated API results fetched page by page on demand, without loading everything into memory up front.',null,'async function* fetchPages(url) {
  let next = url;
  while (next) {
    const res = await fetch(next).then(r => r.json());
    yield res.items;
    next = res.nextPageUrl;
  }
}','javascript','hard',ARRAY['javascript','async','generators']::text[],false,129),
('javascript','async-javascript','javascript-async-javascript-130','What is the fundamental difference between synchronous and asynchronous code execution in JavaScript?','Synchronous code executes immediately, line by line, blocking further execution until each statement completes. Asynchronous code initiates an operation (like a timer, network request, or file read) and continues executing subsequent code immediately, with the result of the async operation delivered later via a callback, promise, or await, without blocking the single JS thread in the meantime.',null,null,null,'easy',ARRAY['javascript','async']::text[],false,130),
('javascript','async-javascript','javascript-async-javascript-131','What happens if you await a value that isn''t a Promise?','await works on any value, not just promises — if the value isn''t a thenable, it''s simply wrapped in an already-resolved Promise and immediately returns that value on the next microtask tick. This means you can safely await a plain number, string, or object without an error; it just adds a tiny amount of asynchronous scheduling.',null,'async function f() {
  const x = await 5; // works fine, x === 5
}','javascript','easy',ARRAY['javascript','async']::text[],false,131),
('javascript','async-javascript','javascript-async-javascript-132','What are the three states of a Promise, and why can it only transition between them once?','A Promise is pending initially, then settles exactly once into either fulfilled (with a value) or rejected (with a reason) — once settled, its state and value are permanently locked and further calls to resolve/reject are no-ops. This ''settle once'' guarantee is what makes promises reliable to consume: any .then()/.catch() attached, even after settlement, always fires with the same, immutable outcome.',null,null,null,'medium',ARRAY['javascript','async','promises']::text[],false,132),
('javascript','async-javascript','javascript-async-javascript-133','What happens if a .then() callback returns another Promise instead of a plain value?','The chain automatically ''flattens'' — instead of resolving with a nested promise-within-a-promise, the outer chain waits for the returned inner promise to settle and adopts its eventual value or rejection. This auto-flattening is what makes sequential async operations chainable without ending up with deeply nested promise structures.',null,null,null,'medium',ARRAY['javascript','async','promises']::text[],false,133),
('javascript','async-javascript','javascript-async-javascript-134','Why does an async function''s promise resolve to undefined if it has no return statement, rather than never resolving?','Reaching the end of the function body (falling off the end) is treated exactly like an implicit return undefined; in regular JavaScript functions, and async functions inherit that behavior — completing execution, even without an explicit return, is what triggers the returned promise to fulfill, just with an undefined value rather than leaving it perpetually pending.',null,null,null,'medium',ARRAY['javascript','async']::text[],false,134),
('javascript','async-javascript','javascript-async-javascript-135','How do you kick off several independent async operations without letting a slow one block the others?','Start all the operations first (without awaiting each individually), collecting their promises, and only then await them together — either with Promise.all if you need all results and want to fail fast on any rejection, or Promise.allSettled if you want every outcome regardless of individual failures. Awaiting each one immediately in sequence would needlessly serialize otherwise-independent work.',null,null,null,'medium',ARRAY['javascript','async']::text[],false,135),
('javascript','async-javascript','javascript-async-javascript-136','How does a finally block behave inside an async function with try/catch?','The finally block runs after the try (and catch, if triggered) completes, regardless of whether the awaited code succeeded, threw, or even if a return happened inside try/catch — making it ideal for cleanup logic, like closing a connection or hiding a loading spinner, that must run no matter the outcome.',null,'async function load() {
  showSpinner();
  try { return await fetchData(); }
  finally { hideSpinner(); }
}','javascript','easy',ARRAY['javascript','async','error-handling']::text[],false,136),
('javascript','async-javascript','javascript-async-javascript-137','How do you cancel a pending setTimeout before it fires?','setTimeout returns an identifier (a number in browsers, a Timeout object in Node), which you pass to clearTimeout() to cancel the scheduled callback before it runs. This is commonly used to reset a debounce timer or clean up a pending timer when a component unmounts.',null,'const id = setTimeout(doThing, 1000);
clearTimeout(id); // cancels it before it fires','javascript','easy',ARRAY['javascript','async','timers']::text[],false,137),
('javascript','async-javascript','javascript-async-javascript-138','At a high level, how does an Observable-based approach like RxJS differ from Promise-based async code?','A Promise represents a single future value and can only resolve once, while an Observable represents a stream of zero, one, or many values over time and can be cancelled (unsubscribed) mid-stream. Observables are better suited to ongoing event sources, like user input, WebSocket messages, or periodic polling, whereas Promises are a natural fit for one-off async operations like a single API call.',null,null,null,'hard',ARRAY['javascript','async','rxjs']::text[],false,138),
('javascript','async-javascript','javascript-async-javascript-139','How do Promises solve the ''callback hell'' problem that plagued earlier asynchronous JavaScript?','Callback hell arises from deeply nesting callbacks for sequential async steps, each one indented inside the previous, making code hard to read and error handling inconsistent since each callback needs its own error check. Promises let you chain steps flatly with .then(), and centralize error handling in a single .catch() at the end of the chain, later improved further by async/await which reads like fully synchronous code.',null,'// Callback hell:
getUser(id, (user) => {
  getPosts(user, (posts) => {
    render(posts);
  });
});
// With async/await:
const user = await getUser(id);
const posts = await getPosts(user);
render(posts);','javascript','easy',ARRAY['javascript','async']::text[],true,139),
('javascript','async-javascript','javascript-async-javascript-140','Why is items.forEach(async item => await process(item)) a common bug, compared to a regular for loop?','The async arrow callback passed to forEach returns a Promise per call, but forEach''s implementation completely discards each callback''s return value and never awaits anything — so it fires off all iterations near-instantly without waiting for any of them, and any code following the forEach call runs before the async work finishes or even before errors within it are observable. A plain for or for...of loop with await inside genuinely pauses the loop itself at each iteration.',null,null,null,'medium',ARRAY['javascript','async']::text[],false,140),
('javascript','async-javascript','javascript-async-javascript-141','How would you await results into named variables when using Promise.all?','Promise.all returns an array of results in the same order as the input array of promises, so you can destructure that array directly into named variables in one line, which reads more clearly than accessing results[0], results[1], etc.',null,'const [user, posts] = await Promise.all([fetchUser(id), fetchPosts(id)]);','javascript','easy',ARRAY['javascript','async','promises']::text[],false,141),
('javascript','async-javascript','javascript-async-javascript-142','How would you add a timeout to a Promise that might otherwise hang indefinitely?','Use Promise.race() between the real operation and a second promise that rejects after a fixed delay via setTimeout — whichever settles first ''wins'', so if the real operation is too slow, the timeout promise rejects first and the overall race rejects with a timeout error.',null,'function withTimeout(promise, ms) {
  const timeout = new Promise((_, reject) => setTimeout(() => reject(new Error(''timeout'')), ms));
  return Promise.race([promise, timeout]);
}','javascript','medium',ARRAY['javascript','async','promises']::text[],false,142),
('javascript','async-javascript','javascript-async-javascript-143','What is a ''thenable'' and why does it matter for how Promises interoperate?','A thenable is any object with a .then(onFulfilled, onRejected) method, regardless of whether it''s a native Promise — the Promise spec is designed to treat any thenable as promise-like, so awaiting or chaining onto a thenable from a different library or a custom implementation still works correctly. This duck-typed interoperability is why different Promise implementations, including ones written before native Promises existed, can be mixed and awaited seamlessly.',null,null,null,'hard',ARRAY['javascript','async','promises']::text[],false,143),
('javascript','async-javascript','javascript-async-javascript-144','How does the microtask queue interact with multiple chained .then() calls versus a single setTimeout?','All queued microtasks, including every pending .then() callback, run to completion before the event loop moves on to the next macrotask, so a long or deeply chained sequence of .then() callbacks will all execute before a setTimeout(fn, 0) callback fires, even though the setTimeout was scheduled first. This is a common source of confusion when reasoning about the exact order operations run in.',null,null,null,'hard',ARRAY['javascript','async','event-loop']::text[],false,144),
('javascript','async-javascript','javascript-async-javascript-145','What''s a subtle bug that can occur from forgetting to return a promise chain inside another async function?','If an async function starts a nested async operation but doesn''t return (or await) its promise, the outer function''s own promise can resolve before the inner operation finishes — meaning any code awaiting the outer function proceeds too early, as if the nested work were already done when it might still be in flight. This is often called an accidental ''fire and forget'' bug.',null,'async function bad() {
  doAsyncThing(); // missing await/return
}
async function good() {
  return doAsyncThing(); // or: await doAsyncThing();
}','javascript','medium',ARRAY['javascript','async']::text[],false,145),
('javascript','async-javascript','javascript-async-javascript-146','How would you test that an async function correctly rejects with a specific error?','In most test frameworks you either await the call inside a try/catch and assert inside the catch block, failing the test if no error was thrown, or use the framework''s dedicated helper for rejection assertions, like Jest''s await expect(fn()).rejects.toThrow(''message''), which awaits the promise and asserts on its rejection reason directly.',null,null,null,'medium',ARRAY['javascript','async','testing']::text[],false,146),
('javascript','async-javascript','javascript-async-javascript-147','What''s a common cause of memory leaks in async/event-driven JavaScript code?','Forgetting to clear timers (clearTimeout/clearInterval) or remove event listeners/subscriptions when they''re no longer needed, such as when a component unmounts, keeps callbacks — and everything they close over — alive indefinitely, since the runtime can''t garbage-collect anything still referenced by an active timer or listener. This is especially common in single-page apps where components mount and unmount repeatedly.',null,null,null,'medium',ARRAY['javascript','async','memory']::text[],false,147),
('javascript','async-javascript','javascript-async-javascript-148','Why can returning a rejected promise from inside a .then() success callback still be caught by a later .catch()?','A .catch() is really just .then(undefined, onRejected) attached further down the chain, and any rejection — whether it comes from the original promise or is produced partway through the chain by a thrown error or an explicitly returned rejected promise — propagates forward past any intermediate .then() calls that don''t have a matching rejection handler, until it reaches the first one that does.',null,null,null,'hard',ARRAY['javascript','async','promises']::text[],false,148),
('javascript','async-javascript','javascript-async-javascript-149','When multiple .then() handlers are attached to the same promise, in what order do they run?','They run in the order they were attached, once the promise settles — each is scheduled as a separate microtask, so if a promise resolves and three .then() callbacks are attached to it, all three run, each as its own microtask, before any subsequently-scheduled macrotask, in registration order relative to each other.',null,null,null,'hard',ARRAY['javascript','async','promises']::text[],false,149),
('javascript','async-javascript','javascript-async-javascript-150','What does ''microtask queue starvation'' mean, and how can it happen?','It''s a situation where microtasks keep scheduling more microtasks, such as a .then() callback that recursively queues another .then(), faster than the queue can drain, which starves the event loop of the chance to reach the next macrotask phase — meaning rendering, timers, and I/O callbacks can be indefinitely delayed since the spec requires the entire microtask queue to empty before moving on. It''s a rare but real performance pitfall in recursive promise-based code.',null,null,null,'hard',ARRAY['javascript','async','event-loop']::text[],false,150),
('javascript','basics','javascript-basics-101','What is the difference between primitive and reference types in JavaScript?','Primitive types (string, number, boolean, null, undefined, symbol, bigint) are immutable and copied by value when assigned or passed to a function. Reference types (objects, arrays, functions) are copied by reference, so two variables can point to the same underlying object and mutations through one are visible through the other.',null,'let a = { x: 1 };
let b = a;
b.x = 2;
console.log(a.x); // 2, same object','javascript','easy',ARRAY['javascript','basics','types']::text[],true,101),
('javascript','basics','javascript-basics-102','What are truthy and falsy values in JavaScript?','A truthy value is any value that evaluates to true in a boolean context, and a falsy value evaluates to false. The only falsy values are false, 0, -0, 0n, "", null, undefined, and NaN — everything else, including empty objects and arrays, is truthy.',null,'if ([]) console.log(''arrays are truthy'');','javascript','easy',ARRAY['javascript','basics']::text[],true,102),
('javascript','basics','javascript-basics-103','Why does typeof NaN return "number"?','NaN (Not-a-Number) is technically a special numeric value defined by the IEEE 754 floating-point standard to represent an invalid or undefined arithmetic result, so JavaScript classifies it under the number type. It''s the only value in JavaScript that is not equal to itself, which is why Number.isNaN() exists to reliably detect it.',null,'typeof NaN === ''number''; // true','javascript','medium',ARRAY['javascript','basics','numbers']::text[],false,103),
('javascript','basics','javascript-basics-104','What is the difference between typeof and instanceof?','typeof returns a string describing the primitive type of a value (like "string", "number", "object", "function"), and is evaluated at the operand level without knowledge of the prototype chain. instanceof checks whether an object''s prototype chain contains a given constructor''s prototype, so it works for distinguishing between different object types like Array vs plain Object.',null,'[] instanceof Array; // true
typeof []; // ''object''','javascript','easy',ARRAY['javascript','basics']::text[],false,104),
('javascript','basics','javascript-basics-105','What is type coercion in JavaScript?','Type coercion is JavaScript''s automatic (implicit) conversion of a value from one type to another when an operator or context expects a different type, such as converting a number to a string during string concatenation with +. It can also be done explicitly using functions like Number(), String(), or Boolean().',null,'1 + ''2'' === ''12''; // number coerced to string','javascript','easy',ARRAY['javascript','basics','coercion']::text[],true,105),
('javascript','basics','javascript-basics-106','Why does 0.1 + 0.2 not equal 0.3 in JavaScript?','JavaScript stores numbers as IEEE 754 double-precision floating-point values, which cannot represent most decimal fractions exactly in binary. The tiny rounding error means 0.1 + 0.2 evaluates to 0.30000000000000004, so comparisons involving decimals should use a small tolerance (epsilon) instead of strict equality.',null,'Math.abs(0.1 + 0.2 - 0.3) < Number.EPSILON;','javascript','medium',ARRAY['javascript','basics','numbers']::text[],false,106),
('javascript','basics','javascript-basics-107','What is an IIFE (Immediately Invoked Function Expression)?','An IIFE is a function that is defined and executed immediately after its creation, typically wrapped in parentheses to force it to be parsed as an expression rather than a declaration. It was historically used to create a private scope and avoid polluting the global namespace before ES6 modules and block scoping existed.',null,'(function () {
  console.log(''runs immediately'');
})();','javascript','easy',ARRAY['javascript','basics','functions']::text[],false,107),
('javascript','basics','javascript-basics-108','What does "use strict" do in JavaScript?','"use strict" enables strict mode, which catches common coding mistakes by throwing errors instead of silently failing — for example, assigning to an undeclared variable, duplicating parameter names, or using reserved words as identifiers. It also disables some confusing features like the `with` statement and changes `this` to be undefined in plain function calls instead of the global object.',null,null,null,'medium',ARRAY['javascript','basics']::text[],false,108),
('javascript','basics','javascript-basics-109','What is the global object in JavaScript, and how do you access it?','The global object holds global variables and functions available everywhere in a JavaScript environment; in browsers it''s `window`, in Node.js it''s `global`. The `globalThis` keyword, introduced in ES2020, provides a standard way to access the global object regardless of the environment.',null,null,null,'easy',ARRAY['javascript','basics']::text[],false,109),
('javascript','basics','javascript-basics-110','What are Symbols in JavaScript and what are they used for?','A Symbol is a primitive type introduced in ES6 that creates a unique and immutable value, often used as a property key to avoid naming collisions with other properties, including ones added by other libraries. Symbols are not enumerable in for...in loops and are commonly used to implement well-known protocol hooks like Symbol.iterator.',null,'const id = Symbol(''id'');
const obj = { [id]: 123 };','javascript','medium',ARRAY['javascript','basics','es6']::text[],false,110),
('javascript','basics','javascript-basics-111','What is BigInt and when would you use it?','BigInt is a primitive type that can represent integers with arbitrary precision, beyond the safe integer limit of Number (2^53 - 1). It''s created by appending `n` to an integer literal or calling BigInt(), and is used when working with very large numbers such as cryptographic calculations or high-precision IDs.',null,'const big = 9007199254740993n;','javascript','medium',ARRAY['javascript','basics','numbers']::text[],false,111),
('javascript','basics','javascript-basics-112','What is the difference between JSON.parse and JSON.stringify?','JSON.stringify() converts a JavaScript value (object, array, primitive) into a JSON-formatted string, useful for sending data over the network or storing it. JSON.parse() does the reverse, converting a JSON string back into a JavaScript value — both throw errors on malformed input.',null,'const str = JSON.stringify({ a: 1 }); // ''{"a":1}''
const obj = JSON.parse(str); // { a: 1 }','javascript','easy',ARRAY['javascript','basics','json']::text[],true,112),
('javascript','basics','javascript-basics-113','Why is NaN !== NaN in JavaScript, and how do you check for it correctly?','Per the IEEE 754 standard, NaN is defined to never equal any value, including itself, since it represents an undefined or unrepresentable numeric result. To reliably check for NaN, use Number.isNaN(value), which — unlike the global isNaN() — does not coerce its argument first.',null,'NaN === NaN; // false
Number.isNaN(NaN); // true','javascript','medium',ARRAY['javascript','basics','numbers']::text[],false,113),
('javascript','basics','javascript-basics-114','What is the difference between block scope, function scope, and global scope?','Global scope is accessible everywhere in the program. Function scope means a variable is accessible anywhere within the function it was declared in, regardless of nested blocks (this is how `var` behaves). Block scope, used by `let` and `const`, restricts a variable''s visibility to the nearest enclosing pair of curly braces, such as an if block or loop.',null,null,null,'easy',ARRAY['javascript','basics','scope']::text[],false,114),
('javascript','basics','javascript-basics-115','What does the void operator do in JavaScript?','The void operator evaluates an expression and always returns undefined, discarding the expression''s result. It''s rarely needed in modern code but historically appeared in `javascript:void(0)` links to prevent page navigation while still executing the expression''s side effects.',null,'void 0; // undefined','javascript','hard',ARRAY['javascript','basics']::text[],false,115),
('javascript','basics','javascript-basics-116','What are labeled statements in JavaScript and when are they useful?','A labeled statement attaches an identifier to a loop or block so that `break` or `continue` can target that specific outer loop instead of just the innermost one. They''re rarely used in modern code because they can hurt readability, but they''re the standard way to break out of nested loops directly.',null,'outer: for (let i = 0; i < 3; i++) {
  for (let j = 0; j < 3; j++) {
    if (j === 1) continue outer;
    console.log(i, j);
  }
}','javascript','hard',ARRAY['javascript','basics','loops']::text[],false,116),
('javascript','basics','javascript-basics-117','What does the comma operator do in JavaScript?','The comma operator evaluates each of its operands from left to right and returns the value of the last operand, discarding the results of the others. It''s most commonly seen in the update clause of a for loop where you need to perform multiple operations, such as `for (let i = 0, j = 10; i < j; i++, j--)`.',null,'const x = (1, 2, 3); // x is 3','javascript','hard',ARRAY['javascript','basics']::text[],false,117),
('javascript','basics','javascript-basics-118','What is Automatic Semicolon Insertion (ASI) and what problems can it cause?','ASI is a JavaScript engine mechanism that automatically inserts semicolons at the end of statements when they''re omitted, based on specific parsing rules. It can cause subtle bugs, most famously when a `return` statement is followed by a newline before the returned value — ASI inserts a semicolon right after `return`, causing the function to return undefined instead of the intended value.',null,'function getValue() {
  return
  {
    value: 1
  };
}
// returns undefined, not the object, due to ASI','javascript','hard',ARRAY['javascript','basics']::text[],true,118),
('javascript','basics','javascript-basics-119','What is short-circuit evaluation in JavaScript?','Short-circuit evaluation means logical operators && and || stop evaluating as soon as the result is determined: && returns the first falsy operand (or the last if all are truthy), and || returns the first truthy operand (or the last if all are falsy). This is commonly used to provide default values or to conditionally call a function.',null,'const name = user.name || ''Guest'';','javascript','easy',ARRAY['javascript','basics','operators']::text[],false,119),
('javascript','basics','javascript-basics-120','What is the difference between the nullish coalescing operator (??) and the logical OR operator (||)?','|| returns the right-hand operand if the left is any falsy value (0, '''', false, null, undefined, NaN), while ?? only falls back to the right-hand operand if the left is specifically null or undefined. This matters when 0 or an empty string are valid values you don''t want overridden.',null,'0 || ''default''; // ''default''
0 ?? ''default''; // 0','javascript','medium',ARRAY['javascript','basics','es6']::text[],true,120),
('javascript','basics','javascript-basics-121','What is optional chaining (?.) and what problem does it solve?','Optional chaining lets you safely access deeply nested object properties without manually checking each level for null or undefined — if any part of the chain is nullish, the whole expression short-circuits and returns undefined instead of throwing a TypeError.',null,'const city = user?.address?.city;','javascript','easy',ARRAY['javascript','basics','es6']::text[],false,121),
('javascript','basics','javascript-basics-122','What are tagged template literals?','A tagged template literal is a template string preceded by a function (the tag) that processes the string and its interpolated values before producing the final result, instead of the default string interpolation. The tag function receives the literal string parts as an array and the interpolated expressions as separate arguments, enabling custom formatting, sanitization, or i18n logic.',null,'function upper(strings, ...values) {
  return strings.reduce((acc, s, i) => acc + s + (values[i]?.toUpperCase() ?? ''''), '''');
}
upper`Hello ${''world''}`; // ''Hello WORLD''','javascript','hard',ARRAY['javascript','basics','es6']::text[],false,122),
('javascript','basics','javascript-basics-123','What is the difference between the in operator and hasOwnProperty()?','The `in` operator checks whether a property exists anywhere in an object''s own properties or its prototype chain. `hasOwnProperty()` checks only the object''s own properties, ignoring anything inherited from the prototype, which makes it more precise when you want to avoid false positives from inherited properties.',null,'''toString'' in {}; // true (inherited)
({}).hasOwnProperty(''toString''); // false','javascript','medium',ARRAY['javascript','basics','objects']::text[],false,123),
('javascript','basics','javascript-basics-124','What is the difference between an array-like object and an iterable in JavaScript?','An array-like object has a numeric `length` property and indexed elements (like `arguments` or a NodeList) but doesn''t have array methods like map or forEach unless converted. An iterable implements the Symbol.iterator protocol, allowing it to be used with for...of loops and the spread operator — arrays and strings are both iterable.',null,'Array.from(document.querySelectorAll(''div'')); // array-like to real array','javascript','hard',ARRAY['javascript','basics','arrays']::text[],false,124),
('javascript','basics','javascript-basics-125','How do you explicitly convert values between types in JavaScript?','Use Number(value) to convert to a number, String(value) to convert to a string, and Boolean(value) to convert to a boolean, all of which follow well-defined coercion rules. These are generally preferred over implicit coercion (like using + '''' to convert to a string) because the intent is clearer to readers.',null,'Number(''42''); // 42
String(42); // ''42''
Boolean(''''); // false','javascript','easy',ARRAY['javascript','basics','coercion']::text[],false,125),
('javascript','basics','javascript-basics-126','What is the difference between for...in and for...of loops?','for...in iterates over the enumerable property keys of an object, including inherited ones, and is typically used with plain objects. for...of iterates over the values of an iterable (arrays, strings, Maps, Sets), making it the preferred choice for looping over array elements directly.',null,'for (const key in {a:1}) {} // ''a''
for (const val of [1,2]) {} // 1, 2','javascript','easy',ARRAY['javascript','basics','loops']::text[],true,126),
('javascript','basics','javascript-basics-127','What are some common quirks of the switch statement in JavaScript?','switch uses strict equality (===) to compare the expression against each case, so type coercion never happens. Cases without a `break` statement will "fall through" and execute the next case''s code too, which is a common source of bugs, and a `default` case can be placed anywhere, not just at the end.',null,'switch (1) {
  case 1:
  case 2:
    console.log(''one or two'');
    break;
  default:
    console.log(''other'');
}','javascript','medium',ARRAY['javascript','basics']::text[],false,127),
('javascript','basics','javascript-basics-128','How does the ternary (conditional) operator work in JavaScript?','The ternary operator `condition ? valueIfTrue : valueIfFalse` is a compact, single-expression alternative to an if/else statement, and is the only operator in JavaScript that takes three operands. It''s commonly used for concise conditional assignment but should be avoided for complex nested logic since it hurts readability.',null,'const status = age >= 18 ? ''adult'' : ''minor'';','javascript','easy',ARRAY['javascript','basics','operators']::text[],false,128),
('javascript','basics','javascript-basics-129','What is the difference between Object.is() and the strict equality operator (===)?','Object.is() behaves like === for almost all values, but differs in two edge cases: Object.is(NaN, NaN) returns true (=== returns false), and Object.is(0, -0) returns false while 0 === -0 returns true. It''s mainly used internally by React and other libraries for precise value comparison.',null,'Object.is(NaN, NaN); // true
NaN === NaN; // false','javascript','hard',ARRAY['javascript','basics']::text[],false,129),
('javascript','basics','javascript-basics-130','Why is string immutability important in JavaScript?','Strings in JavaScript are immutable — methods like toUpperCase() or slice() always return a new string rather than modifying the original. This makes strings safe to share across different parts of a program without unexpected side effects, though it means repeated concatenation in a loop can be less efficient than using an array and join().',null,'let s = ''hi'';
s[0] = ''H''; // silently fails, s is still ''hi''','javascript','medium',ARRAY['javascript','basics','strings']::text[],false,130),
('javascript','basics','javascript-basics-131','How do you reliably check if a value is an array?','Array.isArray(value) is the reliable way to check if a value is an array, because typeof returns ''object'' for arrays just like plain objects. Array.isArray() correctly distinguishes arrays even across different execution contexts (like iframes), unlike an instanceof Array check.',null,'Array.isArray([1,2,3]); // true
typeof [1,2,3]; // ''object''','javascript','easy',ARRAY['javascript','basics','arrays']::text[],false,131),
('javascript','basics','javascript-basics-132','What is the difference between undeclared, undefined, and uninitialized variables?','An undeclared variable was never declared with var/let/const and referencing it throws a ReferenceError (or creates an implicit global in non-strict mode). A declared-but-unassigned variable holds the value undefined. A let/const variable that has been declared but not yet reached in execution order is ''uninitialized'' and sits in the temporal dead zone, throwing a ReferenceError if accessed.',null,null,null,'hard',ARRAY['javascript','basics','scope']::text[],false,132),
('javascript','basics','javascript-basics-133','What is the difference between mutable and immutable operations on arrays?','Mutable array methods like push, pop, splice, and sort modify the original array in place. Immutable operations like map, filter, concat, and the spread operator return a new array, leaving the original untouched — this distinction matters a lot in frameworks like React that rely on reference changes to detect updates.',null,'const sorted = [...arr].sort(); // doesn''t mutate arr','javascript','medium',ARRAY['javascript','basics','arrays']::text[],false,133),
('javascript','basics','javascript-basics-134','What is an accessor property versus a data property in a JavaScript object?','A data property simply stores a value directly. An accessor property is defined with get and/or set functions that compute a value on read or intercept an assignment on write, rather than storing the value directly — both are configured via Object.defineProperty() or object literal getter/setter syntax.',null,null,null,'hard',ARRAY['javascript','basics','objects']::text[],false,134),
('javascript','basics','javascript-basics-135','What is the difference between synchronous and asynchronous code execution in JavaScript at a basic level?','Synchronous code runs line by line, blocking further execution until the current operation completes. Asynchronous code (like setTimeout, fetch, or promises) is scheduled to run later without blocking the main thread, allowing the program to continue executing other code while waiting for the operation to finish.',null,null,null,'easy',ARRAY['javascript','basics','async']::text[],false,135),
('javascript','basics','javascript-basics-136','What does Number.isInteger() check, and how does it differ from Number.isSafeInteger()?','Number.isInteger() returns true if a value is a number with no fractional component, such as 5 or 5.0. Number.isSafeInteger() additionally checks that the value falls within the safe integer range (±(2^53 - 1)) where integers can be represented exactly without precision loss.',null,'Number.isInteger(5.0); // true
Number.isSafeInteger(2 ** 53); // false','javascript','medium',ARRAY['javascript','basics','numbers']::text[],false,136),
('javascript','basics','javascript-basics-137','What is the difference between == null and === null for checking null or undefined?','Because == performs type coercion and null == undefined evaluates to true (while null and undefined are not loosely equal to anything else), `value == null` is a common, concise idiom to check for both null and undefined at once. `value === null` only matches null exactly, requiring a separate check for undefined.',null,'null == undefined; // true
null === undefined; // false','javascript','medium',ARRAY['javascript','basics','operators']::text[],false,137),
('javascript','basics','javascript-basics-138','What is the difference between a shallow and a deep freeze of an object?','Object.freeze() only freezes the top-level properties of an object — nested objects remain mutable. A deep freeze recursively calls Object.freeze() on every nested object as well, which requires a custom recursive helper function since JavaScript has no built-in deepFreeze.',null,'function deepFreeze(obj) {
  Object.values(obj).forEach((v) => typeof v === ''object'' && v && deepFreeze(v));
  return Object.freeze(obj);
}','javascript','hard',ARRAY['javascript','basics','objects']::text[],false,138),
('javascript','basics','javascript-basics-139','What is the difference between Number.parseFloat and Number.parseInt?','parseInt parses a string and returns an integer, stopping at the first non-numeric character, and accepts an optional radix argument (base 2-36). parseFloat parses and returns a floating-point number, including decimal points, but doesn''t support a radix argument.',null,'parseInt(''3.14'', 10); // 3
parseFloat(''3.14''); // 3.14','javascript','easy',ARRAY['javascript','basics','numbers']::text[],false,139),
('javascript','basics','javascript-basics-140','Why should you always specify a radix when using parseInt?','Without an explicit radix, parseInt guesses the base from the string''s format, and historically strings starting with ''0'' were sometimes interpreted as octal in older engines, leading to unexpected results. Always passing 10 (or the intended base) avoids this ambiguity and makes the intent explicit.',null,'parseInt(''08''); // 8 in modern engines, but explicit radix avoids any doubt
parseInt(''08'', 10); // 8','javascript','medium',ARRAY['javascript','basics','numbers']::text[],false,140),
('javascript','basics','javascript-basics-141','What is the difference between a wrapper object and a primitive value in JavaScript?','Primitives like strings and numbers aren''t objects and have no methods of their own, but JavaScript automatically wraps them in a temporary wrapper object (like String or Number) whenever you call a method on them, such as ''hello''.toUpperCase(). The wrapper object is discarded immediately after the method call completes.',null,'typeof ''hi''; // ''string''
typeof new String(''hi''); // ''object''','javascript','hard',ARRAY['javascript','basics','types']::text[],false,141),
('javascript','basics','javascript-basics-142','What is the difference between let redeclaration errors and var redeclaration behavior?','Declaring the same variable name twice with var in the same scope is allowed and simply overwrites the previous binding without error. Doing the same with let or const throws a SyntaxError ("already declared"), because block-scoped declarations are stricter about preventing accidental redeclaration.',null,'var x = 1; var x = 2; // fine
let y = 1; let y = 2; // SyntaxError','javascript','medium',ARRAY['javascript','basics','scope']::text[],false,142),
('javascript','basics','javascript-basics-143','What is the difference between deep equality and reference equality for objects?','Reference equality (===) checks whether two variables point to the exact same object in memory. Deep equality checks whether two objects have the same structure and values, even if they are different objects — JavaScript has no built-in deep equality operator, so it requires a recursive comparison function or a library like lodash''s isEqual.',null,'{ a: 1 } === { a: 1 }; // false, different objects','javascript','medium',ARRAY['javascript','basics','objects']::text[],false,143),
('javascript','basics','javascript-basics-144','What is the with statement in JavaScript and why is it discouraged?','The with statement extends the scope chain for a block, letting you reference an object''s properties without prefixing them, but it makes code ambiguous and hard to optimize since the engine can''t statically determine whether an identifier refers to a variable or an object property. It''s forbidden entirely in strict mode.',null,'with (Math) { console.log(PI); } // avoid this','javascript','hard',ARRAY['javascript','basics']::text[],false,144),
('javascript','basics','javascript-basics-145','What is the difference between Number.MAX_SAFE_INTEGER and Number.MAX_VALUE?','Number.MAX_SAFE_INTEGER (2^53 - 1) is the largest integer that can be represented exactly without losing precision. Number.MAX_VALUE is the largest finite number JavaScript can represent at all, but numbers beyond MAX_SAFE_INTEGER lose integer precision even though they''re still within MAX_VALUE.',null,null,null,'hard',ARRAY['javascript','basics','numbers']::text[],false,145),
('javascript','basics','javascript-basics-146','How does JavaScript determine equality when using the includes() method on an array versus indexOf()?','Array.prototype.includes() uses the SameValueZero algorithm, which treats NaN as equal to itself, so it can correctly find NaN in an array. Array.prototype.indexOf() uses strict equality (===), so it can never find NaN because NaN === NaN is false.',null,'[NaN].includes(NaN); // true
[NaN].indexOf(NaN); // -1','javascript','hard',ARRAY['javascript','basics','arrays']::text[],false,146),
('javascript','basics','javascript-basics-147','What is destructuring with default values in JavaScript?','When destructuring an object or array, you can provide a default value for a property that assigns only if the extracted value is strictly undefined (not just falsy), which is useful for handling optional function parameters or config objects.',null,'const { retries = 3 } = options;','javascript','easy',ARRAY['javascript','basics','es6']::text[],false,147),
('javascript','basics','javascript-basics-148','What is the difference between an expression and a statement in JavaScript?','An expression produces a value and can be used wherever a value is expected, such as `2 + 2` or a function call. A statement performs an action and does not itself produce a usable value, such as an if block or a for loop — though some constructs, like function expressions, blur this line depending on context.',null,null,null,'medium',ARRAY['javascript','basics']::text[],false,148),
('javascript','basics','javascript-basics-149','What is variable shadowing in JavaScript?','Variable shadowing occurs when a variable declared in an inner scope has the same name as a variable in an outer scope, temporarily ''hiding'' the outer variable for the duration of the inner scope. It''s legal with let/const in nested blocks but can make code confusing if used carelessly.',null,'let x = 1;
{
  let x = 2; // shadows outer x
  console.log(x); // 2
}
console.log(x); // 1','javascript','medium',ARRAY['javascript','basics','scope']::text[],false,149),
('javascript','dom','javascript-dom-101','What is the difference between querySelector/querySelectorAll and getElementById/getElementsByClassName?','`querySelector`/`querySelectorAll` accept any CSS selector and give you far more flexibility (attribute selectors, combinators, pseudo-classes), while `getElementById`/`getElementsByClassName` only match by a single id or class name. The older methods also return live collections (that auto-update as the DOM changes) for `getElementsByClassName`, whereas `querySelectorAll` returns a static NodeList snapshot.',null,'document.querySelectorAll(''.item:not(.disabled)'');
document.getElementById(''main'');','javascript','easy',ARRAY['javascript','dom']::text[],false,101),
('javascript','dom','javascript-dom-102','What is the difference between a DOM node and a DOM element?','A node is the general base type in the DOM tree, covering elements, text, comments, and the document itself. An element is specifically a tag-based node (like `<div>` or `<p>`) — every element is a node, but not every node is an element, which is why APIs like `childNodes` (all nodes) and `children` (elements only) behave differently.',null,null,null,'easy',ARRAY['javascript','dom']::text[],false,102),
('javascript','dom','javascript-dom-103','How do you create and insert a new element into the DOM?','`document.createElement(tagName)` creates a detached element, which you then configure (set attributes, text, children) before attaching it to the tree with a method like `appendChild()`, `append()`, or `insertBefore()`. Nothing is visible on the page until the element is actually attached to a node that''s part of the document.',null,'const li = document.createElement(''li'');
li.textContent = ''New item'';
document.querySelector(''ul'').appendChild(li);','javascript','easy',ARRAY['javascript','dom']::text[],false,103),
('javascript','dom','javascript-dom-104','What do insertBefore() and replaceChild() do?','`parent.insertBefore(newNode, referenceNode)` inserts `newNode` into `parent` immediately before `referenceNode` (or at the end if `referenceNode` is `null`). `parent.replaceChild(newNode, oldNode)` swaps `oldNode` out for `newNode` in a single operation, removing the old node from the tree in the process.',null,'list.insertBefore(newItem, list.firstChild);
list.replaceChild(newItem, oldItem);','javascript','easy',ARRAY['javascript','dom']::text[],false,104),
('javascript','dom','javascript-dom-105','What is the classList API used for?','`element.classList` exposes a DOMTokenList with methods like `add()`, `remove()`, `toggle()`, `replace()`, and `contains()` for manipulating an element''s CSS classes individually, without needing to parse or rebuild the whole `className` string manually. `toggle()` is especially handy for show/hide or active-state UI patterns.',null,'el.classList.toggle(''active'');
el.classList.contains(''active''); // true/false','javascript','easy',ARRAY['javascript','dom']::text[],false,105),
('javascript','dom','javascript-dom-106','What is the dataset API and when would you use it?','`element.dataset` exposes an element''s `data-*` HTML attributes as a plain object with camelCase keys — `data-user-id` becomes `dataset.userId`. It''s a clean, standards-based way to attach custom metadata to DOM elements without inventing your own attribute scheme or relying on non-standard attributes.','<div data-user-id="42"></div>','el.dataset.userId; // ''42''
el.dataset.userId = ''43'';','javascript','easy',ARRAY['javascript','dom']::text[],false,106),
('javascript','dom','javascript-dom-107','What is the difference between event.target and event.currentTarget?','`event.target` is the actual element that triggered the event (e.g. the specific button clicked), while `event.currentTarget` is the element the listener is currently attached to and executing on. They differ whenever an event bubbles — a listener on a parent `<ul>` sees `target` as the clicked `<li>` but `currentTarget` as the `<ul>` itself.',null,'list.addEventListener(''click'', (e) => {
  console.log(e.target);        // the clicked <li>
  console.log(e.currentTarget); // the <ul>
});','javascript','medium',ARRAY['javascript','dom','events']::text[],true,107),
('javascript','dom','javascript-dom-108','How do you create and dispatch a custom event?','`new CustomEvent(name, { detail, bubbles })` creates an event carrying arbitrary payload data in `detail`, and `element.dispatchEvent(event)` fires it on that element as if it were a native event, triggering any matching listeners including delegated ones if `bubbles: true` is set. This is the standard way for independent pieces of UI code to communicate through the DOM without direct references to each other.',null,'const event = new CustomEvent(''item-added'', { detail: { id: 42 }, bubbles: true });
el.dispatchEvent(event);
document.addEventListener(''item-added'', (e) => console.log(e.detail.id));','javascript','medium',ARRAY['javascript','dom','events']::text[],false,108),
('javascript','dom','javascript-dom-109','What does the capture option in addEventListener do?','By default, `addEventListener` attaches a listener for the bubbling phase, running after the event has reached its target. Passing `{ capture: true }` (or just `true`) attaches it for the capturing phase instead, so it runs on the way down from the document root to the target, before any bubbling-phase listeners fire.',null,'el.addEventListener(''click'', handler, { capture: true });','javascript','medium',ARRAY['javascript','dom','events']::text[],false,109),
('javascript','dom','javascript-dom-110','What does the once option in addEventListener do?','`{ once: true }` makes the browser automatically remove the listener right after it fires for the first time, saving you from manually calling `removeEventListener` inside the handler. It''s a concise way to handle events you only care about a single occurrence of, like a one-time animation-end callback.','el.addEventListener(''animationend'', handler, { once: true });',null,null,'easy',ARRAY['javascript','dom','events']::text[],false,110),
('javascript','dom','javascript-dom-111','What does the passive option in addEventListener do, and why does it matter for scrolling?','`{ passive: true }` tells the browser the listener will never call `preventDefault()`, allowing the browser to start scrolling immediately instead of waiting to see if the handler cancels it. This is a meaningful performance improvement for high-frequency events like `touchstart`, `touchmove`, and `wheel`, which is why many browsers now default touch listeners to passive.',null,'window.addEventListener(''touchmove'', handler, { passive: true });','javascript','medium',ARRAY['javascript','dom','events','performance']::text[],false,111),
('javascript','dom','javascript-dom-112','Why must you keep a reference to a handler function to remove it later?','`removeEventListener` matches listeners by reference equality of the function (and the same capture/options), so passing a new inline arrow function to `removeEventListener` will never remove a listener added with a different (even identical-looking) function. To be able to remove a listener later, you must store the exact same function reference that was passed to `addEventListener`.',null,'const handler = () => console.log(''click'');
el.addEventListener(''click'', handler);
el.removeEventListener(''click'', handler); // works
// el.removeEventListener(''click'', () => console.log(''click'')); // does NOT work','javascript','medium',ARRAY['javascript','dom','events']::text[],false,112),
('javascript','dom','javascript-dom-113','What is the difference between the load and DOMContentLoaded events?','`DOMContentLoaded` fires once the HTML has been fully parsed and the DOM tree is built, without waiting for stylesheets, images, or other subresources to finish loading. `load` fires later, once the entire page — including all images, styles, and iframes — has fully loaded, so DOM-manipulation code usually only needs to wait for `DOMContentLoaded`.',null,null,null,'easy',ARRAY['javascript','dom','events']::text[],false,113),
('javascript','dom','javascript-dom-114','What is a MutationObserver used for?','`MutationObserver` lets you asynchronously watch a DOM subtree for changes — added/removed nodes, attribute changes, or text content changes — and run a callback with a batch of mutation records, instead of polling the DOM. It''s commonly used by libraries that need to react to DOM changes made by other code they don''t control, like browser extensions or rich text editors.',null,'const observer = new MutationObserver((mutations) => console.log(mutations));
observer.observe(document.body, { childList: true, subtree: true });','javascript','medium',ARRAY['javascript','dom','observers']::text[],false,114),
('javascript','dom','javascript-dom-115','How does IntersectionObserver help implement lazy loading?','`IntersectionObserver` asynchronously notifies you when an observed element enters or exits the browser''s viewport (or another ancestor element), without the performance cost of manually checking `getBoundingClientRect()` on every scroll event. Lazy-loading images use it to only set an `<img>`''s real `src` once the placeholder scrolls into view, deferring the network request until it''s actually needed.',null,'const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.src = entry.target.dataset.src;
      observer.unobserve(entry.target);
    }
  });
});
images.forEach(img => observer.observe(img));','javascript','medium',ARRAY['javascript','dom','performance','observers']::text[],true,115),
('javascript','dom','javascript-dom-116','What does ResizeObserver do?','`ResizeObserver` notifies you when an observed element''s content box (or border box) size changes, letting you react to layout changes caused by CSS, font loading, or a responsive container — without the inaccuracy and performance cost of listening to the window''s `resize` event and re-measuring elements manually.',null,'const ro = new ResizeObserver(entries => {
  entries.forEach(e => console.log(e.contentRect.width));
});
ro.observe(el);','javascript','medium',ARRAY['javascript','dom','observers']::text[],false,116),
('javascript','dom','javascript-dom-117','How does the browser''s built-in constraint validation API work on forms?','Form controls expose validity state through properties like `required`, `pattern`, and `min`/`max`, and the browser evaluates them automatically; `input.checkValidity()` returns whether the element currently satisfies its constraints, and `input.validity` gives a detailed `ValidityState` object (e.g. `valueMissing`, `patternMismatch`). `input.setCustomValidity(message)` lets you add custom validation logic that plugs into the same native error-reporting UI.',null,'if (!emailInput.checkValidity()) {
  console.log(emailInput.validationMessage);
}','javascript','medium',ARRAY['javascript','dom','forms']::text[],false,117),
('javascript','dom','javascript-dom-118','What is the FormData API used for?','`new FormData(formElement)` reads all the named fields of a form (including file inputs) into a key-value structure that can be sent directly as a `multipart/form-data` request body via `fetch`, without manually serializing each field. It supports the same `get`/`set`/`append`/`entries` style API as `Map` and `URLSearchParams`.',null,'const formData = new FormData(document.querySelector(''form''));
fetch(''/submit'', { method: ''POST'', body: formData });','javascript','easy',ARRAY['javascript','dom','forms']::text[],false,118),
('javascript','dom','javascript-dom-119','What is the difference between localStorage, sessionStorage, and cookies?','`localStorage` persists data with no expiration until explicitly cleared and is scoped per-origin across all tabs/windows. `sessionStorage` is scoped to a single tab and cleared when that tab closes. Cookies are much smaller (~4KB), are automatically sent with every matching HTTP request (adding overhead), and support server-set expiration and `httpOnly`/`secure` flags that Web Storage APIs don''t have.',null,null,null,'easy',ARRAY['javascript','dom','storage']::text[],true,119),
('javascript','dom','javascript-dom-120','How does the History API enable client-side routing in a single-page app?','`history.pushState(state, '''', url)` changes the URL shown in the address bar and adds a new entry to the browser''s session history, without triggering a full page reload, and `history.replaceState()` does the same but replaces the current entry instead of adding one. Listening for the `popstate` event lets the app re-render the correct view when the user navigates with the browser''s back/forward buttons.',null,'history.pushState({ page: ''profile'' }, '''', ''/profile'');
window.addEventListener(''popstate'', (e) => render(e.state));','javascript','medium',ARRAY['javascript','dom','routing']::text[],false,120),
('javascript','dom','javascript-dom-121','What is the same-origin policy and how does CORS relate to it?','The same-origin policy is the browser''s default restriction preventing a script from one origin (scheme + host + port) from reading responses from a different origin, to protect against malicious cross-site data access. CORS is the mechanism that lets a server explicitly opt in to allowing specific cross-origin requests, via response headers like `Access-Control-Allow-Origin`, which the browser checks before exposing the response to the calling script.',null,null,null,'medium',ARRAY['javascript','dom','security']::text[],false,121),
('javascript','dom','javascript-dom-122','What does a basic fetch() request look like, and how does it handle errors?','`fetch(url)` returns a Promise that resolves with a Response object once headers arrive — you then call `.json()` or `.text()` to read the body, which returns another Promise. Critically, `fetch()` only rejects on network failure, not on HTTP error status codes like 404 or 500, so you must check `response.ok` (or `response.status`) yourself and throw manually if the request failed at the HTTP level.',null,'const res = await fetch(''/api/users'');
if (!res.ok) throw new Error(`HTTP ${res.status}`);
const users = await res.json();','javascript','easy',ARRAY['javascript','dom','fetch']::text[],true,122),
('javascript','dom','javascript-dom-123','What is the difference between reflow and repaint in the browser?','A reflow (layout) recalculates the geometry — position and size — of elements affected by a change, which is expensive because it can cascade to the whole page. A repaint just redraws pixels for elements whose appearance changed (like color) without affecting layout, which is cheaper. Reading layout properties like `offsetHeight` right after writing a style change forces a synchronous reflow ("layout thrashing"), which is a common performance trap.',null,null,null,'hard',ARRAY['javascript','dom','performance']::text[],true,123),
('javascript','dom','javascript-dom-124','What is the critical rendering path?','The critical rendering path is the sequence of steps the browser takes to convert HTML, CSS, and JavaScript into pixels on screen: parsing HTML into the DOM, parsing CSS into the CSSOM, combining them into a render tree, computing layout, and finally painting. Optimizing it (minimizing render-blocking CSS/JS, reducing DOM depth) is central to improving perceived page-load performance.',null,null,null,'hard',ARRAY['javascript','dom','performance']::text[],false,124),
('javascript','dom','javascript-dom-125','When would you use requestAnimationFrame instead of setTimeout for animations?','`requestAnimationFrame` schedules a callback to run right before the browser''s next repaint, syncing your updates to the display''s actual refresh rate and automatically pausing in background tabs to save resources. `setTimeout`-based animation has no such synchronization, so it can cause visible jank (frames rendered at the wrong time) and keeps running (wasting CPU/battery) even when the tab isn''t visible.',null,'function tick() {
  updatePosition();
  requestAnimationFrame(tick);
}
requestAnimationFrame(tick);','javascript','medium',ARRAY['javascript','dom','performance','animation']::text[],false,125),
('javascript','dom','javascript-dom-126','What is requestIdleCallback used for?','`requestIdleCallback` schedules a callback to run during a browser''s idle periods, after more urgent work (rendering, user input) has been handled, and passes a deadline object so the callback can yield control if it''s running out of idle time. It''s useful for low-priority background work like analytics batching or prefetching, but shouldn''t be used for anything visually important since it offers no timing guarantees.',null,null,null,'medium',ARRAY['javascript','dom','performance']::text[],false,126),
('javascript','dom','javascript-dom-127','What are the core events involved in implementing native drag-and-drop?','The Drag and Drop API fires `dragstart` on the dragged element, `dragover` on potential drop targets (which must call `preventDefault()` to actually allow a drop), and `drop` on the target when the item is released — with `dataTransfer` carrying the payload between them. `dragend` fires on the source element once the operation finishes, regardless of whether the drop succeeded.',null,'el.addEventListener(''dragstart'', (e) => e.dataTransfer.setData(''text/plain'', el.id));
target.addEventListener(''dragover'', (e) => e.preventDefault());
target.addEventListener(''drop'', (e) => console.log(e.dataTransfer.getData(''text/plain'')));','javascript','medium',ARRAY['javascript','dom','drag-and-drop']::text[],false,127),
('javascript','dom','javascript-dom-128','How do you read from and write to the system clipboard in JavaScript?','The async Clipboard API (`navigator.clipboard.writeText()` / `.readText()`) lets you programmatically copy or read text, but it requires a secure context (HTTPS) and, for reading, often requires explicit user permission or a user-initiated event. The older `document.execCommand(''copy'')` approach still works in many browsers as a fallback but is deprecated.',null,'await navigator.clipboard.writeText(''Copied text'');','javascript','easy',ARRAY['javascript','dom','clipboard']::text[],false,128),
('javascript','dom','javascript-dom-129','What does getBoundingClientRect() return and what is it used for?','`element.getBoundingClientRect()` returns the element''s size and position relative to the viewport (`top`, `left`, `width`, `height`, etc.), reflecting its current rendered layout including any CSS transforms. It''s commonly used for things like custom tooltip positioning, drag-and-drop hit testing, or manual scroll-based visibility checks.','const rect = el.getBoundingClientRect();
const isVisible = rect.top < window.innerHeight && rect.bottom > 0;',null,null,'easy',ARRAY['javascript','dom']::text[],false,129),
('javascript','dom','javascript-dom-130','How can JavaScript respond to responsive CSS breakpoints using matchMedia?','`window.matchMedia(query)` evaluates a CSS media query and returns a `MediaQueryList` with a `.matches` boolean, and calling `.addEventListener(''change'', callback)` on it lets your JavaScript react whenever the viewport crosses that breakpoint — without polling `window.innerWidth` on every resize event.',null,'const mq = window.matchMedia(''(max-width: 768px)'');
mq.addEventListener(''change'', (e) => console.log(e.matches ? ''mobile'' : ''desktop''));','javascript','easy',ARRAY['javascript','dom','responsive']::text[],false,130),
('javascript','dom','javascript-dom-131','What is the Shadow DOM and what problem does it solve?','The Shadow DOM lets an element host a separate, encapsulated DOM subtree (a "shadow root") whose internal structure and styles don''t leak out to, or get affected by, the rest of the page — CSS selectors from outside can''t accidentally style inside the shadow tree, and vice versa. It''s the mechanism native form controls and Web Components use to keep their internal implementation details isolated from the surrounding page.',null,'const shadow = el.attachShadow({ mode: ''open'' });
shadow.innerHTML = ''<style>p { color: red; }</style><p>Isolated</p>'';','javascript','hard',ARRAY['javascript','dom','web-components']::text[],false,131),
('javascript','dom','javascript-dom-132','What is a custom element and how do you define one?','A custom element is a user-defined HTML tag registered with `customElements.define(name, class)`, where the class extends `HTMLElement` and can hook into its lifecycle via callbacks like `connectedCallback()` (mounted) and `attributeChangedCallback()` (an observed attribute changed). Combined with the Shadow DOM and `<template>`, this is the foundation of the Web Components standard for building framework-agnostic reusable UI.',null,'class MyBadge extends HTMLElement {
  connectedCallback() { this.textContent = ''Badge''; }
}
customElements.define(''my-badge'', MyBadge);','javascript','hard',ARRAY['javascript','dom','web-components']::text[],false,132),
('javascript','dom','javascript-dom-133','What are some key ARIA attributes used to make dynamic DOM updates accessible?','`aria-live` (e.g. `"polite"`) marks a region so screen readers announce content changes inside it automatically, `aria-expanded`/`aria-hidden` communicate toggled states like accordions or menus, and `aria-label`/`aria-labelledby` provide accessible names for elements that don''t have visible text. These matter most for custom interactive widgets, since native elements like `<button>` already carry correct semantics automatically.',null,null,null,'medium',ARRAY['javascript','dom','accessibility']::text[],false,133),
('javascript','dom','javascript-dom-134','What is focus management and why does it matter for modal dialogs?','Focus management means deliberately controlling which element has keyboard focus as the UI changes — for a modal, that means moving focus into the dialog when it opens, trapping Tab/Shift+Tab within it so focus can''t escape to the page behind it, and returning focus to the triggering element when it closes. Without this, keyboard and screen-reader users lose track of where they are and can interact with content hidden behind the modal.',null,null,null,'medium',ARRAY['javascript','dom','accessibility']::text[],false,134),
('javascript','dom','javascript-dom-135','What is the difference between touch events and pointer events?','Touch events (`touchstart`, `touchmove`, `touchend`) only fire for touch input and can report multiple simultaneous touch points. Pointer events (`pointerdown`, `pointermove`, `pointerup`) unify mouse, touch, and stylus input behind a single event model with a `pointerType` property, letting you write one handler that works correctly across input types instead of separately handling mouse and touch events.',null,null,null,'medium',ARRAY['javascript','dom','events']::text[],false,135),
('javascript','dom','javascript-dom-136','What is the difference between the window and document objects?','`window` represents the browser tab/window itself — the global execution context, providing things like `location`, `history`, `localStorage`, timers, and the global scope. `document` represents the loaded HTML page''s DOM tree specifically, and is actually a property of `window` (`window.document`), giving access to elements, `querySelector`, and page-level metadata.',null,null,null,'easy',ARRAY['javascript','dom']::text[],false,136),
('javascript','dom','javascript-dom-137','How can detached DOM nodes cause a memory leak?','A DOM node is "detached" once it''s removed from the document, but it can''t actually be garbage collected as long as something still references it — most commonly a JavaScript variable, closure, or an event listener that was never cleaned up. Long-lived single-page apps that repeatedly create and remove elements without releasing these references can slowly accumulate detached nodes in memory over time.',null,null,null,'hard',ARRAY['javascript','dom','memory']::text[],false,137),
('javascript','dom','javascript-dom-138','Why is it important to remove event listeners when an element is removed?','An event listener attached to an element (or to a stable target like `window`/`document` referencing that element in its closure) keeps that element''s scope alive, which can prevent both the element and anything it closes over from being garbage collected even after the element leaves the DOM. Explicitly calling `removeEventListener` (or using `{ signal }` with an AbortController to remove many listeners at once) when a component/element is torn down avoids this class of leak.',null,'const controller = new AbortController();
el.addEventListener(''click'', handler, { signal: controller.signal });
// later, removes all listeners registered with this signal:
controller.abort();','javascript','medium',ARRAY['javascript','dom','memory','events']::text[],false,138),
('javascript','dom','javascript-dom-139','What is the storage event used for?','The `storage` event fires on `window` in other tabs/windows of the same origin whenever `localStorage` is modified — notably, it does not fire in the tab that made the change. This makes it a simple way to synchronize state (like a logged-out session or a theme toggle) across multiple open tabs without any server round-trip.',null,'window.addEventListener(''storage'', (e) => {
  if (e.key === ''theme'') applyTheme(e.newValue);
});','javascript','medium',ARRAY['javascript','dom','storage']::text[],false,139),
('javascript','dom','javascript-dom-140','How do you programmatically enter and exit fullscreen mode?','`element.requestFullscreen()` asks the browser to display that element (and its descendants) in fullscreen, returning a Promise that resolves once the transition completes, and `document.exitFullscreen()` reverses it. This must be triggered from a user gesture (like a click) — browsers block fullscreen requests made without direct user interaction.',null,'videoEl.requestFullscreen();
document.exitFullscreen();','javascript','easy',ARRAY['javascript','dom']::text[],false,140),
('javascript','dom','javascript-dom-141','How do you get the user''s current location with the Geolocation API?','`navigator.geolocation.getCurrentPosition(success, error)` asynchronously requests the device''s location, prompting the user for permission first, and invokes the success callback with a `position.coords` object containing `latitude`/`longitude`. `watchPosition()` is the continuous variant that keeps calling the callback as the device moves, useful for live tracking.',null,'navigator.geolocation.getCurrentPosition(
  (pos) => console.log(pos.coords.latitude, pos.coords.longitude),
  (err) => console.error(err)
);','javascript','easy',ARRAY['javascript','dom']::text[],false,141),
('javascript','dom','javascript-dom-142','What is the difference between a shallow and a deep clone with cloneNode()?','`node.cloneNode()` (or `cloneNode(false)`) copies only the node itself, with none of its children. `node.cloneNode(true)` performs a deep clone, copying the entire subtree of descendants along with it. Neither variant copies event listeners attached via `addEventListener` — those must be reattached manually on the clone.','const copy = card.cloneNode(true);',null,null,'easy',ARRAY['javascript','dom']::text[],false,142),
('javascript','dom','javascript-dom-143','What does the contenteditable attribute do?','Setting `contenteditable="true"` on any element makes its content directly editable by the user in the browser, essentially turning it into a rich-text editing surface without a `<textarea>`. It''s the foundation most WYSIWYG/rich-text editors are built on, though it has notoriously inconsistent behavior across browsers for things like paste handling and list formatting.','<div contenteditable="true">Edit me</div>',null,null,'easy',ARRAY['javascript','dom']::text[],false,143),
('javascript','dom','javascript-dom-144','Why is IntersectionObserver generally preferred over scroll event listeners for detecting element visibility?','Scroll-based visibility checks require calling `getBoundingClientRect()` inside a scroll handler, which runs very frequently and forces the browser to recompute layout synchronously, hurting scroll performance even when debounced or throttled. `IntersectionObserver` instead runs asynchronously off the main thread''s critical path, only invoking your callback when the intersection state actually changes, making it both more efficient and simpler to use correctly.',null,null,null,'medium',ARRAY['javascript','dom','performance']::text[],false,144),
('javascript','dom','javascript-dom-145','What is the difference between innerText and textContent?','`textContent` returns all text within an element regardless of CSS, including text inside `<script>`/hidden elements, and is fast since it doesn''t trigger layout. `innerText` is layout-aware — it respects CSS (`display: none` text is excluded, `<br>` becomes a newline) and reading it forces a reflow, making it slower but sometimes more representative of what a user actually sees rendered.',null,null,null,'medium',ARRAY['javascript','dom']::text[],false,145),
('javascript','dom','javascript-dom-146','What is the beforeunload event used for?','`beforeunload` fires just before the page is about to be unloaded (closed, refreshed, or navigated away from), and setting `event.returnValue` (or calling `preventDefault()`) triggers the browser''s native "leave site?" confirmation dialog. It''s typically used to warn users about unsaved changes, but browsers heavily restrict its behavior (no custom message text, must result from user interaction) to prevent abuse.',null,'window.addEventListener(''beforeunload'', (e) => {
  if (hasUnsavedChanges) e.preventDefault();
});','javascript','medium',ARRAY['javascript','dom','events']::text[],false,146),
('javascript','dom','javascript-dom-147','Why is assigning untrusted data to innerHTML a security risk?','`innerHTML` parses its string as real HTML, so any untrusted content containing a `<script>` tag or an event-handler attribute (like `onerror`) can execute arbitrary JavaScript in the page''s context — a classic DOM-based XSS vulnerability. Using `textContent` for plain text, or sanitizing HTML through a trusted library before insertion, avoids this risk.',null,'// Dangerous if `comment` is user-supplied:
el.innerHTML = comment;
// Safe:
el.textContent = comment;','javascript','medium',ARRAY['javascript','dom','security']::text[],true,147),
('javascript','dom','javascript-dom-148','What does Element.closest() do?','`element.closest(selector)` walks up from the element through its ancestors (including itself) and returns the first one matching the given CSS selector, or `null` if none match. It''s frequently used inside a delegated event handler to find the relevant interactive ancestor of whatever was actually clicked, e.g. `event.target.closest(''.card'')`.','const card = event.target.closest(''.card'');',null,null,'easy',ARRAY['javascript','dom']::text[],false,148),
('javascript','dom','javascript-dom-149','What does Node.contains() check for?','`node.contains(otherNode)` returns whether `otherNode` is a descendant of `node` (or is `node` itself). It''s commonly used to detect "click outside" interactions — for example, checking whether the clicked element is contained within a dropdown before deciding to close it.',null,'document.addEventListener(''click'', (e) => {
  if (!dropdown.contains(e.target)) closeDropdown();
});','javascript','easy',ARRAY['javascript','dom']::text[],false,149),
('javascript','es6-plus','javascript-es6-plus-101','What is the temporal dead zone in JavaScript?','The temporal dead zone (TDZ) is the span between entering a scope and the point where a `let` or `const` declaration is actually executed. Accessing the variable anywhere in that span throws a ReferenceError, even though the declaration is technically hoisted — unlike `var`, which is hoisted and initialized to `undefined`.',null,'console.log(x); // ReferenceError
let x = 5;','javascript','medium',ARRAY['javascript','es6','scope']::text[],false,101),
('javascript','es6-plus','javascript-es6-plus-102','How does array destructuring work in JavaScript?','Array destructuring unpacks values from an array into distinct variables based on position, using square-bracket syntax on the left-hand side of an assignment. Elements can be skipped with empty commas, and default values can be supplied for positions that are undefined.',null,'const [first, , third = ''default''] = [1, 2];
// first = 1, third = ''default''','javascript','easy',ARRAY['javascript','es6','destructuring']::text[],true,102),
('javascript','es6-plus','javascript-es6-plus-103','How do you destructure a deeply nested object?','Nested destructuring mirrors the shape of the object being unpacked, using nested braces to reach into sub-objects and optionally renaming variables along the way. If an intermediate property might be missing, a default value should be provided for it to avoid a TypeError when destructuring further.',null,'const { user: { address: { city = ''Unknown'' } = {} } = {} } = response;','javascript','medium',ARRAY['javascript','es6','destructuring']::text[],false,103),
('javascript','es6-plus','javascript-es6-plus-104','How do default values work in destructuring assignments?','A default value in a destructuring pattern is used only when the corresponding property or array element is `undefined` — not when it is `null` or any other falsy value. This makes destructuring defaults a convenient way to handle optional function arguments or optional object properties.','function greet({ name = ''Guest'' } = {}) { return `Hi ${name}`; }',null,null,'easy',ARRAY['javascript','es6','destructuring']::text[],false,104),
('javascript','es6-plus','javascript-es6-plus-105','How can you swap two variables without a temporary variable using ES6?','Array destructuring lets you swap two variables in a single expression by destructuring a new array literal containing them in reversed order — the right-hand side array is fully evaluated before assignment, so no temporary variable is needed.',null,'let a = 1, b = 2;
[a, b] = [b, a];
// a = 2, b = 1','javascript','easy',ARRAY['javascript','es6','destructuring']::text[],false,105),
('javascript','es6-plus','javascript-es6-plus-106','How does a Set differ from an array for storing unique values?','A Set automatically enforces uniqueness — adding a value that already exists is a no-op — whereas an array allows duplicates and requires manual filtering (e.g. `indexOf` or `includes`) to deduplicate. Sets also provide O(1) average `has()` lookups compared to an array''s O(n) `includes()`.',null,'const unique = [...new Set([1, 2, 2, 3])]; // [1, 2, 3]','javascript','easy',ARRAY['javascript','es6','set']::text[],false,106),
('javascript','es6-plus','javascript-es6-plus-107','What is a WeakMap and how does it differ from a Map?','A WeakMap only accepts objects as keys and holds those keys weakly, meaning an entry doesn''t prevent its key object from being garbage collected once there are no other references to it. This makes WeakMap useful for associating metadata with an object (like private data or caches) without causing memory leaks, but it isn''t iterable and has no `size` property.',null,null,null,'medium',ARRAY['javascript','es6','map','memory']::text[],false,107),
('javascript','es6-plus','javascript-es6-plus-108','What is a WeakSet used for?','A WeakSet stores a collection of objects (not primitives) weakly, so an object present only in a WeakSet can still be garbage collected. It''s commonly used to track whether an object has been ''seen'' or ''processed'' before (like marking DOM nodes) without preventing that object from being cleaned up.',null,null,null,'medium',ARRAY['javascript','es6','set','memory']::text[],false,108),
('javascript','es6-plus','javascript-es6-plus-109','What is the purpose of the Symbol type in JavaScript?','Symbol is a primitive type that produces a guaranteed-unique value each time it''s created, even if given the same description string. It''s primarily used to create object property keys that won''t collide with string keys or with properties added by other code, enabling a form of weak namespacing.','const id = Symbol(''id'');
const obj = { [id]: 123 };',null,null,'medium',ARRAY['javascript','es6','symbol']::text[],false,109),
('javascript','es6-plus','javascript-es6-plus-110','What is a well-known symbol, and what does Symbol.iterator do?','Well-known symbols are built-in Symbol values (like `Symbol.iterator`, `Symbol.asyncIterator`, `Symbol.hasInstance`) that JavaScript itself uses as extension points to customize built-in behavior. `Symbol.iterator` specifically defines the default iterator for an object, which is what `for...of` and the spread operator call to iterate over it.',null,'const range = {
  [Symbol.iterator]() {
    let i = 0;
    return { next: () => (i < 3 ? { value: i++, done: false } : { value: undefined, done: true }) };
  }
};
[...range]; // [0, 1, 2]','javascript','hard',ARRAY['javascript','es6','symbol','iterators']::text[],false,110),
('javascript','es6-plus','javascript-es6-plus-111','What is the difference between for...of and for...in?','`for...of` iterates over the values produced by an iterable''s iterator (arrays, strings, Maps, Sets), which is what you usually want. `for...in` iterates over the enumerable property keys of an object, including inherited ones, which makes it error-prone on arrays since it can pick up non-index properties and doesn''t guarantee order.',null,'for (const value of [10, 20]) { /* 10, 20 */ }
for (const key in { a: 1, b: 2 }) { /* ''a'', ''b'' */ }','javascript','easy',ARRAY['javascript','es6','loops']::text[],false,111),
('javascript','es6-plus','javascript-es6-plus-112','What makes an object iterable in JavaScript?','An object is iterable if it implements the iterable protocol by defining a `[Symbol.iterator]` method that returns an iterator — an object with a `.next()` method returning `{ value, done }` pairs. This is the mechanism that lets built-in constructs like `for...of`, spread syntax, and destructuring work uniformly across arrays, strings, Maps, and Sets.',null,null,null,'hard',ARRAY['javascript','es6','iterators']::text[],false,112),
('javascript','es6-plus','javascript-es6-plus-113','What are generator functions and what problem do they solve?','A generator function, declared with `function*`, can pause its execution at `yield` expressions and resume later, producing a sequence of values lazily over time instead of computing them all at once. This makes generators well suited for representing infinite sequences, custom iterators, and cooperative, step-by-step computations.',null,'function* counter() {
  yield 1;
  yield 2;
  yield 3;
}
const it = counter();
it.next(); // { value: 1, done: false }','javascript','medium',ARRAY['javascript','es6','generators']::text[],true,113),
('javascript','es6-plus','javascript-es6-plus-114','How can you pass a value back into a generator via next()?','Calling `.next(value)` on a generator resumes it and makes `value` become the result of the `yield` expression the generator was paused at, enabling two-way communication between the generator and its caller. This allows generators to act as coroutines that both produce and consume values across multiple resumptions.',null,'function* echo() {
  const input = yield ''ready'';
  console.log(input); // ''hello''
}
const g = echo();
g.next();
g.next(''hello'');','javascript','hard',ARRAY['javascript','es6','generators']::text[],false,114),
('javascript','es6-plus','javascript-es6-plus-115','What does yield* do inside a generator?','`yield*` delegates iteration to another iterable (often another generator), forwarding each of its yielded values as if they were yielded directly by the outer generator. It''s a concise way to compose generators without manually looping over the inner one with `for...of`.',null,'function* inner() { yield 1; yield 2; }
function* outer() {
  yield* inner();
  yield 3;
}
[...outer()]; // [1, 2, 3]','javascript','hard',ARRAY['javascript','es6','generators']::text[],false,115),
('javascript','es6-plus','javascript-es6-plus-116','How do static methods work on ES6 classes?','A method marked `static` belongs to the class itself rather than to instances, and is called directly on the class (e.g. `MyClass.method()`) instead of on an object created with `new`. Static methods are commonly used for factory functions or utility helpers that logically relate to the class but don''t need instance state.',null,'class MathUtil {
  static square(x) { return x * x; }
}
MathUtil.square(4); // 16','javascript','easy',ARRAY['javascript','es6','classes']::text[],false,116),
('javascript','es6-plus','javascript-es6-plus-117','What are private class fields in JavaScript?','Private fields, declared with a `#` prefix, are only accessible from within the class body itself — any access from outside, even from a subclass, throws a SyntaxError at parse time. This gives classes true encapsulation, unlike the older convention of prefixing a property name with an underscore, which is just a naming convention with no actual enforcement.',null,'class Counter {
  #count = 0;
  increment() { return ++this.#count; }
}
const c = new Counter();
c.increment(); // 1
c.#count; // SyntaxError','javascript','medium',ARRAY['javascript','es6','classes']::text[],false,117),
('javascript','es6-plus','javascript-es6-plus-118','What is a class expression and how does it differ from a class declaration?','A class expression defines a class as part of an expression (often assigned to a variable) and can be anonymous, whereas a class declaration uses the `class Name {}` statement form. Unlike function declarations, class declarations are not hoisted in a usable way — both forms are only usable after their definition runs, due to the same temporal dead zone rules as `let`/`const`.','const Shape = class { area() { return 0; } };',null,null,'easy',ARRAY['javascript','es6','classes']::text[],false,118),
('javascript','es6-plus','javascript-es6-plus-119','What is a static initialization block in a class?','A static initialization block (`static { ... }`) runs once when the class itself is defined, letting you perform more complex setup for static fields than a single expression allows — including try/catch and access to private static fields. It executes in the order it appears among other static members.',null,'class Config {
  static settings;
  static {
    Config.settings = loadDefaults();
  }
}','javascript','medium',ARRAY['javascript','es6','classes']::text[],false,119),
('javascript','es6-plus','javascript-es6-plus-120','Can classes have private methods, not just private fields?','Yes — prefixing a method name with `#` makes it a private method, callable only from within the class body, following the same access rules as private fields. This lets a class expose a small public API while keeping internal helper logic fully hidden from consumers.',null,'class Validator {
  #isValidFormat(v) { return /^\d+$/.test(v); }
  check(v) { return this.#isValidFormat(v); }
}','javascript','medium',ARRAY['javascript','es6','classes']::text[],false,120),
('javascript','es6-plus','javascript-es6-plus-121','What is a Proxy in JavaScript and what is it used for?','A Proxy wraps a target object and lets you intercept and customize fundamental operations on it — like property reads, writes, and deletion — through a set of trap functions defined in a handler object. It''s used for things like validation, logging, reactive frameworks (tracking reads/writes for reactivity), and default values for missing properties.',null,'const target = {};
const proxy = new Proxy(target, {
  get(obj, prop) { return prop in obj ? obj[prop] : ''N/A''; }
});
proxy.missing; // ''N/A''','javascript','hard',ARRAY['javascript','es6','proxy']::text[],true,121),
('javascript','es6-plus','javascript-es6-plus-122','What is the Reflect object and how does it relate to Proxy?','Reflect is a built-in object that exposes the same set of fundamental operations (get, set, has, deleteProperty, etc.) as static methods, mirroring the trap names used by Proxy. Inside a Proxy trap, calling the corresponding `Reflect` method is the standard way to forward an operation to the target with correct default semantics, rather than reimplementing it manually.',null,'const proxy = new Proxy(target, {
  get(obj, prop, receiver) { return Reflect.get(obj, prop, receiver); }
});','javascript','hard',ARRAY['javascript','es6','proxy','reflect']::text[],false,122),
('javascript','es6-plus','javascript-es6-plus-123','What does Array.from() do and when would you use it?','`Array.from()` creates a real array from an array-like object (something with a `length` and indexed elements, like `arguments` or a NodeList) or from any iterable (like a Set or Map). It also accepts an optional mapping function as a second argument, letting you transform elements while converting them.',null,'Array.from({ length: 3 }, (_, i) => i * 2); // [0, 2, 4]
Array.from(document.querySelectorAll(''li''));','javascript','easy',ARRAY['javascript','es6','arrays']::text[],false,123),
('javascript','es6-plus','javascript-es6-plus-124','What is the difference between Array.of() and the Array constructor?','`Array.of(7)` creates a single-element array `[7]`, while `Array(7)` creates a sparse array with `length` 7 and no actual elements — a well-known footgun of the `Array` constructor when called with a single numeric argument. `Array.of()` always treats its arguments as elements regardless of how many or what type they are.',null,'Array.of(7); // [7]
Array(7);    // [ <7 empty items> ]','javascript','easy',ARRAY['javascript','es6','arrays']::text[],false,124),
('javascript','es6-plus','javascript-es6-plus-125','What is the difference between Array.prototype.find, findIndex, and findLast?','`find()` returns the first element matching a predicate (or `undefined` if none match), `findIndex()` returns that element''s index instead (or -1), and `findLast()` (ES2023) scans from the end of the array to find the last matching element. All three short-circuit as soon as a match is found, unlike `filter()` which always scans the whole array.',null,'[1, 5, 10, 15].find(n => n > 4);      // 5
[1, 5, 10, 15].findLast(n => n > 4);  // 15','javascript','easy',ARRAY['javascript','es6','arrays']::text[],false,125),
('javascript','es6-plus','javascript-es6-plus-126','What is the difference between Array.prototype.includes() and indexOf()?','`includes()` returns a boolean indicating whether a value is present, and correctly detects `NaN` using the SameValueZero algorithm, whereas `indexOf()` uses strict equality and can never find `NaN` (since `NaN !== NaN`). `includes()` is generally preferred when you only care about presence, not position.',null,'[NaN].includes(NaN); // true
[NaN].indexOf(NaN);  // -1','javascript','easy',ARRAY['javascript','es6','arrays']::text[],false,126),
('javascript','es6-plus','javascript-es6-plus-127','What does Array.prototype.at() offer over bracket notation?','`at()` accepts negative indices to count from the end of the array (e.g. `arr.at(-1)` for the last element), something bracket notation can''t do directly since `arr[-1]` just looks up a non-existent property. It works identically to bracket access for non-negative indices.','const last = [10, 20, 30].at(-1); // 30',null,null,'easy',ARRAY['javascript','es6','arrays']::text[],false,127),
('javascript','es6-plus','javascript-es6-plus-128','What is optional chaining and what problem does it solve?','Optional chaining (`?.`) short-circuits to `undefined` instead of throwing when accessing a property, calling a method, or indexing on a value that is `null` or `undefined` along the chain. It replaces verbose manual checks like `obj && obj.a && obj.a.b` with a single concise expression, `obj?.a?.b`.',null,'const city = user?.address?.city; // undefined instead of a TypeError
user?.getName?.();','javascript','easy',ARRAY['javascript','es6','optional-chaining']::text[],true,128),
('javascript','es6-plus','javascript-es6-plus-129','How does the nullish coalescing operator (??) differ from ||?','`??` returns its right-hand operand only when the left-hand operand is `null` or `undefined`, whereas `||` returns the right-hand operand for any falsy left value, including `0`, `''''`, and `false`. This makes `??` the safer choice for defaulting a value that might legitimately be `0` or an empty string.',null,'const count = 0;
count || 10; // 10 (wrong — 0 is a valid count)
count ?? 10; // 0 (correct)','javascript','easy',ARRAY['javascript','es6','operators']::text[],true,129),
('javascript','es6-plus','javascript-es6-plus-130','What does the exponentiation operator (**) do?','The `**` operator raises the left operand to the power of the right operand, providing a more concise, readable alternative to calling `Math.pow()`. It is right-associative, so `2 ** 3 ** 2` evaluates as `2 ** (3 ** 2)`, i.e. 512.','const cube = 3 ** 3; // 27',null,null,'easy',ARRAY['javascript','es6','operators']::text[],false,130),
('javascript','es6-plus','javascript-es6-plus-131','What does Promise.allSettled() do differently from Promise.all()?','`Promise.allSettled()` waits for every promise in the input to settle — whether fulfilled or rejected — and resolves with an array of `{ status, value }` or `{ status, reason }` objects describing each outcome, never short-circuiting on a single rejection. This is useful when you need the results of all operations regardless of individual failures, unlike `Promise.all()` which rejects immediately on the first failure.',null,'const results = await Promise.allSettled([p1, p2, p3]);
results.forEach(r => console.log(r.status));','javascript','medium',ARRAY['javascript','es6','promises']::text[],false,131),
('javascript','es6-plus','javascript-es6-plus-132','What does Promise.any() do?','`Promise.any()` resolves as soon as any one of the input promises fulfills, ignoring rejections unless every promise rejects — in that case it rejects with an `AggregateError` containing all the individual errors. It''s the fulfillment-focused counterpart to `Promise.race()`, which settles on the first outcome regardless of success or failure.',null,null,null,'medium',ARRAY['javascript','es6','promises']::text[],false,132),
('javascript','es6-plus','javascript-es6-plus-133','What do the logical assignment operators &&=, ||=, and ??= do?','These operators conditionally assign based on the current value: `a ||= b` assigns `b` only if `a` is falsy, `a &&= b` assigns `b` only if `a` is truthy, and `a ??= b` assigns `b` only if `a` is `null` or `undefined`. They''re shorthand for the equivalent `if` statement or ternary, and importantly they short-circuit — the right-hand side isn''t evaluated unless the assignment will actually happen.',null,'let config = {};
config.timeout ??= 3000; // only sets if undefined/null','javascript','medium',ARRAY['javascript','es6','operators']::text[],false,133),
('javascript','es6-plus','javascript-es6-plus-134','What are numeric separators in JavaScript?','Numeric separators let you insert underscores into number literals purely for readability, with no effect on the value — for example `1_000_000` is identical to `1000000`. They''re especially useful for large numbers, hex values, and binary literals where digit grouping helps at a glance.','const billion = 1_000_000_000;',null,null,'easy',ARRAY['javascript','es6','numbers']::text[],false,134),
('javascript','es6-plus','javascript-es6-plus-135','What is top-level await and what constraint does it have?','Top-level await lets you use `await` directly in the top-level scope of an ES module, without wrapping it in an async function, which is convenient for one-time async setup like loading configuration before the rest of the module runs. It only works inside actual ES modules — using it in a regular script or a CommonJS file is a syntax error.',null,'// inside an ES module
const config = await fetch(''/config.json'').then(r => r.json());','javascript','medium',ARRAY['javascript','es6','modules','async']::text[],false,135),
('javascript','es6-plus','javascript-es6-plus-136','What is dynamic import() and how does it differ from static import?','`import()` is a function-like expression that asynchronously loads a module and returns a Promise resolving to its exports namespace object, and it can be called anywhere at runtime — including conditionally inside an `if` block. Static `import` statements, by contrast, must appear at the top level of a module and are resolved before any module code runs, which is what enables features like tree-shaking.',null,'button.addEventListener(''click'', async () => {
  const { renderChart } = await import(''./chart.js'');
  renderChart();
});','javascript','medium',ARRAY['javascript','es6','modules']::text[],false,136),
('javascript','es6-plus','javascript-es6-plus-137','What is globalThis and why was it introduced?','`globalThis` provides a standard way to access the global object regardless of environment — `window` in browsers, `self` in web workers, `global` in Node.js — removing the need for environment-specific detection code. Before it existed, writing cross-environment code that needed the global object required awkward feature detection.',null,null,null,'easy',ARRAY['javascript','es6']::text[],false,137),
('javascript','es6-plus','javascript-es6-plus-138','What are tagged template literals used for?','A tagged template literal calls a function (the ''tag'') with the template''s literal string parts and its interpolated expression values passed separately, instead of producing a plain concatenated string. This is the mechanism libraries like styled-components use to parse CSS-in-JS, and it''s also useful for automatically escaping/sanitizing interpolated values (e.g. for safe SQL or HTML construction).',null,'function highlight(strings, ...values) {
  return strings.reduce((acc, s, i) => `${acc}${s}${values[i] ? `**${values[i]}**` : ''''}`, '''');
}
highlight`Score: ${42}`; // ''Score: **42**''','javascript','hard',ARRAY['javascript','es6','template-literals']::text[],false,138),
('javascript','es6-plus','javascript-es6-plus-139','What does the Intl object provide for formatting numbers and dates?','The `Intl` namespace provides locale-aware formatting built into the JavaScript engine, without needing a library — `Intl.NumberFormat` formats numbers, currencies, and percentages, and `Intl.DateTimeFormat` formats dates and times according to a given locale''s conventions. This avoids manually implementing locale-specific separators, currency symbols, and date orderings.',null,'new Intl.NumberFormat(''en-US'', { style: ''currency'', currency: ''USD'' }).format(1234.5);
// "$1,234.50"','javascript','medium',ARRAY['javascript','es6','intl']::text[],false,139),
('javascript','es6-plus','javascript-es6-plus-140','What is BigInt and when would you need it?','BigInt is a primitive type for representing integers of arbitrary precision, created with an `n` suffix (e.g. `10n`) or `BigInt()`, used when a value exceeds `Number.MAX_SAFE_INTEGER` (2^53 - 1) and precision would otherwise be lost. BigInt values can''t be mixed with regular numbers in arithmetic without explicit conversion, and they aren''t supported by `JSON.stringify()` without a custom replacer.',null,'const big = 9007199254740993n;
typeof big; // ''bigint''','javascript','medium',ARRAY['javascript','es6','bigint']::text[],false,140),
('javascript','es6-plus','javascript-es6-plus-141','What does export * from ''./module'' do?','`export * from ''./module''` re-exports every named export from another module through the current module, without needing to import and re-export each one individually — useful for building a single "barrel" entry point that aggregates a folder''s exports. It does not forward that module''s default export; that needs a separate explicit re-export.','// index.js
export * from ''./button.js'';
export * from ''./input.js'';',null,null,'medium',ARRAY['javascript','es6','modules']::text[],false,141),
('javascript','es6-plus','javascript-es6-plus-142','What are the trade-offs between named exports and a default export?','Named exports make a module''s API explicit and enable better auto-import and refactoring tooling since each import must match an actual exported name, and they support tree-shaking cleanly since bundlers can see exactly what''s used. A default export is convenient for modules that expose one primary thing, but its imported name is arbitrary at the call site, which can lead to inconsistent naming across a codebase.',null,null,null,'medium',ARRAY['javascript','es6','modules']::text[],false,142),
('javascript','es6-plus','javascript-es6-plus-143','What happens with circular dependencies between ES modules?','ES modules can have circular imports — module A importing from B while B imports from A — because the module system uses live bindings resolved lazily rather than eagerly copying values. If module A tries to use a binding from B before B has finished initializing it, that binding may still be `undefined` at that point in execution, so code relying on circular imports needs to be careful about what it accesses at module-evaluation time versus later.',null,null,null,'hard',ARRAY['javascript','es6','modules']::text[],false,143),
('javascript','es6-plus','javascript-es6-plus-144','How does the static structure of ES modules enable tree-shaking?','Because ES module `import`/`export` declarations are static — they can''t be conditionally constructed at runtime like CommonJS''s `require()` can — a bundler can analyze the full dependency graph ahead of time and determine exactly which exports are actually used. Unused exports can then be safely stripped from the final bundle, a process known as tree-shaking, which isn''t reliably possible with CommonJS''s dynamic module system.',null,null,null,'medium',ARRAY['javascript','es6','modules','performance']::text[],false,144),
('javascript','es6-plus','javascript-es6-plus-145','What is import.meta used for?','`import.meta` is an object available inside ES modules that exposes metadata about the current module, most commonly `import.meta.url`, which gives the module''s own URL — useful for resolving relative asset paths or constructing worker URLs without hardcoding an absolute path.','const workerUrl = new URL(''./worker.js'', import.meta.url);',null,null,'medium',ARRAY['javascript','es6','modules']::text[],false,145),
('javascript','es6-plus','javascript-es6-plus-146','What is Object.groupBy() and what problem does it solve?','`Object.groupBy()` (a newer addition to the language) takes an iterable and a callback, and returns a plain object whose keys are the callback''s return values and whose values are arrays of the original items that produced each key — replacing the common manual `reduce()` pattern used to bucket items by some property. `Map.groupBy()` is the equivalent that returns a Map instead of a plain object, useful when group keys aren''t strings.',null,'Object.groupBy([1, 2, 3, 4], n => n % 2 === 0 ? ''even'' : ''odd'');
// { odd: [1, 3], even: [2, 4] }','javascript','medium',ARRAY['javascript','es6','arrays']::text[],false,146),
('javascript','es6-plus','javascript-es6-plus-147','What is a WeakRef and when would you use one?','A `WeakRef` holds a weak reference to an object, letting you access it (via `.deref()`) while it''s still alive without preventing it from being garbage collected. It''s an advanced, rarely-needed tool for building caches where entries should be automatically reclaimed under memory pressure — most application code should prefer regular references and let normal scoping manage lifetime.',null,null,null,'hard',ARRAY['javascript','es6','memory']::text[],false,147),
('javascript','es6-plus','javascript-es6-plus-148','What does a FinalizationRegistry do?','A `FinalizationRegistry` lets you register a callback to run after an object has been garbage collected, typically used alongside `WeakRef` to clean up external resources (like a native handle) associated with an object once it''s gone. Because garbage collection timing is not guaranteed or deterministic, this callback should never be relied on for correctness-critical cleanup, only for best-effort resource reclamation.',null,null,null,'hard',ARRAY['javascript','es6','memory']::text[],false,148),
('javascript','functions','javascript-functions-101','Why does the classic "closures in a loop with var" example print the same value for every iteration?','Because var is function-scoped, not block-scoped, all the callbacks created inside the loop share the same single variable binding. By the time the callbacks actually run (asynchronously), the loop has already finished and the shared variable holds its final value. Using let instead creates a new binding for each iteration, so each closure captures its own value.',null,'for (var i = 0; i < 3; i++) {
  setTimeout(() => console.log(i), 0);
}
// logs 3, 3, 3 — use let instead of var to fix','javascript','medium',ARRAY['javascript','functions','closures']::text[],true,101),
('javascript','functions','javascript-functions-102','What is a higher-order function?','A higher-order function is a function that either takes one or more functions as arguments, returns a function, or both. Array methods like map, filter, and reduce are common examples, as are function factories and decorators.',null,'const withLogging = (fn) => (...args) => {
  console.log(''calling with'', args);
  return fn(...args);
};','javascript','easy',ARRAY['javascript','functions']::text[],true,102),
('javascript','functions','javascript-functions-103','What is a pure function?','A pure function always returns the same output for the same input and produces no observable side effects, such as mutating external state, logging, or making network calls. Pure functions are easier to test, reason about, and memoize, and are a core building block of functional programming.',null,'// pure
const add = (a, b) => a + b;

// impure — depends on and mutates external state
let total = 0;
const addToTotal = (n) => (total += n);','javascript','easy',ARRAY['javascript','functions']::text[],false,103),
('javascript','functions','javascript-functions-104','What is memoization and how would you implement it?','Memoization is an optimization technique that caches the result of an expensive function call keyed by its arguments, so subsequent calls with the same arguments return the cached result instead of recomputing it. It only works correctly for pure functions, since it assumes the same input always produces the same output.',null,'function memoize(fn) {
  const cache = new Map();
  return (arg) => {
    if (cache.has(arg)) return cache.get(arg);
    const result = fn(arg);
    cache.set(arg, result);
    return result;
  };
}','javascript','medium',ARRAY['javascript','functions','performance']::text[],true,104),
('javascript','functions','javascript-functions-105','What is function composition?','Function composition is combining two or more functions to produce a new function, where the output of one function becomes the input of the next. It''s a core functional programming technique for building complex behavior from small, reusable, single-purpose functions.',null,'const compose = (...fns) => (x) => fns.reduceRight((acc, fn) => fn(acc), x);
const addThenDouble = compose((x) => x * 2, (x) => x + 1);
addThenDouble(3); // 8','javascript','medium',ARRAY['javascript','functions','functional']::text[],false,105),
('javascript','functions','javascript-functions-106','What are default parameters and what are some edge cases to be aware of?','Default parameters let a function fall back to a specified value when an argument is omitted or explicitly passed as undefined (but not null). A subtle edge case is that default values are evaluated at call time, left to right, and can reference earlier parameters in the same parameter list.',null,'function greet(name = ''Guest'', greeting = `Hello, ${name}`) {
  return greeting;
}
greet(); // ''Hello, Guest''
greet(undefined, ''Hi''); // ''Hi''','javascript','medium',ARRAY['javascript','functions','es6']::text[],false,106),
('javascript','functions','javascript-functions-107','What is the difference between rest parameters and the arguments object?','Rest parameters (...args) collect the remaining arguments into a real Array instance, so array methods like map and filter work directly on it. The arguments object is an array-like (not a real array) available in regular functions, lacks array methods, and — unlike rest params — isn''t available at all in arrow functions.',null,'function sum(...nums) {
  return nums.reduce((a, b) => a + b, 0); // works directly
}','javascript','medium',ARRAY['javascript','functions','es6']::text[],false,107),
('javascript','functions','javascript-functions-108','Why can''t arrow functions be used as object methods that need to access `this`?','Arrow functions don''t have their own `this` binding — they inherit `this` lexically from the surrounding scope at the time they''re defined, which for a top-level object literal method is usually not the object itself. Using a regular function (or method shorthand) ensures `this` refers to the object the method was called on.',null,'const counter = {
  count: 0,
  increment: () => { this.count++; } // wrong — `this` isn''t `counter`
};','javascript','hard',ARRAY['javascript','functions','this']::text[],true,108),
('javascript','functions','javascript-functions-109','What is the difference between a named function expression and an anonymous function expression?','A named function expression has an identifier that is only visible inside the function''s own body (useful for recursion or debugging stack traces), while the outer scope still refers to it via the variable it was assigned to. An anonymous function expression has no such internal name, which makes recursive self-reference and stack traces harder to read.',null,'const factorial = function fact(n) {
  return n <= 1 ? 1 : n * fact(n - 1); // ''fact'' only usable inside here
};','javascript','medium',ARRAY['javascript','functions']::text[],false,109),
('javascript','functions','javascript-functions-110','What is recursion, and what''s a risk to watch out for when using it in JavaScript?','Recursion is when a function calls itself to break a problem into smaller subproblems, with a base case that stops the recursion. The main risk is a stack overflow if the recursion is too deep or the base case is missing or unreachable, since JavaScript engines have a limited call stack size and (unlike some languages) don''t guarantee tail-call optimization in practice.',null,'function factorial(n) {
  if (n <= 1) return 1; // base case
  return n * factorial(n - 1);
}','javascript','easy',ARRAY['javascript','functions','recursion']::text[],false,110),
('javascript','functions','javascript-functions-111','What are generator functions and what does the yield keyword do?','A generator function, declared with function*, can pause and resume its execution using the yield keyword, producing a sequence of values over time instead of returning a single value at once. Calling a generator function returns an iterator, and each call to .next() resumes execution until the next yield.',null,'function* range(start, end) {
  for (let i = start; i <= end; i++) yield i;
}
[...range(1, 3)]; // [1, 2, 3]','javascript','hard',ARRAY['javascript','functions','generators']::text[],false,111),
('javascript','functions','javascript-functions-112','What is generator delegation using yield*?','yield* delegates iteration to another generator or iterable, effectively yielding all of its values one by one as though they were yielded directly from the outer generator. It''s useful for composing generators together without manually looping over the inner generator.',null,'function* inner() { yield 1; yield 2; }
function* outer() {
  yield* inner();
  yield 3;
}
[...outer()]; // [1, 2, 3]','javascript','hard',ARRAY['javascript','functions','generators']::text[],false,112),
('javascript','functions','javascript-functions-113','What is the difference between function hoisting and var hoisting?','Function declarations are hoisted completely, including their full body, so they can be called before their definition appears in the code. var declarations are hoisted but only the declaration itself, initialized to undefined — the assignment stays in place, so accessing the variable before assignment gives undefined rather than the intended value.',null,'sayHi(); // works — ''Hi''
function sayHi() { console.log(''Hi''); }

console.log(x); // undefined, not an error
var x = 5;','javascript','medium',ARRAY['javascript','functions','hoisting']::text[],false,113),
('javascript','functions','javascript-functions-114','What does Function.prototype.length return?','Function.length returns the number of parameters declared before the first one with a default value or a rest parameter — parameters after a default value, and rest parameters themselves, are not counted. It''s occasionally used by libraries to introspect how many arguments a callback expects.',null,'function f(a, b, c = 1, ...rest) {}
f.length; // 2','javascript','hard',ARRAY['javascript','functions']::text[],false,114),
('javascript','functions','javascript-functions-115','What does it mean that functions are "first-class citizens" in JavaScript?','Being first-class means functions can be treated like any other value: assigned to variables, stored in data structures, passed as arguments, and returned from other functions. This is what makes higher-order functions, callbacks, and functional programming patterns possible in JavaScript.',null,null,null,'easy',ARRAY['javascript','functions']::text[],false,115),
('javascript','functions','javascript-functions-116','What is a callback function, and what''s a downside of relying heavily on them?','A callback is a function passed as an argument to another function, to be invoked later — commonly used for asynchronous operations or customizable behavior. Deeply nesting callbacks (especially for sequential async steps) leads to "callback hell," hard-to-read pyramid-shaped code, which Promises and async/await were introduced to address.',null,null,null,'easy',ARRAY['javascript','functions','async']::text[],false,116),
('javascript','functions','javascript-functions-117','Does JavaScript support function overloading like Java does?','No — JavaScript doesn''t support true function overloading; declaring two functions with the same name simply has the second definition overwrite the first. The common workaround is to inspect the number or type of arguments inside a single function and branch behavior accordingly, or use default/rest parameters.',null,'function greet(name, formal) {
  return formal ? `Good day, ${name}` : `Hey ${name}`;
}','javascript','medium',ARRAY['javascript','functions']::text[],false,117),
('javascript','functions','javascript-functions-118','What is partial application, and how does it differ from currying?','Partial application fixes some (but not necessarily all) of a function''s arguments up front, producing a new function that takes the remaining arguments and can be called with any number of them at once. Currying specifically transforms a function into a chain of unary (single-argument) functions — currying is a specific, stricter form of partial application.',null,'const add3 = (a, b, c) => a + b + c;
const addTo5 = add3.bind(null, 5); // partial application
addTo5(2, 3); // 10','javascript','hard',ARRAY['javascript','functions','functional']::text[],false,118),
('javascript','functions','javascript-functions-119','What is new.target used for in a function?','new.target is a meta-property that is set to the constructor function itself when a function is invoked with the `new` keyword, and is undefined otherwise. It''s used inside a function to detect whether it was called as a constructor, letting you guard against a constructor function being called without `new`.',null,'function Person(name) {
  if (!new.target) throw new Error(''Person must be called with new'');
  this.name = name;
}','javascript','hard',ARRAY['javascript','functions']::text[],false,119),
('javascript','functions','javascript-functions-120','What is the difference between a constructor function and a regular function?','Any function can be used as a constructor by calling it with `new`, which creates a new object, sets its prototype, binds `this` to that new object, and returns it implicitly (unless the function explicitly returns another object). By convention, constructor functions are named with a capital letter to signal that they''re meant to be called with `new`.',null,'function Point(x, y) {
  this.x = x;
  this.y = y;
}
const p = new Point(1, 2);','javascript','medium',ARRAY['javascript','functions']::text[],false,120),
('javascript','functions','javascript-functions-121','What is the module pattern in JavaScript?','The module pattern uses an IIFE (or, in modern code, a real ES module) to create a private scope, exposing only a deliberate public API while keeping implementation details hidden. Before native ES modules existed, this was the standard way to emulate encapsulation and avoid polluting the global namespace.',null,'const counterModule = (function () {
  let count = 0;
  return {
    increment: () => ++count,
    get: () => count,
  };
})();','javascript','medium',ARRAY['javascript','functions','patterns']::text[],false,121),
('javascript','functions','javascript-functions-122','What is the difference between the Function constructor and a regular function declaration?','The Function constructor (new Function(''a'', ''b'', ''return a + b'')) creates a function from strings at runtime, similar to eval, and always executes in the global scope rather than the scope where it was created. It''s rarely used in practice because it''s slower, bypasses closures, and poses security/injection risks if used with untrusted input.',null,'const add = new Function(''a'', ''b'', ''return a + b'');
add(2, 3); // 5 — but avoid this pattern','javascript','hard',ARRAY['javascript','functions']::text[],false,122),
('javascript','functions','javascript-functions-123','What is a thunk in JavaScript?','A thunk is a function that wraps an expression or async operation to delay its evaluation until it''s actually called, deferring the work. Thunks are commonly seen in Redux middleware (redux-thunk) to delay dispatching an action until some async work completes.',null,'const thunk = () => fetch(''/api/data'').then((r) => r.json());
// nothing happens until thunk() is called','javascript','hard',ARRAY['javascript','functions','async']::text[],false,123),
('javascript','functions','javascript-functions-124','Is recursion generally faster or slower than an equivalent iterative loop in JavaScript?','Iterative loops are generally faster and more memory-efficient than recursion in JavaScript, because each recursive call adds a new frame to the call stack, and JavaScript engines don''t reliably optimize tail calls in practice despite it being part of the ES6 spec. Recursion is still preferred when it makes the code significantly clearer, such as tree traversal.',null,null,null,'medium',ARRAY['javascript','functions','performance']::text[],false,124),
('javascript','functions','javascript-functions-125','Does JavaScript actually perform tail call optimization?','Tail call optimization (reusing the current stack frame for a function call that is the very last action in a function) is part of the ES6 specification, but in practice only Safari''s JavaScriptCore implements it — V8 (Chrome, Node.js) and Firefox''s engine do not, so recursive code relying on it will still hit a stack overflow on deep recursion in most environments.',null,null,null,'hard',ARRAY['javascript','functions','performance']::text[],false,125),
('javascript','functions','javascript-functions-126','What is a variadic function?','A variadic function is one that accepts a variable number of arguments rather than a fixed count, implemented in JavaScript using rest parameters or the arguments object. Math.max() and console.log() are built-in examples of variadic functions.',null,'function sum(...numbers) {
  return numbers.reduce((a, b) => a + b, 0);
}
sum(1, 2, 3, 4); // 10','javascript','easy',ARRAY['javascript','functions']::text[],false,126),
('javascript','functions','javascript-functions-127','What is method shorthand syntax in object literals?','ES6 introduced a shorter syntax for defining methods on an object literal, omitting the `function` keyword and colon. It behaves the same as assigning a regular function expression to a property, except shorthand methods can use `super` when defined inside an object literal.',null,'const obj = {
  greet() { return ''hi''; } // shorthand, same as greet: function() {...}
};','javascript','easy',ARRAY['javascript','functions','es6']::text[],false,127),
('javascript','functions','javascript-functions-128','How is bind() used for partial application, and how does it differ from a plain wrapper function?','fn.bind(thisArg, ...presetArgs) returns a new function with `this` and some leading arguments permanently fixed, and any further arguments passed at call time are appended after the preset ones. Unlike writing a manual wrapper function, bind() is a built-in, slightly more efficient mechanism and also correctly locks in `this`, not just the arguments.',null,'function multiply(a, b) { return a * b; }
const double = multiply.bind(null, 2);
double(5); // 10','javascript','medium',ARRAY['javascript','functions','this']::text[],false,128),
('javascript','functions','javascript-functions-129','What is a self-invoking function pattern used for besides creating private scope?','Besides encapsulating private state, self-invoking (IIFE) patterns are used to run initialization code exactly once immediately on load, to avoid leaking loop variables into an async callback pre-ES6, and to create isolated contexts for library code that shouldn''t conflict with globals from other scripts on the page.',null,null,null,'medium',ARRAY['javascript','functions','patterns']::text[],false,129),
('javascript','functions','javascript-functions-130','What happens if you forget to bind `this` in a class method passed as a callback?','When a class method is passed as a standalone reference (e.g. as an event handler), it loses its connection to the instance it was defined on, so `this` inside the method becomes undefined (in strict mode/classes) when the method is later invoked. Fixing this requires binding the method in the constructor, using an arrow function class field, or binding at the call site.',null,'class Button {
  constructor() { this.label = ''Click''; }
  handleClick() { console.log(this.label); }
}
const b = new Button();
const fn = b.handleClick;
fn(); // TypeError: Cannot read properties of undefined','javascript','hard',ARRAY['javascript','functions','this']::text[],true,130),
('javascript','functions','javascript-functions-131','What is an arrow function class field, and how does it solve the `this` binding problem?','An arrow function assigned as a class field (e.g. `handleClick = () => {...}`) captures `this` lexically from the constructor at the time the instance is created, so it''s always bound to that instance regardless of how the method is later called or passed around. This avoids needing to manually bind the method in the constructor.',null,'class Button {
  label = ''Click'';
  handleClick = () => console.log(this.label); // always bound to instance
}','javascript','medium',ARRAY['javascript','functions','this']::text[],false,131),
('javascript','functions','javascript-functions-132','What is the difference between eager and lazy evaluation, and how do generators enable laziness in JavaScript?','Eager evaluation computes a value immediately when an expression is encountered; lazy evaluation defers computation until the value is actually needed. Generators enable lazy evaluation because each `yield` pauses execution, so values are only produced on demand as the consumer calls .next(), which is useful for infinite sequences or expensive computations.',null,'function* naturals() {
  let n = 1;
  while (true) yield n++;
}
const it = naturals();
it.next().value; // 1 — nothing else computed yet','javascript','hard',ARRAY['javascript','functions','generators']::text[],false,132),
('javascript','functions','javascript-functions-133','What does it mean for a function to be idempotent, and how does that differ from being pure?','An idempotent function produces the same result and end state no matter how many times it''s called with the same input (common in HTTP PUT/DELETE semantics), but it may still have side effects — repeating them just doesn''t change the outcome further. A pure function additionally must have no side effects at all, which is a stricter requirement than idempotency.',null,null,null,'hard',ARRAY['javascript','functions']::text[],false,133),
('javascript','functions','javascript-functions-134','How can you implement a simple once() function that ensures a callback only runs a single time?','A once() wrapper uses a closure to keep track of whether the wrapped function has already been called, and simply returns the cached result (or does nothing) on subsequent calls, delegating to the original function only the first time.',null,'function once(fn) {
  let called = false, result;
  return (...args) => {
    if (!called) { called = true; result = fn(...args); }
    return result;
  };
}','javascript','medium',ARRAY['javascript','functions','closures']::text[],false,134),
('javascript','functions','javascript-functions-135','What is the difference between a callback-based API and a Promise-based API from a function design perspective?','A callback-based API accepts a function argument that the underlying operation invokes when it completes (often with an error-first signature), giving the caller no direct return value to work with. A Promise-based API instead returns a Promise object immediately, which the caller can chain with .then()/.catch() or await, making composition and error handling far more consistent.',null,null,null,'medium',ARRAY['javascript','functions','async']::text[],false,135),
('javascript','functions','javascript-functions-136','How do you write a curried function manually without a library?','A manually curried function returns a chain of single-argument functions until all expected arguments have been collected, at which point it invokes the original function with all of them. A generic curry() helper can do this for any function by checking fn.length against the accumulated arguments so far.',null,'function curry(fn) {
  return function curried(...args) {
    if (args.length >= fn.length) return fn(...args);
    return (...more) => curried(...args, ...more);
  };
}','javascript','hard',ARRAY['javascript','functions','functional']::text[],false,136),
('javascript','functions','javascript-functions-137','What is the difference between passing a function reference and invoking a function when assigning an event handler?','Passing a function reference (onClick={handleClick}) hands the function itself to be called later by the framework/DOM API. Invoking it immediately (onClick={handleClick()}) calls the function right away during render/setup and assigns whatever it *returns* as the handler, which is almost never the intended behavior.',null,'button.addEventListener(''click'', handleClick); // correct — passes reference
button.addEventListener(''click'', handleClick()); // wrong — calls immediately','javascript','easy',ARRAY['javascript','functions']::text[],true,137),
('javascript','functions','javascript-functions-138','What is a closure, technically, in terms of the JavaScript execution model?','Technically, a closure is the combination of a function together with references to its surrounding lexical environment (the variables in scope at the time it was created). The JavaScript engine keeps that environment alive in memory for as long as the closure itself is reachable, even after the outer function that created it has returned.',null,null,null,'medium',ARRAY['javascript','functions','closures']::text[],false,138),
('javascript','functions','javascript-functions-139','How can closures accidentally cause memory leaks?','Because a closure keeps its entire enclosing scope alive as long as it exists, capturing a large object or DOM node inside a long-lived closure (like an event listener that''s never removed) prevents that memory from being garbage collected even if it''s no longer actually needed. Being deliberate about what a closure captures, and cleaning up listeners/timers, avoids this.',null,null,null,'hard',ARRAY['javascript','functions','closures','performance']::text[],false,139),
('javascript','functions','javascript-functions-140','What is a private variable pattern using closures, before class private fields existed?','Before the # private field syntax, closures were the standard way to emulate private state — a variable declared inside a constructor or factory function is inaccessible from outside, and only the functions returned/attached from that same scope (which close over the variable) can read or modify it.',null,'function createAccount(balance) {
  return {
    deposit: (amount) => (balance += amount),
    getBalance: () => balance,
  };
}
// balance itself is not directly accessible from outside','javascript','medium',ARRAY['javascript','functions','closures']::text[],true,140),
('javascript','functions','javascript-functions-141','What is the difference between arguments.length and a function''s declared parameter count?','arguments.length reflects the actual number of arguments passed at the call site, regardless of how many parameters the function declares. fn.length reflects how many parameters were declared (up to the first default/rest parameter) — the two can easily differ if a function is called with more or fewer arguments than it declares.',null,'function f(a, b) { return arguments.length; }
f(1, 2, 3); // 3, even though f.length is 2','javascript','hard',ARRAY['javascript','functions']::text[],false,141),
('javascript','functions','javascript-functions-142','What is an async function under the hood, in terms of generators and promises?','Conceptually, an async function behaves like a generator function whose yields are automatically driven by a runner that resolves each awaited Promise before resuming execution. You don''t write the generator/runner machinery yourself — async/await is syntactic sugar the engine provides — but understanding that mental model explains why await only works inside async functions.',null,null,null,'hard',ARRAY['javascript','functions','async']::text[],false,142),
('javascript','functions','javascript-functions-143','What is function currying used for in real-world code, beyond interview trivia?','Currying is used to build specialized, reusable functions from a general one by pre-filling common arguments — for example, a generic `request(method, url, body)` function curried into `get = request(''GET'')`, or configuring event handlers, validators, or middleware with shared setup ahead of time.',null,'const request = (method) => (url) => (body) => fetch(url, { method, body });
const post = request(''POST'');
post(''/api/users'')({ name: ''Ana'' });','javascript','medium',ARRAY['javascript','functions','functional']::text[],false,143),
('javascript','functions','javascript-functions-144','What is the difference between Array.prototype.forEach and a for loop in terms of function semantics?','forEach takes a callback function and invokes it once per array element, but it cannot be stopped early with break/continue and always returns undefined, making it purely for side effects. A regular for loop supports break/continue and can be used to build a return value directly, giving more control at the cost of slightly more boilerplate.',null,'arr.forEach((item) => {
  if (item === ''skip'') return; // only skips this iteration, doesn''t stop the loop
});','javascript','medium',ARRAY['javascript','functions','arrays']::text[],false,144),
('javascript','functions','javascript-functions-145','What is a decorator-like pattern for functions in plain JavaScript (without the decorator proposal)?','Even without the experimental decorator syntax, you can achieve the same effect by writing a higher-order function that wraps another function, adding behavior (like logging, timing, or retries) before/after calling the original, then returning the wrapped version.',null,'function withTiming(fn) {
  return (...args) => {
    const start = Date.now();
    const result = fn(...args);
    console.log(`${fn.name} took ${Date.now() - start}ms`);
    return result;
  };
}','javascript','hard',ARRAY['javascript','functions','patterns']::text[],false,145),
('javascript','functions','javascript-functions-146','What is the difference between .call() and a direct method call in terms of what problem it solves?','A direct method call (obj.method()) automatically binds `this` to obj. .call() lets you invoke a function while manually specifying what `this` should be, which is useful for borrowing a method from one object (or from Array.prototype) and applying it to a different, unrelated object.',null,'function greet() { return `Hi, ${this.name}`; }
greet.call({ name: ''Sam'' }); // ''Hi, Sam'' — borrowing greet for another object','javascript','medium',ARRAY['javascript','functions','this']::text[],false,146),
('javascript','functions','javascript-functions-147','How would you convert an array-like arguments object into a real array inside a function?','Use Array.from(arguments) or the spread operator [...arguments] to convert the array-like arguments object into a genuine array with full access to array methods. This is largely unnecessary in modern code since rest parameters already produce a real array directly.',null,'function f() {
  const args = Array.from(arguments);
  return args.map((x) => x * 2);
}','javascript','easy',ARRAY['javascript','functions']::text[],false,147),
('javascript','functions','javascript-functions-148','What''s the difference between throwing inside a regular function versus inside a Promise executor or async function?','Throwing inside a regular synchronous function propagates immediately as a normal exception that the caller must catch with try/catch. Throwing inside a Promise executor or an async function is captured automatically and turned into a rejected Promise instead of an uncaught exception, so it must be handled with .catch() or a try/catch around an await.',null,null,null,'hard',ARRAY['javascript','functions','async']::text[],false,148),
('javascript','functions','javascript-functions-149','What is the significance of a function''s name property, and can it be changed?','Every function has a read-only (but configurable) `name` property reflecting its inferred or declared name, which is inferred automatically when a function is assigned to a variable or object property, even for anonymous arrow functions. It''s mainly useful for debugging and stack traces, and can be explicitly overridden with Object.defineProperty if needed.',null,'const greet = () => {};
greet.name; // ''greet'', inferred from the assignment','javascript','hard',ARRAY['javascript','functions']::text[],false,149),
('javascript','functions','javascript-functions-150','What is the difference between a factory function and a constructor function?','A factory function is a regular function that returns a new object explicitly, without using `new` or `this` — the caller just calls it like any other function. A constructor function relies on `new` to create and implicitly return a new object with `this` bound to it. Factory functions avoid `new`-related footguns (like forgetting it) entirely.',null,'function createPoint(x, y) {
  return { x, y }; // no `new` needed
}
const p = createPoint(1, 2);','javascript','medium',ARRAY['javascript','functions','patterns']::text[],false,150),
('javascript','functions','javascript-functions-151','What does it mean for a function argument to be passed "by reference" versus "by value" in JavaScript?','JavaScript always passes arguments by value — but for objects and arrays, the "value" being copied is a reference to the object, so mutating the object''s contents inside the function affects the caller''s object too, while reassigning the parameter itself to a brand-new object does not affect the caller''s variable.',null,'function mutate(obj) { obj.x = 99; } // affects caller''s object
function reassign(obj) { obj = { x: 99 }; } // does not affect caller''s variable','javascript','medium',ARRAY['javascript','functions']::text[],true,151),
('javascript','objects','javascript-objects-101','What does Object.keys(), Object.values(), and Object.entries() return?','Object.keys() returns an array of a given object''s own enumerable string-keyed property names. Object.values() returns an array of the corresponding values, and Object.entries() returns an array of [key, value] pairs. All three only include own enumerable properties, not inherited ones.',null,'const obj = { a: 1, b: 2 };
Object.keys(obj);   // [''a'', ''b'']
Object.values(obj); // [1, 2]
Object.entries(obj); // [[''a'',1],[''b'',2]]','javascript','easy',ARRAY['javascript','objects']::text[],true,101),
('javascript','objects','javascript-objects-102','What is the difference between Object.assign() and the object spread operator?','Both perform a shallow merge of one or more source objects into a target, copying only own enumerable properties. Object.assign() mutates and returns the first argument (the target), while the spread operator ({...a, ...b}) always creates a brand new object, leaving the originals untouched. Spread is generally preferred for immutable-style merging since it avoids accidentally mutating an existing object.',null,'const merged = Object.assign({}, a, b);
const merged2 = { ...a, ...b };','javascript','easy',ARRAY['javascript','objects']::text[],false,102),
('javascript','objects','javascript-objects-103','What does Object.create() do and when would you use it?','Object.create(proto) creates a new object with the specified object as its prototype, without invoking a constructor. It''s useful for building objects with a specific prototype chain directly, implementing prototypal inheritance without classes, or creating an object with no prototype at all via Object.create(null), which avoids inherited properties like toString.',null,'const animal = { speak() { return ''sound''; } };
const dog = Object.create(animal);
dog.speak(); // ''sound''','javascript','medium',ARRAY['javascript','objects','prototype']::text[],false,103),
('javascript','objects','javascript-objects-104','How does the prototype chain work when looking up a property?','When you access a property on an object, the JS engine first checks the object''s own properties. If not found, it looks at the object''s prototype (via the internal [[Prototype]] link), then that prototype''s prototype, and so on, until it reaches Object.prototype or finds the property. If the chain ends at null without a match, the result is undefined.',null,null,null,'medium',ARRAY['javascript','objects','prototype']::text[],true,104),
('javascript','objects','javascript-objects-105','What is the difference between __proto__ and Object.getPrototypeOf()?','__proto__ is a legacy, non-standard (though widely implemented) accessor property that exposes an object''s prototype, primarily kept for browser compatibility. Object.getPrototypeOf() is the standard, recommended way to read an object''s prototype, and Object.setPrototypeOf() is the standard way to change it. Modern code should avoid __proto__ in favor of these methods or Object.create().',null,null,null,'medium',ARRAY['javascript','objects','prototype']::text[],false,105),
('javascript','objects','javascript-objects-106','How do ES6 classes relate to JavaScript''s prototype-based inheritance?','ES6 class syntax is primarily syntactic sugar over the existing prototype-based inheritance model — methods defined in a class body are added to the class''s prototype object, not to each instance. Under the hood, class Dog extends Animal still sets up a prototype chain: Dog.prototype''s [[Prototype]] is Animal.prototype. Classes don''t introduce a fundamentally new inheritance model, just a cleaner syntax on top of the existing one.',null,null,null,'medium',ARRAY['javascript','objects','classes']::text[],false,106),
('javascript','objects','javascript-objects-107','What are private class fields and how do you declare one?','Private class fields, prefixed with #, are properties that can only be accessed from within the class body itself — not from outside instances or subclasses. They were standardized in ES2022 and provide true encapsulation, unlike the older convention of prefixing a property with an underscore, which is just a naming convention with no actual enforcement.',null,'class Counter {
  #count = 0;
  increment() { return ++this.#count; }
}
const c = new Counter();
c.increment(); // 1','javascript','medium',ARRAY['javascript','objects','classes']::text[],true,107),
('javascript','objects','javascript-objects-108','What are static class members and when are they useful?','Static members are declared with the static keyword and belong to the class itself rather than to instances — they''re accessed as ClassName.member rather than instance.member. They''re useful for utility/factory methods, constants, or counters that logically relate to the class as a whole rather than any particular instance.',null,'class MathUtils {
  static PI = 3.14159;
  static square(n) { return n * n; }
}
MathUtils.square(4); // 16','javascript','easy',ARRAY['javascript','objects','classes']::text[],false,108),
('javascript','objects','javascript-objects-109','How do you define getters and setters inside a class?','Getters and setters are declared with the get and set keywords before a method name, and they let a property look like a plain field from the outside while running custom logic on read or write. They''re commonly used for computed properties or to add validation when a property is assigned.',null,'class Circle {
  constructor(radius) { this._radius = radius; }
  get area() { return Math.PI * this._radius ** 2; }
  set radius(r) { if (r > 0) this._radius = r; }
}','javascript','easy',ARRAY['javascript','objects','classes']::text[],false,109),
('javascript','objects','javascript-objects-110','What are computed property names in object literals?','Computed property names let you use a bracketed expression as an object key at creation time, so the actual key name can be determined dynamically at runtime rather than being a static identifier. This is especially useful when building objects keyed by a variable or function result.',null,'const key = ''score'';
const obj = { [key]: 100 }; // { score: 100 }','javascript','easy',ARRAY['javascript','objects','es6']::text[],false,110),
('javascript','objects','javascript-objects-111','What does Object.defineProperty() let you control that a normal assignment doesn''t?','Object.defineProperty() lets you precisely configure a property''s descriptor: whether it''s writable, enumerable (shows up in for...in/Object.keys), and configurable (can be redefined or deleted), or define it as an accessor with get/set functions. A plain assignment always creates a writable, enumerable, configurable property, so defineProperty is needed for anything more restrictive.',null,'const obj = {};
Object.defineProperty(obj, ''id'', { value: 1, writable: false, enumerable: false });','javascript','hard',ARRAY['javascript','objects']::text[],false,111),
('javascript','objects','javascript-objects-112','What is the difference between hasOwnProperty() and the in operator?','obj.hasOwnProperty(key) checks only the object''s own properties, ignoring the prototype chain. The in operator checks both own properties and inherited ones from the entire prototype chain. So ''toString'' in {} is true (inherited from Object.prototype), but {}.hasOwnProperty(''toString'') is false.',null,null,null,'medium',ARRAY['javascript','objects','prototype']::text[],true,112),
('javascript','objects','javascript-objects-113','How do you correctly compare two objects for equality in JavaScript?','Both == and === compare objects by reference, not by their contents, so two objects with identical properties are never === unless they''re literally the same reference. To compare structural equality you need a deep-equality check — either a manual recursive comparison, JSON.stringify() for simple cases (with caveats around key order and unsupported types), or a library like lodash''s isEqual.',null,null,null,'medium',ARRAY['javascript','objects']::text[],false,113),
('javascript','objects','javascript-objects-114','What is structuredClone() and how does it differ from JSON-based deep cloning?','structuredClone() is a built-in browser/Node API that performs a true deep clone of an object, correctly handling Dates, Maps, Sets, ArrayBuffers, and circular references. The older JSON.parse(JSON.stringify(obj)) trick fails on those cases — it silently drops functions and undefined, breaks Dates into strings, and throws on circular references.',null,'const clone = structuredClone({ date: new Date(), nested: { a: 1 } });','javascript','medium',ARRAY['javascript','objects']::text[],false,114),
('javascript','objects','javascript-objects-115','When would you use a Map instead of a plain object?','Map is preferable when keys aren''t simple strings (Map allows objects, functions, or any value as a key), when insertion order matters for iteration (which Map guarantees), when you need a reliable .size, or when you''re frequently adding/removing keys, since Map doesn''t carry inherited prototype properties that could collide with data. Plain objects remain fine for simple, known-shape records.',null,null,null,'medium',ARRAY['javascript','objects']::text[],true,115),
('javascript','objects','javascript-objects-116','Why would you use a Set instead of an array to track unique values?','A Set automatically enforces uniqueness — adding a duplicate value is a no-op — and its .has() lookup is O(1) on average versus an array''s O(n) .includes() scan. This makes Set the natural choice for deduplication and fast membership checks, though arrays remain better when you need ordering by index or duplicate values.',null,'const unique = [...new Set([1, 2, 2, 3])]; // [1, 2, 3]','javascript','easy',ARRAY['javascript','objects']::text[],false,116),
('javascript','objects','javascript-objects-117','What problem do WeakMap and WeakSet solve?','WeakMap and WeakSet hold their keys (WeakMap) or values (WeakSet) weakly, meaning those references don''t prevent garbage collection — once nothing else references an object used as a key, it (and its associated value) can be collected. This makes them ideal for associating metadata with objects (like caching or private data) without causing memory leaks by keeping objects alive indefinitely.',null,null,null,'hard',ARRAY['javascript','objects','memory']::text[],false,117),
('javascript','objects','javascript-objects-118','What is a mixin in JavaScript and how is it typically implemented?','A mixin is a pattern for sharing reusable behavior across classes that don''t share a common ancestor, working around JavaScript''s lack of multiple inheritance. It''s commonly implemented as a function that takes a base class and returns a new subclass with additional methods mixed in, allowing you to compose several mixins onto one class.',null,'const Serializable = (Base) => class extends Base {
  serialize() { return JSON.stringify(this); }
};
class Model extends Serializable(Object) {}','javascript','hard',ARRAY['javascript','objects','patterns']::text[],false,118),
('javascript','objects','javascript-objects-119','How does the instanceof operator actually work?','obj instanceof Constructor checks whether Constructor.prototype appears anywhere in obj''s prototype chain, not whether obj was literally created by that constructor. This means you can make instanceof return true or false in unexpected ways by manually altering the prototype chain, and it doesn''t work reliably across different realms (e.g., iframes) since each has its own constructor identity.',null,null,null,'medium',ARRAY['javascript','objects','prototype']::text[],false,119),
('javascript','objects','javascript-objects-120','How do you make a custom object iterable with for...of?','You implement the Symbol.iterator method on the object, returning an iterator object with a next() method that yields { value, done } pairs. Once an object has this method, it can be used directly with for...of, spread syntax, and destructuring.',null,'const range = {
  from: 1, to: 3,
  [Symbol.iterator]() {
    let cur = this.from, last = this.to;
    return { next: () => cur <= last ? { value: cur++, done: false } : { done: true } };
  }
};
[...range]; // [1, 2, 3]','javascript','hard',ARRAY['javascript','objects','iterators']::text[],false,120),
('javascript','objects','javascript-objects-121','What''s the difference between spreading an object ({...obj}) and Object.assign({}, obj)?','Functionally they both produce a shallow copy merged into a new object, and in most cases are interchangeable. Object spread is generally considered more readable and slightly faster in modern engines, while Object.assign is still useful when the set of source objects is dynamic (an array of objects) since it can be called with apply or spread arguments itself.',null,null,null,'easy',ARRAY['javascript','objects','es6']::text[],false,121),
('javascript','objects','javascript-objects-122','What are common pitfalls when serializing objects to JSON?','JSON.stringify() silently omits properties whose value is undefined, a function, or a Symbol; it converts Date objects to ISO strings (losing the Date type on parse); and it throws a TypeError on circular references unless you supply a custom replacer function. NaN and Infinity are serialized as null, which can silently corrupt numeric data.',null,null,null,'medium',ARRAY['javascript','objects','json']::text[],false,122),
('javascript','objects','javascript-objects-123','What is property shorthand syntax in object literals?','When a variable''s name matches the key you want in an object literal, you can write just the variable name instead of key: value. This reduces repetition and is extremely common when returning multiple values from a function as an object.',null,'const name = ''Amy'', age = 30;
const user = { name, age }; // same as { name: name, age: age }','javascript','easy',ARRAY['javascript','objects','es6']::text[],false,123),
('javascript','objects','javascript-objects-124','How does optional chaining interact with method calls?','obj?.method?.() calls method only if both obj and obj.method are not null/undefined, short-circuiting to undefined otherwise instead of throwing a TypeError. This is especially useful for calling optional callback props or methods on objects that might not have been initialized yet.',null,'user.onLogin?.(); // only calls onLogin if it exists','javascript','easy',ARRAY['javascript','objects','es6']::text[],false,124),
('javascript','objects','javascript-objects-125','Does Object.freeze() perform a deep freeze?','No — Object.freeze() only freezes the object''s own top-level properties; any nested objects remain fully mutable. To achieve a deep freeze you need to recursively freeze each nested object yourself, which is a common gotcha when developers assume immutability extends further than it does.',null,null,null,'medium',ARRAY['javascript','objects']::text[],true,125),
('javascript','objects','javascript-objects-126','How does the super keyword work in class inheritance?','Inside a subclass constructor, super(...) calls the parent class''s constructor and must be called before this can be used. Inside a method, super.methodName() calls the parent class''s version of that method, which is useful for extending rather than fully replacing inherited behavior.',null,'class Animal {
  speak() { return ''generic sound''; }
}
class Dog extends Animal {
  speak() { return super.speak() + '' (barks)''; }
}','javascript','medium',ARRAY['javascript','objects','classes']::text[],false,126),
('javascript','objects','javascript-objects-127','How do you simulate an abstract class in JavaScript, which has no built-in abstract keyword?','A common pattern is to check in the base class''s constructor whether new.target === BaseClass and throw an error if so, preventing direct instantiation. Methods meant to be ''abstract'' can throw an error like ''must be implemented by subclass'' if not overridden, giving a runtime approximation of an abstract contract.',null,null,null,'hard',ARRAY['javascript','objects','classes','patterns']::text[],false,127),
('javascript','objects','javascript-objects-128','What is method overriding in JavaScript classes?','Method overriding happens when a subclass defines a method with the same name as one on its parent class, replacing the inherited implementation for instances of the subclass. Unlike some languages, JS doesn''t require any special keyword or check signature compatibility — any method name can simply be redefined.',null,null,null,'easy',ARRAY['javascript','objects','classes']::text[],false,128),
('javascript','objects','javascript-objects-129','Why is composition often preferred over inheritance in JavaScript?','Composition — building objects by combining smaller, focused pieces of behavior — tends to produce more flexible code than deep inheritance hierarchies, which can become rigid and hard to change as a codebase grows (the fragile base class problem). JS''s functions-as-values and object literals make composition (passing behavior in, combining small objects) especially natural compared to class-based inheritance chains.',null,null,null,'medium',ARRAY['javascript','objects','patterns']::text[],false,129),
('javascript','objects','javascript-objects-130','How do toString() and valueOf() affect type coercion of objects?','When an object is used in a context expecting a primitive (like string concatenation or arithmetic), JS calls valueOf() first for numeric contexts and toString() first for string contexts (this is now more precisely governed by Symbol.toPrimitive if defined). Overriding these methods lets you control how a custom object behaves when coerced, e.g., in template literals or comparisons.',null,'class Money {
  constructor(cents) { this.cents = cents; }
  valueOf() { return this.cents; }
}
new Money(500) + new Money(250); // 750','javascript','hard',ARRAY['javascript','objects','coercion']::text[],false,130),
('javascript','objects','javascript-objects-131','What is a discriminated union pattern using plain objects, and why is it useful?','It''s a pattern where related object shapes share a common ''tag'' or ''type'' field whose value determines which other fields are present, letting code branch (often with a switch) on that field to safely narrow the shape. It''s commonly used to model state machines or API responses (e.g. { status: ''loading'' } vs { status: ''success'', data } vs { status: ''error'', error }) in a self-describing way.',null,null,null,'medium',ARRAY['javascript','objects','patterns']::text[],false,131),
('javascript','objects','javascript-objects-132','How can you enforce immutability in JavaScript without an external library?','Combine Object.freeze() (applied recursively for deep immutability) with consistently returning new objects/arrays instead of mutating in place (using spread syntax or array methods like map/filter that don''t mutate). This convention-plus-freeze approach mimics immutable data structures without needing a library like Immutable.js, though it doesn''t give you the structural-sharing performance benefits those libraries provide.',null,null,null,'medium',ARRAY['javascript','objects']::text[],false,132),
('javascript','objects','javascript-objects-133','What does it mean for an object property to be ''enumerable''?','Enumerable properties show up in for...in loops, Object.keys(), Object.entries(), and JSON.stringify() output; non-enumerable ones (like most built-in methods on Object.prototype, or properties defined via defineProperty without enumerable: true) are hidden from those but still directly accessible. This distinction lets built-in and library code attach ''hidden'' metadata to objects without polluting normal iteration.',null,null,null,'hard',ARRAY['javascript','objects']::text[],false,133),
('javascript','objects','javascript-objects-134','What is the Reflect object used for?','Reflect provides methods that mirror many object-level operations (like Reflect.get, Reflect.set, Reflect.ownKeys, Reflect.deleteProperty) as regular functions with consistent, predictable return values, rather than operators or statements that can throw or behave inconsistently. It''s most commonly used alongside Proxy, where trap handlers often forward to the corresponding Reflect method for default behavior.',null,null,null,'hard',ARRAY['javascript','objects','reflect']::text[],false,134),
('javascript','objects','javascript-objects-135','What does Object.fromEntries() do?','Object.fromEntries() is the inverse of Object.entries() — it builds a plain object from an iterable of [key, value] pairs, such as an array of entries or a Map. It''s commonly used to convert a Map back into a plain object, or to transform an object''s entries with map/filter and rebuild a new object from the result.',null,'Object.fromEntries([[''a'', 1], [''b'', 2]]); // { a: 1, b: 2 }','javascript','easy',ARRAY['javascript','objects','es6']::text[],false,135),
('javascript','objects','javascript-objects-136','What are the basic traps a Proxy can intercept, and what is a Proxy used for?','A Proxy wraps a target object and lets you intercept fundamental operations via ''traps'' — most commonly get (property read), set (property write), has (the in operator), and deleteProperty. Proxies are used for validation, logging/debugging property access, creating reactive objects (as Vue''s reactivity system does), or implementing virtual/computed properties.',null,'const logged = new Proxy({}, {
  set(target, key, value) { console.log(`set ${key}`); target[key] = value; return true; }
});
logged.x = 5; // logs ''set x''','javascript','hard',ARRAY['javascript','objects','proxy']::text[],false,136),
('javascript','objects','javascript-objects-137','What is the difference between Object.seal() and Object.preventExtensions()?','Object.preventExtensions() only stops new properties from being added, but existing ones can still be modified or deleted. Object.seal() goes further — it also marks all existing properties as non-configurable (so they can''t be deleted or reconfigured, though writable ones can still have their value changed), sitting between preventExtensions and the fully locked-down freeze.',null,null,null,'hard',ARRAY['javascript','objects']::text[],false,137),
('javascript','objects','javascript-objects-138','What is duck typing and how does it relate to polymorphism in JavaScript?','Duck typing means code cares only about whether an object has the methods/properties it needs at the moment it''s used (''if it walks like a duck and quacks like a duck...''), rather than its declared type or class. Because JavaScript is dynamically typed, this enables a lightweight form of polymorphism — different objects can be used interchangeably anywhere they implement the expected shape, without needing a shared base class or interface.',null,null,null,'medium',ARRAY['javascript','objects','patterns']::text[],false,138),
('javascript','objects','javascript-objects-139','What is method chaining and how do you implement a fluent interface?','Method chaining lets you call multiple methods on the same object in a single expression (obj.a().b().c()), which is achieved simply by having each method return this (or a new instance of the same type) instead of undefined. It''s commonly seen in builder patterns, query builders, and libraries like jQuery or array methods that return arrays.',null,'class QueryBuilder {
  where(cond) { this.conditions = cond; return this; }
  build() { return `WHERE ${this.conditions}`; }
}
new QueryBuilder().where(''id=1'').build();','javascript','easy',ARRAY['javascript','objects','patterns']::text[],false,139),
('javascript','objects','javascript-objects-140','How would you implement a simple object pool, and why might you need one?','An object pool pre-allocates and reuses a fixed set of objects instead of constantly creating and discarding them, which reduces garbage collection pressure in performance-sensitive code like games or high-frequency event handling. It typically exposes acquire() (returns a free object from the pool, creating one if needed) and release(obj) (returns the object to the pool for reuse) methods.',null,null,null,'hard',ARRAY['javascript','objects','performance']::text[],false,140),
('javascript','objects','javascript-objects-141','How do you clone a class instance while preserving its prototype (and thus its methods)?','A plain spread ({...instance}) or Object.assign({}, instance) copies own properties but loses the prototype, turning the result into a plain object without the class''s methods. To preserve the prototype, use Object.create(Object.getPrototypeOf(instance), Object.getOwnPropertyDescriptors(instance)), or simply pass the instance''s properties into new instance.constructor(...) if the constructor supports it.',null,null,null,'hard',ARRAY['javascript','objects','classes']::text[],false,141),
('javascript','objects','javascript-objects-142','Does JavaScript guarantee the iteration order of object properties?','Mostly yes, per the ES2015+ specification: integer-like keys are iterated first in ascending numeric order, followed by string keys in insertion order, followed by Symbol keys in insertion order. This is a common point of confusion since older mental models assumed object key order was entirely unspecified.',null,null,null,'hard',ARRAY['javascript','objects']::text[],false,142),
('javascript','objects','javascript-objects-143','How can you use a plain object or Map to memoize a function''s results?','Memoization caches a function''s return value keyed by its arguments (often serialized to a string, or used directly as a Map key for object/array arguments) so that a repeated call with the same input returns the cached result instead of recomputing. This trades memory for speed and is especially effective for expensive, pure (side-effect-free) computations.',null,'function memoize(fn) {
  const cache = new Map();
  return (arg) => {
    if (cache.has(arg)) return cache.get(arg);
    const result = fn(arg);
    cache.set(arg, result);
    return result;
  };
}','javascript','medium',ARRAY['javascript','objects','performance']::text[],false,143),
('javascript','objects','javascript-objects-144','What is the difference between Object.is() and the === operator?','Object.is() behaves like === for almost all values, but corrects two edge cases: Object.is(NaN, NaN) is true (whereas NaN === NaN is false), and Object.is(0, -0) is false (whereas 0 === -0 is true). It''s mainly used internally by React and other libraries for precise equality checks, and rarely needed in everyday application code.',null,'Object.is(NaN, NaN); // true
NaN === NaN; // false','javascript','medium',ARRAY['javascript','objects']::text[],false,144),
('javascript','objects','javascript-objects-145','How do you declare a private method inside a class?','Prefix the method name with # just like a private field: #helper() { ... }. It can only be called from within the class body (this.#helper()), and attempting to access it from outside — even via bracket notation — throws a syntax/reference error, giving genuine encapsulation rather than convention-based privacy.',null,'class Auth {
  #hash(password) { return password.split('''').reverse().join(''''); }
  login(password) { return this.#hash(password) === this.storedHash; }
}','javascript','medium',ARRAY['javascript','objects','classes']::text[],false,145),
('javascript','objects','javascript-objects-146','Can static fields be private, and what''s a use case?','Yes — combining static and # (e.g. static #instances = 0) creates a private field shared across the class itself rather than instances, inaccessible from outside. A common use case is a private static counter tracking how many instances have been created, exposed only through a public static getter.',null,'class Widget {
  static #count = 0;
  constructor() { Widget.#count++; }
  static get count() { return Widget.#count; }
}','javascript','medium',ARRAY['javascript','objects','classes']::text[],false,146),
('javascript','objects','javascript-objects-147','Why might you validate input inside a setter rather than in the constructor alone?','Validating in a setter ensures the invariant holds not just at construction time but every time the property is reassigned later in the object''s lifetime, since the constructor typically delegates to the same setter (this.radius = radius) rather than duplicating the check. This centralizes validation logic in one place instead of scattering it across every assignment site.',null,'class Circle {
  set radius(r) {
    if (r <= 0) throw new Error(''radius must be positive'');
    this._radius = r;
  }
}','javascript','medium',ARRAY['javascript','objects','classes']::text[],false,147),
('javascript','objects','javascript-objects-148','What''s the risk of using object keys derived from user input without sanitization?','If user input is used directly as an object key without care, it can collide with inherited properties like __proto__, constructor, or toString, potentially leading to prototype pollution — where an attacker manipulates an object''s prototype to affect unrelated code. Using a Map (which has no prototype pollution risk) or Object.create(null) for such lookup tables avoids this class of bug entirely.',null,null,null,'hard',ARRAY['javascript','objects','security']::text[],false,148),
('javascript','objects','javascript-objects-149','What is the difference between a plain object literal and a class instance in terms of underlying representation?','Both are ultimately objects with an internal prototype link — a class instance''s [[Prototype]] points to the class''s prototype (carrying its methods), while an object literal''s [[Prototype]] points directly to Object.prototype by default. Functionally, a class is really just a more structured, conventionalized way to produce objects with a shared prototype and consistent initialization via the constructor.',null,null,null,'medium',ARRAY['javascript','objects','classes']::text[],false,149),
('javascript','objects','javascript-objects-150','How would you deeply merge two nested objects, unlike Object.assign''s shallow merge?','A deep merge recursively walks both objects, and whenever a key exists in both and both values are plain objects, it merges them recursively instead of letting one simply overwrite the other; for non-object values, the source typically takes precedence. There''s no built-in deep merge in JS, so it''s either hand-written recursively or delegated to a utility library like lodash''s merge.',null,'function deepMerge(target, source) {
  for (const key in source) {
    if (source[key] instanceof Object && key in target) {
      Object.assign(source[key], deepMerge(target[key], source[key]));
    }
  }
  return { ...target, ...source };
}','javascript','medium',ARRAY['javascript','objects']::text[],false,150),
('react','advanced-react','react-advanced-react-101','How does React Suspense work for data fetching, beyond just lazy-loading components?','A data source that supports Suspense (like a framework''s data layer, or React''s own use() hook with a promise) can "throw" a pending promise during render, which tells React to pause that part of the tree and show the nearest Suspense boundary''s fallback until the promise resolves. This lets components read data as if it were already available, with the loading state handled declaratively by a wrapping <Suspense> instead of manual isLoading checks scattered through the component.',null,null,null,'hard',ARRAY['react','advanced','suspense']::text[],true,101),
('react','advanced-react','react-advanced-react-102','How should you decide where to place Suspense boundaries in a component tree?','A boundary placed too high shows a large, jarring loading fallback for the whole page even if only a small part is waiting on data; a boundary placed too low means every nested async component blocks its own tiny corner independently, which can look inconsistent. The common approach is to group logically-related content that should appear together behind one boundary, and let independent sections (like a sidebar versus a main content area) have their own separate boundaries so they can load and appear independently.',null,'<Suspense fallback={<PageSkeleton />}>
  <Header />
  <Suspense fallback={<SidebarSkeleton />}>
    <Sidebar />
  </Suspense>
  <MainContent />
</Suspense>','jsx','medium',ARRAY['react','advanced','suspense']::text[],false,102),
('react','advanced-react','react-advanced-react-103','What does "concurrent" mean when people describe React 18 as a concurrent renderer?','Concurrent doesn''t mean multi-threaded — React still runs on a single thread. It means React can start rendering an update, pause partway through, work on something more urgent instead, and come back to (or discard) the paused render later, rather than always running a render to completion once started, the way React''s original synchronous renderer worked.',null,null,null,'hard',ARRAY['react','advanced','concurrent-rendering']::text[],false,103),
('react','advanced-react','react-advanced-react-104','What is the difference between hydration and a full client-side render?','A full client-side render builds the DOM from scratch in the browser using JavaScript. Hydration takes HTML that was already rendered (on the server or at build time) and attaches React''s event listeners and internal state to that existing markup without re-creating the DOM nodes, so the page appears instantly while React "wakes up" the already-visible content and makes it interactive.',null,null,null,'medium',ARRAY['react','advanced','hydration']::text[],false,104),
('react','advanced-react','react-advanced-react-105','What is islands architecture / partial hydration, at a high level?','Instead of hydrating an entire page''s worth of React as one unit, islands architecture ships and hydrates only the small, genuinely interactive pieces of a page (like a like-button or a carousel) independently, while the rest of the page stays static HTML with no JavaScript attached at all. This is a framework-agnostic pattern aimed at pages that are mostly static content with only a few interactive widgets, minimizing the amount of JS shipped and hydrated.',null,null,null,'hard',ARRAY['react','advanced','hydration']::text[],false,105),
('react','advanced-react','react-advanced-react-106','What are React Server Components and how do they differ from traditional server-side rendering?','Server Components run exclusively on the server (or at build time) and never ship their code to the client at all — not even for hydration — whereas traditional SSR renders HTML on the server but still sends the full component JavaScript to the browser so it can hydrate. Server Components can also directly access server-only resources (a database, the filesystem) without an API layer, and can be mixed with regular Client Components for the interactive parts of a page.',null,null,null,'hard',ARRAY['react','advanced','server-components']::text[],true,106),
('react','advanced-react','react-advanced-react-107','How would you implement the children-as-a-function (render props) pattern?','Instead of accepting a static children element, the component calls the children prop as a function during its own render, passing it internal state or data the caller needs. This lets the parent component own some shared logic (like tracking mouse position or a data subscription) while giving the consumer full control over what markup to render with that data.',null,'function MouseTracker({ children }) {
  const [pos, setPos] = useState({ x: 0, y: 0 });
  return (
    <div onMouseMove={(e) => setPos({ x: e.clientX, y: e.clientY })}>
      {children(pos)}
    </div>
  );
}

<MouseTracker>{(pos) => <p>{pos.x}, {pos.y}</p>}</MouseTracker>','jsx','medium',ARRAY['react','advanced','render-props']::text[],false,107),
('react','advanced-react','react-advanced-react-108','How do compound components use Context internally to share implicit state between siblings?','A compound component (like <Tabs><Tabs.List><Tabs.Tab /></Tabs.List></Tabs>) sets up a Context provider in its root component to hold shared state (like the currently active tab), and each child subcomponent reads that Context internally rather than receiving the state as an explicit prop. This gives consumers a clean, declarative JSX API while the components coordinate behind the scenes.',null,'const TabsContext = createContext();
function Tabs({ children }) {
  const [active, setActive] = useState(0);
  return <TabsContext.Provider value={{ active, setActive }}>{children}</TabsContext.Provider>;
}
function Tab({ index, children }) {
  const { active, setActive } = useContext(TabsContext);
  return <button onClick={() => setActive(index)}>{children}</button>;
}','jsx','medium',ARRAY['react','advanced','compound-components']::text[],false,108),
('react','advanced-react','react-advanced-react-109','Why do events triggered inside a Portal still bubble up through the React component tree instead of the DOM tree?','React maintains its own synthetic event system layered on top of the DOM, and it dispatches bubbling events based on the React component tree (where the Portal''s content was logically rendered), not the actual DOM parent it was inserted into. This means a click inside a modal rendered via a portal into document.body will still trigger onClick handlers on its logical React ancestors, even though those ancestors aren''t its DOM ancestors.',null,null,null,'medium',ARRAY['react','advanced','portals']::text[],false,109),
('react','advanced-react','react-advanced-react-110','What kinds of errors do React error boundaries NOT catch?','Error boundaries only catch errors thrown during rendering, in lifecycle methods, and in constructors of the tree below them. They do not catch errors inside event handlers (those need a regular try/catch), errors in asynchronous code like setTimeout or promise callbacks, errors during server-side rendering, or errors thrown in the error boundary''s own code.',null,null,null,'medium',ARRAY['react','advanced','error-boundaries']::text[],true,110),
('react','advanced-react','react-advanced-react-111','How would you let a user recover from an error caught by an error boundary, instead of leaving them stuck on a fallback UI?','The error boundary''s fallback UI can include a "try again" button that resets the boundary''s internal hasError state, which causes React to attempt rendering the children again — useful if the error was transient (like a flaky network request). For errors tied to specific data (like a broken route), the reset can also be tied to a key change so navigating away and back gets a completely fresh boundary instance.',null,'class ErrorBoundary extends React.Component {
  state = { hasError: false };
  static getDerivedStateFromError() { return { hasError: true }; }
  render() {
    if (this.state.hasError) {
      return <button onClick={() => this.setState({ hasError: false })}>Try again</button>;
    }
    return this.props.children;
  }
}','jsx','medium',ARRAY['react','advanced','error-boundaries']::text[],false,111),
('react','advanced-react','react-advanced-react-112','How can the key prop be used as an advanced reset mechanism beyond simple lists?','Changing a key anywhere — not just in a list — tells React to treat the element as a completely new instance rather than updating the existing one, discarding all of its state, refs, and effects. This is a useful trick for resetting a complex form or wizard when switching between unrelated records, without manually resetting every individual field.',null,'<EditForm key={selectedRecord.id} record={selectedRecord} />','jsx','medium',ARRAY['react','advanced','key']::text[],false,112),
('react','advanced-react','react-advanced-react-113','How would you apply the controlled/uncontrolled pattern when designing a reusable library component?','A well-designed reusable component (like a dropdown or accordion) usually supports both modes: it manages its own internal state by default (uncontrolled, easy for basic usage), but accepts optional value and onChange props that, when provided, let the parent fully control the state instead. This dual API is common in component libraries because it serves simple use cases out of the box while still giving advanced consumers full control when they need it.',null,'function Toggle({ value, onChange, defaultValue = false }) {
  const [internal, setInternal] = useState(defaultValue);
  const isControlled = value !== undefined;
  const isOn = isControlled ? value : internal;
  const toggle = () => isControlled ? onChange(!isOn) : setInternal((v) => !v);
  return <button onClick={toggle}>{isOn ? ''On'' : ''Off''}</button>;
}','jsx','hard',ARRAY['react','advanced','component-design']::text[],false,113),
('react','advanced-react','react-advanced-react-114','How would you build a custom hook that wraps a third-party imperative API (like a chart library or a map SDK)?','The hook typically creates the imperative instance inside a useEffect (or useLayoutEffect if it touches layout) tied to a ref pointing at a DOM container, exposes an imperative-to-declarative bridge by updating the instance whenever relevant React state/props change, and tears the instance down in the effect''s cleanup function to avoid leaking it on unmount or re-initialization.',null,'function useChart(containerRef, data) {
  useEffect(() => {
    const chart = new ChartLib(containerRef.current);
    chart.render(data);
    return () => chart.destroy();
  }, [data]);
}','jsx','medium',ARRAY['react','advanced','custom-hooks']::text[],false,114),
('react','advanced-react','react-advanced-react-115','How does ref forwarding work through multiple layers of wrapping components?','By default, a ref passed to a custom component doesn''t automatically attach to a DOM node inside it, because refs aren''t part of the normal props flow. forwardRef lets a component explicitly accept a ref as a second argument and forward it to whichever inner DOM node or child it chooses, and this can be chained through several nested wrapper components as long as each one also forwards the ref it receives.',null,'const FancyInput = forwardRef((props, ref) => (
  <input ref={ref} className="fancy" {...props} />
));

const inputRef = useRef(null);
<FancyInput ref={inputRef} />;','jsx','medium',ARRAY['react','advanced','refs']::text[],false,115),
('react','advanced-react','react-advanced-react-116','How does combining the Provider pattern with a custom hook create a common library-authoring idiom?','A Context Provider supplies shared state to a subtree, and a paired custom hook (like useAuth() or useTheme()) wraps useContext to both read that value and throw a helpful error if it''s called outside the Provider — giving consumers a clean, self-documenting API instead of importing and using the raw Context object directly everywhere.',null,'const AuthContext = createContext(null);
export function useAuth() {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error(''useAuth must be used within AuthProvider'');
  return ctx;
}','jsx','medium',ARRAY['react','advanced','context']::text[],true,116),
('react','advanced-react','react-advanced-react-117','What problem did React''s fiber architecture solve compared to the older stack reconciler?','The original "stack" reconciler processed the entire component tree synchronously and couldn''t be paused once started, which could block the main thread long enough to make an app feel unresponsive during large updates. Fiber restructured reconciliation into units of work that can be paused, resumed, prioritized, or abandoned, which is the foundation that makes features like concurrent rendering, Suspense, and useTransition possible.',null,null,null,'hard',ARRAY['react','advanced','fiber']::text[],true,117),
('react','advanced-react','react-advanced-react-118','What is time slicing in the context of React''s concurrent features?','Time slicing is the technique of breaking rendering work into small chunks and yielding control back to the browser between chunks, so the main thread stays free to handle higher-priority work like user input or animation frames instead of being monopolized by a single long render. It''s what allows React to render a large update "in the background" without freezing the page.',null,null,null,'hard',ARRAY['react','advanced','concurrent-rendering']::text[],false,118),
('react','advanced-react','react-advanced-react-119','Why does React avoid synchronous rendering that blocks the main thread for large updates?','Because JavaScript in the browser runs on a single thread shared with layout, painting, and input handling, a long synchronous render blocks all of that, making the page feel frozen and unresponsive to clicks or typing during the update. Concurrent rendering exists specifically to let React interrupt a big render to handle urgent input first, keeping the app feeling responsive even while a large update is still in progress.',null,null,null,'medium',ARRAY['react','advanced','concurrent-rendering']::text[],false,119),
('react','advanced-react','react-advanced-react-120','Why does React''s Strict Mode intentionally invoke component functions and effects twice in development?','Double-invoking helps surface bugs caused by impure render logic or missing effect cleanup — if a render function or an effect isn''t idempotent (e.g. it mutates something outside its scope, or an effect forgets to clean up a subscription before running again), running it twice makes the bug visible immediately in development instead of only appearing later under concurrent rendering in production. It only happens in development builds and has no effect on production behavior.',null,null,null,'medium',ARRAY['react','advanced','strict-mode']::text[],false,120),
('react','advanced-react','react-advanced-react-121','How would you approach testing an error boundary?','Render a component tree with the error boundary wrapping a deliberately-throwing child (a small test-only component that throws in its render), then assert that the fallback UI appears instead of the app crashing. Since React logs errors to the console during this, tests typically suppress or spy on console.error for that specific test to keep output clean, while still asserting the boundary''s behavior.',null,'function Bomb() { throw new Error(''boom''); }

test(''shows fallback on error'', () => {
  render(<ErrorBoundary><Bomb /></ErrorBoundary>);
  expect(screen.getByText(/something went wrong/i)).toBeInTheDocument();
});','jsx','medium',ARRAY['react','advanced','testing']::text[],false,121),
('react','advanced-react','react-advanced-react-122','What are the key considerations when building an accessible modal using a Portal?','Beyond rendering the modal outside the normal DOM flow via a portal, an accessible modal needs to trap keyboard focus inside itself while open (so Tab doesn''t escape to the page behind it), return focus to the triggering element on close, be dismissible via the Escape key, and use appropriate ARIA attributes (role="dialog", aria-modal="true") so assistive technology announces it correctly.',null,null,null,'hard',ARRAY['react','advanced','accessibility']::text[],false,122),
('react','advanced-react','react-advanced-react-123','What is the "lifting content up" pattern, and how does it differ from passing configuration objects as props?','Instead of a component accepting a configuration prop describing what to render (like a headerConfig object with a title and icon name), "lifting content up" has the parent pass actual JSX as a prop or children, giving the parent full flexibility over markup and behavior without the child needing to anticipate every possible configuration. It trades some structure/type-safety for significantly more composability.',null,'// Configuration object — child must know every possible shape
<Card header={{ title: ''Stats'', icon: ''chart'' }} />

// Lifted content — parent has full control
<Card header={<h2><ChartIcon /> Stats</h2>} />','jsx','medium',ARRAY['react','advanced','component-design']::text[],false,123),
('react','advanced-react','react-advanced-react-124','Why did the React ecosystem largely move from HOCs and render props to hooks?','HOCs and render props both share stateful logic by wrapping components, which adds extra layers to the component tree ("wrapper hell"), makes props harder to trace back to their source, and can create naming collisions between multiple HOCs. Hooks let you extract and reuse the exact same stateful logic as a plain function call inside a component, with no extra nesting, clearer data flow, and props/types that are easy to trace directly.',null,null,null,'medium',ARRAY['react','advanced','hooks']::text[],false,124),
('react','advanced-react','react-advanced-react-125','How would you build a reusable useFetch custom hook that wraps fetch with loading and error state?','The hook wraps a fetch call in a useEffect keyed on the URL, tracks loading/data/error in state, and — critically — checks a "still relevant" flag or uses an AbortController in the cleanup function so a stale response from a superseded request doesn''t overwrite state after the component has moved on to a new URL or unmounted.',null,'function useFetch(url) {
  const [state, setState] = useState({ data: null, loading: true, error: null });
  useEffect(() => {
    let active = true;
    setState((s) => ({ ...s, loading: true }));
    fetch(url).then((r) => r.json())
      .then((data) => active && setState({ data, loading: false, error: null }))
      .catch((error) => active && setState({ data: null, loading: false, error }));
    return () => { active = false; };
  }, [url]);
  return state;
}','jsx','medium',ARRAY['react','advanced','custom-hooks']::text[],true,125),
('react','advanced-react','react-advanced-react-126','How would you avoid "wrapper hell" when a component needs several cross-cutting concerns from multiple HOCs?','Composing several HOCs (withAuth(withTheme(withLogging(Component)))) creates deep, hard-to-debug nesting in the React tree and in the props chain. Converting each HOC''s logic into a custom hook and calling all of them directly inside a single component — useAuth(), useTheme(), useLogging() — achieves the same functionality flat, with each concern independently visible and no extra component layers.',null,null,null,'medium',ARRAY['react','advanced','hoc']::text[],false,126),
('react','advanced-react','react-advanced-react-127','How does the key prop interact with list reordering when combined with animation libraries?','Animation libraries that animate list reordering (like sliding items into their new position) rely on stable keys to know that an item at a new index is actually the same logical item that moved, rather than a different item that happened to land there. If keys aren''t stable (e.g. using array index), the library will animate the wrong items or fail to animate the move at all, since it can''t tell which DOM node corresponds to which piece of data across the reorder.',null,null,null,'medium',ARRAY['react','advanced','key']::text[],false,127),
('react','advanced-react','react-advanced-react-128','What does streaming SSR mean, and what problem does it solve compared to traditional SSR?','Traditional SSR waits until the entire page''s HTML is generated on the server before sending any of it to the browser, so a single slow data dependency delays the whole response. Streaming SSR sends HTML in chunks as it becomes ready — the shell and fast content arrive immediately while slower sections stream in afterward wrapped in Suspense boundaries — so users see and can interact with part of the page much sooner.',null,null,null,'hard',ARRAY['react','advanced','ssr']::text[],false,128),
('react','advanced-react','react-advanced-react-129','What is the difference between a Server Component and a Client Component in a framework that supports React Server Components?','Server Components render only on the server, can be async and read server-only resources directly, and produce no client-side JavaScript. Client Components (marked explicitly, e.g. with a "use client" directive) run in the browser, can use hooks like useState and useEffect, and handle interactivity — a typical app composes both, using Server Components for static/data-heavy parts of the tree and Client Components only where interactivity is actually needed.',null,null,null,'hard',ARRAY['react','advanced','server-components']::text[],false,129),
('react','advanced-react','react-advanced-react-130','How can you use useImperativeHandle to customize what a parent''s ref actually receives from a child?','By default, forwardRef exposes the raw underlying DOM node to the parent''s ref. useImperativeHandle lets the child instead expose a custom object with only specific methods it chooses to allow (like focus() or reset()), hiding the rest of its internals and giving the parent a deliberately limited imperative API rather than direct DOM access.',null,'const VideoPlayer = forwardRef((props, ref) => {
  const videoRef = useRef(null);
  useImperativeHandle(ref, () => ({
    play: () => videoRef.current.play(),
    pause: () => videoRef.current.pause(),
  }));
  return <video ref={videoRef} src={props.src} />;
});','jsx','hard',ARRAY['react','advanced','refs']::text[],false,130),
('react','advanced-react','react-advanced-react-131','What is the difference between the render props pattern and simply passing children?','Plain children are a static React element — whatever was passed is rendered as-is. Render props pass a function as children (or as another prop), which the component invokes during its own render and can supply arguments to, letting the consumer''s rendered output depend on internal state the component controls, something static children can''t do.',null,null,null,'easy',ARRAY['react','advanced','render-props']::text[],false,131),
('react','advanced-react','react-advanced-react-132','How would you implement a compound component that also supports an alternative flat/prop-driven API for simpler use cases?','The compound component''s root can accept optional props (like a tabs array) and, if provided, internally render the same subcomponents it would otherwise expect the consumer to compose manually — giving simple use cases a one-line API while still exposing the flexible <Tabs.Tab> composition for consumers who need custom markup per tab.',null,null,null,'hard',ARRAY['react','advanced','compound-components']::text[],false,132),
('react','advanced-react','react-advanced-react-133','Why can''t a Client Component directly import and render a Server Component as a child in frameworks that support RSC?','Once a component tree crosses into a Client Component, everything below it must also be capable of running in the browser, but Server Components are never sent to the client at all — so a Client Component can''t import one directly. The workaround is for a Server Component higher up the tree to render the Server Component and pass its already-rendered output down as the children prop to the Client Component, which the Client Component can then simply display.',null,null,null,'hard',ARRAY['react','advanced','server-components']::text[],false,133),
('react','advanced-react','react-advanced-react-134','What is a common gotcha when combining Suspense with data that depends on props that change frequently?','If a component wrapped by Suspense re-suspends every time its data-fetching key changes (e.g. a search query updating on every keystroke), the fallback UI can flash repeatedly instead of the previous content staying visible while new data loads. Wrapping the update that changes the key in useTransition (or using useDeferredValue) keeps the old content on screen and marks the pending state instead of immediately unmounting to the fallback.',null,null,null,'hard',ARRAY['react','advanced','suspense']::text[],false,134),
('react','advanced-react','react-advanced-react-135','How does React decide whether to reuse a DOM node or destroy and recreate it when the element type changes at the same position?','If two elements at the same position in the tree have different types (e.g. a <div> replaced by a <span>, or one custom component replaced by a different one), React tears down the entire old subtree — including its state and DOM nodes — and builds a fresh one, rather than trying to patch the differences. This is why conditionally rendering entirely different component types (instead of the same type with different props) resets any local state those components held.',null,null,null,'hard',ARRAY['react','advanced','reconciliation']::text[],false,135),
('react','advanced-react','react-advanced-react-136','What is the purpose of the use() hook introduced alongside Suspense-based data fetching?','use() lets a component read the value of a promise or a Context directly during render, suspending the component (via Suspense) if the promise hasn''t resolved yet. Unlike other hooks, it can be called conditionally, which makes it more flexible for reading async resources exactly where they''re needed rather than always at the top of a component.',null,'function Comments({ commentsPromise }) {
  const comments = use(commentsPromise);
  return <ul>{comments.map((c) => <li key={c.id}>{c.text}</li>)}</ul>;
}','jsx','hard',ARRAY['react','advanced','suspense']::text[],false,136),
('react','advanced-react','react-advanced-react-137','How would you design a component library''s theming system to support both a default theme and consumer overrides?','A common approach layers a ThemeProvider around the app supplying design tokens (colors, spacing, typography) via Context with sensible defaults, letting consumers override only the specific tokens they need by merging their partial theme object over the defaults, rather than requiring a full theme object to be supplied every time.',null,'const defaultTheme = { colors: { primary: ''#2563eb'' }, spacing: { sm: 8 } };
function ThemeProvider({ theme = {}, children }) {
  const merged = { ...defaultTheme, ...theme, colors: { ...defaultTheme.colors, ...theme.colors } };
  return <ThemeContext.Provider value={merged}>{children}</ThemeContext.Provider>;
}','jsx','medium',ARRAY['react','advanced','component-design']::text[],false,137),
('react','advanced-react','react-advanced-react-138','What is the difference between an error boundary catching a render error and a global window error handler?','A window.onerror or window.addEventListener(''error'', ...) handler catches uncaught errors anywhere in the page''s JavaScript, including outside React entirely, but can''t stop React from unmounting the broken tree or show a React-rendered fallback in its place. An error boundary is specifically integrated into React''s render lifecycle, so it can catch the error before the broken UI is committed and swap in a fallback UI seamlessly within the component tree.',null,null,null,'medium',ARRAY['react','advanced','error-boundaries']::text[],false,138),
('react','advanced-react','react-advanced-react-139','How can compound components expose an imperative escape hatch (like a ref-based API) alongside their declarative props?','The root compound component can accept a ref via forwardRef and use useImperativeHandle to expose specific imperative methods (like next() or reset() for a carousel), giving consumers a declarative props-based API for the common case while still allowing direct imperative control for edge cases that don''t fit cleanly into props.',null,null,null,'hard',ARRAY['react','advanced','compound-components']::text[],false,139),
('react','advanced-react','react-advanced-react-140','Why might two components with the exact same output still be treated differently by React''s reconciler based on how they''re defined?','If a component function is defined inside another component''s body (re-created on every parent render), React sees a new component type on every render — even though the JSX output looks identical — and will unmount/remount it instead of updating it in place, silently resetting its state. Defining components at the module level (outside other components) avoids this trap.',null,'// Bug: Inner defined inside Outer — new type every render, resets state
function Outer() {
  function Inner() { const [n, setN] = useState(0); return <button onClick={() => setN(n+1)}>{n}</button>; }
  return <Inner />;
}','jsx','hard',ARRAY['react','advanced','reconciliation']::text[],true,140),
('react','advanced-react','react-advanced-react-141','What role does the ErrorBoundary''s getDerivedStateFromError differ from componentDidCatch?','getDerivedStateFromError is called during the render phase and should be a pure function that returns new state to trigger the fallback UI — it can''t perform side effects. componentDidCatch is called during the commit phase and is where side effects belong, such as logging the error to a monitoring service; most error boundaries implement both, one to render the fallback and the other to report the error.',null,'class ErrorBoundary extends React.Component {
  state = { hasError: false };
  static getDerivedStateFromError() { return { hasError: true }; }
  componentDidCatch(error, info) { logErrorToService(error, info); }
  render() { return this.state.hasError ? <Fallback /> : this.props.children; }
}','jsx','medium',ARRAY['react','advanced','error-boundaries']::text[],false,141),
('react','advanced-react','react-advanced-react-142','How would you structure a custom hook so it can be safely called conditionally, given the rules of hooks forbid conditional hook calls?','You can''t call a hook conditionally, but you can make the hook itself always run and internally decide whether to do work based on an argument — for example, a useFetch(url) hook can always be called, but skip the actual fetch inside its effect if url is null, keeping the hook call itself unconditional while its behavior is effectively conditional.',null,'function useFetch(url) {
  useEffect(() => {
    if (!url) return; // conditional behavior, not a conditional hook call
    fetch(url);
  }, [url]);
}','jsx','medium',ARRAY['react','advanced','hooks']::text[],false,142),
('react','advanced-react','react-advanced-react-143','What is the difference between React''s legacy Context API (contextTypes) and the modern createContext API?','The legacy API required class components to declare contextTypes and read context off this.context, worked only with class components, and had no way to opt out of re-rendering when unrelated context changed. The modern createContext/useContext API works in both function and class components, supports multiple independent contexts cleanly via nesting, and is the only option going forward since the legacy API is deprecated.',null,null,null,'easy',ARRAY['react','advanced','context']::text[],false,143),
('react','advanced-react','react-advanced-react-144','How would you build a generic useLocalStorage custom hook that keeps React state in sync with localStorage?','The hook initializes state by reading from localStorage (falling back to a default if nothing is stored or parsing fails), and wraps the setter so every update also writes the new value back to localStorage, wrapped in try/catch since storage access can throw (private browsing, quota exceeded, or a non-browser environment during SSR).',null,'function useLocalStorage(key, defaultValue) {
  const [value, setValue] = useState(() => {
    try { return JSON.parse(localStorage.getItem(key)) ?? defaultValue; }
    catch { return defaultValue; }
  });
  useEffect(() => {
    try { localStorage.setItem(key, JSON.stringify(value)); } catch {}
  }, [key, value]);
  return [value, setValue];
}','jsx','medium',ARRAY['react','advanced','custom-hooks']::text[],false,144),
('react','advanced-react','react-advanced-react-145','Why is it recommended to keep error boundaries relatively granular rather than having just one at the app root?','A single root-level error boundary means any unhandled error anywhere in the app replaces the entire UI with a fallback, even if the error occurred in a small, non-critical widget. Placing boundaries around independent sections (a sidebar, a single card in a dashboard) contains failures to just that section, letting the rest of the app keep working.',null,null,null,'medium',ARRAY['react','advanced','error-boundaries']::text[],false,145),
('react','advanced-react','react-advanced-react-146','What does it mean for a custom hook to "leak" implementation details, and how would you avoid it?','A hook leaks details when its return value or required arguments expose internals that force consumers to understand how it works internally (like returning a raw dispatch function alongside state, or requiring a specific shape only relevant to the hook''s own logic) rather than a clean, purpose-built API. Designing the hook''s public surface around what the consumer actually needs to do (return an updater function with a clear name, not raw internals) keeps it reusable and easy to refactor later.',null,null,null,'medium',ARRAY['react','advanced','custom-hooks']::text[],false,146),
('react','advanced-react','react-advanced-react-147','How would you type a compound component''s subcomponents so TypeScript knows Tabs.Tab and Tabs.List are valid?','You attach the subcomponents as static properties on the root component and declare that shape in the type, e.g. by intersecting the root component''s function type with an object type listing each subcomponent, so Tabs.Tab and Tabs.List are recognized both at runtime (as actual properties) and by the type checker.',null,'const Tabs = Object.assign(TabsRoot, { List: TabsList, Tab: TabsTab });
// Tabs.List and Tabs.Tab are now valid, type-checked usages','tsx','medium',ARRAY['react','advanced','typescript']::text[],false,147),
('react','advanced-react','react-advanced-react-148','What is the difference between a controlled component re-rendering on every keystroke and how that scales in a large form?','A single controlled input re-rendering on every keystroke is cheap, but if that state lives at the top of a large form and every field is controlled from the same parent, every keystroke re-renders the entire form and all its fields, not just the one being typed into. Splitting form state per-field (or using a form library with field-level subscriptions) keeps each keystroke''s re-render scoped to just the relevant field.',null,null,null,'medium',ARRAY['react','advanced','forms']::text[],false,148),
('react','advanced-react','react-advanced-react-149','Why might you wrap a list of Fragments with keys instead of wrapping each group in an extra <div>?','React.Fragment (or the <>...</> shorthand) lets a component return multiple sibling elements without adding an extra wrapping DOM node, which matters when that wrapper would break CSS layout (like flex or grid) that depends on direct-child relationships. When rendering a list of fragments, each still needs a key (using the explicit <React.Fragment key={id}> form, since the shorthand <> syntax doesn''t accept props).',null,'{items.map((item) => (
  <React.Fragment key={item.id}>
    <dt>{item.term}</dt>
    <dd>{item.definition}</dd>
  </React.Fragment>
))}','jsx','easy',ARRAY['react','advanced','fragments']::text[],false,149),
('react','advanced-react','react-advanced-react-150','What''s the difference between defaultProps and using default parameter values in a function component?','defaultProps is set as a static property on the component and works for both class and function components, but is being phased out for function components in favor of plain JavaScript default parameter values in the function signature, which are simpler, more idiomatic, and work identically for destructured props. Default parameters are now the recommended approach; defaultProps still exists mainly for backward compatibility and class components.',null,'// Preferred
function Avatar({ size = 40, alt = ''User avatar'' }) { /* ... */ }

// Legacy
function Avatar({ size, alt }) { /* ... */ }
Avatar.defaultProps = { size: 40, alt: ''User avatar'' };','jsx','easy',ARRAY['react','advanced','props']::text[],false,150),
('react','components','react-components-101','What does it mean for a component to be ''pure'' in React?','A pure component always renders the same output given the same props and state, with no side effects during rendering (no mutating external variables, no network calls in the render body itself). Purity is what makes optimizations like React.memo safe — React can skip re-rendering a pure component when its inputs haven''t changed, because it trusts the output would be identical anyway.',null,null,null,'medium',ARRAY['react','components','purity']::text[],true,101),
('react','components','react-components-102','What is the presentational vs. container component pattern?','A presentational component focuses purely on how things look — it receives data and callbacks via props and has little to no internal logic. A container component focuses on how things work — it manages state, fetches data, and passes the results down to presentational components to render. This separation makes presentational components easy to reuse and test in isolation, though hooks have made the pattern less rigid than it used to be.',null,null,null,'medium',ARRAY['react','components','patterns']::text[],false,102),
('react','components','react-components-103','What is the compound components pattern?','Compound components are a set of components designed to be used together, sharing implicit state (usually via context) so the parent can expose a flexible, declarative API instead of one giant component with many boolean props. A classic example is a <Tabs> component that exposes <Tabs.List>, <Tabs.Tab>, and <Tabs.Panel> as related pieces that communicate behind the scenes.',null,'<Tabs defaultValue="a">
  <Tabs.List>
    <Tabs.Tab value="a">First</Tabs.Tab>
    <Tabs.Tab value="b">Second</Tabs.Tab>
  </Tabs.List>
  <Tabs.Panel value="a">Content A</Tabs.Panel>
  <Tabs.Panel value="b">Content B</Tabs.Panel>
</Tabs>','jsx','hard',ARRAY['react','components','patterns']::text[],true,103),
('react','components','react-components-104','Why does React favor composition over inheritance for reusing component logic?','React components rarely benefit from a class-inheritance hierarchy the way traditional OOP UI toolkits did, because most reuse needs (sharing markup, injecting behavior, wrapping with extra functionality) are better solved by passing components as props or children (composition) than by extending a base component class. The React team has explicitly recommended composition over inheritance since early React documentation.',null,null,null,'easy',ARRAY['react','components','composition']::text[],false,104),
('react','components','react-components-105','How do you provide default values for props in a functional component?','The idiomatic way is to use a default parameter value when destructuring props in the function signature, so the default only applies when the prop is undefined. The older defaultProps static property still works on function components but is deprecated in modern React in favor of default parameters.',null,'function Button({ label = ''Submit'', variant = ''primary'' }) {
  return <button className={variant}>{label}</button>;
}','jsx','easy',ARRAY['react','components','props']::text[],false,105),
('react','components','react-components-106','How can you use props.children to build a generic wrapper component?','By accepting children and rendering it somewhere inside your own markup, a component can wrap arbitrary content with consistent styling or behavior without needing to know what that content actually is. This is how generic components like Card, Modal, or Layout are usually implemented.',null,'function Panel({ title, children }) {
  return (
    <section className="panel">
      <h2>{title}</h2>
      {children}
    </section>
  );
}','jsx','easy',ARRAY['react','components','children']::text[],false,106),
('react','components','react-components-107','Why must React component names start with a capital letter?','JSX uses capitalization to distinguish between a built-in DOM tag and a custom component: a lowercase tag like <div> compiles to a plain string type (''div''), while a capitalized tag like <Button> compiles to a reference to the Button variable. If a component were named lowercase, JSX would treat it as an unknown HTML tag instead of your component.',null,'function button() { return <button />; }
<button />   // renders a native <button>, NOT your component!

function Button() { return <button />; }
<Button />   // correctly renders your component','jsx','easy',ARRAY['react','components','jsx']::text[],false,107),
('react','components','react-components-108','How do you conditionally render an entire component with an early return?','Instead of nesting a ternary deep inside JSX, a component can return early — for example returning null or a loading indicator before its main return statement — to keep the primary render logic flat and readable when a whole branch of the UI should be skipped.',null,'function UserProfile({ user }) {
  if (!user) return <p>No user selected.</p>;
  return <h1>{user.name}</h1>;
}','jsx','easy',ARRAY['react','components','conditional-rendering']::text[],false,108),
('react','components','react-components-109','How do you use Fragments with keys when a list item renders multiple elements?','When each item in a mapped list needs to render more than one sibling element (for example a <dt>/<dd> pair), you can''t wrap them in a keyed <div> without affecting layout, so you use an explicit <React.Fragment key={...}> instead — the shorthand <> </> syntax doesn''t support the key prop.',null,'{terms.map(t => (
  <React.Fragment key={t.id}>
    <dt>{t.term}</dt>
    <dd>{t.definition}</dd>
  </React.Fragment>
))}','jsx','medium',ARRAY['react','components','lists']::text[],false,109),
('react','components','react-components-110','What is forwardRef and when would you use it?','By default, function components don''t accept a ref — refs passed to them are ignored. forwardRef wraps a component to receive the ref as a second argument and manually attach it to an inner DOM node, which is needed when you build a reusable input-like component and want consumers to still be able to focus/measure the underlying element directly.',null,'const FancyInput = forwardRef((props, ref) => (
  <input ref={ref} className="fancy" {...props} />
));

// usage
const ref = useRef(null);
<FancyInput ref={ref} />','jsx','hard',ARRAY['react','components','refs']::text[],true,110),
('react','components','react-components-111','What does useImperativeHandle do, and how does it relate to forwardRef?','useImperativeHandle customizes what value a ref exposes when used together with forwardRef — instead of exposing the raw DOM node, the component can expose a curated object of specific methods (e.g. { focus, reset }), hiding internal implementation details from the parent. It''s used sparingly, since exposing imperative APIs works against React''s declarative model and should be reserved for cases that genuinely need it.',null,'const CustomInput = forwardRef((props, ref) => {
  const inputRef = useRef(null);
  useImperativeHandle(ref, () => ({
    focus: () => inputRef.current.focus(),
  }));
  return <input ref={inputRef} {...props} />;
});','jsx','hard',ARRAY['react','components','refs']::text[],false,111),
('react','components','react-components-112','How can a component expose ''slots'' of custom JSX via named props?','When a component needs more than one distinct region of custom content (not just a single children block), it can accept multiple props that are themselves JSX, such as header and footer, and render each in its designated spot. This is a common way to build layout components with more structure than plain children allows.',null,'function Dialog({ header, children, footer }) {
  return (
    <div className="dialog">
      <div className="dialog-header">{header}</div>
      <div className="dialog-body">{children}</div>
      <div className="dialog-footer">{footer}</div>
    </div>
  );
}','jsx','medium',ARRAY['react','components','composition']::text[],false,112),
('react','components','react-components-113','What does it mean to think of components as a ''design system''?','A design system is a shared library of consistent, reusable components (buttons, inputs, cards) with a well-defined API and consistent styling, used across an entire product or organization instead of each team reinventing its own version. Treating components this way encourages thinking about props as a stable public contract, since many unrelated features may depend on the same component.',null,null,null,'easy',ARRAY['react','components','design-systems']::text[],false,113),
('react','components','react-components-114','How would you render a dynamic list of controlled text inputs, each with its own value?','Keep an array (or object keyed by id) in state representing each input''s current value, render one controlled <input> per array item using its index or id as the key, and update just that one entry in the array (immutably) in the onChange handler rather than replacing the whole array with a single new value.',null,'function updateField(index, value) {
  setFields(prev => prev.map((f, i) => (i === index ? value : f)));
}

fields.map((value, i) => (
  <input key={i} value={value} onChange={e => updateField(i, e.target.value)} />
))','jsx','medium',ARRAY['react','components','forms']::text[],false,114),
('react','components','react-components-115','What are some component-design considerations when building a multi-step form?','A common approach is to keep the overall form data and current step index in the parent, and render one ''step'' component at a time based on that index, passing down the relevant slice of data plus onNext/onBack callbacks. Each step component stays focused on its own fields, while validation can happen per-step (before allowing ''Next'') or all at once on final submit, depending on the desired UX.',null,null,null,'medium',ARRAY['react','components','forms']::text[],false,115),
('react','components','react-components-116','What''s the difference between a ''wrapper'' component built through composition versus a higher-order component?','A composition-based wrapper simply renders its children (or a specific prop) inside some extra markup or context provider — you use it directly in JSX like any other component. A higher-order component (HOC) is a function that takes a component and returns a brand new component with injected props, so you use its output rather than nesting the original component as a child — composition is generally simpler and easier to trace than HOCs for most cases.',null,'// Composition-based wrapper (simpler)
function Centered({ children }) {
  return <div className="centered">{children}</div>;
}
<Centered><Form /></Centered>','jsx','medium',ARRAY['react','components','composition','hoc']::text[],false,116),
('react','components','react-components-117','How does the single responsibility principle apply to React components?','Each component should ideally be responsible for one clear piece of UI or behavior — for example, separating a data-fetching concern from a purely presentational list item — rather than one component trying to handle layout, data fetching, and business logic all at once. This makes components easier to test, reuse, and reason about in isolation.',null,null,null,'easy',ARRAY['react','components','design']::text[],false,117),
('react','components','react-components-118','How can component composition (children as props) help avoid prop drilling, as an alternative to Context?','Instead of passing a deeply-nested piece of data down through many layers as a prop, a parent that already has that data can render the deeply-nested component itself and pass it in as children (or another JSX prop) to an intermediate component, which just renders {children} without needing to know about that data at all. This sidesteps drilling through components that don''t actually use the value, without introducing Context.',null,'// Instead of: <Layout data={data} /> forwarding data through many layers
function App() {
  return <Layout><Sidebar data={data} /></Layout>;
}
function Layout({ children }) {
  return <div className="layout">{children}</div>; // never touches `data`
}','jsx','medium',ARRAY['react','components','composition']::text[],false,118),
('react','components','react-components-119','Why does wrapping a child component in React.memo sometimes fail to prevent re-renders?','React.memo does a shallow comparison of props, so if a parent passes a new object, array, or inline function literal as a prop on every render (even with the same logical contents), the shallow comparison sees a different reference and re-renders the child anyway. Fixing this usually means memoizing that prop value with useMemo/useCallback in the parent, or restructuring so the prop is a stable primitive.',null,'// Breaks memoization: new object every render
<Child style={{ color: ''red'' }} />

// Fixed: stable reference
const style = useMemo(() => ({ color: ''red'' }), []);
<Child style={style} />','jsx','medium',ARRAY['react','components','performance','memo']::text[],true,119),
('react','components','react-components-120','Why can passing a new object or array literal as a prop on every render hurt performance?','Even if two object literals are structurally identical, they are different references in memory, so any child relying on reference equality (React.memo, a useEffect dependency, useMemo) treats them as ''changed'' every single render. This can cause unnecessary re-renders or effect re-runs that wouldn''t happen if the same reference were reused.',null,'// New array reference every render, even though contents are the same
<List items={[1, 2, 3]} />','jsx','hard',ARRAY['react','components','performance']::text[],false,120),
('react','components','react-components-121','What are common conventions for organizing component files in a React project?','Common approaches include grouping by feature/domain (each feature folder contains its own components, hooks, and styles) or grouping by type (a top-level components/, hooks/, pages/ split). Most teams also co-locate a component''s styles, tests, and sub-components in the same folder as the component itself, rather than scattering related files across the project.',null,null,null,'easy',ARRAY['react','components','project-structure']::text[],false,121),
('react','components','react-components-122','What does it mean to ''colocate'' state near where it''s used?','Colocating state means keeping a piece of state in the lowest (most local) component that actually needs it, rather than defaulting to lifting everything to a top-level parent or global store. This keeps unrelated parts of the tree from re-rendering when that state changes, and keeps the component''s logic easier to understand in isolation.',null,null,null,'medium',ARRAY['react','components','state']::text[],false,122),
('react','components','react-components-123','When should you extract a piece of JSX into its own component versus keeping it inline?','Good signals to extract a component include the JSX being reused in more than one place, the block being complex enough to hurt readability where it is, or the block needing its own isolated state or memoization. If none of those apply, keeping markup inline is often simpler than introducing an extra file and prop-passing overhead for no real benefit.',null,null,null,'easy',ARRAY['react','components','design']::text[],false,123),
('react','components','react-components-124','What''s the convention for naming event handler props on a custom component?','By convention, a component exposes an event-like prop as onSomething (e.g. onSave, onClose, onSelect) mirroring the native DOM onClick/onChange naming, while the internal function that implements the behavior is usually named handleSomething. Following this convention makes a component''s API predictable to anyone familiar with React norms.',null,'function Modal({ onClose }) {
  function handleBackdropClick() {
    onClose();
  }
  return <div onClick={handleBackdropClick}>...</div>;
}','jsx','easy',ARRAY['react','components','conventions']::text[],false,124),
('react','components','react-components-125','How do you make a component render nothing under certain conditions?','A component can return null (or false, or undefined) from its render function, and React simply renders nothing to the DOM for it — no wrapper element, no placeholder. This is commonly used for components that should conditionally ''disappear'' entirely, like a banner that''s dismissed.',null,'function Banner({ visible, message }) {
  if (!visible) return null;
  return <div className="banner">{message}</div>;
}','jsx','easy',ARRAY['react','components','conditional-rendering']::text[],false,125),
('react','components','react-components-126','Does it matter whether you write <Input /> or <Input></Input> in JSX?','No — for a component or element with no children, the self-closing form <Input /> and the explicit open/close form <Input></Input> compile to the exact same output. Self-closing is just the conventional, more concise style for childless elements; JSX requires closing every tag one way or the other, unlike HTML where some tags can be left unclosed.',null,null,null,'easy',ARRAY['react','components','jsx']::text[],false,126),
('react','components','react-components-127','How do you forward unknown ''rest'' props onto the underlying DOM element in a wrapper component?','Destructure the props you specifically care about, then spread the remaining rest object onto the DOM element, so consumers can still pass through standard HTML attributes (id, aria-label, data-* attributes, etc.) without the wrapper having to explicitly declare every single one.',null,'function PrimaryButton({ variant, children, ...rest }) {
  return (
    <button className={`btn btn-${variant}`} {...rest}>
      {children}
    </button>
  );
}','jsx','medium',ARRAY['react','components','props']::text[],false,127),
('react','components','react-components-128','How should a custom component handle accessibility props like aria-label?','A well-designed custom component should forward standard ARIA attributes through to the underlying native element (usually via rest-prop spreading) rather than swallowing them, so consumers can still make the component accessible in context. Some components also set sensible default ARIA attributes internally (like role and aria-expanded on a custom dropdown) so consumers don''t have to wire up accessibility from scratch every time.',null,'function IconButton({ label, icon, ...rest }) {
  return (
    <button aria-label={label} {...rest}>
      {icon}
    </button>
  );
}','jsx','medium',ARRAY['react','components','accessibility']::text[],false,128),
('react','components','react-components-129','What''s the general philosophy behind testing React components?','The widely recommended approach (popularized by Testing Library) is to test components the way a user would interact with them — querying by visible text or accessible roles and simulating clicks/typing — rather than reaching into internal implementation details like component state or instance methods. This makes tests more resilient to refactors, since they only break when actual user-facing behavior changes, not when internals are reorganized.',null,null,null,'medium',ARRAY['react','components','testing']::text[],false,129),
('react','components','react-components-130','Why did class components require render() to be a pure function, and how does that carry over to function components?','React could call render() multiple times (or not at all, if it decided the output wasn''t needed) as part of its internal scheduling, so relying on side effects inside render() led to unpredictable behavior. The same rule carries over to function components: the function body itself (everything outside of useEffect) must stay free of side effects, with useEffect being the designated place for anything that needs to interact with the outside world.',null,null,null,'hard',ARRAY['react','components','purity']::text[],false,130),
('react','components','react-components-131','What is the ''children as a function'' (render prop) pattern at a basic level?','Instead of passing static JSX as children, you pass a function as children, and the component calls that function with some internal data, letting the caller decide exactly what to render with it. It''s less common today since custom hooks solve the same ''share stateful logic'' problem with less nesting, but it still appears in some libraries.',null,'function MouseTracker({ children }) {
  const [pos, setPos] = useState({ x: 0, y: 0 });
  return (
    <div onMouseMove={e => setPos({ x: e.clientX, y: e.clientY })}>
      {children(pos)}
    </div>
  );
}

<MouseTracker>{pos => <p>{pos.x}, {pos.y}</p>}</MouseTracker>','jsx','medium',ARRAY['react','components','patterns','render-props']::text[],false,131),
('react','components','react-components-132','How do you conditionally apply CSS classes based on props or state in JSX?','For a couple of simple conditions, template literals or ternaries are enough; for anything more complex, a small utility like clsx or classnames is commonly used to combine class names based on truthy values, which reads more clearly than manually concatenating strings.',null,'import clsx from ''clsx'';

<button className={clsx(''btn'', { ''btn-active'': isActive, ''btn-disabled'': disabled })} />','jsx','easy',ARRAY['react','components','styling']::text[],false,132),
('react','components','react-components-133','What''s the difference between using inline styles and CSS classes in a React component?','Inline styles (style={{ color: ''red'' }}) are a JavaScript object applied directly to the element''s style attribute, useful for one-off dynamic values computed at runtime, but they can''t use pseudo-classes, media queries, or be overridden by normal CSS specificity rules as easily. CSS classes (via plain CSS, CSS Modules, or a CSS-in-JS library) support the full power of CSS and are generally preferred for anything beyond a truly dynamic, per-instance value.',null,'// Inline: fine for one dynamic value
<div style={{ width: `${progress}%` }} />

// Class: fine for everything else
<div className="progress-bar" />','jsx','easy',ARRAY['react','components','styling']::text[],false,133),
('react','components','react-components-134','How would you pass a whole component (not just data) as a prop to another component?','Because components are just values (function references) in React, you can pass a component itself as a prop and render it with JSX syntax inside the receiving component, which is useful for injecting a customizable piece of UI, like letting a List component accept an ItemComponent to render for each row.',null,'function List({ items, ItemComponent }) {
  return items.map(item => <ItemComponent key={item.id} item={item} />);
}

<List items={users} ItemComponent={UserRow} />','jsx','medium',ARRAY['react','components','composition']::text[],false,134),
('react','components','react-components-135','What''s the difference between a fully controlled input and one using defaultValue?','A fully controlled input''s value always comes from state, and every keystroke must go through onChange to update that state before the input reflects it. An input with defaultValue is uncontrolled — React sets its initial value once and then lets the DOM manage the value itself afterward, which is useful when you don''t need to react to every change, just read the value later (e.g. via a ref or on submit).',null,'// Uncontrolled: React doesn''t track keystrokes
<input defaultValue="Jane" ref={inputRef} />','jsx','medium',ARRAY['react','components','forms']::text[],false,135),
('react','components','react-components-136','Why does an event handler prop passed to a memoized child need a stable reference?','If a memoized child is wrapped in React.memo but receives an inline arrow function as a prop (e.g. onClick={() => doThing()}), a new function is created on every parent render, which breaks the shallow-equality check React.memo relies on and causes the child to re-render anyway. Wrapping the handler in useCallback in the parent keeps the same function reference across renders (as long as its own dependencies don''t change) so the memoized child can actually skip re-rendering.',null,'const handleClick = useCallback(() => setCount(c => c + 1), []);
<MemoizedButton onClick={handleClick} />','jsx','hard',ARRAY['react','components','performance','usecallback']::text[],false,136),
('react','components','react-components-137','Is prop validation still worth doing in a TypeScript React project?','TypeScript catches type mismatches at compile time, but it can''t validate data coming from truly dynamic runtime sources like an API response or third-party integration whose shape wasn''t guaranteed. For components consuming genuinely untrusted external data, a lightweight runtime check (or a schema validation library) can still add a useful safety net that pure compile-time types can''t provide.',null,null,null,'medium',ARRAY['react','components','typescript']::text[],false,137),
('react','components','react-components-138','What''s the trade-off between using named exports and a default export for a component?','A default export lets the importer name the component anything they want on import, which can lead to inconsistent naming across a codebase and makes some ''find all usages'' tooling less reliable. Named exports enforce a single consistent name everywhere and make auto-import tooling more predictable, which is why many style guides now prefer named exports for components, especially in larger codebases.',null,'// Named export (consistent name everywhere it''s imported)
export function Button() { /* ... */ }
import { Button } from ''./Button'';','jsx','easy',ARRAY['react','components','conventions']::text[],false,138),
('react','components','react-components-139','What is a polymorphic component in React?','A polymorphic component accepts an ''as'' prop (or similar) that lets the caller choose which underlying HTML element or component it renders as, while keeping the same styling and behavior — for example, a Text component that can render as a <p>, <span>, or <h1> depending on context, without needing a separate component for each.',null,'function Text({ as: Tag = ''p'', children, ...rest }) {
  return <Tag {...rest}>{children}</Tag>;
}

<Text as="h1">Title</Text>
<Text as="span">Inline text</Text>','jsx','hard',ARRAY['react','components','patterns']::text[],false,139),
('react','components','react-components-140','What API design decisions matter most when building a reusable Modal component?','A typical Modal needs at minimum an isOpen boolean and an onClose callback so the parent stays in control of when it''s shown, plus children for its content — implementation details like rendering via a portal, trapping focus, and closing on Escape/backdrop click should live inside the Modal itself so every consumer gets that behavior for free rather than reimplementing it.',null,'<Modal isOpen={isOpen} onClose={() => setIsOpen(false)}>
  <p>Are you sure?</p>
</Modal>','jsx','medium',ARRAY['react','components','design']::text[],false,140),
('react','components','react-components-141','What does it mean to separate a component''s ''props API'' from its implementation details?','A component''s props are its public contract with consumers — what it accepts and what it promises to do with it — while everything inside the component (how it''s structured internally, what hooks it uses, whether it renders one element or ten) is an implementation detail that should be free to change without breaking anyone using the component. Good component design keeps that props API small, stable, and intention-revealing, independent of internal refactors.',null,null,null,'medium',ARRAY['react','components','design']::text[],false,141),
('react','components','react-components-142','When rendering a list of togglable items, should each item''s open/closed state live in the parent or in each item component?','If only one item can be open at a time (like an accordion), the ''which item is open'' state needs to live in the parent so it can enforce that constraint across siblings. If each item toggles completely independently with no relationship to its siblings, colocating the open/closed state inside each item component is simpler and avoids unnecessary parent re-renders.',null,null,null,'medium',ARRAY['react','components','state']::text[],false,142),
('react','components','react-components-143','What''s the benefit of splitting a large form into smaller field components?','Splitting a form into per-field components (e.g. a TextField, SelectField) lets each field manage its own local UI concerns (like showing/hiding a validation message) and can limit re-renders to just the field that changed rather than the entire form, while also making the individual field markup reusable across different forms in the app.',null,null,null,'easy',ARRAY['react','components','forms']::text[],false,143),
('react','components','react-components-144','Is it possible to over-fragment a UI into too many small components?','Yes — splitting every few lines of JSX into its own component can add indirection (having to jump between many files to follow one flow) and prop-passing overhead without meaningfully improving reuse or clarity. Extraction should be driven by genuine reuse, complexity, or isolation needs (see when-to-extract guidance) rather than an arbitrary rule like ''components should be under N lines.''',null,null,null,'medium',ARRAY['react','components','design']::text[],false,144),
('react','components','react-components-145','How would you render something only in the browser, not during server rendering, inside a component?','Check for the browser environment (commonly via a mounted flag set in a useEffect, since useEffect only runs on the client) and conditionally render the browser-only content once that flag is true, rather than checking typeof window directly during render, which would cause a mismatch between server and client output.',null,'const [mounted, setMounted] = useState(false);
useEffect(() => setMounted(true), []);
if (!mounted) return null;
return <BrowserOnlyWidget />;','jsx','medium',ARRAY['react','components','ssr']::text[],false,145),
('react','components','react-components-146','How did the render props pattern''s nesting problem lead to the adoption of custom hooks?','Sharing multiple pieces of stateful logic via render props meant nesting each provider component inside the next (''wrapper hell''), which made the JSX tree deep and hard to follow even though none of those wrappers rendered any actual visible markup. Custom hooks solve the same reuse problem by letting a component call several hooks side by side as plain function calls, with no extra nesting or indirection in the render tree at all.',null,null,null,'medium',ARRAY['react','components','patterns','hooks']::text[],false,146),
('react','components','react-components-147','How can a component expose multiple distinct ''slots'' of JSX beyond a single children prop?','Define separate named props for each slot (e.g. leftIcon, rightIcon, title) that each accept JSX, and render them in their respective positions — this is useful when a component''s layout has more than one meaningful content region and a single children block wouldn''t convey which content goes where.',null,'function ListItem({ leftIcon, title, rightIcon }) {
  return (
    <div className="list-item">
      {leftIcon}
      <span>{title}</span>
      {rightIcon}
    </div>
  );
}','jsx','medium',ARRAY['react','components','composition']::text[],false,147),
('react','components','react-components-148','What''s the difference between a component definition and a component instance?','A component definition (the function itself, e.g. function Button() {...}) is a single reusable blueprint. Each time you write <Button /> in JSX, React creates a separate instance of it with its own independent state — rendering <Button /> three times produces three distinct instances, each unaware of the others'' state, even though they all share the same underlying definition.',null,'<div>
  <Button label="A" />
  <Button label="B" />
  <Button label="C" />
</div> // 3 independent instances of the same Button definition','jsx','medium',ARRAY['react','components','fundamentals']::text[],false,148),
('react','components','react-components-149','What actually happens if you ignore React''s ''each child in a list should have a unique key'' warning?','The list still renders, but React falls back to matching items by their position in the array, which can cause state to attach to the wrong item after an insertion, removal, or reorder — for example, a text input''s typed value can visibly ''jump'' to a different row if an earlier item is removed from the array. Fixing the warning by adding a stable, unique key eliminates this class of bug.',null,null,null,'easy',ARRAY['react','components','keys','debugging']::text[],true,149),
('react','components','react-components-150','What are the three high-level lifecycle stages every component goes through?','Mounting is when a component is created and inserted into the DOM for the first time. Updating is when it re-renders in response to changed props, state, or context. Unmounting is when it''s removed from the DOM entirely. In function components these map to useEffect running after mount/update, and its cleanup function running before unmount or before the effect re-runs.',null,null,null,'easy',ARRAY['react','components','lifecycle']::text[],true,150),
('react','fundamentals','react-fundamentals-101','What does it mean that React uses a declarative programming model?','In a declarative model you describe what the UI should look like for a given state, and React figures out how to update the DOM to match. This contrasts with an imperative model, where you manually write step-by-step instructions to create, update, and remove DOM nodes yourself.',null,null,null,'easy',ARRAY['react','fundamentals']::text[],false,101),
('react','fundamentals','react-fundamentals-102','What is meant by React''s component tree and unidirectional data flow?','A React application is structured as a tree of nested components, with data (props) flowing from parent to child in one direction only. A child cannot directly modify data owned by a parent; it can only request a change by calling a function the parent passed down, which keeps data flow predictable and easy to trace.',null,null,null,'easy',ARRAY['react','fundamentals','data-flow']::text[],false,102),
('react','fundamentals','react-fundamentals-103','What does JSX actually compile down to?','JSX is syntactic sugar that a build tool (Babel or the Vite/SWC compiler) transforms into plain JavaScript function calls before it runs in the browser. Historically it compiled to React.createElement() calls; since the ''automatic'' JSX runtime (React 17+), it compiles to calls into react/jsx-runtime instead, which is slightly more efficient and no longer requires importing React just to use JSX.',null,'// JSX
const el = <h1 className="title">Hello</h1>;

// Compiles to (classic runtime)
const el = React.createElement(''h1'', { className: ''title'' }, ''Hello'');','jsx','medium',ARRAY['react','fundamentals','jsx']::text[],false,103),
('react','fundamentals','react-fundamentals-104','What are React Fragments and why do they matter?','A Fragment (<React.Fragment> or the <>...</> shorthand) lets a component return multiple sibling elements without wrapping them in an extra DOM node. Before fragments existed, every component had to return a single root element, which often forced unnecessary wrapper divs that could break CSS layouts like flexbox or grid.',null,'function Row() {
  return (
    <>
      <td>Name</td>
      <td>Age</td>
    </>
  );
}','jsx','easy',ARRAY['react','fundamentals','jsx']::text[],true,104),
('react','fundamentals','react-fundamentals-105','What''s a common pitfall when using the && operator for conditional rendering?','If the left-hand value is a falsy number like 0 (e.g. items.length && <List />), React renders the literal 0 to the screen instead of nothing, because 0 is a valid renderable child, not just a ''falsy'' value that gets skipped. The fix is to coerce to a boolean first, e.g. items.length > 0 && <List />.',null,'// Bug: renders "0" when the array is empty
{items.length && <List items={items} />}

// Fixed
{items.length > 0 && <List items={items} />}','jsx','medium',ARRAY['react','fundamentals','jsx','gotcha']::text[],false,105),
('react','fundamentals','react-fundamentals-106','What are the common ways to do conditional rendering in React?','Common patterns include the && operator for ''render this or nothing'', the ternary operator for ''render this or that'', and an early return statement inside the component function when an entire branch of markup should be skipped. Which one to use is mostly a readability choice based on how many conditions and how much markup are involved.',null,null,null,'easy',ARRAY['react','fundamentals','jsx']::text[],false,106),
('react','fundamentals','react-fundamentals-107','How do you render a list of items in React?','Call .map() on an array to transform each item into a JSX element, and return the resulting array directly inside your JSX — React renders arrays of elements automatically. Each element in the resulting array needs a unique key prop so React can track it correctly across re-renders.',null,'<ul>
  {users.map((user) => (
    <li key={user.id}>{user.name}</li>
  ))}
</ul>','jsx','easy',ARRAY['react','fundamentals','lists']::text[],false,107),
('react','fundamentals','react-fundamentals-108','What does React.StrictMode do, and why does it double-invoke things in development?','StrictMode is a wrapper component that opts a subtree into extra development-only checks, such as warning about deprecated APIs and unsafe lifecycle methods. In development it intentionally double-invokes things like component function bodies and some effects to help surface side effects that aren''t properly cleaned up — it has no effect in production builds.',null,null,null,'medium',ARRAY['react','fundamentals','strict-mode']::text[],true,108),
('react','fundamentals','react-fundamentals-109','At a conceptual level, how does React''s reconciliation algorithm decide what to update?','Reconciliation is the process where React compares a newly rendered tree of elements against the previous one to compute the minimal set of real DOM changes needed. It uses heuristics to keep this fast: elements of different types are treated as entirely different subtrees and rebuilt from scratch, while elements of the same type at the same position are diffed prop-by-prop and their children are reconciled recursively, using keys to match items in a list across renders.',null,null,null,'hard',ARRAY['react','fundamentals','reconciliation','internals']::text[],true,109),
('react','fundamentals','react-fundamentals-110','How does React fit into building a single-page application (SPA)?','React itself only handles rendering the view layer for a given piece of state — it doesn''t include routing or data fetching out of the box. Building a full SPA typically means combining React with a router (like React Router) to handle client-side navigation without full page reloads, plus a data-fetching strategy, since React alone is just the UI library at the core.',null,null,null,'easy',ARRAY['react','fundamentals','spa']::text[],false,110),
('react','fundamentals','react-fundamentals-111','What is React''s synthetic event system?','React wraps native browser DOM events in a cross-browser SyntheticEvent object that normalizes differences between browsers and exposes the same API everywhere. Since React 17, these events are attached at the root of the rendered tree rather than on document, which made it easier to embed multiple React versions on the same page.',null,null,null,'medium',ARRAY['react','fundamentals','events']::text[],false,111),
('react','fundamentals','react-fundamentals-112','Conceptually, why does reconciliation need keys at all?','Without keys, React has no reliable way to tell whether an item in a list was added, removed, or reordered versus simply changed — it would have to guess based on position alone, which can mismatch component state to the wrong data after a list changes. A stable, unique key gives React an explicit identity to match old and new elements against, independent of their position in the array.',null,null,null,'medium',ARRAY['react','fundamentals','reconciliation','keys']::text[],true,112),
('react','fundamentals','react-fundamentals-113','What can you inspect using React DevTools?','React DevTools is a browser extension that adds Components and Profiler tabs to your dev tools. The Components tab lets you browse the live component tree, inspect and edit props/state/hooks values, and see which component owns a given DOM node; the Profiler tab records renders over time and helps identify unnecessary or expensive re-renders.',null,null,null,'easy',ARRAY['react','fundamentals','tooling']::text[],false,113),
('react','fundamentals','react-fundamentals-114','What is the difference between the react and react-dom packages?','The react package contains the core APIs for defining components and managing state/hooks, independent of any particular rendering target. react-dom contains the renderer that knows how to translate React''s output into actual browser DOM operations — other renderers, like React Native, swap in a different rendering package while reusing the same react core.',null,null,null,'easy',ARRAY['react','fundamentals','packages']::text[],false,114),
('react','fundamentals','react-fundamentals-115','What is the difference between createRoot and the legacy ReactDOM.render?','createRoot (React 18+) creates a root that opts an app into React''s concurrent rendering features, such as automatic batching and transitions. The older ReactDOM.render (deprecated in React 18) always ran in the legacy synchronous rendering mode and does not support those concurrent features.',null,'import { createRoot } from ''react-dom/client'';

const root = createRoot(document.getElementById(''root''));
root.render(<App />);','jsx','medium',ARRAY['react','fundamentals','react-18']::text[],true,115),
('react','fundamentals','react-fundamentals-116','What does it mean that React 18 batches state updates automatically?','When multiple setState calls happen within the same event handler, React groups them into a single re-render instead of re-rendering after each one, which improves performance. Before React 18, this automatic batching only happened inside React event handlers; React 18 extends it to updates inside promises, timeouts, and native event handlers too.',null,'function handleClick() {
  setCount(c => c + 1);
  setFlag(f => !f);
  // Only one re-render happens, not two
}','jsx','hard',ARRAY['react','fundamentals','react-18','performance']::text[],true,116),
('react','fundamentals','react-fundamentals-117','At a high level, what are React 18''s concurrent rendering features?','Concurrent rendering lets React prepare multiple versions of the UI in memory and interrupt, pause, or discard a render in progress if something more urgent (like user input) comes in, rather than always finishing one render before starting the next. Features built on this include automatic batching, transitions (useTransition, marking updates as non-urgent), and streaming server rendering with Suspense.',null,null,null,'hard',ARRAY['react','fundamentals','react-18','concurrent']::text[],false,117),
('react','fundamentals','react-fundamentals-118','What does a React element actually look like under the hood?','A React element is a plain, lightweight JavaScript object describing a piece of UI — it has a type (a tag name string or a component function/class), a props object, and a key. Elements are not the actual rendered DOM nodes; React reads these objects to figure out what real DOM to create or update.',null,'// Roughly what <h1 className="x">Hi</h1> becomes
{
  type: ''h1'',
  props: { className: ''x'', children: ''Hi'' },
  key: null
}','jsx','medium',ARRAY['react','fundamentals','internals']::text[],false,118),
('react','fundamentals','react-fundamentals-119','Why does React discourage directly manipulating the DOM yourself?','React maintains its own internal representation of the UI to efficiently compute and batch updates; if you manually change a DOM node React manages, React''s internal model goes out of sync with reality, which can cause bugs or have your change silently overwritten on the next render. When you truly need direct DOM access (e.g. focusing an input), you should do it through a ref rather than querySelector-style code.',null,null,null,'easy',ARRAY['react','fundamentals','dom']::text[],false,119),
('react','fundamentals','react-fundamentals-120','How does building a counter in React compare to doing it with vanilla JS DOM manipulation?','In vanilla JS you''d manually select the counter element, keep a variable for the count, and imperatively update textContent on every click. In React you keep the count in state and describe what the counter should render for any given count value — clicking the button just updates state, and React handles updating the actual text on screen for you.',null,'// React
function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(c => c + 1)}>{count}</button>;
}','jsx','medium',ARRAY['react','fundamentals','state']::text[],false,120),
('react','fundamentals','react-fundamentals-121','Why does using semantic HTML still matter inside JSX?','JSX ultimately renders real DOM elements, so using semantic tags like <button>, <nav>, and <main> instead of generic <div>s with click handlers still matters for accessibility, keyboard navigation, and SEO, exactly as it would in plain HTML. React doesn''t add any semantics on its own — it just gives you a more convenient way to describe the same DOM tree.',null,null,null,'easy',ARRAY['react','fundamentals','accessibility']::text[],false,121),
('react','fundamentals','react-fundamentals-122','What does spreading props with {...props} do?','The spread syntax {...props} expands all key-value pairs of a props object onto a JSX element as individual attributes, which is a concise way to forward a whole set of props through to an underlying element or component without listing each one manually.',null,'function Wrapper(props) {
  return <input {...props} className="styled" />;
}','jsx','easy',ARRAY['react','fundamentals','props']::text[],false,122),
('react','fundamentals','react-fundamentals-123','What is props.children and where does it come from?','props.children holds whatever JSX (or text) was nested between a component''s opening and closing tags when it was used. It''s just a regular prop under the hood, automatically populated by JSX rather than something you have to pass explicitly.',null,'function Card({ children }) {
  return <div className="card">{children}</div>;
}

<Card>
  <p>Nested content</p>
</Card>','jsx','easy',ARRAY['react','fundamentals','props','children']::text[],false,123),
('react','fundamentals','react-fundamentals-124','How does a parent re-rendering affect its child components?','By default, when a component re-renders, React also re-renders all of its child components regardless of whether their own props actually changed — it''s just usually cheap because React only commits real DOM changes where something differs. This is why techniques like React.memo exist: to let a specific child opt out of re-rendering when its props are unchanged.',null,null,null,'medium',ARRAY['react','fundamentals','rendering']::text[],false,124),
('react','fundamentals','react-fundamentals-125','Why do React apps typically need a bundler like Vite or webpack?','Browsers don''t understand JSX or, historically, ES modules with bare package imports, so a bundler transforms JSX into plain JavaScript, resolves and bundles dependencies from node_modules, and produces optimized output (code splitting, minification) for production. Vite additionally provides a fast dev server with hot module replacement during development.',null,null,null,'easy',ARRAY['react','fundamentals','tooling']::text[],false,125),
('react','fundamentals','react-fundamentals-126','What is the difference between client-side rendering and hydration?','In pure client-side rendering, the browser downloads a near-empty HTML shell and JavaScript builds the entire UI after the page loads. Hydration is what happens when the server already sent fully-rendered HTML for the initial view, and React attaches event listeners and internal state to that existing markup on the client instead of re-creating the DOM from scratch, making the page interactive.',null,null,null,'hard',ARRAY['react','fundamentals','ssr','hydration']::text[],false,126),
('react','fundamentals','react-fundamentals-127','What role does a tool like Vite or Create React App play in a React project?','These are project scaffolding and build tooling setups that wire together a dev server, JSX/TypeScript compilation, bundling, and sensible defaults so you don''t have to hand-configure a bundler yourself. Vite has largely replaced Create React App (now unmaintained) because of its much faster dev server startup and hot reload, using native ES modules during development.',null,null,null,'easy',ARRAY['react','fundamentals','tooling']::text[],false,127),
('react','fundamentals','react-fundamentals-128','How do PropTypes and TypeScript compare for type-checking props?','PropTypes is a runtime library that checks prop types while the app is running in development and logs a console warning on mismatch, but it has no effect on production behavior and doesn''t catch errors until the component actually renders with bad data. TypeScript checks prop types statically at compile time, catching many mismatches before the code ever runs, which is why most modern React projects use TypeScript instead of, or in addition to, PropTypes.',null,null,null,'medium',ARRAY['react','fundamentals','typescript']::text[],false,128),
('react','fundamentals','react-fundamentals-129','What is ''lifting state up'' and why is it one of the first patterns React developers learn?','Lifting state up means moving state from a component into its closest common ancestor when two or more sibling components need to read or react to the same value. It''s foundational because it''s the simplest way to keep multiple components in sync without any extra library — later patterns like Context exist to avoid lifting state so far up that it has to be threaded through many layers of props.',null,null,null,'medium',ARRAY['react','fundamentals','state']::text[],false,129),
('react','fundamentals','react-fundamentals-130','What actually causes a React component to re-render?','A component re-renders when its own state changes, when the props passed to it change, when a context value it consumes changes, or when its parent re-renders (regardless of whether that parent''s re-render was caused by something relevant to the child). Understanding this is the starting point for diagnosing unnecessary re-renders and applying memoization correctly.',null,null,null,'medium',ARRAY['react','fundamentals','rendering']::text[],false,130),
('react','fundamentals','react-fundamentals-131','What does declarative UI actually buy you compared to jQuery-style DOM scripting?','With jQuery-style code, every possible state transition needs its own hand-written sequence of DOM mutations, and it gets exponentially harder to reason about as the number of states grows. With React''s declarative model, you only describe the UI for each state once, and React''s diffing handles figuring out the transition between any two states for you, which scales far better as an application grows in complexity.',null,null,null,'easy',ARRAY['react','fundamentals']::text[],false,131),
('react','fundamentals','react-fundamentals-132','Why did early React require every component to return a single root element?','React.createElement (and the underlying rendering model) originally worked with a single element tree per call, so a component''s render output had to be one element with everything else nested inside it. Fragments were added later specifically to lift this restriction without requiring an extra wrapper DOM node.',null,'// Not allowed without a Fragment/wrapper
function Bad() {
  return (
    <li>One</li>
    <li>Two</li>
  );
}','jsx','medium',ARRAY['react','fundamentals','jsx']::text[],false,132),
('react','fundamentals','react-fundamentals-133','What does it mean that ''JSX is just JavaScript''?','Because JSX compiles down to regular function calls, any valid JavaScript expression can be embedded inside curly braces {} within JSX — variables, function calls, ternaries, array methods, and so on. This means you don''t need a separate templating language or special syntax to express logic in your markup; it''s the same language throughout.',null,'const name = ''Sam'';
const el = <p>Hello, {name.toUpperCase()}!</p>;','jsx','easy',ARRAY['react','fundamentals','jsx']::text[],false,133),
('react','fundamentals','react-fundamentals-134','Why can''t a component return multiple sibling elements without a wrapper?','A component''s return value must be a single value in JavaScript, and JSX compiles to a single nested element tree — you can''t return two independent element trees from one function call. A Fragment (or any wrapping element) solves this by giving the multiple elements a single, if invisible, parent to be returned as one unit.',null,null,null,'easy',ARRAY['react','fundamentals','jsx']::text[],false,134),
('react','fundamentals','react-fundamentals-135','How do JSX prop names differ from standard HTML attribute names?','Some JSX props are renamed because they''d otherwise collide with reserved JavaScript words or don''t map cleanly to the DOM property: class becomes className, for becomes htmlFor, and most attributes use camelCase (onclick becomes onClick, tabindex becomes tabIndex) to match JavaScript naming conventions rather than HTML''s lowercase-only attributes.',null,'<label htmlFor="email" className="label">Email</label>','jsx','easy',ARRAY['react','fundamentals','jsx']::text[],false,135),
('react','fundamentals','react-fundamentals-136','How does JSX render boolean, null, and undefined values?','React treats false, true, null, and undefined as ''nothing to render'' when they appear as JSX children, so they simply produce no output rather than an error or the literal text. This is what makes the {condition && <Component />} pattern work: when condition is false, JSX renders nothing.',null,'<div>{null}{undefined}{false}{true}</div> // renders an empty div','jsx','medium',ARRAY['react','fundamentals','jsx']::text[],false,136),
('react','fundamentals','react-fundamentals-137','What does it mean that React is ''a UI library, not an application framework''?','React only concerns itself with turning state into rendered UI and handling events — it deliberately doesn''t prescribe how to do routing, data fetching, or global state management, unlike a full framework such as Angular. Teams choose and assemble their own supporting libraries (React Router, TanStack Query, Redux/Zustand, etc.), which gives flexibility at the cost of more setup decisions.',null,null,null,'medium',ARRAY['react','fundamentals']::text[],false,137),
('react','fundamentals','react-fundamentals-138','What is the difference between React''s render phase and commit phase?','The render phase is where React calls your component functions to figure out what the new tree of elements should look like and diffs it against the previous tree — this phase can be paused, aborted, or restarted by React and must be free of side effects. The commit phase is where React actually applies the computed DOM mutations and runs effects like useEffect/componentDidMount, and this phase always runs synchronously to completion once started.',null,null,null,'hard',ARRAY['react','fundamentals','internals']::text[],false,138),
('react','fundamentals','react-fundamentals-139','What actually triggers a component''s render phase to begin?','A render is triggered by a state update (via a state setter), a prop change coming from a re-rendering parent, or a context value update for a component subscribed to that context. React schedules and may batch several triggers together before actually running the render phase once.',null,null,null,'hard',ARRAY['react','fundamentals','internals','rendering']::text[],false,139),
('react','fundamentals','react-fundamentals-140','At a fundamentals level, why must hooks always be called in the same order on every render?','React associates each hook call with internal state purely by the order it was called in during a render, not by name — there''s no other identifier. If a hook is called conditionally so the order changes between renders, React would attach the wrong stored state to the wrong hook call, corrupting component state in subtle ways.',null,null,null,'medium',ARRAY['react','fundamentals','hooks']::text[],false,140),
('react','fundamentals','react-fundamentals-141','How do refs differ from React''s normal declarative rendering model?','Refs (useRef, or a DOM ref returned from forwardRef) give you an ''escape hatch'' to imperatively read or mutate a DOM node or hold a mutable value directly, bypassing the render-based data flow. They''re meant for cases the declarative model doesn''t cover well, like focusing an input or measuring an element''s size, not as a general substitute for state.',null,'const inputRef = useRef(null);
// Imperative: directly call a DOM method
inputRef.current.focus();','jsx','medium',ARRAY['react','fundamentals','refs']::text[],false,141),
('react','fundamentals','react-fundamentals-142','How does React decide what to do when a JSX child is a plain string, number, or object?','Strings and numbers are rendered directly as text nodes. Plain objects (other than valid React elements or arrays of children) are not valid as a React child and will throw an error at runtime — you''d need to convert the object to a string or extract the specific fields you want to render.',null,'<div>{42}</div>       // renders "42"
<div>{{ a: 1 }}</div> // throws: Objects are not valid as a React child','jsx','medium',ARRAY['react','fundamentals','jsx']::text[],false,142),
('react','fundamentals','react-fundamentals-143','What kinds of JavaScript expressions are NOT allowed inside JSX curly braces?','Curly braces in JSX must contain an expression that produces a value, not a statement — so if/else blocks, for loops, and variable declarations (let/const) can''t go directly inside {}. You work around this by computing the value beforehand (outside the JSX) or using expression-based alternatives like the ternary operator or .map().',null,'// Invalid: if is a statement, not an expression
{if (loggedIn) { <p>Hi</p> }}

// Valid: ternary is an expression
{loggedIn ? <p>Hi</p> : null}','jsx','easy',ARRAY['react','fundamentals','jsx']::text[],false,143),
('react','fundamentals','react-fundamentals-144','What''s the difference between a React application and a React component library?','A React application is a deployable product with routing, pages, and business logic built on top of React. A component library is a reusable package of components (and often hooks/utilities) meant to be installed as a dependency of other applications, typically published with its own build step, type definitions, and documentation rather than a running app.',null,null,null,'easy',ARRAY['react','fundamentals']::text[],false,144),
('react','fundamentals','react-fundamentals-145','Why is onClick={() => doSomething()} usually correct while onClick={doSomething()} is usually a bug?','onClick={doSomething()} calls doSomething immediately during render and passes whatever it returns as the handler, instead of passing a function to run later on click. Wrapping it in an arrow function, onClick={() => doSomething()}, defers the call until the click actually happens, and also lets you pass arguments that plain onClick={doSomething} (without parentheses) wouldn''t allow.',null,'// Bug: runs on every render, not on click
<button onClick={deleteItem(id)}>Delete</button>

// Correct: runs only when clicked
<button onClick={() => deleteItem(id)}>Delete</button>','jsx','medium',ARRAY['react','fundamentals','events','gotcha']::text[],false,145),
('react','fundamentals','react-fundamentals-146','Does a key prop need to be unique across the whole app, or just somewhere specific?','A key only needs to be unique among its immediate siblings within the same array being rendered — it does not need to be globally unique across the entire component tree. Using the same key value in two unrelated lists elsewhere in the app causes no conflict at all.',null,'// Both lists can safely reuse key="1", key="2" independently
<ul>{users.map(u => <li key={u.id}>{u.name}</li>)}</ul>
<ul>{posts.map(p => <li key={p.id}>{p.title}</li>)}</ul>','jsx','medium',ARRAY['react','fundamentals','keys']::text[],false,146),
('react','fundamentals','react-fundamentals-147','What does ''one-way data binding'' mean, and how does it differ from two-way binding?','In one-way binding, data flows from state into the UI in a single direction, and any change originating from the UI (like typing in an input) has to explicitly call a function to update state, which then flows back down to re-render the UI. Two-way binding, common in frameworks like older Angular or Vue''s v-model, automatically syncs a form input''s value back into a bound variable without the developer wiring an explicit handler.',null,null,null,'medium',ARRAY['react','fundamentals','data-flow']::text[],false,147),
('react','fundamentals','react-fundamentals-148','What is the role of the index.html file in a Vite-based React app?','index.html is the actual entry point Vite serves — it contains a script tag pointing at your main JavaScript/TypeScript entry file (e.g. src/main.jsx) and typically a single empty <div id="root">. React then mounts the entire application into that div at runtime; almost none of the visible UI is written directly in this file.',null,null,null,'easy',ARRAY['react','fundamentals','tooling']::text[],false,148),
('react','fundamentals','react-fundamentals-149','When can''t you use the <>...</> Fragment shorthand and need React.Fragment explicitly instead?','The shorthand syntax <>...</> cannot accept any props, including a key. If you''re rendering a list of fragments (for example, grouping multiple elements per array item without an extra wrapper), you need the explicit <React.Fragment key={...}> form so each fragment can carry the required key.',null,'{items.map(item => (
  <React.Fragment key={item.id}>
    <dt>{item.term}</dt>
    <dd>{item.description}</dd>
  </React.Fragment>
))}','jsx','easy',ARRAY['react','fundamentals','jsx']::text[],false,149),
('react','fundamentals','react-fundamentals-150','What is meant by ''component-driven development''?','Component-driven development is the practice of building a UI from the bottom up — starting with small, isolated, reusable components (often developed and tested independently, e.g. in Storybook) and composing them into larger features and pages. It encourages thinking in terms of a reusable component library rather than building each page as one large, monolithic piece of markup.',null,null,null,'medium',ARRAY['react','fundamentals','design']::text[],false,150),
('react','hooks','react-hooks-101','When should you use useReducer instead of useState?','useReducer is preferable when state logic is complex, involves multiple sub-values that update together, or when the next state depends on the previous state in non-trivial ways. It centralizes update logic in a single reducer function, making state transitions more predictable and testable than scattering multiple useState calls and setter calls across a component.',null,'function reducer(state, action) {
  switch (action.type) {
    case ''increment'': return { count: state.count + 1 };
    default: return state;
  }
}
const [state, dispatch] = useReducer(reducer, { count: 0 });','jsx','medium',ARRAY['react','hooks','usereducer']::text[],true,101),
('react','hooks','react-hooks-102','How do you consume a context value using the useContext hook?','Call useContext(SomeContext) inside a function component to read the nearest matching Provider''s value directly, without wrapping the component in a Consumer render-prop. The component re-renders automatically whenever the Provider''s value changes, and if no Provider is present, useContext returns the context''s default value.',null,'const theme = useContext(ThemeContext);','jsx','easy',ARRAY['react','hooks','usecontext']::text[],false,102),
('react','hooks','react-hooks-103','What is a custom hook and why should its name start with "use"?','A custom hook is simply a JavaScript function that calls other hooks internally to encapsulate and share reusable stateful logic across components. Prefixing its name with "use" is a naming convention that lets both React and the ESLint hooks plugin recognize it as a hook, applying the rules of hooks and enabling correct linting.',null,'function useWindowWidth() {
  const [width, setWidth] = useState(window.innerWidth);
  useEffect(() => {
    const onResize = () => setWidth(window.innerWidth);
    window.addEventListener(''resize'', onResize);
    return () => window.removeEventListener(''resize'', onResize);
  }, []);
  return width;
}','jsx','easy',ARRAY['react','hooks','custom-hooks']::text[],true,103),
('react','hooks','react-hooks-104','When exactly does a useEffect cleanup function run?','The cleanup function returned from an effect runs right before the component re-runs that same effect (when its dependencies change) and again when the component unmounts. This ordering ensures that a previous effect''s side effect, like a subscription or timer, is torn down before a new one is set up, preventing duplicate subscriptions or leaks.',null,null,null,'medium',ARRAY['react','hooks','useeffect']::text[],false,104),
('react','hooks','react-hooks-105','What common bugs occur when the useEffect dependency array is incomplete?','Omitting a value the effect actually reads from its dependency array causes the effect to close over a stale version of that value, a "stale closure". This often shows up as data going out of sync or event handlers acting on old values, and is why the exhaustive-deps ESLint rule flags missing dependencies.',null,'useEffect(() => {
  console.log(count); // always logs the initial count
}, []); // missing ''count'' dependency','jsx','medium',ARRAY['react','hooks','useeffect']::text[],true,105),
('react','hooks','react-hooks-106','Beyond timing, what practical difference does choosing useLayoutEffect over useEffect make?','useEffect runs asynchronously after the browser has painted, so any DOM mutation inside it can cause a visible flicker before the update is applied. useLayoutEffect runs synchronously after DOM mutations but before the browser paints, blocking the paint until it finishes — useful for measuring or adjusting layout so the user never sees the unadjusted frame, at the cost of potentially blocking rendering if the work is slow.',null,null,null,'hard',ARRAY['react','hooks','uselayouteffect']::text[],false,106),
('react','hooks','react-hooks-107','What is the main pitfall of fetching data directly inside a useEffect, and how is it commonly avoided?','If a component re-renders quickly, for example a prop like an id changes twice in a row, an earlier fetch can resolve after a later one and overwrite fresher data with stale results — a race condition. The common fix is to track a "cancelled" flag or use an AbortController in the effect''s cleanup function so a stale response is ignored once a newer request has started.',null,'useEffect(() => {
  let cancelled = false;
  fetchUser(id).then((data) => { if (!cancelled) setUser(data); });
  return () => { cancelled = true; };
}, [id]);','jsx','medium',ARRAY['react','hooks','useeffect','data-fetching']::text[],false,107),
('react','hooks','react-hooks-108','What is the difference between setCount(count + 1) and setCount(c => c + 1)?','setCount(count + 1) uses the value of count captured when the function was defined, which can be stale if multiple updates happen before a re-render. setCount(c => c + 1) passes an updater function that always receives the latest pending state, making it safe to call multiple times in a row without losing updates.',null,'setCount(c => c + 1);
setCount(c => c + 1); // count increases by 2, reliably','jsx','easy',ARRAY['react','hooks','usestate']::text[],false,108),
('react','hooks','react-hooks-109','What does it mean to pass a function to useState, and why would you do that?','When the initial state is expensive to compute, passing a function tells React to call it only once, on the initial render, rather than recomputing and discarding the value on every re-render as you would by calling the expensive function directly as the argument. This is called lazy initialization.',null,'const [items, setItems] = useState(() => JSON.parse(localStorage.getItem(''items'')) ?? []);','jsx','medium',ARRAY['react','hooks','usestate']::text[],false,109),
('react','hooks','react-hooks-110','Is it better to use several useState calls or one useState holding an object?','Several independent useState calls are usually preferable for unrelated values since each setter only updates its own piece of state and doesn''t require manually spreading the rest. A single state object makes sense when values are tightly related and typically change together, but every update must spread the previous object since, unlike class component setState, useState does not shallow-merge automatically.',null,null,null,'medium',ARRAY['react','hooks','usestate']::text[],false,110),
('react','hooks','react-hooks-111','How does useRef let you store a value across renders without causing a re-render?','useRef(initialValue) returns a plain mutable object with a .current property that persists for the lifetime of the component. Reading or writing ref.current does not trigger a re-render or schedule an update, unlike setState, making refs suitable for values like timer IDs or previous values that the component needs to remember but never needs to display.',null,null,null,'easy',ARRAY['react','hooks','useref']::text[],false,111),
('react','hooks','react-hooks-112','If both useRef and useState can hold a value, when should you choose one over the other?','Choose useState when a change to the value should cause the component to re-render and reflect the new value in the UI. Choose useRef when you need to persist a value between renders, like a timer ID, a DOM node, or a flag, but updating it should never by itself cause a re-render.',null,null,null,'easy',ARRAY['react','hooks','useref','usestate']::text[],false,112),
('react','hooks','react-hooks-113','What does useImperativeHandle do, and when is it appropriate to use?','useImperativeHandle, used together with forwardRef, lets a component customize the instance value exposed to a parent''s ref, exposing only a specific imperative API like a focus() method instead of the raw DOM node. It should be used sparingly — only when a parent genuinely needs to imperatively trigger behavior on a child that can''t be expressed declaratively through props.',null,'const Input = forwardRef((props, ref) => {
  const inputRef = useRef(null);
  useImperativeHandle(ref, () => ({
    focus: () => inputRef.current.focus(),
  }));
  return <input ref={inputRef} {...props} />;
});','jsx','hard',ARRAY['react','hooks','useimperativehandle','forwardref']::text[],false,113),
('react','hooks','react-hooks-114','What problem does useDeferredValue solve?','useDeferredValue lets you defer re-rendering a non-urgent part of the UI, like a large filtered list, behind a fast-changing value like a text input, so React can keep the input responsive while the expensive part updates slightly behind, showing the previous value until the new one is ready. It''s a way to deprioritize rendering work without debouncing or throttling the actual input state.',null,'const deferredQuery = useDeferredValue(query);
// list filtered by deferredQuery can lag slightly behind the input','jsx','hard',ARRAY['react','hooks','usedeferredvalue','performance']::text[],false,114),
('react','hooks','react-hooks-115','What does useTransition let you do?','useTransition returns a startTransition function and an isPending flag, letting you mark a state update as a low-priority "transition" that can be interrupted by more urgent updates like typing. React keeps the UI responsive by rendering the urgent update first and the transition update when it has spare time, while isPending lets you show a pending indicator during that gap.',null,'const [isPending, startTransition] = useTransition();
startTransition(() => setFilter(nextFilter));','jsx','hard',ARRAY['react','hooks','usetransition','performance']::text[],false,115),
('react','hooks','react-hooks-116','What is useId used for?','useId generates a stable, unique string ID that is consistent between server and client renders, primarily for wiring up accessibility attributes like htmlFor/id pairs on form labels and inputs without risking hydration mismatches. It should not be used as a list key or for generating IDs shown to end users.',null,'const id = useId();
<label htmlFor={id}>Name</label>
<input id={id} />','jsx','easy',ARRAY['react','hooks','useid','accessibility']::text[],false,116),
('react','hooks','react-hooks-117','What problem does useSyncExternalStore solve?','useSyncExternalStore lets a component safely subscribe to a value that lives outside React''s own state, like a browser API or a third-party store, while guaranteeing the value stays consistent even under React''s concurrent rendering features, which could otherwise tear between subscribing and reading the store. It''s the low-level hook that most external state-management library bindings are built on.',null,null,null,'hard',ARRAY['react','hooks','usesyncexternalstore']::text[],false,117),
('react','hooks','react-hooks-118','Can a custom hook call other hooks internally, including other custom hooks?','Yes — a custom hook is just a function, so it can call built-in hooks like useState and useEffect, as well as other custom hooks, as long as it still follows the rules of hooks by only calling them at its own top level, not conditionally. This composability is what lets you build higher-level hooks out of smaller reusable ones.',null,'function useAuthenticatedUser() {
  const [token] = useLocalStorage(''token'');
  const { data } = useFetch(token ? ''/api/me'' : null);
  return data;
}','jsx','easy',ARRAY['react','hooks','custom-hooks']::text[],false,118),
('react','hooks','react-hooks-119','How do you typically test a custom hook in isolation?','Since hooks can only be called inside a component, testing utilities like React Testing Library''s renderHook render the hook inside a minimal throwaway test component and expose its return value and a way to trigger re-renders via act(). This lets you assert on the hook''s returned state and behavior without needing to build and interact with a full UI.',null,null,null,'medium',ARRAY['react','hooks','testing']::text[],false,119),
('react','hooks','react-hooks-120','What typically causes an infinite loop with useEffect, and how do you fix it?','An infinite loop usually happens when an effect updates a piece of state that is also listed in its dependency array, causing the effect to re-run after every render it triggers. The fix is to either remove the unnecessary dependency, use a functional state update that doesn''t need the value as a dependency, or restructure the logic so the effect doesn''t need to depend on the state it''s setting.',null,'useEffect(() => {
  setCount(count + 1); // depends on and updates ''count'' -> infinite loop
}, [count]);','jsx','medium',ARRAY['react','hooks','useeffect']::text[],true,120),
('react','hooks','react-hooks-121','What does the react-hooks/exhaustive-deps ESLint rule check, and why does it matter?','It statically analyzes an effect, or useMemo/useCallback, and warns when a value used inside is missing from the dependency array, since that''s the most common source of stale-closure bugs. It''s a strong default to follow, though there are rare, well-understood cases, like intentionally running an effect only once, where suppressing it with a comment is appropriate rather than papering over a real bug.',null,null,null,'medium',ARRAY['react','hooks','eslint','useeffect']::text[],false,121),
('react','hooks','react-hooks-122','Does React call an effect''s cleanup function differently on unmount versus a dependency change?','The cleanup runs the same way in both cases — right before the effect runs again for a dependency change, and once more on unmount — so the cleanup function can''t easily tell which case triggered it. If different behavior is needed for unmount specifically, that distinction has to be tracked manually, for example with a ref flag, rather than relied upon from the cleanup call itself.',null,null,null,'medium',ARRAY['react','hooks','useeffect']::text[],false,122),
('react','hooks','react-hooks-123','What does passing an empty dependency array to useEffect mean, and when is it a mistake?','An empty array tells React the effect doesn''t depend on any reactive value, so it should run only once after the initial render and its cleanup only on unmount, appropriate for a one-time subscription or fetch that doesn''t depend on props or state. It''s a mistake when the effect body actually reads props or state, since the effect will then silently keep using the original values forever.',null,'useEffect(() => {
  document.title = ''Dashboard'';
}, []); // runs once, fine — doesn''t depend on props/state','jsx','easy',ARRAY['react','hooks','useeffect']::text[],false,123),
('react','hooks','react-hooks-124','Can a custom hook share state between two different components?','Not by itself — each component that calls a custom hook gets its own independent instance of any useState inside it, so the hook alone doesn''t share state across components. To actually share state, the hook needs to read from something external and shared, like a module-level variable, Context, or an external store.',null,null,null,'medium',ARRAY['react','hooks','custom-hooks']::text[],false,124),
('react','hooks','react-hooks-125','Why would you wrap an object or array in useMemo before passing it as a prop?','Without useMemo, a new object or array literal is created on every render, giving it a new reference each time — so a child wrapped in React.memo would still re-render because its prop "changed" by reference even though its contents are identical. Memoizing the object keeps the same reference across renders as long as its dependencies haven''t changed, letting a memoized child correctly skip re-rendering.',null,'const options = useMemo(() => ({ page, pageSize }), [page, pageSize]);
<MemoizedList options={options} />','jsx','medium',ARRAY['react','hooks','usememo','performance']::text[],false,125),
('react','hooks','react-hooks-126','What''s a common mistake when using useCallback?','A common mistake is omitting a value the callback actually closes over from its dependency array, which "freezes" the callback with a stale version of that value — functionally the same stale-closure bug as with useEffect. Another is wrapping every handler in useCallback reflexively even when nothing downstream benefits from a stable reference, adding overhead without a real payoff.',null,'const onSave = useCallback(() => save(id), []); // missing ''id'' -> always saves the first id','jsx','medium',ARRAY['react','hooks','usecallback']::text[],true,126),
('react','hooks','react-hooks-127','Why must hooks always be called in the same order on every render?','React tracks each hook call by its position within a component using an internal list associated with that component''s instance, not by name — so if a hook is skipped on one render due to a condition or early return but called on the next, every subsequent hook''s stored state gets misaligned with the wrong call. Always calling hooks unconditionally at the top level keeps that internal list stable between renders.',null,null,null,'hard',ARRAY['react','hooks']::text[],true,127),
('react','hooks','react-hooks-128','How do you replicate componentDidMount''s behavior using hooks?','Use useEffect with an empty dependency array — since the effect runs once after the first render and never again unless the component unmounts and remounts, it behaves equivalently to componentDidMount for run-once setup logic like an initial data fetch.',null,'useEffect(() => {
  fetchInitialData();
}, []);','jsx','easy',ARRAY['react','hooks','useeffect','lifecycle']::text[],false,128),
('react','hooks','react-hooks-129','How do you replicate componentDidUpdate using hooks?','List the specific props or state you want to react to in useEffect''s dependency array — the effect then runs after every render where one of those values changed, similar to checking prevProps inside componentDidUpdate. Unlike componentDidUpdate, the effect also runs after the very first render unless you explicitly guard against that with a ref-based check.',null,'useEffect(() => {
  console.log(''userId changed to'', userId);
}, [userId]);','jsx','medium',ARRAY['react','hooks','useeffect','lifecycle']::text[],false,129),
('react','hooks','react-hooks-130','How do you replicate componentWillUnmount using hooks?','Return a cleanup function from useEffect — React calls it right before the component unmounts, and also before the effect re-runs due to a dependency change, so unsubscribing, clearing timers, or cancelling requests belongs inside that returned function.',null,'useEffect(() => {
  const id = setInterval(tick, 1000);
  return () => clearInterval(id); // like componentWillUnmount
}, []);','jsx','easy',ARRAY['react','hooks','useeffect','lifecycle']::text[],false,130),
('react','hooks','react-hooks-131','How did automatic state batching change between React 17 and React 18?','In React 17, multiple setState calls were only batched into a single re-render inside React event handlers — calls inside promises, setTimeout, or native event listeners each triggered a separate re-render. React 18''s automatic batching extends that behavior everywhere by default, so multiple state updates anywhere are batched into one re-render unless explicitly opted out with flushSync.',null,'setTimeout(() => {
  setA(1);
  setB(2); // React 18: one re-render. React 17: two re-renders.
}, 0);','jsx','hard',ARRAY['react','hooks','usestate','performance']::text[],false,131),
('react','hooks','react-hooks-132','Can the function you pass directly to useEffect be async?','No — useEffect expects its callback to return either nothing or a cleanup function, but an async function always returns a Promise, which React can''t use as a cleanup function and will warn about. Instead, define an async function inside the effect and call it immediately, or use .then() directly.',null,'useEffect(() => {
  async function load() { setData(await fetchData()); }
  load();
}, []);','jsx','easy',ARRAY['react','hooks','useeffect','async']::text[],false,132),
('react','hooks','react-hooks-133','What is the standard pattern for subscribing to a browser event inside useEffect?','Add the listener inside the effect body and return a cleanup function that removes the exact same listener reference, so the subscription is properly torn down when dependencies change or the component unmounts — forgetting the cleanup is a common source of duplicate listeners and memory leaks.',null,'useEffect(() => {
  const onScroll = () => setY(window.scrollY);
  window.addEventListener(''scroll'', onScroll);
  return () => window.removeEventListener(''scroll'', onScroll);
}, []);','jsx','medium',ARRAY['react','hooks','useeffect']::text[],false,133),
('react','hooks','react-hooks-134','When is wrapping a value in useMemo not worth it?','If the computation is cheap, like a simple arithmetic expression or string concatenation, the overhead of tracking dependencies and comparing them on every render can outweigh the cost of just recomputing the value directly. useMemo pays off for genuinely expensive computations or when referential stability is needed downstream, not as a reflexive wrapper around every derived value.',null,null,null,'medium',ARRAY['react','hooks','usememo','performance']::text[],false,134),
('react','hooks','react-hooks-135','When does wrapping a function in useCallback provide no real benefit?','If the function isn''t passed to a memoized child component or used as a dependency elsewhere, memoizing its reference doesn''t prevent any actual re-render or recomputation — it just adds the overhead of the dependency check for no payoff. useCallback is worth it specifically when a stable reference is required downstream, not as a default habit on every handler.',null,null,null,'medium',ARRAY['react','hooks','usecallback','performance']::text[],false,135),
('react','hooks','react-hooks-136','How do you access a prop or state''s previous value using useRef?','Store the current value in a ref inside a useEffect that runs after every render, so by the time the next render happens, the ref still holds the value from the render before — a common "usePrevious" custom hook pattern.',null,'function usePrevious(value) {
  const ref = useRef();
  useEffect(() => { ref.current = value; });
  return ref.current;
}','jsx','medium',ARRAY['react','hooks','useref','custom-hooks']::text[],false,136),
('react','hooks','react-hooks-137','How would you measure a DOM element''s size using useRef?','Attach the ref to the element via the ref attribute, then read its dimensions with methods like getBoundingClientRect() — typically inside a useLayoutEffect so the measurement happens synchronously after the DOM updates but before the browser paints, avoiding a visible jump.',null,'const boxRef = useRef(null);
useLayoutEffect(() => {
  const { width } = boxRef.current.getBoundingClientRect();
  setWidth(width);
}, []);','jsx','medium',ARRAY['react','hooks','useref','uselayouteffect']::text[],false,137),
('react','hooks','react-hooks-138','What''s a common pitfall with a context''s default value?','If a component calls useContext without ever being wrapped by a matching Provider, it silently receives the default value passed to createContext instead of an error, which can mask a missing Provider and cause confusing bugs. A common mitigation is to default the context to null and throw a clear error from a wrapper hook if no Provider was found.',null,'function useAuth() {
  const ctx = useContext(AuthContext);
  if (!ctx) throw new Error(''useAuth must be used within AuthProvider'');
  return ctx;
}','jsx','medium',ARRAY['react','hooks','usecontext']::text[],false,138),
('react','hooks','react-hooks-139','What is the third argument to useReducer used for?','The optional third argument is an init function that lazily computes the initial state from the second argument, which is useful when the initial state is expensive to compute or needs to be derived from props — similar in spirit to useState''s lazy initializer function.',null,'function init(initialCount) { return { count: initialCount }; }
const [state, dispatch] = useReducer(reducer, initialCount, init);','jsx','medium',ARRAY['react','hooks','usereducer']::text[],false,139),
('react','hooks','react-hooks-140','What''s a common convention for structuring useReducer actions?','Actions are typically plain objects with a type string field describing what happened, often an event name like ''user/loaded'', plus an optional payload field carrying any data the reducer needs — mirroring the Flux/Redux action shape even outside of Redux itself.',null,'dispatch({ type: ''user/loaded'', payload: user });','jsx','easy',ARRAY['react','hooks','usereducer']::text[],false,140),
('react','hooks','react-hooks-141','Does the dispatch function returned by useReducer change between renders?','No — React guarantees that the dispatch function''s identity is stable for the lifetime of the component, so it''s safe to omit from dependency arrays and to pass down to deeply nested components without causing extra re-renders.',null,null,null,'easy',ARRAY['react','hooks','usereducer']::text[],false,141),
('react','hooks','react-hooks-142','Should a custom hook return an array or an object?','Return an array, like useState does, when there are only one or two values and callers will likely want to rename them freely via destructuring. Return an object when there are several values, since named destructuring is more self-documenting and doesn''t depend on a fixed positional order.',null,'const [value, setValue] = useToggle(false); // array: order matters, free naming
const { data, loading, error } = useFetch(url); // object: self-documenting','jsx','easy',ARRAY['react','hooks','custom-hooks']::text[],false,142),
('react','hooks','react-hooks-143','If a component has multiple useEffect calls, in what order do they run?','They run in the order they''re declared in the component, top to bottom, after the render has committed to the DOM — and their cleanup functions likewise run in that same declared order before the effects re-run or the component unmounts.',null,null,null,'medium',ARRAY['react','hooks','useeffect']::text[],false,143),
('react','hooks','react-hooks-144','Why would you specifically choose useLayoutEffect for a DOM measurement instead of useEffect?','Because useEffect runs after the browser paints, any state update triggered by a measurement made there causes a visible flash of the un-adjusted layout before the corrected one appears. useLayoutEffect runs synchronously before the paint, so a measurement-driven adjustment is applied before the user ever sees the wrong frame.',null,null,null,'medium',ARRAY['react','hooks','uselayouteffect']::text[],false,144),
('react','hooks','react-hooks-145','Why does React warn about useLayoutEffect during server-side rendering?','useLayoutEffect never runs on the server since there''s no DOM or paint step to synchronize with, so React warns that its effect is being skipped, which can cause a mismatch between the server-rendered markup and what the client expects before hydration. The common workaround is a small hook that falls back to useEffect when running in a non-browser environment.',null,'const useIsomorphicLayoutEffect = typeof window !== ''undefined'' ? useLayoutEffect : useEffect;','jsx','hard',ARRAY['react','hooks','uselayouteffect','ssr']::text[],false,145),
('react','hooks','react-hooks-146','What does forwardRef do?','forwardRef lets a functional component accept a ref from its parent and forward it to a specific child element, usually a DOM node, inside it, since function components don''t receive refs as a regular prop by default. It''s commonly used to let a parent focus or measure an underlying input rendered by a reusable component.',null,'const FancyInput = forwardRef((props, ref) => <input ref={ref} className="fancy" {...props} />);','jsx','easy',ARRAY['react','hooks','forwardref']::text[],false,146),
('react','hooks','react-hooks-147','How does eslint-plugin-react-hooks help enforce the rules of hooks?','Its rules-of-hooks rule statically analyzes the code and flags hook calls that appear inside conditionals, loops, or nested functions, or in a non-component/non-hook function, catching violations at lint time before they cause runtime state-mismatch bugs. It complements, but doesn''t replace, the exhaustive-deps rule, which separately checks dependency arrays.',null,null,null,'easy',ARRAY['react','hooks','eslint']::text[],false,147),
('react','hooks','react-hooks-148','What does a renderHook-style testing utility provide?','It mounts a custom hook inside a hidden test component and returns a result object whose .current property reflects the hook''s latest return value, plus helpers to trigger re-renders and wrap state updates in act() — letting you assert on a hook''s behavior directly without writing a throwaway UI component yourself.',null,null,null,'medium',ARRAY['react','hooks','testing']::text[],false,148),
('react','hooks','react-hooks-149','Why does React warn "Cannot update a component while rendering a different component"?','Calling a state setter directly in the body of a render function, rather than in an effect or event handler, can trigger a synchronous re-render of another component while the current one is still rendering, leading to inconsistent state and potential infinite loops. The fix is almost always to move that setState call into a useEffect or an event handler instead.',null,null,null,'medium',ARRAY['react','hooks','usestate']::text[],false,149),
('react','hooks','react-hooks-150','How can a ref be used to always call the latest version of a prop function from inside a stable callback?','Store the latest function in a ref, updating it on every render, then have the stable callback (for example useCallback with empty deps) read and invoke ref.current instead of closing over the function directly — this avoids re-creating the stable callback just to pick up a new function reference, while still always calling the freshest version.',null,'const onSaveRef = useRef(onSave);
useEffect(() => { onSaveRef.current = onSave; });
const stableSave = useCallback((data) => onSaveRef.current(data), []);','jsx','hard',ARRAY['react','hooks','useref','usecallback']::text[],false,150),
('react','performance','react-performance-101','How do you use the React DevTools Profiler to diagnose a performance problem?','The Profiler tab records a session while you interact with the app, then shows a flame graph of every commit with how long each component took to render and why it rendered (props changed, state changed, parent re-rendered, etc.). You look for components that render often or take disproportionately long, then work backward to find the state update or prop change triggering them. It''s the standard first step before reaching for memoization, since it tells you where the actual cost is instead of guessing.',null,null,null,'medium',ARRAY['react','performance','devtools']::text[],true,101),
('react','performance','react-performance-102','How would you identify which prop or state change caused a slow re-render?','The DevTools Profiler can highlight the reason for each render ("why did this render") when that setting is enabled, showing which props or state changed. For a manual check, a useEffect that logs the changed props/state on every render, or a small custom hook that diffs the previous and current props, can pinpoint the culprit without any extra tooling.',null,'function useWhyDidYouUpdate(name, props) {
  const prev = useRef(props);
  useEffect(() => {
    const changed = Object.entries(props).filter(([k, v]) => prev.current[k] !== v);
    if (changed.length) console.log(name, ''changed:'', changed);
    prev.current = props;
  });
}','jsx','medium',ARRAY['react','performance','debugging']::text[],false,102),
('react','performance','react-performance-103','Give an example of using useMemo to avoid recomputing an expensive value on every render.','useMemo caches the result of a computation and only recalculates it when the values in its dependency array change, instead of on every render. It''s most useful for genuinely expensive work like sorting or filtering a large array, not trivial arithmetic where the memoization overhead can outweigh the savings.',null,'const sortedUsers = useMemo(
  () => [...users].sort((a, b) => b.score - a.score),
  [users]
);','jsx','medium',ARRAY['react','performance','usememo']::text[],true,103),
('react','performance','react-performance-104','How does useCallback prevent a memoized child component from re-rendering unnecessarily?','Without useCallback, a function defined inside a component body is a new reference on every render, which breaks React.memo''s shallow prop comparison on any child that receives it as a prop. useCallback returns the same function reference across renders as long as its dependencies haven''t changed, so a memoized child receiving it as a prop can correctly skip re-rendering.',null,'const handleSelect = useCallback((id) => setSelectedId(id), []);
return <MemoizedList items={items} onSelect={handleSelect} />;','jsx','medium',ARRAY['react','performance','usecallback']::text[],false,104),
('react','performance','react-performance-105','How can changing a component''s key be used to reset its internal state and remount it?','React uses the key prop to decide whether to reuse or discard a component instance between renders. Changing the key forces React to unmount the old instance (discarding all its internal state and effects) and mount a fresh one, which is a simple way to fully reset a complex form or widget instead of manually clearing every piece of state.',null,'<UserForm key={selectedUserId} userId={selectedUserId} />','jsx','medium',ARRAY['react','performance','key']::text[],false,105),
('react','performance','react-performance-106','Why does passing a new object or array literal as a prop on every render defeat React.memo?','React.memo does a shallow comparison of props, and object/array literals created inline (e.g. style={{ color: ''red'' }}) produce a brand-new reference every render even if the contents are identical. That means the shallow comparison always reports "changed," so the memoized child re-renders anyway — the fix is to hoist the literal outside the component or wrap it in useMemo.',null,'// Bad: new object every render
<Chart options={{ responsive: true }} />

// Better: stable reference
const chartOptions = useMemo(() => ({ responsive: true }), []);
<Chart options={chartOptions} />','jsx','medium',ARRAY['react','performance','memo']::text[],false,106),
('react','performance','react-performance-107','Are inline arrow functions in JSX actually a performance problem?','In most apps, no — creating a new function on every render is cheap, and modern JS engines handle it fine. It only becomes a real problem when that function is passed as a prop to a component wrapped in React.memo, because the new reference breaks the memoization and causes an otherwise-avoidable re-render; in that specific case, useCallback is worth reaching for.',null,null,null,'easy',ARRAY['react','performance']::text[],false,107),
('react','performance','react-performance-108','How would you lazy-load an image that''s below the fold in a React app?','The simplest approach is the native loading="lazy" attribute on an <img> tag, which defers loading until the image is near the viewport with no JavaScript required. For more control (like showing a placeholder or triggering earlier), an IntersectionObserver-based custom hook can toggle the image''s src only once it''s about to enter view.',null,'<img src={photo.url} loading="lazy" alt={photo.alt} />','jsx','easy',ARRAY['react','performance','images']::text[],false,108),
('react','performance','react-performance-109','What is route-based code splitting and how does it differ from lazy-loading a single component?','Route-based code splitting applies React.lazy at the routing layer so each page''s JS bundle is only downloaded when the user actually navigates to that route, rather than as part of the initial bundle. It differs from lazy-loading one component (like a modal) in scale and intent: it''s a deliberate architectural strategy to keep the initial load small, applied consistently across every top-level route rather than to a single heavy widget.',null,'const Dashboard = lazy(() => import(''./pages/Dashboard''));
const Settings = lazy(() => import(''./pages/Settings''));

<Routes>
  <Route path="/dashboard" element={<Dashboard />} />
  <Route path="/settings" element={<Settings />} />
</Routes>','jsx','medium',ARRAY['react','performance','code-splitting']::text[],false,109),
('react','performance','react-performance-110','How does tree-shaking reduce bundle size, and what can prevent it from working?','Tree-shaking is a build-time step where the bundler analyzes ES module imports/exports and removes code that''s never actually used, relying on the static (not dynamic) nature of ES module syntax to prove what''s safe to drop. It''s defeated by importing an entire library''s default export (e.g. import _ from ''lodash'' instead of import debounce from ''lodash/debounce''), by libraries shipped only as CommonJS, or by side-effectful module code the bundler can''t prove is safe to remove.',null,null,null,'medium',ARRAY['react','performance','bundling']::text[],false,110),
('react','performance','react-performance-111','Why should selectors reading from a global store be memoized?','A selector that derives a new array or object from store state (e.g. filtering a list) returns a new reference every time it runs, which can make consuming components think the data changed even when the underlying source didn''t, triggering unnecessary re-renders. Memoizing the selector (e.g. with reselect-style caching) ensures it only recomputes and returns a new reference when its actual inputs change.',null,'const selectActiveUsers = createSelector(
  (state) => state.users,
  (users) => users.filter((u) => u.active)
);','jsx','medium',ARRAY['react','performance','state-management']::text[],false,111),
('react','performance','react-performance-112','How can prop drilling contribute to unnecessary re-renders across a wide component tree?','When a value is passed down through several intermediate components just to reach a deeply nested one, every intermediate component re-renders whenever that value changes, even if it never uses the value itself. Lifting the value into Context or a store lets only the components that actually consume it re-render, rather than the whole chain in between.',null,null,null,'easy',ARRAY['react','performance','prop-drilling']::text[],false,112),
('react','performance','react-performance-113','Why can a Context value cause every consumer to re-render even if only part of the value changed?','Context consumers re-render whenever the Provider''s value prop changes by reference, regardless of which specific field inside that value actually changed. If the value is a single object containing many unrelated pieces of state, updating any one of them creates a new object and re-renders every consumer, even ones that only cared about a field that didn''t change.',null,'// One consumer only needs `theme`, but re-renders when `user` changes too
const { theme, user } = useContext(AppContext);','jsx','medium',ARRAY['react','performance','context']::text[],false,113),
('react','performance','react-performance-114','How would you debounce a search input''s onChange handler in a React component?','Keep the input''s displayed value in local state so typing feels instant, but wrap the expensive operation (an API call or a heavy filter) in a debounced function via useMemo or useEffect with a timeout, so it only fires once the user pauses typing. useDeferredValue in React 18 offers a related, built-in alternative for deferring a derived render without manually managing timers.',null,'const [query, setQuery] = useState('''');
useEffect(() => {
  const id = setTimeout(() => fetchResults(query), 300);
  return () => clearTimeout(id);
}, [query]);','jsx','medium',ARRAY['react','performance','debounce']::text[],true,114),
('react','performance','react-performance-115','What are the trade-offs of using react-window or react-virtualized for large lists?','Both libraries render only the rows currently visible in the viewport (plus a small buffer), dramatically cutting DOM node count and improving scroll performance for lists with thousands of items. The trade-off is added complexity: rows typically need a fixed or measurable height, browser find-in-page and some accessibility patterns need extra handling, and the API adds a layer of indirection compared to a plain .map().',null,null,null,'medium',ARRAY['react','performance','virtualization']::text[],false,115),
('react','performance','react-performance-116','How can reading layout properties like offsetHeight inside a useEffect cause layout thrashing?','Reading a layout property forces the browser to synchronously recalculate layout if any pending DOM writes haven''t been flushed yet. Interleaving reads and writes across multiple effects (read, write, read, write) forces this recalculation repeatedly in the same frame instead of once, which can visibly slow down updates — batching all reads before all writes, or using useLayoutEffect deliberately and sparingly, avoids the thrashing.',null,null,null,'hard',ARRAY['react','performance','dom']::text[],false,116),
('react','performance','react-performance-117','What performance benefit does React 18''s automatic batching provide?','Before React 18, multiple setState calls inside a promise callback, setTimeout, or native event handler each triggered a separate re-render. React 18 batches all state updates within a single tick automatically regardless of where they occur, so several setState calls in an async handler now produce just one re-render instead of several.',null,'function handleClick() {
  fetchData().then(() => {
    setLoading(false); // batched
    setData(result);   // batched — only one re-render total
  });
}','jsx','medium',ARRAY['react','performance','react-18']::text[],false,117),
('react','performance','react-performance-118','What does "concurrent rendering" mean in React 18+?','Concurrent rendering lets React prepare (render) multiple versions of the UI without committing them, and interrupt, pause, or abandon a render in progress if something more urgent (like user input) comes in. This is what enables features like useTransition and Suspense to keep an app responsive during expensive updates, in contrast to React''s older synchronous renderer which always ran a render to completion once started.',null,null,null,'hard',ARRAY['react','performance','concurrent-rendering']::text[],true,118),
('react','performance','react-performance-119','What problem does useTransition solve?','useTransition lets you mark a state update as non-urgent, so React can render it in the background and keep the UI responsive to more urgent updates (like typing) instead of blocking the main thread. A common use case is filtering a large list as the user types: the input stays instantly responsive while the (potentially slow) filtered list update happens as a lower-priority transition.',null,'const [isPending, startTransition] = useTransition();

function handleChange(e) {
  setQuery(e.target.value); // urgent
  startTransition(() => {
    setFilteredList(filterItems(e.target.value)); // non-urgent
  });
}','jsx','medium',ARRAY['react','performance','usetransition']::text[],true,119),
('react','performance','react-performance-120','What does useDeferredValue do and how does it differ from debouncing?','useDeferredValue returns a version of a value that "lags behind" the real value during expensive renders, letting React prioritize more urgent updates first and catch up when it has spare time. Unlike debouncing, it doesn''t use a fixed timer — React decides when to update the deferred value based on actual rendering capacity, so it adapts to the device''s performance instead of an arbitrary delay.',null,'const deferredQuery = useDeferredValue(query);
const results = useMemo(() => search(deferredQuery), [deferredQuery]);','jsx','medium',ARRAY['react','performance','usedeferredvalue']::text[],false,120),
('react','performance','react-performance-121','Which Web Vitals metrics are most relevant to a React app''s perceived performance?','Largest Contentful Paint (LCP) measures how long the main content takes to appear, which is heavily influenced by initial bundle size and data-fetch waterfalls in a React SPA. Interaction to Next Paint (INP) measures how responsive the app feels to clicks and typing, which is directly affected by expensive re-renders and long-running synchronous work on the main thread — exactly what memoization and transitions aim to fix.',null,null,null,'easy',ARRAY['react','performance','web-vitals']::text[],false,121),
('react','performance','react-performance-122','Why might having too many separate Context consumers hurt performance?','Every component that calls useContext subscribes to that Context and re-renders whenever its value changes, so spreading dozens of components across the tree as direct consumers of a frequently-updating Context means dozens of re-renders per update. Reducing the number of direct consumers (by reading the value once near the top and passing down only what''s needed, or splitting the Context) limits the blast radius of each update.',null,null,null,'medium',ARRAY['react','performance','context']::text[],false,122),
('react','performance','react-performance-123','Why would you split one large Context into several smaller ones?','If unrelated pieces of state (like theme and user session) live in one Context object, updating either one re-renders every consumer of the whole Context, including ones that only care about the other piece. Splitting into separate Context providers means a component only re-renders when the specific slice of state it actually reads changes.',null,'// Instead of one AppContext with { theme, user, cart }
const ThemeContext = createContext();
const UserContext = createContext();
const CartContext = createContext();','jsx','medium',ARRAY['react','performance','context']::text[],false,123),
('react','performance','react-performance-124','Can over-memoizing a component ever hurt performance?','Yes — every useMemo, useCallback, or React.memo adds a comparison check that itself costs time and memory, so wrapping cheap components or trivial computations in memoization can end up being slower than just letting them re-render normally. Memoization pays off when the wrapped work is genuinely expensive or the component re-renders very frequently with unchanged props; it shouldn''t be applied by default everywhere.',null,null,null,'medium',ARRAY['react','performance','memoization']::text[],false,124),
('react','performance','react-performance-125','What''s the difference between React''s render phase and commit phase, and why do side effects belong in the commit phase?','The render phase is where React calls your component functions to figure out what should change — it''s meant to be pure and can be paused, aborted, or restarted by React (especially under concurrent rendering), so it must have no side effects. The commit phase is where React actually applies changes to the real DOM and is guaranteed to run to completion, which is why useEffect (and DOM mutations, subscriptions, etc.) runs after commit rather than during render.',null,null,null,'hard',ARRAY['react','performance','internals']::text[],true,125),
('react','performance','react-performance-126','Why can inline style objects in JSX be less performant than CSS classes?','An inline style={{ ... }} object is a new object on every render, which both forces a new style recalculation and (as with any inline object) can break shallow prop comparisons on memoized children. CSS classes are parsed once by the browser and applying/removing them is comparatively cheap, and they also allow the browser to cache computed styles more effectively.',null,'// Less ideal
<div style={{ color: isActive ? ''blue'' : ''gray'' }} />

// Better
<div className={isActive ? ''text-active'' : ''text-inactive''} />','jsx','easy',ARRAY['react','performance','styling']::text[],false,126),
('react','performance','react-performance-127','Why does using array index as a key combined with .map() sometimes cause unnecessary re-renders?','When items are reordered, inserted, or removed, an index-based key stays attached to the same position rather than the same logical item, so React thinks the item at that position simply "changed" and re-renders/reconciles it with new props instead of recognizing it moved. Using a stable, unique identifier from the data itself (like an id) lets React correctly match items across renders and skip unnecessary work for items that didn''t actually change.',null,'// Fragile: index changes when list is reordered
{items.map((item, i) => <Row key={i} {...item} />)}

// Stable
{items.map((item) => <Row key={item.id} {...item} />)}','jsx','medium',ARRAY['react','performance','key']::text[],false,127),
('react','performance','react-performance-128','What strategies exist for optimizing images in a React application?','Serving correctly sized and modern-format images (WebP/AVIF), using loading="lazy" for offscreen images, specifying width/height to avoid layout shift, and using a CDN or image optimization service to generate responsive srcset variants are the main levers. None of these are React-specific, but they matter disproportionately in SPAs since images are often the largest contributor to LCP.',null,null,null,'easy',ARRAY['react','performance','images']::text[],false,128),
('react','performance','react-performance-129','At a high level, how do React Server Components reduce client-side work?','Server Components render on the server and send the result to the client as a lightweight description rather than shipping the component''s JavaScript to the browser at all, which reduces both bundle size and the amount of client-side rendering work. This is distinct from traditional SSR, which still ships the component code to the client for hydration — Server Components never run on the client in the first place.',null,null,null,'hard',ARRAY['react','performance','server-components']::text[],true,129),
('react','performance','react-performance-130','What is a request waterfall in data fetching and how can you avoid one in a React app?','A waterfall happens when a child component''s data fetch only starts after its parent''s fetch has finished and it has rendered, turning what could be parallel requests into a slow sequential chain. Avoiding it means starting independent fetches as early and in parallel as possible — for example, kicking off all needed requests at the top of a route before rendering children, or using Promise.all, rather than letting each nested component trigger its own fetch on mount.',null,'// Waterfall: profile fetch waits, then posts fetch waits
// Better: fire both immediately
const [profile, posts] = await Promise.all([fetchProfile(id), fetchPosts(id)]);','jsx','medium',ARRAY['react','performance','data-fetching']::text[],false,130),
('react','performance','react-performance-131','How can you use the <Profiler> component programmatically instead of just the DevTools Profiler UI?','Wrapping part of the tree in React''s built-in <Profiler> component with an onRender callback lets you capture render timings (actual duration, base duration, phase) in code, which is useful for automated performance regression tests or logging real-user render times in production, not just manual inspection in DevTools.',null,'<Profiler id="Dashboard" onRender={(id, phase, actualDuration) => {
  console.log(id, phase, actualDuration);
}}>
  <Dashboard />
</Profiler>','jsx','medium',ARRAY['react','performance','profiler']::text[],false,131),
('react','performance','react-performance-132','Why might a large component tree benefit from splitting into smaller components even without memoization?','Splitting a large component into smaller ones creates natural re-render boundaries: when state changes inside a small child, only that child (and its own children) re-renders, rather than the entire large component and everything inside it having to re-execute. This structural benefit comes for free from component composition, before any memoization is even introduced.',null,null,null,'easy',ARRAY['react','performance','components']::text[],false,132),
('react','performance','react-performance-133','What is the difference between the shallow comparison used by React.memo and deep equality?','React.memo''s default comparison checks each prop with Object.is (reference equality for objects/arrays/functions, value equality for primitives) one level deep — it does not look inside nested objects to see if their contents match. Deep equality would recursively compare every nested value, which is more accurate but far more expensive to run on every render, which is why React deliberately defaults to the cheaper shallow check.',null,'// Same content, different reference — shallow comparison says "changed"
React.memo(Chart)({ config: { color: ''red'' } });
React.memo(Chart)({ config: { color: ''red'' } });','jsx','medium',ARRAY['react','performance','memo']::text[],false,133),
('react','performance','react-performance-134','How does the useState functional updater form help avoid unnecessary effect dependencies?','Using setCount(c => c + 1) instead of setCount(count + 1) lets a callback update state based on the latest value without needing count itself in its closure or a useEffect dependency array, which avoids re-creating that callback (and re-running effects that depend on it) every time count changes.',null,'const increment = useCallback(() => {
  setCount((c) => c + 1); // no need to list `count` as a dependency
}, []);','jsx','medium',ARRAY['react','performance','usestate']::text[],false,134),
('react','performance','react-performance-135','Why can spreading all props ({...props}) onto a DOM element hurt performance and cause bugs?','Spreading an entire props object onto a native DOM element passes through anything the caller included, including props that aren''t valid HTML attributes, which React will warn about and the browser will ignore or mishandle. It also makes it harder for React (and the developer) to reason about which specific values actually affect rendering, since the element re-renders whenever any prop in that spread object changes, even unrelated ones.',null,'// Risky: passes through arbitrary props, including invalid HTML attrs
function Input(props) { return <input {...props} />; }','jsx','easy',ARRAY['react','performance','props']::text[],false,135),
('react','performance','react-performance-136','What''s the performance cost of creating a new array with .filter().map() on every render for a large list?','For a large array, chaining .filter().map() allocates two new arrays and does two full passes every single render, even if the underlying data hasn''t changed. Wrapping the derived array in useMemo keyed on the actual dependencies (the source array and filter criteria) avoids redoing that work on renders where nothing relevant changed.',null,'const visibleItems = useMemo(
  () => items.filter((i) => i.active).map((i) => ({ ...i, label: i.name.toUpperCase() })),
  [items]
);','jsx','medium',ARRAY['react','performance','usememo']::text[],false,136),
('react','performance','react-performance-137','How would you avoid re-creating a regex or complex object on every render?','If the value never depends on props or state, define it once outside the component (as a module-level constant) instead of inside the render function, so it''s created a single time regardless of how many times the component re-renders. If it does depend on props/state but is expensive to build, useMemo is the right tool instead.',null,'// Outside the component — created once, not per render
const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;

function validate(email) { return EMAIL_REGEX.test(email); }','jsx','easy',ARRAY['react','performance']::text[],false,137),
('react','performance','react-performance-138','What role does the diffing algorithm''s O(n) heuristic play in keeping reconciliation fast?','A fully general tree-diffing algorithm is O(n^3) in the number of nodes, which would be far too slow for UI updates. React''s reconciler instead uses heuristics — comparing elements of the same type at the same tree position, and using keys to match items in a list — reducing the comparison to roughly O(n), trading some theoretical accuracy (it can miss some optimal diffs) for practical speed.',null,null,null,'hard',ARRAY['react','performance','internals']::text[],false,138),
('react','performance','react-performance-139','Why is it recommended to avoid deeply nested Context providers for frequently changing values?','Every level of nested Context adds another subscription point, and if a frequently-updating value sits high in a deeply nested provider tree, every consumer anywhere below it re-renders on each update regardless of how deep it''s nested. For fast-changing values, keeping the state as local as possible, or using a store with fine-grained subscriptions instead of Context, avoids that broad re-render fan-out.',null,null,null,'medium',ARRAY['react','performance','context']::text[],false,139),
('react','performance','react-performance-140','What''s the benefit of colocating state as close as possible to where it''s used ("state colocation")?','Keeping state in the smallest, lowest component that needs it means updates to that state only re-render that component and its children, instead of a higher-up ancestor and its entire subtree. It also makes the codebase easier to reason about, since state that''s only relevant to one small piece of UI doesn''t leak into a larger shared scope.',null,null,null,'easy',ARRAY['react','performance','state']::text[],true,140),
('react','performance','react-performance-141','How can you measure the actual bundle size impact of a dependency before adding it?','Tools like a bundler''s built-in analyzer (e.g. rollup-plugin-visualizer, webpack-bundle-analyzer) or bundlephobia.com show the real minified/gzipped size a package adds, including its own transitive dependencies, before you commit to using it. This catches cases where a convenience library turns out to add far more weight than its usefulness justifies.',null,null,null,'easy',ARRAY['react','performance','bundling']::text[],false,141),
('react','performance','react-performance-142','What''s the difference between prefetching and lazy-loading a route in a React SPA?','Lazy-loading defers downloading a route''s code until the user actually navigates to it, which keeps the initial bundle small but adds a delay the first time that route is visited. Prefetching goes a step further by downloading a lazily-loaded route''s code ahead of time — for example, when the user hovers a link — so that by the time they click, the code is already cached and the navigation feels instant.',null,null,null,'medium',ARRAY['react','performance','code-splitting']::text[],false,142),
('react','performance','react-performance-143','Why might a large useEffect dependency array with unstable references cause performance issues?','If any dependency in the array is a new object, array, or function reference on every render, the effect re-runs on every render regardless of whether anything meaningful actually changed — potentially re-subscribing, re-fetching, or re-computing on every single render. Stabilizing those dependencies with useMemo/useCallback, or restructuring the effect to depend on primitive values instead, fixes the thrashing.',null,'// Runs every render because `options` is a new object each time
useEffect(() => { subscribe(options); }, [options]);','jsx','medium',ARRAY['react','performance','useeffect']::text[],false,143),
('react','performance','react-performance-144','What performance considerations apply when rendering a table with thousands of rows and interactive cells?','Beyond virtualizing the rows themselves, each interactive cell (inputs, buttons, dropdowns) multiplies the DOM node count and event listener overhead, so it''s worth minimizing per-cell complexity and memoizing row components so editing one cell doesn''t re-render the entire table. Sorting, filtering, and aggregation should also happen off the render path (memoized or computed incrementally) rather than being recalculated from scratch on every keystroke or click.',null,null,null,'hard',ARRAY['react','performance','tables']::text[],false,144),
('react','performance','react-performance-145','How does memoizing a component with children still allow the children to re-render when they should?','React.memo only controls whether the wrapped component itself re-executes based on its own props; the children prop is just another prop, and if the parent passing those children re-renders and creates new child elements, the memoized wrapper still receives a "changed" children prop and re-renders, which then lets those children render normally. Memoization only helps when the children passed in are themselves stable across renders (e.g. passed down unchanged from a higher, less-frequently-updating ancestor).',null,null,null,'hard',ARRAY['react','performance','memo']::text[],false,145),
('react','performance','react-performance-146','Why can excessive use of the Context API for frequently updating values be worse than prop drilling?','Prop drilling at least lets intermediate components stay untouched if you thread props manually with care, and tools exist to flag unnecessary passes; Context, by contrast, silently subscribes every consumer in the tree to updates, so a fast-changing Context value can cause far more widespread re-rendering than explicit prop passing would, especially if consumers are spread across many unrelated branches.',null,null,null,'medium',ARRAY['react','performance','context']::text[],false,146),
('react','performance','react-performance-147','What tools can you use to detect memory leaks caused by uncanceled subscriptions in React components?','The browser DevTools Memory tab (taking heap snapshots before/after mounting and unmounting a component repeatedly) can reveal detached DOM nodes or growing listener counts that indicate a leak. React''s Strict Mode also helps surface these bugs in development by intentionally double-invoking effects, which exposes missing cleanup functions that would otherwise leak a subscription, timer, or event listener on unmount.',null,null,null,'medium',ARRAY['react','performance','memory-leaks']::text[],false,147),
('react','performance','react-performance-148','How would you profile and optimize a slow initial page load in a React app, beyond just code splitting?','Beyond splitting the bundle, look at the network waterfall for render-blocking resources, minimize the critical CSS/JS needed for first paint, preload key fonts and above-the-fold images, and check whether server-side rendering or static generation could deliver meaningful content before the JS bundle even finishes downloading. Lighthouse and the Performance tab in DevTools are the standard tools for surfacing exactly which resource or task is dominating the load timeline.',null,null,null,'medium',ARRAY['react','performance','initial-load']::text[],false,148),
('react','performance','react-performance-149','What''s the performance impact of anonymous inline event handlers versus useCallback-wrapped ones in most apps?','For a plain DOM element (a button, an input) the difference is negligible — React doesn''t need a stable reference to attach an event listener efficiently, so an inline arrow function is perfectly fine there. The distinction only matters when that handler is passed down as a prop to a child wrapped in React.memo, where an unstable reference would defeat the memoization; outside of that specific case, reaching for useCallback everywhere is unnecessary overhead.',null,null,null,'easy',ARRAY['react','performance','usecallback']::text[],false,149),
('react','performance','react-performance-150','Why does batching multiple setState calls into one render matter for performance?','Without batching, each setState call would trigger its own full render-and-commit cycle, so an event handler that calls setState three times would re-render the component three times in a row for work that only needed to happen once. Batching collects all the updates that occur within the same tick and applies them together in a single render, which is both faster and avoids showing the UI in a series of inconsistent intermediate states.',null,'function handleSubmit() {
  setLoading(false);
  setError(null);
  setData(result); // all three batched into a single re-render
}','jsx','easy',ARRAY['react','performance','batching']::text[],true,150),
('react','state','react-state-101','Why shouldn''t you store a value in state if it can be computed from existing props or state?','Storing a derivable value in its own state creates two sources of truth that can drift out of sync whenever the underlying data changes but you forget to update the derived copy. It''s simpler and safer to compute it directly during render, optionally memoized with useMemo if expensive, so it''s always consistent with its source data by construction.',null,null,null,'medium',ARRAY['react','state','derived-state']::text[],true,101),
('react','state','react-state-102','What''s the problem with syncing a prop into state using useEffect, and what''s the better alternative?','Copying a prop into local state via useEffect creates a state update that lags one render behind the prop change, causing a visible flash of stale data and an extra unnecessary render. If the component truly needs its own editable copy of the prop, it''s better to derive the initial value directly during the first render, and if the prop can change later and should reset the local copy, use a key prop to remount instead of syncing with an effect.',null,null,null,'hard',ARRAY['react','state','useeffect']::text[],false,102),
('react','state','react-state-103','What does "state colocation" mean in React?','It means keeping a piece of state as close as possible to the components that actually use it, rather than lifting everything to a shared ancestor by default. This limits the render impact of a state change to the smallest necessary part of the tree and keeps components easier to reason about in isolation.',null,null,null,'easy',ARRAY['react','state']::text[],false,103),
('react','state','react-state-104','What signals suggest a component''s state logic should move from useState to useReducer?','Signs include several related pieces of state that always change together, next-state calculations that depend on the previous state in non-obvious ways, or update logic scattered across many event handlers that would benefit from being centralized and unit-testable as a single pure function.',null,null,null,'medium',ARRAY['react','state','usereducer']::text[],false,104),
('react','state','react-state-105','Why must a reducer function passed to useReducer be pure?','React may call a reducer more than once per dispatched action in some cases, such as in development with Strict Mode to help surface side effects, so a reducer that mutates its arguments or performs side effects like network calls could produce inconsistent or duplicated results. A reducer should only compute and return a new state object based on its arguments, with no side effects or mutation.',null,null,null,'medium',ARRAY['react','state','usereducer']::text[],false,105),
('react','state','react-state-106','How do useReducer and Context combine to create a lightweight global state pattern?','A single useReducer call lives in a top-level provider component, and its state and dispatch function are exposed through one or two Context providers, letting any descendant read the shared state or dispatch actions to update it without prop drilling. This mimics the core Redux pattern of a single store, actions, and a reducer using only built-in React APIs, at the cost of Context''s re-render characteristics at larger scale.',null,'const StateContext = createContext(null);
const DispatchContext = createContext(null);
function Provider({ children }) {
  const [state, dispatch] = useReducer(reducer, initialState);
  return (
    <StateContext.Provider value={state}>
      <DispatchContext.Provider value={dispatch}>{children}</DispatchContext.Provider>
    </StateContext.Provider>
  );
}','jsx','hard',ARRAY['react','state','usereducer','context']::text[],false,106),
('react','state','react-state-107','What does it mean to "normalize" state shape, and why does it help?','Normalizing means storing collections as a flat map keyed by ID, similar to a database table, instead of deeply nested arrays of arrays. It makes updating or looking up a single item O(1) and avoids duplicating the same entity in multiple nested places, which is a common source of state getting out of sync.',null,'// nested (harder to update one comment)
{ posts: [{ id: 1, comments: [{ id: 10, text: ''...'' }] }] }

// normalized (easy to update by id)
{ posts: { 1: { id: 1, commentIds: [10] } }, comments: { 10: { id: 10, text: ''...'' } } }','jsx','medium',ARRAY['react','state','normalization']::text[],false,107),
('react','state','react-state-108','How do you update a deeply nested piece of state without mutating it?','Spread every level of the object from the changed property up to the root, creating new objects for each level along the path while reusing the untouched siblings by reference. This is verbose for deeply nested shapes, which is itself a good argument for normalizing state or using a library like Immer to write seemingly-mutable updates that produce immutable results.',null,'setUser(prev => ({
  ...prev,
  address: { ...prev.address, city: ''Austin'' },
}));','jsx','medium',ARRAY['react','state','immutability']::text[],true,108),
('react','state','react-state-109','How do you add, remove, and update an item in an array stored in state?','Add with the spread operator to create a new array including the new item, remove with Array.filter to build a new array excluding the target, and update with Array.map to return a new array where only the matching item is replaced with a modified copy — each produces a brand-new array rather than mutating the original with push or splice, which React needs to detect the change.',null,'setItems(prev => [...prev, newItem]); // add
setItems(prev => prev.filter(i => i.id !== id)); // remove
setItems(prev => prev.map(i => i.id === id ? { ...i, done: true } : i)); // update','jsx','easy',ARRAY['react','state','immutability']::text[],true,109),
('react','state','react-state-110','How can a stale closure cause a race condition when updating state?','If an async callback, like a fetch handler, was created during a render where a variable had an old value, calling a state setter inside it based on that captured variable can overwrite newer state with outdated data once it finally resolves, especially if a more recent render''s callback resolves first. Using functional state updates, or checking a ref or flag that tracks the latest request, avoids acting on outdated captured values.',null,null,null,'hard',ARRAY['react','state','async']::text[],false,110),
('react','state','react-state-111','Why should you prefer setCount(c => c + 1) over setCount(count + 1) in most cases?','The functional form always receives React''s latest pending state value rather than whatever count was when the closure was created, so it''s safe even when multiple updates are queued in the same tick or the handler is called from a stale closure like inside a timer. It removes an entire category of "off by one" state bugs caused by batching.',null,'setCount(c => c + 1);','jsx','easy',ARRAY['react','state','usestate']::text[],true,111),
('react','state','react-state-112','What is "automatic batching" in React 18?','React 18 batches multiple state updates that happen within the same synchronous block of code, including inside promises, timeouts, and native event handlers and not just React''s own event handlers as in React 17, into a single re-render for better performance. It can be opted out of for a specific update using flushSync when an immediate, unbatched render is genuinely required.',null,null,null,'medium',ARRAY['react','state','performance']::text[],false,112),
('react','state','react-state-113','At a high level, when would you reach for a dedicated state management library instead of just Context?','Once an app has a lot of frequently-changing shared state, many independent consumers that shouldn''t all re-render together, or needs like time-travel debugging and middleware, dedicated libraries such as Redux Toolkit, Zustand, Jotai, or Recoil offer more efficient selective subscriptions and tooling than plain Context, which re-renders every consumer on any value change by default.',null,null,null,'medium',ARRAY['react','state','state-management']::text[],false,113),
('react','state','react-state-114','Why can Context cause more re-renders than expected, and how do you mitigate it?','Every component that calls useContext for a given Context re-renders whenever that Context''s value changes, even if the component only cares about one field of a larger value object, and a new object literal passed as the value on every render defeats memoization entirely. Mitigations include memoizing the value with useMemo, splitting one large context into several smaller, more narrowly-scoped ones, or using a selector-based library for fine-grained subscriptions.',null,'const value = useMemo(() => ({ user, theme }), [user, theme]);
<AppContext.Provider value={value}>{children}</AppContext.Provider>','jsx','hard',ARRAY['react','state','context','performance']::text[],true,114),
('react','state','react-state-115','How do you decide between passing a prop, using Context, or reaching for an external store?','Use props when only one or two levels of the tree need the data, since it''s the most explicit and easiest to trace. Reach for Context when several components at varying depths need read access to relatively stable, infrequently-changing data. Reach for an external store or library when state changes frequently, is needed widely, and re-render performance or cross-cutting features like persistence or middleware matter.',null,null,null,'medium',ARRAY['react','state','context']::text[],false,115),
('react','state','react-state-116','What''s the difference between "server state" and "client state," and why does the distinction matter?','Client state is data that only ever lives in the browser, like a modal''s open/closed flag or a form draft, fully owned by the app. Server state is a client-side cache of data that actually lives on a server and can go stale, requiring concerns like refetching, caching, and synchronization that plain useState or useReducer weren''t designed for, which is why libraries like React Query or SWR exist specifically to manage it.',null,null,null,'medium',ARRAY['react','state','data-fetching']::text[],false,116),
('react','state','react-state-117','What are the main tradeoffs between controlled state, uncontrolled refs, and a form library for managing form state?','Fully controlled state, with every field backed by useState, gives full control and easy validation but can cause a re-render on every keystroke across a large form. Uncontrolled inputs with refs avoid that re-render cost by reading values only on submit, at the expense of harder real-time validation. Form libraries like React Hook Form aim for the best of both by using uncontrolled inputs internally while still exposing a controlled-feeling API with built-in validation and minimal re-renders.',null,null,null,'hard',ARRAY['react','state','forms']::text[],false,117),
('react','state','react-state-118','In what sense is the URL a form of application state?','Values encoded in the path or query string, like a selected tab, page number, or filter, represent state just like a useState value would, but with the added benefit of being shareable, bookmarkable, and preserved across a refresh — which is why pagination and filters commonly live in the URL instead of component state.',null,null,null,'easy',ARRAY['react','state','routing']::text[],false,118),
('react','state','react-state-119','What''s a simple pattern for persisting a piece of state to localStorage?','Initialize state lazily by reading from localStorage on the first render, then use a useEffect that writes the current value to localStorage whenever it changes, commonly wrapped into a reusable useLocalStorage custom hook.',null,'const [value, setValue] = useState(() => JSON.parse(localStorage.getItem(key)) ?? initial);
useEffect(() => { localStorage.setItem(key, JSON.stringify(value)); }, [key, value]);','jsx','easy',ARRAY['react','state','localstorage']::text[],false,119),
('react','state','react-state-120','How can changing a component''s key prop be used to reset its internal state?','React treats a component with a different key as an entirely new element, unmounting the old instance and discarding its state, then mounting a fresh one — so changing a form component''s key when switching between editing different records resets all of its internal state instantly without manual reset logic.',null,'<UserForm key={selectedUserId} user={selectedUser} />','jsx','medium',ARRAY['react','state','keys']::text[],false,120),
('react','state','react-state-121','What does the "single source of truth" principle mean for state design?','Each piece of data should have exactly one place it''s stored and considered authoritative — every other place that needs it should read or derive from that one source rather than keeping its own separate copy, which is what keeps duplicated state from silently drifting out of sync.',null,null,null,'easy',ARRAY['react','state']::text[],false,121),
('react','state','react-state-122','What is a "waterfall" of state updates, and why is it a performance concern?','It''s when fetching or computing one piece of state depends on first fetching another, which depends on a third, causing sequential renders and network round-trips instead of parallel ones — for example, fetching a user, then waiting for that to fetch their permissions, then waiting for that to fetch their dashboard data. Restructuring to kick off independent requests in parallel, or colocating data needs, avoids the compounding latency.',null,null,null,'hard',ARRAY['react','state','performance','data-fetching']::text[],false,122),
('react','state','react-state-123','What is an optimistic UI update?','It''s updating local state to reflect the expected result of an action, like marking a todo as done, immediately, before the server has actually confirmed it, so the UI feels instant, and rolling the state back if the request later fails, rather than waiting for a round-trip before showing any change.',null,null,null,'medium',ARRAY['react','state','data-fetching']::text[],false,123),
('react','state','react-state-124','Why might you pass a function to useState instead of an object literal directly, even for state that isn''t expensive to compute?','Passing a function guarantees the initializer only runs once on mount, which matters when constructing the initial object involves calling something with side effects, like Date.now() or a random ID generator, that shouldn''t re-run on every render even though its result is discarded after the first.',null,'const [id] = useState(() => crypto.randomUUID());','jsx','easy',ARRAY['react','state','usestate']::text[],false,124),
('react','state','react-state-125','How do you share state across different pages or routes in a single-page app?','Lift the state above the router, for example in a top-level provider, so it persists across navigations instead of being recreated per route, use Context or an external store to make it accessible from any route, or encode it in the URL itself if it should also survive a full page refresh.',null,null,null,'medium',ARRAY['react','state','routing']::text[],false,125),
('react','state','react-state-126','What is a "state machine" approach to modeling UI state, and what problem does it solve?','Instead of tracking independent booleans like isLoading, isError, and isSuccess, which can combine into invalid states like both being true at once, a state machine defines one explicit status value that can only be one of a fixed set of named states with defined transitions between them, making impossible states actually impossible to represent.',null,'type Status = ''idle'' | ''loading'' | ''success'' | ''error'';
const [status, setStatus] = useState(''idle'');','tsx','medium',ARRAY['react','state','state-machines']::text[],false,126),
('react','state','react-state-127','Why is tracking isLoading, isError, and hasData as three separate booleans risky?','Nothing prevents them from ending up in a combination that shouldn''t be possible, like isLoading and isError both true at once, and the component has to guess which one "wins" when rendering — a single status enum makes only one state representable at a time and removes that ambiguity entirely.',null,null,null,'medium',ARRAY['react','state','state-machines']::text[],false,127),
('react','state','react-state-128','What is a "selector" in the context of external state stores, and why does it help performance?','A selector is a small function that reads and derives just the specific slice of a larger store''s state a component needs, and most store libraries only re-render components whose selected slice actually changed — so a component selecting just one field won''t re-render when unrelated parts of the store update, unlike a component that reads the whole store object.',null,null,null,'hard',ARRAY['react','state','state-management','performance']::text[],false,128),
('react','state','react-state-129','How can component composition reduce the need for prop drilling?','Instead of passing a value down through several layers just so a deeply nested component can use it, a parent can render the deeply nested component directly and pass it in as children or another element prop at the top level — the intermediate components never need to know about the value at all since they just render whatever element they were given.',null,'function Layout({ sidebar, content }) {
  return <div><aside>{sidebar}</aside><main>{content}</main></div>;
}
// caller renders <Layout sidebar={<UserPanel user={user} />} content={...} />','jsx','medium',ARRAY['react','state','composition']::text[],false,129),
('react','state','react-state-130','Why might you split one large Context into several smaller ones?','Since every consumer of a Context re-renders whenever its value changes, bundling unrelated data like theme, user, and notifications into one context means a change to any of them re-renders everything that reads any of them. Splitting into separate contexts per concern lets a component subscribe only to the slice it actually needs.',null,'<ThemeContext.Provider value={theme}>
  <UserContext.Provider value={user}>
    {children}
  </UserContext.Provider>
</ThemeContext.Provider>','jsx','medium',ARRAY['react','state','context','performance']::text[],false,130),
('react','state','react-state-131','Why does an effect depending on an object or array from state sometimes run more often than expected?','If that object or array is recreated as a new reference on every render, even with identical contents, an effect depending on it sees a "changed" dependency on every render and re-runs every time, since dependency comparison in React is by reference, not deep equality. Memoizing the object or array with useMemo, or depending on its primitive fields individually, fixes it.',null,null,null,'medium',ARRAY['react','state','useeffect']::text[],false,131),
('react','state','react-state-132','How do you immutably update a single item inside an array of objects by its id?','Map over the array, returning a new object for the matching item with the changed fields spread in, and passing every other item through unchanged, then set that new array as state.',null,'setTodos(prev => prev.map(t => t.id === id ? { ...t, done: true } : t));','jsx','easy',ARRAY['react','state','immutability']::text[],false,132),
('react','state','react-state-133','What''s the correct way to toggle a boolean piece of state?','Use the functional updater form, flipping the previous value, rather than referencing a possibly-stale captured variable — this works correctly even if the toggle is called multiple times in quick succession.',null,'setIsOpen(prev => !prev);','jsx','easy',ARRAY['react','state','usestate']::text[],false,133),
('react','state','react-state-134','Should a form''s fields be tracked as one state object or as separate useState calls per field?','A single object usually scales better for forms with many fields since one generic onChange handler can update any field by key, whereas separate useState calls require a separate handler, or handler factory, per field but avoid needing to spread the whole object on every keystroke.',null,'const [form, setForm] = useState({ name: '''', email: '''' });
function handleChange(e) {
  setForm(prev => ({ ...prev, [e.target.name]: e.target.value }));
}','jsx','medium',ARRAY['react','state','forms']::text[],false,134),
('react','state','react-state-135','How does a controlled input''s value relate to component state?','The input''s displayed value is always driven by a state variable, and every keystroke fires onChange, which updates that state — meaning React, not the DOM, is the single source of truth for what''s currently in the field at all times.',null,null,null,'easy',ARRAY['react','state','forms']::text[],false,135),
('react','state','react-state-136','How would you debounce a state update that drives an expensive operation, like a search API call?','Keep the raw input value in state as-is for instant UI feedback, but derive a second, debounced value that''s updated only after the user pauses typing for a delay using a small useEffect with setTimeout and clearTimeout, and trigger the expensive operation off that debounced value instead of the raw one.',null,'useEffect(() => {
  const id = setTimeout(() => setDebounced(query), 300);
  return () => clearTimeout(id);
}, [query]);','jsx','medium',ARRAY['react','state','performance']::text[],false,136),
('react','state','react-state-137','How can you keep a piece of state in sync across multiple open browser tabs?','Persist the value to localStorage, and in each tab listen for the browser''s native storage event, which fires in other tabs, not the one that made the change, whenever localStorage is modified — updating local state from that event keeps every open tab reflecting the same value.',null,'useEffect(() => {
  function onStorage(e) {
    if (e.key === ''theme'') setTheme(e.newValue);
  }
  window.addEventListener(''storage'', onStorage);
  return () => window.removeEventListener(''storage'', onStorage);
}, []);','jsx','hard',ARRAY['react','state','localstorage']::text[],false,137),
('react','state','react-state-138','What are the tradeoffs between localStorage, sessionStorage, and cookies for persisting state?','localStorage persists indefinitely across tabs and browser restarts but isn''t sent to the server and has no built-in expiry. sessionStorage is scoped to a single tab and cleared when it closes, useful for per-session drafts. Cookies are automatically sent with every request, useful for auth, but have a small size limit and add overhead to every network request if overused.',null,null,null,'medium',ARRAY['react','state','localstorage']::text[],false,138),
('react','state','react-state-139','Why is storing both an items array and a separate itemCount in state usually a mistake?','itemCount can always be derived as items.length, so keeping it as separate state means every place that adds or removes an item must remember to update both, and any place that forgets creates a silently incorrect count — deriving it on the fly avoids that entire class of bugs.',null,'const itemCount = items.length; // derive, don''t store separately','jsx','easy',ARRAY['react','state','derived-state']::text[],false,139),
('react','state','react-state-140','What does the "single writer" principle mean for shared state?','Only one clearly defined place, such as a specific reducer, store action, or component, should be responsible for actually changing a given piece of shared state — other parts of the app can read it or request a change through that single path, but shouldn''t mutate it directly, which keeps changes traceable and prevents conflicting updates from different places.',null,null,null,'medium',ARRAY['react','state','state-management']::text[],false,140),
('react','state','react-state-141','Is it ever appropriate to pass a setState function itself down to a child as a prop?','Yes — for simple cases, passing the setter directly is simpler than wrapping it in a named callback prop, and since a setState function''s identity from useState is stable, it doesn''t cause extra re-renders. Wrapping it in a more descriptive callback becomes worthwhile once the child needs more than a raw setter, like validation before updating.',null,'<Toggle open={open} setOpen={setOpen} />','jsx','easy',ARRAY['react','state','usestate']::text[],false,141),
('react','state','react-state-142','How does using a reducer help keep related state changes atomic?','A single dispatched action can update multiple related fields together inside one reducer call, avoiding the situation with multiple separate setState calls where, due to batching or a re-render triggered mid-sequence, a component could theoretically be read in a state where only some of several related updates have landed.',null,'case ''FORM_SUBMIT_SUCCESS'':
  return { ...state, status: ''success'', data: action.payload, error: null };','jsx','medium',ARRAY['react','state','usereducer']::text[],false,142),
('react','state','react-state-143','What''s a clean way to represent the state of an async operation like a fetch?','Model it as a single status field with a fixed set of values such as idle, loading, success, and error, alongside the data and error fields, rather than several independent booleans — so the UI can render one clear switch or if-chain based on status instead of untangling combinations of isLoading, isError, and hasData.',null,'const [state, setState] = useState({ status: ''idle'', data: null, error: null });','jsx','medium',ARRAY['react','state','data-fetching']::text[],false,143),
('react','state','react-state-144','What''s the risk of one large component owning most of an app''s state directly?','It becomes a bottleneck where any state change re-renders the whole subtree unless carefully optimized, it''s hard to reason about which piece of state affects which part of the UI, and it discourages reusing the smaller pieces elsewhere — breaking state up and colocating it closer to where it''s used keeps things more maintainable.',null,null,null,'medium',ARRAY['react','state','composition']::text[],false,144),
('react','state','react-state-145','How do you test a reducer''s state transitions?','Since a reducer is a pure function of state and action producing a new state, you can call it directly in a test with a given starting state and action, and assert on the returned state — no component rendering or React testing utilities are needed at all.',null,null,null,'easy',ARRAY['react','state','testing','usereducer']::text[],false,145),
('react','state','react-state-146','Why doesn''t a state variable reflect its new value immediately after calling its setter?','Calling a state setter schedules an update for the next render rather than mutating the variable in place — the variable inside the current function''s closure still holds the old value until that component re-executes on the next render, which is why reading it on the very next line still shows the previous value.',null,'setCount(count + 1);
console.log(count); // still logs the OLD value','jsx','easy',ARRAY['react','state','usestate']::text[],true,146),
('react','state','react-state-147','If you need to act on a state value right after updating it, what should you do instead of reading the state variable on the next line?','Either compute the new value in a local variable and use that directly, or move the logic that depends on the new value into a useEffect that runs after the update has actually been applied and the component has re-rendered.',null,'const next = count + 1;
setCount(next);
doSomethingWith(next); // use the local variable, not ''count''','jsx','medium',ARRAY['react','state','usestate']::text[],false,147),
('react','state','react-state-148','What''s a simple way to reset a component''s state back to its initial values?','Store the initial state as a constant, or a function that produces it, and call the same setter again with that constant — with useReducer, dispatching a dedicated reset action that returns the original initial state works well for resetting several fields at once.',null,'const initialState = { name: '''', email: '''' };
setForm(initialState); // reset','jsx','easy',ARRAY['react','state','forms']::text[],false,148),
('react','state','react-state-149','If two pieces of state must always stay consistent with each other, what''s the better approach than updating both in sync?','Store only one of them as actual state, and compute the other from it during render, using useMemo if the computation is costly — that way there''s no possibility of them drifting apart, since the derived value can never be set to an inconsistent value on its own.',null,null,null,'medium',ARRAY['react','state','derived-state']::text[],false,149),
('react','state','react-state-150','Is passing a prop through one or two intermediate components always a problem worth solving with Context?','No — passing a prop through a shallow, well-understood chain of one or two levels is often clearer and easier to trace than introducing Context, which adds an indirection that can make it harder to see where a value actually comes from. Context earns its complexity once the chain gets deep or the same value is needed from many unrelated branches of the tree.',null,null,null,'easy',ARRAY['react','state','context']::text[],false,150),
('html','accessibility','html-accessibility-101','What is web accessibility (a11y) and why does it matter?','Web accessibility means designing and building sites so people with disabilities — visual, auditory, motor, or cognitive — can perceive, understand, navigate, and interact with them, often using assistive technology like screen readers or switch devices. Beyond being a legal requirement in many jurisdictions (like the ADA or EN 301 549), accessible sites tend to also be more usable and SEO-friendly for everyone.',null,null,null,'easy',ARRAY['html','accessibility']::text[],true,101),
('html','accessibility','html-accessibility-102','What is ARIA and when should you use it, according to the first rule of ARIA?','ARIA (Accessible Rich Internet Applications) is a set of attributes that add semantic information to HTML for assistive technology, covering roles, states, and properties. The first rule of ARIA is: if a native HTML element or attribute already has the semantics and behavior you need, use it instead of re-purposing a generic element with ARIA — ARIA only changes what''s announced, never the element''s actual behavior or keyboard handling.',null,null,null,'medium',ARRAY['html','accessibility','aria']::text[],true,102),
('html','accessibility','html-accessibility-103','What is the difference between ARIA roles, states, and properties?','A role (like role="button" or role="alert") defines what an element is or how it functions for assistive technology. Properties (like aria-label or aria-required) describe characteristics that generally don''t change during the element''s lifecycle. States (like aria-checked or aria-expanded) represent dynamic conditions that change in response to user interaction and must be kept in sync with JavaScript.',null,'<button aria-expanded="false" aria-controls="menu">Menu</button>','html','medium',ARRAY['html','accessibility','aria']::text[],false,103),
('html','accessibility','html-accessibility-104','What is the purpose of the alt attribute on <img>, and when should it be left empty?','alt provides a text alternative describing an image''s content or function for screen readers and in cases the image fails to load. It should be left as an empty string (alt="") — not omitted — for purely decorative images with no informational value, which tells assistive technology to skip announcing it entirely rather than reading the filename.',null,'<img src="decorative-swirl.png" alt="">
<img src="chart-q3.png" alt="Q3 revenue grew 15% year over year">','html','easy',ARRAY['html','accessibility','media']::text[],true,104),
('html','accessibility','html-accessibility-105','What is the difference between aria-label, aria-labelledby, and a visible <label> or heading?','aria-label sets an accessible name directly as a string, useful when no visible text exists (like an icon-only button). aria-labelledby references the id(s) of other elements whose text content is concatenated to form the accessible name, useful for reusing existing visible text. A visible label/heading is always preferable when feasible, since it benefits sighted users too, not just assistive technology.',null,'<button aria-label="Close dialog">×</button>
<h2 id="billing-heading">Billing Address</h2>
<section aria-labelledby="billing-heading">...</section>','html','medium',ARRAY['html','accessibility','aria']::text[],true,105),
('html','accessibility','html-accessibility-106','What does the tabindex attribute do, and why should positive values generally be avoided?','tabindex="0" adds an element to the natural tab order at its position in the DOM. tabindex="-1" makes an element programmatically focusable (via .focus()) but removes it from the tab order. Positive values (tabindex="1", "2"...) force a custom tab order that overrides the DOM order, which is almost always a maintenance and usability trap since it''s easy to create an order that doesn''t match the visual layout.',null,'<div tabindex="0" role="button">Custom focusable element</div>','html','medium',ARRAY['html','accessibility']::text[],true,106),
('html','accessibility','html-accessibility-107','Why is it important to maintain a logical focus order that matches the visual reading order of a page?','Keyboard and screen reader users navigate primarily via Tab and arrow keys, relying on focus order to understand the page''s structure sequentially. If CSS repositions elements visually (e.g. with flexbox order or absolute positioning) without a matching DOM order, keyboard users experience a confusing, illogical jump between elements that doesn''t match what they see.',null,null,null,'medium',ARRAY['html','accessibility']::text[],false,107),
('html','accessibility','html-accessibility-108','What is a focus trap and when is it appropriate to use one?','A focus trap constrains keyboard Tab navigation to loop within a specific set of elements, most commonly a modal dialog, so users can''t accidentally tab to content behind or outside it while it''s open. It''s appropriate for true modal contexts, but must always include a way to close and return focus to the triggering element, and should never be applied to the whole page or non-modal content.',null,null,null,'hard',ARRAY['html','accessibility']::text[],false,108),
('html','accessibility','html-accessibility-109','What is the purpose of a ''skip to main content'' link?','A skip link is typically the first focusable element on the page, visually hidden until focused, that lets keyboard users jump directly to the main content, bypassing repetitive navigation menus that would otherwise need to be tabbed through on every single page load.',null,'<a class="skip-link" href="#main-content">Skip to main content</a>
...
<main id="main-content">','html','medium',ARRAY['html','accessibility']::text[],false,109),
('html','accessibility','html-accessibility-110','What is the difference between aria-hidden="true" and the hidden HTML attribute?','The hidden attribute removes an element from both the visual rendering and the accessibility tree entirely — it''s not displayed or announced. aria-hidden="true" hides an element only from assistive technology while it can remain visually present, which is useful for purely decorative icons next to visible text, but dangerous if misapplied to visible interactive content since it becomes invisible to screen readers while still focusable.',null,'<button>
  <svg aria-hidden="true">...</svg>
  Save
</button>','html','hard',ARRAY['html','accessibility','aria']::text[],false,110),
('html','accessibility','html-accessibility-111','What is an ARIA live region, and what''s the difference between aria-live="polite" and aria-live="assertive"?','A live region tells assistive technology to announce content changes within it even when it''s not currently focused, useful for things like form errors or async status updates. aria-live="polite" waits until the screen reader finishes its current announcement before reading the update, while aria-live="assertive" interrupts immediately — assertive should be reserved for urgent, time-sensitive messages like errors, since overuse is disruptive.',null,'<div aria-live="polite" id="status"></div>
<script>
  document.getElementById(''status'').textContent = ''Saved successfully'';
</script>','html','hard',ARRAY['html','accessibility','aria']::text[],false,111),
('html','accessibility','html-accessibility-112','How would you make a custom dropdown built from <div> elements accessible, versus using a native <select>?','A native <select> gets keyboard support, focus management, and correct roles for free. A custom div-based dropdown needs role="listbox"/role="option" (or role="combobox" for a search-filterable version), aria-expanded on the trigger, aria-activedescendant or explicit focus management for the highlighted option, and full keyboard handling for arrow keys, Enter, Escape, and typeahead — all of which is substantial work to replicate correctly, which is why native elements are preferred whenever the design allows.',null,null,null,'hard',ARRAY['html','accessibility','aria']::text[],false,112),
('html','accessibility','html-accessibility-113','Why is relying solely on color to convey information (like form errors or status) an accessibility problem?','Users with color blindness or low vision may not be able to distinguish the color difference, and screen reader users receive no information from color at all. Status or error states should be reinforced with text, icons, or patterns in addition to color — for example, an invalid field should also show error text, not just a red border.',null,null,null,'medium',ARRAY['html','accessibility']::text[],false,113),
('html','accessibility','html-accessibility-114','What is the WCAG minimum contrast ratio requirement for normal body text at the AA level?','WCAG 2.1 Level AA requires a contrast ratio of at least 4.5:1 between normal-sized text and its background, and at least 3:1 for large text (roughly 18pt+ or 14pt+ bold). Level AAA raises these to 7:1 and 4.5:1 respectively, and UI components/graphical objects have their own separate 3:1 minimum under WCAG 2.1''s non-text contrast criterion.',null,null,null,'medium',ARRAY['html','accessibility','wcag']::text[],false,114),
('html','accessibility','html-accessibility-115','How should decorative icon fonts or SVG icons used without accompanying text be made accessible?','An icon that conveys meaning on its own (like a trash icon acting as a delete button) needs an accessible name via aria-label on the interactive element itself, or visually-hidden text inside it. A purely decorative icon accompanying visible text should be hidden from assistive technology with aria-hidden="true" so it isn''t announced redundantly.',null,'<button aria-label="Delete item">
  <svg aria-hidden="true"><!-- trash icon --></svg>
</button>','html','medium',ARRAY['html','accessibility','aria']::text[],false,115),
('html','accessibility','html-accessibility-116','What does the accessibility tree represent, and how does it relate to the DOM?','The accessibility tree is a parallel structure the browser derives from the DOM, containing only the nodes and information relevant to assistive technology — each node''s role, accessible name, state, and value. Elements can be excluded from it (like those hidden via CSS, the hidden attribute, or aria-hidden), and this tree is what screen readers actually query, not the raw DOM.',null,null,null,'hard',ARRAY['html','accessibility']::text[],false,116),
('html','accessibility','html-accessibility-117','What is the accessible name of an element and how is it computed when multiple sources are present?','The accessible name is the text assistive technology announces to identify an element. Its computation follows a precedence order defined by the Accessible Name and Description Computation spec: aria-labelledby takes priority over aria-label, which takes priority over a native labeling mechanism (like a <label>), which takes priority over the element''s title attribute or visible text content.',null,null,null,'hard',ARRAY['html','accessibility','aria']::text[],false,117),
('html','accessibility','html-accessibility-118','Why should interactive elements always have a visible focus indicator, and when is it acceptable to remove the default outline?','A visible focus indicator lets keyboard users see which element currently has focus as they tab through the page; removing it with `outline: none` and providing nothing in its place makes the site unusable for keyboard-only users. It''s acceptable to customize (not remove) the default outline''s appearance, as long as a clear, sufficiently contrasted alternative replaces it, ideally only shown for keyboard users via the :focus-visible pseudo-class.',null,'button:focus-visible {
  outline: 2px solid #2563eb;
  outline-offset: 2px;
}','css','medium',ARRAY['html','accessibility','css']::text[],true,118),
('html','accessibility','html-accessibility-119','What are the four principles of WCAG, summarized by the acronym POUR?','Perceivable — information must be presentable in ways users can perceive (e.g. text alternatives, sufficient contrast). Operable — interface components must be usable via keyboard and given enough time. Understandable — content and operation must be predictable and clear. Robust — content must work reliably across current and future assistive technologies and browsers.',null,null,null,'medium',ARRAY['html','accessibility','wcag']::text[],false,119),
('html','accessibility','html-accessibility-120','How would you make an image that''s also a link accessible, given the image might have decorative alt text elsewhere on the page?','When an <img> is wrapped in an <a>, the alt text should describe the link''s destination or purpose, not just the image''s visual content — for example alt="Go to homepage" rather than alt="Company logo", since the accessible name of the whole link is derived from the image''s alt text when no other link text is present.',null,'<a href="/">
  <img src="logo.png" alt="Acme Inc. home">
</a>','html','medium',ARRAY['html','accessibility','media']::text[],false,120),
('html','accessibility','html-accessibility-121','What role does the <button> element''s implicit keyboard behavior play, and why does recreating it with a styled <div> often go wrong?','A native <button> is automatically focusable, is announced as ''button'' with its accessible name, and responds to both Enter and Space key presses to activate its click handler. A <div onclick="..."> has none of this by default — developers often forget tabindex="0", role="button", and manually wiring both Enter and Space key handlers, resulting in a control that''s invisible to keyboard users.',null,null,null,'medium',ARRAY['html','accessibility']::text[],false,121),
('html','accessibility','html-accessibility-122','What is the difference between aria-describedby and aria-labelledby?','aria-labelledby contributes to (or overrides) an element''s accessible name — its primary identification. aria-describedby provides supplementary descriptive text, exposed to assistive technology as a description rather than the name, commonly used to associate hint text or validation error messages with an input without duplicating the label.',null,'<input id="pwd" type="password" aria-describedby="pwd-hint">
<p id="pwd-hint">Must be at least 8 characters.</p>','html','medium',ARRAY['html','accessibility','aria']::text[],false,122),
('html','accessibility','html-accessibility-123','How should form validation errors be announced to screen reader users when a form is submitted with invalid fields?','Errors should be programmatically associated with their field via aria-describedby, the invalid field should get aria-invalid="true", and a summary of errors is often placed in a live region (or given focus) at the top of the form so screen reader users are immediately informed rather than having to discover errors by tabbing through every field again.',null,'<input id="email" aria-invalid="true" aria-describedby="email-error">
<span id="email-error" role="alert">Enter a valid email address.</span>','html','hard',ARRAY['html','accessibility','forms']::text[],false,123),
('html','accessibility','html-accessibility-124','Why is using role=''presentation'' or role=''none'' on a table sometimes necessary, and what does it do?','role="presentation" (or its alias role="none") strips an element''s default semantic role from the accessibility tree while keeping it visually unchanged, historically used on <table> elements that were being (mis)used purely for layout rather than tabular data, so screen readers wouldn''t announce confusing row/column navigation for what is really just a visual grid.',null,'<table role="presentation">
  <tr><td>Logo</td><td>Nav</td></tr>
</table>','html','hard',ARRAY['html','accessibility','aria']::text[],false,124),
('html','accessibility','html-accessibility-125','What are landmark roles and how do screen reader users typically use them to navigate a page?','Landmark roles (banner, navigation, main, complementary, contentinfo, search, form) mark out major regions of a page, most of which map directly to semantic HTML elements (<header>, <nav>, <main>, <aside>, <footer>). Screen reader users can pull up a landmarks list to jump directly between these regions instead of navigating linearly through the whole page.',null,null,null,'medium',ARRAY['html','accessibility','aria']::text[],false,125),
('html','accessibility','html-accessibility-126','What is the difference between a screen reader announcing an element''s role automatically versus needing an explicit ARIA role?','Native HTML elements have implicit ARIA roles baked into the browser''s accessibility mapping — a <button> is automatically role ''button'', an <a href> is role ''link'' — so no explicit role attribute is needed. An explicit role is only necessary when using a generic element (like <div> or <span>) to build a custom widget that has no native HTML equivalent, or in rare cases to override an element''s default semantics.',null,null,null,'medium',ARRAY['html','accessibility','aria']::text[],false,126),
('html','accessibility','html-accessibility-127','How does the prefers-reduced-motion media feature relate to HTML/CSS accessibility, even though it''s a CSS feature?','prefers-reduced-motion lets a page detect that the user has requested reduced motion at the OS level (often due to vestibular disorders triggered by animation) and respond by disabling or simplifying animations and transitions defined in the page''s CSS. It''s an accessibility consideration that spans both markup (structuring content so it''s still usable without motion cues) and CSS implementation.',null,'@media (prefers-reduced-motion: reduce) {
  * { animation: none !important; transition: none !important; }
}','css','hard',ARRAY['html','accessibility','css']::text[],false,127),
('html','accessibility','html-accessibility-128','What common testing tools or techniques would you use to audit a page''s accessibility?','Automated tools like axe DevTools, Lighthouse, or WAVE catch programmatically detectable issues (missing alt text, low contrast, missing labels), but they only cover a subset of WCAG criteria. Manual testing — navigating the page using only the keyboard, and testing with an actual screen reader like NVDA, JAWS, or VoiceOver — is necessary to catch issues like illogical focus order or unclear announcements that automated tools can''t detect.',null,null,null,'medium',ARRAY['html','accessibility']::text[],false,128),
('html','accessibility','html-accessibility-129','What is the purpose of visually-hidden (screen-reader-only) CSS classes, and how do they differ from display: none?','A visually-hidden class positions content off-screen or collapses it to a 1px box (without using display: none or visibility: hidden, which both remove it from the accessibility tree) so it remains readable by screen readers while being invisible sighted users — commonly used to give icon-only buttons extra descriptive text without cluttering the visual design.',null,'.sr-only {
  position: absolute;
  width: 1px; height: 1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
}','css','hard',ARRAY['html','accessibility','css']::text[],false,129),
('html','accessibility','html-accessibility-130','Why does an accordion or tab widget built with plain <div>s need role, aria-selected, and keyboard handling, while <details>/<summary> doesn''t?','<details>/<summary> is a native disclosure widget that the browser already exposes with the correct implicit role, expanded state, and keyboard behavior (click or Enter toggles it, and it''s reachable via Tab). A custom div-based tab or accordion widget has none of that built in, so the developer must manually add roles like tab/tabpanel, manage aria-selected/aria-expanded state, and implement arrow-key or Enter/Space handling to match the ARIA Authoring Practices pattern for that widget.',null,null,null,'medium',ARRAY['html','accessibility','aria']::text[],false,130),
('html','basics','html-basics-101','What does HTML stand for and what is its role in web development?','HTML stands for HyperText Markup Language. It provides the structural skeleton of a web page by defining elements like headings, paragraphs, links, and images, which the browser then parses into the DOM. CSS handles presentation and JavaScript handles behavior, but HTML is the content and structure layer they both build on.',null,null,null,'easy',ARRAY['html','basics']::text[],true,101),
('html','basics','html-basics-102','What is the purpose of the <!DOCTYPE html> declaration?','The doctype tells the browser to render the page in standards mode according to the HTML5 specification, rather than falling back to quirks mode, which emulates older, inconsistent browser behavior. It isn''t an HTML tag itself but an instruction to the parser and must be the very first line of the document.',null,'<!DOCTYPE html>
<html lang="en">
  <head></head>
  <body></body>
</html>','html','easy',ARRAY['html','basics','document-structure']::text[],false,102),
('html','basics','html-basics-103','What is the difference between an HTML element and an HTML tag?','A tag is the markup syntax itself, like <p> (opening tag) or </p> (closing tag). An element is the complete unit made up of the opening tag, its content, and the closing tag (or a single self-closing tag for void elements). People often use the terms interchangeably, but technically the element is the whole structure.',null,null,null,'easy',ARRAY['html','basics']::text[],false,103),
('html','basics','html-basics-104','What are void (self-closing) elements in HTML? Give examples.','Void elements cannot have any child content and never need a closing tag because they represent a single, complete piece of markup, such as an image or a line break. Common examples are <img>, <br>, <hr>, <input>, <meta>, and <link>. In HTML5 the trailing slash (<br />) is optional and purely stylistic.',null,'<img src="logo.png" alt="Company logo">
<br>
<hr>','html','easy',ARRAY['html','basics','elements']::text[],false,104),
('html','basics','html-basics-105','What is the difference between the <div> and <span> elements?','<div> is a block-level container that starts on a new line and takes up the full available width by default, used to group larger chunks of content. <span> is an inline container that only wraps its content without forcing a line break, used for styling or scripting a small portion of text within a line.',null,'<div>Block-level content</div>
<p>Some text with a <span>highlighted word</span> inline.</p>','html','easy',ARRAY['html','basics','elements']::text[],true,105),
('html','basics','html-basics-106','What is the difference between block-level and inline elements in HTML?','Block-level elements (like <div>, <p>, <h1>) always start on a new line and expand to fill the width of their parent container, and they can contain other block or inline elements. Inline elements (like <span>, <a>, <strong>) flow within the surrounding text, only take up as much width as their content needs, and generally can''t contain block-level children.',null,null,null,'easy',ARRAY['html','basics','css']::text[],false,106),
('html','basics','html-basics-107','What is the difference between an HTML attribute and a DOM property?','Attributes are defined in the HTML markup and represent the initial state of an element; they''re always strings. Properties live on the corresponding DOM object in JavaScript and can hold other types, and for many attributes (like value or checked) the property reflects the current live state, which can diverge from the original attribute after user interaction.',null,'// input.getAttribute(''value'') stays the initial HTML value
// input.value reflects what the user currently typed','javascript','medium',ARRAY['html','basics','dom']::text[],false,107),
('html','basics','html-basics-108','What is the purpose of the lang attribute on the <html> tag?','The lang attribute declares the primary language of the page''s content, such as lang="en" or lang="fr". It helps screen readers choose the correct pronunciation rules, lets browsers offer accurate translation prompts, and assists search engines with language-specific indexing.',null,'<html lang="en">','html','easy',ARRAY['html','basics','accessibility']::text[],false,108),
('html','basics','html-basics-109','What is the difference between id and class attributes?','id must be unique within a page and is used to identify a single specific element, commonly for anchor links, JavaScript targeting, or label associations. class can be applied to multiple elements and an element can have multiple classes, making it suited for reusable styling and grouping.',null,'<button id="submit-btn" class="btn btn-primary">Submit</button>','html','easy',ARRAY['html','basics','css']::text[],false,109),
('html','basics','html-basics-110','What are HTML comments and how do you write them?','HTML comments are written as <!-- comment text --> and are ignored by the browser''s rendering engine, though they remain visible in the page source. They''re used to leave notes for developers or temporarily disable a block of markup, but shouldn''t be relied on to hide sensitive information since anyone can view them.',null,'<!-- TODO: replace with dynamic content -->
<p>Static placeholder</p>','html','easy',ARRAY['html','basics']::text[],false,110),
('html','basics','html-basics-111','What is the difference between HTML entities and raw special characters?','Characters like <, >, and & have special meaning in HTML markup, so to display them literally you must use their entity references, such as &lt;, &gt;, and &amp;. Using the raw character instead can break parsing or be misinterpreted as the start of a tag or entity.',null,'<p>5 &lt; 10 &amp;&amp; 10 &gt; 5</p>','html','medium',ARRAY['html','basics']::text[],false,111),
('html','basics','html-basics-112','What is the correct basic structure of an HTML document?','A valid HTML document starts with <!DOCTYPE html>, followed by an <html> root element containing a <head> (metadata, title, links to stylesheets/scripts) and a <body> (the visible page content). Browsers are lenient about missing tags, but omitting them can cause inconsistent rendering or invalid markup.',null,'<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>My Page</title>
</head>
<body>
  <h1>Hello World</h1>
</body>
</html>','html','easy',ARRAY['html','basics','document-structure']::text[],true,112),
('html','basics','html-basics-113','What is the difference between the <head> and <body> sections of an HTML document?','The <head> contains metadata that isn''t directly rendered as page content, such as the title, character encoding, linked stylesheets, and meta tags. The <body> contains everything the user actually sees and interacts with, like text, images, and forms.',null,null,null,'easy',ARRAY['html','basics','document-structure']::text[],false,113),
('html','basics','html-basics-114','How do you create hyperlinks in HTML, and what does the target attribute do?','Hyperlinks are created with the <a> tag and an href attribute pointing to a URL, fragment, or resource. The target attribute controls where the link opens; target="_blank" opens it in a new tab, and it should be paired with rel="noopener noreferrer" to prevent the new page from accessing window.opener for security reasons.',null,'<a href="https://example.com" target="_blank" rel="noopener noreferrer">Visit site</a>','html','easy',ARRAY['html','basics','links']::text[],false,114),
('html','basics','html-basics-115','What is the difference between relative and absolute URLs in HTML?','An absolute URL includes the full path, including protocol and domain (e.g. https://example.com/about), and always points to the same location regardless of where the current page is hosted. A relative URL (e.g. /about or ../images/logo.png) is resolved based on the current page''s location, making it more portable across environments like staging and production.',null,'<a href="/about">About</a>
<a href="https://example.com/about">About (absolute)</a>','html','easy',ARRAY['html','basics','links']::text[],false,115),
('html','basics','html-basics-116','How do you create ordered and unordered lists in HTML?','An unordered list uses <ul> with <li> child items, rendered with bullet points by default. An ordered list uses <ol> with <li> items, rendered with numbers, and supports a start attribute and a type attribute (e.g. "a", "i") to change the numbering style.',null,'<ul>
  <li>Apples</li>
  <li>Oranges</li>
</ul>
<ol start="3">
  <li>Step three</li>
  <li>Step four</li>
</ol>','html','easy',ARRAY['html','basics','elements']::text[],false,116),
('html','basics','html-basics-117','What is the purpose of the <table> element and its child elements like <thead>, <tbody>, and <tfoot>?','<table> defines a tabular data grid, with <tr> for rows, <th> for header cells, and <td> for data cells. <thead>, <tbody>, and <tfoot> group rows semantically into header, body, and footer sections, which helps both styling (e.g. sticky headers) and accessibility, since screen readers can announce column headers correctly.',null,'<table>
  <thead>
    <tr><th>Name</th><th>Age</th></tr>
  </thead>
  <tbody>
    <tr><td>Alice</td><td>30</td></tr>
  </tbody>
</table>','html','medium',ARRAY['html','basics','tables']::text[],false,117),
('html','basics','html-basics-118','What are data-* attributes and when would you use them?','data-* attributes let you store custom, private data on HTML elements without resorting to non-standard attributes or extra DOM properties. They''re accessible in JavaScript via the element.dataset object and are commonly used to pass server-rendered values to client-side scripts or to hook up event delegation.',null,'<button data-user-id="42">Delete</button>
<script>
  button.addEventListener(''click'', () => console.log(button.dataset.userId));
</script>','html','medium',ARRAY['html','basics','dom']::text[],false,118),
('html','basics','html-basics-119','What is the difference between HTML and XHTML?','XHTML is a stricter, XML-based reformulation of HTML that requires well-formed markup — every tag must be properly closed, attribute values must be quoted, and tags must be lowercase, or the browser refuses to render the page. HTML5 is more forgiving and error-tolerant, which is why it largely superseded XHTML in modern web development.',null,null,null,'hard',ARRAY['html','basics']::text[],false,119),
('html','basics','html-basics-120','How does the browser handle invalid or malformed HTML?','HTML parsers are designed to be error-tolerant: browsers apply well-defined error-recovery rules to auto-close unclosed tags, move misplaced elements, and reparent invalid nesting so the page still renders instead of failing outright. This forgiving behavior is convenient but can lead to unpredictable layout bugs, so writing valid, well-nested markup is still best practice.',null,null,null,'hard',ARRAY['html','basics','parsing']::text[],false,120),
('html','basics','html-basics-121','What is the difference between the <b> / <i> tags and the <strong> / <em> tags?','<strong> and <em> carry semantic meaning — strong importance and stressed emphasis respectively — which is conveyed to assistive technology and typically rendered as bold/italic. <b> and <i> are purely presentational (bold and italic styling) with no semantic weight, so they''re appropriate only when the visual style has no underlying importance, like a keyword or a book title.',null,'<p><strong>Warning:</strong> this action is <em>irreversible</em>.</p>','html','medium',ARRAY['html','basics','semantic-html']::text[],true,121),
('html','basics','html-basics-122','What does the contenteditable attribute do?','Setting contenteditable="true" on an element makes its content directly editable by the user in the browser, essentially turning it into a rich text editing surface without needing a <textarea> or <input>. It''s commonly used to build custom WYSIWYG editors but requires careful sanitization since it can introduce XSS risks if the resulting HTML is saved and re-rendered.',null,'<div contenteditable="true">Click here and start typing...</div>','html','medium',ARRAY['html','basics','elements']::text[],false,122),
('html','basics','html-basics-123','What is the purpose of the <template> element?','<template> holds markup that is parsed by the browser but not rendered or executed until it''s explicitly cloned and inserted into the DOM via JavaScript. It''s useful for defining reusable chunks of markup, such as row templates for a table, without them appearing on the page or triggering resource loads (like images) until activated.',null,'<template id="row-template">
  <tr><td class="name"></td></tr>
</template>
<script>
  const clone = document.getElementById(''row-template'').content.cloneNode(true);
</script>','html','hard',ARRAY['html','basics','dom']::text[],false,123),
('html','basics','html-basics-124','What is the difference between the <script> attributes async and defer?','Both let a script download in parallel with HTML parsing instead of blocking it. defer executes the script only after parsing finishes, in the order the scripts appear in the document. async executes the script as soon as it finishes downloading, which can be before parsing completes and out of order relative to other async scripts.',null,'<script src="app.js" defer></script>
<script src="analytics.js" async></script>','html','medium',ARRAY['html','basics','performance']::text[],true,124),
('html','basics','html-basics-125','Why is it generally recommended to place <script> tags at the end of the <body>, or use defer?','Script tags without async or defer block HTML parsing while they download and execute, delaying when the page becomes visible and interactive. Placing them at the end of <body>, or marking them defer, lets the browser render the visible content first and fetch/run scripts without blocking the critical rendering path.',null,null,null,'medium',ARRAY['html','basics','performance']::text[],false,125),
('html','basics','html-basics-126','What is the difference between the <ol>, <ul>, and <dl> elements?','<ol> and <ul> represent ordered and unordered lists of items respectively, using <li> children. <dl> represents a description (or definition) list, pairing <dt> terms with one or more <dd> descriptions, commonly used for glossaries, FAQs, or metadata key-value pairs.',null,'<dl>
  <dt>HTML</dt>
  <dd>HyperText Markup Language</dd>
</dl>','html','medium',ARRAY['html','basics','elements']::text[],false,126),
('html','basics','html-basics-127','What is the purpose of the colspan and rowspan attributes in a table?','colspan lets a table cell (<td> or <th>) span across multiple columns, and rowspan lets it span across multiple rows, both used to merge cells for layouts like grouped headers. They must be used carefully because they can make tables harder for screen readers to interpret correctly if not paired with proper header associations.',null,'<tr>
  <th colspan="2">Full Name</th>
</tr>
<tr>
  <td>Jane</td><td>Doe</td>
</tr>','html','medium',ARRAY['html','basics','tables']::text[],false,127),
('html','basics','html-basics-128','What character encoding should modern HTML documents declare, and why does it matter?','Modern documents should declare <meta charset="UTF-8"> as the first element in <head>, which covers virtually every character and symbol used worldwide. Declaring the wrong or no encoding can cause the browser to misinterpret bytes, producing garbled text (''mojibake'') for non-ASCII characters like accented letters or emoji.',null,'<head>
  <meta charset="UTF-8">
</head>','html','easy',ARRAY['html','basics','document-structure']::text[],false,128),
('html','basics','html-basics-129','What is the difference between the <pre> element and normal text rendering?','By default, browsers collapse consecutive whitespace and line breaks in HTML into a single space. <pre> preserves whitespace and line breaks exactly as written in the source, and renders text in a monospace font by default, which makes it useful for displaying code blocks or ASCII-formatted content.',null,'<pre>
function add(a, b) {
  return a + b;
}
</pre>','html','medium',ARRAY['html','basics','elements']::text[],false,129),
('html','basics','html-basics-130','How do you comment out a section of HTML to prevent it from rendering, and what are the limitations of doing so?','Wrapping markup in <!-- --> prevents it from rendering, but HTML comments cannot be nested — if the commented content itself contains ''--'', it can prematurely close the comment. Unlike JavaScript or CSS comments, HTML comments also can''t be used inside attribute values or tag names.',null,'<!-- <p>This paragraph is hidden</p> -->','html','hard',ARRAY['html','basics']::text[],false,130),
('html','basics','html-basics-131','What is the difference between the <a> element''s href and the deprecated <a name="..."> anchor pattern?','Historically, <a name="section"> created a named anchor that other links could jump to with #section. This is deprecated in favor of giving any element an id attribute, since any element (not just <a>) can now be a link target via its id, and href continues to define the destination of a link, not the target.',null,'<h2 id="faq">FAQ</h2>
<a href="#faq">Jump to FAQ</a>','html','hard',ARRAY['html','basics','links']::text[],false,131),
('html','basics','html-basics-132','What is the purpose of the <base> element?','<base href="..."> set in the <head> defines a default base URL that all relative URLs on the page (links, images, form actions) are resolved against, overriding the document''s own URL. It can only appear once per document and must come before any element referencing a URL.',null,'<head>
  <base href="https://example.com/app/">
</head>','html','hard',ARRAY['html','basics','document-structure']::text[],false,132),
('html','basics','html-basics-133','What are custom elements and the general idea behind Web Components?','Web Components is a set of browser APIs (Custom Elements, Shadow DOM, HTML Templates) that let developers define their own reusable, encapsulated HTML tags, like <my-widget>, with their own behavior and scoped styling. Custom elements must contain a hyphen in their tag name to avoid colliding with future standard HTML elements.',null,'class MyWidget extends HTMLElement {
  connectedCallback() {
    this.textContent = ''Hello from custom element'';
  }
}
customElements.define(''my-widget'', MyWidget);','javascript','hard',ARRAY['html','basics','web-components']::text[],false,133),
('html','basics','html-basics-134','What is the difference between the <link> and <a> elements?','<link> goes in the <head> and defines a relationship between the current document and an external resource, most commonly a stylesheet (rel="stylesheet"), without producing visible content. <a> creates a clickable hyperlink in the page body that navigates the user to another resource or location.',null,'<link rel="stylesheet" href="styles.css">','html','easy',ARRAY['html','basics','elements']::text[],false,134),
('html','basics','html-basics-135','What does the disabled attribute do, and how does it differ across form elements?','The disabled boolean attribute prevents an element from being interacted with, focused, or submitted as part of a form, and browsers typically render it visually greyed out. Unlike readonly, a disabled input''s value is excluded entirely from form submission data, whereas a readonly input''s value is still submitted.',null,'<button disabled>Submit</button>
<input type="text" disabled value="Can''t edit or submit this">','html','medium',ARRAY['html','basics','forms']::text[],false,135),
('html','basics','html-basics-136','What is progressive enhancement in the context of HTML?','Progressive enhancement is the practice of building a baseline experience with plain, semantic HTML that works without CSS or JavaScript, then layering on styling and interactivity as enhancements for browsers that support them. It ensures the core content and functionality remain accessible even if a script fails to load or a user has JavaScript disabled.',null,null,null,'hard',ARRAY['html','basics','best-practices']::text[],false,136),
('html','forms','html-forms-101','What is the purpose of the <form> element''s action and method attributes?','action specifies the URL the form''s data is submitted to, and method specifies the HTTP method used — typically GET (appends data as a query string, suitable for searches/filters) or POST (sends data in the request body, suitable for submissions that create or modify data). If action is omitted, the form submits to the current page''s URL.',null,'<form action="/submit" method="post">
  <input type="text" name="username">
  <button type="submit">Send</button>
</form>','html','easy',ARRAY['html','forms']::text[],true,101),
('html','forms','html-forms-102','Why is the name attribute required on form inputs, and how does it relate to the id attribute?','The name attribute determines the key used when the input''s value is serialized and submitted with the form (e.g. name="email" produces email=... in the request). id is unrelated to submission — it''s used for referencing the element in CSS, JavaScript, or associating it with a <label> via the label''s for attribute — an input can have one without the other, though most have both.',null,'<label for="email-input">Email</label>
<input id="email-input" name="email" type="email">','html','medium',ARRAY['html','forms']::text[],false,102),
('html','forms','html-forms-103','Why should every form input have an associated <label>?','A properly associated <label> gives the input an accessible name that screen readers announce, and clicking the label text focuses or activates the input, which is especially helpful for small targets like checkboxes and radio buttons. Association is done either by wrapping the input inside the label, or by matching the label''s for attribute to the input''s id.',null,'<label>
  <input type="checkbox" name="subscribe"> Subscribe to newsletter
</label>','html','easy',ARRAY['html','forms','accessibility']::text[],true,103),
('html','forms','html-forms-104','What is the difference between GET and POST as form submission methods, beyond how they pass data?','GET requests are idempotent and cacheable, and data appears visibly in the URL, so they should never be used for sensitive data or operations with side effects. POST requests are not cached or bookmarked by default, don''t expose data in the URL, and can carry a much larger and more complex payload, including file uploads, making them appropriate for state-changing operations.',null,null,null,'medium',ARRAY['html','forms']::text[],false,104),
('html','forms','html-forms-105','What are some of the newer HTML5 input types and what do they provide over a plain text input?','HTML5 added types like email, url, tel, number, date, time, color, range, and search, which enable appropriate mobile keyboards, built-in browser validation, and native UI widgets (like a date picker or color swatch) without any JavaScript. They degrade gracefully to a plain text input in browsers that don''t support a specific type.',null,'<input type="email" name="email" required>
<input type="date" name="dob">
<input type="range" min="0" max="100" value="50">','html','easy',ARRAY['html','forms']::text[],true,105),
('html','forms','html-forms-106','How does the required attribute enable native form validation, and what happens when a required field is empty on submit?','Adding required to an input tells the browser to block form submission and display a native validation message (styleable to a limited degree, and positioned by the browser) pointing at the offending field, using the built-in Constraint Validation API. It only runs on submission attempts (or explicit checkValidity() calls), not on every keystroke.',null,'<input type="text" name="username" required>','html','easy',ARRAY['html','forms','validation']::text[],false,106),
('html','forms','html-forms-107','What are the pattern, min, max, and maxlength attributes used for in form validation?','pattern provides a regular expression the input''s value must match (for text-like inputs). min and max constrain numeric or date-like input values to a range. maxlength (and minlength) constrain the character count of text input. All of these trigger native browser validation and the :invalid CSS pseudo-class when violated.',null,'<input type="text" pattern="[A-Za-z]{3,}" title="At least 3 letters">
<input type="number" min="1" max="10">','html','medium',ARRAY['html','forms','validation']::text[],false,107),
('html','forms','html-forms-108','Why is client-side HTML form validation not sufficient on its own for securing an application?','Client-side validation (required, pattern, type="email", etc.) runs entirely in the browser and can be trivially bypassed by disabling JavaScript, editing the DOM, or sending a raw HTTP request directly to the server. It''s a UX improvement for immediate feedback, but the server must always re-validate and sanitize every submitted value independently.',null,null,null,'medium',ARRAY['html','forms','validation']::text[],true,108),
('html','forms','html-forms-109','What is the difference between the <select> element''s single-select and multiple attribute modes?','By default a <select> renders a dropdown allowing exactly one <option> to be chosen. Adding the multiple attribute changes it into a scrollable list box where the user can select several options at once (typically via Ctrl/Cmd+click), and its submitted value becomes an array-like set of all selected options rather than a single value.',null,'<select name="tags" multiple>
  <option value="js">JavaScript</option>
  <option value="css">CSS</option>
</select>','html','easy',ARRAY['html','forms']::text[],false,109),
('html','forms','html-forms-110','What is the purpose of <fieldset> and <legend> in a form?','<fieldset> groups a set of related form controls, such as a set of radio buttons for the same question, and <legend> provides a caption/label for that whole group. Screen readers announce the legend text before each control''s own label as the user tabs into the group, which gives crucial context that individual labels alone might not convey.',null,'<fieldset>
  <legend>Shipping Method</legend>
  <label><input type="radio" name="ship" value="standard"> Standard</label>
  <label><input type="radio" name="ship" value="express"> Express</label>
</fieldset>','html','medium',ARRAY['html','forms','accessibility']::text[],false,110),
('html','forms','html-forms-111','What is the difference between a controlled radio button group and independent checkboxes?','Radio buttons sharing the same name attribute form a mutually exclusive group where only one can be checked at a time, representing a single choice from a set. Checkboxes are independent of each other even when they share a name (which instead lets the server receive multiple values for that key), representing zero or more selections.',null,'<input type="radio" name="size" value="s"> Small
<input type="radio" name="size" value="m"> Medium
<input type="checkbox" name="toppings" value="cheese"> Cheese
<input type="checkbox" name="toppings" value="olives"> Olives','html','easy',ARRAY['html','forms']::text[],false,111),
('html','forms','html-forms-112','What is the difference between a <button type="submit">, a <button type="button">, and a <button type="reset">?','type="submit" (the default inside a <form>, even if omitted) triggers form submission when clicked. type="button" does nothing on its own and is meant for attaching custom JavaScript behavior. type="reset" resets all fields in the form back to their initial default values. A common bug is forgetting to set type="button" on a button meant only to trigger a script, causing it to unintentionally submit the form.',null,'<button type="button" onclick="doSomething()">Preview</button>','html','medium',ARRAY['html','forms']::text[],true,112),
('html','forms','html-forms-113','How do you handle file uploads in an HTML form, and why does it require a special enctype?','A file input is created with <input type="file">, and the enclosing <form> must set enctype="multipart/form-data" (with method="post") because the default encoding, application/x-www-form-urlencoded, can''t represent binary file data. Without the correct enctype, the file''s contents are not sent, only its filename.',null,'<form method="post" enctype="multipart/form-data">
  <input type="file" name="avatar" accept="image/*">
</form>','html','medium',ARRAY['html','forms']::text[],false,113),
('html','forms','html-forms-114','What does the accept attribute do on a file input?','The accept attribute is a hint that filters which file types the browser''s file picker shows or allows selecting, specified as MIME types (image/*), file extensions (.pdf), or a mix. It is only a UX convenience — it doesn''t enforce anything server-side, so the backend must still validate the actual uploaded file''s type and content.',null,'<input type="file" accept=".jpg,.png,image/*">','html','easy',ARRAY['html','forms']::text[],false,114),
('html','forms','html-forms-115','What is the difference between the placeholder attribute and a <label>, and why can''t placeholder replace a label?','placeholder shows hint text inside an empty input that disappears once the user starts typing, and it''s not reliably announced or treated as an accessible name by all assistive technology. A <label> persists visually and is the standards-based accessible name, so relying on placeholder alone leaves users without context once they start typing or if they use a screen reader.',null,'<label for="search">Search</label>
<input id="search" type="search" placeholder="e.g. running shoes">','html','medium',ARRAY['html','forms','accessibility']::text[],true,115),
('html','forms','html-forms-116','What is the purpose of the autocomplete attribute?','autocomplete hints to the browser whether and how it should offer to autofill an input, using standardized tokens like "email", "given-name", "current-password", or "new-password". Setting it correctly speeds up form completion via browser-remembered data and password managers, and setting autocomplete="off" can be used to opt out for sensitive one-time fields, though browsers may not always honor it.',null,'<input type="email" name="email" autocomplete="email">
<input type="password" name="new-password" autocomplete="new-password">','html','medium',ARRAY['html','forms']::text[],false,116),
('html','forms','html-forms-117','What is the datalist element and how does it enhance a text input?','<datalist> provides a set of predefined <option> suggestions for an <input> it''s linked to via the input''s list attribute, giving an autocomplete-style dropdown while still allowing the user to type any free-form value (unlike <select>, which restricts to the listed options only).',null,'<input list="browsers" name="browser">
<datalist id="browsers">
  <option value="Chrome">
  <option value="Firefox">
</datalist>','html','medium',ARRAY['html','forms']::text[],false,117),
('html','forms','html-forms-118','What is the difference between readonly and disabled on a form input?','readonly prevents the user from editing the value but the input remains focusable, is still included in form submission, and still receives styling as an enabled field. disabled prevents both editing and focus, is visually greyed out by default, and — crucially — its value is excluded entirely from the submitted form data.',null,'<input type="text" value="Locked but submitted" readonly>
<input type="text" value="Not submitted" disabled>','html','medium',ARRAY['html','forms']::text[],true,118),
('html','forms','html-forms-119','What does novalidate do on a form, and when might you use it?','Adding novalidate to a <form> disables the browser''s built-in constraint validation on submit, which is useful when you want to implement fully custom validation logic and error messaging in JavaScript instead of relying on the browser''s default validation bubbles and behavior.',null,'<form novalidate>
  <input type="email" required>
</form>','html','hard',ARRAY['html','forms','validation']::text[],false,119),
('html','forms','html-forms-120','How does the Constraint Validation API let you customize native validation messages in JavaScript?','Each form control exposes a validity object (ValidityState) with boolean flags like valueMissing or typeMismatch, and calling setCustomValidity(''message'') on the element overrides the browser''s default validation message with your own text, which is then shown when checkValidity() or reportValidity() runs.',null,'const input = document.querySelector(''input'');
input.addEventListener(''invalid'', () => {
  input.setCustomValidity(''Please enter a valid company email.'');
});','javascript','hard',ARRAY['html','forms','validation']::text[],false,120),
('html','forms','html-forms-121','What is the difference between the formaction, formmethod, and form attributes on a submit button?','These attributes let a single <button> or <input type="submit"> override the enclosing form''s action, method, or even target a different <form> entirely (via the form attribute referencing that form''s id), which is handy when one form has multiple submit buttons that need to POST to different endpoints.',null,'<form id="checkout" action="/save-draft" method="post">
  <button type="submit">Save Draft</button>
  <button type="submit" formaction="/checkout">Place Order</button>
</form>','html','hard',ARRAY['html','forms']::text[],false,121),
('html','forms','html-forms-122','What is the difference between an <input> and a <textarea> for text entry?','<input type="text"> is a single-line text field. <textarea> is a resizable, multi-line plain-text field whose content is placed between its opening and closing tags (rather than a value attribute), and it supports rows/cols attributes to hint at its default size.',null,'<textarea name="bio" rows="4" cols="40">Default bio text</textarea>','html','easy',ARRAY['html','forms']::text[],false,122),
('html','forms','html-forms-123','Can you submit a form without JavaScript, and what happens to the page when you do?','Yes — a plain HTML form submission via a submit button is native browser behavior that triggers a full page navigation to the action URL using the specified method, no JavaScript required. This is the foundation progressive enhancement builds on: JavaScript can intercept submit and call event.preventDefault() to handle it asynchronously instead, but the form still works as a fallback if scripts fail.',null,null,null,'medium',ARRAY['html','forms']::text[],false,123),
('html','forms','html-forms-124','What is the difference between the step attribute''s effect on number, range, and date inputs?','step defines the granularity of valid values relative to the min (or a default base), constraining values to increments of the step — for example step="5" on a number input only allows multiples of 5 from the min. It applies similarly to range and date-related input types (e.g. step="7" on a date input to only allow weekly increments), and the default step for number inputs is 1.',null,'<input type="number" min="0" max="100" step="5">','html','medium',ARRAY['html','forms']::text[],false,124),
('html','forms','html-forms-125','What is the difference between the input event and the change event on a text input?','The input event fires synchronously on every value change, including every keystroke, paste, or programmatic input via user interaction. The change event fires only after the value has been committed and the element loses focus (for text inputs), or immediately on selection for things like checkboxes and <select>. input is preferred for live validation/filtering; change is preferred when you only care about the final committed value.',null,'input.addEventListener(''input'', () => console.log(''every keystroke''));
input.addEventListener(''change'', () => console.log(''after blur''));','javascript','medium',ARRAY['html','forms']::text[],false,125),
('html','forms','html-forms-126','How would you group multiple related checkboxes so they''re announced together to screen reader users, similar to radio buttons?','Wrap the checkboxes in a <fieldset> with a <legend> describing the group as a whole (e.g. ''Select your interests''), just as you would for radio buttons — this gives assistive technology the group context even though, unlike radios, each checkbox is independently toggleable and doesn''t need to share a name to be grouped this way.',null,'<fieldset>
  <legend>Interests</legend>
  <label><input type="checkbox" name="interests" value="sports"> Sports</label>
  <label><input type="checkbox" name="interests" value="music"> Music</label>
</fieldset>','html','medium',ARRAY['html','forms','accessibility']::text[],false,126),
('html','forms','html-forms-127','What happens to unchecked checkboxes and radio buttons during form submission?','Unlike text inputs, an unchecked checkbox or radio button contributes nothing to the submitted form data at all — its name/value pair simply isn''t included. This is why server-side code often needs to treat a missing key as ''false'' rather than expecting an explicit false value, and why a hidden input with the same name is sometimes added as a fallback default.',null,null,null,'medium',ARRAY['html','forms']::text[],false,127),
('html','forms','html-forms-128','What is the purpose of the <optgroup> element inside a <select>?','<optgroup> groups related <option> elements under a shared, non-selectable label within a dropdown, improving scannability for long lists — for example grouping country options by continent. The group''s label is exposed to assistive technology, but the <optgroup> itself can''t be selected, only its child options.',null,'<select name="country">
  <optgroup label="Europe">
    <option value="fr">France</option>
    <option value="de">Germany</option>
  </optgroup>
</select>','html','medium',ARRAY['html','forms']::text[],false,128),
('html','forms','html-forms-129','How does the HTML5 email input type''s built-in validation differ from validating an email address with a custom regex?','type="email" validates against a permissive pattern defined by the HTML spec (roughly matching the general shape of an address with an @ and a domain), which intentionally does not fully enforce RFC 5322 email syntax rules, favoring simplicity over strict correctness. Real-world email validation is famously hard to get fully right with regex, so most production systems still verify addresses by sending a confirmation email.',null,null,null,'hard',ARRAY['html','forms','validation']::text[],false,129),
('html','forms','html-forms-130','Why would you use the inputmode attribute alongside type="text" instead of just relying on the input''s type?','inputmode hints which virtual keyboard layout a mobile device should show (like "numeric" or "decimal") without changing the input''s actual value type or triggering type-specific validation/UI, which is useful when you need numeric-looking input (like a credit card number) but still want it treated and formatted as plain text rather than a number.',null,'<input type="text" inputmode="numeric" pattern="[0-9]*" name="zip">','html','hard',ARRAY['html','forms']::text[],false,130),
('html','forms','html-forms-131','What are some common accessibility pitfalls in HTML forms besides missing labels?','Common pitfalls include relying only on color to indicate an error state, not associating error messages with their input via aria-describedby, using placeholder as the only label, not grouping related controls with fieldset/legend, and building custom dropdowns or checkboxes from <div>s without proper keyboard support and ARIA roles instead of using native form elements.',null,null,null,'hard',ARRAY['html','forms','accessibility']::text[],false,131),
('html','media','html-media-101','What is the purpose of the alt attribute on an <img> tag, and what happens if it''s omitted entirely versus left empty?','alt provides a text alternative for an image, shown if the image fails to load and read aloud by screen readers. Leaving it empty (alt="") marks the image as decorative and tells assistive technology to skip it. Omitting the attribute entirely is invalid and worse — screen readers may fall back to announcing the full image filename or URL, which is rarely useful.',null,'<img src="team-photo.jpg" alt="Our five-person engineering team at the 2025 offsite">','html','easy',ARRAY['html','media','accessibility']::text[],true,101),
('html','media','html-media-102','What is the difference between the width/height attributes on <img> and setting width/height in CSS?','Setting width and height as HTML attributes lets the browser reserve the correct aspect-ratio space in the layout before the image finishes downloading, preventing content from jumping around (cumulative layout shift) as images load. CSS width/height can still be used to control the final rendered size responsively, but the HTML attributes should reflect the image''s intrinsic aspect ratio so the browser can compute the right placeholder box.',null,'<img src="hero.jpg" width="1200" height="600" alt="Hero banner" style="max-width: 100%; height: auto;">','html','medium',ARRAY['html','media','performance']::text[],true,102),
('html','media','html-media-103','What is the srcset attribute on <img> and what problem does it solve?','srcset lets you provide multiple versions of an image at different resolutions along with a width or pixel-density descriptor, so the browser can pick the most appropriate one based on the device''s screen size and pixel density, saving bandwidth on smaller or lower-DPI screens instead of always downloading one large image.',null,'<img
  src="photo-800.jpg"
  srcset="photo-400.jpg 400w, photo-800.jpg 800w, photo-1600.jpg 1600w"
  sizes="(max-width: 600px) 100vw, 800px"
  alt="Mountain landscape">','html','medium',ARRAY['html','media','performance']::text[],true,103),
('html','media','html-media-104','What is the difference between srcset with width descriptors (w) and pixel density descriptors (x)?','Width descriptors (e.g. 400w) tell the browser the actual pixel width of each image file, and are used together with the sizes attribute so the browser can calculate which file best fits the image''s rendered display size across different viewports. Density descriptors (e.g. 2x) instead directly map images to a device pixel ratio, simpler to use but only appropriate when the image is always displayed at a fixed CSS size regardless of viewport.',null,'<img src="icon.png" srcset="icon.png 1x, icon@2x.png 2x" alt="Settings icon">','html','hard',ARRAY['html','media','performance']::text[],false,104),
('html','media','html-media-105','What is the <picture> element used for, and how does it differ from srcset on a plain <img>?','<picture> lets you specify entirely different image sources for different conditions using multiple <source> elements — different art direction (cropping) per breakpoint via media queries, or different file formats (AVIF, WebP, JPEG fallback) via the type attribute — with a required <img> as the final fallback. Plain srcset on <img> only swaps resolution/density of the same image, not the image content or format selection logic.',null,'<picture>
  <source media="(max-width: 600px)" srcset="cropped-mobile.jpg">
  <source srcset="full-desktop.jpg">
  <img src="full-desktop.jpg" alt="Product hero shot">
</picture>','html','medium',ARRAY['html','media','performance']::text[],false,105),
('html','media','html-media-106','What does the loading="lazy" attribute do on an <img> or <iframe>, and when should you avoid it?','loading="lazy" tells the browser to defer loading the resource until it''s about to enter the viewport, saving bandwidth and speeding up initial page load for offscreen content. It should be avoided on above-the-fold images (especially a Largest Contentful Paint candidate), since deferring a visible image actually hurts perceived load performance — those should load eagerly, or even be preloaded.',null,'<img src="below-fold.jpg" alt="Product detail" loading="lazy">','html','medium',ARRAY['html','media','performance']::text[],true,106),
('html','media','html-media-107','How do you embed a video in HTML using the <video> element, and what are its key attributes?','<video> embeds a video with native browser controls when the controls attribute is present. Common attributes include autoplay (starts playback automatically, generally requires muted to work in most browsers), loop, muted, poster (a placeholder image shown before playback), and preload (hints how much to buffer ahead of time).',null,'<video controls width="640" poster="thumb.jpg">
  <source src="movie.mp4" type="video/mp4">
  <source src="movie.webm" type="video/webm">
  Your browser doesn''t support video playback.
</video>','html','easy',ARRAY['html','media']::text[],false,107),
('html','media','html-media-108','Why do most browsers require the muted attribute alongside autoplay on <video>?','Browsers enforce an autoplay policy that blocks audible autoplaying media by default to prevent an intrusive experience for users, since unexpected sound is one of the most common complaints about the web. Muted autoplay is allowed because it has no audio impact, which is why background/hero videos almost always combine autoplay muted loop playsinline.',null,'<video autoplay muted loop playsinline src="background.mp4"></video>','html','medium',ARRAY['html','media']::text[],false,108),
('html','media','html-media-109','What is the purpose of the <track> element inside a <video>, and why does it matter for accessibility?','<track> attaches supplementary timed text to a video, most commonly WebVTT captions (kind="captions") or subtitles (kind="subtitles"), letting deaf or hard-of-hearing users, non-native speakers, or muted-autoplay viewers follow along. kind="descriptions" can also provide audio-description text tracks for blind users.',null,'<video controls>
  <source src="talk.mp4" type="video/mp4">
  <track kind="captions" src="captions-en.vtt" srclang="en" label="English" default>
</video>','html','medium',ARRAY['html','media','accessibility']::text[],false,109),
('html','media','html-media-110','How does the <audio> element differ from <video> in terms of markup and default behavior?','<audio> shares nearly the same attribute set as <video> (controls, autoplay, loop, muted, preload, multiple <source> children for format fallback) but plays only sound and has no visual frame or poster image. Its default native controls render as a minimal playback bar rather than a video canvas with overlay controls.',null,'<audio controls>
  <source src="podcast.mp3" type="audio/mpeg">
  <source src="podcast.ogg" type="audio/ogg">
</audio>','html','easy',ARRAY['html','media']::text[],false,110),
('html','media','html-media-111','What is the purpose of providing multiple <source> elements inside <video> or <audio>?','Different browsers historically support different codecs and container formats due to licensing (e.g. some browsers don''t support certain MP4 codecs, others prefer WebM). Listing multiple <source> elements with different type attributes lets the browser pick the first format it can play, falling back gracefully instead of failing to load the media at all.',null,null,null,'medium',ARRAY['html','media']::text[],false,111),
('html','media','html-media-112','What is the <canvas> element used for, and how do you draw on it?','<canvas> provides a blank, scriptable bitmap surface for drawing graphics, charts, or animations pixel by pixel via JavaScript, using either the 2D rendering context (CanvasRenderingContext2D) or WebGL for hardware-accelerated 3D. Unlike SVG, canvas content is immediate-mode (drawn imperatively, with no retained DOM representation of individual shapes), so redrawing is needed whenever content changes.',null,'<canvas id="chart" width="300" height="150"></canvas>
<script>
  const ctx = document.getElementById(''chart'').getContext(''2d'');
  ctx.fillStyle = ''blue'';
  ctx.fillRect(10, 10, 100, 50);
</script>','html','medium',ARRAY['html','media','canvas']::text[],false,112),
('html','media','html-media-113','What is the key architectural difference between <canvas> and inline SVG for rendering graphics?','Canvas is a pixel-based, immediate-mode API — once something is drawn, the canvas has no memory of it as a distinct object, so hit-testing, accessibility, and updates require the developer to redraw and track state manually. SVG is a vector-based, retained-mode format where each shape is a real DOM node that can be styled with CSS, targeted with event listeners individually, and is naturally accessible and resolution-independent.',null,null,null,'hard',ARRAY['html','media','canvas']::text[],true,113),
('html','media','html-media-114','How can you make canvas content accessible, given it has no inherent DOM representation of what''s drawn?','Fallback content placed between the opening and closing <canvas> tags is rendered by browsers that don''t support canvas and can also be used by assistive technology in some cases; more robustly, you can mirror important information in an accessible off-canvas summary, or use ARIA attributes on the canvas element itself (like role and aria-label) combined with a text description of dynamic changes.',null,'<canvas width="400" height="200" role="img" aria-label="Bar chart showing sales by quarter">
  Your browser doesn''t support canvas. Sales: Q1 $10k, Q2 $15k.
</canvas>','html','hard',ARRAY['html','media','canvas','accessibility']::text[],false,114),
('html','media','html-media-115','What is an <iframe> and what are common use cases for it?','<iframe> embeds another HTML document inline within the current page, rendered in its own independent browsing context with its own DOM, scripts, and (by default) origin isolation. Common use cases include embedding YouTube videos, maps, payment widgets, ads, or third-party embeddable content that must stay sandboxed from the host page.',null,'<iframe src="https://www.youtube.com/embed/dQw4w9WgXcQ" title="Embedded video player" width="560" height="315"></iframe>','html','easy',ARRAY['html','media','iframe']::text[],false,115),
('html','media','html-media-116','Why does every <iframe> need a title attribute?','The title attribute gives an iframe an accessible name, which screen readers announce so users understand what the embedded content is before deciding whether to enter it. Without a title, assistive technology may announce something unhelpful like the raw source URL or just ''iframe'', giving no context about the embedded content''s purpose.',null,'<iframe src="map.html" title="Interactive store locator map"></iframe>','html','medium',ARRAY['html','media','iframe','accessibility']::text[],false,116),
('html','media','html-media-117','What security risks does embedding third-party content in an <iframe> introduce, and how does the sandbox attribute help?','An unrestricted iframe can run scripts, submit forms, open popups, or navigate the top-level page, which is risky when embedding untrusted third-party content. The sandbox attribute applies a set of restrictions by default (disabling scripts, forms, popups, and more), and individual capabilities can be selectively re-enabled with tokens like allow-scripts or allow-same-origin as needed.',null,'<iframe src="https://third-party.example" sandbox="allow-scripts allow-same-origin" title="Widget"></iframe>','html','hard',ARRAY['html','media','iframe']::text[],false,117),
('html','media','html-media-118','What does the referrerpolicy attribute on an <iframe> or <img> control?','referrerpolicy controls how much information is included in the Referer header when the browser requests the embedded resource — options range from no-referrer (sends nothing) to unsafe-url (always sends the full URL including query params), letting you balance the embedded content''s need for referrer context against leaking potentially sensitive URL data to third parties.',null,'<iframe src="https://example.com/widget" referrerpolicy="no-referrer" title="Widget"></iframe>','html','hard',ARRAY['html','media','iframe']::text[],false,118),
('html','media','html-media-119','What is the difference between an SVG embedded inline in HTML and one referenced via <img src="icon.svg">?','Inline SVG (written directly as <svg> markup in the HTML) becomes part of the page''s DOM, so it can be styled and animated with CSS, targeted with JavaScript, and its individual elements can respond to events. An SVG loaded via <img> (or as a CSS background) is treated as an opaque, static image resource — it can''t be styled from the parent document''s CSS and its internal elements aren''t scriptable.',null,'<svg width="24" height="24" viewBox="0 0 24 24">
  <circle cx="12" cy="12" r="10" fill="currentColor"/>
</svg>','html','medium',ARRAY['html','media','svg']::text[],false,119),
('html','media','html-media-120','What is the object-fit CSS property and why is it commonly paired with responsive images or video?','object-fit controls how a replaced element''s content (like an <img> or <video>) is resized to fit its box when the box''s aspect ratio doesn''t match the media''s intrinsic aspect ratio — cover crops to fill the box, contain scales to fit entirely within it, and fill stretches to match exactly. It''s essential for responsive galleries or thumbnails where images of varying aspect ratios need to fit uniform-sized containers without distortion.',null,'img.thumbnail { width: 200px; height: 200px; object-fit: cover; }','css','medium',ARRAY['html','media','css']::text[],false,120),
('html','media','html-media-121','How would you provide a fallback for browsers that don''t support a modern image format like WebP or AVIF?','Use <picture> with multiple <source> elements ordered from most to least modern format, each with a type attribute the browser checks for support, and a plain <img> as the mandatory last child that serves as the universal fallback (e.g. a JPEG) for any browser that doesn''t match any source.',null,'<picture>
  <source srcset="photo.avif" type="image/avif">
  <source srcset="photo.webp" type="image/webp">
  <img src="photo.jpg" alt="Description">
</picture>','html','medium',ARRAY['html','media','performance']::text[],false,121),
('html','media','html-media-122','What is the purpose of the crossorigin attribute on media elements like <img>, <video>, or <audio>?','crossorigin enables CORS-mode fetching of the resource, which is required if you need to read the media''s pixel data (e.g. via canvas.getImageData or drawing it into a WebGL texture) when it''s hosted on a different origin — without it, the browser ''taints'' the canvas and blocks pixel-level access even though the media still displays normally.',null,'<img src="https://cdn.example.com/photo.jpg" crossorigin="anonymous" alt="Photo">','html','hard',ARRAY['html','media','canvas']::text[],false,122),
('html','media','html-media-123','Why might you use the preload attribute on <video>, and what are its possible values?','preload hints how eagerly the browser should buffer the media before playback begins: "none" avoids downloading anything until the user interacts (good for pages with many videos), "metadata" fetches only duration/dimensions, and "auto" lets the browser buffer as much as it deems appropriate — the browser can still choose to ignore the hint based on factors like data-saving settings.',null,'<video preload="metadata" controls src="lecture.mp4"></video>','html','medium',ARRAY['html','media','performance']::text[],false,123),
('html','media','html-media-124','How do you make an embedded YouTube or Vimeo iframe responsive so it scales with its container''s width?','Since <iframe> has fixed width/height attributes, a common CSS technique wraps it in a container with a fixed aspect-ratio (or the older padding-bottom percentage trick) and positions the iframe to fill that container absolutely — modern CSS can do this more directly with the aspect-ratio property applied straight to the iframe.',null,'.video-wrapper {
  aspect-ratio: 16 / 9;
}
.video-wrapper iframe {
  width: 100%;
  height: 100%;
}','css','medium',ARRAY['html','media','iframe','css']::text[],false,124),
('html','media','html-media-125','What is the difference between the <embed> and <object> elements, and are they still commonly used?','Both can embed external resources like PDFs or plugin-driven content (<embed> is simpler and self-closing; <object> supports fallback content between its tags and a more structured param-based configuration). Both are largely legacy today since the plugin technologies they were designed for (Flash, Java applets) are obsolete; modern use cases like embedding a PDF are more often handled with an <iframe> or a dedicated viewer library.',null,'<object data="report.pdf" type="application/pdf" width="600" height="400">
  <p>PDF cannot be displayed. <a href="report.pdf">Download it</a>.</p>
</object>','html','hard',ARRAY['html','media']::text[],false,125),
('html','media','html-media-126','What are Cumulative Layout Shift (CLS) issues related to media, and how do you prevent them?','CLS occurs when content visibly shifts position after the initial render, often because an image or embed with no reserved dimensions suddenly takes up space once it finishes loading, pushing surrounding content down. Always specifying width/height attributes (or an aspect-ratio in CSS) on images, videos, and iframes lets the browser reserve the correct space up front, eliminating the shift.',null,null,null,'medium',ARRAY['html','media','performance']::text[],false,126),
('html','media','html-media-127','How would you provide a text description for a complex image, like an infographic or detailed chart, beyond a short alt attribute?','A short alt attribute should summarize the image''s purpose, but for complex images the full explanation is better placed as visible or adjacent text content on the page (or linked via aria-describedby to a longer description elsewhere), since alt text is meant to be a concise equivalent, not a place for lengthy prose that''s awkward for screen reader users to sit through inline.',null,'<img src="infographic.png" alt="2025 revenue breakdown by region" aria-describedby="infographic-desc">
<p id="infographic-desc">Detailed breakdown: North America 45%, Europe 30%, APAC 25%.</p>','html','hard',ARRAY['html','media','accessibility']::text[],false,127),
('html','media','html-media-128','What is the playsinline attribute used for on <video>?','On mobile browsers, particularly iOS Safari, video would historically play in a fullscreen native player by default rather than inline within the page layout. playsinline overrides that behavior so the video plays embedded in its normal position on the page, which is essential for background/hero videos and custom video UIs on mobile.',null,'<video autoplay muted loop playsinline src="hero-bg.mp4"></video>','html','hard',ARRAY['html','media']::text[],false,128),
('html','media','html-media-129','What is the difference between the <video> element''s currentTime, duration, and paused JavaScript properties?','currentTime is a read/write property holding the current playback position in seconds, which can be set to seek. duration is a read-only property exposing the media''s total length once metadata has loaded (NaN before that). paused is a read-only boolean reflecting whether playback is currently stopped, used together with the play() and pause() methods to build custom playback controls.',null,'const video = document.querySelector(''video'');
video.currentTime = 30; // seek to 30s
if (video.paused) video.play();','javascript','medium',ARRAY['html','media']::text[],false,129),
('html','media','html-media-130','How would you display a static poster image for an audio player, since <audio> has no built-in poster attribute like <video>?','Unlike <video>, <audio> has no poster attribute, so any cover art or thumbnail must be rendered separately in the surrounding markup, typically as an <img> placed alongside the <audio> element and styled together as a custom player layout, since the audio element itself only renders its playback controls.',null,'<div class="player">
  <img src="album-art.jpg" alt="Album cover for Midnight Drive">
  <audio controls src="track.mp3"></audio>
</div>','html','easy',ARRAY['html','media']::text[],false,130),
('html','metadata-performance','html-metadata-performance-101','What is the purpose of the <title> element, and where does it appear in the browser?','<title> defines the document''s title, shown in the browser tab, bookmarks, and browser history, and it''s typically what search engines display as the clickable headline in search results. Each page should have a unique, descriptive title, ideally under about 60 characters so it isn''t truncated in search results.',null,'<head>
  <title>Pricing Plans - Acme Inc.</title>
</head>','html','easy',ARRAY['html','metadata-performance','seo']::text[],false,101),
('html','metadata-performance','html-metadata-performance-102','What does the meta description tag do, and does it directly affect search ranking?','<meta name="description" content="..."> provides a short summary of the page that search engines often (but not always) display as the snippet text under the title in search results. It does not directly influence ranking algorithms, but a well-written description can improve click-through rate, which indirectly benefits SEO performance.',null,'<meta name="description" content="Compare Acme''s pricing plans and find the right fit for your team.">','html','easy',ARRAY['html','metadata-performance','seo']::text[],true,102),
('html','metadata-performance','html-metadata-performance-103','What is the viewport meta tag and why is it essential for responsive design?','<meta name="viewport" content="width=device-width, initial-scale=1"> tells mobile browsers to render the page at the device''s actual width instead of a default desktop-width virtual viewport (typically 980px) that gets zoomed out. Without it, responsive CSS media queries won''t behave as intended on mobile, since the browser would already be simulating a wide desktop layout.',null,'<meta name="viewport" content="width=device-width, initial-scale=1">','html','easy',ARRAY['html','metadata-performance']::text[],true,103),
('html','metadata-performance','html-metadata-performance-104','What is the difference between the defer and async attributes on <script> tags, and how does each affect page load performance?','Both let the script download without blocking HTML parsing. defer delays execution until after parsing completes and preserves the document order among multiple deferred scripts, making it predictable for scripts with dependencies. async executes the moment the script finishes downloading, potentially before parsing finishes and out of order relative to other scripts, which suits independent scripts like analytics that don''t depend on DOM state or other scripts.',null,'<script src="vendor.js" defer></script>
<script src="app.js" defer></script>
<script src="ads.js" async></script>','html','medium',ARRAY['html','metadata-performance']::text[],true,104),
('html','metadata-performance','html-metadata-performance-105','What does <link rel="preload"> do, and when should you use it?','preload tells the browser to fetch a critical resource (like a hero image, a web font, or a key script) with high priority early in the page load, before the browser would normally discover it via CSS or deep in the HTML. It''s meant for resources the current page definitely needs soon — overusing preload on non-critical resources can actually hurt performance by competing for bandwidth with truly critical ones.',null,'<link rel="preload" href="/fonts/inter-var.woff2" as="font" type="font/woff2" crossorigin>','html','medium',ARRAY['html','metadata-performance']::text[],true,105),
('html','metadata-performance','html-metadata-performance-106','What is the difference between preload, prefetch, and preconnect resource hints?','preload fetches a resource the current page needs with high priority, immediately. prefetch fetches a resource at low priority that a future navigation (like the next likely page) will probably need, so it''s ready in cache ahead of time. preconnect establishes an early network connection (DNS, TCP, TLS handshake) to another origin without fetching a specific resource yet, useful for third-party domains you know you''ll request from soon.',null,'<link rel="preconnect" href="https://fonts.gstatic.com">
<link rel="prefetch" href="/next-page.html">','html','hard',ARRAY['html','metadata-performance']::text[],false,106),
('html','metadata-performance','html-metadata-performance-107','Why does placing <link rel="stylesheet"> tags in the <head> (rather than at the end of <body>) matter for rendering?','Browsers block rendering until all CSS discovered so far is downloaded and parsed, to avoid a flash of unstyled content — this is why stylesheets belong in <head>, loaded as early as possible. If stylesheets were placed at the end of body, the page would render unstyled first and then suddenly restyle, producing a jarring flash.',null,null,null,'medium',ARRAY['html','metadata-performance']::text[],false,107),
('html','metadata-performance','html-metadata-performance-108','What is render-blocking CSS/JS, and how does it impact metrics like First Contentful Paint?','Render-blocking resources are ones the browser must download and process before it can paint any content, delaying First Contentful Paint. CSS in <head> without media query splitting, and synchronous <script> tags without async/defer, are the classic offenders — techniques like inlining critical CSS, deferring non-critical scripts, and splitting stylesheets by media type all reduce this blocking time.',null,'<link rel="stylesheet" href="print.css" media="print">','html','hard',ARRAY['html','metadata-performance']::text[],false,108),
('html','metadata-performance','html-metadata-performance-109','What is the purpose of Open Graph meta tags, and where are they used?','Open Graph tags (og:title, og:description, og:image, og:url, etc.) are a metadata protocol originally created by Facebook that let social platforms and chat apps render a rich preview card when a link is shared, instead of a bare URL. Twitter/X has its own overlapping but distinct set of twitter:* tags for the same purpose on its platform.',null,'<meta property="og:title" content="Pricing Plans - Acme Inc.">
<meta property="og:description" content="Find the right plan for your team.">
<meta property="og:image" content="https://acme.com/og-image.png">','html','medium',ARRAY['html','metadata-performance','seo']::text[],false,109),
('html','metadata-performance','html-metadata-performance-110','What is the purpose of the <link rel="canonical"> tag?','A canonical tag tells search engines which URL is the authoritative version of a page when the same or very similar content is reachable via multiple URLs (e.g. with different query parameters, trailing slashes, or http vs https), consolidating ranking signals to one URL and avoiding duplicate-content penalties.',null,'<link rel="canonical" href="https://example.com/products/shoes">','html','medium',ARRAY['html','metadata-performance','seo']::text[],false,110),
('html','metadata-performance','html-metadata-performance-111','What is the purpose of a favicon and how do you declare multiple sizes/formats for different devices?','A favicon is the small icon shown in browser tabs, bookmarks, and history. Modern sites declare multiple <link rel="icon"> entries with different sizes and formats (including an .ico fallback and various PNG sizes for different platforms), plus apple-touch-icon for iOS home screen bookmarks, since no single format/size satisfies every browser and OS combination.',null,'<link rel="icon" href="/favicon.ico" sizes="any">
<link rel="icon" href="/icon.svg" type="image/svg+xml">
<link rel="apple-touch-icon" href="/apple-touch-icon.png">','html','easy',ARRAY['html','metadata-performance']::text[],false,111),
('html','metadata-performance','html-metadata-performance-112','How do structured data / JSON-LD in the <head> help search engines, and what does a basic example look like?','Structured data marks up a page''s content using a standardized vocabulary (usually schema.org) so search engines can understand entities like products, articles, or recipes precisely enough to show rich results — star ratings, prices, breadcrumbs — directly in search listings. JSON-LD is the recommended format, embedded as a <script type="application/ld+json"> block rather than mixed into visible HTML attributes.',null,'<script type="application/ld+json">
{
  "@context": "https://schema.org",
  "@type": "Product",
  "name": "Running Shoes",
  "offers": { "@type": "Offer", "price": "79.99", "priceCurrency": "USD" }
}
</script>','html','hard',ARRAY['html','metadata-performance','seo']::text[],false,112),
('html','metadata-performance','html-metadata-performance-113','What is the robots meta tag used for, and how does it differ from a robots.txt file?','<meta name="robots" content="noindex, nofollow"> is a per-page directive controlling whether search engines index that specific page and follow its links; robots.txt is a site-wide file that controls which URLs crawlers are even allowed to request in the first place. A page blocked entirely by robots.txt won''t be crawled, so its meta robots tag (if any) is never even seen.',null,'<meta name="robots" content="noindex, nofollow">','html','medium',ARRAY['html','metadata-performance','seo']::text[],false,113),
('html','metadata-performance','html-metadata-performance-114','Why should the <meta charset> declaration be one of the very first elements in <head>?','The browser must know the character encoding before it can correctly parse any subsequent text, including other meta tags and the title — the HTML spec requires charset to be declared within the first 1024 bytes of the document to guarantee it''s read before encoding-sensitive content, which is why it''s conventionally placed as the first line inside <head>.',null,'<head>
  <meta charset="UTF-8">
  <title>My Page</title>
</head>','html','medium',ARRAY['html','metadata-performance']::text[],false,114),
('html','metadata-performance','html-metadata-performance-115','What is font loading strategy, and why does the font-display CSS property matter for perceived performance?','By default, browsers may block text rendering (a ''flash of invisible text'') while waiting for a custom @font-face to download. font-display: swap tells the browser to render text immediately with a fallback font and swap in the custom font once loaded, trading a brief font-swap flash for much faster perceived content availability — often paired with preloading the font file itself.',null,'@font-face {
  font-family: ''Inter'';
  src: url(''/fonts/inter.woff2'') format(''woff2'');
  font-display: swap;
}','css','medium',ARRAY['html','metadata-performance','css']::text[],false,115),
('html','metadata-performance','html-metadata-performance-116','What is the difference between critical CSS inlining and linking an external stylesheet, from a performance standpoint?','Inlining the minimal CSS needed to render above-the-fold content directly in a <style> tag in <head> avoids an extra render-blocking network round trip for that critical portion, improving First Contentful Paint. The remaining non-critical CSS is still loaded externally (often asynchronously), trading a larger initial HTML payload for faster perceived rendering on the first visit.',null,'<style>
  /* critical above-the-fold styles inlined here */
  body { margin: 0; font-family: sans-serif; }
</style>
<link rel="stylesheet" href="non-critical.css" media="print" onload="this.media=''all''">','html','hard',ARRAY['html','metadata-performance']::text[],false,116),
('html','metadata-performance','html-metadata-performance-117','What does the type="module" attribute on <script> do, and how does it affect loading behavior?','type="module" marks a script as an ES module, enabling import/export syntax and running the script in strict mode with its own top-level scope. Module scripts are deferred by default (like the defer attribute), execute only once even if referenced multiple times, and are fetched with CORS, which matters when loading modules from a different origin.',null,'<script type="module" src="main.js"></script>','html','medium',ARRAY['html','metadata-performance']::text[],false,117),
('html','metadata-performance','html-metadata-performance-118','How do you provide a fallback script for browsers that don''t support ES modules, using nomodule?','A <script nomodule src="legacy-bundle.js"> tag is ignored entirely by browsers that understand type="module", but executed by older browsers that don''t recognize the module attribute at all — a common pattern (differential/module-nomodule serving) to ship modern, smaller ES module code to capable browsers while still supporting legacy ones with a transpiled bundle.',null,'<script type="module" src="modern.js"></script>
<script nomodule src="legacy.js"></script>','html','hard',ARRAY['html','metadata-performance']::text[],false,118),
('html','metadata-performance','html-metadata-performance-119','What are Core Web Vitals, and which of them is most directly influenced by HTML/markup decisions?','Core Web Vitals are Google''s user-centric performance metrics: Largest Contentful Paint (LCP, loading speed of the main content), Interaction to Next Paint (INP, responsiveness), and Cumulative Layout Shift (CLS, visual stability). CLS is the one most directly shaped by HTML decisions — specifying image/iframe dimensions, reserving space for ads or embeds, and avoiding late-injected content above existing content all reduce it.',null,null,null,'medium',ARRAY['html','metadata-performance']::text[],true,119),
('html','metadata-performance','html-metadata-performance-120','How does deferring non-critical third-party scripts (like analytics or chat widgets) improve page performance?','Third-party scripts often aren''t needed for the initial render and can block the main thread or delay other resources if loaded eagerly. Loading them with defer/async, injecting them dynamically after the load event, or using a facade pattern (loading the real widget only on user interaction) keeps them from competing with critical rendering-path resources.',null,'window.addEventListener(''load'', () => {
  const s = document.createElement(''script'');
  s.src = ''https://widget.example.com/chat.js'';
  document.body.appendChild(s);
});','javascript','medium',ARRAY['html','metadata-performance']::text[],false,120),
('html','metadata-performance','html-metadata-performance-121','What is the theme-color meta tag used for?','<meta name="theme-color" content="#0f172a"> hints the browser to tint its UI chrome (like the mobile browser''s address bar or the task-switcher card) to match the site''s brand color, which is a small but common polish detail for mobile web apps and PWAs.',null,'<meta name="theme-color" content="#0f172a">','html','easy',ARRAY['html','metadata-performance']::text[],false,121),
('html','metadata-performance','html-metadata-performance-122','What is a web app manifest, and how is it linked from an HTML document?','A manifest.json file describes a web application''s name, icons, theme colors, and display mode (like standalone, to hide the browser chrome), enabling ''Add to Home Screen'' installability as a Progressive Web App. It''s linked from the document with <link rel="manifest" href="/manifest.json">.',null,'<link rel="manifest" href="/manifest.json">','html','medium',ARRAY['html','metadata-performance']::text[],false,122),
('html','metadata-performance','html-metadata-performance-123','What is the difference between a hard page reload''s parsing cost and how the browser''s preload scanner helps mitigate it?','While the main HTML parser builds the DOM sequentially and can be blocked by synchronous scripts, browsers run a separate, speculative ''preload scanner'' that scans ahead in the raw HTML to discover and start fetching resources (images, scripts, stylesheets) referenced further down the document, even before the main parser reaches them — this is why resources declared as plain HTML attributes (like <img src>) are typically discovered and fetched sooner than ones injected dynamically via JavaScript.',null,null,null,'hard',ARRAY['html','metadata-performance']::text[],false,123),
('html','metadata-performance','html-metadata-performance-124','How does specifying a fetchpriority attribute on an <img> or <link> help performance?','fetchpriority="high" or "low" gives the browser an explicit hint to override its default resource-priority heuristics, useful for telling it that a specific hero image is more important than other same-type resources discovered around the same time (like carousel images below the fold), improving how quickly the most important content is prioritized during contention.',null,'<img src="hero.jpg" alt="Hero banner" fetchpriority="high">','html','hard',ARRAY['html','metadata-performance']::text[],false,124),
('html','metadata-performance','html-metadata-performance-125','What is the purpose of the <meta name="viewport"> maximum-scale and user-scalable options, and why are they generally discouraged?','These options can restrict or disable pinch-to-zoom on mobile devices, which was once used to prevent unwanted zooming on form-heavy pages. It''s now considered a serious accessibility anti-pattern since it prevents low-vision users from zooming in to read content, so modern guidance is to always allow user scaling and only rely on width=device-width, initial-scale=1.',null,'<!-- Avoid this -->
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">','html','hard',ARRAY['html','metadata-performance','accessibility']::text[],false,125),
('html','metadata-performance','html-metadata-performance-126','How do hreflang tags help with internationalized (multi-language) SEO?','<link rel="alternate" hreflang="..."> tags tell search engines which URL to serve for a given language/region so users see the appropriate localized version in search results, preventing duplicate-content issues between translated pages and improving relevance for international audiences.',null,'<link rel="alternate" hreflang="en" href="https://example.com/en/">
<link rel="alternate" hreflang="fr" href="https://example.com/fr/">','html','hard',ARRAY['html','metadata-performance','seo']::text[],false,126),
('html','metadata-performance','html-metadata-performance-127','Why does minimizing the number of render-blocking <link rel="stylesheet"> requests matter, and what''s one common technique to reduce their impact?','Each render-blocking stylesheet adds a network round trip the browser must wait through before painting, so fewer, well-bundled stylesheets (or HTTP/2+ multiplexing) reduce that delay. A common technique is splitting stylesheets by media type (e.g. print.css with media="print") so the browser only treats stylesheets relevant to the current rendering context as blocking.',null,null,null,'medium',ARRAY['html','metadata-performance']::text[],false,127),
('html','metadata-performance','html-metadata-performance-128','What is the difference between the meta tag''s name and property attributes, as seen in standard meta tags versus Open Graph tags?','name is used by the standard HTML meta tag vocabulary (description, viewport, robots, theme-color) and is what the HTML spec itself defines. property is technically an RDFa attribute co-opted by the Open Graph protocol (og:title, og:image) rather than a native HTML meta concept, which is why Open Graph tags look slightly different syntactically even though both ultimately populate the same <meta> element with a content value.',null,'<meta name="description" content="Standard meta tag">
<meta property="og:title" content="Open Graph tag">','html','hard',ARRAY['html','metadata-performance','seo']::text[],false,128),
('html','metadata-performance','html-metadata-performance-129','What is the difference between the async, defer, and module loading orders when a page has several <script> tags mixing all three?','Deferred and module scripts (both default to deferred behavior) execute in document order, always after parsing completes and before DOMContentLoaded fires. Async scripts execute as soon as they''ve individually finished downloading, in whatever order that happens to be, potentially interleaved before or after parsing finishes and without respecting document order relative to other scripts — this unpredictability is why async is reserved for scripts with no dependencies on other scripts or DOM state.',null,null,null,'hard',ARRAY['html','metadata-performance']::text[],false,129),
('html','metadata-performance','html-metadata-performance-130','What is the purpose of the <noscript> element, and where in the document can it be used?','<noscript> defines content that''s only rendered when JavaScript is disabled or unsupported in the browser, commonly used to show a fallback message or a tracking pixel''s non-JS equivalent. It can appear in both <head> (limited to metadata-like content such as <link> or <style>) and <body> (where it can contain regular visible markup).',null,'<noscript>
  <p>This site requires JavaScript to function properly. Please enable it in your browser settings.</p>
</noscript>','html','medium',ARRAY['html','metadata-performance']::text[],false,130),
('html','semantic-html','html-semantic-html-101','What is semantic HTML and why does it matter?','Semantic HTML uses elements whose tag names convey the meaning and role of their content, such as <nav> for navigation or <article> for self-contained content, rather than generic <div>/<span> wrappers with classes. It improves accessibility for screen readers, gives search engines better context for indexing, and makes markup easier for other developers to understand at a glance.',null,null,null,'easy',ARRAY['html','semantic-html','accessibility']::text[],true,101),
('html','semantic-html','html-semantic-html-102','What is the difference between <header> and <h1>-<h6> heading elements?','<header> is a sectioning-related container for introductory content or navigation aids, like a logo and site nav, and can appear multiple times per page (e.g. once per <article>). Heading elements (<h1> through <h6>) define the actual title/hierarchy of a section''s content and are used to build the page''s outline; a <header> typically contains a heading but is not one itself.',null,'<header>
  <img src="logo.png" alt="Logo">
  <h1>Site Title</h1>
</header>','html','easy',ARRAY['html','semantic-html']::text[],false,102),
('html','semantic-html','html-semantic-html-103','What is the difference between <section> and <div>?','<section> represents a thematic grouping of content that generally should have its own heading and makes sense as a standalone unit in the document outline, such as a chapter or a tab panel. <div> is a purely generic container with no semantic meaning, used only when no other semantic element fits or purely for styling/scripting hooks.',null,'<section>
  <h2>Customer Reviews</h2>
  <p>...</p>
</section>','html','medium',ARRAY['html','semantic-html']::text[],true,103),
('html','semantic-html','html-semantic-html-104','When should you use <article> versus <section>?','<article> represents a self-contained piece of content that would make sense distributed independently, like a blog post, news story, or forum comment. <section> is a more general thematic grouping within a page and doesn''t need to be independently distributable — an <article> can contain multiple <section>s, and a <section> can contain multiple <article>s.',null,'<article>
  <h2>Blog Post Title</h2>
  <section>
    <h3>Introduction</h3>
  </section>
</article>','html','medium',ARRAY['html','semantic-html']::text[],true,104),
('html','semantic-html','html-semantic-html-105','What is the purpose of the <main> element, and how many can a document have?','<main> wraps the dominant, unique content of a document — excluding repeated elements like headers, footers, navigation, or sidebars — and helps assistive technology users skip directly to the primary content. There should be exactly one visible <main> per page, and it must not be nested inside <article>, <aside>, <header>, <footer>, or another <main>.',null,'<main>
  <h1>Article Title</h1>
  <p>Primary page content...</p>
</main>','html','medium',ARRAY['html','semantic-html','accessibility']::text[],false,105),
('html','semantic-html','html-semantic-html-106','What is the <nav> element used for, and should every group of links be wrapped in it?','<nav> identifies a block of major navigation links, like a site''s primary menu, breadcrumb trail, or table of contents. Not every group of links needs <nav> — a footer''s list of miscellaneous links doesn''t require it — using it only for significant navigation blocks keeps the semantics meaningful for screen reader users who often jump between landmarks.',null,'<nav aria-label="Main">
  <ul>
    <li><a href="/">Home</a></li>
    <li><a href="/about">About</a></li>
  </ul>
</nav>','html','easy',ARRAY['html','semantic-html','accessibility']::text[],false,106),
('html','semantic-html','html-semantic-html-107','What is the <aside> element used for?','<aside> represents content that''s tangentially related to the surrounding content, such as a pull quote, a sidebar with related links, or an author bio, and could be removed without reducing the meaning of the main content. Its meaning is relative to its nesting context — inside an <article> it might represent related footnotes, while at the page level it might be a sidebar widget.',null,'<aside>
  <h3>Related Articles</h3>
  <ul>...</ul>
</aside>','html','easy',ARRAY['html','semantic-html']::text[],false,107),
('html','semantic-html','html-semantic-html-108','What is the purpose of the <footer> element, and can a page have more than one?','<footer> represents closing information for its nearest sectioning ancestor, typically containing things like copyright notices, author info, or related links. A page can have multiple footers — one for the whole document and separate ones nested inside individual <article> or <section> elements.',null,null,null,'easy',ARRAY['html','semantic-html']::text[],false,108),
('html','semantic-html','html-semantic-html-109','What is the HTML document outline algorithm, and is it still relevant in modern HTML5?','The document outline algorithm was meant to let sectioning elements (<article>, <section>, <aside>, <nav>) generate an implicit nested outline based on structure rather than heading levels alone. In practice no major browser or assistive technology ever fully implemented it, so it was removed from the HTML5.1+ spec — meaning heading levels (<h1>-<h6>) still need to be used correctly and hierarchically for real-world accessibility.',null,null,null,'hard',ARRAY['html','semantic-html','accessibility']::text[],false,109),
('html','semantic-html','html-semantic-html-110','Why is it generally discouraged to skip heading levels, like going from <h1> directly to <h3>?','Screen reader users frequently navigate a page by jumping between headings, and skipped levels make the hierarchy confusing since it implies a subsection exists at a level that was never introduced. Headings should reflect a logical, sequential outline of the content, even if the visual font size is adjusted separately with CSS.',null,null,null,'medium',ARRAY['html','semantic-html','accessibility']::text[],false,110),
('html','semantic-html','html-semantic-html-111','What is the <figure> and <figcaption> pairing used for?','<figure> wraps self-contained content like an image, diagram, code listing, or chart that could be moved away from the main flow without affecting the document''s meaning, and <figcaption> provides its caption. This pairing lets you associate a caption with content in a semantically explicit way that a plain <img> plus adjacent <p> does not.',null,'<figure>
  <img src="chart.png" alt="Quarterly revenue chart">
  <figcaption>Fig 1. Revenue grew 20% QoQ.</figcaption>
</figure>','html','medium',ARRAY['html','semantic-html']::text[],false,111),
('html','semantic-html','html-semantic-html-112','What is the difference between the <time> element and plain text representing a date?','<time> wraps a human-readable date or time and, via its datetime attribute, provides a machine-readable equivalent in a standardized format, which search engines and browser extensions (like calendar add-to-actions) can parse reliably. Plain text dates have no structured meaning a machine can extract, even if they read clearly to a human.',null,'<p>Published on <time datetime="2026-01-15">January 15, 2026</time></p>','html','medium',ARRAY['html','semantic-html']::text[],false,112),
('html','semantic-html','html-semantic-html-113','What is the purpose of the <address> element?','<address> denotes contact information for the author or owner of the nearest <article> or the whole document — it can hold a physical address, email, phone number, or social link, but must not be used for arbitrary postal addresses unrelated to contact info (like an address inside an article about a historical event).',null,'<address>
  Contact: <a href="mailto:hi@example.com">hi@example.com</a>
</address>','html','hard',ARRAY['html','semantic-html']::text[],false,113),
('html','semantic-html','html-semantic-html-114','What is the difference between <mark>, <strong>, and <em>?','<mark> highlights text as relevant in the current context, like search results matching a query, and is rendered with a yellow background by default. <strong> indicates strong importance or urgency (bold by default), and <em> indicates stressed emphasis that changes the meaning of a sentence when read aloud (italic by default) — all three carry distinct semantic meaning beyond their default visual styling.',null,'<p>Results for <mark>climate</mark> change policy.</p>','html','medium',ARRAY['html','semantic-html']::text[],false,114),
('html','semantic-html','html-semantic-html-115','What is the <details> and <summary> pairing, and what does it provide natively?','<details> creates a native, collapsible disclosure widget, and <summary> defines the always-visible label that toggles it open or closed when clicked. It provides expand/collapse behavior, keyboard support, and correct ARIA semantics entirely without JavaScript, making it a solid default for simple accordions or FAQ sections.',null,'<details>
  <summary>What is your refund policy?</summary>
  <p>Refunds are processed within 5 business days.</p>
</details>','html','medium',ARRAY['html','semantic-html','accessibility']::text[],true,115),
('html','semantic-html','html-semantic-html-116','What does the hidden attribute do, and how does it differ from CSS display: none?','The hidden boolean attribute tells the browser to remove an element from the rendering tree entirely, similarly to display: none, but it''s expressed directly in the markup as semantic intent rather than styling. It''s still overridable by CSS (e.g. an author could set [hidden] { display: block }), and unlike display: none applied via a class, it clearly signals in the HTML itself that the content is intentionally not shown.',null,'<div hidden>This content is not rendered.</div>','html','medium',ARRAY['html','semantic-html']::text[],false,116),
('html','semantic-html','html-semantic-html-117','Why would a screen reader user rely on landmark elements like <header>, <nav>, <main>, and <footer>?','Landmark elements are automatically exposed to assistive technology as navigable regions, letting screen reader users jump directly between page landmarks (e.g. ''go to main content'' or ''go to navigation'') instead of tabbing through every element sequentially. This is one of the biggest practical accessibility wins semantic HTML provides essentially for free.',null,null,null,'medium',ARRAY['html','semantic-html','accessibility']::text[],false,117),
('html','semantic-html','html-semantic-html-118','What is the difference between the <b> element and semantic elements like <strong>, in terms of how they map to accessibility?','<b> has no semantic role and is announced by screen readers no differently from plain text — it''s purely a stylistic hint for bold rendering. <strong> is exposed with an implicit ARIA role/emphasis that some screen readers announce with a change in tone or verbal cue, conveying that the content carries real importance.',null,null,null,'hard',ARRAY['html','semantic-html','accessibility']::text[],false,118),
('html','semantic-html','html-semantic-html-119','How would you semantically mark up a blog post with an author bio and related-posts sidebar?','The post itself is an <article> containing its own <header> (title, date) and body content. The author bio, if tightly coupled to that post, can be an <aside> nested inside the <article>; a page-level related-posts widget not specific to this one post would be an <aside> outside the <article>, and the whole page would still have one <main> wrapping the primary content.',null,'<main>
  <article>
    <header><h1>Post Title</h1></header>
    <p>...</p>
    <aside>Written by Jane, a senior editor.</aside>
  </article>
  <aside>
    <h2>Related Posts</h2>
  </aside>
</main>','html','hard',ARRAY['html','semantic-html']::text[],false,119),
('html','semantic-html','html-semantic-html-120','What is the difference between <ol> and <ul> from a semantic standpoint, beyond just numbering versus bullets?','<ol> semantically communicates that the sequence or order of items matters (like ranking, steps in a recipe, or a table of contents), while <ul> communicates that the items are a collection with no inherent order. Choosing the wrong one can mislead both sighted users relying on visual numbering and assistive technology that announces list type and item count.',null,null,null,'medium',ARRAY['html','semantic-html']::text[],false,120),
('html','semantic-html','html-semantic-html-121','Can <section> elements be nested, and does each one require a heading?','Yes, sections can be nested to represent sub-themes within a broader theme, such as chapters containing sub-sections. Each section should generally have a heading to identify its content for the outline and for assistive technology, though a heading isn''t strictly enforced by the parser — omitting one is a common accessibility mistake even though it validates.',null,null,null,'medium',ARRAY['html','semantic-html']::text[],false,121),
('html','semantic-html','html-semantic-html-122','What is a common mistake developers make when overusing <div> instead of semantic elements, and why does it hurt SEO?','Wrapping everything in generic <div>s with class names like ''header'' or ''nav'' looks similar visually but gives search engine crawlers and screen readers no structural signal about content importance or relationships. Search engines use semantic cues (like <article> and heading hierarchy) to understand which content is primary, which can influence indexing and rich-result eligibility.',null,null,null,'medium',ARRAY['html','semantic-html','seo']::text[],false,122),
('html','semantic-html','html-semantic-html-123','What does the <dialog> element provide natively, compared to a custom modal built with <div>s?','<dialog> is a native modal/non-modal dialog element that, when opened with showModal(), automatically traps focus, renders on the top layer above other content, supports the ::backdrop pseudo-element, and closes on the Escape key — all behaviors a hand-built <div> modal has to reimplement manually with JavaScript and ARIA.',null,'<dialog id="confirm">
  <p>Are you sure?</p>
  <button onclick="confirm.close()">Cancel</button>
</dialog>
<script>confirm.showModal();</script>','html','hard',ARRAY['html','semantic-html','accessibility']::text[],false,123),
('html','semantic-html','html-semantic-html-124','How should you semantically structure a page with a persistent site header, a hero section, several content sections, and a footer?','A typical structure wraps the logo and primary <nav> in a top-level <header>, places the unique page content (hero, feature sections) inside a single <main> using <section> for each thematic block, and closes with a <footer> for copyright/links — keeping <header> and <footer> outside <main> since they represent repeated, page-level chrome rather than unique content.',null,'<body>
  <header><nav>...</nav></header>
  <main>
    <section>Hero</section>
    <section>Features</section>
  </main>
  <footer>...</footer>
</body>','html','medium',ARRAY['html','semantic-html','document-structure']::text[],false,124),
('html','semantic-html','html-semantic-html-125','What is the role of ARIA landmark roles in relation to native semantic HTML elements?','Native elements like <nav>, <main>, and <header> already have implicit ARIA landmark roles (navigation, main, banner) built in, so adding role="navigation" to a <nav> is redundant. ARIA roles are meant to fill gaps where no native element exists or to override semantics on generic elements, following the general rule of using native HTML before reaching for ARIA.',null,null,null,'hard',ARRAY['html','semantic-html','accessibility']::text[],false,125),
('html','semantic-html','html-semantic-html-126','Why might a <header> or <footer> nested inside an <article> have a different meaning than one at the top level of the page?','A sectioning element''s <header>/<footer> is scoped to its nearest ancestor sectioning content — so a <header> inside an <article> represents that article''s own intro (like its title and byline), not the whole page''s banner. This context-dependent scoping is what lets the same tag names be reused meaningfully at multiple nesting levels.',null,null,null,'hard',ARRAY['html','semantic-html']::text[],false,126),
('html','semantic-html','html-semantic-html-127','What is the <picture> element and how does it differ semantically from just using <img>?','<picture> wraps multiple <source> elements offering different image files or formats for different conditions (viewport width, pixel density, format support), with a fallback <img> required as the last child for browsers that don''t support <picture> or when no source matches. It''s about giving the browser a choice of resources for the same conceptual image, not about changing what the image semantically represents.',null,'<picture>
  <source srcset="photo.avif" type="image/avif">
  <source srcset="photo.webp" type="image/webp">
  <img src="photo.jpg" alt="Sunset over the bay">
</picture>','html','hard',ARRAY['html','semantic-html','media']::text[],false,127),
('html','semantic-html','html-semantic-html-128','Should a company logo linking to the homepage be marked up with <h1> or plain text inside the link?','It depends on the page: on the homepage itself, wrapping the site name/logo in <h1> is reasonable since it is the page''s primary heading. On interior pages, the actual page content''s heading should be the <h1>, and the logo link is better left as plain text or an image with descriptive alt text inside <a>, to avoid having a misleading second, unrelated <h1>.',null,null,null,'hard',ARRAY['html','semantic-html','seo']::text[],false,128),
('html','semantic-html','html-semantic-html-129','What is the difference between <cite> and <blockquote> / <q>?','<blockquote> marks a longer, block-level quotation from another source, and <q> marks a short inline quotation (browsers add quotation marks automatically). <cite> specifically references the title of a creative work being cited (like a book or article title), not the person being quoted — a common mistake is using <cite> to wrap an author''s name instead of the work''s title.',null,'<blockquote cite="https://example.com/source">
  <p>The only way to do great work is to love what you do.</p>
</blockquote>
<p>— <cite>Steve Jobs, Stanford Commencement Address</cite></p>','html','hard',ARRAY['html','semantic-html']::text[],false,129),
('html','semantic-html','html-semantic-html-130','How does using semantic HTML reduce the amount of ARIA needed on a page?','Many semantic elements come with implicit ARIA roles, states, and keyboard behavior built in by the browser — a native <button> is focusable, announces as ''button'', and responds to Space/Enter automatically, whereas a <div role="button" tabindex="0"> requires manually replicating all of that with JavaScript. This is summarized by the accessibility principle ''no ARIA is better than bad ARIA'' — prefer native elements first.',null,null,null,'medium',ARRAY['html','semantic-html','accessibility']::text[],true,130),
('html','semantic-html','html-semantic-html-131','What is the difference between the <output> element and a plain <span> for displaying a calculated result?','<output> is a semantic form-associated element specifically meant to display the result of a calculation or user action (like a slider total), and it associates with its triggering inputs via the `for` attribute while also being announced by assistive technology as a live result region when appropriately used. A plain <span> conveys no such relationship or semantic meaning.',null,'<form oninput="result.value = parseInt(a.value) + parseInt(b.value)">
  <input id="a" type="number" value="0"> +
  <input id="b" type="number" value="0"> =
  <output name="result" for="a b">0</output>
</form>','html','hard',ARRAY['html','semantic-html','forms']::text[],false,131),
('css','advanced-css','css-advanced-css-101','What is CSS specificity and how would you rank the specificity of an ID selector, a class selector, and a type selector?','Specificity determines which rule wins when multiple selectors match the same element and target the same property. Ranked from highest to lowest: inline styles beat ID selectors, ID selectors beat class/attribute/pseudo-class selectors, and those beat type (element) selectors and pseudo-elements — a single ID always outweighs any number of classes.',null,'#nav { color: blue; }     /* specificity: 1,0,0 */
.link { color: red; }     /* specificity: 0,1,0 */
a { color: green; }       /* specificity: 0,0,1 */','css','medium',ARRAY['css','advanced-css','specificity']::text[],true,101),
('css','advanced-css','css-advanced-css-102','What is the CSS cascade layers feature (@layer) and what problem does it solve?','@layer lets you explicitly group CSS rules into named layers with a defined priority order, so that specificity conflicts are resolved by layer order first, before falling back to normal specificity within a layer. This solves the long-standing problem of needing to fight specificity wars (like overriding a third-party library''s high-specificity selectors) by simply declaring your own layer to come after theirs.',null,'@layer reset, base, components, utilities;

@layer base {
  h1 { font-size: 2rem; }
}','css','hard',ARRAY['css','advanced-css','cascade-layers']::text[],false,102),
('css','advanced-css','css-advanced-css-103','What are CSS container queries and what CSS properties are required to use them?','Container queries let an element''s styles respond to the size of a containing ancestor rather than the viewport, enabling truly reusable, context-aware components. To use them, you mark an ancestor as a query container with container-type (inline-size for width-based queries) and optionally container-name, then write rules inside an @container block targeting that container.',null,'.card-wrapper {
  container-type: inline-size;
}

@container (min-width: 300px) {
  .card { display: flex; }
}','css','hard',ARRAY['css','advanced-css','container-queries']::text[],true,103),
('css','advanced-css','css-advanced-css-104','What does the CSS :is() pseudo-class do, and how does it affect specificity?',':is() accepts a comma-separated selector list and matches any element that matches at least one of them, letting you write compact selectors instead of repeating a common ancestor path for each variant. Its specificity is equal to that of its most specific argument, not the sum of all arguments, and an invalid selector in the list doesn''t invalidate the whole rule.',null,':is(header, main, footer) a {
  color: blue;
}','css','medium',ARRAY['css','advanced-css','selectors']::text[],false,104),
('css','advanced-css','css-advanced-css-105','What is the difference between :is() and :where() in terms of specificity?',':is() takes on the specificity of its most specific argument, so it still contributes real specificity weight to the overall selector. :where() behaves identically in terms of matching but always contributes zero specificity, regardless of what''s inside it, making it useful for writing easily-overridable base styles, like in a CSS reset.',null,':where(h1, h2, h3) { margin: 0; } /* specificity 0 — trivially overridable */','css','hard',ARRAY['css','advanced-css','selectors']::text[],false,105),
('css','advanced-css','css-advanced-css-106','What does the CSS :has() pseudo-class do, and why was it significant when it shipped?',':has() matches an element if it contains a descendant (or sibling, depending on the combinator used) matching the given selector — effectively a ''parent selector''. It was significant because CSS previously had no way to style an element based on its children or following siblings, a limitation that required JavaScript to work around for years.',null,'form:has(input:invalid) {
  border-color: red;
}','css','hard',ARRAY['css','advanced-css','selectors']::text[],true,106),
('css','advanced-css','css-advanced-css-107','What is the difference between the CSS ::before/::after pseudo-elements and the ::marker pseudo-element?','::before and ::after insert generated content as a child before or after an element''s actual content. ::marker specifically targets the marker box of a list item (the bullet or number), letting you style or customize it — like its color or content — separately from the list item''s own text, which was previously difficult without wrapping the text in an extra span.',null,'li::marker {
  color: tomato;
  content: "→ ";
}','css','medium',ARRAY['css','advanced-css','pseudo-elements']::text[],false,107),
('css','advanced-css','css-advanced-css-108','How does the order of !important declarations interact with cascade layers?','Normally, later layers win over earlier ones. But for declarations marked !important, this priority order reverses — an !important declaration in an earlier layer beats an !important declaration in a later layer, and any !important declaration beats a non-important one regardless of layer, which is a deliberate design choice to keep layers from being trivially subverted.',null,null,null,'hard',ARRAY['css','advanced-css','cascade-layers']::text[],false,108),
('css','advanced-css','css-advanced-css-109','What is the difference between the @supports at-rule and a browser feature-detection JavaScript library like Modernizr?','@supports lets you write conditional CSS directly, applying a block of styles only if the browser supports a given property/value pair (or a combination via and/or/not), all evaluated natively by the CSS engine with no JavaScript or extra network request required. Modernizr and similar tools run JavaScript feature tests and add classes to the document that CSS then targets, which is more flexible for testing JS APIs but adds a runtime dependency.',null,'@supports (display: grid) {
  .layout { display: grid; }
}','css','medium',ARRAY['css','advanced-css','supports']::text[],false,109),
('css','advanced-css','css-advanced-css-110','What are CSS logical properties, and how do they differ from physical properties like margin-left?','Physical properties (margin-left, border-top) always refer to a fixed visual direction regardless of writing mode. Logical properties (margin-inline-start, border-block-end) refer to direction relative to the text''s writing mode and direction (e.g. left-to-right vs. right-to-left), so the same rule automatically adapts correctly when a page is localized into an RTL language like Arabic, without needing separate RTL overrides.',null,'.card {
  margin-inline-start: 1rem;
  padding-block: 0.5rem;
}','css','hard',ARRAY['css','advanced-css','logical-properties']::text[],false,110),
('css','advanced-css','css-advanced-css-111','How is specificity calculated for combined selectors, such as an attribute selector combined with a pseudo-class?','Specificity is computed by summing counts across three tiers regardless of how selectors are combined: ID selectors (highest tier), classes/attribute-selectors/pseudo-classes (middle tier), and type-selectors/pseudo-elements (lowest tier). A selector like `input[type="text"]:focus` has one attribute selector and one pseudo-class, both counting in the middle tier, giving it a specificity of (0, 2, 0).',null,'input[type="text"]:focus { border-color: blue; } /* specificity 0,2,0 */','css','hard',ARRAY['css','advanced-css','specificity']::text[],false,111),
('css','advanced-css','css-advanced-css-112','What is the difference between the CSS all property''s initial, inherit, unset, and revert keyword values?','all: initial resets every property on the element to its specification default, ignoring inheritance entirely. all: inherit forces every property to take its parent''s computed value. all: unset acts as inherit for naturally-inherited properties and initial for non-inherited ones. all: revert resets properties to the value they would have had from the user-agent stylesheet (or user stylesheet), effectively undoing author-level styles while preserving browser defaults.',null,'.widget {
  all: unset;
}','css','hard',ARRAY['css','advanced-css','cascade']::text[],false,112),
('css','advanced-css','css-advanced-css-113','How does CSS scoping with the @scope at-rule work?','@scope lets you limit a block of styles to apply only within a specific DOM subtree, defined by a scoping root, and optionally a scoping limit (a boundary selector beyond which the styles no longer apply). This provides native style encapsulation similar to what CSS Modules or Shadow DOM offer, without needing a build step or Shadow DOM''s stricter isolation.',null,'@scope (.card) to (.card-footer) {
  p { color: gray; }
}','css','hard',ARRAY['css','advanced-css']::text[],false,113),
('css','advanced-css','css-advanced-css-114','What is the difference between the :first-child, :first-of-type, and :only-child pseudo-classes?',':first-child matches an element only if it''s the very first child of its parent, regardless of tag type. :first-of-type matches an element if it''s the first sibling of its specific tag type, even if other different-tag siblings come before it. :only-child matches an element only if it''s the sole child of its parent, with no other siblings of any type.',null,null,null,'medium',ARRAY['css','advanced-css','pseudo-classes']::text[],false,114),
('css','advanced-css','css-advanced-css-115','How does the shadow DOM affect CSS style encapsulation, and how can styles cross that boundary intentionally?','Styles defined inside a shadow root don''t leak out to the main document, and outer page styles don''t leak in by default, giving web components strong style isolation. CSS custom properties are a deliberate exception — they pierce the shadow boundary and inherit into shadow trees, making them the standard mechanism for theming web components from outside.',null,null,null,'hard',ARRAY['css','advanced-css']::text[],false,115),
('css','advanced-css','css-advanced-css-116','What is the CSS Object Model (CSSOM), and how does it relate to the DOM during rendering?','The CSSOM is the browser''s in-memory tree representation of all parsed CSS rules, analogous to how the DOM represents parsed HTML. The rendering engine combines the DOM and CSSOM to produce the render tree, which determines what''s actually painted to the screen — CSS parsing can block rendering because the browser must have the full CSSOM before it can compute final styles.',null,null,null,'hard',ARRAY['css','advanced-css','rendering']::text[],false,116),
('css','advanced-css','css-advanced-css-117','What does the CSS :focus-visible pseudo-class solve compared to plain :focus?',':focus matches whenever an element has focus, including after a mouse click, which often produces an unwanted focus ring for pointer users. :focus-visible only matches when the browser''s heuristics determine the focus indicator should be shown — typically for keyboard navigation — letting you style visible focus rings for accessibility without showing them on every mouse click.',null,'button:focus-visible {
  outline: 2px solid blue;
}','css','medium',ARRAY['css','advanced-css','pseudo-classes']::text[],false,117),
('css','advanced-css','css-advanced-css-118','What is the difference between the :nth-child(An+B) formula''s An and B parts, and how would you select every 3rd item starting from the 2nd?','In the An+B syntax, A is the cycle size (repeat every A elements) and B is the offset (starting point). `:nth-child(3n+2)` selects every 3rd element starting at the 2nd (matching positions 2, 5, 8, ...), which is useful for styling repeating patterns in grids or tables like alternating row groups.',null,'li:nth-child(3n + 2) { background: #f5f5f5; }','css','hard',ARRAY['css','advanced-css','pseudo-classes']::text[],false,118),
('css','advanced-css','css-advanced-css-119','How does CSS handle unknown or invalid property values — does an entire rule fail if one declaration is invalid?','CSS parsing is fault-tolerant at the declaration level: an individual invalid declaration (unrecognized property or malformed value) is simply discarded and ignored, but the rest of the rule block and stylesheet continues to parse and apply normally. This graceful degradation is why adding a newer property alongside an older fallback (declared first) is a safe progressive enhancement technique.',null,'.box {
  display: flex;
  display: grid; /* wins in browsers that support it; ignored otherwise if unsupported... actually both are valid, so this line demonstrates fallback ordering, not invalid parsing */
}','css','medium',ARRAY['css','advanced-css','cascade']::text[],false,119),
('css','advanced-css','css-advanced-css-120','What is the difference between @media and @container queries when it comes to what CSS properties can be conditioned on?','@media conditions on characteristics of the whole browsing environment — viewport size, orientation, color scheme, resolution, etc. — regardless of any particular element. @container conditions on the size (or, more recently, style via container-query style queries) of a specific nearby ancestor that''s opted in as a query container, making it scoped to a component rather than the whole page.',null,null,null,'medium',ARRAY['css','advanced-css','container-queries']::text[],false,120),
('css','advanced-css','css-advanced-css-121','What is a CSS custom media query, and how does @custom-media (or its successor proposals) aim to improve maintainability?','Custom media queries let you name a reusable media condition once (like --small-viewport: (max-width: 600px)) and reference that name across multiple @media rules, similar to how custom properties centralize values. This avoids repeating and risking inconsistent breakpoint values throughout a stylesheet — it''s currently available via PostCSS plugins and is part of ongoing CSS Media Queries Level 5 proposals.',null,'@custom-media --small-viewport (max-width: 600px);

@media (--small-viewport) {
  .nav { flex-direction: column; }
}','css','hard',ARRAY['css','advanced-css','media-queries']::text[],false,121),
('css','advanced-css','css-advanced-css-122','What does the CSS :target pseudo-class do?',':target matches an element whose id matches the current URL fragment identifier (the part after #), letting you style content differently when it''s been linked to directly — commonly used for CSS-only tabs, accordions, or highlighting a jumped-to section without any JavaScript.',null,'section:target {
  background: yellow;
}','css','medium',ARRAY['css','advanced-css','pseudo-classes']::text[],false,122),
('css','advanced-css','css-advanced-css-123','How does the CSS cascade order (origin and importance) work across user-agent styles, author styles, and user styles?','From lowest to highest priority: user-agent (browser default) styles, then user styles (set by the person browsing, rare today), then author styles (the page''s own CSS) — each with a normal and an !important variant, where every !important declaration across all origins outranks every normal declaration, but user !important actually outranks author !important, giving end users ultimate override power for accessibility needs.',null,null,null,'hard',ARRAY['css','advanced-css','cascade']::text[],false,123),
('css','advanced-css','css-advanced-css-124','What is the difference between CSS Modules and cascade layers (@layer) as approaches to avoiding style collisions?','CSS Modules solve collisions at build time by automatically generating unique class names scoped per file, so identical class names in different files never clash, but this requires a bundler/build step. Cascade layers solve a different problem — controlling which group of rules wins in the cascade regardless of specificity — and work natively in the browser without any build tooling, but don''t rename classes or prevent naming collisions themselves.',null,null,null,'hard',ARRAY['css','advanced-css','cascade-layers']::text[],false,124),
('css','advanced-css','css-advanced-css-125','What does the CSS @font-face at-rule do, and what does font-display control?','@font-face lets you define and load a custom web font by specifying its source file(s) and giving it a family name to reference elsewhere. font-display controls how the browser handles the loading gap before the font is ready — swap immediately shows a fallback font and swaps once the custom font loads, block briefly hides text until the font loads (or times out), and optional/fallback offer intermediate strategies balancing layout stability against custom font usage.',null,'@font-face {
  font-family: ''Inter'';
  src: url(''inter.woff2'') format(''woff2'');
  font-display: swap;
}','css','medium',ARRAY['css','advanced-css','fonts']::text[],false,125),
('css','advanced-css','css-advanced-css-126','How does the CSS content-visibility property improve rendering performance for long pages?','content-visibility: auto lets the browser skip layout, style, and paint work for elements that are off-screen, only rendering them once they''re about to become visible (similar to native lazy-rendering), which can dramatically speed up initial render of very long pages. It''s typically paired with contain-intrinsic-size to give off-screen elements a placeholder size so the page doesn''t jump as content becomes visible and its real size is calculated.',null,'.section {
  content-visibility: auto;
  contain-intrinsic-size: 500px;
}','css','hard',ARRAY['css','advanced-css','performance']::text[],false,126),
('css','advanced-css','css-advanced-css-127','What is the difference between the CSS contain property''s layout, paint, size, and style values?','contain lets you tell the browser that an element''s internals don''t affect the rest of the page in certain ways, enabling performance optimizations. layout isolates layout calculations so the element''s internal layout doesn''t affect external layout. paint clips rendering to the element''s bounds and skips painting when off-screen. size ensures the element''s size can be determined without examining its children''s content. style contains the scope of counter/quote style changes.',null,'.widget {
  contain: layout paint;
}','css','hard',ARRAY['css','advanced-css','performance']::text[],false,127),
('css','advanced-css','css-advanced-css-128','What is the difference between the CSS :empty pseudo-class and checking for an element with no visible text via JavaScript?',':empty matches an element that has absolutely no children at all — not even whitespace text nodes — making it strict about literal DOM emptiness rather than visible content. A JavaScript check can trim whitespace, check computed rendered dimensions, or account for elements that contain only comments (which :empty does allow), giving more nuanced ''is this visually empty'' detection than the CSS pseudo-class alone provides.',null,'p:empty {
  display: none;
}','css','medium',ARRAY['css','advanced-css','pseudo-classes']::text[],false,128),
('css','advanced-css','css-advanced-css-129','How do CSS custom properties interact with cascade layers — which one takes priority when both are involved in a conflict?','Custom properties follow the same cascade rules as any other property, so their resolved value is determined by cascade layer order, specificity, and source order just like a normal declaration such as color. A custom property set in a later layer overrides the same custom property set in an earlier layer, following the same layer-priority-before-specificity rule that applies to all cascade layer resolution.',null,null,null,'hard',ARRAY['css','advanced-css','cascade-layers']::text[],false,129),
('css','advanced-css','css-advanced-css-130','What is a common real-world use case for the CSS :has() pseudo-class beyond simple parent selection?','A frequently cited use case is styling a form group differently when it contains an invalid input, or dimming a card layout when it contains no image, without adding any JavaScript-driven conditional classes: `.card:has(img)` versus `.card:not(:has(img))`. It effectively replaces many previously JS-only ''style based on children''s state'' patterns with pure, declarative CSS.',null,'.card:not(:has(img)) {
  padding-top: 2rem;
}','css','medium',ARRAY['css','advanced-css','selectors']::text[],false,130),
('css','animations','css-animations-101','What is the difference between CSS transitions and CSS animations?','A transition smoothly interpolates a property between two states (start and end) triggered by some change, like a hover or class toggle, and requires an external trigger to run. An animation, defined with @keyframes, can have multiple intermediate steps, run automatically on load, loop indefinitely, and doesn''t need a triggering state change to start.',null,null,null,'easy',ARRAY['css','animations','transitions']::text[],true,101),
('css','animations','css-animations-102','What CSS properties does the transition shorthand accept?','The transition shorthand accepts, in order, transition-property (which CSS property to animate, or ''all''), transition-duration, transition-timing-function, and transition-delay. Multiple transitions can be comma-separated to animate different properties with different timings.',null,'.box {
  transition: transform 0.3s ease-in-out, opacity 0.2s linear 0.1s;
}','css','easy',ARRAY['css','animations','transitions']::text[],true,102),
('css','animations','css-animations-103','How do you define a keyframe animation and apply it to an element?','@keyframes defines named percentage-based steps (or ''from''/''to'') describing property values at points during the animation''s duration. You then apply it with the animation shorthand or individual animation-* properties, specifying at minimum the animation''s name and duration.',null,'@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

.el {
  animation: fadeIn 0.5s ease-out;
}','css','easy',ARRAY['css','animations','keyframes']::text[],true,103),
('css','animations','css-animations-104','What is the difference between the 2D transform functions translate(), scale(), and rotate()?','translate(x, y) moves an element along the x and/or y axis without affecting layout flow. scale(x, y) resizes an element proportionally or non-uniformly from its transform-origin. rotate(angle) spins an element around a fixed point (also determined by transform-origin) by the given angle, typically in degrees.',null,'.box {
  transform: translate(10px, 20px) rotate(45deg) scale(1.2);
}','css','easy',ARRAY['css','animations','transforms']::text[],true,104),
('css','animations','css-animations-105','Why are transform and opacity the preferred properties to animate for performance?','Animating transform and opacity can often be handled entirely by the GPU via compositing, skipping the browser''s layout (reflow) and paint steps entirely. Animating properties like width, top, or margin triggers layout recalculation on every frame, which is much more expensive and can cause janky, dropped-frame animations, especially on lower-powered devices.',null,null,null,'medium',ARRAY['css','animations','performance']::text[],true,105),
('css','animations','css-animations-106','What does the animation-fill-mode property control?','animation-fill-mode determines what styles apply to an element before the animation starts and after it ends. forwards retains the last keyframe''s computed values after the animation finishes, backwards applies the first keyframe''s values during the animation-delay period, and both combines both behaviors — without it, the element reverts to its pre-animation styles once the animation ends.',null,'.el {
  animation: slideIn 1s ease forwards;
}','css','medium',ARRAY['css','animations','keyframes']::text[],false,106),
('css','animations','css-animations-107','What is the difference between animation-iteration-count: infinite and setting a specific numeric value?','A numeric value for animation-iteration-count runs the animation exactly that many times (fractional values are allowed and stop partway through the last cycle) and then stops, reverting unless fill-mode retains the end state. infinite makes the animation repeat forever with no natural stopping point, commonly used for loading spinners or continuous decorative motion.',null,'.spinner {
  animation: spin 1s linear infinite;
}','css','easy',ARRAY['css','animations','keyframes']::text[],false,107),
('css','animations','css-animations-108','What does the transform-origin property control?','transform-origin sets the point around which a transform (like rotate or scale) is applied, defaulting to the center of the element (50% 50%). Changing it — for example to ''0 0'' (top-left) — changes how the element visually pivots or grows, which matters a lot for effects like a rotating hinge or a corner-anchored scale.',null,'.hinge {
  transform-origin: top left;
  transform: rotate(20deg);
}','css','medium',ARRAY['css','animations','transforms']::text[],false,108),
('css','animations','css-animations-109','What are CSS timing functions (ease, linear, ease-in-out, cubic-bezier) and how do they affect an animation''s feel?','A timing function defines the rate of change of an animated property over its duration, not just its start and end values. linear progresses at a constant rate, ease starts slow, speeds up, then slows down near the end (the default), ease-in starts slow and accelerates, ease-out starts fast and decelerates, and cubic-bezier(x1, y1, x2, y2) defines a fully custom easing curve for precise control.',null,'.box {
  transition: transform 0.4s cubic-bezier(0.34, 1.56, 0.64, 1);
}','css','medium',ARRAY['css','animations','transitions']::text[],false,109),
('css','animations','css-animations-110','How do you animate multiple keyframe steps with different percentages instead of just from/to?','Instead of only ''from'' and ''to'' (equivalent to 0% and 100%), @keyframes can define any number of percentage checkpoints, letting you control the exact value of properties at intermediate points in the animation timeline, such as creating a bounce or pulse effect with several stages.',null,'@keyframes pulse {
  0% { transform: scale(1); }
  50% { transform: scale(1.1); }
  100% { transform: scale(1); }
}','css','easy',ARRAY['css','animations','keyframes']::text[],false,110),
('css','animations','css-animations-111','What is the difference between animation-direction values normal, reverse, alternate, and alternate-reverse?','normal plays each iteration forward from 0% to 100%. reverse always plays backward from 100% to 0%. alternate plays forward on odd iterations and backward on even ones, creating a smooth back-and-forth loop instead of a jump-cut reset. alternate-reverse is the same but starts with a backward iteration first.',null,'.breathe {
  animation: pulse 2s ease-in-out infinite alternate;
}','css','medium',ARRAY['css','animations','keyframes']::text[],false,111),
('css','animations','css-animations-112','How can you pause and resume a CSS animation, and when is this useful?','animation-play-state can be set to paused or running, often toggled via :hover or a JavaScript class toggle, letting you freeze an animation at its current frame and resume from that same point later. This is useful for interactive effects, like pausing a marquee or spinner when the user hovers over it.',null,'.marquee:hover {
  animation-play-state: paused;
}','css','medium',ARRAY['css','animations','keyframes']::text[],false,112),
('css','animations','css-animations-113','What is the difference between the 2D and 3D CSS transform functions, such as rotate() vs rotateX()/rotateY()/rotate3d()?','2D transform functions like rotate(), translate(), and scale() operate purely on the flat x/y plane. 3D functions like rotateX(), rotateY(), rotate3d(), and translate3d() operate in three-dimensional space along the z-axis too, and require a perspective value (on the element or its parent) to actually render with visible depth rather than looking flat.',null,'.card {
  perspective: 1000px;
}
.card-inner {
  transform: rotateY(180deg);
  transition: transform 0.6s;
}','css','hard',ARRAY['css','animations','transforms']::text[],false,113),
('css','animations','css-animations-114','What does the CSS perspective property do for 3D transforms?','perspective defines the distance between the viewer and the z=0 plane, which controls how dramatic the 3D depth effect appears — a smaller value creates a more exaggerated, close-up perspective, while a larger value creates a subtler effect. It must be set on the parent of a 3D-transformed element (or via the transform function perspective()) for 3D transforms like rotateX/Y to render with visible depth.',null,'.scene {
  perspective: 800px;
}','css','hard',ARRAY['css','animations','transforms']::text[],false,114),
('css','animations','css-animations-115','How do you detect when a CSS transition or animation has finished, using JavaScript?','The transitionend event fires when a CSS transition completes, and animationend (plus animationstart and animationiteration) fire for keyframe animations. These events let JavaScript synchronize follow-up logic, like removing an element from the DOM after a fade-out transition finishes, without relying on a manually estimated setTimeout.',null,'el.addEventListener(''transitionend'', () => el.remove());','javascript','medium',ARRAY['css','animations','transitions']::text[],false,115),
('css','animations','css-animations-116','Why won''t a CSS transition run if you change a property''s value and the transition property at the same time in the same style change?','A transition only animates a change between the previously computed value and a new value that the browser detects as a change over subsequent frames. If both the transition-defining styles and the property''s target value are applied in the same synchronous style recalculation (e.g. both set at once via a class added on page load), the browser may never register an initial state to transition from, so the change appears instantly instead of animating — a common fix is to force a reflow or use requestAnimationFrame before toggling the class.',null,null,null,'hard',ARRAY['css','animations','transitions']::text[],false,116),
('css','animations','css-animations-117','What is the will-change property and when should you use it?','will-change hints to the browser that a property (like transform or opacity) is about to change, letting it optimize ahead of time by promoting the element to its own compositor layer. It should be used sparingly and only right before an animation starts (often added/removed dynamically), since applying it broadly or permanently can waste memory by creating unnecessary layers.',null,'.el {
  will-change: transform;
}','css','hard',ARRAY['css','animations','performance']::text[],false,117),
('css','animations','css-animations-118','How would you create a smooth loading spinner using only CSS?','A common approach styles a circular element with a transparent border on one side and a solid color on the rest, then applies an infinite linear keyframe animation that rotates it a full 360 degrees, creating a continuously spinning effect with no JavaScript required.',null,'@keyframes spin {
  to { transform: rotate(360deg); }
}
.spinner {
  border: 4px solid #eee;
  border-top-color: #333;
  border-radius: 50%;
  animation: spin 1s linear infinite;
}','css','medium',ARRAY['css','animations','keyframes']::text[],false,118),
('css','animations','css-animations-119','What is the difference between transitioning height: auto directly versus using max-height or the newer interpolate-size for a collapse/expand animation?','CSS transitions cannot animate to or from the keyword ''auto'' because the browser doesn''t know an interpolatable start/end pixel value, so transitioning height: auto jumps instantly instead of animating. A common workaround transitions max-height to a value comfortably larger than the content, and the newer interpolate-size: allow-keywords CSS property (with calc-size()) is designed to solve this natively by letting auto participate in transitions.',null,'.panel {
  max-height: 0;
  overflow: hidden;
  transition: max-height 0.3s ease;
}
.panel.open {
  max-height: 500px;
}','css','hard',ARRAY['css','animations','transitions']::text[],false,119),
('css','animations','css-animations-120','How do CSS animations interact with the prefers-reduced-motion media feature for accessibility?','Wrapping non-essential animation/transition declarations in an @media (prefers-reduced-motion: reduce) block lets you disable, shorten, or replace motion for users who have indicated a system preference against it, which matters for users with vestibular disorders. A common pattern sets animation-duration and transition-duration to a near-zero value instead of removing the effect entirely, preserving state changes without the motion.',null,'@media (prefers-reduced-motion: reduce) {
  * { animation-duration: 0.01ms !important; transition-duration: 0.01ms !important; }
}','css','medium',ARRAY['css','animations','accessibility']::text[],false,120),
('css','animations','css-animations-121','What is the difference between animating with CSS transitions/keyframes and animating with the Web Animations API (WAAPI) in JavaScript?','CSS transitions/animations are declared statically in stylesheets and run on the compositor without needing JavaScript, which is simplest for fixed, predictable effects. The Web Animations API lets you construct, control (play, pause, reverse, seek), and dynamically generate animations from JavaScript with the same underlying performance characteristics, which is better suited for animations whose parameters depend on runtime data or user interaction.',null,'el.animate([{ opacity: 0 }, { opacity: 1 }], { duration: 300, fill: ''forwards'' });','javascript','hard',ARRAY['css','animations']::text[],false,121),
('css','animations','css-animations-122','What does the CSS matrix() transform function represent, and why is it rarely written by hand?','matrix() takes six values that together encode a full 2D affine transformation (combining translation, rotation, scale, and skew) as a single 3x3 transformation matrix. It''s rarely hand-written because the values aren''t intuitive; instead, developers use the individual functions like translate/rotate/scale, and matrix() is typically only seen as a computed output value or produced programmatically.',null,'transform: matrix(1, 0, 0, 1, 50, 100); /* equivalent to translate(50px, 100px) */','css','hard',ARRAY['css','animations','transforms']::text[],false,122),
('css','animations','css-animations-123','How can you stagger animations across multiple elements using only CSS?','Applying a different animation-delay to each element — often calculated via the :nth-child() pseudo-class or with a CSS custom property multiplied by an index — makes elements start their animation at slightly offset times, creating a staggered, cascading visual effect instead of all elements animating in unison.',null,'.item:nth-child(1) { animation-delay: 0s; }
.item:nth-child(2) { animation-delay: 0.1s; }
.item:nth-child(3) { animation-delay: 0.2s; }','css','medium',ARRAY['css','animations','keyframes']::text[],false,123),
('css','animations','css-animations-124','What is the backface-visibility property used for?','backface-visibility controls whether the back side of a 3D-transformed element is rendered when it''s rotated to face away from the viewer (e.g. past 90 degrees on rotateY). Setting it to hidden is the standard technique for flip-card effects, so the reversed, mirrored content of the front face isn''t visible through the back during the flip.',null,'.card-face {
  backface-visibility: hidden;
}','css','hard',ARRAY['css','animations','transforms']::text[],false,124),
('css','animations','css-animations-125','Can you animate CSS Grid or Flexbox layout properties like grid-template-columns directly, and what''s the usual workaround?','Historically, properties like grid-template-columns were not smoothly animatable because they resolve to a list of track sizes rather than a single interpolatable value, so changes would jump instantly. Modern browsers have added interpolation support for many of these properties, but a common cross-browser-safe workaround is animating transform or a numeric custom property with @property instead, and deriving layout from that.',null,null,null,'hard',ARRAY['css','animations','grid']::text[],false,125),
('css','animations','css-animations-126','What is the purpose of the @property at-rule in relation to animating CSS custom properties?','By default, CSS custom properties are treated as untyped strings, so the browser can''t meaningfully interpolate between two values (e.g. animating a gradient angle or a color stored in a variable) — it just swaps instantly. @property lets you register a custom property with a specific syntax type (like <angle> or <color>), an initial value, and inheritance behavior, which enables smooth animation/transition of that custom property.',null,'@property --angle {
  syntax: ''<angle>'';
  initial-value: 0deg;
  inherits: false;
}','css','hard',ARRAY['css','animations','custom-properties']::text[],false,126),
('css','animations','css-animations-127','What is the difference between transition-delay and animation-delay?','Both delay the start of their respective effect by the specified time, but transition-delay only matters the first time a transition-triggering property change occurs (e.g. a class toggle), while animation-delay applies once when the animation begins, and — if animation-direction is set to backwards or both — the first keyframe''s styles are visibly applied during that delay period.',null,null,null,'medium',ARRAY['css','animations','keyframes']::text[],false,127),
('css','animations','css-animations-128','How do you create a smooth hover-triggered scale/lift effect on a card using CSS transitions?','Set a transition on the transform and box-shadow properties on the base state, then define the changed values (like a slight scale-up and a larger shadow) on the :hover pseudo-class — the browser automatically animates between the two states over the given duration, without needing keyframes for a simple two-state effect.',null,'.card {
  transition: transform 0.2s ease, box-shadow 0.2s ease;
}
.card:hover {
  transform: translateY(-4px) scale(1.02);
  box-shadow: 0 10px 20px rgba(0,0,0,0.15);
}','css','easy',ARRAY['css','animations','transitions']::text[],false,128),
('css','animations','css-animations-129','What is the skew() transform function and what visual effect does it create?','skew(x-angle, y-angle) distorts an element by slanting it along the x and/or y axis, turning rectangles into parallelogram-like shapes, without changing its actual size or rotation. It''s often used for decorative section dividers or stylized button/card effects.',null,'.banner {
  transform: skewY(-3deg);
}','css','medium',ARRAY['css','animations','transforms']::text[],false,129),
('css','animations','css-animations-130','What is the difference between animating with steps() as a timing function versus a smooth easing curve like ease?','steps(n) divides an animation into a fixed number of discrete jumps rather than a continuous transition, holding each intermediate value for an equal fraction of the duration before jumping to the next — commonly used for sprite-sheet animations or typewriter text effects. Smooth easing functions like ease or cubic-bezier() interpolate continuously between values, producing fluid rather than stepped motion.',null,'.sprite {
  animation: run 1s steps(8) infinite;
}','css','hard',ARRAY['css','animations','keyframes']::text[],false,130),
('css','basics','css-basics-101','What is the CSS box model?','The box model describes how every element is rendered as a rectangular box made of four layers, from innermost to outermost: content, padding, border, and margin. The content holds text or child elements, padding adds space inside the border, the border wraps the padding, and the margin adds space outside the border separating it from other elements.',null,null,null,'easy',ARRAY['css','basics','box-model']::text[],true,101),
('css','basics','css-basics-102','What is the difference between box-sizing: content-box and box-sizing: border-box?','With content-box (the default), width and height apply only to the content area, so padding and border are added on top, making the rendered box larger than the specified size. With border-box, width and height include padding and border, so the element''s total rendered size matches the value you set, which makes layout math far more predictable.',null,'* {
  box-sizing: border-box;
}','css','easy',ARRAY['css','basics','box-model']::text[],true,102),
('css','basics','css-basics-103','What is the difference between a class selector and an ID selector?','A class selector (.name) can be applied to multiple elements and multiple classes can be applied to a single element, making it ideal for reusable styling. An ID selector (#name) must be unique per page and has much higher specificity than a class, which is why relying on IDs for styling is generally discouraged in favor of classes.',null,'.card { color: blue; }
#header { color: red; }','css','easy',ARRAY['css','basics','selectors']::text[],false,103),
('css','basics','css-basics-104','What is the difference between a descendant combinator and a child combinator?','The descendant combinator (a space, e.g. `div p`) selects an element that is nested anywhere inside another element, no matter how deep. The child combinator (`>`, e.g. `div > p`) only selects elements that are direct children, one level deep, ignoring deeper nested matches.',null,'div p { color: green; }   /* any nested p */
div > p { color: red; }   /* only direct child p */','css','easy',ARRAY['css','basics','selectors']::text[],false,104),
('css','basics','css-basics-105','What is the difference between display: none and visibility: hidden?','display: none removes the element from the document entirely — it takes up no space and isn''t rendered, as if it weren''t in the DOM at all. visibility: hidden keeps the element''s space reserved in the layout but makes it invisible, so surrounding elements don''t shift to fill the gap.',null,null,null,'easy',ARRAY['css','basics','display']::text[],true,105),
('css','basics','css-basics-106','What are the different values of the display property and what do they do?','block elements start on a new line and take up the full available width (e.g. div, p). inline elements flow within text and don''t accept width/height or vertical margin (e.g. span, a). inline-block behaves like inline for flow but accepts width, height, and margin like a block. none removes the element from rendering entirely.',null,null,null,'easy',ARRAY['css','basics','display']::text[],false,106),
('css','basics','css-basics-107','How does margin collapsing work in CSS?','When two block elements'' vertical margins meet — such as the bottom margin of one element and the top margin of the next sibling, or a parent and its first/last child with no border or padding between them — the two margins collapse into a single margin equal to the larger of the two, rather than adding together. Horizontal margins never collapse, and margin collapsing can be prevented with padding, a border, or by establishing a new block formatting context (e.g. overflow: auto or display: flow-root).',null,null,null,'medium',ARRAY['css','basics','box-model']::text[],false,107),
('css','basics','css-basics-108','What is the difference between a shorthand and a longhand CSS property?','A longhand property sets exactly one value, like border-top-width or margin-left. A shorthand property, like border, margin, or font, combines several related longhand properties into a single declaration, saving space — but omitting a sub-value in a shorthand usually resets it to its initial value, which can accidentally override a previously set longhand.',null,'margin: 10px 20px; /* top/bottom: 10px, left/right: 20px */','css','easy',ARRAY['css','basics']::text[],false,108),
('css','basics','css-basics-109','What is the difference between the :hover, :focus, and :active pseudo-classes?',':hover applies while the pointer is positioned over an element. :focus applies when an element (like an input or link) has received keyboard or programmatic focus. :active applies only during the moment an element is being clicked or pressed, such as between mousedown and mouseup.',null,null,null,'easy',ARRAY['css','basics','pseudo-classes']::text[],false,109),
('css','basics','css-basics-110','What is the difference between an attribute selector and a class selector?','A class selector matches elements that carry a specific class in their class attribute. An attribute selector, like [type="text"] or [data-active], matches elements based on the presence or value of any HTML attribute, which is useful for styling form inputs or elements without needing to add extra classes.',null,'input[type="checkbox"] { accent-color: teal; }','css','medium',ARRAY['css','basics','selectors']::text[],false,110),
('css','basics','css-basics-111','How do universal selectors and grouping selectors work?','The universal selector (*) matches every element on the page and is often used for CSS resets. A grouping selector uses a comma to apply the same declaration block to multiple, unrelated selectors at once, avoiding repetition.',null,'h1, h2, h3 {
  font-family: sans-serif;
}','css','easy',ARRAY['css','basics','selectors']::text[],false,111),
('css','basics','css-basics-112','What is the difference between inline styles, internal stylesheets, and external stylesheets?','Inline styles are written directly on an element via the style attribute and have very high specificity, making them hard to override and hard to maintain. Internal stylesheets use a <style> block inside the HTML document''s head. External stylesheets live in a separate .css file linked with <link>, which is the most maintainable approach since it allows caching and reuse across pages.',null,null,null,'easy',ARRAY['css','basics']::text[],false,112),
('css','basics','css-basics-113','What is the CSS cascade, at a high level?','The cascade is the algorithm the browser uses to resolve conflicting CSS declarations for the same element and property. It considers, in order, the origin and importance of the rule (author vs. user-agent, !important), specificity of the selector, and finally source order — the last declared rule wins if everything else is equal.',null,null,null,'medium',ARRAY['css','basics','cascade']::text[],false,113),
('css','basics','css-basics-114','What does the !important rule do and why is it generally discouraged?','Adding !important to a declaration makes it override any other declaration for that property, regardless of specificity or source order (except another !important rule with higher specificity). It''s discouraged because it breaks the normal cascade, makes debugging harder, and often forces other developers to also use !important to override it, creating a maintenance spiral.',null,'.alert { color: red !important; }','css','easy',ARRAY['css','basics','cascade']::text[],false,114),
('css','basics','css-basics-115','What is the difference between the overflow values visible, hidden, scroll, and auto?','visible (default) lets content spill outside its container without clipping. hidden clips overflowing content and hides it. scroll always shows scrollbars, even if content fits. auto only shows scrollbars when content actually overflows the container''s box.',null,null,null,'easy',ARRAY['css','basics']::text[],false,115),
('css','basics','css-basics-116','What is the difference between em and rem units?','em is relative to the font-size of the current element''s parent (or the element itself for properties like font-size), which means it compounds when nested, since each level multiplies against its own parent''s size. rem is always relative to the root (html) element''s font-size, making it more predictable for consistent sizing across a page.',null,'html { font-size: 16px; }
.text { font-size: 1.5rem; } /* always 24px */','css','medium',ARRAY['css','basics','units']::text[],true,116),
('css','basics','css-basics-117','How does CSS specificity get calculated?','Specificity is calculated as a tuple of counts: inline styles rank highest, followed by the number of ID selectors, then the number of classes/attributes/pseudo-classes, then the number of type selectors/pseudo-elements. Higher counts in an earlier category always beat any number of items in a lower category, e.g. one ID beats a hundred classes.',null,'#nav .link { color: blue; } /* 1 id, 1 class → higher specificity */
.link { color: red; }       /* 1 class */','css','medium',ARRAY['css','basics','specificity']::text[],true,117),
('css','basics','css-basics-118','What is the difference between a pseudo-class and a pseudo-element?','A pseudo-class (single colon, e.g. :hover, :first-child) selects an element based on a state or position it''s already in. A pseudo-element (double colon, e.g. ::before, ::first-line) targets a specific sub-part of an element, or generates content that isn''t otherwise represented in the DOM.',null,'p::first-letter { font-size: 2em; }','css','medium',ARRAY['css','basics','pseudo-elements']::text[],false,118),
('css','basics','css-basics-119','How does the CSS ::before and ::after pseudo-element work, and what is required for them to render?','::before and ::after insert generated content as the first or last child of an element, respectively, without adding markup to the DOM. They require the content property to be set (even to an empty string) or they won''t render at all, and they''re commonly used for decorative icons, tooltips, or clearfix hacks.',null,'.required::after {
  content: " *";
  color: red;
}','css','medium',ARRAY['css','basics','pseudo-elements']::text[],false,119),
('css','basics','css-basics-120','What is CSS inheritance and which properties are inherited by default?','Inheritance means a child element automatically takes on certain computed property values from its parent unless explicitly overridden. Text-related properties like color, font-family, font-size, and line-height inherit by default, while box-model properties like margin, padding, border, and width do not, since inheriting layout properties would make positioning unpredictable.',null,null,null,'medium',ARRAY['css','basics','cascade']::text[],false,120),
('css','basics','css-basics-121','How do you force a non-inherited property to inherit its parent''s value, or reset it entirely?','The `inherit` keyword forces any property to explicitly take its parent''s computed value, even properties that don''t inherit by default. `initial` resets a property to its specification-defined default, and `unset` acts like inherit for naturally-inheriting properties or initial for non-inheriting ones.',null,'button {
  font: inherit;
}','css','hard',ARRAY['css','basics','cascade']::text[],false,121),
('css','basics','css-basics-122','What is the difference between the :nth-child() and :nth-of-type() pseudo-classes?',':nth-child(n) selects an element based on its position among all sibling elements, regardless of tag type, so it only matches if the element at that position is also the correct tag. :nth-of-type(n) selects based on position counted only among siblings of the same tag, which avoids mismatches when different element types are interleaved.',null,'li:nth-child(2n) { background: #eee; } /* even list items */','css','medium',ARRAY['css','basics','pseudo-classes']::text[],false,122),
('css','basics','css-basics-123','What is a CSS reset or normalize stylesheet, and why is one commonly used?','Browsers ship with different default user-agent stylesheets (different default margins on headings, list bullet styles, form control fonts, etc.), which causes inconsistent rendering across browsers. A CSS reset aggressively zeroes out these defaults, while a normalize stylesheet instead makes defaults consistent across browsers while preserving useful baseline styling — both are applied early to establish a predictable starting point.',null,null,null,'medium',ARRAY['css','basics']::text[],false,123),
('css','basics','css-basics-124','What is the difference between the CSS width: auto and width: 100%?','For a block element, width: auto lets the browser calculate the width to fill the available space in the containing block while still respecting margins, so a fixed margin plus auto width leaves the exact remaining space. width: 100% forces the content box to be exactly the full width of the parent, which — with content-box sizing — can cause overflow if padding or border is also set, since they''re added on top.',null,null,null,'hard',ARRAY['css','basics','box-model']::text[],false,124),
('css','basics','css-basics-125','What are combinators in CSS selectors, and what are the four main types?','Combinators define the relationship between two selectors. The four main types are: the descendant combinator (space) for any nested descendant, the child combinator (>) for direct children only, the adjacent sibling combinator (+) for the immediately following sibling, and the general sibling combinator (~) for any sibling that comes after, regardless of distance.',null,'h2 + p { margin-top: 0; }  /* paragraph immediately after an h2 */','css','medium',ARRAY['css','basics','selectors']::text[],false,125),
('css','basics','css-basics-126','What is the difference between the outline property and the border property?','border is part of the box model — it takes up space, affects layout, and can have different widths per side. outline is drawn outside the border, doesn''t affect layout or box size, and is typically used to indicate keyboard focus for accessibility; removing it without a visible replacement harms keyboard users.',null,'button:focus-visible {
  outline: 2px solid blue;
}','css','medium',ARRAY['css','basics','accessibility']::text[],false,126),
('css','basics','css-basics-127','How does the CSS box model differ between block-level and inline elements regarding margin and padding?','Block-level elements respect margin and padding on all four sides, affecting the layout of surrounding elements. Inline elements respect horizontal (left/right) padding and margin visually, but vertical (top/bottom) margin has no effect on layout, and vertical padding can visually overlap surrounding lines without pushing them away.',null,null,null,'hard',ARRAY['css','basics','box-model']::text[],false,127),
('css','basics','css-basics-128','What is a CSS comment and how do you write one?','CSS comments are written between /* and */ and can span multiple lines; they''re ignored by the browser at render time. Unlike some languages, CSS has no single-line // comment syntax.',null,'/* This hides the sidebar on small screens */
.sidebar { display: none; }','css','easy',ARRAY['css','basics']::text[],false,128),
('css','basics','css-basics-129','What is the difference between absolute and relative units in CSS?','Absolute units, like px, cm, and pt, always represent the same physical or fixed size regardless of context. Relative units, like %, em, rem, vw, and vh, are computed relative to another value — a parent''s size, the root font size, or the viewport — making them essential for responsive, scalable layouts.',null,null,null,'easy',ARRAY['css','basics','units']::text[],false,129),
('css','basics','css-basics-130','What does the :not() pseudo-class do, and what is one limitation of its classic form?',':not() excludes elements that match the given selector from being selected, e.g. `li:not(.active)` selects all list items except those with the active class. In its original (level 3) form it only accepted a single simple selector, though modern CSS (Selectors Level 4) allows a comma-separated selector list inside it.',null,'input:not([type="submit"]) {
  border: 1px solid gray;
}','css','medium',ARRAY['css','basics','selectors']::text[],false,130),
('css','basics','css-basics-131','How do CSS custom properties differ from Sass/SCSS variables at a conceptual level?','Sass variables are compiled away at build time into static values, so they have no runtime presence and can''t be read or changed once compiled. CSS custom properties (--name) are live in the DOM, can be read and updated via JavaScript, cascade and inherit like normal CSS properties, and can even be changed dynamically per-element or via media queries.',null,null,null,'medium',ARRAY['css','basics']::text[],false,131),
('css','basics','css-basics-132','What happens when two CSS rules have the exact same specificity and target the same element?','When specificity is tied, the cascade falls back to source order: whichever rule appears later in the stylesheet (or in a later linked stylesheet) wins. This is why the order of CSS rules and imports matters even when specificity looks identical.',null,'.btn { color: blue; }
.btn { color: green; } /* wins - declared later */','css','easy',ARRAY['css','basics','cascade']::text[],false,132),
('css','layout','css-layout-101','What is Flexbox and what problem does it solve?','Flexbox is a one-dimensional layout model for distributing space among items in a row or a column, even when their sizes are unknown or dynamic. It solves problems that were painful with floats or tables, like vertical centering, equal-height columns, and evenly distributing extra space between items.',null,'.container {
  display: flex;
}','css','easy',ARRAY['css','layout','flexbox']::text[],true,101),
('css','layout','css-layout-102','What is the difference between justify-content and align-items in Flexbox?','justify-content aligns flex items along the main axis (horizontal by default, in a row container), controlling how extra space is distributed between and around items. align-items aligns items along the cross axis (perpendicular to the main axis), controlling how each item is positioned within the line''s height or width.',null,'.container {
  display: flex;
  justify-content: center;
  align-items: center;
}','css','easy',ARRAY['css','layout','flexbox']::text[],true,102),
('css','layout','css-layout-103','How does the flex-grow, flex-shrink, and flex-basis shorthand (flex) work?','flex-basis sets an item''s initial main-size before extra space is distributed. flex-grow is a unitless number that determines how much of the remaining free space an item should consume relative to its siblings. flex-shrink determines how much an item should shrink relative to siblings when there isn''t enough space. The flex shorthand combines all three, e.g. `flex: 1 1 0` or the common `flex: 1`.',null,'.item {
  flex: 1 1 200px;
}','css','medium',ARRAY['css','layout','flexbox']::text[],true,103),
('css','layout','css-layout-104','What is CSS Grid and how does it differ from Flexbox?','CSS Grid is a two-dimensional layout system that lets you control rows and columns simultaneously, making it ideal for overall page layouts and complex grids. Flexbox is one-dimensional, laying items out in a single row or column at a time — the two are complementary, and it''s common to use Grid for the page shell and Flexbox for smaller components within it.',null,'.grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 16px;
}','css','easy',ARRAY['css','layout','grid']::text[],true,104),
('css','layout','css-layout-105','What does the fr unit mean in CSS Grid?','The fr unit represents a fraction of the available (leftover) space in a grid container after fixed-size tracks and gaps are accounted for. `grid-template-columns: 1fr 2fr` splits remaining space so the second column gets twice as much as the first.',null,'grid-template-columns: 200px 1fr 1fr;','css','medium',ARRAY['css','layout','grid']::text[],false,105),
('css','layout','css-layout-106','How do you create a responsive grid without media queries using auto-fit or auto-fill?','repeat(auto-fit, minmax(200px, 1fr)) or repeat(auto-fill, ...) lets the grid automatically calculate how many columns fit the container width, wrapping items as needed. auto-fit collapses empty tracks to fill leftover space among existing items, while auto-fill preserves empty tracks even if there''s no content, which affects sizing when there are fewer items than would fill a row.',null,'.grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
  gap: 1rem;
}','css','hard',ARRAY['css','layout','grid']::text[],false,106),
('css','layout','css-layout-107','What are the different values of the CSS position property?','static is the default flow position, unaffected by top/left/right/bottom. relative offsets an element from its normal position while still reserving its original space. absolute removes the element from normal flow and positions it relative to its nearest positioned ancestor (or the initial containing block). fixed positions relative to the viewport and stays put during scrolling. sticky toggles between relative and fixed depending on scroll position within its containing block.',null,null,null,'easy',ARRAY['css','layout','positioning']::text[],true,107),
('css','layout','css-layout-108','What does position: sticky actually do, and what are common pitfalls when it doesn''t work?','position: sticky treats an element as relative until it crosses a specified threshold (like top: 0) during scrolling within its nearest scrolling ancestor, at which point it becomes fixed within that container''s bounds. Common pitfalls: an ancestor with overflow: hidden, auto, or scroll can clip the sticky effect, and a parent without enough height for the element to scroll within will prevent it from appearing to stick at all.',null,'.header {
  position: sticky;
  top: 0;
}','css','medium',ARRAY['css','layout','positioning']::text[],true,108),
('css','layout','css-layout-109','What is a positioned element''s containing block for absolute positioning?','An absolutely positioned element is placed relative to its nearest ancestor whose position is anything other than static (relative, absolute, fixed, or sticky). If no such ancestor exists, it''s positioned relative to the initial containing block, which is roughly the viewport.',null,'.parent {
  position: relative;
}
.child {
  position: absolute;
  top: 0;
  right: 0;
}','css','medium',ARRAY['css','layout','positioning']::text[],false,109),
('css','layout','css-layout-110','How do you vertically and horizontally center an element in CSS?','The most common modern approach is Flexbox: set display: flex, justify-content: center, and align-items: center on the parent. Grid offers an even shorter equivalent with place-items: center. Absolute positioning with top: 50%, left: 50%, and transform: translate(-50%, -50%) is another common technique when Flexbox/Grid isn''t applicable.',null,'.center {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100vh;
}','css','easy',ARRAY['css','layout','flexbox']::text[],true,110),
('css','layout','css-layout-111','What is the difference between align-items and align-self?','align-items is set on the flex or grid container and applies a default cross-axis alignment to all children. align-self is set on an individual item and overrides the container''s align-items value for just that one item.',null,'.item {
  align-self: flex-end;
}','css','easy',ARRAY['css','layout','flexbox']::text[],false,111),
('css','layout','css-layout-112','What does the CSS float property do and why is it rarely used for layout today?','float takes an element out of normal flow and pushes it to the left or right, letting inline content wrap around it — originally intended for wrapping text around images. It was repurposed for multi-column layouts before Flexbox and Grid existed, but caused issues like collapsing parent heights (requiring clearfix hacks), which Flexbox and Grid solve natively without side effects.',null,null,null,'medium',ARRAY['css','layout','float']::text[],false,112),
('css','layout','css-layout-113','What is the clearfix hack and why was it needed?','When all children of a container are floated, the container collapses to zero height because floated elements don''t contribute to their parent''s height in normal flow. The clearfix hack adds a pseudo-element after the content with `clear: both` (or uses `display: flow-root`) to force the container to expand and contain its floated children.',null,'.clearfix::after {
  content: "";
  display: table;
  clear: both;
}','css','hard',ARRAY['css','layout','float']::text[],false,113),
('css','layout','css-layout-114','How do grid-template-areas work in CSS Grid?','grid-template-areas lets you name grid regions as a visual ASCII-art-like map of strings, and then assign each child to a named area with the grid-area property. It''s a very readable way to define page layouts like header/sidebar/main/footer without manually counting rows and columns.',null,'.layout {
  display: grid;
  grid-template-areas:
    "header header"
    "sidebar main"
    "footer footer";
}
.header { grid-area: header; }','css','medium',ARRAY['css','layout','grid']::text[],false,114),
('css','layout','css-layout-115','What is the difference between grid-gap (gap) and margin when spacing grid or flex items?','gap applies uniform spacing only between grid/flex items, without adding space before the first or after the last item, and doesn''t cause margin-collapsing issues. margin must be applied per-item and can create uneven spacing at the edges of the container unless carefully managed with negative margins or :not(:last-child) selectors.',null,'.grid {
  display: grid;
  gap: 16px;
}','css','easy',ARRAY['css','layout','grid']::text[],false,115),
('css','layout','css-layout-116','What does the z-index property do, and when does it have no effect?','z-index controls the stacking order of positioned elements along the z-axis — higher values render on top of lower ones. It only has an effect on elements with a position value other than static; on a static element, z-index is ignored entirely.',null,'.modal {
  position: fixed;
  z-index: 1000;
}','css','medium',ARRAY['css','layout','positioning']::text[],true,116),
('css','layout','css-layout-117','What is a stacking context in CSS?','A stacking context is a self-contained group of elements whose z-index values are only compared against each other, not against elements outside the context. Certain properties — a positioned element with a non-auto z-index, opacity less than 1, transform, filter, or will-change — create a new stacking context, which can cause a high z-index inside one context to still render below a lower z-index element in a different, later stacking context.',null,null,null,'hard',ARRAY['css','layout','positioning']::text[],false,117),
('css','layout','css-layout-118','How do you make flex items wrap onto multiple lines?','By default, flex items shrink to fit on one line (flex-wrap: nowrap). Setting flex-wrap: wrap allows items to flow onto additional lines when they don''t fit the container''s main-axis size, and align-content can then control how those wrapped lines are spaced along the cross axis.',null,'.container {
  display: flex;
  flex-wrap: wrap;
}','css','easy',ARRAY['css','layout','flexbox']::text[],false,118),
('css','layout','css-layout-119','What is the difference between grid-auto-rows/grid-auto-columns and grid-template-rows/grid-template-columns?','grid-template-rows/columns explicitly define the sizes of the named tracks in the grid. grid-auto-rows/columns set the sizing for any implicit tracks the browser creates automatically when content is placed outside the explicitly defined grid, such as extra rows added when there are more items than defined cells.',null,'.grid {
  grid-template-columns: repeat(3, 1fr);
  grid-auto-rows: 100px;
}','css','hard',ARRAY['css','layout','grid']::text[],false,119),
('css','layout','css-layout-120','How can you make a single flex or grid item span multiple columns?','In Grid, use grid-column: span 2 (or explicit line numbers like grid-column: 1 / 3) on an item to make it stretch across multiple tracks. Flexbox has no native column-spanning concept since it''s one-dimensional, but flex-basis or flex-grow can be used to make an item take proportionally more space along the main axis.',null,'.featured {
  grid-column: span 2;
}','css','medium',ARRAY['css','layout','grid']::text[],false,120),
('css','layout','css-layout-121','What is the order property in Flexbox used for?','order lets you visually reorder flex items independently of their source order in the DOM, using integer values (default 0) — items with lower order values render first. It only changes visual/painting order, not the DOM or tab order, which can create accessibility mismatches if used carelessly.',null,'.first-visually {
  order: -1;
}','css','medium',ARRAY['css','layout','flexbox']::text[],false,121),
('css','layout','css-layout-122','How do you center a flex container''s content while also aligning individual items differently?','Set justify-content and align-items on the container for the default alignment of all children, then override an individual item''s cross-axis alignment with align-self on that specific item — there''s no per-item override for justify-content on the main axis since it operates on the whole line''s distribution, not single items.',null,null,null,'medium',ARRAY['css','layout','flexbox']::text[],false,122),
('css','layout','css-layout-123','What is the difference between inset and setting top, right, bottom, and left individually?','inset is a shorthand property that sets top, right, bottom, and left in one declaration, following the same order syntax as margin/padding shorthands. `inset: 0` is a common way to make a positioned element fill its containing block completely.',null,'.overlay {
  position: absolute;
  inset: 0;
}','css','medium',ARRAY['css','layout','positioning']::text[],false,123),
('css','layout','css-layout-124','How would you build a classic ''holy grail'' layout (header, footer, and three columns) using CSS Grid?','Define a grid container with named template areas for header, nav, main, aside, and footer, sizing the middle row to 1fr so it fills remaining vertical space, and the side columns to fixed or auto widths. Assign each child to its area with grid-area, which produces a robust, source-order-independent layout in far less code than older float or table-based techniques.',null,'.layout {
  display: grid;
  grid-template-columns: 200px 1fr 200px;
  grid-template-rows: auto 1fr auto;
  grid-template-areas:
    "header header header"
    "nav main aside"
    "footer footer footer";
  min-height: 100vh;
}','css','hard',ARRAY['css','layout','grid']::text[],false,124),
('css','layout','css-layout-125','What causes an element with position: absolute to be positioned relative to the viewport instead of its intended parent?','If none of the element''s ancestors have a position value other than static, there''s no positioned containing block to anchor to, so the browser falls back to the initial containing block, which behaves like the viewport. The fix is to add position: relative (with no offsets) to the intended parent, establishing it as the containing block.',null,null,null,'medium',ARRAY['css','layout','positioning']::text[],false,125),
('css','layout','css-layout-126','What is the difference between the CSS multi-column layout (columns) and CSS Grid?','Multi-column layout (column-count, column-width) flows a single block of content automatically across a fixed number of newspaper-style columns, breaking content wherever it naturally overflows — it''s designed for text reflow, not precise item placement. Grid explicitly places distinct items into a defined row/column structure, giving full control over where each element sits.',null,'.article {
  column-count: 3;
  column-gap: 2rem;
}','css','hard',ARRAY['css','layout']::text[],false,126),
('css','layout','css-layout-127','How do min-content, max-content, and fit-content behave as sizing keywords?','min-content sizes an element to the smallest width it can be without overflowing its content (e.g. the widest unbreakable word). max-content sizes it to fit all content on one line without wrapping, however wide that requires. fit-content behaves like max-content but caps out at the available space, wrapping content once it would exceed the container.',null,'.box {
  width: fit-content;
}','css','hard',ARRAY['css','layout','sizing']::text[],false,127),
('css','layout','css-layout-128','Why might flex items not shrink below their content size even with flex-shrink set?','Flex items have an implicit min-width (or min-height for column direction) of auto by default, which prevents them from shrinking smaller than their content''s minimum size, overriding flex-shrink. Setting min-width: 0 (or min-height: 0) on the item removes this constraint, which is the classic fix for text or images overflowing a flex child.',null,'.item {
  flex: 1;
  min-width: 0;
}','css','hard',ARRAY['css','layout','flexbox']::text[],false,128),
('css','layout','css-layout-129','What is the difference between grid-row/grid-column line-based placement and named lines or areas?','Line-based placement uses numeric grid line indexes, e.g. grid-column: 2 / 4, counting from the edges of the explicit grid. Named lines let you assign custom names to specific lines in grid-template-columns/rows and reference them by name instead of number, which can be more readable. Named areas (grid-template-areas plus grid-area) go a step further, letting you place items by a descriptive region name rather than any line math.',null,null,null,'hard',ARRAY['css','layout','grid']::text[],false,129),
('css','layout','css-layout-130','How does the CSS aspect-ratio property simplify layout that previously needed the ''padding-top trick''?','Before aspect-ratio existed, maintaining a fixed width-to-height ratio for elements like video embeds required a padding-top percentage hack on a wrapper combined with absolute positioning of the inner content. aspect-ratio: width / height lets you declare the ratio directly on the element itself, and the browser computes the matching dimension automatically.',null,'.video-wrapper {
  aspect-ratio: 16 / 9;
}','css','medium',ARRAY['css','layout','sizing']::text[],false,130),
('css','layout','css-layout-131','What does the CSS place-items shorthand do?','place-items is a shorthand for align-items and justify-items in a single declaration, most commonly used with Grid. `place-items: center` centers items along both axes simultaneously, making it a quick one-liner for centering content.',null,'.grid {
  display: grid;
  place-items: center;
}','css','easy',ARRAY['css','layout','grid']::text[],false,131),
('css','responsive-design','css-responsive-design-101','What is a media query and how is it used for responsive design?','A media query applies a block of CSS only when a specified condition about the device or viewport is true, such as a minimum or maximum width. They let a single stylesheet adapt layout, typography, or visibility based on screen size, orientation, or other device characteristics.',null,'@media (max-width: 768px) {
  .sidebar { display: none; }
}','css','easy',ARRAY['css','responsive-design','media-queries']::text[],true,101),
('css','responsive-design','css-responsive-design-102','What does ''mobile-first'' responsive design mean, and why is it generally preferred?','Mobile-first means writing base styles for the smallest screen first, then progressively enhancing the layout for larger screens using min-width media queries. It''s preferred because it forces you to prioritize essential content and performance for constrained devices, and generally results in less CSS overriding itself compared to a desktop-first approach using max-width.',null,'/* base: mobile */
.card { flex-direction: column; }

@media (min-width: 768px) {
  .card { flex-direction: row; }
}','css','medium',ARRAY['css','responsive-design','mobile-first']::text[],true,102),
('css','responsive-design','css-responsive-design-103','What is the difference between the viewport units vw, vh, and vmin/vmax?','vw and vh are 1% of the viewport''s width and height respectively. vmin is 1% of whichever of the two (width or height) is smaller, and vmax is 1% of whichever is larger — vmin/vmax are useful for sizing elements consistently regardless of device orientation.',null,'.hero {
  height: 100vh;
  font-size: 5vmin;
}','css','medium',ARRAY['css','responsive-design','units']::text[],false,103),
('css','responsive-design','css-responsive-design-104','What does the viewport meta tag do, and why is it necessary for responsive design?','The <meta name="viewport" content="width=device-width, initial-scale=1"> tag tells mobile browsers to render the page at the device''s actual CSS pixel width instead of a default desktop-width virtual viewport (typically 980px) that gets zoomed out. Without it, media queries based on device width won''t behave as expected because the browser assumes a much wider layout viewport.',null,'<meta name="viewport" content="width=device-width, initial-scale=1">','html','medium',ARRAY['css','responsive-design']::text[],true,104),
('css','responsive-design','css-responsive-design-105','What is the difference between fluid (percentage-based) and fixed layouts?','A fixed layout uses absolute units like px for widths, so the layout stays the same size regardless of viewport, often causing overflow or wasted space on different screens. A fluid layout uses relative units like %, which resize proportionally with the viewport or parent container, adapting more gracefully across screen sizes.',null,null,null,'easy',ARRAY['css','responsive-design','units']::text[],false,105),
('css','responsive-design','css-responsive-design-106','How do you make an image responsive so it scales with its container?','Setting max-width: 100% and height: auto on an image prevents it from overflowing its container while preserving its aspect ratio, scaling down proportionally on smaller screens. Using width: 100% alone can cause upscaling and distortion beyond the image''s natural size unless additionally constrained.',null,'img {
  max-width: 100%;
  height: auto;
}','css','easy',ARRAY['css','responsive-design','images']::text[],true,106),
('css','responsive-design','css-responsive-design-107','What is the srcset attribute and how does it relate to responsive images in CSS/HTML?','srcset lets an <img> element offer multiple image sources at different resolutions, letting the browser choose the most appropriate one based on the device''s pixel density and viewport size, paired with the sizes attribute to describe expected rendered width. This improves performance by avoiding downloading unnecessarily large images on smaller or lower-density screens, complementing CSS-only responsive scaling.',null,'<img srcset="small.jpg 480w, large.jpg 1080w" sizes="(max-width: 600px) 480px, 1080px" src="large.jpg">','html','hard',ARRAY['css','responsive-design','images']::text[],false,107),
('css','responsive-design','css-responsive-design-108','What are CSS breakpoints, and how do you decide where to place them?','Breakpoints are the viewport widths at which a layout''s media queries change the design to better fit the available space. Rather than targeting specific devices (which constantly change), best practice is to let the content itself dictate breakpoints — add one wherever the design starts to look cramped or awkward as the viewport is resized, rather than hardcoding common device widths.',null,null,null,'medium',ARRAY['css','responsive-design','media-queries']::text[],false,108),
('css','responsive-design','css-responsive-design-109','What is the difference between min-width and max-width media queries, and how do they interact with mobile-first vs. desktop-first strategies?','A min-width media query applies its styles when the viewport is at least that wide, which is the basis of mobile-first design (progressively adding styles as screens grow). A max-width media query applies when the viewport is at most that wide, which is typical of desktop-first design (overriding base desktop styles as screens shrink). Mixing both approaches inconsistently in one project often leads to specificity conflicts and hard-to-follow overrides.',null,null,null,'medium',ARRAY['css','responsive-design','media-queries']::text[],false,109),
('css','responsive-design','css-responsive-design-110','How can you use CSS Grid or Flexbox alone to build a responsive layout without any media queries?','Using flexible sizing functions like repeat(auto-fit, minmax(min, max)) in Grid, or flex-wrap combined with flex-basis/flex-grow in Flexbox, lets the browser automatically adjust the number of columns or wrap items based on available space. This intrinsic responsiveness avoids hardcoded breakpoints entirely for many card-grid or list layouts.',null,'.cards {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
  gap: 1rem;
}','css','hard',ARRAY['css','responsive-design','grid']::text[],false,110),
('css','responsive-design','css-responsive-design-111','What is a CSS length unit''s difference between vw and %  when sizing an element''s width?','vw is always relative to the full viewport width, regardless of the element''s parent, so an element sized in vw ignores any padding or width constraints on its ancestors. % is relative to the containing block''s own computed width, so a percentage-width element scales with its immediate parent, which can differ significantly from the full viewport.',null,null,null,'medium',ARRAY['css','responsive-design','units']::text[],false,111),
('css','responsive-design','css-responsive-design-112','How do you handle typography that scales smoothly across screen sizes without relying purely on multiple media query breakpoints?','clamp(min, preferred, max) lets font-size scale fluidly with the viewport (often using vw in the preferred value) while never going below a minimum or above a maximum readable size. This produces smooth, continuous scaling instead of the abrupt jumps caused by discrete breakpoint-based font sizes.',null,'h1 {
  font-size: clamp(1.5rem, 4vw, 3rem);
}','css','medium',ARRAY['css','responsive-design','typography']::text[],true,112),
('css','responsive-design','css-responsive-design-113','What is the difference between the CSS orientation media feature and width/height-based media queries?','orientation: portrait or orientation: landscape targets whether the viewport''s height is greater than its width (portrait) or vice versa (landscape), which is useful for adapting layouts on rotatable devices like tablets. Width/height media queries target specific pixel dimensions directly and don''t account for the relationship between the two, so a device could be ''landscape'' at very different absolute widths.',null,'@media (orientation: landscape) {
  .gallery { grid-template-columns: repeat(4, 1fr); }
}','css','medium',ARRAY['css','responsive-design','media-queries']::text[],false,113),
('css','responsive-design','css-responsive-design-114','What are container queries and how do they differ from media queries?','Media queries respond to the viewport''s dimensions, so a component''s styles change based on the whole page''s size, regardless of how much space the component itself actually has. Container queries (@container) respond to the size of a nearby ancestor that''s been declared a containment context, letting a component adapt based on its own available space — essential for truly reusable components that might appear in a narrow sidebar or a wide main area.',null,'.card-container {
  container-type: inline-size;
}

@container (min-width: 400px) {
  .card { flex-direction: row; }
}','css','hard',ARRAY['css','responsive-design','container-queries']::text[],false,114),
('css','responsive-design','css-responsive-design-115','What is the difference between resolution-based media queries (like min-resolution) and viewport-size media queries?','Viewport-size media queries (min-width, max-width) target the CSS pixel dimensions of the browser viewport. Resolution media queries (min-resolution, targeting dppx or dpi) target the device''s pixel density, letting you serve higher-resolution background images or assets to high-DPI (Retina-style) screens regardless of the viewport''s logical size.',null,'@media (min-resolution: 2dppx) {
  .logo { background-image: url(''logo@2x.png''); }
}','css','hard',ARRAY['css','responsive-design','media-queries']::text[],false,115),
('css','responsive-design','css-responsive-design-116','How do you hide an element visually for sighted users while keeping it accessible to screen readers in a responsive layout?','display: none or visibility: hidden remove content from both the visual rendering and the accessibility tree, hiding it from screen readers too. To hide only visually, use a ''visually-hidden'' utility class that clips the element to a 1px box with overflow hidden and absolute positioning, instead of collapsing it, which keeps it available to assistive technology while removing it from sighted view.',null,'.visually-hidden {
  position: absolute;
  width: 1px;
  height: 1px;
  overflow: hidden;
  clip: rect(0, 0, 0, 0);
  white-space: nowrap;
}','css','hard',ARRAY['css','responsive-design','accessibility']::text[],false,116),
('css','responsive-design','css-responsive-design-117','What is the difference between adaptive design and responsive design?','Responsive design uses fluid grids and media queries so the layout continuously reflows to fit any viewport size. Adaptive design instead detects the device or viewport and serves one of several fixed, discrete layouts tailored to specific breakpoints, without the continuous fluidity in between.',null,null,null,'medium',ARRAY['css','responsive-design']::text[],false,117),
('css','responsive-design','css-responsive-design-118','How can the CSS min(), max(), and clamp() functions help avoid excessive media queries in responsive layouts?','min(), max(), and clamp() let a single declaration adapt to context without a breakpoint: min() picks the smaller of two values (useful for capping width), max() picks the larger (useful for a minimum floor), and clamp() combines both to keep a value within a range while scaling fluidly in between — reducing the number of explicit @media rules needed for fluid sizing.',null,'.container {
  width: min(90%, 1200px);
}','css','medium',ARRAY['css','responsive-design','functions']::text[],false,118),
('css','responsive-design','css-responsive-design-119','What does the CSS prefers-color-scheme media feature detect, and how is it used?','prefers-color-scheme detects whether the user''s operating system or browser is set to a light or dark color theme, letting the page automatically apply matching styles without a manual toggle. It''s commonly combined with CSS custom properties to swap an entire color palette in one media query block.',null,'@media (prefers-color-scheme: dark) {
  :root { --bg: #111; --text: #eee; }
}','css','medium',ARRAY['css','responsive-design','media-queries']::text[],false,119),
('css','responsive-design','css-responsive-design-120','What does the prefers-reduced-motion media feature do and why does it matter for responsive/accessible design?','prefers-reduced-motion detects a user''s OS-level preference to minimize non-essential animations, often set by people with vestibular disorders who can experience discomfort from motion. Wrapping animations and transitions in this media query lets you provide a reduced or instant alternative instead of forcing motion on everyone.',null,'@media (prefers-reduced-motion: reduce) {
  * { animation: none !important; transition: none !important; }
}','css','medium',ARRAY['css','responsive-design','accessibility']::text[],false,120),
('css','responsive-design','css-responsive-design-121','What is the difference between the CSS ch unit and the em unit?','The ch unit is defined as the width of the ''0'' (zero) character in the element''s font, making it especially useful for sizing text containers to a specific number of characters wide, like limiting a paragraph to 65ch for readability. em is based on the font-size itself and doesn''t correspond directly to character width, which varies by font.',null,'p {
  max-width: 65ch;
}','css','medium',ARRAY['css','responsive-design','units']::text[],false,121),
('css','responsive-design','css-responsive-design-122','How would you build a responsive navigation bar that collapses into a hamburger menu on small screens using only CSS and a checkbox/label pattern?','A hidden checkbox input paired with a <label> styled as a hamburger icon lets you toggle visibility of a navigation menu purely with CSS, using the :checked pseudo-class and a sibling combinator to show/hide the menu, all wrapped in a media query so the toggle only applies below a certain breakpoint. This avoids JavaScript for the simplest cases, though a JS-based approach is usually preferred for better accessibility (e.g. proper aria-expanded state).',null,'@media (max-width: 600px) {
  .nav-links { display: none; }
  #menu-toggle:checked ~ .nav-links { display: flex; }
}','css','hard',ARRAY['css','responsive-design']::text[],false,122),
('css','responsive-design','css-responsive-design-123','What does the CSS object-fit property do, and how does it help with responsive images?','object-fit controls how a replaced element''s content (like an img or video) is resized to fit its box when the box''s aspect ratio doesn''t match the content''s natural ratio. cover scales to fill the box while cropping overflow, contain scales to fit entirely within the box while preserving aspect ratio (possibly leaving empty space), and fill stretches to match the box exactly, distorting the content if needed.',null,'img {
  width: 100%;
  height: 300px;
  object-fit: cover;
}','css','medium',ARRAY['css','responsive-design','images']::text[],false,123),
('css','responsive-design','css-responsive-design-124','What are the dvh, svh, and lvh viewport units, and what problem do they solve on mobile browsers?','Traditional vh is based on the largest possible viewport, which causes layout jumps on mobile browsers when the address bar or toolbar shows/hides and changes the actual visible area. lvh (large viewport height) matches the old vh behavior, svh (small viewport height) accounts for UI chrome always being visible, and dvh (dynamic viewport height) updates live as the visible viewport changes, giving more accurate full-height sizing on mobile.',null,'.full-height {
  height: 100dvh;
}','css','hard',ARRAY['css','responsive-design','units']::text[],false,124),
('css','responsive-design','css-responsive-design-125','Why is it generally discouraged to disable pinch-to-zoom with the viewport meta tag?','Setting user-scalable=no or a fixed maximum-scale in the viewport meta tag prevents users from zooming in, which is a significant accessibility barrier for people with low vision who rely on pinch-zoom to read content. It''s considered a WCAG failure and modern browsers increasingly ignore these restrictions for that reason.',null,null,null,'hard',ARRAY['css','responsive-design','accessibility']::text[],false,125),
('css','responsive-design','css-responsive-design-126','How do relative font sizing strategies (setting html { font-size: 62.5% }) simplify working with rem units?','Since browsers default to a 16px root font-size, setting html { font-size: 62.5% } makes the root equal to 10px, so any rem value maps directly to a simple base-10 pixel equivalent (e.g. 1.6rem = 16px), which is easier to reason about than multiplying by 16. This must be done carefully, since it changes the effective size of any unstyled default rem-based measurements too, and it''s increasingly considered unnecessary with modern calc/clamp workflows.',null,'html {
  font-size: 62.5%; /* 1rem = 10px */
}','css','medium',ARRAY['css','responsive-design','units']::text[],false,126),
('css','responsive-design','css-responsive-design-127','What is the difference between using CSS Grid''s auto-fit and manually specifying different grid-template-columns per breakpoint?','auto-fit with minmax() creates an intrinsically responsive grid that recalculates column count continuously as the container resizes, without any explicit breakpoints, adapting to any width. Manually specifying grid-template-columns inside separate media queries gives precise, designed control over exactly how many columns appear at specific breakpoints, which is useful when the design calls for exact column counts rather than whatever fits.',null,null,null,'hard',ARRAY['css','responsive-design','grid']::text[],false,127),
('css','responsive-design','css-responsive-design-128','What is the difference between print stylesheets and screen media queries?','A print stylesheet, targeted with @media print, applies styles specifically when a page is printed or exported to PDF — commonly used to hide navigation/ads, force black text, and add page-break rules. Screen media queries (@media screen) target on-screen rendering, and most responsive design work targets screen by default since @media without a type applies to all media types including print.',null,'@media print {
  nav, footer { display: none; }
}','css','medium',ARRAY['css','responsive-design','media-queries']::text[],false,128),
('css','responsive-design','css-responsive-design-129','How does the CSS gap property behave responsively compared to fixed-pixel margins between grid or flex items?','Using a relative unit or clamp() with gap lets spacing between items scale naturally with the viewport or container without needing separate margin overrides per breakpoint. Fixed-pixel margins on individual items require careful negative-margin or :not(:last-child) handling to avoid uneven edges, and don''t automatically scale across screen sizes the way a responsive gap value can.',null,'.grid {
  display: grid;
  gap: clamp(0.5rem, 2vw, 2rem);
}','css','medium',ARRAY['css','responsive-design','grid']::text[],false,129),
('css','responsive-design','css-responsive-design-130','What is the difference between ''fixed breakpoints'' and ''content-based breakpoints'' in responsive design strategy?','Fixed breakpoints are predetermined pixel widths (like 576px, 768px, 992px, often from a framework like Bootstrap) applied regardless of the specific component''s content. Content-based breakpoints are chosen by resizing the actual component and noting exactly where its content starts to look broken or cramped, which produces more tailored results but requires more manual testing per component.',null,null,null,'medium',ARRAY['css','responsive-design','media-queries']::text[],false,130),
('css','variables-functions','css-variables-functions-101','What are CSS custom properties (CSS variables) and how do you declare and use one?','Custom properties are author-defined variables, written with a double-hyphen prefix (--name), that store a reusable value and are read with the var() function. Unlike preprocessor variables, they''re live in the DOM, participate in the cascade and inheritance, and can be read or changed at runtime with JavaScript or media queries.',null,':root {
  --primary-color: #3366ff;
}

.button {
  background: var(--primary-color);
}','css','easy',ARRAY['css','variables-functions','custom-properties']::text[],true,101),
('css','variables-functions','css-variables-functions-102','Why are CSS custom properties typically declared on the :root selector?',':root targets the document''s root element (equivalent to html but with higher specificity), which is the highest point in the DOM tree, so any custom property declared there is inherited by every element on the page unless explicitly overridden further down. This makes it the conventional place for global design tokens like colors, spacing, and fonts.',null,null,null,'easy',ARRAY['css','variables-functions','custom-properties']::text[],false,102),
('css','variables-functions','css-variables-functions-103','How does the var() function''s second argument (fallback value) work?','var(--name, fallback) supplies a fallback value to use if the referenced custom property is not defined or is invalid, similar to a default parameter. The fallback can itself be another var() call, another custom property, or any valid CSS value, and only applies when the variable is genuinely missing, not when it''s an empty string.',null,'color: var(--text-color, #333);','css','easy',ARRAY['css','variables-functions','custom-properties']::text[],true,103),
('css','variables-functions','css-variables-functions-104','What is the calc() function used for in CSS?','calc() performs mathematical calculations (addition, subtraction, multiplication, division) directly in a CSS value, and crucially allows mixing different units in the same expression, such as combining a percentage and a fixed pixel value — something that''s otherwise impossible without calc().',null,'.sidebar {
  width: calc(100% - 250px);
}','css','easy',ARRAY['css','variables-functions','calc']::text[],true,104),
('css','variables-functions','css-variables-functions-105','Why must there be whitespace around the + and - operators inside a calc() expression, but not around * and /?','The CSS calc() grammar requires whitespace around + and - because without it, a value like calc(100%-20px) would be ambiguous with a negative unit token, so the parser can''t reliably distinguish an operator from a sign. * and / don''t have this ambiguity since a value can''t begin with those characters, so whitespace around them is optional (though good practice for readability).',null,'width: calc(100% - 20px);  /* correct */
width: calc(100%-20px);   /* invalid */','css','medium',ARRAY['css','variables-functions','calc']::text[],false,105),
('css','variables-functions','css-variables-functions-106','What do the min() and max() CSS functions do?','min() evaluates a comma-separated list of values and uses the smallest one, commonly used to cap a size (e.g. never exceed a max width). max() uses the largest value, commonly used to enforce a minimum floor. Both can mix units and include calc()-like expressions directly inside them.',null,'.box {
  width: min(90vw, 600px);
  font-size: max(1rem, 2vw);
}','css','medium',ARRAY['css','variables-functions','functions']::text[],true,106),
('css','variables-functions','css-variables-functions-107','How does the clamp() function work, and what are its three arguments?','clamp(min, preferred, max) returns the preferred value, but constrains it to never go below the min or above the max — effectively a shorthand for max(min, min(preferred, max)). It''s widely used for fluid typography and spacing that scales smoothly with the viewport while staying within safe bounds.',null,'font-size: clamp(1rem, 2vw + 0.5rem, 2rem);','css','medium',ARRAY['css','variables-functions','clamp']::text[],true,107),
('css','variables-functions','css-variables-functions-108','What is the difference between CSS custom properties and Sass/LESS preprocessor variables in terms of runtime behavior?','Preprocessor variables are resolved once at compile time into static values baked into the final CSS output, so they can''t be inspected or changed after the build. CSS custom properties persist in the browser at runtime, cascade and inherit like normal properties, can differ per element or media query, and can be read and mutated live via JavaScript''s element.style.setProperty().',null,'el.style.setProperty(''--primary-color'', ''red'');','javascript','medium',ARRAY['css','variables-functions','custom-properties']::text[],false,108),
('css','variables-functions','css-variables-functions-109','How can you override a CSS custom property for just a specific component or scope?','Because custom properties follow the normal cascade and inheritance rules, redeclaring the same --name on a more specific selector (like a component class) overrides the value for that element and its descendants, without affecting the rest of the page that still inherits the :root value.',null,':root { --spacing: 1rem; }
.compact { --spacing: 0.5rem; }','css','medium',ARRAY['css','variables-functions','custom-properties']::text[],false,109),
('css','variables-functions','css-variables-functions-110','How would you implement a light/dark theme toggle using CSS custom properties?','Define semantic custom properties (like --bg-color, --text-color) at :root for the default theme, then redefine those same property names inside a `[data-theme="dark"]` selector (or a prefers-color-scheme media query). Toggling a data attribute or class on the root element switches every property that references those variables instantly, without needing separate stylesheets.',null,':root { --bg: white; --text: black; }
[data-theme="dark"] { --bg: #111; --text: #eee; }
body { background: var(--bg); color: var(--text); }','css','medium',ARRAY['css','variables-functions','custom-properties']::text[],true,110),
('css','variables-functions','css-variables-functions-111','What happens if a CSS custom property is referenced but was never defined, and there''s no fallback in var()?','If a custom property is undefined and var() has no fallback, the property using it is treated as having its ''guaranteed-invalid value'' — for most properties this behaves like the unset keyword, either falling back to the inherited value or the initial value, effectively making the declaration act as if it weren''t there at all.',null,'color: var(--undefined-color); /* falls back to inherited/initial */','css','hard',ARRAY['css','variables-functions','custom-properties']::text[],false,111),
('css','variables-functions','css-variables-functions-112','Can CSS custom properties be used inside media queries or other at-rules directly?','Custom properties cannot be directly substituted into a media query''s condition (e.g. `@media (min-width: var(--breakpoint))` is invalid) because media query conditions are evaluated before the cascade resolves custom property values. Workarounds include container queries with units defined differently, or using JavaScript to read/set styles based on matchMedia, since the native CSS environment/custom property system doesn''t yet fully bridge this gap for most at-rules.',null,null,null,'hard',ARRAY['css','variables-functions','custom-properties']::text[],false,112),
('css','variables-functions','css-variables-functions-113','How do you combine calc() with a CSS custom property?','A custom property can be referenced with var() inside a calc() expression just like any other value, letting you build derived values (like spacing scales) from a single base variable, keeping the relationship between values centralized and easy to adjust.',null,':root { --base: 8px; }
.card { padding: calc(var(--base) * 2); }','css','easy',ARRAY['css','variables-functions','calc']::text[],false,113),
('css','variables-functions','css-variables-functions-114','What is the @property at-rule and what advantages does it give over a plain custom property?','@property lets you formally register a custom property with a specific syntax type (like <color> or <length>), an initial-value, and whether it inherits. This enables the browser to properly type-check the value, provide a real default (unlike the guaranteed-invalid fallback of untyped variables), and — critically — allows the property to be smoothly animated/transitioned, which untyped string-based custom properties cannot be.',null,'@property --progress {
  syntax: ''<number>'';
  initial-value: 0;
  inherits: false;
}','css','hard',ARRAY['css','variables-functions','custom-properties']::text[],false,114),
('css','variables-functions','css-variables-functions-115','How can nested calc() expressions or var() calls be used to build a spacing/typography scale system?','By defining a single base unit as a custom property and deriving all other spacing/type sizes from it using calc() with multipliers (e.g. --space-sm: calc(var(--space-base) * 0.5)), you create a consistent, easily-tunable design scale where changing one root value cascades proportionally through every derived value.',null,':root {
  --space-base: 1rem;
  --space-sm: calc(var(--space-base) * 0.5);
  --space-lg: calc(var(--space-base) * 2);
}','css','medium',ARRAY['css','variables-functions','calc']::text[],false,115),
('css','variables-functions','css-variables-functions-116','What does the CSS env() function do, and what is a common use case?','env() accesses user-agent-defined environment variables that aren''t under author control, most commonly the safe-area-inset values on devices with notches or rounded corners (like iPhones), letting you pad content away from those obstructed areas. It behaves syntactically like var() and also supports a fallback value as a second argument.',null,'.footer {
  padding-bottom: env(safe-area-inset-bottom, 0px);
}','css','hard',ARRAY['css','variables-functions','functions']::text[],false,116),
('css','variables-functions','css-variables-functions-117','How do the attr() and counter() functions work in CSS?','attr() retrieves the value of an HTML attribute on the current element and inserts it as a string, most commonly used with the content property on ::before/::after (e.g. showing a data-tooltip attribute''s text). counter() (with counter-reset and counter-increment) generates automatic sequential numbering, such as custom numbered headings or ordered list styling, without hardcoding numbers in the markup.',null,'a::after {
  content: " (" attr(href) ")";
}','css','medium',ARRAY['css','variables-functions','functions']::text[],false,117),
('css','variables-functions','css-variables-functions-118','What is the difference between the CSS var() function and the SCSS/Sass interpolation syntax #{$variable}?','SCSS''s #{$variable} interpolation is a compile-time text substitution that happens before any CSS is generated, so it can be used anywhere in a rule, including inside selector names or property names, but produces static output. var() is a runtime CSS feature restricted to value positions (not selectors or property names in standard CSS), but it stays live and reactive to cascade, inheritance, and JavaScript changes after the page loads.',null,null,null,'hard',ARRAY['css','variables-functions','custom-properties']::text[],false,118),
('css','variables-functions','css-variables-functions-119','How can you use CSS custom properties to pass dynamic values from JavaScript into CSS, such as for a progress bar or drag position?','JavaScript can call element.style.setProperty(''--progress'', ''75%'') to update a custom property directly on an element''s inline style, and any CSS referencing var(--progress) will re-render immediately. This is an efficient bridge for frequently-changing values (like animation progress or mouse position) without manipulating full inline styles or triggering full class toggles.',null,'el.style.setProperty(''--x'', `${mouseX}px`);
/* CSS: */
.el { transform: translateX(var(--x)); }','javascript','medium',ARRAY['css','variables-functions','custom-properties']::text[],false,119),
('css','variables-functions','css-variables-functions-120','What is the difference between clamp() and using multiple media queries to achieve responsive sizing?','clamp() produces continuous, fluid scaling between its minimum and maximum bounds as the viewport changes, with no discrete jump points. Media queries produce step-function sizing — the value stays fixed within a breakpoint range and jumps abruptly at each threshold — which requires more code to add finer-grained steps and can look less smooth during resizing.',null,null,null,'medium',ARRAY['css','variables-functions','clamp']::text[],false,120),
('css','variables-functions','css-variables-functions-121','How do you use calc() to create a full-viewport-height section that accounts for a fixed-height header?','By subtracting the known fixed header height from 100vh inside calc(), the section fills exactly the remaining vertical space below the header, rather than overflowing the page by the header''s height as a plain 100vh would.',null,'.hero {
  height: calc(100vh - 64px);
}','css','easy',ARRAY['css','variables-functions','calc']::text[],false,121),
('css','variables-functions','css-variables-functions-122','What are CSS color functions like rgb(), hsl(), and the newer oklch(), and why might you choose one over another?','rgb() defines color via red/green/blue channels, and hsl() via hue/saturation/lightness, which is more intuitive for creating tints, shades, and consistent color relationships by hand. oklch() is a newer, perceptually uniform color space where equal numeric changes in lightness or chroma look equally different to the human eye, making it better suited for generating accessible, evenly-spaced color palettes programmatically.',null,'color: hsl(220 90% 56%);
color: oklch(65% 0.2 250);','css','medium',ARRAY['css','variables-functions','color']::text[],false,122),
('css','variables-functions','css-variables-functions-123','How does modern CSS support relative color syntax, and what problem does it solve?','Relative color syntax (e.g. `hsl(from var(--base) h s calc(l - 20%))`) lets you derive a new color from an existing one by referencing and modifying its individual channel values directly in CSS. It solves the problem of needing separate hardcoded custom properties (or a preprocessor/JavaScript) for every color variant like a hover-darkened or disabled-lightened shade of a base color.',null,'.btn:hover {
  background: hsl(from var(--btn-color) h s calc(l - 10%));
}','css','hard',ARRAY['css','variables-functions','color']::text[],false,123),
('css','variables-functions','css-variables-functions-124','What does the CSS var() function''s inheritance behavior mean for a custom property set inside a media query?','A custom property redeclared inside a media query only takes effect while that media query''s condition matches; when it stops matching, elements fall back to whatever value they''d otherwise inherit or the initial value if nothing else defines it. This makes custom properties a clean way to swap entire sets of values (spacing, colors, font sizes) responsively without duplicating whole rule blocks per breakpoint.',null,'@media (min-width: 768px) {
  :root { --gutter: 2rem; }
}','css','medium',ARRAY['css','variables-functions','custom-properties']::text[],false,124),
('css','variables-functions','css-variables-functions-125','Can you use calc() to mix a CSS custom property with a unit that''s determined dynamically, and what''s a limitation to be aware of?','Yes — for example calc(var(--multiplier) * 1px) turns a unitless custom property into a usable length. A key limitation is that a custom property storing just a raw number (with no unit) can''t be used directly as a length without this kind of multiplication trick, since CSS won''t implicitly attach a unit to it, and mismatched or invalid combinations make the whole declaration invalid.',null,':root { --scale: 1.5; }
.icon { width: calc(var(--scale) * 24px); }','css','medium',ARRAY['css','variables-functions','calc']::text[],false,125),
('css','variables-functions','css-variables-functions-126','What is the difference between a shorthand property''s initial value reset and a custom property''s inheritance when using a CSS reset?','A standard property reset with `all: unset` or a shorthand resets every sub-property to its initial value, which does not preserve custom properties. Custom properties are unaffected by `all: initial/unset` unless you also explicitly reset with `all: revert` in certain browsers, or you target them individually — this distinction matters when building isolated components that shouldn''t leak inherited design tokens.',null,null,null,'hard',ARRAY['css','variables-functions','custom-properties']::text[],false,126),
('css','variables-functions','css-variables-functions-127','How would you build a reusable button component''s sizing variants (small, medium, large) using CSS custom properties?','Define a base set of custom properties on the button class for padding and font-size, then create modifier classes (like .btn--sm) that only override those specific custom properties rather than repeating every declaration, letting the base rule''s calc()-derived or direct values automatically adapt.',null,'.btn {
  padding: var(--btn-padding, 0.5rem 1rem);
  font-size: var(--btn-font-size, 1rem);
}
.btn--lg {
  --btn-padding: 0.75rem 1.5rem;
  --btn-font-size: 1.25rem;
}','css','medium',ARRAY['css','variables-functions','custom-properties']::text[],false,127),
('css','variables-functions','css-variables-functions-128','What is the difference between the CSS round() function and using calc() with modulo-like logic?','round() (part of newer CSS Values Level 4 math functions) explicitly rounds a value to the nearest multiple of a step, with a rounding strategy argument (nearest, up, down, to-zero), returning a clean snapped value in one function call. Before round() existed, similar snapping required verbose calc() workarounds since CSS has no built-in modulo operator, making round() a meaningful simplification for things like snapping grid tracks to whole pixels.',null,'width: round(nearest, 47px, 10px); /* 50px */','css','hard',ARRAY['css','variables-functions','functions']::text[],false,128),
('css','variables-functions','css-variables-functions-129','Why can''t CSS custom properties be used to conditionally toggle an entire block of unrelated declarations the way a Sass @mixin or @if can?','Custom properties only ever resolve to a single value substituted into a property''s value position — they can''t expand into multiple property declarations, selectors, or conditional logic blocks the way a preprocessor mixin can. CSS does have limited conditional logic through things like the toggle() proposal or @container/@supports, but true multi-declaration branching still generally requires a preprocessor or JavaScript-driven class toggling.',null,null,null,'hard',ARRAY['css','variables-functions','custom-properties']::text[],false,129),
('css','variables-functions','css-variables-functions-130','What is the difference between the CSS repeat() function used in grid-template-columns and a custom property storing a track count?','repeat(count, size) is a Grid-specific shorthand that expands into a repeated sequence of tracks in place, evaluated purely at layout time. Storing a column count in a custom property (e.g. --cols: 4) and referencing it inside repeat(var(--cols), 1fr) lets JavaScript or a media query change the effective column count dynamically by updating just that one variable, without rewriting the whole grid-template-columns declaration.',null,':root { --cols: 3; }
.grid { grid-template-columns: repeat(var(--cols), 1fr); }','css','medium',ARRAY['css','variables-functions','grid']::text[],false,130),
('tailwind','basics','tailwind-basics-101','What is Tailwind CSS and how does it differ from traditional CSS frameworks?','Tailwind CSS is a utility-first CSS framework that provides low-level, single-purpose classes like `flex`, `pt-4`, and `text-center` instead of pre-built components like `.card` or `.btn-primary`. Instead of writing custom CSS or overriding a framework''s opinionated components, you compose designs directly in your markup by combining utility classes, which keeps styling colocated with the HTML/JSX it affects.',null,'<button class="bg-blue-600 text-white font-semibold px-4 py-2 rounded hover:bg-blue-700">
  Save
</button>','html','easy',ARRAY['tailwind','basics','utility-first']::text[],true,101),
('tailwind','basics','tailwind-basics-102','What does "utility-first" mean, and how is it different from a component-based framework like Bootstrap?','Utility-first means you build UI by composing many small, single-purpose classes directly in markup rather than relying on predefined, multi-property components. Bootstrap gives you semantic components like `.card` or `.navbar` that come with baked-in visual opinions you often have to override; Tailwind gives you unopinionated primitives so you build your own components without fighting existing styles.',null,null,null,'easy',ARRAY['tailwind','basics','utility-first']::text[],false,102),
('tailwind','basics','tailwind-basics-103','How does Tailwind''s utility class naming convention generally work?','Most utility names follow a `property-value` pattern, such as `m-4` for margin, `text-lg` for font size, or `bg-red-500` for background color, where the value maps to an entry in the theme (a spacing scale, color palette, or font-size scale). Related properties share prefixes (`p-`, `px-`, `py-`, `pt-`, etc. for padding), which makes the class list predictable once you learn the pattern instead of memorizing arbitrary names.',null,null,null,'easy',ARRAY['tailwind','basics','naming-convention']::text[],false,103),
('tailwind','basics','tailwind-basics-104','What are the tradeoffs of using Tailwind utility classes instead of writing custom CSS?','Utility classes speed up development since you don''t context-switch between HTML and a stylesheet, and they naturally prevent CSS files from growing unbounded because you reuse the same small set of classes everywhere. The tradeoff is more verbose markup and a learning curve around class names, though tools like Prettier''s Tailwind plugin and component extraction (React components, `@apply`) mitigate the readability concern.',null,null,null,'easy',ARRAY['tailwind','basics','tradeoffs']::text[],false,104),
('tailwind','basics','tailwind-basics-105','How do you set up Tailwind CSS in a project, at a high level?','You install `tailwindcss` (and `postcss`/`autoprefixer` for older setups) as a dev dependency, generate a config file, and add the `@tailwind base`, `@tailwind components`, and `@tailwind utilities` directives (or the newer single `@import "tailwindcss"` in v4) to your main CSS file. You then point Tailwind''s `content` option at your template/component files so it knows which files to scan for class names, and import that CSS file once in your app''s entry point.',null,'@tailwind base;
@tailwind components;
@tailwind utilities;','css','easy',ARRAY['tailwind','basics','setup']::text[],false,105),
('tailwind','basics','tailwind-basics-106','How does Tailwind''s default spacing scale map numbers to actual pixel/rem values?','Tailwind''s default spacing scale is based on a 0.25rem (4px) increment, so `p-1` is `0.25rem`, `p-4` is `1rem` (16px), and `p-8` is `2rem` (32px). Values are expressed in rem rather than px so spacing scales properly if a user changes their browser''s base font size for accessibility.',null,null,null,'medium',ARRAY['tailwind','basics','spacing']::text[],false,106),
('tailwind','basics','tailwind-basics-107','What is Preflight in Tailwind CSS?','Preflight is Tailwind''s built-in base-style reset, injected by the `@tailwind base` directive, that normalizes browser inconsistencies before utilities are applied — for example, it removes default margins on headings and paragraphs, sets `box-sizing: border-box` globally, and resets list styles and form element appearance. It''s based on modern-normalize but tuned specifically to make utility classes behave predictably across browsers.',null,null,null,'medium',ARRAY['tailwind','basics','preflight']::text[],false,107),
('tailwind','basics','tailwind-basics-108','Why does Tailwind remove default margins from headings, paragraphs, and lists via Preflight?','Browsers apply inconsistent default margins to elements like `h1` and `p`, which makes spacing unpredictable when you rely purely on utility classes for layout. Preflight zeroes these out so that all spacing is explicit and controlled through margin/padding/gap utilities instead of fighting invisible user-agent defaults.',null,null,null,'easy',ARRAY['tailwind','basics','preflight']::text[],false,108),
('tailwind','basics','tailwind-basics-109','How would you build a simple styled button using only Tailwind utility classes?','You stack utilities for each visual concern — background color, text color, padding, border radius, font weight, and interactive states — directly on the element''s class attribute. There''s no separate CSS rule to write; the combination of classes fully describes the button''s appearance and behavior.',null,'<button class="inline-flex items-center gap-2 rounded-md bg-emerald-600 px-4 py-2 text-sm font-medium text-white shadow hover:bg-emerald-700 disabled:opacity-50">
  Submit
</button>','html','easy',ARRAY['tailwind','basics','components']::text[],true,109),
('tailwind','basics','tailwind-basics-110','What''s the difference between using inline styles and Tailwind utility classes for styling?','Inline styles (`style="..."`) accept raw CSS values but can''t use pseudo-classes like `:hover`, media queries, or your design system''s constrained scale, and they have the highest CSS specificity, which makes overriding them hard. Tailwind utilities compile to real CSS classes backed by a constrained theme, so they support responsive prefixes, state variants, and are far easier to override or compose than inline styles.',null,null,null,'easy',ARRAY['tailwind','basics','css']::text[],false,110),
('tailwind','basics','tailwind-basics-111','How does Tailwind''s color naming convention work, e.g. `bg-blue-500`?','Colors follow a `{property}-{color}-{shade}` pattern, where the shade is a number from 50 (lightest) to 950 (darkest) in steps of 100, giving a consistent, predictable palette across every color. This numeric scale mirrors Material Design–style palettes and makes it easy to pick a slightly lighter or darker variant of the same hue without guessing hex values.',null,null,null,'easy',ARRAY['tailwind','basics','colors']::text[],false,111),
('tailwind','basics','tailwind-basics-112','Why doesn''t the order of utility classes in the `class` attribute affect the rendered result?','Tailwind classes are single-property, single-selector rules, so the order they appear in the `class` attribute in markup has no effect on specificity — specificity is instead determined by the order the underlying CSS rules are generated in the stylesheet, which Tailwind controls internally. The gotcha is when two utilities target the *same* CSS property (like `px-4 px-6`); whichever rule appears later in the compiled stylesheet wins, which may not match the order they''re written in the class list.',null,null,null,'hard',ARRAY['tailwind','basics','specificity']::text[],false,112),
('tailwind','basics','tailwind-basics-113','How do arbitrary values work in Tailwind, such as `w-[137px]`?','Square-bracket syntax lets you escape the constrained design scale and supply any raw CSS value directly in the class name, which Tailwind''s JIT engine picks up and generates a matching utility for on the fly. This is meant for one-off cases that don''t fit the theme scale, not as a replacement for the scale itself.',null,'<div class="w-[137px] top-[calc(100%-2rem)] bg-[#1da1f2]">
  Custom sizing
</div>','html','medium',ARRAY['tailwind','basics','arbitrary-values']::text[],false,113),
('tailwind','basics','tailwind-basics-114','What are Tailwind''s default responsive breakpoints?','By default Tailwind ships with `sm` (640px), `md` (768px), `lg` (1024px), `xl` (1280px), and `2xl` (1536px), each used as a min-width media query prefix like `md:flex`. These are fully customizable in `tailwind.config.js` under `theme.screens` if a project needs different breakpoints.',null,null,null,'medium',ARRAY['tailwind','basics','breakpoints']::text[],false,114),
('tailwind','basics','tailwind-basics-115','What do the `space-x` and `space-y` utilities do?','`space-x-{n}` and `space-y-{n}` add horizontal or vertical margin between adjacent sibling elements without needing gap on a flex/grid container or manual margins on each child, by applying the margin only to elements after the first via the `> * + *` selector pattern. They''re commonly used for simple stacked lists or inline groups before reaching for `gap` on a flex container.',null,'<div class="flex space-x-4">
  <span>One</span>
  <span>Two</span>
  <span>Three</span>
</div>','html','medium',ARRAY['tailwind','basics','spacing']::text[],false,115),
('tailwind','basics','tailwind-basics-116','What''s the difference between `p-4` and using `px-4 py-4` together?','`p-4` sets padding on all four sides to the same value in a single class, while `px-4 py-4` sets horizontal padding and vertical padding separately using two classes — for a uniform value on all sides they produce an identical result, but `px`/`py` (or the individual `pt`/`pr`/`pb`/`pl`) let you diverge horizontal and vertical spacing independently.',null,null,null,'easy',ARRAY['tailwind','basics','spacing']::text[],false,116),
('tailwind','basics','tailwind-basics-117','How do you apply negative margins in Tailwind, e.g. to pull an element up?','Negative spacing utilities are prefixed with a dash before the property, such as `-mt-4` for a negative top margin, rather than a dash before the number. This is commonly used to offset overlapping elements like a badge positioned over the corner of a card.',null,'<div class="relative">
  <img src="/avatar.png" class="rounded-full" />
  <span class="absolute -top-1 -right-1 h-3 w-3 rounded-full bg-red-500"></span>
</div>','html','medium',ARRAY['tailwind','basics','spacing']::text[],false,117),
('tailwind','basics','tailwind-basics-118','What do `divide-x` and `divide-y` do in Tailwind?','`divide-x-{n}` and `divide-y-{n}` add a border between adjacent children (again via the `> * + *` selector) instead of margin, which is useful for creating dividers in a list or table-like layout without adding an extra element for each separator. `divide-color` utilities like `divide-gray-200` control the border''s color.',null,null,null,'medium',ARRAY['tailwind','basics','borders']::text[],false,118),
('tailwind','basics','tailwind-basics-119','How does Tailwind help enforce visual consistency across a team compared to hand-written CSS?','Because utilities are generated from a shared, constrained theme (fixed spacing scale, color palette, font sizes), developers can''t accidentally introduce a one-off `margin: 13px` or a slightly-off shade of blue the way they could in free-form CSS. Design decisions are centralized in `tailwind.config.js`, so the whole team draws from the same finite set of values by default.',null,null,null,'medium',ARRAY['tailwind','basics','design-system']::text[],false,119),
('tailwind','basics','tailwind-basics-120','How does Tailwind compare to CSS-in-JS libraries like styled-components?','Styled-components generates scoped CSS at runtime (or build time with compilers) from template literals colocated with component logic, while Tailwind generates a static stylesheet ahead of time from utility classes referenced in markup, avoiding any runtime style-injection cost. Tailwind tends to produce a smaller, more cacheable CSS bundle since styles are deduplicated utility classes rather than per-component generated class names.',null,null,null,'medium',ARRAY['tailwind','basics','css-in-js']::text[],false,120),
('tailwind','basics','tailwind-basics-121','How do you conditionally apply Tailwind classes in a React component?','Since class names are just strings, you can build them conditionally with template literals, ternaries, or a helper library like `clsx`/`classnames`, which is preferred because it cleanly handles falsy values and object/array syntax. For merging conflicting Tailwind classes (e.g. two different `px-*` values from a prop and a default), `tailwind-merge` is commonly layered on top to keep only the last one.',null,'import clsx from ''clsx'';

function Button({ variant, className }) {
  return (
    <button
      className={clsx(
        ''rounded px-4 py-2 font-medium'',
        variant === ''primary'' ? ''bg-blue-600 text-white'' : ''bg-gray-200 text-gray-900'',
        className
      )}
    />
  );
}','jsx','medium',ARRAY['tailwind','basics','react']::text[],true,121),
('tailwind','basics','tailwind-basics-122','What are the main pros and cons of the utility-first approach?','Pros: no naming CSS classes, no dead CSS to prune since unused utilities are never generated, consistent design tokens, and fast iteration without switching files. Cons: more verbose and repetitive markup, a real learning curve for the class vocabulary, and a risk of scattering the same combination of classes across many components without an extraction step (a component or `@apply`).',null,null,null,'medium',ARRAY['tailwind','basics','tradeoffs']::text[],false,122),
('tailwind','basics','tailwind-basics-123','How do you reference a Tailwind theme value from within custom CSS using the `theme()` function?','The `theme()` function lets you pull a value out of your Tailwind config''s theme object directly inside a custom CSS rule, using dot notation to the token, so custom styles stay in sync with the same design tokens utilities use. It''s most useful for one-off custom CSS that utilities can''t express, like a complex `background-image` gradient using theme colors.','.gradient-brand {
  background-image: linear-gradient(to right, theme(''colors.indigo.500''), theme(''colors.purple.500''));
}','.gradient-brand {
  background-image: linear-gradient(to right, theme(''colors.indigo.500''), theme(''colors.purple.500''));
}','css','hard',ARRAY['tailwind','basics','theme-function']::text[],false,123),
('tailwind','basics','tailwind-basics-124','How do you truncate overflowing text with a single Tailwind utility?','The `truncate` utility applies `overflow: hidden`, `text-overflow: ellipsis`, and `white-space: nowrap` together to clip single-line text with an ellipsis. For multi-line clamping, Tailwind provides `line-clamp-{n}` utilities that rely on the `-webkit-line-clamp` property.',null,'<p class="truncate w-48">This is a very long line of text that will be cut off</p>','html','easy',ARRAY['tailwind','basics','typography']::text[],false,124),
('tailwind','basics','tailwind-basics-125','What does the `group` class do, and how is it typically used?','Adding `group` to a parent element lets descendant elements react to that parent''s state using variants like `group-hover:`, `group-focus:`, or `group-disabled:`, which is essential when you want hovering a card to also change the appearance of an icon or text nested inside it, since CSS has no native parent-selector for pseudo-classes.',null,'<a href="#" class="group flex items-center gap-2">
  <span class="text-gray-700 group-hover:text-blue-600">Read more</span>
  <svg class="h-4 w-4 text-gray-400 group-hover:translate-x-1 transition"></svg>
</a>','html','medium',ARRAY['tailwind','basics','group']::text[],true,125),
('tailwind','basics','tailwind-basics-126','What is the `peer` class used for in Tailwind?','`peer` marks an element so that a *sibling* (not descendant) can style itself based on that element''s state using `peer-{variant}:` classes, commonly used for styling a label or helper text based on an input''s `:focus`, `:invalid`, or `:checked` state without JavaScript.',null,'<input type="email" class="peer border border-gray-300" required />
<p class="invisible peer-invalid:visible text-sm text-red-500">Enter a valid email</p>','html','medium',ARRAY['tailwind','basics','peer']::text[],false,126),
('tailwind','basics','tailwind-basics-127','Do you need to worry about vendor prefixes when writing Tailwind utility classes?','No — Tailwind runs your compiled CSS through Autoprefixer (or an equivalent built-in Lightning CSS step in v4) as part of its build pipeline, so vendor-prefixed properties like `-webkit-line-clamp` or older flexbox prefixes are added automatically based on your configured browser support targets. You never write prefixes by hand in either your utility usage or in `@apply` blocks.',null,null,null,'hard',ARRAY['tailwind','basics','autoprefixer']::text[],false,127),
('tailwind','basics','tailwind-basics-128','Is Tailwind''s utility class markup hard to learn, and how do teams typically ramp up?','There''s a genuine initial learning curve since you''re memorizing a large but consistent vocabulary instead of writing arbitrary CSS, but most developers become productive within days because the naming follows predictable patterns and editor tooling (the official IntelliSense extension) provides autocomplete and hover previews. Teams often pair it with a documented set of extracted components so the vocabulary only needs to be learned once, at the component layer.',null,null,null,'easy',ARRAY['tailwind','basics','developer-experience']::text[],false,128),
('tailwind','basics','tailwind-basics-129','What do the `@tailwind base`, `@tailwind components`, and `@tailwind utilities` directives each inject?','`@tailwind base` injects Preflight and other base element styles, `@tailwind components` injects any component classes registered via plugins or `@layer components` (empty by default), and `@tailwind utilities` injects all the generated utility classes, which must come last so utilities can override component styles when both are present.',null,'@tailwind base;
@tailwind components;
@tailwind utilities;','css','easy',ARRAY['tailwind','basics','directives']::text[],false,129),
('tailwind','basics','tailwind-basics-130','How do you force a utility to override another style using the important modifier?','Prefixing a utility with `!` (e.g. `!text-red-500`) appends `!important` to that specific declaration, which is useful for overriding a third-party library''s inline styles or a stubborn specificity conflict without disabling Tailwind''s important strategy globally. It should be used sparingly since relying on it broadly usually signals a deeper specificity problem.',null,'<input class="text-gray-900 !text-red-500" />','html','medium',ARRAY['tailwind','basics','important-modifier']::text[],false,130),
('tailwind','basics','tailwind-basics-131','Does using many utility classes make markup less semantic, and how do teams address that concern?','Utility classes don''t affect the semantic meaning of HTML elements themselves — you still choose `<button>`, `<nav>`, or `<article>` appropriately — but long class lists can make markup harder to scan visually. Teams manage this by extracting repeated utility combinations into framework components (a React `<Button>`), using `@apply` for genuinely reusable low-level classes, and relying on editor formatting/class-sorting plugins to keep class lists organized.',null,null,null,'hard',ARRAY['tailwind','basics','semantics']::text[],false,131),
('tailwind','basics','tailwind-basics-132','How does Tailwind avoid the specificity wars that plague large hand-written CSS or BEM-based codebases?','Because nearly every utility class is a single, flat CSS selector targeting one property, all utilities share the same low specificity, so conflicts are resolved by source order in the generated stylesheet rather than escalating specificity battles (extra classes, IDs, or nested selectors) the way large BEM/SCSS codebases often do. This flat specificity is also why Tailwind explicitly orders variants like `hover:` and `focus:` later in the stylesheet so they reliably override the base utility.',null,null,null,'hard',ARRAY['tailwind','basics','specificity']::text[],false,132),
('tailwind','configuration','tailwind-configuration-101','What is `tailwind.config.js` for, and what are its main top-level sections?','It''s the central configuration file that controls which files Tailwind scans for class names (`content`), the design tokens available to utilities (`theme`), any registered plugins (`plugins`), and options like `darkMode`. Everything Tailwind generates — colors, spacing, breakpoints, font sizes — ultimately traces back to values defined or extended in this file.',null,'module.exports = {
  content: [''./src/**/*.{html,js,jsx,tsx}''],
  theme: { extend: {} },
  plugins: [],
};','javascript','easy',ARRAY['tailwind','configuration','config-file']::text[],true,101),
('tailwind','configuration','tailwind-configuration-102','What does the `content` option in the config do, and why is it critical to set correctly?','The `content` array tells Tailwind which files to scan for class name usage so it knows exactly which utility classes to actually generate in the final CSS output. If a template path is missing from `content`, any classes used only in that file will be silently omitted from the build, which is one of the most common Tailwind setup bugs (styles "not working" in production).',null,'module.exports = {
  content: [''./index.html'', ''./src/**/*.{js,jsx,ts,tsx}''],
};','javascript','easy',ARRAY['tailwind','configuration','content']::text[],true,102),
('tailwind','configuration','tailwind-configuration-103','What is the difference between setting values directly under `theme` versus under `theme.extend`?','Keys placed directly under `theme` (e.g. `theme.colors`) completely replace Tailwind''s default values for that key, so you lose all default colors unless you redefine them yourself. Keys placed under `theme.extend` are merged on top of the defaults, adding new values (or overriding a specific one) while keeping everything else from the default theme intact — `extend` is what you reach for in the vast majority of cases.',null,'module.exports = {
  theme: {
    extend: {
      colors: { brand: ''#5b21b6'' }, // adds brand, keeps all default colors
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','theme']::text[],true,103),
('tailwind','configuration','tailwind-configuration-104','How would you completely replace Tailwind''s default color palette with your own brand colors?','You assign an object directly to `theme.colors` (not `theme.extend.colors`), which discards every default color like `blue` and `emerald` and leaves only the colors you define. This is used when a project has a strict brand palette and doesn''t want the default palette available at all, keeping generated CSS smaller and design choices constrained.',null,'module.exports = {
  theme: {
    colors: {
      white: ''#ffffff'',
      black: ''#000000'',
      primary: ''#0ea5e9'',
      danger: ''#dc2626'',
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','colors']::text[],false,104),
('tailwind','configuration','tailwind-configuration-105','How do you access Tailwind''s default theme values inside your own config, for example to extend the default font family?','You import `defaultTheme` from `tailwindcss/defaultTheme` and spread or reference its values, which lets you add to a default array (like appending a custom font ahead of the default sans stack) instead of retyping the whole fallback list.',null,'const defaultTheme = require(''tailwindcss/defaultTheme'');

module.exports = {
  theme: {
    extend: {
      fontFamily: {
        sans: [''Inter'', ...defaultTheme.fontFamily.sans],
      },
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','default-theme']::text[],false,105),
('tailwind','configuration','tailwind-configuration-106','How do you add a custom breakpoint alongside Tailwind''s default `sm`/`md`/`lg`/`xl`/`2xl` screens?','You extend `theme.extend.screens` with a new key and a min-width value, which adds an additional responsive variant (e.g. `xs:` or `3xl:`) usable anywhere a built-in breakpoint prefix would be. If you instead assign directly to `theme.screens`, you replace the default breakpoints entirely rather than adding to them.',null,'module.exports = {
  theme: {
    extend: {
      screens: { ''3xl'': ''1920px'' },
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','breakpoints']::text[],false,106),
('tailwind','configuration','tailwind-configuration-107','How do you register an official Tailwind plugin like `@tailwindcss/forms` or `@tailwindcss/typography` in the config?','After installing the plugin package, you `require()` it and add it to the `plugins` array in `tailwind.config.js`. Plugins can inject base styles, component classes, new utilities, or new variants, and some accept an options object as an argument to customize their behavior.',null,'module.exports = {
  plugins: [
    require(''@tailwindcss/forms''),
    require(''@tailwindcss/typography''),
  ],
};','javascript','medium',ARRAY['tailwind','configuration','plugins']::text[],false,107),
('tailwind','configuration','tailwind-configuration-108','What is the `safelist` option used for in the Tailwind config?','`safelist` forces specific class names (or patterns via regex) to always be generated in the output CSS even if they aren''t found by scanning your `content` files, which is needed when class names are built dynamically at runtime (e.g. `` `bg-${color}-500` `` from a variable) and therefore invisible to Tailwind''s static scanner.',null,'module.exports = {
  safelist: [''bg-red-500'', ''bg-green-500'', { pattern: /bg-(red|green|blue)-(400|500|600)/ }],
};','javascript','medium',ARRAY['tailwind','configuration','safelist']::text[],false,108),
('tailwind','configuration','tailwind-configuration-109','What is the `presets` option in Tailwind''s configuration for?','`presets` lets you define a shared, reusable base configuration (a design system) in one file and import it as the starting point for multiple projects'' configs, rather than duplicating theme customizations everywhere. Each project can still layer its own `theme.extend` and `plugins` on top of the shared preset.',null,'// tailwind.config.js
module.exports = {
  presets: [require(''@company/tailwind-preset'')],
  theme: { extend: { colors: { accent: ''#f59e0b'' } } },
};','javascript','hard',ARRAY['tailwind','configuration','presets']::text[],false,109),
('tailwind','configuration','tailwind-configuration-110','Can theme values in the config be defined as functions instead of static objects, and why would you do that?','Yes — many theme keys accept a function that receives a `{ theme }` helper (and sometimes other utilities), letting you compute one theme section from another, such as deriving `spacing` values or generating negative margin scales programmatically instead of hand-listing every value. This is more advanced and mainly used when building shareable presets or plugins.',null,'module.exports = {
  theme: {
    extend: {
      textColor: ({ theme }) => theme(''colors''),
    },
  },
};','javascript','hard',ARRAY['tailwind','configuration','theme']::text[],false,110),
('tailwind','configuration','tailwind-configuration-111','How do you configure dark mode strategy in Tailwind, and what are the available options?','The `darkMode` option controls how the `dark:` variant is triggered: `''media''` (default in older versions) applies dark styles based on the OS-level `prefers-color-scheme` media query automatically, while `''class''` (or `''selector''` in newer versions) applies dark styles only when a `.dark` class is present on an ancestor (usually `<html>`), which lets the app toggle dark mode manually with JavaScript instead of following the OS setting.',null,'module.exports = {
  darkMode: ''class'',
};','javascript','easy',ARRAY['tailwind','configuration','dark-mode']::text[],true,111),
('tailwind','configuration','tailwind-configuration-112','What does the `important` option in the Tailwind config do?','Setting `important: true` makes every generated utility use `!important`, which is sometimes used when integrating Tailwind into a legacy codebase where existing CSS has high specificity you need to reliably override. You can also scope it to a selector string (e.g. `''#app''`) so only utilities nested under that selector are marked important, which is gentler than a global flag.',null,null,null,'medium',ARRAY['tailwind','configuration','important']::text[],false,112),
('tailwind','configuration','tailwind-configuration-113','What is `corePlugins` used for in the Tailwind config?','`corePlugins` lets you disable specific built-in utility groups entirely (by passing an array of names to disable, or an object mapping names to `false`), which is useful when a feature conflicts with another library or you want a smaller CSS output by removing utilities your project never uses, such as disabling `preflight` if you already have your own reset.',null,'module.exports = {
  corePlugins: {
    preflight: false,
  },
};','javascript','medium',ARRAY['tailwind','configuration','core-plugins']::text[],false,113),
('tailwind','configuration','tailwind-configuration-114','What does the `separator` option in the config control?','`separator` changes the character used between a variant prefix and the utility name, defaulting to `:` (as in `hover:bg-blue-500`). It''s rarely changed, but exists for compatibility with template engines or tools where a colon in a class name causes parsing issues.',null,null,null,'medium',ARRAY['tailwind','configuration','separator']::text[],false,114),
('tailwind','configuration','tailwind-configuration-115','How would you extend the font-size scale to include a custom size with a specific line height?','Font-size entries can be a plain string for just the size, or a tuple `[fontSize, lineHeight]` (or `[fontSize, { lineHeight, letterSpacing }]`) so a single utility like `text-xl` sets both properties together, avoiding the need to pair it with a separate leading utility every time.',null,'module.exports = {
  theme: {
    extend: {
      fontSize: {
        display: [''3.5rem'', { lineHeight: ''1.1'', letterSpacing: ''-0.02em'' }],
      },
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','typography']::text[],false,115),
('tailwind','configuration','tailwind-configuration-116','How do you add custom border-radius or box-shadow tokens to the theme?','You extend `theme.extend.borderRadius` or `theme.extend.boxShadow` with new named keys mapping to CSS values, which then become usable as `rounded-{key}` or `shadow-{key}` utilities alongside the built-in scale.',null,'module.exports = {
  theme: {
    extend: {
      borderRadius: { xl2: ''1.25rem'' },
      boxShadow: { card: ''0 4px 14px 0 rgba(0,0,0,0.1)'' },
    },
  },
};','javascript','easy',ARRAY['tailwind','configuration','design-tokens']::text[],false,116),
('tailwind','configuration','tailwind-configuration-117','How do you define custom keyframes and animation utilities in the Tailwind config?','You add entries under `theme.extend.keyframes` describing the animation steps, and a matching entry under `theme.extend.animation` that references the keyframe name along with timing/duration/iteration settings, producing a new `animate-{name}` utility class.',null,'module.exports = {
  theme: {
    extend: {
      keyframes: {
        fadeIn: { ''0%'': { opacity: 0 }, ''100%'': { opacity: 1 } },
      },
      animation: {
        ''fade-in'': ''fadeIn 0.3s ease-out'',
      },
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','animation']::text[],false,117),
('tailwind','configuration','tailwind-configuration-118','Where do you configure the default centering and padding behavior of the `container` utility class?','The `theme.container` key accepts options like `center: true` to auto-center the container with margins, and a `padding` value (a single value or an object per breakpoint) for horizontal gutter, both of which apply whenever the `container` utility class is used.',null,'module.exports = {
  theme: {
    container: {
      center: true,
      padding: { DEFAULT: ''1rem'', lg: ''2rem'' },
    },
  },
};','javascript','easy',ARRAY['tailwind','configuration','container']::text[],false,118),
('tailwind','configuration','tailwind-configuration-119','How would you extend Tailwind''s z-index scale to add values for custom stacking contexts like modals or toasts?','You add named keys under `theme.extend.zIndex` mapping to numeric string values, which is preferable to reaching for arbitrary `z-[999]` values scattered throughout a codebase because it centralizes the app''s stacking order in one place and documents intent through the key name.',null,'module.exports = {
  theme: {
    extend: {
      zIndex: { modal: ''1000'', toast: ''1100'' },
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','z-index']::text[],false,119),
('tailwind','configuration','tailwind-configuration-120','What''s the general structure you''d expect in a typical `tailwind.config.js` file?','A typical config has `content` (glob paths to scan), `darkMode` (strategy), `theme` (with a nested `extend` object for customizations), and `plugins` (an array of registered plugin functions) — these four sections cover the vast majority of what teams configure day to day.',null,null,null,'easy',ARRAY['tailwind','configuration','config-file']::text[],false,120),
('tailwind','configuration','tailwind-configuration-121','How does theme configuration change in Tailwind CSS v4 compared to the v3 `tailwind.config.js` approach?','Tailwind v4 moves toward CSS-first configuration: instead of (or alongside) a JS config file, you define design tokens directly in CSS using an `@theme` block with CSS custom properties, and content detection is automatic in most setups rather than requiring an explicit `content` array. The JS config format is still supported for backward compatibility and complex cases like plugins, but the CSS-native approach is now the primary path for simple theme customization.','@theme {
  --color-brand: #5b21b6;
  --font-display: ''Inter'', sans-serif;
}','@theme {
  --color-brand: #5b21b6;
  --font-display: ''Inter'', sans-serif;
}','css','hard',ARRAY['tailwind','configuration','tailwind-v4']::text[],false,121),
('tailwind','configuration','tailwind-configuration-122','Why would you use a theme function like `({ theme }) => ({...})` instead of a plain object when configuring a design token?','A function gives you access to other already-resolved parts of the theme via the `theme()` helper, letting you derive one token set from another — for example, generating `outlineColor` values that mirror your color palette exactly, so a future palette change automatically propagates instead of requiring the derived section to be manually kept in sync.',null,null,null,'hard',ARRAY['tailwind','configuration','theme']::text[],false,122),
('tailwind','configuration','tailwind-configuration-123','How can you combine a shared company-wide preset with project-specific theme overrides without losing either?','You list the shared config in the `presets` array, and any `theme.extend` values you add in the project''s own config are merged on top of the preset''s theme (which itself may already extend Tailwind''s defaults) — so the resolution order is Tailwind defaults, then preset, then the local project''s own overrides, each layer only overriding what it explicitly defines.',null,null,null,'hard',ARRAY['tailwind','configuration','presets']::text[],false,123),
('tailwind','configuration','tailwind-configuration-124','How would you add a completely custom color to the palette without discarding Tailwind''s built-in colors?','Add it under `theme.extend.colors` with a name that doesn''t collide with a default color, optionally as an object of shades (50–950) if you want a full scale, or a single value if you only need one shade like a fixed `brand` color.',null,'module.exports = {
  theme: {
    extend: {
      colors: {
        brand: {
          light: ''#93c5fd'',
          DEFAULT: ''#3b82f6'',
          dark: ''#1d4ed8'',
        },
      },
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','colors']::text[],false,124),
('tailwind','configuration','tailwind-configuration-125','What is the `DEFAULT` key convention used for within a theme object like `colors.brand`?','When a nested theme object includes a `DEFAULT` key, that value is used when the utility is referenced without a suffix — e.g. `bg-brand` maps to `colors.brand.DEFAULT` — while `bg-brand-light` and `bg-brand-dark` map to the other named keys in that object. It''s the convention Tailwind''s own default theme uses internally for things like `colors.gray.DEFAULT` equivalents.',null,null,null,'medium',ARRAY['tailwind','configuration','theme']::text[],false,125),
('tailwind','configuration','tailwind-configuration-126','How would you customize the default `fontFamily` theme to use a custom monospace font for code blocks?','You extend `theme.extend.fontFamily.mono` with your custom font stack, which then changes what the `font-mono` utility renders, keeping the utility name the same across the codebase while swapping the underlying font.',null,'module.exports = {
  theme: {
    extend: {
      fontFamily: {
        mono: [''"JetBrains Mono"'', ''monospace''],
      },
    },
  },
};','javascript','easy',ARRAY['tailwind','configuration','typography']::text[],false,126),
('tailwind','configuration','tailwind-configuration-127','What happens if two plugins registered in the config both try to define the same utility class?','Plugins are applied in the order they appear in the `plugins` array, so a later plugin''s definition for the same utility/selector will override an earlier one at the CSS-generation level, following the same last-rule-wins behavior as any other CSS cascade conflict Tailwind produces internally.',null,null,null,'hard',ARRAY['tailwind','configuration','plugins']::text[],false,127),
('tailwind','configuration','tailwind-configuration-128','How do you disable Tailwind''s Preflight base reset entirely while keeping all utility classes?','Set `corePlugins: { preflight: false }` in the config, which stops Tailwind from injecting its base reset styles but leaves every other utility, variant, and component class fully functional — useful when integrating Tailwind into an existing app that already has its own CSS reset.',null,'module.exports = {
  corePlugins: { preflight: false },
};','javascript','medium',ARRAY['tailwind','configuration','preflight']::text[],false,128),
('tailwind','configuration','tailwind-configuration-129','How would you configure a custom spacing scale that adds a few extra values without losing the default scale?','Add the extra keys under `theme.extend.spacing`, using string keys (they don''t have to be numeric) mapped to any valid CSS length, which then become usable across every utility that consumes the spacing scale — margin, padding, width, height, gap, and more, since they all reference the same shared spacing scale by default.',null,'module.exports = {
  theme: {
    extend: {
      spacing: { 18: ''4.5rem'', 128: ''32rem'' },
    },
  },
};','javascript','medium',ARRAY['tailwind','configuration','spacing']::text[],false,129),
('tailwind','configuration','tailwind-configuration-130','Why is it recommended to define `content` globs as specifically as possible rather than pointing at the whole project?','Narrower, accurate globs keep Tailwind''s build fast since it has fewer files to scan on every rebuild, and they avoid accidentally scanning generated output, `node_modules`, or non-template files that could either slow the scan or (in rare cases) produce false-positive class matches from unrelated text.',null,null,null,'medium',ARRAY['tailwind','configuration','content']::text[],false,130),
('tailwind','configuration','tailwind-configuration-131','Can you use environment variables or conditional logic inside `tailwind.config.js`?','Yes — since the config is a plain JavaScript (or TypeScript, in newer versions) module executed by Node, you can read `process.env` values or branch logic to, for example, enable a debug plugin only outside production, exactly as you would in any other Node config file.',null,'module.exports = {
  plugins: process.env.NODE_ENV === ''development'' ? [require(''./debug-plugin'')] : [],
};','javascript','medium',ARRAY['tailwind','configuration','config-file']::text[],false,131),
('tailwind','customization-plugins','tailwind-customization-plugins-101','What does the `@apply` directive do in Tailwind CSS?','`@apply` lets you compose existing utility classes into a custom, named CSS class, so instead of repeating a long utility list on every instance of a component in markup, you define it once in CSS and reference it by a single class name. It''s processed at build time, so the result is plain CSS with no runtime cost.',null,'.btn-primary {
  @apply bg-blue-600 text-white font-semibold px-4 py-2 rounded hover:bg-blue-700;
}','css','easy',ARRAY['tailwind','customization','apply']::text[],true,101),
('tailwind','customization-plugins','tailwind-customization-plugins-102','When should you reach for `@apply` versus extracting a reusable component in your JS framework?','`@apply` makes sense for simple, purely visual repetition (like a consistent button style used in a non-component context, such as server-rendered HTML or markdown output), while a framework component is generally preferred in React/Vue apps because it also encapsulates behavior, props, and accessibility logic, not just class names — most teams use components as the primary extraction mechanism and reach for `@apply` only in edge cases.',null,null,null,'medium',ARRAY['tailwind','customization','apply']::text[],false,102),
('tailwind','customization-plugins','tailwind-customization-plugins-103','Can you use variant-prefixed utilities like `hover:` or `md:` inside an `@apply` rule?','Yes — Tailwind supports applying variant-prefixed utilities directly inside `@apply`, such as `@apply hover:bg-blue-700 md:px-6;`, which get compiled into the appropriate nested pseudo-class rules or media queries within the resulting custom class, exactly as if they''d been written in markup.',null,'.card {
  @apply rounded-lg border p-4 hover:shadow-md md:p-6;
}','css','medium',ARRAY['tailwind','customization','apply']::text[],false,103),
('tailwind','customization-plugins','tailwind-customization-plugins-104','What is the `@layer` directive used for in a Tailwind CSS file?','`@layer` assigns a block of custom CSS to one of Tailwind''s three internal layers — `base`, `components`, or `utilities` — so it''s injected in the correct position relative to Tailwind''s generated styles and correctly deduplicated/purged along with everything else. Without `@layer`, custom CSS added outside the directives can end up in the wrong cascade position or get treated inconsistently by the build.',null,'@layer components {
  .btn {
    @apply rounded px-4 py-2 font-medium;
  }
}','css','easy',ARRAY['tailwind','customization','layers']::text[],false,104),
('tailwind','customization-plugins','tailwind-customization-plugins-105','How would you define a reusable `.card` component class using `@apply`?','You write a normal CSS class selector inside `@layer components` and use `@apply` to pull in the utility classes that define its look, giving you a single semantic class name to use in markup instead of repeating the full utility list everywhere the card pattern appears.',null,'@layer components {
  .card {
    @apply rounded-xl border border-gray-200 bg-white p-6 shadow-sm;
  }
}','css','medium',ARRAY['tailwind','customization','apply']::text[],true,105),
('tailwind','customization-plugins','tailwind-customization-plugins-106','What''s a downside of overusing `@apply` across a large stylesheet instead of using utilities directly in markup?','Heavy use of `@apply` recreates the exact problem utility-first CSS was designed to avoid — you end up maintaining a growing, hand-named stylesheet again, losing the main benefit of styles being colocated and immediately visible in the markup. It can also subtly increase specificity compared to plain utility classes, since you''re now dealing with a named class selector rather than flat utility selectors, making later overrides slightly harder.',null,null,null,'hard',ARRAY['tailwind','customization','apply']::text[],false,106),
('tailwind','customization-plugins','tailwind-customization-plugins-107','How do you write a basic Tailwind plugin that adds new utility classes using `addUtilities`?','You call Tailwind''s `plugin()` function with a callback that receives helper functions, and call `addUtilities()` with an object of new CSS rules, which registers them as first-class utilities (including variant support) rather than ad hoc CSS.',null,'const plugin = require(''tailwindcss/plugin'');

module.exports = plugin(function ({ addUtilities }) {
  addUtilities({
    ''.text-shadow'': { textShadow: ''0 2px 4px rgba(0,0,0,0.3)'' },
  });
});','javascript','medium',ARRAY['tailwind','customization','plugins']::text[],false,107),
('tailwind','customization-plugins','tailwind-customization-plugins-108','How does `addComponents` differ from `addUtilities` inside a Tailwind plugin?','`addComponents` registers higher-level, multi-property component-style classes into the `components` layer (conceptually similar to what `@apply` produces), while `addUtilities` registers single-purpose utility classes into the `utilities` layer. The distinction mainly affects cascade ordering — component styles are meant to be easily overridden by utilities, matching the layer order `base` → `components` → `utilities`.',null,'module.exports = plugin(function ({ addComponents }) {
  addComponents({
    ''.btn'': {
      padding: ''.5rem 1rem'',
      borderRadius: ''.25rem'',
      fontWeight: ''600'',
    },
  });
});','javascript','medium',ARRAY['tailwind','customization','plugins']::text[],false,108),
('tailwind','customization-plugins','tailwind-customization-plugins-109','What does `addBase` do inside a Tailwind plugin?','`addBase` injects raw CSS rules into the `base` layer, the same layer Preflight uses, which is appropriate for setting default styles on plain HTML elements (like a custom `h1` default size) rather than opt-in classes — base styles apply automatically wherever the matching element appears, with no class needed.',null,'module.exports = plugin(function ({ addBase, theme }) {
  addBase({
    h1: { fontSize: theme(''fontSize.2xl'') },
  });
});','javascript','medium',ARRAY['tailwind','customization','plugins']::text[],false,109),
('tailwind','customization-plugins','tailwind-customization-plugins-110','How would you create a custom variant using `addVariant` in a Tailwind plugin?','`addVariant(name, definition)` registers a new variant prefix that generates the given selector modification wherever it''s used, letting you create project-specific state variants beyond the built-in ones — for example, a `theme-dark:` variant tied to a `[data-theme="dark"]` attribute selector instead of the standard `dark:` class strategy.',null,'module.exports = plugin(function ({ addVariant }) {
  addVariant(''theme-dark'', ''[data-theme="dark"] &'');
});','javascript','hard',ARRAY['tailwind','customization','plugins']::text[],false,110),
('tailwind','customization-plugins','tailwind-customization-plugins-111','What is `matchUtilities` used for in a Tailwind plugin, and how does it differ from `addUtilities`?','`matchUtilities` dynamically generates a whole family of utilities from a theme scale (much like Tailwind''s own built-in utilities do), so a single registration can produce `.my-util-{key}` for every value in a given theme object, including support for arbitrary values — `addUtilities` only registers the fixed, literal set of classes you write out explicitly.',null,'module.exports = plugin(function ({ matchUtilities, theme }) {
  matchUtilities(
    { ''text-shadow'': (value) => ({ textShadow: value }) },
    { values: theme(''textShadow'') }
  );
});','javascript','medium',ARRAY['tailwind','customization','plugins']::text[],false,111),
('tailwind','customization-plugins','tailwind-customization-plugins-112','What is `matchVariant` used for in Tailwind''s plugin API?','`matchVariant` is the dynamic counterpart to `addVariant`, letting you register a whole parameterized family of variants that accept a value (including arbitrary values), similar to how the built-in `data-[state=open]:` or `aria-[...]:` variants work internally — useful for building a custom variant that needs to accept different selector parameters rather than a single fixed one.',null,null,null,'hard',ARRAY['tailwind','customization','plugins']::text[],false,112),
('tailwind','customization-plugins','tailwind-customization-plugins-113','What do the official first-party Tailwind plugins offer, and which are most commonly used?','The most widely used official plugins are `@tailwindcss/forms` (resets form elements to a consistent, easily-styleable baseline), `@tailwindcss/typography` (the `prose` class for styling raw HTML content like blog posts or markdown output), `@tailwindcss/aspect-ratio` (aspect-ratio helpers, now largely superseded by the built-in `aspect-*` utilities), and `@tailwindcss/container-queries` (container query support, now built into core in v4).',null,null,null,'easy',ARRAY['tailwind','customization','official-plugins']::text[],false,113),
('tailwind','customization-plugins','tailwind-customization-plugins-114','How do you use the `@tailwindcss/typography` plugin''s `prose` class?','Adding the `prose` class to a container automatically applies sensible, well-designed typographic styles (heading sizes, paragraph spacing, link colors, list markers) to any raw HTML rendered inside it, which is ideal for content you don''t control the individual tags of, like markdown-rendered blog posts, without needing to write custom CSS for every possible HTML element.',null,'<article class="prose dark:prose-invert lg:prose-lg">
  <h1>Post title</h1>
  <p>Rendered markdown content...</p>
</article>','html','medium',ARRAY['tailwind','customization','typography-plugin']::text[],false,114),
('tailwind','customization-plugins','tailwind-customization-plugins-115','Why does the `@tailwindcss/forms` plugin exist, and what problem does it solve?','Native form elements (checkboxes, radios, selects) come with inconsistent, hard-to-override, browser-specific default styling. The forms plugin resets them to a clean, minimal baseline that''s easy to restyle predictably with ordinary utility classes, instead of every project needing to hand-write its own form-element reset.',null,null,null,'medium',ARRAY['tailwind','customization','forms-plugin']::text[],false,115),
('tailwind','customization-plugins','tailwind-customization-plugins-116','What is a CSS data-type hint in an arbitrary value, like `text-[length:1rem]` versus `text-[1rem]`, and when is it needed?','Tailwind usually infers what kind of CSS value an arbitrary value represents (a color, a length, etc.) from its syntax, but some values are ambiguous — `text-[1rem]` could mean font size, color, or text-shadow depending on context. A data-type hint prefix like `length:` or `color:` explicitly tells Tailwind how to interpret the value so it generates the correct property.',null,'<p class="text-[length:1rem] text-[color:#334155]">Explicit arbitrary value types</p>','html','hard',ARRAY['tailwind','customization','arbitrary-values']::text[],false,116),
('tailwind','customization-plugins','tailwind-customization-plugins-117','How does Tailwind''s JIT engine decide which CSS to actually generate for a project?','Rather than pre-generating every possible utility combination ahead of time, the Just-In-Time engine scans the files listed in `content` for strings that look like class names, and generates CSS only for the ones it finds, including arbitrary-value classes it discovers on the fly — this is why unused classes never bloat the output and why a class name must appear as a complete, unbroken string in your source for it to be detected.',null,null,null,'hard',ARRAY['tailwind','customization','jit']::text[],false,117),
('tailwind','customization-plugins','tailwind-customization-plugins-118','What is the arbitrary properties syntax in Tailwind, like `[mask-type:luminance]`, and how is it different from an arbitrary value?','An arbitrary value fills in the value for an existing utility (`bg-[#1da1f2]` still uses the known `background-color` property). An arbitrary property lets you write an entirely custom CSS property/value pair that Tailwind doesn''t have a named utility for at all, wrapped in square brackets as `[property:value]`, generating a one-off declaration directly.',null,'<div class="[mask-type:luminance] [writing-mode:vertical-rl]">Custom properties</div>','html','medium',ARRAY['tailwind','customization','arbitrary-values']::text[],false,118),
('tailwind','customization-plugins','tailwind-customization-plugins-119','How can you reference a CSS custom property (variable) as part of an arbitrary value?','You can pass a `var(--my-var)` reference inside an arbitrary value''s brackets, or use the shorthand parenthesis syntax for custom properties (`bg-(--brand-color)` in newer Tailwind versions), which is useful for values that are set dynamically at runtime — for example, a theme color injected via inline `style` on a root element and consumed by utility classes throughout the page.',null,'<div style="--brand-color: #7c3aed" class="bg-[var(--brand-color)] text-white">Dynamic brand color</div>','html','hard',ARRAY['tailwind','customization','arbitrary-values']::text[],false,119),
('tailwind','customization-plugins','tailwind-customization-plugins-120','What''s the tradeoff between using `@apply` to centralize styling versus using CSS custom properties for theming?','`@apply` centralizes a *fixed set* of utility classes into one class name at build time, which is great for structural repetition but doesn''t change at runtime. CSS custom properties (often combined with Tailwind''s arbitrary value syntax or theme config) allow values to be swapped dynamically in the browser — such as toggling a brand color at runtime — without a rebuild, which `@apply`-based classes can''t do on their own.',null,null,null,'medium',ARRAY['tailwind','customization','theming']::text[],false,120),
('tailwind','customization-plugins','tailwind-customization-plugins-121','What are the tradeoffs of extracting a repeated utility pattern into a CSS component class versus a framework component (e.g. a React component)?','A CSS class extraction (via `@apply`) is framework-agnostic and works in plain HTML, but only captures styling, not behavior, props, or slots. A framework component captures both styling and logic/behavior in one reusable unit and integrates naturally with props-driven variants, but ties the reuse to that specific framework — in a React or Vue codebase, component extraction is almost always the better default.',null,null,null,'medium',ARRAY['tailwind','customization','apply']::text[],false,121),
('tailwind','customization-plugins','tailwind-customization-plugins-122','How would you write a plugin that registers a reusable `.btn` component class using theme values instead of hardcoded ones?','Inside `addComponents`, you use the `theme()` helper the plugin callback receives to pull colors, spacing, and radius straight from the project''s configured design tokens, so the generated component automatically stays consistent with whatever theme customizations the project has made, rather than hardcoding values that could drift out of sync.',null,'module.exports = plugin(function ({ addComponents, theme }) {
  addComponents({
    ''.btn'': {
      backgroundColor: theme(''colors.blue.600''),
      color: theme(''colors.white''),
      padding: `${theme(''spacing.2'')} ${theme(''spacing.4'')}`,
      borderRadius: theme(''borderRadius.md''),
    },
  });
});','javascript','hard',ARRAY['tailwind','customization','plugins']::text[],false,122),
('tailwind','customization-plugins','tailwind-customization-plugins-123','How do you use Tailwind''s configured breakpoints inside a custom CSS media query with the `screens()` function?','`screens(''md'')` resolves to the min-width value configured for that breakpoint in `theme.screens`, letting hand-written custom CSS media queries stay in sync with the same breakpoint values utilities use, instead of hardcoding a pixel value that could drift if the config changes later.',null,'@media (min-width: screens(''md'')) {
  .custom-banner {
    padding: 2rem;
  }
}','css','medium',ARRAY['tailwind','customization','theme-function']::text[],false,123),
('tailwind','customization-plugins','tailwind-customization-plugins-124','What''s the difference between `plugin()` and `plugin.withOptions()` when authoring a Tailwind plugin?','`plugin()` creates a plugin with fixed, non-configurable behavior. `plugin.withOptions()` creates a plugin *factory* — a function that accepts an options object from the consumer''s config and returns a configured plugin instance — which is needed whenever a plugin should be customizable per project, like letting consumers choose a class prefix or toggle a feature.',null,'module.exports = plugin.withOptions(function (options = {}) {
  return function ({ addUtilities }) {
    addUtilities({ ''.custom'': { color: options.color || ''black'' } });
  };
});','javascript','easy',ARRAY['tailwind','customization','plugins']::text[],false,124),
('tailwind','customization-plugins','tailwind-customization-plugins-125','Do declarations inside an `@apply` rule follow the normal CSS cascade with respect to their source order?','Yes — if two applied utilities set the same CSS property, the one whose generated rule comes later in Tailwind''s compiled stylesheet wins, following the same rules as any conflicting utilities used directly in markup; the order they''re listed textually inside the `@apply` line itself doesn''t determine the winner, since `@apply` just expands to the underlying rules.',null,null,null,'medium',ARRAY['tailwind','customization','apply']::text[],false,125),
('tailwind','customization-plugins','tailwind-customization-plugins-126','Can you `@apply` a class that itself uses an arbitrary value, like `@apply bg-[#123456]`?','Yes — `@apply` works with any valid utility class Tailwind can generate, including ones built from arbitrary values, since at build time it''s simply pulling in the already-generated CSS declarations for that class name regardless of how the class was produced.',null,'.brand-banner {
  @apply bg-[#123456] text-white p-6;
}','css','hard',ARRAY['tailwind','customization','apply']::text[],false,126),
('tailwind','customization-plugins','tailwind-customization-plugins-127','Why does `@apply` require Tailwind to run as part of a PostCSS build step rather than working in a plain CSS file loaded directly in the browser?','`@apply` is not valid standard CSS syntax — it''s a Tailwind-specific at-rule that must be resolved by Tailwind''s PostCSS plugin during the build, which looks up each referenced utility''s already-computed CSS declarations and inlines them into the custom class. Without running through that build step, browsers would simply ignore or error on the `@apply` line since it has no native meaning.',null,null,null,'medium',ARRAY['tailwind','customization','build-tools']::text[],false,127),
('tailwind','customization-plugins','tailwind-customization-plugins-128','How would you add a brand-new custom utility class scoped correctly into Tailwind''s utilities layer without writing a full plugin?','You can wrap plain CSS class definitions in `@layer utilities` directly in your stylesheet, which registers them alongside Tailwind''s own generated utilities for correct cascade ordering and lets them be purged if unused, without needing the JavaScript plugin API at all — appropriate for a handful of simple, project-specific one-off utilities.',null,'@layer utilities {
  .text-shadow-sm {
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.15);
  }
}','css','medium',ARRAY['tailwind','customization','layers']::text[],false,128),
('tailwind','customization-plugins','tailwind-customization-plugins-129','What happened to plugins like `@tailwindcss/aspect-ratio` and `@tailwindcss/line-clamp` in more recent Tailwind versions?','Both were originally separate official plugins, but their functionality (the `aspect-*` and `line-clamp-*` utilities) was merged directly into Tailwind''s core utility set in later v3 releases, so most projects no longer need to install them separately — they''re only still relevant for older projects pinned to earlier versions.',null,null,null,'easy',ARRAY['tailwind','customization','official-plugins']::text[],false,129),
('tailwind','customization-plugins','tailwind-customization-plugins-130','How would you force `!important` on a specific `@apply`-generated declaration without making the whole config important?','Prefixing an individual utility with `!` inside the `@apply` line, like `@apply !bg-blue-500;`, marks just that declaration as important, which is the scoped alternative to setting the global `important: true` config option and is useful when only one property in a component class needs to reliably win against a stubborn external override.',null,'.banner {
  @apply !bg-blue-500 text-white p-4;
}','css','medium',ARRAY['tailwind','customization','apply']::text[],false,130),
('tailwind','customization-plugins','tailwind-customization-plugins-131','How do you register a variant that applies under multiple different selector conditions at once using `addVariant`?','`addVariant` accepts an array of selector strings (or an array of raw CSS rule strings) instead of a single one, which generates the variant''s styles under each condition, useful for a variant that should trigger from more than one state — for example, treating both `:hover` and `:focus` as a single combined `interactive:` variant.',null,'module.exports = plugin(function ({ addVariant }) {
  addVariant(''interactive'', [''&:hover'', ''&:focus'']);
});','javascript','hard',ARRAY['tailwind','customization','plugins']::text[],false,131),
('tailwind','layout-utilities','tailwind-layout-utilities-101','What''s the difference between the `flex` and `inline-flex` display utilities?','`flex` sets `display: flex`, making the element a block-level flex container that takes up the full available width like a normal block element. `inline-flex` sets `display: inline-flex`, making the flex container itself behave like an inline element, sizing to its content and flowing alongside surrounding inline content — commonly used for small inline widgets like a button with an icon.',null,null,null,'easy',ARRAY['tailwind','layout','flexbox']::text[],true,101),
('tailwind','layout-utilities','tailwind-layout-utilities-102','How do you control the main axis direction of a flex container in Tailwind?','`flex-row` (the default) lays children out horizontally, `flex-col` stacks them vertically, and `flex-row-reverse`/`flex-col-reverse` reverse the order along that axis — all mapping directly to the CSS `flex-direction` property.',null,'<div class="flex flex-col md:flex-row gap-4">
  <div>Item A</div>
  <div>Item B</div>
</div>','html','easy',ARRAY['tailwind','layout','flexbox']::text[],false,102),
('tailwind','layout-utilities','tailwind-layout-utilities-103','What do `justify-start`, `justify-center`, `justify-between`, and `justify-around` control in a flex container?','These map to `justify-content` and control how children are distributed along the flex container''s main axis: `justify-start` packs them at the beginning, `justify-center` centers the group, `justify-between` spreads them with equal space between items (none at the edges), and `justify-around` adds equal space around each item, including at the edges.',null,null,null,'easy',ARRAY['tailwind','layout','flexbox']::text[],false,103),
('tailwind','layout-utilities','tailwind-layout-utilities-104','What do `items-start`, `items-center`, and `items-stretch` control in a flex container?','These map to `align-items` and control alignment along the flex container''s cross axis (perpendicular to the main axis): `items-start` aligns children to the start of the cross axis, `items-center` centers them, and `items-stretch` (the default) stretches children to fill the container''s cross-axis size if they don''t have an explicit height/width set.',null,null,null,'easy',ARRAY['tailwind','layout','flexbox']::text[],false,104),
('tailwind','layout-utilities','tailwind-layout-utilities-105','What''s the practical difference between `justify-content` and `align-items` utilities in a flex layout?','`justify-*` utilities always act along the main axis (the direction items flow — horizontal by default, or vertical with `flex-col`), while `items-*` (align-items) always act along the cross axis, perpendicular to the main axis. Swapping `flex-row` for `flex-col` effectively swaps which physical direction each set of utilities affects, since the main and cross axes rotate with it.',null,null,null,'medium',ARRAY['tailwind','layout','flexbox']::text[],false,105),
('tailwind','layout-utilities','tailwind-layout-utilities-106','What does the `flex-wrap` utility do, and when would you use it?','`flex-wrap` sets `flex-wrap: wrap`, allowing flex children to wrap onto multiple lines instead of shrinking or overflowing when they don''t fit on a single row — commonly combined with `gap` for building tag lists or responsive card grids that reflow naturally as the container width changes. `flex-nowrap` (the default) forces everything onto one line.','<div class="flex flex-wrap gap-2">...</div>','<div class="flex flex-wrap gap-2">
  <span class="px-2 py-1 bg-gray-100 rounded">Tag 1</span>
  <span class="px-2 py-1 bg-gray-100 rounded">Tag 2</span>
</div>','html','medium',ARRAY['tailwind','layout','flexbox']::text[],false,106),
('tailwind','layout-utilities','tailwind-layout-utilities-107','How does the `gap` utility differ from `space-x`/`space-y` for spacing flex or grid children?','`gap-{n}` maps directly to the CSS `gap` property, which works natively in both flex and grid containers and adds spacing between items without affecting the container''s own edges or requiring a `> * + *` selector trick. `space-x`/`space-y` only work reliably in flex/block contexts (not grid) and apply margin instead, which can interact awkwardly with wrapping — `gap` is generally the more modern, predictable choice.',null,null,null,'medium',ARRAY['tailwind','layout','gap']::text[],false,107),
('tailwind','layout-utilities','tailwind-layout-utilities-108','What do `flex-1`, `flex-auto`, and `flex-none` do, and how do they relate to `flex-grow`/`flex-shrink`/`flex-basis`?','These are shorthand utilities combining the three flex properties: `flex-1` sets `flex: 1 1 0%` (grow and shrink equally, ignoring content size), `flex-auto` sets `flex: 1 1 auto` (grow and shrink based on content size), and `flex-none` sets `flex: none` (item won''t grow or shrink at all, sizing purely from its own width/height). `flex-1` is the most common choice for making a sidebar''s main content area fill remaining space.',null,'<div class="flex">
  <aside class="flex-none w-48">Sidebar</aside>
  <main class="flex-1">Main content fills remaining space</main>
</div>','html','medium',ARRAY['tailwind','layout','flexbox']::text[],true,108),
('tailwind','layout-utilities','tailwind-layout-utilities-109','How do you turn an element into a CSS Grid container in Tailwind?','Applying `grid` sets `display: grid`, turning the element into a grid container whose children become grid items by default, after which you define the track structure with utilities like `grid-cols-{n}` and `grid-rows-{n}`.',null,'<div class="grid grid-cols-3 gap-4">
  <div>1</div><div>2</div><div>3</div>
</div>','html','easy',ARRAY['tailwind','layout','grid']::text[],false,109),
('tailwind','layout-utilities','tailwind-layout-utilities-110','How does `grid-cols-{n}` work, and what CSS does it generate?','`grid-cols-{n}` sets `grid-template-columns: repeat(n, minmax(0, 1fr))`, creating `n` equal-width columns. The `minmax(0, 1fr)` (rather than just `1fr`) is important because it prevents grid items with long unbreakable content, like a long word or a wide image, from forcing the column wider than intended.',null,null,null,'easy',ARRAY['tailwind','layout','grid']::text[],false,110),
('tailwind','layout-utilities','tailwind-layout-utilities-111','What do `col-span-{n}` and `row-span-{n}` do in a grid layout?','`col-span-{n}` makes a grid item span `n` columns instead of the default one, and `row-span-{n}` does the same for rows, both mapping to `grid-column: span n / span n` (and the row equivalent) — useful for featured items that should visually take up more space than a standard grid cell.',null,'<div class="grid grid-cols-3 gap-4">
  <div class="col-span-2">Wide item</div>
  <div>Normal item</div>
</div>','html','medium',ARRAY['tailwind','layout','grid']::text[],false,111),
('tailwind','layout-utilities','tailwind-layout-utilities-112','How do you set the number of rows in a grid, and when is it necessary versus letting content define row count?','`grid-rows-{n}` sets `grid-template-rows: repeat(n, minmax(0, 1fr))`, which is only necessary when you need explicit control over a fixed number of equal-height rows (like a calendar or a dashboard grid); most grids don''t set an explicit row count and instead let rows form implicitly based on however many items wrap, sized by `grid-auto-rows`.',null,null,null,'medium',ARRAY['tailwind','layout','grid']::text[],false,112),
('tailwind','layout-utilities','tailwind-layout-utilities-113','How do you set different horizontal and vertical gaps in a grid layout?','`gap-x-{n}` controls column gap and `gap-y-{n}` controls row gap independently, while plain `gap-{n}` sets both to the same value — useful when a grid''s rows need tighter spacing than its columns or vice versa.',null,'<div class="grid grid-cols-3 gap-x-6 gap-y-2">...</div>','html','medium',ARRAY['tailwind','layout','grid']::text[],false,113),
('tailwind','layout-utilities','tailwind-layout-utilities-114','How would you build a responsive grid that automatically fits as many columns as space allows, each at least 200px wide, without media queries?','Since Tailwind''s numeric `grid-cols-{n}` utilities are fixed counts, this pattern requires an arbitrary value that passes raw CSS `repeat(auto-fit, minmax(200px, 1fr))` to `grid-template-columns`, which lets the browser compute the column count dynamically based on available width — no breakpoint-specific classes needed.',null,'<div class="grid grid-cols-[repeat(auto-fit,minmax(200px,1fr))] gap-4">
  <div>Card</div>
  <div>Card</div>
</div>','html','hard',ARRAY['tailwind','layout','grid']::text[],false,114),
('tailwind','layout-utilities','tailwind-layout-utilities-115','What do `place-items`, `place-content`, and `place-self` do?','These are shorthand utilities that set both the row and column alignment in one class: `place-items-{value}` combines `align-items` + `justify-items` for all grid/flex items at once, `place-content-{value}` combines `align-content` + `justify-content` for the whole content area, and `place-self-{value}` overrides alignment for a single item along both axes.',null,'<div class="grid h-40 place-items-center">
  <span>Centered both ways</span>
</div>','html','medium',ARRAY['tailwind','layout','grid']::text[],false,115),
('tailwind','layout-utilities','tailwind-layout-utilities-116','What''s the difference between `align-self` and `justify-self` utilities on an individual grid or flex item?','`self-{value}` (align-self) overrides the cross-axis alignment for a single flex item, or the block-axis (row) alignment for a single grid item, independent of the container''s `items-*` setting. `justify-self-{value}` similarly overrides the inline-axis (column) alignment for one grid item — `justify-self` has no effect in a flex container since flex only has a single cross axis controlled by `align-self`.',null,null,null,'medium',ARRAY['tailwind','layout','grid']::text[],false,116),
('tailwind','layout-utilities','tailwind-layout-utilities-117','What does the `order` utility do, and does it change the underlying DOM order?','`order-{n}` maps to the CSS `order` property, changing the *visual* order in which flex or grid items are rendered without touching the actual DOM/source order, which matters for accessibility and keyboard/screen-reader navigation since assistive tech and tab order typically still follow DOM order, not visual order.',null,'<div class="flex">
  <div class="order-2">Renders second visually</div>
  <div class="order-1">Renders first visually</div>
</div>','html','medium',ARRAY['tailwind','layout','flexbox']::text[],false,117),
('tailwind','layout-utilities','tailwind-layout-utilities-118','When would you choose Flexbox over Grid, and vice versa, for a layout?','Flexbox is one-dimensional and excels at distributing items along a single row or column, like a navbar, a button group, or a card''s internal content stack. Grid is two-dimensional and better suited for layouts where you need explicit control over both rows and columns simultaneously, like a page shell, dashboard, or photo gallery — many real UIs use both together, Grid for the page structure and Flexbox for alignment within individual components.',null,null,null,'easy',ARRAY['tailwind','layout','flexbox']::text[],true,118),
('tailwind','layout-utilities','tailwind-layout-utilities-119','How do `col-start-{n}` and `col-end-{n}` allow explicit grid item placement?','`col-start-{n}` and `col-end-{n}` map to `grid-column-start`/`grid-column-end`, letting you place an item at an exact track line number rather than relying on the automatic flow order — useful for layouts where certain items need to occupy specific, non-sequential positions in the grid, like a hero banner that starts at column 2.',null,'<div class="grid grid-cols-4 gap-4">
  <div class="col-start-2 col-end-4">Positioned item</div>
</div>','html','medium',ARRAY['tailwind','layout','grid']::text[],false,119),
('tailwind','layout-utilities','tailwind-layout-utilities-120','What''s the difference between the explicit and implicit grid, and how do `grid-auto-rows`/`grid-auto-flow` utilities relate to it?','The explicit grid is the track structure you define with `grid-cols-*`/`grid-rows-*`; the implicit grid consists of extra rows or columns the browser automatically generates to fit items that overflow the explicit definition. `grid-auto-rows-{value}` / `grid-auto-cols-{value}` control the sizing of those auto-generated tracks, and `grid-auto-flow-{row|col|dense}` controls whether extra items flow by filling rows or columns first, with `dense` allowing the algorithm to backfill earlier empty cells.',null,null,null,'hard',ARRAY['tailwind','layout','grid']::text[],false,120),
('tailwind','layout-utilities','tailwind-layout-utilities-121','How would you build a responsive top navigation bar with a logo on the left and links on the right using Flexbox utilities?','You make the nav a flex container with `justify-between` to push the logo and link group to opposite ends, `items-center` to vertically align them, and `gap-*` inside the link group for spacing between individual links.',null,'<nav class="flex items-center justify-between px-6 py-4">
  <span class="font-bold">Logo</span>
  <div class="flex gap-6">
    <a href="#">Home</a>
    <a href="#">About</a>
  </div>
</nav>','html','medium',ARRAY['tailwind','layout','flexbox']::text[],false,121),
('tailwind','layout-utilities','tailwind-layout-utilities-122','How would you build a responsive card grid that shows 1 column on mobile, 2 on tablet, and 3 on desktop?','Combine a base `grid-cols-1` with responsive overrides, e.g. `grid-cols-1 sm:grid-cols-2 lg:grid-cols-3`, following the mobile-first cascade so each breakpoint only needs to specify what changes from the previous one, plus a `gap-*` utility for consistent spacing between cards.',null,'<div class="grid grid-cols-1 gap-6 sm:grid-cols-2 lg:grid-cols-3">
  <div class="rounded-lg border p-4">Card</div>
  <div class="rounded-lg border p-4">Card</div>
</div>','html','medium',ARRAY['tailwind','layout','grid']::text[],true,122),
('tailwind','layout-utilities','tailwind-layout-utilities-123','How do you center a single element both horizontally and vertically inside its parent using Flexbox?','Make the parent a flex container and apply `justify-center` to center along the main axis and `items-center` to center along the cross axis, which together center the child in both directions regardless of `flex-row`/`flex-col` orientation.',null,'<div class="flex h-screen items-center justify-center">
  <div class="rounded bg-white p-8 shadow">Centered modal</div>
</div>','html','easy',ARRAY['tailwind','layout','flexbox']::text[],true,123),
('tailwind','layout-utilities','tailwind-layout-utilities-124','How would you center content using Grid instead of Flexbox?','Applying `grid` plus `place-items-center` on the parent centers a single child both horizontally and vertically in one utility, which is a slightly more concise equivalent to the `flex items-center justify-center` pattern when you only need to center one item and don''t need any other flex-specific behavior.',null,'<div class="grid h-screen place-items-center">
  <div>Centered content</div>
</div>','html','easy',ARRAY['tailwind','layout','grid']::text[],false,124),
('tailwind','layout-utilities','tailwind-layout-utilities-125','What''s the difference between using `flex-1` and `w-full` to make an element fill available horizontal space in a flex row?','`w-full` sets a fixed width of 100% of the parent, which in a flex row context actually means 100% of the *flex container''s* width regardless of siblings, often causing overflow if there are other items. `flex-1` instead tells the item to grow and shrink to fill whatever space remains after accounting for sibling items, which is almost always what you want for a flexible column alongside a fixed-width sidebar.',null,null,null,'medium',ARRAY['tailwind','layout','flexbox']::text[],false,125),
('tailwind','layout-utilities','tailwind-layout-utilities-126','What is CSS subgrid, and how do you use it with Tailwind''s `grid-cols-subgrid`/`grid-rows-subgrid` utilities?','Subgrid lets a nested grid item inherit its parent grid''s track sizing instead of defining its own independent tracks, which solves the long-standing problem of aligning nested grid content (like card titles across multiple cards) to a shared outer grid. `col-span-{n} grid-cols-subgrid` on a nested grid container makes its columns line up exactly with the parent''s corresponding column tracks.',null,'<div class="grid grid-cols-4">
  <div class="col-span-2 grid grid-cols-subgrid">
    <div class="col-start-1">Aligned to parent column 1</div>
  </div>
</div>','html','hard',ARRAY['tailwind','layout','grid']::text[],false,126),
('tailwind','layout-utilities','tailwind-layout-utilities-127','How do you maintain a fixed aspect ratio for a responsive element, like an embedded video, using Tailwind?','The `aspect-{ratio}` utility (e.g. `aspect-video` for 16/9, `aspect-square` for 1/1, or an arbitrary `aspect-[4/3]`) maps to the CSS `aspect-ratio` property, letting an element automatically compute its height from its width (or vice versa) to preserve the ratio as the container resizes, without the older padding-hack technique.',null,'<div class="aspect-video w-full overflow-hidden rounded">
  <iframe src="..." class="h-full w-full"></iframe>
</div>','html','medium',ARRAY['tailwind','layout','aspect-ratio']::text[],false,127),
('tailwind','layout-utilities','tailwind-layout-utilities-128','How do you center a fixed-max-width page layout horizontally using Tailwind?','The `container` utility sets a `max-width` matching the current breakpoint, and combined with `mx-auto` (equal auto left/right margins) it horizontally centers that fixed-width block in the viewport — a very common pattern for a page wrapper that shouldn''t stretch edge-to-edge on wide screens.',null,'<div class="container mx-auto px-4">
  Page content constrained and centered
</div>','html','easy',ARRAY['tailwind','layout','container']::text[],false,128),
('tailwind','layout-utilities','tailwind-layout-utilities-129','How do you position an element absolutely within a relatively positioned flex parent using Tailwind''s inset utilities?','Give the parent `relative` so it establishes a positioning context, then give the child `absolute` plus inset utilities like `top-0`, `right-0`, or the shorthand `inset-0` (all four sides) to position it precisely relative to that parent — a common pattern for overlay badges, close buttons, or full-cover loading spinners inside a flex/grid item.',null,'<div class="relative flex">
  <img src="/thumb.jpg" class="h-24 w-24" />
  <span class="absolute -right-1 -top-1 rounded-full bg-red-500 px-1.5 text-xs text-white">3</span>
</div>','html','medium',ARRAY['tailwind','layout','positioning']::text[],false,129),
('tailwind','layout-utilities','tailwind-layout-utilities-130','How do flex items achieve equal height columns automatically, without any explicit height utility?','The default `align-items: stretch` behavior of flex containers means that unless a flex item has an explicit height (or `items-start`/`self-start` is applied), each item stretches to match the height of the tallest sibling in the row automatically, which is why simple `flex` rows of cards naturally end up equal height with no extra CSS.',null,'<div class="flex gap-4">
  <div class="flex-1 rounded border p-4">Short content</div>
  <div class="flex-1 rounded border p-4">Much longer content that wraps onto several lines</div>
</div>','html','medium',ARRAY['tailwind','layout','flexbox']::text[],false,130),
('tailwind','layout-utilities','tailwind-layout-utilities-131','If both a `w-*` utility and a `basis-*` (flex-basis) utility are applied to the same flex item, which one wins?','`flex-basis` takes priority over `width` for sizing along the main axis of a flex container per the CSS spec, so if both are set on the same item in a `flex-row` context, `basis-*` determines the initial main-size before growing/shrinking is applied, while `width` is effectively ignored for that axis (though it would still apply if the item were in a `flex-col` container, since `width` then governs the cross axis).',null,null,null,'hard',ARRAY['tailwind','layout','flexbox']::text[],false,131),
('tailwind','layout-utilities','tailwind-layout-utilities-132','What''s the difference between `grid-cols-3` and an arbitrary value like `grid-cols-[1fr_2fr_1fr]`?','`grid-cols-3` generates three equal-width tracks via `repeat(3, minmax(0, 1fr))`. The arbitrary value `grid-cols-[1fr_2fr_1fr]` (underscores stand in for spaces inside the brackets) defines three tracks with different proportional widths directly, giving you full control over unequal column ratios that the standard numeric scale can''t express.',null,'<div class="grid grid-cols-[1fr_2fr_1fr] gap-4">
  <div>Sidebar</div>
  <div>Main</div>
  <div>Aside</div>
</div>','html','medium',ARRAY['tailwind','layout','grid']::text[],false,132),
('tailwind','performance','tailwind-performance-101','What is "purging" in the context of Tailwind CSS, and why does it matter for production bundle size?','Purging refers to removing every utility class Tailwind doesn''t detect being used in your project''s templates, so the shipped CSS file only contains the small subset of classes your app actually references instead of the full utility set (which would be several megabytes uncompressed). Modern Tailwind (v3+) doesn''t generate the full set at all and purge afterward — it generates on-demand via the JIT engine — but the practical effect is the same: only used classes ship to production.',null,null,null,'easy',ARRAY['tailwind','performance','purging']::text[],true,101),
('tailwind','performance','tailwind-performance-102','What is JIT (Just-In-Time) mode in Tailwind, and how did it change the build workflow compared to earlier versions?','Earlier Tailwind versions generated the entire possible utility stylesheet ahead of time and relied on a separate purge step (often PurgeCSS) to strip unused classes afterward, which was slow and sometimes fragile. JIT mode, made the default in Tailwind v3, generates CSS on-demand by scanning your source files directly and compiling only the classes actually found, making both dev and production builds dramatically faster and eliminating the need for a separate purge tool.',null,null,null,'easy',ARRAY['tailwind','performance','jit']::text[],true,102),
('tailwind','performance','tailwind-performance-103','How does the `content` array in `tailwind.config.js` affect the accuracy of what gets included in the final CSS?','The `content` globs are the *only* source Tailwind''s scanner reads to determine which classes exist in your project, so any file containing class names that isn''t covered by these globs will have those classes silently missing from the compiled output — this makes `content` both a performance lever (narrower globs scan faster) and a correctness requirement (too narrow, and real classes get dropped).',null,null,null,'medium',ARRAY['tailwind','performance','content-scanning']::text[],false,103),
('tailwind','performance','tailwind-performance-104','Why does constructing class names dynamically with string concatenation, like `` `text-${color}-500` ``, break Tailwind''s optimization and require a workaround?','Tailwind''s scanner looks for complete, literal class name strings in your source files — it doesn''t execute your JavaScript, so it can''t resolve a template literal at build time and never sees the actual class names like `text-red-500` that would exist at runtime, meaning those styles simply won''t be generated. The fix is either writing out the full class names as literal strings (even in a lookup object) or explicitly adding them to `safelist` in the config.',null,'// Broken: scanner can''t see the final class name
const cls = `text-${color}-500`;

// Works: full class names are literal and detectable
const colorMap = { red: ''text-red-500'', blue: ''text-blue-500'' };
const cls = colorMap[color];','javascript','medium',ARRAY['tailwind','performance','content-scanning']::text[],true,104),
('tailwind','performance','tailwind-performance-105','Does adding more utility classes to a component increase the runtime rendering cost in the browser?','No — once compiled, utility classes are ordinary CSS class selectors applied by the browser''s existing style engine, so having ten classes on an element costs essentially the same as one at runtime; the browser''s selector matching is highly optimized regardless of class count. The only real cost is the size of the stylesheet the browser has to parse once and the length of the `class` attribute string itself, both of which are negligible in practice.',null,null,null,'easy',ARRAY['tailwind','performance','runtime']::text[],false,105),
('tailwind','performance','tailwind-performance-106','Why doesn''t Tailwind''s compiled CSS grow linearly as a codebase grows larger?','Because CSS output is driven by the *distinct* set of utility classes actually used, not by the number of components or files, and most projects reuse the same handful of spacing, color, and layout utilities repeatedly across many components — after an initial ramp-up, adding more components tends to reuse existing classes far more often than it introduces brand-new ones, so CSS size growth flattens out over time rather than scaling with lines of code.',null,null,null,'easy',ARRAY['tailwind','performance','bundle-size']::text[],false,106),
('tailwind','performance','tailwind-performance-107','What role does a minifier like `cssnano` play in the Tailwind build pipeline?','After Tailwind generates the utility CSS for the classes it detected, a minifier like `cssnano` (commonly run automatically in production mode by frameworks like Next.js, or added manually to the PostCSS pipeline) strips whitespace, shortens color values, merges duplicate rules, and otherwise compresses the stylesheet, reducing the already-small JIT output even further before it''s shipped.',null,null,null,'medium',ARRAY['tailwind','performance','minification']::text[],false,107),
('tailwind','performance','tailwind-performance-108','Why does a single shared utility stylesheet from Tailwind tend to cache and compress better than per-component CSS-in-JS output?','A single global CSS file only needs to be downloaded and parsed once per page load (and cached across navigations if unchanged), whereas some CSS-in-JS solutions inject styles per component at runtime, which adds JS execution overhead and can duplicate similar styles across bundles. Tailwind''s highly repetitive class names (many components repeating `flex`, `p-4`, `text-sm`, etc.) also compress extremely well with gzip/brotli, since repeated tokens compress far better than unique, per-component generated class names.',null,null,null,'hard',ARRAY['tailwind','performance','caching']::text[],false,108),
('tailwind','performance','tailwind-performance-109','Before JIT mode existed, why did teams need a separate tool like PurgeCSS alongside Tailwind, and what problem did that introduce?','Early Tailwind versions pre-generated the full utility stylesheet (megabytes of CSS covering every possible class), and PurgeCSS was run as a separate post-processing step to scan templates and strip anything unused before shipping to production. This added build complexity and a real risk of PurgeCSS misconfiguration silently stripping classes that were actually used (e.g. ones built dynamically), a class of bugs that JIT''s scan-based generation model avoids by never generating the unused classes in the first place.',null,null,null,'medium',ARRAY['tailwind','performance','purgecss']::text[],false,109),
('tailwind','performance','tailwind-performance-110','If you add custom CSS via `@layer utilities` or `@layer components`, does it still benefit from Tailwind''s unused-class removal?','Only indirectly — Tailwind''s JIT scanner detects usage of *class names*, so a custom class you define with `@layer` will only be included in the output if that exact class name is found in your `content` files, just like a built-in utility. However, if you write raw CSS outside of `@layer` (plain selectors not gated by class detection), it''s included unconditionally regardless of whether it''s actually used anywhere.',null,null,null,'medium',ARRAY['tailwind','performance','layers']::text[],false,110),
('tailwind','performance','tailwind-performance-111','How do helper libraries like `clsx` and `tailwind-merge` affect the performance and correctness of dynamically composed class strings?','`clsx` efficiently joins conditional class name fragments into one string at a negligible runtime cost (a simple string-building function), while `tailwind-merge` additionally deduplicates conflicting Tailwind utilities (e.g. a default `px-2` overridden by a passed-in `px-4`) so the final DOM class attribute doesn''t contain contradictory classes that could otherwise create unpredictable CSS specificity/order bugs in the browser.',null,'import { twMerge } from ''tailwind-merge'';

twMerge(''px-2 py-1 bg-red-500'', ''px-4 bg-blue-500''); // ''py-1 px-4 bg-blue-500''','javascript','hard',ARRAY['tailwind','performance','class-merging']::text[],false,111),
('tailwind','performance','tailwind-performance-112','How does the breadth of your `content` globs affect Tailwind''s dev-server rebuild speed, separate from production output size?','During development, every file save triggers Tailwind to re-scan the files matched by `content` for class names, so extremely broad globs (like accidentally including `node_modules` or generated build output) slow down every incremental rebuild, not just the final production bundle — keeping globs scoped tightly to actual source/template directories keeps hot-reload feedback fast.',null,null,null,'medium',ARRAY['tailwind','performance','dev-server']::text[],false,112),
('tailwind','performance','tailwind-performance-113','Does relying heavily on arbitrary values (e.g. `w-[137px]`) hurt Tailwind''s output size compared to using the theme''s spacing scale?','Somewhat, in aggregate — a value pulled from the theme scale (like `w-8`) is highly likely to be reused across many components since it''s a shared, finite set, while a one-off arbitrary value like `w-[137px]` is far less likely to be reused elsewhere, so heavy reliance on arbitrary values tends to produce more distinct, non-reused class rules over a large codebase. The effect on total bytes is usually small, but it''s a reason to prefer the design-token scale for values that recur.',null,null,null,'medium',ARRAY['tailwind','performance','arbitrary-values']::text[],false,113),
('tailwind','performance','tailwind-performance-114','What is the Oxide engine in Tailwind CSS v4, and what performance improvement does it bring?','Oxide is Tailwind v4''s new engine, with performance-critical parts (like the class scanner and CSS generator) rewritten in Rust instead of pure JavaScript, resulting in dramatically faster full and incremental builds — Tailwind''s own benchmarks report full rebuilds over 10x faster and incremental rebuilds measured in microseconds rather than milliseconds compared to the v3 JIT engine.',null,null,null,'hard',ARRAY['tailwind','performance','tailwind-v4']::text[],false,114),
('tailwind','performance','tailwind-performance-115','How do modern build tools like Vite integrate with Tailwind to keep dev-server rebuilds fast?','Vite (and similar tools) run Tailwind as a PostCSS plugin inside their own fast, native-code-powered dev pipeline and combine it with hot module replacement, so only the CSS actually affected by a change needs recompiling and re-injecting into the page — this incremental approach, paired with Tailwind''s own fast class-scanning, is what makes utility class edits feel instantaneous during local development compared to full production builds.',null,null,null,'medium',ARRAY['tailwind','performance','build-tools']::text[],false,115),
('tailwind','performance','tailwind-performance-116','Why do repetitive utility class names compress especially well with gzip or brotli compared to more varied hand-written CSS?','Both algorithms work by finding and referencing repeated byte sequences, and Tailwind''s generated CSS is full of highly repetitive patterns — identical property/value pairs reused across many selectors, plus predictable class-name patterns — which compress extremely efficiently, often resulting in a final gzipped/brotli size much smaller than the already-small raw CSS output.',null,null,null,'medium',ARRAY['tailwind','performance','compression']::text[],false,116),
('tailwind','performance','tailwind-performance-117','Is critical/above-the-fold CSS extraction still a meaningful optimization when using Tailwind?','It''s less impactful than with traditional large stylesheets, because Tailwind''s total compiled CSS for a typical app is already small (often tens of KB before compression), so the render-blocking cost of loading the full stylesheet up front is usually negligible compared to sites shipping hundreds of KB of legacy or framework CSS — most teams don''t bother with critical CSS splitting specifically because of this.',null,null,null,'medium',ARRAY['tailwind','performance','critical-css']::text[],false,117),
('tailwind','performance','tailwind-performance-118','Does defining a large number of unused colors or spacing values in `tailwind.config.js` bloat the final CSS output?','No — theme configuration only defines what values are *available* for utilities to reference; actual output size is determined purely by which specific classes are found during content scanning, so a project can define dozens of unused custom colors in `theme.extend.colors` and it will have zero effect on the compiled CSS size as long as no class referencing them appears in the scanned source files.',null,null,null,'hard',ARRAY['tailwind','performance','theme']::text[],false,118),
('tailwind','performance','tailwind-performance-119','Does using the same utility class in hundreds of different components result in duplicate CSS rules in the output?','No — Tailwind deduplicates generated CSS automatically; a class like `flex` is compiled into exactly one rule in the stylesheet no matter how many thousands of times it''s referenced across the codebase, which is precisely why utility reuse (rather than writing new custom classes per component) is what keeps Tailwind''s output small at scale.',null,null,null,'easy',ARRAY['tailwind','performance','deduplication']::text[],false,119),
('tailwind','performance','tailwind-performance-120','How would you check the actual size of your final compiled Tailwind CSS in a project?','Run your project''s production build command and inspect the generated CSS asset''s file size in the build output directory (or your bundler''s build report), then compare the raw size against the gzipped/brotli size for a realistic sense of what''s actually transferred over the network — most well-scoped Tailwind projects end up with a final compiled stylesheet in the tens of kilobytes before compression.',null,null,null,'easy',ARRAY['tailwind','performance','build-analysis']::text[],false,120),
('tailwind','performance','tailwind-performance-121','Why is the Tailwind "Play CDN" script (`<script src="https://cdn.tailwindcss.com">`) explicitly not recommended for production use?','The Play CDN compiles Tailwind''s CSS live in the browser using JavaScript on every page load, which means shipping the entire compiler to the client, scanning the DOM at runtime, and generating styles on the fly — dramatically slower and heavier than a small, pre-built static stylesheet from a proper build step. It''s intended purely for quick prototypes, demos, or documentation examples, not real applications.',null,null,null,'medium',ARRAY['tailwind','performance','play-cdn']::text[],true,121),
('tailwind','performance','tailwind-performance-122','How does Tailwind''s content scanning behave in a monorepo with a shared component library used by multiple apps?','Each app''s `content` config needs to include the glob paths of the shared component library''s source files (not just its own app-specific files), otherwise classes used only inside shared components won''t be detected and will be missing from that app''s generated CSS — a common gotcha is scanning the library''s compiled `dist` output instead of (or in addition to) its source, which can miss classes if the build process transforms or strips them.',null,null,null,'hard',ARRAY['tailwind','performance','monorepo']::text[],false,122),
('tailwind','performance','tailwind-performance-123','Does extracting utility combinations into `@apply`-based classes meaningfully reduce the final compiled CSS size?','Not meaningfully in most cases — because Tailwind already deduplicates identical utility rules regardless of how many elements reference them, wrapping a set of utilities in a named `@apply` class doesn''t reduce the number of underlying CSS declarations generated; the main benefit of `@apply` is reducing repetition in your *markup/JS source*, not shrinking the compiled CSS output.',null,null,null,'medium',ARRAY['tailwind','performance','apply']::text[],false,123),
('tailwind','performance','tailwind-performance-124','Can having multiple `@tailwind` or `@import "tailwindcss"` directives across different CSS files in a project cause build performance issues?','Yes — each import triggers Tailwind to run its full scanning and generation pipeline again, so duplicating the directive across multiple entry stylesheets (rather than importing Tailwind once from a single root stylesheet and importing that root everywhere else) can meaningfully slow down both dev rebuilds and production builds, and often causes duplicate CSS output as an added downside.',null,null,null,'medium',ARRAY['tailwind','performance','build-tools']::text[],false,124),
('tailwind','performance','tailwind-performance-125','Do responsive (`md:`) and state (`hover:`) variants multiply the size of the generated CSS significantly?','Each distinct variant+utility combination you actually use generates its own rule, so heavy use of many different variant combinations does add some CSS, but only for combinations that are genuinely referenced in your source — Tailwind never pre-generates the full cartesian product of every utility against every variant, which is exactly why JIT mode keeps output proportional to real usage rather than growing combinatorially.',null,null,null,'medium',ARRAY['tailwind','performance','variants']::text[],false,125),
('tailwind','performance','tailwind-performance-126','What is a practical checklist for keeping a Tailwind project''s production CSS lean over time?','Keep `content` globs accurate and scoped to real template/component files (including any shared package sources), avoid dynamically constructing class name strings without a lookup map or safelist, favor the theme''s spacing/color scale over arbitrary one-off values where a value recurs, avoid the Play CDN in production, and periodically check the built CSS asset size to catch regressions like an accidentally over-broad `content` path or an unintended global scan of `node_modules`.',null,null,null,'medium',ARRAY['tailwind','performance','best-practices']::text[],false,126),
('tailwind','performance','tailwind-performance-127','How does the `safelist` config option affect final CSS size, and what''s the tradeoff of over-using it?','Every entry (or regex pattern) in `safelist` forces Tailwind to generate that class unconditionally, bypassing content scanning entirely, so overusing broad safelist patterns (like a wide regex matching many color/shade combinations "just in case") can quietly reintroduce a large amount of unused CSS that JIT mode was specifically designed to avoid — it should be reserved for the minimum set of genuinely dynamic classes that can''t be expressed as literal strings.',null,null,null,'hard',ARRAY['tailwind','performance','safelist']::text[],false,127),
('tailwind','performance','tailwind-performance-128','Why is it recommended to avoid loading Tailwind''s full default color palette or icon-heavy plugins in a project that only ever uses a handful of them?','Since unused theme values never make it into the output regardless of how large the theme object is, this isn''t actually a real CSS-size concern — the only place it matters is config/IDE performance (a very large theme can slightly slow down editor autocomplete and config resolution) and conceptual clarity for the team, not the shipped bundle, which stays driven purely by what''s detected in `content`.',null,null,null,'hard',ARRAY['tailwind','performance','theme']::text[],false,128),
('tailwind','performance','tailwind-performance-129','Does enabling the global `important: true` config option have any effect on CSS output size or generation performance?','It has a negligible effect on file size — it just appends `!important` to each already-generated declaration — but it doesn''t change how many classes get generated or how they''re detected, so it has essentially no measurable impact on build performance or final bundle size; its cost is more about specificity predictability and cascade debuggability than performance.',null,null,null,'easy',ARRAY['tailwind','performance','important']::text[],false,129),
('tailwind','performance','tailwind-performance-130','In Tailwind v4''s CSS-first `@theme` configuration, do unused custom properties still get shipped in the final CSS?','By default, `@theme` tokens are exposed as real CSS custom properties (`:root` variables) so they''re available for runtime use (e.g. in arbitrary values or plain CSS), which means, unlike JS-config theme values, they are emitted regardless of whether every single one is referenced by a utility class — though this is typically a small, fixed cost since it''s one variable declaration per token rather than per usage, and can be scoped down further with `@theme inline` if needed.',null,null,null,'hard',ARRAY['tailwind','performance','tailwind-v4']::text[],false,130),
('tailwind','responsive-states','tailwind-responsive-states-101','What does "mobile-first" mean in Tailwind''s responsive design system, and how do the breakpoint prefixes work?','Unstyled (unprefixed) utilities apply at all screen sizes as the base style, and breakpoint prefixes like `md:` or `lg:` apply as `min-width` media queries that override the base style from that width upward. This means you design for the smallest screen first and layer on adjustments for larger viewports, rather than starting from desktop and overriding down.',null,'<div class="text-sm md:text-base lg:text-lg">Responsive text</div>','html','easy',ARRAY['tailwind','responsive','breakpoints']::text[],true,101),
('tailwind','responsive-states','tailwind-responsive-states-102','How would you make a sidebar hidden on small screens but visible as a flex container from the `md` breakpoint up?','You apply the base utility for the smallest screens and prefix the override with the breakpoint at which it should change — `hidden` as the default and `md:flex` to switch to a flex layout once the viewport reaches the `md` breakpoint, since `md:` only adds its declarations at `min-width: 768px` and above.',null,'<aside class="hidden md:flex md:w-64 md:flex-col">Sidebar</aside>','html','easy',ARRAY['tailwind','responsive','breakpoints']::text[],false,102),
('tailwind','responsive-states','tailwind-responsive-states-103','How do you apply a style only within a specific range of viewport widths, such as only between `md` and `lg`?','Tailwind''s named breakpoints are min-width only, so to cap the upper end you combine a `min-*` (or default) breakpoint prefix with a `max-*` variant, e.g. `md:max-lg:flex-row` applies only from `md` up to just below `lg`. In Tailwind v4, `max-*` variants for every breakpoint are built in for exactly this use case.',null,'<div class="flex-col md:max-lg:flex-row lg:flex-col">Range-specific layout</div>','html','medium',ARRAY['tailwind','responsive','breakpoints']::text[],false,103),
('tailwind','responsive-states','tailwind-responsive-states-104','How do you style an element''s hover state with Tailwind?','Prefix any utility with `hover:` and it''s compiled into a CSS rule scoped to the `:hover` pseudo-class, so `hover:bg-blue-700` only applies when the pointer is over that specific element. Multiple hover utilities can be combined freely, just like any other variant.',null,'<button class="bg-blue-600 hover:bg-blue-700 transition-colors">Hover me</button>','html','easy',ARRAY['tailwind','responsive','hover']::text[],true,104),
('tailwind','responsive-states','tailwind-responsive-states-105','What is the difference between the `focus:` and `focus-visible:` variants?','`focus:` maps to the `:focus` pseudo-class and applies whenever an element has focus, whether reached by mouse click or keyboard. `focus-visible:` maps to `:focus-visible`, which browsers only trigger when focus should be visibly indicated — typically keyboard navigation — so it avoids showing a focus ring on a mouse click while still supporting keyboard accessibility.',null,'<button class="focus-visible:ring-2 focus-visible:ring-blue-500 focus:outline-none">Accessible focus</button>','html','medium',ARRAY['tailwind','responsive','focus']::text[],false,105),
('tailwind','responsive-states','tailwind-responsive-states-106','What does the `active:` variant do, and how does it differ from `focus:`?','`active:` maps to the `:active` pseudo-class, which applies only while the element is being actively pressed (e.g. mouse button down or during a tap), and stops applying the moment it''s released. `focus:` persists as long as the element retains keyboard/programmatic focus, which can be much longer than the brief active press state.',null,'<button class="bg-indigo-600 active:bg-indigo-800">Press me</button>','html','medium',ARRAY['tailwind','responsive','active']::text[],false,106),
('tailwind','responsive-states','tailwind-responsive-states-107','How does the `disabled:` variant work, and what selector does it target?','`disabled:` compiles to a rule scoped with `:disabled`, so it applies automatically to form elements like `<button>` or `<input>` that have the HTML `disabled` attribute set, letting you style dimmed backgrounds, a not-allowed cursor, or muted text without writing any conditional class logic in JavaScript.',null,'<button disabled class="bg-gray-300 text-gray-500 disabled:cursor-not-allowed disabled:opacity-60">Submit</button>','html','medium',ARRAY['tailwind','responsive','disabled']::text[],false,107),
('tailwind','responsive-states','tailwind-responsive-states-108','How do you style an element differently in dark mode using the `dark:` variant?','Once `darkMode` is configured (either `media`, following OS preference, or `class`/`selector`, toggled manually), prefixing a utility with `dark:` scopes it to apply only when dark mode is active, letting you pair a light-mode value with its dark-mode counterpart directly in the markup, e.g. `bg-white dark:bg-gray-900`.',null,'<div class="bg-white text-gray-900 dark:bg-gray-900 dark:text-gray-100">Themed card</div>','html','easy',ARRAY['tailwind','responsive','dark-mode']::text[],true,108),
('tailwind','responsive-states','tailwind-responsive-states-109','Can you stack the `dark:` variant with a state variant like `hover:`, and what would that look like?','Yes — variants can be freely chained in any order, such as `dark:hover:bg-gray-700`, which only applies the background when the element is both hovered and dark mode is active, letting you fine-tune interactive states independently per theme.',null,'<button class="hover:bg-gray-100 dark:hover:bg-gray-700">Themed hover</button>','html','medium',ARRAY['tailwind','responsive','dark-mode']::text[],false,109),
('tailwind','responsive-states','tailwind-responsive-states-110','What do the `first:`, `last:`, `odd:`, and `even:` variants target?','`first:` and `last:` map to `:first-child`/`:last-child`, and `odd:`/`even:` map to `:nth-child(odd)`/`:nth-child(even)`, which together are commonly used to style table rows with alternating background colors or to remove a border only on the last item in a list without adding extra wrapper markup.',null,'<ul>
  <li class="border-b last:border-b-0 odd:bg-gray-50">Row</li>
</ul>','html','medium',ARRAY['tailwind','responsive','pseudo-classes']::text[],false,110),
('tailwind','responsive-states','tailwind-responsive-states-111','How would you style the placeholder text color of an input using Tailwind?','The `placeholder:` variant targets the `::placeholder` pseudo-element, letting you apply text color, opacity, or other typography utilities specifically to placeholder text without affecting the actual entered value''s styling.',null,'<input placeholder="Search..." class="placeholder:text-gray-400 placeholder:italic" />','html','medium',ARRAY['tailwind','responsive','placeholder']::text[],false,111),
('tailwind','responsive-states','tailwind-responsive-states-112','What does the `file:` variant let you style, and why is it needed?','The `file:` variant targets the `::file-selector-button` pseudo-element, which is the browser-generated "Choose file" button inside a `<input type="file">` element. Since that button isn''t a normal element you can select with a class, Tailwind needs this dedicated variant to restyle it consistently across browsers.',null,'<input type="file" class="file:mr-4 file:rounded file:border-0 file:bg-blue-50 file:px-4 file:py-2 file:text-blue-700" />','html','medium',ARRAY['tailwind','responsive','forms']::text[],false,112),
('tailwind','responsive-states','tailwind-responsive-states-113','What are `motion-safe:` and `motion-reduce:` variants used for?','They map to the `prefers-reduced-motion` media query, letting you conditionally apply animation/transition utilities only when the user hasn''t requested reduced motion (`motion-safe:`) or apply alternate, simplified styles when they have (`motion-reduce:`), which is important for accessibility compliance for users sensitive to motion.',null,'<div class="motion-safe:animate-bounce motion-reduce:animate-none">Notification</div>','html','hard',ARRAY['tailwind','responsive','accessibility']::text[],false,113),
('tailwind','responsive-states','tailwind-responsive-states-114','What does the `print:` variant do?','`print:` scopes a utility to the `@media print` query, so you can hide navigation/interactive chrome or adjust colors/spacing specifically for a printed version of the page, e.g. `print:hidden` on a sidebar or `print:text-black` to force readable ink colors.',null,'<nav class="print:hidden">Site navigation</nav>','html','easy',ARRAY['tailwind','responsive','print']::text[],false,114),
('tailwind','responsive-states','tailwind-responsive-states-115','How do ARIA state variants like `aria-checked:` or `aria-expanded:` work in Tailwind?','These variants target elements based on their ARIA attribute value, e.g. `aria-expanded:rotate-180` applies only when the element has `aria-expanded="true"`, which is especially useful for accessible custom components (accordions, comboboxes) where visual state should stay in sync with the ARIA state that assistive technology reads, rather than a separate class toggled by JavaScript.',null,'<button aria-expanded="true" class="transition-transform aria-expanded:rotate-180">
  <svg>...</svg>
</button>','html','hard',ARRAY['tailwind','responsive','aria']::text[],false,115),
('tailwind','responsive-states','tailwind-responsive-states-116','How do data-attribute variants like `data-[state=open]:` work in Tailwind?','Data attribute variants let you style an element conditionally based on the value of a `data-*` attribute using arbitrary variant syntax, which is extremely common when integrating with headless UI libraries (Radix, Headless UI) that expose component state through data attributes like `data-state="open"` instead of classes.',null,'<div data-state="open" class="hidden data-[state=open]:block">Panel content</div>','html','hard',ARRAY['tailwind','responsive','data-attributes']::text[],false,116),
('tailwind','responsive-states','tailwind-responsive-states-117','What does the `has-*` variant do, and what CSS feature does it rely on?','`has-[selector]:` maps to the modern CSS `:has()` relational pseudo-class, letting a parent element style itself based on the state of its descendants — for example, highlighting a label when its nested checkbox is checked — without needing a sibling-only `peer` setup or JavaScript. It requires a browser that supports `:has()`.',null,'<label class="has-[:checked]:bg-blue-50 has-[:checked]:ring-1 has-[:checked]:ring-blue-400">
  <input type="checkbox" class="mr-2" /> Option
</label>','html','hard',ARRAY['tailwind','responsive','has-variant']::text[],false,117),
('tailwind','responsive-states','tailwind-responsive-states-118','Besides `group-hover:`, what other `group-*` state variants are commonly available?','Any state variant Tailwind supports can be combined with `group-`, including `group-focus:`, `group-active:`, `group-disabled:`, and `group-invalid:`, all of which react to the parent element (marked with `group`) reaching that pseudo-class/attribute state, not the element they''re applied to.',null,null,null,'medium',ARRAY['tailwind','responsive','group']::text[],false,118),
('tailwind','responsive-states','tailwind-responsive-states-119','How would you style a custom label based on whether its sibling checkbox is checked, using `peer-checked:`?','Mark the checkbox input with `peer`, then any following sibling element can use `peer-checked:` to react to that checkbox''s `:checked` state, which is the standard CSS-only pattern for building toggle switches or custom checkboxes without JavaScript.',null,'<input type="checkbox" class="peer sr-only" />
<div class="h-6 w-11 rounded-full bg-gray-300 peer-checked:bg-green-500"></div>','html','medium',ARRAY['tailwind','responsive','peer']::text[],false,119),
('tailwind','responsive-states','tailwind-responsive-states-120','How do you disambiguate multiple nested `group` or `peer` elements so a variant targets the right ancestor?','You can name a group or peer with `group/{name}` (or `peer/{name}`) and then reference that specific name in the variant, like `group-hover/card:opacity-100`, which prevents the wrong nested group from accidentally triggering the style when groups are nested inside each other.',null,'<div class="group/card">
  <div class="group/icon">
    <span class="opacity-0 group-hover/card:opacity-100">Card hover</span>
  </div>
</div>','html','hard',ARRAY['tailwind','responsive','group']::text[],false,120),
('tailwind','responsive-states','tailwind-responsive-states-121','In what order can responsive, dark mode, and state variants be stacked, e.g. `dark:md:hover:bg-gray-700`?','Tailwind allows variants to be chained in any order you write them, and it resolves to the same final selector regardless of the order in the class name — internally Tailwind sorts and generates the equivalent nested media query and pseudo-class combination. Convention favors a consistent order (often responsive, then dark, then state) purely for team readability, not because the order changes behavior.',null,'<button class="bg-gray-100 dark:md:hover:bg-gray-700">Multi-variant</button>','html','medium',ARRAY['tailwind','responsive','variants']::text[],false,121),
('tailwind','responsive-states','tailwind-responsive-states-122','How do arbitrary variants work, such as `[&:nth-child(3)]:bg-red-500`?','Arbitrary variant syntax lets you write a raw CSS selector fragment inside square brackets, with `&` representing the element itself, so you can target selectors Tailwind doesn''t ship a named variant for (like a specific `:nth-child`, a sibling combinator, or a custom attribute selector) without writing separate custom CSS.',null,'<li class="[&:nth-child(3)]:font-bold">Third item</li>','html','hard',ARRAY['tailwind','responsive','arbitrary-variants']::text[],false,122),
('tailwind','responsive-states','tailwind-responsive-states-123','What does the `not-` prefix do when combined with another variant, like `not-disabled:hover:bg-blue-700`?','`not-` negates the following pseudo-class or feature check, compiling to a `:not()` wrapper, so `not-disabled:hover:` only applies the hover style when the element is NOT disabled — useful for making sure a hover effect doesn''t visually apply (or apply inconsistently) on elements that are currently disabled.',null,'<button class="not-disabled:hover:bg-blue-700">Hover-aware button</button>','html','hard',ARRAY['tailwind','responsive','variants']::text[],false,123),
('tailwind','responsive-states','tailwind-responsive-states-124','What does the `supports-[...]` variant do?','`supports-[property:value]:` wraps the utility in an `@supports` feature query, applying it only in browsers that support the given CSS feature — handy for progressively enhancing a layout with something like `backdrop-filter` while providing a sane fallback for browsers that don''t support it.',null,'<div class="bg-white supports-[backdrop-filter]:bg-white/60 supports-[backdrop-filter]:backdrop-blur">Panel</div>','html','hard',ARRAY['tailwind','responsive','feature-queries']::text[],false,124),
('tailwind','responsive-states','tailwind-responsive-states-125','What does the `visited:` variant style, and where is it typically used?','`visited:` maps to the `:visited` pseudo-class and applies only to `<a>` elements the user has already navigated to, most commonly used to change a visited link''s text color, though browsers restrict which CSS properties can actually be styled via `:visited` for privacy reasons.',null,'<a href="/docs" class="text-blue-600 visited:text-purple-600">Documentation</a>','html','medium',ARRAY['tailwind','responsive','links']::text[],false,125),
('tailwind','responsive-states','tailwind-responsive-states-126','What''s the difference between `focus:` and `focus-within:`?','`focus:` only applies when the exact element it''s on receives focus. `focus-within:` applies to an element when it or ANY of its descendants has focus, which is useful for highlighting an entire form field wrapper (border, background) when the input inside it is focused, without needing the wrapper itself to be focusable.',null,'<div class="border focus-within:border-blue-500">
  <input class="outline-none" />
</div>','html','medium',ARRAY['tailwind','responsive','focus']::text[],false,126),
('tailwind','responsive-states','tailwind-responsive-states-127','How do form validation state variants like `required:`, `invalid:`, and `valid:` work?','These map directly to the native `:required`, `:invalid`, and `:valid` CSS pseudo-classes on form elements, letting you style a field''s border or background based on HTML5 constraint validation state (like a required field, or a `type="email"` input with malformed content) without writing any custom validation-state JavaScript.',null,'<input type="email" required class="border-gray-300 invalid:border-red-500 valid:border-green-500" />','html','easy',ARRAY['tailwind','responsive','forms']::text[],false,127),
('tailwind','responsive-states','tailwind-responsive-states-128','What are container queries in Tailwind, and how do they differ from the standard responsive breakpoint variants?','Container queries (via the `@container` utility on a wrapper and `@sm:`/`@md:`-style variants on children, built into Tailwind v4 and available as a plugin in v3) style an element based on the size of its nearest container rather than the viewport, which is essential for components that need to adapt their layout regardless of where they''re placed on the page — a sidebar widget versus the same component in a full-width main area.',null,'<div class="@container">
  <div class="flex flex-col @md:flex-row">Card content</div>
</div>','html','hard',ARRAY['tailwind','responsive','container-queries']::text[],false,128),
('tailwind','responsive-states','tailwind-responsive-states-129','What does the `selection:` variant style?','`selection:` targets the `::selection` pseudo-element, letting you customize the background and text color of text the user highlights with their cursor, which is a nice branding touch that''s otherwise awkward to apply consistently without a global CSS rule.',null,'<p class="selection:bg-yellow-200 selection:text-black">Try selecting this text.</p>','html','medium',ARRAY['tailwind','responsive','selection']::text[],false,129),
('tailwind','responsive-states','tailwind-responsive-states-130','How do you add pseudo-element content using the `before:` and `after:` variants?','`before:` and `after:` generate `::before`/`::after` pseudo-elements, and because CSS requires a `content` property for them to render at all, Tailwind requires you to also set the `content-[''...'']` utility (or `content-none`) alongside them, after which normal utilities can style that pseudo-element''s positioning, color, and size.',null,'<span class="relative before:absolute before:-left-3 before:content-[''*''] before:text-red-500">Required field</span>','html','easy',ARRAY['tailwind','responsive','pseudo-elements']::text[],false,130),
('tailwind','responsive-states','tailwind-responsive-states-131','Why does `md:text-lg` also apply at the `lg` and `xl` breakpoints, and how would you make it apply only at `md`?','Because responsive prefixes are `min-width` media queries, `md:text-lg` means "apply from `md` upward," so it stays in effect at every larger breakpoint unless a later, larger prefix (like `lg:text-xl`) overrides it. To scope a style to *only* the `md` range, pair it with a `max-*` variant, e.g. `md:max-lg:text-lg`.',null,null,null,'medium',ARRAY['tailwind','responsive','breakpoints']::text[],false,131),
('tailwind','responsive-states','tailwind-responsive-states-132','How would you hide an element only below a custom, one-off breakpoint without adding a new named breakpoint to the config?','You can use an arbitrary value inside the `max-` variant, such as `max-[850px]:hidden`, which generates a one-off `max-width: 850px` media query inline without needing to register a permanent named breakpoint in `tailwind.config.js` — useful for a single component with an unusual layout threshold.',null,'<div class="max-[850px]:hidden">Wide-screen-only content</div>','html','hard',ARRAY['tailwind','responsive','arbitrary-values']::text[],false,132),
('nextjs','api-routes','nextjs-api-routes-101','What is a Route Handler in the App Router, and how do you create one?','A Route Handler is a server-side function that handles raw HTTP requests for a given route, defined by adding a `route.js`/`route.ts` file inside an `app/` folder and exporting async functions named after HTTP methods like `GET`, `POST`, `PUT`, `DELETE`, or `PATCH`. Unlike a page, it returns a `Response` object directly instead of rendering React UI, making it the App Router''s equivalent of an API endpoint.',null,'// app/api/hello/route.js
export async function GET() {
  return Response.json({ message: ''Hello'' });
}','javascript','easy',ARRAY['nextjs','api-routes','route-handlers']::text[],true,101),
('nextjs','api-routes','nextjs-api-routes-102','How did API routes work in the Pages Router, before Route Handlers existed?','Any file placed under `pages/api/` became an API endpoint automatically, exporting a single default handler function that received Node-style `req` and `res` objects and was responsible for branching on `req.method` itself and calling `res.status(...).json(...)` to respond, rather than exporting separate functions per HTTP method.',null,'// pages/api/hello.js
export default function handler(req, res) {
  if (req.method === ''GET'') {
    res.status(200).json({ message: ''Hello'' });
  }
}','javascript','easy',ARRAY['nextjs','api-routes','pages-router']::text[],false,102),
('nextjs','api-routes','nextjs-api-routes-103','How do you read the request body (e.g. JSON) inside a Route Handler?','The handler receives a standard Web `Request` object, so you call its built-in methods to read the body: `await request.json()` for JSON payloads, `await request.formData()` for form submissions, or `await request.text()` for raw text — the same API you''d use with `fetch` on the client, rather than a Node-specific stream API.',null,'export async function POST(request) {
  const body = await request.json();
  return Response.json({ received: body });
}','javascript','easy',ARRAY['nextjs','api-routes','route-handlers']::text[],true,103),
('nextjs','api-routes','nextjs-api-routes-104','How do you access dynamic route segments (like an `[id]` param) inside a Route Handler?','Route Handlers receive a second argument, commonly destructured as `{ params }`, containing the values matched by dynamic segments in the route''s folder path — the same way `params` is passed to `page.js` components.',null,'// app/api/users/[id]/route.js
export async function GET(request, { params }) {
  const user = await getUser(params.id);
  return Response.json(user);
}','javascript','easy',ARRAY['nextjs','api-routes','route-handlers','dynamic-routes']::text[],false,104),
('nextjs','api-routes','nextjs-api-routes-105','How do you read query string parameters inside a Route Handler?','Construct a `URL` object from `request.url` and use its `searchParams` property, since the Route Handler''s `request` argument is a standard Web `Request` with just the raw URL string, not a pre-parsed query object like the Pages Router''s `req.query`.',null,'export async function GET(request) {
  const { searchParams } = new URL(request.url);
  const query = searchParams.get(''q'');
  return Response.json({ query });
}','javascript','easy',ARRAY['nextjs','api-routes','route-handlers']::text[],false,105),
('nextjs','api-routes','nextjs-api-routes-106','Can a Route Handler and a `page.js` file coexist in the same route segment?','No — a folder can define either a `page.js` (rendering a UI page) or a `route.js` (handling raw requests) at the same route path, but not both, since Next.js needs an unambiguous single handler for a given URL and HTTP method combination.',null,null,null,'easy',ARRAY['nextjs','api-routes','route-handlers']::text[],false,106),
('nextjs','api-routes','nextjs-api-routes-107','How do Route Handlers handle GET request caching by default, and how is this different from POST/PUT/DELETE?','In earlier Next.js versions, `GET` Route Handlers were cached by default similar to static fetches unless they used dynamic APIs; in current versions, Route Handlers are not cached by default and are treated as dynamic. Handlers for `POST`, `PUT`, `PATCH`, and `DELETE` are never cached in any version, since they represent mutations rather than idempotent reads.',null,null,null,'medium',ARRAY['nextjs','api-routes','route-handlers','caching']::text[],false,107),
('nextjs','api-routes','nextjs-api-routes-108','How do you set custom response headers or a specific status code from a Route Handler?','Pass a second argument to `Response.json()` (or construct a plain `new Response(...)`) with `status` and `headers` fields, since Route Handlers work with the standard Web `Response` constructor rather than an Express-style `res` object with chained methods.',null,'return Response.json({ error: ''Not found'' }, { status: 404, headers: { ''X-Custom'': ''value'' } });','javascript','easy',ARRAY['nextjs','api-routes','route-handlers']::text[],false,108),
('nextjs','api-routes','nextjs-api-routes-109','What is Next.js Middleware, and where does it run relative to a request?','Middleware is code defined in a `middleware.js`/`middleware.ts` file at the project root that runs before a request completes, intercepting requests matched by its `config.matcher` before they reach a route, Route Handler, or static asset. It runs in the lightweight Edge Runtime, letting you rewrite, redirect, modify headers, or respond early based on things like cookies or geolocation, without touching the destination route''s own code.',null,'// middleware.js
export function middleware(request) {
  if (!request.cookies.get(''session'')) {
    return Response.redirect(new URL(''/login'', request.url));
  }
}

export const config = { matcher: ''/dashboard/:path*'' };','javascript','medium',ARRAY['nextjs','api-routes','middleware']::text[],true,109),
('nextjs','api-routes','nextjs-api-routes-110','What is a common real-world use case for Middleware, such as authentication gating?','A typical pattern is checking for a valid session cookie or auth token in Middleware before allowing access to protected routes (like `/dashboard/*`), redirecting unauthenticated users to a login page before any page or Route Handler code even runs — centralizing that check in one place instead of repeating it in every protected route.',null,null,null,'medium',ARRAY['nextjs','api-routes','middleware','authentication']::text[],true,110),
('nextjs','api-routes','nextjs-api-routes-111','Why does Middleware run in the Edge Runtime rather than the standard Node.js runtime, and what limitation does that impose?','Middleware needs to run extremely fast on every matched request, often before the actual route even executes, so it uses the lightweight Edge Runtime for low cold-start latency at globally distributed locations. This means Middleware can''t use full Node.js APIs (like the `fs` module or many npm packages that depend on Node internals), limiting it mostly to logic based on headers, cookies, and simple redirects/rewrites.',null,null,null,'medium',ARRAY['nextjs','api-routes','middleware','edge']::text[],false,111),
('nextjs','api-routes','nextjs-api-routes-112','How do you scope Middleware to only run on specific paths?','Export a `config` object with a `matcher` property from `middleware.js` — it accepts a path string, an array of path patterns (supporting basic wildcards), or a more detailed matcher object, restricting which requests trigger the middleware instead of running it on literally every request including static assets.',null,'export const config = {
  matcher: [''/dashboard/:path*'', ''/settings/:path*''],
};','javascript','easy',ARRAY['nextjs','api-routes','middleware']::text[],false,112),
('nextjs','api-routes','nextjs-api-routes-113','What is the difference between a redirect and a rewrite performed from Middleware?','`NextResponse.redirect(url)` sends the browser a redirect response, changing the URL the user sees. `NextResponse.rewrite(url)` serves content from a different internal path while keeping the original URL displayed in the browser unchanged — useful for things like A/B testing or serving different content based on a cookie without altering the visible URL.',null,'import { NextResponse } from ''next/server'';

export function middleware(request) {
  return NextResponse.rewrite(new URL(''/variant-b'', request.url));
}','javascript','medium',ARRAY['nextjs','api-routes','middleware']::text[],false,113),
('nextjs','api-routes','nextjs-api-routes-114','How do you handle CORS for a Route Handler that needs to be called from a different origin?','Set the appropriate `Access-Control-Allow-Origin` (and related) headers on the `Response` you return from your `GET`/`POST` handlers, and additionally export an `OPTIONS` handler to respond to the browser''s CORS preflight request, since Next.js doesn''t apply CORS headers automatically.',null,'export async function GET() {
  return Response.json({ ok: true }, {
    headers: { ''Access-Control-Allow-Origin'': ''https://example.com'' },
  });
}','javascript','medium',ARRAY['nextjs','api-routes','route-handlers','cors']::text[],false,114),
('nextjs','api-routes','nextjs-api-routes-115','Can Route Handlers use the full Node.js runtime instead of the Edge Runtime, and how do you choose?','Yes — Route Handlers default to the Node.js runtime unless you explicitly export `export const runtime = ''edge''`, which gives access to full Node APIs (file system, many npm packages) at the cost of higher cold-start latency and no global edge distribution. Choose Edge for latency-sensitive, lightweight logic and Node.js for anything needing broader library compatibility.',null,'export const runtime = ''edge''; // or ''nodejs'' (default)','javascript','medium',ARRAY['nextjs','api-routes','route-handlers','edge']::text[],false,115),
('nextjs','api-routes','nextjs-api-routes-116','How do you validate and parse incoming request data safely in a Route Handler?','A common pattern is to parse the JSON body and then run it through a schema validation library like Zod, returning a 400 response with validation errors if it fails, rather than trusting the shape of `request.json()` directly, which is untyped and could be malformed or malicious.',null,'import { z } from ''zod'';

const schema = z.object({ email: z.string().email() });

export async function POST(request) {
  const result = schema.safeParse(await request.json());
  if (!result.success) return Response.json({ error: ''Invalid'' }, { status: 400 });
  // proceed with result.data
}','typescript','medium',ARRAY['nextjs','api-routes','route-handlers','validation']::text[],false,116),
('nextjs','api-routes','nextjs-api-routes-117','How would you implement rate limiting for a Next.js API endpoint?','Common approaches include using Middleware (or the Route Handler itself) to check a request counter stored in an external store like Redis, keyed by IP address or user ID, rejecting requests once a threshold is exceeded within a time window — Next.js has no built-in rate limiting, so it''s typically implemented with a library (e.g. Upstash''s rate limit SDK) or a reverse proxy/CDN feature in front of the app.',null,null,null,'hard',ARRAY['nextjs','api-routes','middleware','security']::text[],false,117),
('nextjs','api-routes','nextjs-api-routes-118','What is the difference between calling a Server Action and calling a Route Handler from a form submission?','A Server Action can be wired directly to a form''s `action` prop and invoked without you writing any client-side fetch code or manually managing an endpoint URL, with Next.js handling the request/response plumbing. A Route Handler requires you to manually `fetch()` it from client code (or point a plain HTML form''s `action`/`method` at its URL) and handle the response yourself, which is more explicit but also more boilerplate.',null,null,null,'medium',ARRAY['nextjs','api-routes','server-actions','route-handlers']::text[],false,118),
('nextjs','api-routes','nextjs-api-routes-119','How do you stream a response from a Route Handler, such as for a chat completion API?','Return a `Response` constructed with a `ReadableStream` as its body, writing chunks to the stream''s controller as data becomes available; libraries like the Vercel AI SDK wrap this pattern for streaming LLM responses so you don''t have to hand-write the stream plumbing yourself.',null,'export async function POST() {
  const stream = new ReadableStream({
    async start(controller) {
      controller.enqueue(new TextEncoder().encode(''chunk''));
      controller.close();
    },
  });
  return new Response(stream);
}','javascript','hard',ARRAY['nextjs','api-routes','route-handlers','streaming']::text[],false,119),
('nextjs','api-routes','nextjs-api-routes-120','How do you access and set cookies from within a Route Handler?','Call `cookies()` from `next/headers` inside the handler to read incoming cookies with `.get()`, or to set a cookie on the response with `.set()`; alternatively, you can build a `Response` and set the `Set-Cookie` header manually if you need finer control.',null,'import { cookies } from ''next/headers'';

export async function POST() {
  cookies().set(''session'', ''abc123'', { httpOnly: true });
  return Response.json({ ok: true });
}','javascript','medium',ARRAY['nextjs','api-routes','route-handlers','cookies']::text[],false,120),
('nextjs','api-routes','nextjs-api-routes-121','What HTTP methods can a single `route.js` file export handlers for, and what happens for an unsupported method?','A `route.js` can export any combination of `GET`, `POST`, `PUT`, `PATCH`, `DELETE`, `HEAD`, and `OPTIONS`. If a request arrives using a method with no corresponding exported function, Next.js automatically responds with a `405 Method Not Allowed`, without you having to write that branching logic yourself.',null,null,null,'easy',ARRAY['nextjs','api-routes','route-handlers']::text[],false,121),
('nextjs','api-routes','nextjs-api-routes-122','How would you protect a Route Handler so only authenticated requests can access it?','Inside the handler, read the session/token (from cookies via `cookies()`, or an `Authorization` header via `request.headers.get(...)`), verify it against your auth system, and return a `401` response early if it''s missing or invalid — or handle this more globally by gating the route''s path pattern in Middleware before the handler even runs.',null,'export async function GET(request) {
  const auth = request.headers.get(''authorization'');
  if (!isValidToken(auth)) return Response.json({ error: ''Unauthorized'' }, { status: 401 });
  // proceed
}','javascript','medium',ARRAY['nextjs','api-routes','route-handlers','authentication']::text[],false,122),
('nextjs','api-routes','nextjs-api-routes-123','Can Middleware read or modify the request body?','No — Middleware runs before the request body is available to be read in a meaningful way for most use cases, and it''s designed around lightweight decisions based on the URL, headers, and cookies rather than body inspection; body parsing and validation should happen in the destination Route Handler or Server Action instead.',null,null,null,'medium',ARRAY['nextjs','api-routes','middleware']::text[],false,123),
('nextjs','api-routes','nextjs-api-routes-124','How do dynamic segments interact with catch-all routes inside `app/api`, for example to build a proxy endpoint?','A folder like `app/api/proxy/[...path]/route.js` captures the remainder of the URL path as an array in `params.path`, letting a single handler forward arbitrary nested paths to another service — for example, reconstructing the target URL by joining `params.path` and forwarding the incoming request to it.',null,'// app/api/proxy/[...path]/route.js
export async function GET(request, { params }) {
  const target = `https://upstream.example.com/${params.path.join(''/'')}`;
  const res = await fetch(target);
  return new Response(res.body, { status: res.status });
}','javascript','hard',ARRAY['nextjs','api-routes','route-handlers','dynamic-routes']::text[],false,124),
('nextjs','api-routes','nextjs-api-routes-125','What''s the difference between throwing an error in a Route Handler versus in a page''s server component, in terms of what the client receives?','An uncaught error in a page''s server component is caught by the nearest `error.js` boundary and rendered as an error UI page. An uncaught error in a Route Handler results in a generic 500 response with no HTML error boundary involved, since the handler''s job is to return a raw response — so Route Handlers typically wrap logic in try/catch and return explicit error responses rather than relying on error boundaries.',null,null,null,'medium',ARRAY['nextjs','api-routes','route-handlers','error-handling']::text[],false,125),
('nextjs','api-routes','nextjs-api-routes-126','How would you implement a webhook endpoint (e.g. for Stripe) as a Next.js Route Handler, and what''s a key security consideration?','Create a `route.js` exporting a `POST` handler that reads the raw request body and the signature header the provider sends, then verifies the payload''s signature using the provider''s SDK before trusting the data — this is critical because webhook endpoints are publicly reachable URLs, so signature verification prevents forged requests from triggering your business logic.',null,'export async function POST(request) {
  const sig = request.headers.get(''stripe-signature'');
  const body = await request.text();
  const event = stripe.webhooks.constructEvent(body, sig, webhookSecret);
  // handle event
  return Response.json({ received: true });
}','javascript','hard',ARRAY['nextjs','api-routes','route-handlers','webhooks']::text[],false,126),
('nextjs','api-routes','nextjs-api-routes-127','How do you chain multiple pieces of logic in Middleware, such as auth checking and locale detection, since only one `middleware.js` file is allowed per project?','Since Next.js only runs a single Middleware function per matched request, you compose multiple concerns inside that one function (or factor them into helper functions called in sequence) — checking auth first, then locale, then any other logic — rather than declaring multiple separate middleware files as you might in an Express app.',null,null,null,'medium',ARRAY['nextjs','api-routes','middleware']::text[],false,127),
('nextjs','api-routes','nextjs-api-routes-128','What status code and response shape conventions are commonly followed when building a REST-style API with Route Handlers?','Common conventions include returning `200` for successful reads, `201` for successful creation, `204` (no body) for successful deletion, `400` for invalid input, `401`/`403` for auth failures, and `404` for missing resources — with a consistent JSON error shape like `{ error: ''message'' }` so client code can handle failures uniformly across endpoints.',null,null,null,'easy',ARRAY['nextjs','api-routes','route-handlers','rest']::text[],false,128),
('nextjs','api-routes','nextjs-api-routes-129','How is `NextRequest` different from the standard `Request` object in a Route Handler or Middleware?','`NextRequest` (from `next/server`) extends the standard Web `Request` with Next.js-specific conveniences, such as a parsed `.cookies` accessor and `.nextUrl`, a pre-parsed URL object with easy access to `pathname` and `searchParams` without manually constructing a `new URL(...)`. Route Handlers can type their argument as either, but Middleware always receives a `NextRequest`.',null,null,null,'medium',ARRAY['nextjs','api-routes','middleware','route-handlers']::text[],false,129),
('nextjs','api-routes','nextjs-api-routes-130','Why would you choose a Route Handler over a Server Action for a form submission that only your own app''s frontend will ever call?','Server Actions are usually simpler for this case since they avoid manually wiring a fetch call and endpoint, but a Route Handler might still be preferred if you need precise control over the HTTP response (custom headers, non-JSON content types, streaming), want the endpoint to also be callable by external clients later, or are integrating with tooling that expects a conventional REST endpoint.',null,null,null,'medium',ARRAY['nextjs','api-routes','server-actions','route-handlers']::text[],false,130),
('nextjs','api-routes','nextjs-api-routes-131','Can you export a default handler from a `route.js` file the way you could in `pages/api`?','No — `route.js` requires named exports matching HTTP method names (`GET`, `POST`, etc.); a default export has no meaning in this file and won''t be treated as a catch-all handler, unlike the single default-exported handler function convention used in the Pages Router''s `pages/api` files.',null,null,null,'easy',ARRAY['nextjs','api-routes','route-handlers']::text[],false,131),
('nextjs','api-routes','nextjs-api-routes-132','How would you upload and handle a file via a Route Handler?','Read the incoming request with `await request.formData()`, extract the file entry (which comes back as a `File`/`Blob`-like object), and then write it to storage — either streaming it to disk in a Node.js runtime or, more commonly in production, forwarding it to an object storage service like S3 or Vercel Blob rather than the app server''s local filesystem, which isn''t durable across deployments.',null,'export async function POST(request) {
  const formData = await request.formData();
  const file = formData.get(''file'');
  const buffer = Buffer.from(await file.arrayBuffer());
  // upload buffer to storage
  return Response.json({ ok: true });
}','javascript','hard',ARRAY['nextjs','api-routes','route-handlers','file-upload']::text[],false,132),
('nextjs','api-routes','nextjs-api-routes-133','What is the purpose of the `OPTIONS` export on a Route Handler, and when is it invoked automatically?','Browsers send an `OPTIONS` preflight request automatically before certain cross-origin requests (like ones with custom headers or non-simple methods) to check whether the actual request is allowed. Exporting an `OPTIONS` handler lets you respond to this preflight with the appropriate CORS headers; if you don''t export one and CORS is needed, the preflight will fail and the browser will block the real request.',null,null,null,'hard',ARRAY['nextjs','api-routes','route-handlers','cors']::text[],false,133),
('nextjs','api-routes','nextjs-api-routes-134','How do you test a Route Handler locally without deploying, and what tools are commonly used?','Run `next dev` and hit the endpoint directly with a tool like `curl`, Postman, or Thunder Client against `http://localhost:3000/api/...`, or write integration tests with a library like Vitest/Jest that imports the exported `GET`/`POST` functions directly and calls them with a constructed `Request` object, bypassing the need for a running server for pure unit-style tests.',null,null,null,'medium',ARRAY['nextjs','api-routes','route-handlers','testing']::text[],false,134),
('nextjs','api-routes','nextjs-api-routes-135','How does Next.js Middleware differ conceptually from a traditional Express middleware chain?','Express middleware forms an ordered chain of functions per request, each calling `next()` to pass control along, and you can register many of them per route. Next.js Middleware is a single function per project (scoped by a matcher) that runs once before routing, closer to a gatekeeper/interceptor pattern than a composable pipeline, and it can''t directly modify the destination route''s rendering logic — only redirect, rewrite, or pass the request through with modified headers/cookies.',null,null,null,'hard',ARRAY['nextjs','api-routes','middleware','comparison']::text[],false,135),
('nextjs','data-fetching','nextjs-data-fetching-101','How do you fetch data in an App Router server component?','You mark the component function `async` and `await` your data source directly inside it — a `fetch` call, a database query, or an ORM call — with no separate data-fetching function required. Since the component runs only on the server, this code (and any secrets or database credentials it uses) never reaches the client bundle.',null,'async function Page() {
  const res = await fetch(''https://api.example.com/posts'');
  const posts = await res.json();
  return <PostList posts={posts} />;
}','jsx','easy',ARRAY['nextjs','data-fetching','server-components']::text[],true,101),
('nextjs','data-fetching','nextjs-data-fetching-102','How does Next.js extend the native `fetch` API for caching purposes?','Next.js patches the global `fetch` so that requests made on the server can opt into its caching system via extra options: `cache: ''force-cache''` (the default for most fetches, cached indefinitely until revalidated) or `cache: ''no-store''` (never cached, always fresh), plus `next: { revalidate: <seconds> }` for time-based revalidation and `next: { tags: [...] }` for tag-based on-demand revalidation.',null,'fetch(''https://api.example.com/data'', { next: { revalidate: 3600, tags: [''products''] } });','javascript','medium',ARRAY['nextjs','data-fetching','caching','fetch']::text[],true,102),
('nextjs','data-fetching','nextjs-data-fetching-103','What does `cache: ''no-store''` do on a `fetch` call in the App Router, and what side effect does it have on the route?','It tells Next.js never to cache that specific request''s response, so it''s re-fetched fresh on every render. As a side effect, using it also opts the entire route into dynamic rendering, since a route containing genuinely uncacheable data can no longer be served as a single static page.',null,null,null,'easy',ARRAY['nextjs','data-fetching','caching','fetch']::text[],false,103),
('nextjs','data-fetching','nextjs-data-fetching-104','How do you fetch data from multiple sources in parallel inside a server component, rather than sequentially?','Start all the fetch calls before awaiting any of them (or use `Promise.all`), so they run concurrently instead of each one blocking the next. Writing `await fetch(a); await fetch(b);` back to back creates an unnecessary request waterfall, where the second fetch doesn''t start until the first finishes.',null,'const [postsRes, userRes] = await Promise.all([
  fetch(''/api/posts''),
  fetch(''/api/user''),
]);','javascript','medium',ARRAY['nextjs','data-fetching','performance']::text[],false,104),
('nextjs','data-fetching','nextjs-data-fetching-105','What is a request waterfall in the context of Next.js data fetching, and how can nested Server Components cause one?','A request waterfall happens when fetches run sequentially because each depends on the previous component finishing before the next one starts fetching — for example, a parent component awaits its own data before rendering a child, and only then does the child start its own await. This serializes requests that could have run in parallel; fixing it usually means hoisting fetches higher up and starting them together, or using `Promise.all`.',null,null,null,'hard',ARRAY['nextjs','data-fetching','performance','waterfalls']::text[],false,105),
('nextjs','data-fetching','nextjs-data-fetching-106','What is request memoization (deduplication) in the App Router''s fetch caching, and why is it useful?','Within a single render pass, if multiple components call `fetch` with the exact same URL and options, Next.js automatically deduplicates them into a single underlying network request and shares the result — so you can call the same data-fetching function from several components (e.g. a layout and a page that both need the current user) without worrying about redundant requests.',null,null,null,'hard',ARRAY['nextjs','data-fetching','caching','fetch']::text[],false,106),
('nextjs','data-fetching','nextjs-data-fetching-107','How do you fetch data that isn''t from a URL-based API — for example, querying a database directly — inside a server component, and does Next.js''s fetch caching still apply?','You just call your ORM or database client''s query functions directly inside the async server component, since there''s no browser sandbox restriction on the server. Next.js''s built-in `fetch` cache patch doesn''t apply to non-fetch calls; to cache those, you wrap them with React''s `cache()` function for per-request memoization, or Next.js''s `unstable_cache` for cross-request caching with revalidation.',null,'import { cache } from ''react'';

const getUser = cache(async (id) => db.user.findUnique({ where: { id } }));','javascript','medium',ARRAY['nextjs','data-fetching','caching','database']::text[],false,107),
('nextjs','data-fetching','nextjs-data-fetching-108','What was `getServerSideProps` in the Pages Router, and when did you use it?','`getServerSideProps` is a function exported from a Pages Router page that runs on the server on every request, fetches whatever data the page needs, and returns it as props to the page component. You''d use it when a page needs fresh, request-specific data on every load — for example, content behind authentication or data that must never be stale.',null,'export async function getServerSideProps(context) {
  const res = await fetch(''https://api.example.com/data'');
  const data = await res.json();
  return { props: { data } };
}','javascript','medium',ARRAY['nextjs','data-fetching','pages-router','getserversideprops']::text[],false,108),
('nextjs','data-fetching','nextjs-data-fetching-109','What was `getStaticProps` in the Pages Router, and how does its `revalidate` option relate to ISR?','`getStaticProps` runs at build time (or on-demand for fallback pages) to fetch data and generate a page''s static HTML. Returning a `revalidate` number in seconds from it opts that page into ISR, so Next.js will regenerate the page in the background after that many seconds have passed since the last successful generation.',null,'export async function getStaticProps() {
  const data = await fetchData();
  return { props: { data }, revalidate: 60 };
}','javascript','medium',ARRAY['nextjs','data-fetching','pages-router','getstaticprops']::text[],true,109),
('nextjs','data-fetching','nextjs-data-fetching-110','What is `getInitialProps` and why is it now considered legacy compared to `getStaticProps`/`getServerSideProps`?','`getInitialProps` was the original Next.js data-fetching API, usable on both pages and `_app.js`, that ran on the server for the initial request and on the client for subsequent navigations. It''s considered legacy because it disables automatic static optimization for any page that uses it, has less predictable behavior across navigation types, and was superseded by the clearer, purpose-specific `getStaticProps`/`getServerSideProps` split.',null,null,null,'medium',ARRAY['nextjs','data-fetching','pages-router','legacy']::text[],false,110),
('nextjs','data-fetching','nextjs-data-fetching-111','Can `getStaticProps` and `getServerSideProps` be used together on the same page?','No — a Pages Router page can export at most one of `getStaticProps`, `getServerSideProps`, or neither (making it a plain statically optimized page with no data fetching); Next.js throws a build error if both are present, since they represent mutually exclusive rendering strategies for that page.',null,null,null,'easy',ARRAY['nextjs','data-fetching','pages-router']::text[],false,111),
('nextjs','data-fetching','nextjs-data-fetching-112','How do you fetch data inside a Client Component in the App Router, since it can''t be an `async` component?','A Client Component can''t itself be an `async function` (that''s only supported for Server Components), so data fetching there uses familiar client patterns: `useEffect` combined with `useState`, or a data-fetching library like TanStack Query or SWR, which also adds caching, revalidation, and loading/error state handling out of the box.',null,'''use client'';
import useSWR from ''swr'';

function Profile() {
  const { data, isLoading } = useSWR(''/api/user'', fetcher);
  if (isLoading) return <p>Loading...</p>;
  return <p>{data.name}</p>;
}','jsx','medium',ARRAY['nextjs','data-fetching','client-components']::text[],false,112),
('nextjs','data-fetching','nextjs-data-fetching-113','What is SWR (or React Query/TanStack Query) commonly used for in a Next.js app, given that Server Components can already fetch data?','These libraries handle client-side data fetching concerns Server Components don''t cover: revalidating data on window focus or interval, optimistic updates, caching across client-side navigations, retrying failed requests, and keeping data fresh after mutations — all useful for genuinely interactive, client-rendered features even in an app that also uses Server Components elsewhere.',null,null,null,'easy',ARRAY['nextjs','data-fetching','client-components','swr']::text[],false,113),
('nextjs','data-fetching','nextjs-data-fetching-114','How do you pass data fetched in a server component down to a client component?','Fetch the data in the (async) server component as usual, then pass it as a regular prop into the client component you render — as long as the value is serializable (plain objects, arrays, strings, numbers), since props crossing the server-to-client boundary get serialized and sent to the browser as part of the React payload.',null,'async function Page() {
  const data = await getData();
  return <ClientChart data={data} />;
}','jsx','medium',ARRAY['nextjs','data-fetching','server-components','client-components']::text[],false,114),
('nextjs','data-fetching','nextjs-data-fetching-115','Why can''t you pass a function (like an event handler) as a prop from a Server Component directly into a Client Component?','Props crossing from server to client have to be serialized to be sent over the network as part of the RSC payload, and functions/closures aren''t serializable — there''s no way to send executable server code to run natively in the browser this way. The exceptions are Server Actions, which are specially compiled into callable references the client can invoke as a request back to the server.',null,null,null,'medium',ARRAY['nextjs','data-fetching','server-components','client-components']::text[],false,115),
('nextjs','data-fetching','nextjs-data-fetching-116','What is a Server Action, and how does it relate to data fetching/mutations?','A Server Action is an async function marked with `''use server''` that can be called directly from a Client Component (e.g. as a form''s `action` or from an event handler) and runs on the server, letting you perform mutations like writing to a database without manually creating an API route. Next.js compiles the reference into a special endpoint the client invokes, handling the network call for you.',null,'''use server'';

export async function createPost(formData) {
  await db.post.create({ data: { title: formData.get(''title'') } });
}','javascript','hard',ARRAY['nextjs','data-fetching','server-actions']::text[],true,116),
('nextjs','data-fetching','nextjs-data-fetching-117','How do Server Actions typically trigger a UI update after a mutation, given that the page might be statically cached?','A Server Action commonly calls `revalidatePath()` or `revalidateTag()` after performing its mutation, which invalidates the relevant cached data/route so the next render reflects the change; it can also call `redirect()` to navigate the user afterward. Without an explicit revalidation call, a statically cached page wouldn''t automatically reflect the mutation.',null,'''use server'';
import { revalidatePath } from ''next/cache'';

export async function addComment(formData) {
  await db.comment.create({ data: {/* ... */} });
  revalidatePath(''/post/123'');
}','javascript','hard',ARRAY['nextjs','data-fetching','server-actions','caching']::text[],false,117),
('nextjs','data-fetching','nextjs-data-fetching-118','What is the Data Cache in Next.js, and how is it different from the Router Cache (client-side cache)?','The Data Cache is a server-side persistent cache for `fetch` results (and manually cached functions), shared across requests and deployments, which is what powers ISR-style revalidation. The Router Cache is a client-side, in-memory cache of previously visited route segments'' React Server Component payloads, used to make back/forward navigation and revisiting recently viewed routes feel instant without a server round trip.',null,null,null,'hard',ARRAY['nextjs','data-fetching','caching']::text[],false,118),
('nextjs','data-fetching','nextjs-data-fetching-119','How do you disable Next.js''s fetch caching entirely for a specific request?','Pass `{ cache: ''no-store'' }` as an option to that specific `fetch` call — this tells Next.js to skip the Data Cache for that request and always fetch fresh data, independent of any route-level or global caching configuration.',null,'await fetch(url, { cache: ''no-store'' });','javascript','easy',ARRAY['nextjs','data-fetching','caching','fetch']::text[],false,119),
('nextjs','data-fetching','nextjs-data-fetching-120','How do you fetch data for a page that needs both a static shell and streamed dynamic content, combining `generateStaticParams` with per-request fetching?','Use `generateStaticParams` to pre-render the known set of param values at build time, and inside the page component, `await` your data fetch normally — for params not covered by `generateStaticParams`, Next.js generates the page on first request (assuming `dynamicParams` isn''t set to false) and caches it afterward. Any part of the page requiring truly per-request data can additionally be wrapped in `<Suspense>` with its own uncached fetch inside a nested async component.',null,null,null,'hard',ARRAY['nextjs','data-fetching','static-generation','streaming']::text[],false,120),
('nextjs','data-fetching','nextjs-data-fetching-121','What does `revalidateTag()` do, and how is it different from `revalidatePath()`?','`revalidateTag(tag)` invalidates every cached `fetch` request that was tagged with a matching tag via `next: { tags: [...] }`, regardless of which route it belongs to — useful when one data change affects many different pages. `revalidatePath(path)` invalidates the cache for a specific route path directly, which is simpler when you know exactly which route needs refreshing.',null,'await fetch(url, { next: { tags: [''posts''] } });
// later:
revalidateTag(''posts'');','javascript','medium',ARRAY['nextjs','data-fetching','caching']::text[],false,121),
('nextjs','data-fetching','nextjs-data-fetching-122','How do you read cookies or headers during server-side data fetching in the App Router?','Call the `cookies()` or `headers()` functions from `next/headers` inside a Server Component, Route Handler, or Server Action — they return the request''s cookies/headers synchronously (or as a promise in newer versions) without needing to be explicitly passed down as props. Using either automatically makes the route dynamic, since the result depends on the specific incoming request.',null,'import { cookies } from ''next/headers'';

async function Page() {
  const token = cookies().get(''session'')?.value;
  const data = await fetchWithAuth(token);
  return <Dashboard data={data} />;
}','jsx','medium',ARRAY['nextjs','data-fetching','cookies','headers']::text[],false,122),
('nextjs','data-fetching','nextjs-data-fetching-123','What are common patterns for handling loading and error states for data fetching in a server component, since you can''t use `useState` for a loading flag there?','Loading state is handled declaratively via a `loading.js` file (or a nested `<Suspense>` boundary), which automatically shows while the awaited data fetch is pending. Error state is handled by throwing an error from the fetch/data logic and letting the nearest `error.js` boundary catch it, or by returning `notFound()` for a specific ''not found'' case — there''s no manual `isLoading`/`error` state to track like in client-side fetching.',null,null,null,'medium',ARRAY['nextjs','data-fetching','loading-states','error-handling']::text[],false,123),
('nextjs','data-fetching','nextjs-data-fetching-124','How does Next.js''s data fetching model keep secrets like API keys out of the client bundle?','Data fetching that happens inside a Server Component, Route Handler, or Server Action runs exclusively on the server, so any API keys or database credentials referenced in that code (including non-`NEXT_PUBLIC_` environment variables) never get bundled or sent to the browser — only the resulting rendered output or returned data does.',null,null,null,'easy',ARRAY['nextjs','data-fetching','server-components','security']::text[],false,124),
('nextjs','data-fetching','nextjs-data-fetching-125','What happens if you call `fetch` with the same URL and options in two different Server Components rendered on the same page?','Next.js''s request memoization deduplicates them automatically within a single render pass, issuing only one actual network request and reusing the result for both callers — this happens transparently without any extra code, as long as the URL and fetch options match exactly.',null,null,null,'easy',ARRAY['nextjs','data-fetching','fetch','caching']::text[],false,125),
('nextjs','data-fetching','nextjs-data-fetching-126','What is `unstable_cache`, and when would you reach for it instead of relying on fetch caching alone?','`unstable_cache` (from `next/cache`) wraps an arbitrary async function — not just `fetch` — so its result is cached across requests with an optional revalidation time and tags, similar to how fetch caching works but usable for database queries, third-party SDK calls, or any computation that doesn''t go through the patched `fetch`.',null,'import { unstable_cache } from ''next/cache'';

const getPosts = unstable_cache(async () => db.post.findMany(), [''posts''], { revalidate: 60 });','javascript','medium',ARRAY['nextjs','data-fetching','caching']::text[],false,126),
('nextjs','data-fetching','nextjs-data-fetching-127','How would you fetch and display paginated data in the App Router using `searchParams`?','Read the page number from the `searchParams` prop passed into the page component, pass it into your data-fetching call, and render `<Link>`s to adjacent pages that update the `page` query parameter — each navigation triggers a fresh (or cached, depending on config) fetch for that page''s data since `searchParams` changes are treated as a new request to the server component.',null,'async function Page({ searchParams }) {
  const page = Number(searchParams.page) || 1;
  const items = await getItems(page);
  return <List items={items} />;
}','jsx','medium',ARRAY['nextjs','data-fetching','pagination']::text[],false,127),
('nextjs','data-fetching','nextjs-data-fetching-128','Why is fetching data directly inside a Server Component often described as removing the need for a traditional client-to-API-route round trip?','In a classic SPA, the browser has to render a loading state, then make a network request to your own API, wait for a response, and re-render with the data — two network hops in total (page load, then API call). A Server Component fetches its data on the server before sending any HTML, so the browser receives the page already containing the data in one hop, with no separate client-side API call needed for that content.',null,null,null,'easy',ARRAY['nextjs','data-fetching','server-components','performance']::text[],false,128),
('nextjs','data-fetching','nextjs-data-fetching-129','What is the `use()` hook (from React) used for in the context of data fetching in Next.js, and how does it differ from `await` in a server component?','`use()` lets a Client Component (or a component rendered conditionally) read the value of a promise, suspending the component until it resolves, which is useful when a promise is created in a parent Server Component and passed down for the client to resume rendering once ready. Unlike `await` in an async Server Component (which blocks that component''s own rendering on the server), `use()` integrates with Suspense on the client and can be called conditionally inside client rendering logic.',null,'''use client'';
import { use } from ''react'';

function Comments({ commentsPromise }) {
  const comments = use(commentsPromise);
  return <ul>{comments.map(c => <li key={c.id}>{c.text}</li>)}</ul>;
}','jsx','hard',ARRAY['nextjs','data-fetching','streaming','suspense']::text[],false,129),
('nextjs','data-fetching','nextjs-data-fetching-130','How do route handlers (in `app/api/.../route.js`) fit into a data-fetching architecture when Server Components can already query data directly?','Route handlers are still needed for endpoints consumed by external clients (mobile apps, third-party webhooks, other services), for functionality that must be callable from a Client Component via a plain HTTP request instead of a Server Action, or when you specifically need to control raw request/response details like headers and status codes that a Server Component doesn''t expose directly.',null,null,null,'medium',ARRAY['nextjs','data-fetching','route-handlers']::text[],false,130),
('nextjs','data-fetching','nextjs-data-fetching-131','What''s a common mistake when migrating `getServerSideProps` logic to the App Router''s data fetching?','A common mistake is treating every migrated data fetch as needing `cache: ''no-store''` to replicate `getServerSideProps`''s always-fresh behavior, when much of that data could actually be cached with a `revalidate` window for better performance — `getServerSideProps` had no caching by default, but the App Router''s caching defaults are an opportunity to improve performance, not just a behavior to blindly replicate.',null,null,null,'hard',ARRAY['nextjs','data-fetching','migration','caching']::text[],false,131),
('nextjs','data-fetching','nextjs-data-fetching-132','How do you type the data returned from a fetch call in a Server Component when using TypeScript?','Since `fetch`''s `res.json()` returns `Promise<any>` by default, you typically define an interface or type for the expected shape and assert or validate the response against it — either with a simple type assertion (`(await res.json()) as Post[]`) or, more safely, with a runtime validation library like Zod that both parses and gives you an inferred type.',null,'interface Post { id: string; title: string; }
const posts = (await res.json()) as Post[];','typescript','medium',ARRAY['nextjs','data-fetching','typescript']::text[],false,132),
('nextjs','data-fetching','nextjs-data-fetching-133','What is the difference between static params generated by `generateStaticParams` and dynamically rendering unknown params at request time?','Params returned by `generateStaticParams` are rendered and cached at build time, so requests for those specific paths are served instantly with no per-request work. Params not in that list are rendered on-demand the first time they''re requested (unless `dynamicParams` is set to `false`, which makes unlisted params 404 instead), and the result is then cached for subsequent visitors just like the build-time ones.',null,null,null,'hard',ARRAY['nextjs','data-fetching','static-generation']::text[],false,133),
('nextjs','fundamentals','nextjs-fundamentals-101','What is Next.js and what problem does it solve on top of React?','Next.js is a React framework that adds the pieces React itself doesn''t provide out of the box: file-based routing, server-side rendering and static generation, an opinionated build pipeline, and built-in optimizations for images, fonts, and scripts. Instead of assembling a router, a bundler, and a rendering strategy yourself, Next.js gives you a batteries-included structure so you can focus on building pages and features.',null,null,null,'easy',ARRAY['nextjs','fundamentals']::text[],true,101),
('nextjs','fundamentals','nextjs-fundamentals-102','What is the difference between Next.js and Create React App / plain Vite React?','Plain React tooling like Vite gives you a client-only single-page app with no built-in routing or server rendering; you bolt on a router and data-fetching library yourself. Next.js is a full framework: it ships file-based routing, server-side rendering/static generation, API routes, and image/font optimization by default, and it runs a Node.js (or edge) server rather than shipping a purely static bundle.',null,null,null,'easy',ARRAY['nextjs','fundamentals','comparison']::text[],false,102),
('nextjs','fundamentals','nextjs-fundamentals-103','What are the two routing systems Next.js supports, and how do they differ?','The Pages Router (the original system) uses a `pages/` directory where each file maps to a route, and data fetching is done with functions like `getServerSideProps` and `getStaticProps`. The App Router (introduced in Next.js 13, now the default) uses an `app/` directory built on React Server Components, folder-based routing with special files like `page.js` and `layout.js`, and fetches data directly inside async server components.',null,null,null,'medium',ARRAY['nextjs','fundamentals','app-router']::text[],true,103),
('nextjs','fundamentals','nextjs-fundamentals-104','What is the minimum project structure needed to start a Next.js app?','A Next.js app needs a `package.json` with the `next`, `react`, and `react-dom` dependencies, a `next.config.js` (optional, for custom config), and either a `pages/` or `app/` directory containing at least one route file. `create-next-app` scaffolds this structure automatically along with sensible defaults for TypeScript, ESLint, and Tailwind if selected.',null,'npx create-next-app@latest my-app','javascript','easy',ARRAY['nextjs','fundamentals','project-structure']::text[],false,104),
('nextjs','fundamentals','nextjs-fundamentals-105','What does the `public/` folder do in a Next.js project?','Files placed in `public/` are served as static assets directly from the site''s root URL — for example, `public/logo.png` is reachable at `/logo.png`. It''s the right place for favicons, robots.txt, manifest files, and images that don''t need build-time optimization, as opposed to images imported into components, which Next.js can optimize automatically via the `next/image` pipeline.',null,null,null,'easy',ARRAY['nextjs','fundamentals','project-structure']::text[],false,105),
('nextjs','fundamentals','nextjs-fundamentals-106','What is the root `layout.js` file in the App Router, and why is it required?','The root layout at `app/layout.js` wraps every page in the application and must render the `<html>` and `<body>` tags, since the App Router no longer uses a `_document.js` or `_app.js` file for that purpose. It''s the one layout every route shares, making it the natural place for global providers, fonts, and shared chrome like a navbar.',null,'export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}','jsx','easy',ARRAY['nextjs','fundamentals','app-router','layouts']::text[],false,106),
('nextjs','fundamentals','nextjs-fundamentals-107','What did `_app.js` and `_document.js` do in the Pages Router?','`_app.js` is a special component that wraps every page, useful for persisting layout, injecting global CSS, or wrapping the app in context providers across navigations. `_document.js` customizes the shared `<html>` and `<body>` markup that wraps the entire page (e.g. adding a `lang` attribute or custom fonts link) and only renders on the server, so it can''t contain interactive logic.',null,null,null,'easy',ARRAY['nextjs','fundamentals','pages-router']::text[],false,107),
('nextjs','fundamentals','nextjs-fundamentals-108','How do you add global CSS to a Next.js application?','Global CSS can only be imported in one place: the root layout (`app/layout.js`) in the App Router, or `_app.js` in the Pages Router. Next.js intentionally disallows importing global stylesheets inside individual page or component files to avoid style leakage and unpredictable ordering.',null,'import ''./globals.css'';

export default function RootLayout({ children }) {
  return <html><body>{children}</body></html>;
}','jsx','easy',ARRAY['nextjs','fundamentals','styling']::text[],false,108),
('nextjs','fundamentals','nextjs-fundamentals-109','What is `next.config.js` used for?','`next.config.js` (or `next.config.ts`/`.mjs`) is the framework''s central configuration file, used for things like setting up redirects and rewrites, configuring allowed image domains, enabling experimental features, customizing webpack/Turbopack, and setting environment-specific build options. Most Next.js apps run fine with no custom config at all, since the defaults are designed to be sensible out of the box.',null,'/** @type {import(''next'').NextConfig} */
const nextConfig = {
  images: { domains: [''example.com''] },
};

module.exports = nextConfig;','javascript','easy',ARRAY['nextjs','fundamentals','configuration']::text[],false,109),
('nextjs','fundamentals','nextjs-fundamentals-110','What build commands does a typical Next.js `package.json` define, and what does each do?','`next dev` starts a local development server with fast refresh and detailed error overlays. `next build` compiles and optimizes the app for production, generating the `.next/` output directory. `next start` runs that production build with a Node.js server, and `next lint` runs ESLint using Next.js''s recommended rule set.',null,null,null,'easy',ARRAY['nextjs','fundamentals','tooling']::text[],false,110),
('nextjs','fundamentals','nextjs-fundamentals-111','What is Turbopack, and how does it relate to webpack in Next.js?','Turbopack is a Rust-based bundler built by Vercel intended as a faster successor to webpack, used in Next.js for local development (`next dev --turbo`, or the default in newer versions) to dramatically speed up cold starts and hot module replacement. As of recent Next.js versions it''s stabilizing for production builds too, but webpack remains the default/fallback for builds that rely on unsupported plugins or loaders.',null,null,null,'medium',ARRAY['nextjs','fundamentals','tooling','turbopack']::text[],false,111),
('nextjs','fundamentals','nextjs-fundamentals-112','What is the difference between a Next.js page and a component?','A page is any file inside `app/` named `page.js` (or, in the Pages Router, any file directly under `pages/`) — Next.js automatically turns it into a routable URL. A regular component is just a normal React function that can live anywhere and is only rendered when explicitly imported and used, with no automatic route created for it.',null,null,null,'easy',ARRAY['nextjs','fundamentals','routing']::text[],false,112),
('nextjs','fundamentals','nextjs-fundamentals-113','How does Next.js handle TypeScript support out of the box?','Next.js detects a `tsconfig.json` (or you can let `create-next-app` generate one) and automatically configures TypeScript support, including type-checking during `next build` and generating types for routes and config. You don''t need to manually wire up a TypeScript loader or babel preset — Next.js''s compiler (SWC) handles the transpilation.',null,null,null,'easy',ARRAY['nextjs','fundamentals','typescript']::text[],false,113),
('nextjs','fundamentals','nextjs-fundamentals-114','What is SWC and why did Next.js replace Babel with it by default?','SWC is a Rust-based JavaScript/TypeScript compiler that Next.js uses by default for transpilation and minification instead of Babel. It''s significantly faster than Babel because it''s compiled to native code rather than running in JavaScript, which speeds up both development builds and production builds; a custom `.babelrc` still opts a project back into Babel when needed.',null,null,null,'hard',ARRAY['nextjs','fundamentals','tooling','swc']::text[],false,114),
('nextjs','fundamentals','nextjs-fundamentals-115','How do environment variables work in Next.js, and what does the `NEXT_PUBLIC_` prefix mean?','Variables in `.env.local` (or `.env.production`, etc.) are available on the server by default, but are not exposed to client-side JavaScript unless prefixed with `NEXT_PUBLIC_`. That prefix tells Next.js to inline the value into the client bundle at build time, so only non-sensitive values (like a public API base URL) should ever use it.',null,'// .env.local
DATABASE_URL=postgres://...        # server only
NEXT_PUBLIC_API_URL=https://api.example.com  # exposed to the browser','javascript','medium',ARRAY['nextjs','fundamentals','environment-variables']::text[],false,115),
('nextjs','fundamentals','nextjs-fundamentals-116','What is `create-next-app` and what choices does it prompt you for?','`create-next-app` is the official CLI scaffolding tool for bootstrapping a new Next.js project. It prompts for options like TypeScript vs JavaScript, ESLint setup, Tailwind CSS, whether to use the `src/` directory, and whether to use the App Router, then generates a working starter project with those choices applied.',null,null,null,'easy',ARRAY['nextjs','fundamentals','tooling']::text[],false,116),
('nextjs','fundamentals','nextjs-fundamentals-117','What is the purpose of the optional `src/` directory in a Next.js project?','Using a `src/` directory (e.g. `src/app/` instead of `app/` at the project root) is purely organizational — it separates application source code from root-level config files like `next.config.js`, `package.json`, and `.eslintrc`. Next.js supports both layouts identically; it''s a preference, not a functional difference.',null,null,null,'easy',ARRAY['nextjs','fundamentals','project-structure']::text[],false,117),
('nextjs','fundamentals','nextjs-fundamentals-118','Who maintains Next.js, and how does that relationship shape the framework?','Next.js is developed and maintained primarily by Vercel, a hosting company, though it''s open source and accepts community contributions. This relationship means Next.js is deeply optimized for deployment on Vercel''s platform (features like ISR and edge functions map closely to Vercel infrastructure), though it can be self-hosted or deployed elsewhere with some feature trade-offs.',null,null,null,'easy',ARRAY['nextjs','fundamentals','ecosystem']::text[],false,118),
('nextjs','fundamentals','nextjs-fundamentals-119','What does ''zero-config'' mean in the context of Next.js, and where does it stop being true?','Zero-config means a new Next.js project works with sensible defaults for routing, bundling, TypeScript, and CSS support without hand-writing build configuration. It stops being true once you need custom behavior — custom webpack loaders, non-default redirects, multi-tenant routing, or specific caching rules — at which point you configure `next.config.js` explicitly.',null,null,null,'medium',ARRAY['nextjs','fundamentals','configuration']::text[],false,119),
('nextjs','fundamentals','nextjs-fundamentals-120','What is a React Server Component, at a fundamental level, and why does Next.js default to it?','A React Server Component renders entirely on the server and sends only its resulting output (not its JavaScript) to the browser, meaning it never re-renders on the client and can''t use browser-only APIs or hooks like `useState`. The App Router defaults every component to a server component because it reduces client bundle size and lets components fetch data directly without an API round trip, reserving client components for interactivity.',null,null,null,'medium',ARRAY['nextjs','fundamentals','server-components']::text[],true,120),
('nextjs','fundamentals','nextjs-fundamentals-121','What is the `metadata` export used for in the App Router?','Exporting a `metadata` object (or a `generateMetadata` function) from a `page.js` or `layout.js` file lets Next.js manage `<head>` content like the page title, meta description, and Open Graph tags declaratively, without manually rendering a `<head>` tag. It replaces the `next/head` component used in the Pages Router.','export const metadata = { title: ''My Page'', description: ''A page about things'' };',null,null,'easy',ARRAY['nextjs','fundamentals','metadata','seo']::text[],false,121),
('nextjs','fundamentals','nextjs-fundamentals-122','How did the Pages Router historically manage `<head>` content, and what component did it use?','The Pages Router uses the `next/head` component, which lets you declare tags like `<title>` and meta tags inside a page component; Next.js merges and deduplicates these into the document head during rendering. It has to be used inside each page''s JSX rather than as a plain export, unlike the App Router''s `metadata` object.',null,'import Head from ''next/head'';

function Page() {
  return (
    <>
      <Head><title>My Page</title></Head>
      <main>...</main>
    </>
  );
}','jsx','easy',ARRAY['nextjs','fundamentals','pages-router','seo']::text[],false,122),
('nextjs','fundamentals','nextjs-fundamentals-123','Why might a team choose to stick with the Pages Router on a new project instead of the App Router?','Reasons include familiarity and existing team expertise, reliance on libraries or patterns (like certain data-fetching libraries or class components) that don''t yet fully support Server Components, or a simpler mental model when the app doesn''t need fine-grained server/client component splitting. The Pages Router is still fully supported, just no longer the default or the focus of new feature development.',null,null,null,'medium',ARRAY['nextjs','fundamentals','pages-router','app-router']::text[],false,123),
('nextjs','fundamentals','nextjs-fundamentals-124','What is the `.next/` folder, and should it be committed to version control?','`.next/` is the build output directory Next.js generates during `next dev` (a dev cache) and `next build` (production artifacts including compiled pages, server bundles, and static assets). It should never be committed — it''s machine-generated, environment-specific, and regenerated on every build, so it belongs in `.gitignore`.',null,null,null,'easy',ARRAY['nextjs','fundamentals','project-structure']::text[],false,124),
('nextjs','fundamentals','nextjs-fundamentals-125','How does Next.js support absolute imports and custom path aliases?','Next.js reads the `baseUrl` and `paths` settings from `tsconfig.json` (or `jsconfig.json` for JS projects) to enable absolute imports and custom aliases like `@/components/Button` instead of long relative paths, with no extra bundler configuration required.',null,'// tsconfig.json
{ "compilerOptions": { "baseUrl": ".", "paths": { "@/*": ["./src/*"] } } }','javascript','easy',ARRAY['nextjs','fundamentals','tooling']::text[],false,125),
('nextjs','fundamentals','nextjs-fundamentals-126','What does ''framework'' mean in Next.js''s own description of itself, as opposed to a ''library''?','Next.js describes itself as a framework because it dictates structure and makes architectural decisions for you — routing conventions, where data fetching happens, how rendering strategies are chosen per route — rather than being an unopinionated toolkit you assemble freely, which is how React itself is described as a library. This trade-off gives faster setup and consistent patterns across a team at the cost of some flexibility.',null,null,null,'easy',ARRAY['nextjs','fundamentals','comparison']::text[],false,126),
('nextjs','fundamentals','nextjs-fundamentals-127','Can you use Next.js purely as a static site generator with no server at runtime?','Yes — using `output: ''export''` in `next.config.js` produces a fully static HTML/CSS/JS export that can be hosted on any static file host with no Node.js server required. This disables features that need a live server at request time, such as `getServerSideProps`, on-demand ISR, and API routes/route handlers that aren''t statically generated.',null,'// next.config.js
module.exports = { output: ''export'' };','javascript','medium',ARRAY['nextjs','fundamentals','static-export']::text[],false,127),
('nextjs','fundamentals','nextjs-fundamentals-128','What role does the `layout.js` vs `template.js` distinction play in the App Router?','A `layout.js` persists across navigations between sibling routes and preserves its state and DOM (children just swap inside it), while a `template.js` re-mounts from scratch on every navigation, resetting state and re-running effects. Templates are useful when you specifically want fresh state or re-triggered animations/effects on each navigation rather than the persistence layouts provide.',null,null,null,'hard',ARRAY['nextjs','fundamentals','app-router','layouts']::text[],false,128),
('nextjs','fundamentals','nextjs-fundamentals-129','What is Next.js''s font optimization feature and why does it matter for fundamentals like page structure?','`next/font` (built into the App Router and available in the Pages Router) self-hosts Google Fonts or local font files at build time rather than fetching them from an external CDN at runtime, eliminating extra network requests and layout shift from font swapping. It''s imported and applied directly in a layout or page file, making font loading part of the app''s declared structure instead of a separate `<link>` tag.','import { Inter } from ''next/font/google'';
const inter = Inter({ subsets: [''latin''] });',null,null,'medium',ARRAY['nextjs','fundamentals','fonts']::text[],false,129),
('nextjs','fundamentals','nextjs-fundamentals-130','Why is Next.js commonly described as a good default choice for production React apps?','It bundles the pieces most production apps need anyway — routing, SSR/SSG, image and font optimization, code splitting, and API endpoints — behind sensible defaults, reducing the setup and decision-making overhead compared to hand-assembling a React SPA. Its strong SEO story (via server rendering) and first-class Vercel hosting integration also make it a common default for content-facing and marketing-adjacent sites, not just dashboards.',null,null,null,'easy',ARRAY['nextjs','fundamentals']::text[],false,130),
('nextjs','fundamentals','nextjs-fundamentals-131','What is the difference between `page.js`, `layout.js`, `loading.js`, and `error.js` as App Router file conventions?','`page.js` defines the unique UI for a route and makes it publicly accessible. `layout.js` wraps a segment and its children with shared UI that persists across navigation. `loading.js` automatically wraps the segment in a React Suspense boundary and renders while the page''s data is loading. `error.js` wraps the segment in an error boundary and renders when a rendering error occurs within it.',null,null,null,'medium',ARRAY['nextjs','fundamentals','app-router','conventions']::text[],true,131),
('nextjs','fundamentals','nextjs-fundamentals-132','How do you migrate an existing Pages Router app to the App Router?','Next.js supports incremental migration: both `pages/` and `app/` can coexist in the same project, with `app/` routes taking precedence over `pages/` routes if they overlap. Teams typically move one route or feature at a time, converting `getServerSideProps`/`getStaticProps` data fetching into async server components and splitting out client components where interactivity is needed, rather than rewriting the whole app at once.',null,null,null,'medium',ARRAY['nextjs','fundamentals','migration']::text[],false,132),
('nextjs','performance','nextjs-performance-101','What does the `next/image` component do differently from a plain HTML `<img>` tag?','`next/image` automatically optimizes images by resizing and re-encoding them (typically to WebP/AVIF) on demand, lazy-loads images below the fold by default, generates a responsive `srcset` for different device sizes, and requires explicit width/height (or `fill`) to prevent layout shift while the image loads. A plain `<img>` does none of this automatically.',null,'import Image from ''next/image'';

<Image src="/photo.jpg" alt="A photo" width={600} height={400} />','jsx','easy',ARRAY['nextjs','performance','image-optimization']::text[],true,101),
('nextjs','performance','nextjs-performance-102','Why does `next/image` require you to specify `width` and `height` (or use `fill`)?','Knowing the image''s intrinsic dimensions ahead of time lets the browser reserve the correct amount of space for it before the image finishes loading, preventing cumulative layout shift (CLS) as content jumps around the page. The `fill` prop is the escape hatch for when the image should size itself to its parent container instead of fixed dimensions.',null,null,null,'easy',ARRAY['nextjs','performance','image-optimization','cls']::text[],false,102),
('nextjs','performance','nextjs-performance-103','How do remote images (hosted on another domain) work with `next/image`, and what configuration do they require?','For security, Next.js doesn''t optimize images from arbitrary external domains by default — you must explicitly allow them via `images.remotePatterns` (or the older `images.domains`) in `next.config.js`, listing the hostnames the optimizer is permitted to fetch from and process.',null,'// next.config.js
module.exports = {
  images: {
    remotePatterns: [{ protocol: ''https'', hostname: ''cdn.example.com'' }],
  },
};','javascript','easy',ARRAY['nextjs','performance','image-optimization','configuration']::text[],false,103),
('nextjs','performance','nextjs-performance-104','What is the `priority` prop on `next/image`, and when should you use it?','`priority` tells Next.js to preload an image and skip its default lazy loading, which should be used for the Largest Contentful Paint (LCP) image on a page — typically a hero image above the fold — since lazily loading it would delay the metric that most affects perceived load speed.',null,'<Image src="/hero.jpg" alt="Hero" width={1200} height={600} priority />','jsx','medium',ARRAY['nextjs','performance','image-optimization','lcp']::text[],false,104),
('nextjs','performance','nextjs-performance-105','How does `next/font` improve performance compared to loading fonts via a `<link>` tag to Google Fonts?','`next/font` downloads and self-hosts the font files at build time, serving them from your own domain instead of making a separate runtime request to Google''s CDN, which removes an extra network round trip and avoids leaking user IP addresses to a third party. It also automatically applies `font-display: swap`-like behavior and precomputed fallback metrics to minimize layout shift while the font loads.',null,'import { Inter } from ''next/font/google'';

const inter = Inter({ subsets: [''latin''], display: ''swap'' });','javascript','medium',ARRAY['nextjs','performance','fonts']::text[],true,105),
('nextjs','performance','nextjs-performance-106','How does code splitting work automatically in Next.js?','Next.js splits JavaScript by route by default — each page/route only ships the code it actually needs, rather than one giant bundle for the whole app, so navigating to a page doesn''t download code for pages you haven''t visited. Shared dependencies used across multiple routes are extracted into common chunks so they''re only downloaded once.',null,null,null,'medium',ARRAY['nextjs','performance','code-splitting']::text[],true,106),
('nextjs','performance','nextjs-performance-107','How do you manually code-split a large client component using `next/dynamic`?','Wrap the import in `dynamic(() => import(''./Component''))`, which generates a separate chunk loaded only when that component is actually rendered, instead of being bundled into the initial page load — useful for large, rarely-used components like a rich text editor or a chart library that most visitors never trigger.',null,'import dynamic from ''next/dynamic'';

const Editor = dynamic(() => import(''../components/Editor''), { ssr: false });','jsx','medium',ARRAY['nextjs','performance','code-splitting','dynamic-import']::text[],false,107),
('nextjs','performance','nextjs-performance-108','What does the `ssr: false` option do when used with `next/dynamic`, and when is it necessary?','It skips server-side rendering for that component entirely, rendering it only in the browser — necessary for components that depend on browser-only APIs (like `window` or a library that assumes a DOM exists) and would error or behave incorrectly if Next.js tried to render them on the server.',null,null,null,'easy',ARRAY['nextjs','performance','dynamic-import','ssr']::text[],false,108),
('nextjs','performance','nextjs-performance-109','How do you analyze what''s actually inside a Next.js production bundle?','The `@next/bundle-analyzer` package wraps `next.config.js` and, when enabled via an environment variable, generates an interactive treemap visualization of each chunk''s contents and size after `next build`, making it easy to spot unexpectedly large dependencies pulled into the client bundle.',null,'// next.config.js
const withBundleAnalyzer = require(''@next/bundle-analyzer'')({ enabled: process.env.ANALYZE === ''true'' });
module.exports = withBundleAnalyzer({});','javascript','medium',ARRAY['nextjs','performance','bundle-analysis']::text[],false,109),
('nextjs','performance','nextjs-performance-110','What is a common cause of an unexpectedly large client bundle in a Next.js App Router project?','A frequent cause is importing a large library (like a full charting or date library, or an entire icon set) inside a file that''s part of the Client Component boundary when only a small piece of it is actually needed, or marking a component `''use client''` unnecessarily high up the tree, pulling everything beneath it into the client bundle instead of keeping most of the tree as zero-JS Server Components.',null,null,null,'medium',ARRAY['nextjs','performance','bundle-analysis','client-components']::text[],false,110),
('nextjs','performance','nextjs-performance-111','How does the `next/script` component help with performance, and what loading strategies does it offer?','`next/script` gives fine-grained control over when and how third-party scripts (analytics, ads, chat widgets) load, via a `strategy` prop: `beforeInteractive` for scripts needed before hydration, `afterInteractive` (default) to load soon after the page becomes interactive, and `lazyOnload` to defer loading until the browser is idle — preventing render-blocking scripts from hurting load performance metrics.',null,'import Script from ''next/script'';

<Script src="https://example.com/analytics.js" strategy="lazyOnload" />','jsx','medium',ARRAY['nextjs','performance','scripts']::text[],false,111),
('nextjs','performance','nextjs-performance-112','How does Incremental Static Regeneration contribute to performance beyond just SEO freshness?','By serving pre-built static HTML for most requests (only regenerating occasionally in the background), ISR avoids the per-request rendering cost of SSR while still keeping content reasonably fresh, which reduces server load and lets those cached pages be served from a CDN edge with very low latency, unlike SSR which must run rendering logic on every single request.',null,null,null,'medium',ARRAY['nextjs','performance','isr','caching']::text[],false,112),
('nextjs','performance','nextjs-performance-113','What role does React Server Components play in reducing client-side JavaScript, and why does that matter for performance?','Server Components render fully on the server and send zero JavaScript to the client for that portion of the tree, whereas a fully client-rendered equivalent would ship its code, its dependencies, and require hydration. Less client JavaScript means a smaller bundle to download and parse, faster Time to Interactive, and less main-thread work, which is especially impactful on lower-end devices.',null,null,null,'medium',ARRAY['nextjs','performance','server-components']::text[],true,113),
('nextjs','performance','nextjs-performance-114','What are Core Web Vitals, and how does Next.js help you measure them?','Core Web Vitals are Google''s standardized metrics for real-world page experience: Largest Contentful Paint (loading speed), Interaction to Next Paint (responsiveness), and Cumulative Layout Shift (visual stability). Next.js provides a built-in `useReportWebVitals` hook (and integrates with Vercel Analytics/Speed Insights) to capture these metrics from real users and report them to an analytics endpoint.',null,null,null,'easy',ARRAY['nextjs','performance','web-vitals']::text[],false,114),
('nextjs','performance','nextjs-performance-115','How does prefetching via `<Link>` improve perceived navigation performance, and what is its cost?','Prefetching loads a linked route''s code (and cached static data) before the user clicks, so the actual navigation feels instant since the work was already done ahead of time. The cost is extra network requests and bandwidth usage for routes the user may never actually visit, which is why it''s automatically limited to viewport-visible links and can be disabled per-link with `prefetch={false}` when it''s not worth the trade-off.',null,null,null,'easy',ARRAY['nextjs','performance','prefetching']::text[],false,115),
('nextjs','performance','nextjs-performance-116','What is streaming with Suspense, and how does it improve perceived performance for slow data?','Instead of blocking the entire page''s response until the slowest data-fetching component finishes, wrapping that component in `<Suspense>` lets Next.js send the rest of the page immediately and stream in the slow section once it''s ready, so users see and can interact with most of the page far sooner instead of staring at a blank screen or spinner for the whole page.',null,null,null,'medium',ARRAY['nextjs','performance','streaming','suspense']::text[],false,116),
('nextjs','performance','nextjs-performance-117','How does automatic static optimization decide whether a Pages Router page ships as static HTML?','If a page doesn''t export `getServerSideProps` or use `getInitialProps`, Next.js automatically pre-renders it to static HTML at build time (behaving like SSG even without an explicit `getStaticProps`), which is faster to serve and can be fully cached, without the developer having to opt in explicitly.',null,null,null,'medium',ARRAY['nextjs','performance','pages-router','static-optimization']::text[],false,117),
('nextjs','performance','nextjs-performance-118','What performance problem can arise from putting a large client-side data-fetching library or heavy state management setup in a project''s root layout?','Because the root layout wraps every single route in the app, any Client Component boundary or heavy dependency placed there gets included in the JavaScript sent to every page, even pages that don''t need it — it''s better to push such dependencies down to only the specific routes/components that actually use them, keeping the shared root layout as lean as possible.',null,null,null,'hard',ARRAY['nextjs','performance','layouts','code-splitting']::text[],false,118),
('nextjs','performance','nextjs-performance-119','What image formats does `next/image` automatically serve, and how does it decide which one to use?','Next.js''s image optimizer can serve modern formats like WebP or AVIF instead of the original format (e.g. JPEG/PNG) when the requesting browser supports them, based on the request''s `Accept` header, falling back to the original format for browsers without support — this typically produces significantly smaller file sizes than the source image at equivalent visual quality.',null,null,null,'easy',ARRAY['nextjs','performance','image-optimization']::text[],false,119),
('nextjs','performance','nextjs-performance-120','How do you lazy-load a below-the-fold image using `next/image`, and is this the default behavior?','It is the default behavior — every `next/image` component is lazy-loaded (deferred until it''s near the viewport) automatically unless you pass the `priority` prop, which disables lazy loading and preloads the image instead. You don''t need to add a separate `loading="lazy"` attribute yourself as you might with a plain `<img>`.',null,null,null,'easy',ARRAY['nextjs','performance','image-optimization']::text[],false,120),
('nextjs','performance','nextjs-performance-121','What is the purpose of route segment config options like `export const revalidate` for balancing freshness against performance?','Setting `revalidate` at the route level lets you tune how often cached content is allowed to go stale before regenerating, letting you choose an interval that matches how frequently the underlying data actually changes — a longer interval maximizes cache hits and performance, while a shorter one trades some performance for fresher content, without going all the way to fully dynamic (uncached) rendering.',null,null,null,'medium',ARRAY['nextjs','performance','caching','isr']::text[],false,121),
('nextjs','performance','nextjs-performance-122','How does minimizing the number of Client Component boundaries in a tree help performance, beyond just bundle size?','Fewer/smaller Client Component boundaries mean less work for React to hydrate on the client (attaching event listeners and reconciling state), which directly reduces main-thread blocking time during page load — even if the visual output looks identical, a tree with excessive unnecessary `''use client''` boundaries does more client-side work than one that keeps as much as possible as server-rendered, non-hydrated markup.',null,null,null,'hard',ARRAY['nextjs','performance','client-components','hydration']::text[],false,122),
('nextjs','performance','nextjs-performance-123','What is a common strategy for optimizing a large list or table''s rendering performance in Next.js?','Beyond generic React techniques like `React.memo` and virtualization libraries (e.g. `react-window` or `@tanstack/react-virtual`) for rendering only visible rows, Next.js-specific approaches include paginating data at the fetch level so the server never sends more than a page''s worth of items, and rendering the list itself as a Server Component so the markup ships without extra client JS per row.',null,null,null,'hard',ARRAY['nextjs','performance','lists','virtualization']::text[],false,123),
('nextjs','performance','nextjs-performance-124','How can misconfigured caching lead to a performance regression rather than an improvement in Next.js?','Caching content that should have been dynamic (e.g. accidentally caching a user-specific page) can serve stale or wrong data to the wrong users, and fixing it under pressure by disabling caching broadly (`cache: ''no-store''` everywhere) can regress performance far below what tuned, selective caching would have achieved — caching needs to be applied deliberately per data source rather than as an all-or-nothing setting.',null,null,null,'hard',ARRAY['nextjs','performance','caching','gotcha']::text[],false,124),
('nextjs','performance','nextjs-performance-125','What does `next build`''s output summary (the route type table) tell you about your app''s performance profile?','After running `next build`, Next.js prints a table listing every route along with symbols indicating whether it''s static (○), dynamic server-rendered (λ/ƒ), or SSG with ISR, plus the JavaScript size for each page — this gives a quick audit of which routes are being served from fast cached HTML versus rendered per request, and which pages ship unusually large bundles.',null,null,null,'medium',ARRAY['nextjs','performance','build-output']::text[],false,125),
('nextjs','performance','nextjs-performance-126','How does deploying to the edge versus a single-region server affect performance for a globally distributed user base?','Edge-deployed routes (Middleware, or Route Handlers/pages using `runtime: ''edge''`) execute at data centers close to the requesting user worldwide, cutting network latency significantly compared to a single-region Node.js deployment where far-away users incur a longer round trip to reach the one server location — the trade-off is the Edge Runtime''s more limited API surface.',null,null,null,'medium',ARRAY['nextjs','performance','edge']::text[],false,126),
('nextjs','performance','nextjs-performance-127','What is the purpose of the `sizes` prop on `next/image`, and why does omitting it hurt performance for responsive layouts?','`sizes` tells the browser how wide the image will actually be displayed at different viewport widths (e.g. `(max-width: 768px) 100vw, 50vw`), which lets it pick the most appropriately sized image from the generated `srcset` instead of guessing and potentially downloading a much larger image than needed for the layout, wasting bandwidth.',null,'<Image src="/banner.jpg" alt="Banner" fill sizes="(max-width: 768px) 100vw, 50vw" />','jsx','medium',ARRAY['nextjs','performance','image-optimization']::text[],false,127),
('nextjs','performance','nextjs-performance-128','How would you diagnose why a specific Next.js page has a slow Time to First Byte (TTFB) in production?','Check whether the route is unexpectedly dynamic (e.g. an accidental `cookies()`/`no-store` fetch forcing per-request rendering when it could be cached), profile the slow data-fetching calls themselves (database queries without indexes, slow third-party APIs), and check whether the deployment region is far from most users — the build output''s route type table and server-side logging/tracing are the usual starting points.',null,null,null,'hard',ARRAY['nextjs','performance','ttfb','debugging']::text[],false,128),
('nextjs','performance','nextjs-performance-129','Why is minimizing third-party script usage often one of the highest-leverage performance fixes in a Next.js app?','Third-party scripts (analytics, ads, chat widgets, A/B testing tools) are often large, block the main thread during parsing/execution, and are outside the app''s own code-splitting and optimization pipeline — using `next/script` with an appropriate loading strategy, or removing scripts that provide little value, frequently produces a bigger Core Web Vitals improvement than micro-optimizing the app''s own component code.',null,null,null,'medium',ARRAY['nextjs','performance','scripts','web-vitals']::text[],false,129),
('nextjs','performance','nextjs-performance-130','What is the difference between build-time image optimization and Next.js''s default on-demand image optimization?','By default, `next/image` optimizes images on demand the first time they''re requested (and caches the result), rather than generating every size/format variant at build time, which keeps build times fast even with many images. This requires a running image optimization server (built into `next start` or a hosting platform''s equivalent) — a fully static export (`output: ''export''`) can''t do on-demand optimization and instead needs a custom loader or pre-optimized source images.',null,null,null,'hard',ARRAY['nextjs','performance','image-optimization','static-export']::text[],false,130),
('nextjs','rendering-strategies','nextjs-rendering-strategies-101','What are the four main rendering strategies available in Next.js?','Static Site Generation (SSG) renders pages to HTML at build time. Server-Side Rendering (SSR) renders HTML on each request. Incremental Static Regeneration (ISR) is static generation that can be re-generated in the background after a set interval or on demand. Client-Side Rendering (CSR) renders content in the browser using JavaScript after an initial (often minimal) HTML shell loads.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies']::text[],true,101),
('nextjs','rendering-strategies','nextjs-rendering-strategies-102','What is the difference between SSG and SSR, and how do you choose between them?','SSG generates a page''s HTML once at build time and serves the same static file to every visitor, making it extremely fast and cacheable but unable to reflect per-request data. SSR generates HTML fresh on every request, so it can include request-specific or frequently changing data, at the cost of a server round trip on every visit. Choose SSG for content that''s the same for all users and doesn''t change often (marketing pages, blog posts); choose SSR when content must be personalized or truly real-time.',null,null,null,'medium',ARRAY['nextjs','rendering-strategies','ssr','ssg']::text[],true,102),
('nextjs','rendering-strategies','nextjs-rendering-strategies-103','What is Incremental Static Regeneration (ISR) and what problem does it solve?','ISR lets you use static generation for pages but automatically regenerate them in the background after a specified revalidation interval, without needing to rebuild the entire site. It solves the problem of SSG becoming stale for content that changes occasionally (like product prices or article edits) while still serving fast, cached static pages most of the time instead of rendering on every request like SSR.',null,'fetch(''https://api.example.com/data'', { next: { revalidate: 60 } });','javascript','medium',ARRAY['nextjs','rendering-strategies','isr']::text[],false,103),
('nextjs','rendering-strategies','nextjs-rendering-strategies-104','In ISR, what actually happens when a request comes in after the revalidation window has expired?','The stale cached page is still served immediately to that visitor (stale-while-revalidate behavior), and Next.js triggers a regeneration of the page in the background. Once regeneration completes, subsequent requests get the fresh version — no single visitor is ever forced to wait for a rebuild, unlike the ''blocking'' fallback mode for entirely new paths.',null,null,null,'hard',ARRAY['nextjs','rendering-strategies','isr']::text[],false,104),
('nextjs','rendering-strategies','nextjs-rendering-strategies-105','What is on-demand revalidation, and how does it differ from time-based ISR?','Time-based ISR revalidates a page automatically after a fixed number of seconds elapses. On-demand revalidation instead lets your code explicitly trigger a re-generation immediately — for example, calling `revalidatePath()` or `revalidateTag()` from a server action or API route right after a CMS webhook fires — so content updates can appear instantly instead of waiting for the next interval.',null,'import { revalidatePath } from ''next/cache'';

await revalidatePath(''/blog/my-post'');','javascript','medium',ARRAY['nextjs','rendering-strategies','isr','caching']::text[],false,105),
('nextjs','rendering-strategies','nextjs-rendering-strategies-106','What is client-side rendering (CSR) in a Next.js app, and when does it apply?','CSR happens when a component is marked `''use client''` and fetches or computes its own data inside `useEffect` or with a client data-fetching library, rendering entirely in the browser after JavaScript loads. It applies for content that''s genuinely user- or interaction-specific and doesn''t benefit from being in the initial server-rendered HTML, such as a live chat widget or an authenticated user''s real-time notification feed.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','csr']::text[],false,106),
('nextjs','rendering-strategies','nextjs-rendering-strategies-107','What is the difference between a Server Component and a Client Component in the App Router?','A Server Component renders on the server, never ships its own JavaScript to the browser, and can directly access backend resources like databases, but it can''t use state, effects, or browser-only APIs. A Client Component, marked with the `''use client''` directive, renders on the server for the initial HTML too but is then hydrated and can re-render in the browser, so it supports hooks like `useState` and `useEffect` and can respond to user interaction.',null,'''use client'';
import { useState } from ''react'';

function Counter() {
  const [count, setCount] = useState(0);
  return <button onClick={() => setCount(count + 1)}>{count}</button>;
}','jsx','easy',ARRAY['nextjs','rendering-strategies','server-components','client-components']::text[],false,107),
('nextjs','rendering-strategies','nextjs-rendering-strategies-108','Why can''t you import a Server Component into a Client Component the same way you''d import any component?','Once you''re inside a Client Component boundary, React treats everything under it as needing to run/hydrate on the client, so a Server Component imported and rendered directly inside a Client Component would effectively become a Client Component too, losing its server-only benefits. The supported pattern is to pass Server Components down as `children` (or another prop) from a parent Server Component, so the Client Component just renders whatever was already rendered on the server without importing it itself.',null,'// Server Component passes a Server Component as children into a Client Component
<ClientWrapper>
  <ServerChild />
</ClientWrapper>','jsx','medium',ARRAY['nextjs','rendering-strategies','server-components','client-components']::text[],false,108),
('nextjs','rendering-strategies','nextjs-rendering-strategies-109','What does the `''use client''` directive actually do?','`''use client''` at the top of a file marks the boundary between server-only and client-capable code — everything exported from that file (and everything it imports that doesn''t have its own boundary) becomes part of the client bundle and can use hooks and browser APIs. It doesn''t mean the component skips server rendering entirely; it''s still rendered to HTML on the server for the initial load and then hydrated on the client.',null,null,null,'medium',ARRAY['nextjs','rendering-strategies','client-components']::text[],true,109),
('nextjs','rendering-strategies','nextjs-rendering-strategies-110','What is hydration, and why does a ''hydration mismatch'' error occur?','Hydration is the process where React attaches event listeners and internal state to server-rendered HTML already present in the DOM, making it interactive, rather than re-rendering from scratch. A hydration mismatch error happens when the HTML React generates on the client during hydration doesn''t match what the server actually sent — common causes include using `Date.now()`, `Math.random()`, or browser-only globals like `window` directly during render.',null,null,null,'medium',ARRAY['nextjs','rendering-strategies','hydration']::text[],true,110),
('nextjs','rendering-strategies','nextjs-rendering-strategies-111','How do you choose a rendering strategy for a specific page in the App Router, since there''s no explicit per-page function to pick one like `getStaticProps`?','The App Router infers the strategy from what a route does: a route with no dynamic data access is statically rendered by default at build time, while using dynamic APIs (like reading cookies/headers, or a `fetch` with `cache: ''no-store''`) automatically opts the route into dynamic (per-request) rendering. You can also force behavior explicitly with the `dynamic` route segment config, e.g. `export const dynamic = ''force-dynamic''`.',null,'export const dynamic = ''force-dynamic''; // opt a route out of static rendering','javascript','hard',ARRAY['nextjs','rendering-strategies','app-router']::text[],false,111),
('nextjs','rendering-strategies','nextjs-rendering-strategies-112','What does ''static rendering'' mean in the App Router, and how does it relate to SSG from the Pages Router?','Static rendering in the App Router is conceptually the App Router''s version of SSG: route output is computed once, either at build time or lazily on first request afterward, and served as cached HTML to subsequent visitors until revalidated. Unlike the Pages Router, there''s no separate `getStaticProps` function to opt in — it''s the default behavior for routes that don''t use dynamic data.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','app-router','ssg']::text[],false,112),
('nextjs','rendering-strategies','nextjs-rendering-strategies-113','What is ''dynamic rendering'' in the App Router, and what triggers it?','Dynamic rendering means a route is rendered on the server at request time rather than cached statically, analogous to SSR. It''s triggered automatically by using request-specific data like `cookies()`, `headers()`, or `searchParams`, by a `fetch` call configured with `cache: ''no-store''` or a short `revalidate`, or explicitly via `export const dynamic = ''force-dynamic''`.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','app-router']::text[],false,113),
('nextjs','rendering-strategies','nextjs-rendering-strategies-114','What is streaming SSR, and how does Next.js use it with Suspense?','Streaming SSR sends HTML to the browser in chunks as it becomes ready, instead of waiting for the entire page to finish rendering on the server before sending anything. Wrapping a slow-loading part of a page in a `<Suspense>` boundary lets Next.js send the rest of the page immediately and stream in that section''s HTML (plus a script to swap it in) once its data resolves, improving perceived load time.',null,'<Suspense fallback={<Spinner />}>
  <SlowComponent />
</Suspense>','jsx','hard',ARRAY['nextjs','rendering-strategies','streaming','suspense']::text[],false,114),
('nextjs','rendering-strategies','nextjs-rendering-strategies-115','Why is SSR generally better for SEO than pure client-side rendering?','With SSR, the HTML sent to the browser already contains the actual page content, so search engine crawlers can index it without needing to execute JavaScript. With pure CSR, the initial HTML is often just an empty shell, and while major crawlers like Googlebot can execute JavaScript, it''s slower, less reliable, and many other crawlers/social media link previewers don''t execute JS at all.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','ssr','seo']::text[],false,115),
('nextjs','rendering-strategies','nextjs-rendering-strategies-116','What are the performance trade-offs of SSR compared to SSG?','SSR adds latency on every request because the server has to render the page fresh each time (including any data fetching involved), whereas SSG serves a pre-built static file that can be cached at the edge/CDN and returned near-instantly. SSR is also more expensive to scale since it consumes server compute per request, while SSG scales essentially for free once built.',null,null,null,'medium',ARRAY['nextjs','rendering-strategies','ssr','ssg','performance']::text[],false,116),
('nextjs','rendering-strategies','nextjs-rendering-strategies-117','How did you opt a page into SSG versus SSR in the Pages Router?','Exporting `getStaticProps` from a page made it statically generated at build time. Exporting `getServerSideProps` instead made it server-rendered on every request. A page could only use one or the other, not both, and Next.js inferred the rendering strategy purely from which function was present.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','pages-router']::text[],false,117),
('nextjs','rendering-strategies','nextjs-rendering-strategies-118','What is partial prerendering (PPR) in Next.js?','Partial Prerendering is a newer Next.js rendering model that combines a static shell (prerendered at build time) with dynamic ''holes'' streamed in at request time, all within a single route response, instead of a route being entirely static or entirely dynamic. It lets a page serve instantly with static content while personalized or real-time sections stream in afterward, without the developer having to split the page into separate routes.',null,null,null,'hard',ARRAY['nextjs','rendering-strategies','partial-prerendering']::text[],false,118),
('nextjs','rendering-strategies','nextjs-rendering-strategies-119','What is edge rendering in Next.js, and how does it differ from rendering in a traditional Node.js server region?','Edge rendering runs a route''s server code in a lightweight JavaScript runtime deployed to many geographically distributed edge locations (rather than one or a few origin server regions), reducing latency for globally distributed users. The Edge Runtime is more restricted than Node.js — it doesn''t support all Node APIs (like the `fs` module) — so not every server function can run there unmodified.',null,'export const runtime = ''edge'';','javascript','medium',ARRAY['nextjs','rendering-strategies','edge']::text[],false,119),
('nextjs','rendering-strategies','nextjs-rendering-strategies-120','When would you deliberately choose CSR over SSR/SSG for a section of a Next.js page?','CSR makes sense for content that''s highly interactive and user-specific, doesn''t need to be indexed by search engines, or depends on data only available in the browser (like `localStorage`, geolocation, or a websocket connection) — for example, a shopping cart drawer, a live dashboard widget, or a rich in-browser editor. Rendering these on the server would add no SEO or first-paint value and complicate the server code for no benefit.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','csr']::text[],false,120),
('nextjs','rendering-strategies','nextjs-rendering-strategies-121','Can a single Next.js page combine multiple rendering strategies for different parts of itself?','Yes — this is one of the App Router''s core strengths. A page can be statically rendered overall, include a Suspense-wrapped section that streams in dynamically rendered content, and nest Client Components that render/re-render entirely in the browser, all within one route, rather than being locked into a single strategy for the whole page as the Pages Router effectively was.',null,null,null,'hard',ARRAY['nextjs','rendering-strategies','app-router']::text[],false,121),
('nextjs','rendering-strategies','nextjs-rendering-strategies-122','What''s the risk of calling `cookies()` or `headers()` inside a component that''s meant to be statically rendered?','Both functions read request-specific data, so calling either one inside a route automatically opts that entire route out of static rendering and into dynamic, per-request rendering — even if the rest of the route''s logic would otherwise be static. This can silently turn a fast cached page into a slower dynamic one if used without realizing the consequence.',null,null,null,'hard',ARRAY['nextjs','rendering-strategies','dynamic-apis']::text[],false,122),
('nextjs','rendering-strategies','nextjs-rendering-strategies-123','How does Next.js decide the revalidation time for a statically rendered route composed of multiple `fetch` calls with different `revalidate` values?','Next.js uses the shortest (most frequent) `revalidate` value among all `fetch` requests made within that route as the effective revalidation interval for the whole route''s static cache, ensuring no cached data on the page is served staler than the most time-sensitive fetch requested.',null,null,null,'hard',ARRAY['nextjs','rendering-strategies','isr','caching']::text[],false,123),
('nextjs','rendering-strategies','nextjs-rendering-strategies-124','What is the difference between rendering and hydration cost, and why does Next.js try to minimize client JavaScript?','Rendering cost is the work the server does to produce HTML; hydration cost is the work the browser does afterward to attach React''s behavior to that HTML, which requires downloading, parsing, and executing JavaScript for every hydrated component. By defaulting to Server Components (which need zero client JS), Next.js reduces the amount of code the browser must hydrate, directly improving metrics like Time to Interactive.',null,null,null,'medium',ARRAY['nextjs','rendering-strategies','hydration','performance']::text[],false,124),
('nextjs','rendering-strategies','nextjs-rendering-strategies-125','Why might using `Math.random()` or `new Date()` directly inside a Server Component''s render logic cause problems?','If that value differs between the server-rendered HTML and what the client would compute on re-render (or during hydration checks), React flags a hydration mismatch. For values that legitimately need to be random or time-based, the safer pattern is to compute them once and pass them down, or defer them to a Client Component with `useEffect` so they only run after hydration completes.',null,null,null,'medium',ARRAY['nextjs','rendering-strategies','hydration','gotcha']::text[],false,125),
('nextjs','rendering-strategies','nextjs-rendering-strategies-126','What does ''time to first byte'' (TTFB) tell you when comparing SSG, ISR, and SSR?','TTFB measures how quickly the server starts sending a response. SSG and cached ISR pages have very low TTFB because the HTML is already built and often served from a CDN edge cache. SSR has a higher TTFB because the server must run rendering logic and any data fetching for that specific request before it can respond.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','ssr','ssg','performance']::text[],false,126),
('nextjs','rendering-strategies','nextjs-rendering-strategies-127','How does Next.js''s rendering model handle a page that needs both fast static content and personalized, per-user content, like an e-commerce product page with a personalized ''recommended for you'' section?','The common pattern is to statically render (or ISR-cache) the bulk of the page — product details, images, descriptions — and wrap the personalized section in a Client Component or a Suspense-streamed dynamic component that fetches user-specific recommendations separately, so the static shell loads instantly while the personalized part fills in afterward.',null,null,null,'hard',ARRAY['nextjs','rendering-strategies','app-router','streaming']::text[],false,127),
('nextjs','rendering-strategies','nextjs-rendering-strategies-128','What is the `force-static` route segment config, and how does it differ from the default static behavior?','`export const dynamic = ''force-static''` forces a route to be statically rendered even if it uses dynamic APIs like `cookies()` or `searchParams` — those calls return empty values instead of opting the route into dynamic rendering. It''s useful when you know a route should always be static and want a build-time guarantee rather than relying on Next.js''s automatic inference.',null,'export const dynamic = ''force-static'';','javascript','hard',ARRAY['nextjs','rendering-strategies','app-router']::text[],false,128),
('nextjs','rendering-strategies','nextjs-rendering-strategies-129','Why is ''the App Router uses Server Components'' not the same statement as ''the App Router only does server rendering''?','Server Components describe where a component''s code runs and whether it ships JavaScript to the client, which is a per-component decision, not a whole-page rendering strategy — a page built from Server Components can still be statically generated, incrementally regenerated, or dynamically rendered per request. Rendering strategy (static vs. dynamic) and the server/client component split are two independent, orthogonal decisions in the App Router.',null,null,null,'hard',ARRAY['nextjs','rendering-strategies','server-components']::text[],false,129),
('nextjs','rendering-strategies','nextjs-rendering-strategies-130','What visual/UX problem can occur with pure CSR that SSR/SSG avoid, and what is it commonly called?','Pure CSR often shows a blank page or a loading spinner while JavaScript downloads and runs before any real content appears, sometimes called a ''flash of blank content'' or, once basic layout renders but data hasn''t loaded, a layout shift as content pops in. SSR and SSG avoid this by delivering meaningful content in the very first HTML response, so users see something immediately even before JavaScript finishes loading.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','csr','ux']::text[],false,130),
('nextjs','rendering-strategies','nextjs-rendering-strategies-131','How does the `revalidate` route segment config differ from passing `revalidate` to individual `fetch` calls?','Exporting `export const revalidate = 60` from a `page.js` or `layout.js` sets a default revalidation time for the entire route segment, applying to every cacheable data request within it unless a specific `fetch` call overrides it with its own `next: { revalidate }` option. It''s a convenient way to set one revalidation policy for a route without annotating every fetch call individually.',null,'export const revalidate = 60; // seconds','javascript','medium',ARRAY['nextjs','rendering-strategies','isr']::text[],false,131),
('nextjs','rendering-strategies','nextjs-rendering-strategies-132','Why is a marketing landing page usually a better fit for SSG than SSR, while a user''s account settings page is usually a better fit for SSR or CSR?','A marketing page shows the same content to every visitor and rarely changes, so pre-building it once at compile time and serving the cached HTML to everyone is both faster and cheaper. An account settings page is inherently per-user and often requires authentication, so it needs request-time or client-time data that can''t be baked into one shared static file ahead of time.',null,null,null,'easy',ARRAY['nextjs','rendering-strategies','ssg','ssr']::text[],false,132),
('nextjs','routing','nextjs-routing-101','How does file-based routing work in Next.js?','Next.js maps the file system directly to URL routes: in the App Router, each folder under `app/` becomes a URL segment, and a `page.js` file inside it makes that segment publicly reachable. In the Pages Router, every file directly under `pages/` (excluding special files) becomes a route matching its file path, e.g. `pages/about.js` maps to `/about`.',null,null,null,'easy',ARRAY['nextjs','routing']::text[],true,101),
('nextjs','routing','nextjs-routing-102','What is the key structural difference between routing in the App Router versus the Pages Router?','In the Pages Router, a file''s path directly determines both its route and its component in one file. In the App Router, a folder defines the URL segment, but the folder itself renders nothing — you need a `page.js` file inside it to make that segment an actual page, which allows the same folder to also hold `layout.js`, `loading.js`, and other co-located files without them becoming routes.',null,null,null,'medium',ARRAY['nextjs','routing','app-router']::text[],false,102),
('nextjs','routing','nextjs-routing-103','How do you create a dynamic route segment in Next.js?','Wrap a folder or file name in square brackets to make it a dynamic segment that matches any value at that position in the URL. In the App Router, `app/blog/[slug]/page.js` matches `/blog/anything` and exposes `anything` as `params.slug`; the Pages Router equivalent is `pages/blog/[slug].js`.',null,'// app/blog/[slug]/page.js
export default function BlogPost({ params }) {
  return <h1>{params.slug}</h1>;
}','jsx','easy',ARRAY['nextjs','routing','dynamic-routes']::text[],true,103),
('nextjs','routing','nextjs-routing-104','What is a catch-all route segment, and how does it differ from an optional catch-all?','A catch-all segment, written `[...slug]`, matches any number of path segments after that point and exposes them as an array, e.g. `/a/b/c` becomes `[''a'', ''b'', ''c'']`; it requires at least one segment to match. An optional catch-all, written `[[...slug]]`, additionally matches the route with zero extra segments (the base path itself), which a plain catch-all does not.','// app/shop/[[...slug]]/page.js matches /shop, /shop/a, /shop/a/b',null,null,'medium',ARRAY['nextjs','routing','dynamic-routes']::text[],false,104),
('nextjs','routing','nextjs-routing-105','How do nested layouts work in the App Router?','Each folder in the route hierarchy can define its own `layout.js`, and layouts nest automatically: a route''s rendered output is wrapped by its own layout, which is wrapped by its parent''s layout, all the way up to the root layout. This lets you share UI (like a sidebar for a specific section) with only the routes under that folder, without re-declaring it in every page.',null,null,null,'easy',ARRAY['nextjs','routing','layouts']::text[],false,105),
('nextjs','routing','nextjs-routing-106','What is a route group in the App Router, and what problem does it solve?','A route group is a folder name wrapped in parentheses, like `(marketing)`, which lets you organize routes into logical groups or apply a shared layout to a subset of routes without that folder name appearing in the URL. It solves the problem of needing different root-level layouts for different sections of a site (e.g. a marketing site vs. a dashboard) that would otherwise all share one top-level layout.','app/(marketing)/about/page.js  // URL is still /about',null,null,'medium',ARRAY['nextjs','routing','route-groups']::text[],false,106),
('nextjs','routing','nextjs-routing-107','What is a parallel route in the App Router, and when would you use one?','Parallel routes, denoted with a `@folder` naming convention (a ''slot''), let you render two or more independent pages in the same layout simultaneously, each with its own loading and error state — useful for things like a dashboard that shows an analytics panel and a team activity feed side by side, each fetching and rendering independently.','app/@analytics/page.js and app/@team/page.js rendered together via layout.js''s {analytics} and {team} props',null,null,'hard',ARRAY['nextjs','routing','parallel-routes']::text[],false,107),
('nextjs','routing','nextjs-routing-108','What is an intercepting route in the App Router, and what''s a typical use case?','Intercepting routes, using conventions like `(.)folder`, let you load a route from another part of the app within the current layout, commonly used to show a photo in a modal when navigated to via client-side navigation, while still showing the full standalone page if the URL is loaded directly (e.g. on refresh or shared link). Instagram''s photo-in-modal-over-feed pattern is the classic example.',null,null,null,'hard',ARRAY['nextjs','routing','intercepting-routes']::text[],false,108),
('nextjs','routing','nextjs-routing-109','How do you programmatically navigate between pages in the App Router?','Use the `useRouter` hook from `next/navigation` inside a client component and call `router.push(path)` to navigate, or `router.replace(path)` to navigate without adding a history entry. Note this is a different `useRouter` from the one in `next/router`, which is used only in the Pages Router.',null,'''use client'';
import { useRouter } from ''next/navigation'';

function LoginButton() {
  const router = useRouter();
  return <button onClick={() => router.push(''/dashboard'')}>Go</button>;
}','jsx','medium',ARRAY['nextjs','routing','navigation']::text[],false,109),
('nextjs','routing','nextjs-routing-110','What is the `<Link>` component and why should it be used instead of a plain `<a>` tag?','`next/link`''s `<Link>` component enables client-side navigation between routes without a full page reload, and it automatically prefetches the linked page''s code in the background when it scrolls into the viewport (in production), making subsequent navigation feel instant. A plain `<a>` tag would trigger a full browser page reload for every internal navigation, losing client-side state and being noticeably slower.',null,'import Link from ''next/link'';

<Link href="/about">About</Link>','jsx','easy',ARRAY['nextjs','routing','link']::text[],true,110),
('nextjs','routing','nextjs-routing-111','How do you read the current pathname and search params inside a client component in the App Router?','The `usePathname` hook from `next/navigation` returns the current URL''s path, and `useSearchParams` returns a read-only `URLSearchParams`-like object for the query string. Both are client-only hooks and must be used inside a component marked `''use client''`.',null,'''use client'';
import { usePathname, useSearchParams } from ''next/navigation'';

function Debug() {
  const pathname = usePathname();
  const searchParams = useSearchParams();
  return <p>{pathname}?{searchParams.toString()}</p>;
}','jsx','medium',ARRAY['nextjs','routing','hooks']::text[],false,111),
('nextjs','routing','nextjs-routing-112','How did dynamic routing work in the Pages Router before the App Router existed?','A file named with square brackets, like `pages/posts/[id].js`, became a dynamic route, and the matched value was accessed via the `useRouter` hook''s `router.query.id` on the client, or as the `params.id` argument passed into `getStaticProps`/`getServerSideProps` on the server.',null,'import { useRouter } from ''next/router'';

function Post() {
  const { query } = useRouter();
  return <p>{query.id}</p>;
}','jsx','medium',ARRAY['nextjs','routing','pages-router']::text[],false,112),
('nextjs','routing','nextjs-routing-113','How do you build a custom 404 page in Next.js?','In the App Router, create `app/not-found.js`, which Next.js renders automatically for unmatched routes or when the `notFound()` function is called from a server component. In the Pages Router, create `pages/404.js`, which Next.js statically generates and serves for any unmatched route.',null,null,null,'easy',ARRAY['nextjs','routing','error-handling']::text[],false,113),
('nextjs','routing','nextjs-routing-114','What does the `notFound()` function do in the App Router?','Calling `notFound()` inside a server component or route handler immediately halts rendering and tells Next.js to render the nearest `not-found.js` boundary with a 404 status code — commonly used when a dynamic route''s data lookup (e.g. a blog post by slug) comes back empty.',null,'import { notFound } from ''next/navigation'';

async function Page({ params }) {
  const post = await getPost(params.slug);
  if (!post) notFound();
  return <article>{post.title}</article>;
}','jsx','easy',ARRAY['nextjs','routing','error-handling']::text[],false,114),
('nextjs','routing','nextjs-routing-115','How do you set up redirects and rewrites in Next.js?','Both are configured in `next.config.js` via async `redirects()` and `rewrites()` functions that return arrays of source/destination pairs. A redirect sends the browser a 301/302 response and changes the visible URL; a rewrite proxies the request to a different internal path while keeping the URL the user sees unchanged.',null,'async redirects() {
  return [{ source: ''/old'', destination: ''/new'', permanent: true }];
}','javascript','medium',ARRAY['nextjs','routing','redirects']::text[],false,115),
('nextjs','routing','nextjs-routing-116','How can you trigger a redirect from inside a server component or server action?','Call the `redirect()` function from `next/navigation`, passing the destination path — it works by throwing a special error internally that Next.js''s rendering pipeline catches to perform the redirect, so any code after the call in the same function won''t execute.',null,'import { redirect } from ''next/navigation'';

async function Page() {
  const user = await getUser();
  if (!user) redirect(''/login'');
  return <Dashboard user={user} />;
}','jsx','easy',ARRAY['nextjs','routing','redirects']::text[],false,116),
('nextjs','routing','nextjs-routing-117','What is the difference between `params` and `searchParams` in an App Router page component?','`params` contains the values matched by dynamic route segments in the URL path (e.g. `[id]` becomes `params.id`). `searchParams` contains the parsed query string (e.g. `?sort=asc` becomes `searchParams.sort`). Both are passed as props to `page.js` components, with `params` also available to layouts.',null,'// app/products/[id]/page.js
function Page({ params, searchParams }) {
  // params.id, searchParams.sort
}','jsx','easy',ARRAY['nextjs','routing','dynamic-routes']::text[],false,117),
('nextjs','routing','nextjs-routing-118','How do you create nested dynamic routes, such as `/shop/[category]/[product]`?','Nest dynamic segment folders inside each other in the App Router: `app/shop/[category]/[product]/page.js`. Both segments are available together as `params.category` and `params.product` on the page component, matching URLs like `/shop/shoes/sneaker-42`.',null,null,null,'easy',ARRAY['nextjs','routing','dynamic-routes']::text[],false,118),
('nextjs','routing','nextjs-routing-119','What is `generateStaticParams` used for?','`generateStaticParams`, exported from a dynamic route''s `page.js`, tells Next.js which values of a dynamic segment to pre-render at build time (similar in purpose to `getStaticPaths` in the Pages Router). It returns an array of param objects, and Next.js statically generates one page per entry, falling back to on-demand rendering for any params not included, depending on `dynamicParams` settings.',null,'export async function generateStaticParams() {
  const posts = await getPosts();
  return posts.map((post) => ({ slug: post.slug }));
}','jsx','medium',ARRAY['nextjs','routing','static-generation']::text[],true,119),
('nextjs','routing','nextjs-routing-120','How did `getStaticPaths` work in the Pages Router, and what does its `fallback` option control?','`getStaticPaths`, exported alongside `getStaticProps` on a dynamic page, returns the list of param combinations to pre-render at build time. Its `fallback` option controls what happens for paths not in that list: `false` returns a 404, `true` serves a fallback loading state while generating the page on first request, and `''blocking''` waits server-side and serves the fully rendered page on first request with no fallback flash.',null,null,null,'medium',ARRAY['nextjs','routing','pages-router','static-generation']::text[],false,120),
('nextjs','routing','nextjs-routing-121','How do you show a loading UI while a route segment''s data is being fetched in the App Router?','Add a `loading.js` file to a route segment''s folder — Next.js automatically wraps that segment''s `page.js` in a React Suspense boundary and renders `loading.js` as the fallback until the async data-fetching in the page finishes, giving instant loading states without manually adding Suspense yourself.',null,'// app/dashboard/loading.js
export default function Loading() {
  return <p>Loading dashboard...</p>;
}','jsx','medium',ARRAY['nextjs','routing','loading-states']::text[],false,121),
('nextjs','routing','nextjs-routing-122','How does Next.js handle route-level error boundaries in the App Router?','An `error.js` file in a route segment''s folder automatically wraps that segment in a React error boundary. It must be a client component and receives `error` and `reset` props, where `reset` lets the user retry rendering the segment without a full page reload.',null,'''use client'';
export default function Error({ error, reset }) {
  return <button onClick={() => reset()}>Try again: {error.message}</button>;
}','jsx','medium',ARRAY['nextjs','routing','error-handling']::text[],false,122),
('nextjs','routing','nextjs-routing-123','What is route prefetching in Next.js and when does it happen automatically?','Prefetching means Next.js loads a route''s code (and, for static routes, its data) in the background before the user actually navigates to it, so the navigation feels instant. In the App Router, `<Link>` components automatically prefetch routes as they enter the viewport in production mode; it''s disabled in development and can be turned off per-link with `prefetch={false}`.',null,null,null,'medium',ARRAY['nextjs','routing','prefetching','performance']::text[],false,123),
('nextjs','routing','nextjs-routing-124','Can a single Next.js app mix the App Router and the Pages Router at the same time?','Yes — Next.js allows `app/` and `pages/` to coexist in the same project for incremental migration. If a route exists in both, the `app/` version takes precedence; this dual setup is meant as a transitional state rather than a long-term architecture.',null,null,null,'medium',ARRAY['nextjs','routing','migration']::text[],false,124),
('nextjs','routing','nextjs-routing-125','How do you create a route that only handles specific HTTP methods, like a webhook endpoint, without it becoming a normal page?','In the App Router, add a `route.js` file to a folder instead of `page.js`, and export functions named after HTTP methods (`GET`, `POST`, etc.) — this makes the segment a Route Handler that returns raw responses instead of rendering UI. A folder can''t have both `page.js` and `route.js` at the same segment level.',null,'// app/api/webhook/route.js
export async function POST(request) {
  const body = await request.json();
  return Response.json({ received: true });
}','javascript','medium',ARRAY['nextjs','routing','route-handlers']::text[],false,125),
('nextjs','routing','nextjs-routing-126','What is shallow routing, and does the App Router support it the same way the Pages Router did?','Shallow routing means updating the URL (e.g. query params) without re-running data-fetching functions or losing component state — in the Pages Router this was done via `router.push(url, undefined, { shallow: true })`. The App Router doesn''t have a direct shallow option on `useRouter`; similar effects are typically achieved by manipulating `URLSearchParams` and using `router.replace` combined with client state, or `history.pushState` directly.',null,null,null,'medium',ARRAY['nextjs','routing','pages-router']::text[],false,126),
('nextjs','routing','nextjs-routing-127','How do you access dynamic route params inside a layout, and what''s the limitation compared to a page?','A `layout.js` receives `params` for the dynamic segments it wraps, just like a page does, but it does not receive `searchParams` — layouts don''t re-render on query-string-only navigation, so exposing search params to them would encourage patterns that break that optimization.',null,null,null,'hard',ARRAY['nextjs','routing','layouts','dynamic-routes']::text[],false,127),
('nextjs','routing','nextjs-routing-128','What naming convention marks a folder as private in the App Router, excluding it from routing?','Prefixing a folder name with an underscore, like `_components` or `_lib`, opts it and all its subfolders out of routing entirely, even if it contains a `page.js`. It''s useful for colocating implementation details (helper components, utilities) next to the routes that use them without accidentally creating a URL.','app/dashboard/_components/Chart.js  // not routable',null,null,'medium',ARRAY['nextjs','routing','conventions']::text[],false,128),
('nextjs','routing','nextjs-routing-129','How do you handle internationalized (i18n) routing in Next.js?','The Pages Router has built-in i18n routing support configured in `next.config.js` (locale list, default locale, and automatic locale-prefixed paths like `/fr/about`). The App Router doesn''t have built-in i18n routing; instead you implement it yourself using a `[locale]` dynamic segment at the root of `app/` combined with middleware to detect and redirect based on the user''s preferred locale.',null,null,null,'medium',ARRAY['nextjs','routing','i18n']::text[],false,129),
('nextjs','routing','nextjs-routing-130','What happens if two different route segments could match the same URL, such as a static route and a dynamic route at the same level?','Next.js prioritizes more specific matches: a static segment (e.g. `app/blog/latest/page.js`) takes precedence over a dynamic segment at the same level (e.g. `app/blog/[slug]/page.js`) for a request to `/blog/latest`. This lets you carve out specific static pages that would otherwise be swallowed by a dynamic catch-all sibling.',null,null,null,'hard',ARRAY['nextjs','routing','dynamic-routes']::text[],false,130),
('nextjs','routing','nextjs-routing-131','How do you link to a dynamic route and pass along its parameter using the `<Link>` component?','Build the `href` string with the dynamic value interpolated directly into the path, matching the folder''s bracket segment, since `<Link>` doesn''t have a separate params API — the href is just the final URL string (or a URL object with `pathname`/`query` in the Pages Router style).',null,'<Link href={`/blog/${post.slug}`}>{post.title}</Link>','jsx','easy',ARRAY['nextjs','routing','link','dynamic-routes']::text[],false,131),
('nodejs','event-loop-async','nodejs-event-loop-async-101','What is the event loop in Node.js, and why is it central to how Node handles concurrency?','The event loop is the mechanism that lets Node perform non-blocking I/O despite JavaScript running on a single thread — it repeatedly checks whether there''s pending work (timers, I/O callbacks, immediates) to process, executing callbacks as their associated operations complete rather than blocking the thread while waiting. It''s what allows a single Node process to handle thousands of concurrent connections without spawning a thread per request.',null,null,null,'easy',ARRAY['nodejs','event-loop']::text[],true,101),
('nodejs','event-loop-async','nodejs-event-loop-async-102','What are the main phases of the Node.js event loop, in order?','The libuv event loop cycles through: timers (expired setTimeout/setInterval callbacks), pending callbacks (certain deferred system-level callbacks), idle/prepare (internal use), poll (retrieving new I/O events and running their callbacks), check (setImmediate callbacks), and close callbacks (like socket.on(''close'')). Microtasks (process.nextTick and Promise callbacks) run between each of these phases, not as a phase themselves.',null,null,null,'hard',ARRAY['nodejs','event-loop']::text[],true,102),
('nodejs','event-loop-async','nodejs-event-loop-async-103','What is the difference between the call stack and the event loop?','The call stack is where JavaScript tracks function calls currently executing, following LIFO order — it''s what the single JS thread is actively working through synchronously. The event loop is a separate mechanism that watches for the call stack to become empty and then pushes queued callbacks (from timers, I/O, microtasks) onto it, effectively scheduling asynchronous work to run once synchronous code has finished.',null,null,null,'easy',ARRAY['nodejs','event-loop']::text[],false,103),
('nodejs','event-loop-async','nodejs-event-loop-async-104','What is the difference between microtasks and macrotasks in Node.js?','Macrotasks include timer callbacks, I/O callbacks, and setImmediate callbacks, each processed in a distinct event loop phase, with only one macrotask typically running per loop iteration per phase. Microtasks — process.nextTick callbacks and resolved Promise .then() callbacks — are drained completely (including any new microtasks they schedule) between every phase transition and even between individual macrotasks, so microtasks always run before the loop proceeds further.',null,null,null,'hard',ARRAY['nodejs','event-loop','microtasks']::text[],true,104),
('nodejs','event-loop-async','nodejs-event-loop-async-105','In Node.js, does process.nextTick() or a resolved Promise''s .then() callback run first?','process.nextTick() callbacks run before Promise microtask callbacks — Node maintains a separate nextTick queue that''s fully drained first, and only then does it process the Promise microtask queue, both of which happen before the event loop advances to its next phase. This makes nextTick even higher priority than promise-based microtasks, which is why heavy recursive use of it can starve I/O.',null,'process.nextTick(() => console.log(''nextTick''));
Promise.resolve().then(() => console.log(''promise''));
// Output: nextTick, then promise','javascript','medium',ARRAY['nodejs','event-loop','microtasks']::text[],false,105),
('nodejs','event-loop-async','nodejs-event-loop-async-106','Given setTimeout(fn, 0) and setImmediate(fn) both called from the main module, which runs first?','It''s actually not guaranteed from the main module — timer precision means setTimeout(fn, 0) may fire before or after setImmediate depending on system performance and how long process startup took, since both compete near the start of the loop. However, when both are called from inside an I/O callback (e.g. within fs.readFile''s callback), setImmediate is guaranteed to run first, since the poll phase transitions directly to the check phase before circling back to timers.',null,'const fs = require(''fs'');
fs.readFile(__filename, () => {
  setTimeout(() => console.log(''timeout''), 0);
  setImmediate(() => console.log(''immediate'')); // this runs first here
});','javascript','hard',ARRAY['nodejs','event-loop','timers']::text[],false,106),
('nodejs','event-loop-async','nodejs-event-loop-async-107','Why can heavy use of process.nextTick() starve the event loop?','Because Node fully drains the nextTick queue before moving on, if a nextTick callback recursively schedules another nextTick callback, that queue can grow indefinitely and never let the event loop proceed to the timers, I/O, or check phases — effectively blocking all other operations even though each individual callback is fast. This is a well-known pitfall that led to changes/guidance discouraging deeply recursive nextTick usage in library code.',null,null,null,'hard',ARRAY['nodejs','event-loop','process']::text[],false,107),
('nodejs','event-loop-async','nodejs-event-loop-async-108','How does Node.js handle a CPU-intensive synchronous operation, like a large JSON.parse or a tight loop?','Since JS execution is single-threaded, a long synchronous computation blocks the event loop entirely — no other callbacks, timers, or incoming requests can be processed until it finishes, which can make a server unresponsive under load. The usual fixes are breaking the work into smaller async chunks, offloading it to a worker thread, or moving it to a separate process/service if it''s consistently expensive.',null,null,null,'medium',ARRAY['nodejs','event-loop','performance']::text[],false,108),
('nodejs','event-loop-async','nodejs-event-loop-async-109','What does it mean for an operation to be ''non-blocking'' in Node, and give an example of a blocking equivalent.','A non-blocking operation returns control to the caller immediately and delivers its result later via a callback, promise, or event, letting other code run in the meantime — e.g. fs.readFile(path, callback). Its blocking equivalent, fs.readFileSync(path), halts the entire thread until the file is fully read, which is fine for one-off CLI scripts but harmful in a server handling concurrent requests.',null,'// Blocking
const data = fs.readFileSync(''file.txt'');
// Non-blocking
fs.readFile(''file.txt'', (err, data) => { /* ... */ });','javascript','easy',ARRAY['nodejs','event-loop','fs']::text[],false,109),
('nodejs','event-loop-async','nodejs-event-loop-async-110','What is the libuv thread pool, and which Node operations use it?','The thread pool is a fixed-size pool of background worker threads (default size 4, configurable via UV_THREADPOOL_SIZE) that libuv uses to run operations the OS doesn''t provide a truly async interface for. Examples include most fs module operations, some crypto functions (like pbkdf2 and scrypt), DNS lookups via dns.lookup, and zlib compression — the main thread stays free while these run in the background, with a callback fired on completion.',null,null,null,'medium',ARRAY['nodejs','event-loop','libuv']::text[],false,110),
('nodejs','event-loop-async','nodejs-event-loop-async-111','How would you convert a callback-based Node API into one that returns a Promise?','You can wrap the call manually in new Promise((resolve, reject) => {...}), calling reject on an error-first callback''s err argument and resolve otherwise, or use Node''s built-in util.promisify() utility, which does exactly that for any function following the standard (err, result) => callback convention.',null,'const util = require(''util'');
const fs = require(''fs'');
const readFile = util.promisify(fs.readFile);
const data = await readFile(''file.txt'', ''utf8'');','javascript','medium',ARRAY['nodejs','async','promises']::text[],true,111),
('nodejs','event-loop-async','nodejs-event-loop-async-112','What does the ''error-first callback'' convention mean in Node.js APIs, and why is it used?','By convention, Node-style callbacks take an error as their first argument (null/undefined if no error occurred), followed by the result: callback(err, result). This standardizes error handling across the entire core API and the broader ecosystem, since every callback consumer can check the same first argument rather than each API having its own ad hoc error-reporting mechanism.',null,'fs.readFile(''file.txt'', (err, data) => {
  if (err) return console.error(err);
  console.log(data.toString());
});','javascript','easy',ARRAY['nodejs','async','error-handling']::text[],false,112),
('nodejs','event-loop-async','nodejs-event-loop-async-113','Why does an error thrown inside a plain Node-style asynchronous callback not get caught by a surrounding try/catch?','By the time the callback actually runs, the original synchronous call (and its try/catch context) has already finished executing and been popped off the call stack — the callback runs later, in a fresh stack frame triggered by the event loop, so a try/catch wrapped around the original call can''t intercept an exception thrown inside it. This is why the error-first callback pattern exists: errors must be explicitly passed as data instead of thrown.',null,'try {
  fs.readFile(''x'', () => { throw new Error(''boom''); }); // NOT caught here
} catch (e) { /* never runs */ }','javascript','hard',ARRAY['nodejs','async','error-handling']::text[],false,113),
('nodejs','event-loop-async','nodejs-event-loop-async-114','How does async/await work under the hood in Node.js, in terms of the event loop?','await pauses execution of the async function at that point without blocking the thread — control returns to the event loop, which continues processing other callbacks, and once the awaited promise settles, the rest of the async function resumes as a microtask. It''s syntactic sugar over promise chaining, so it ultimately relies on the same microtask queue mechanics as .then().',null,null,null,'medium',ARRAY['nodejs','async','event-loop']::text[],false,114),
('nodejs','event-loop-async','nodejs-event-loop-async-115','What keeps a Node.js process alive, and what causes it to exit on its own?','A Node process stays alive as long as there''s at least one pending operation in the event loop''s queue: an open server listening for connections, an active timer, a pending I/O callback, or an unresolved async operation registered with libuv. Once there''s nothing left to process — no pending timers, no open handles, no queued callbacks — the event loop has nothing more to do and the process exits naturally with code 0.',null,null,null,'easy',ARRAY['nodejs','event-loop','process']::text[],false,115),
('nodejs','event-loop-async','nodejs-event-loop-async-116','What is server.unref() (or timer.unref()) used for?','unref() marks a handle (like a timer or an open socket) as not counting toward keeping the event loop alive, so if it''s the only thing left pending, the process can still exit naturally instead of hanging. It''s commonly used for background housekeeping timers that shouldn''t prevent a script or CLI tool from exiting once its main work is done.',null,'const timer = setInterval(() => {}, 1000);
timer.unref(); // won''t keep the process alive by itself','javascript','medium',ARRAY['nodejs','event-loop','timers']::text[],false,116),
('nodejs','event-loop-async','nodejs-event-loop-async-117','How would you run multiple independent async database queries concurrently in a Node.js route handler?','Start all the query calls first (without awaiting each individually) so they all begin their async work in the same tick, collect their returned promises, and await them together with Promise.all — this runs them concurrently rather than serially, since each query''s I/O wait time can overlap with the others''.',null,'const [user, orders] = await Promise.all([
  db.query(''SELECT * FROM users WHERE id = $1'', [id]),
  db.query(''SELECT * FROM orders WHERE user_id = $1'', [id]),
]);','javascript','medium',ARRAY['nodejs','async','promises']::text[],false,117),
('nodejs','event-loop-async','nodejs-event-loop-async-118','What is an unhandled promise rejection in Node.js, and what happens if you don''t handle it?','It occurs when a promise rejects and no .catch() (or try/catch around an await) observes that rejection. In modern Node versions (since v15), an unhandled rejection crashes the process by default (previously it just logged a warning), which is meant to surface silent async failures early rather than let them go unnoticed in production.',null,null,null,'medium',ARRAY['nodejs','async','error-handling']::text[],true,118),
('nodejs','event-loop-async','nodejs-event-loop-async-119','How can you implement a delay/sleep function in Node.js using promises?','Wrap setTimeout in a Promise that resolves once the timer fires, then await it — there''s no native sleep() function in JS/Node, so this pattern is the idiomatic way to pause execution within an async function without blocking the event loop.',null,'const sleep = (ms) => new Promise((resolve) => setTimeout(resolve, ms));
await sleep(1000);
console.log(''1 second later'');','javascript','easy',ARRAY['nodejs','async','timers']::text[],false,119),
('nodejs','event-loop-async','nodejs-event-loop-async-120','What''s the risk of using a synchronous method like fs.readFileSync inside an Express route handler?','Since Node handles many concurrent requests on a single thread, a synchronous call blocks that thread entirely for its duration — every other in-flight request (and the ability to accept new ones) is frozen until it completes, which can badly degrade throughput and latency under load, especially for larger files. The async fs.readFile (or fs.promises.readFile) equivalent avoids this by not blocking the event loop.',null,null,null,'medium',ARRAY['nodejs','event-loop','express']::text[],false,120),
('nodejs','event-loop-async','nodejs-event-loop-async-121','What is ''callback hell'' and how does Node''s ecosystem typically address it today?','Callback hell refers to deeply nested, hard-to-read callback-based async code, historically common in Node due to its error-first callback convention for I/O. Modern Node code largely avoids it by using Promise-returning APIs (fs.promises, util.promisify) combined with async/await, which lets sequential asynchronous logic read like ordinary synchronous code.',null,null,null,'easy',ARRAY['nodejs','async']::text[],false,121),
('nodejs','event-loop-async','nodejs-event-loop-async-122','How does Node.js decide when a setInterval callback should be paused or cleared to avoid memory/resource leaks?','Node itself doesn''t decide this automatically — an interval keeps running (and keeps the process alive) until explicitly cleared with clearInterval(id) or its handle is unref()''d, so developers must track the returned interval ID and clear it when the associated work is no longer needed, such as on server shutdown or when a connection closes.',null,null,null,'medium',ARRAY['nodejs','timers','memory']::text[],false,122),
('nodejs','event-loop-async','nodejs-event-loop-async-123','What''s the difference between setImmediate() and process.nextTick() in terms of intended use?','process.nextTick() is meant for very high-priority work that should happen before the event loop continues at all, often used internally by APIs to ensure a callback isn''t called synchronously (maintaining consistent async behavior). setImmediate() is meant to defer work until after the current poll phase''s I/O callbacks finish but still within the current loop cycle, useful for breaking up CPU-heavy work into chunks without starving I/O the way recursive nextTick calls can.',null,null,null,'hard',ARRAY['nodejs','event-loop','process']::text[],false,123),
('nodejs','event-loop-async','nodejs-event-loop-async-124','How would you measure how ''busy'' or blocked the Node.js event loop is?','You can measure event loop lag/delay by scheduling a callback (e.g. with setImmediate or a timer) and measuring how much longer it took to fire than expected — a growing gap indicates the loop is busy with synchronous work. Node also exposes perf_hooks.monitorEventLoopDelay() for this, and APM tools/libraries like clinic.js or blocked-at build on similar techniques to flag blocking code in production.',null,null,null,'medium',ARRAY['nodejs','event-loop','performance']::text[],false,124),
('nodejs','event-loop-async','nodejs-event-loop-async-125','Why might async/await make debugging stack traces easier compared to raw callback-based code?','Because async/await code is structured to look synchronous, an uncaught error''s stack trace tends to reflect the logical call sequence of the async function, whereas deeply nested or chained callbacks often produce stack traces that only show the immediate callback context, losing the surrounding call history. Modern V8 also improves ''async stack traces'' specifically to stitch together the awaited call chain for clearer debugging.',null,null,null,'medium',ARRAY['nodejs','async','debugging']::text[],false,125),
('nodejs','event-loop-async','nodejs-event-loop-async-126','What happens if you call an async function without awaiting or handling its returned promise?','The function still executes, but the caller has no way to know when it finishes or whether it threw — this is sometimes intentional ''fire and forget'', but if the promise rejects and nothing else attaches a .catch() to it, it becomes an unhandled rejection, which in modern Node can crash the process.',null,null,null,'medium',ARRAY['nodejs','async','promises']::text[],false,126),
('nodejs','event-loop-async','nodejs-event-loop-async-127','How does the ''poll'' phase of the Node event loop work?','The poll phase calculates how long it should block and wait for new I/O events (network connections, file reads, etc.), then processes events in its queue, executing their callbacks. If the poll queue is empty and there are setImmediate callbacks scheduled, it moves on to the check phase immediately rather than waiting; if there are pending timers whose thresholds have been reached, it also wraps up early so they can be processed on the next loop iteration.',null,null,null,'hard',ARRAY['nodejs','event-loop']::text[],false,127),
('nodejs','event-loop-async','nodejs-event-loop-async-128','How would you run several async tasks with a concurrency limit in Node.js (e.g., processing 1000 files but only 10 at a time)?','A common approach is to maintain a pool of ''worker'' promises equal to the concurrency limit, each pulling the next item from a shared queue and processing it, then repeating until the queue is empty — effectively a manual worker-pool pattern. Libraries like p-limit or p-queue implement this pattern so you don''t have to hand-roll it, wrapping each task call to enforce the limit transparently.',null,'async function processWithLimit(items, limit, fn) {
  const queue = [...items];
  const workers = Array.from({ length: limit }, async () => {
    while (queue.length) await fn(queue.shift());
  });
  await Promise.all(workers);
}','javascript','hard',ARRAY['nodejs','async','performance']::text[],false,128),
('nodejs','event-loop-async','nodejs-event-loop-async-129','What is the difference between fs.readFile, fs.readFileSync, and fs.promises.readFile?','fs.readFile is the classic callback-based async API. fs.readFileSync blocks the thread until the read completes, returning the result directly. fs.promises.readFile (or the newer fs/promises module) is the promise-based async variant, letting you use await instead of a callback while remaining non-blocking — generally the preferred style in modern async/await codebases.',null,'import { readFile } from ''node:fs/promises'';
const data = await readFile(''file.txt'', ''utf8'');','javascript','easy',ARRAY['nodejs','async','fs']::text[],false,129),
('nodejs','event-loop-async','nodejs-event-loop-async-130','Why is it recommended to avoid mixing callback-style and promise-style async code within the same function?','Mixing styles makes error handling inconsistent — errors from a callback path need an if (err) check while errors from a promise path need a .catch() or try/catch, and it''s easy to forget one, letting an error silently disappear. It also makes control flow harder to reason about, since the two styles resume execution differently (callback invocation vs. microtask scheduling), so consistently picking one style (usually async/await) per function keeps the logic predictable.',null,null,null,'medium',ARRAY['nodejs','async']::text[],false,130),
('nodejs','event-loop-async','nodejs-event-loop-async-131','What is a common cause of an Express (or generic Node) server hanging and never responding to a request?','A frequent cause is an async route handler whose promise never settles — e.g. a database call awaiting a connection that never resolves, or a middleware that calls next() conditionally but misses a branch, leaving the response never sent and the connection open until a client-side timeout. Adding explicit request timeouts and ensuring every code path either responds or explicitly errors out helps prevent this.',null,null,null,'medium',ARRAY['nodejs','async','express']::text[],false,131),
('nodejs','event-loop-async','nodejs-event-loop-async-132','How does Node''s event loop handle multiple timers scheduled with different delays?','Node maintains timers in a way that lets it process them in order of when they''re due to fire; during the timers phase, it executes callbacks for any timer whose threshold has elapsed, in the order they expire. Because JS is single-threaded, if a callback earlier in the phase takes a long time, later timers due around the same moment will fire late, since the loop can''t run two callbacks simultaneously.',null,null,null,'medium',ARRAY['nodejs','event-loop','timers']::text[],false,132),
('nodejs','event-loop-async','nodejs-event-loop-async-133','What is ''async context'' or AsyncLocalStorage used for in Node.js?','AsyncLocalStorage (from the ''async_hooks'' module) lets you store and retrieve request-scoped data — like a request ID or authenticated user — that stays associated with a chain of asynchronous calls without having to explicitly pass it as a parameter through every function. It''s commonly used for logging/tracing context in web servers, similar to thread-local storage in multi-threaded languages.',null,'const { AsyncLocalStorage } = require(''async_hooks'');
const als = new AsyncLocalStorage();
als.run({ requestId: ''123'' }, () => {
  // any async code here can call als.getStore() to read { requestId: ''123'' }
});','javascript','hard',ARRAY['nodejs','async','async-hooks']::text[],false,133),
('nodejs','express-apis','nodejs-express-apis-101','What is Express.js, and what problem does it solve on top of Node''s built-in http module?','Express is a minimal web framework built on Node''s http module that adds routing, middleware chaining, and helpers for handling requests/responses (like parsing bodies or sending JSON) without hand-rolling them from scratch. It doesn''t replace http — it wraps it, giving you a much more convenient API for building REST APIs and web servers while still exposing the underlying request/response objects when needed.',null,null,null,'easy',ARRAY['nodejs','express']::text[],true,101),
('nodejs','express-apis','nodejs-express-apis-102','What is middleware in Express, and what is the signature of a middleware function?','Middleware is a function that runs during the request-response cycle, with access to the request, response, and a next() function to pass control to the next middleware in the chain: (req, res, next) => {...}. Middleware can modify req/res, end the response, or call next() to continue — Express apps are essentially a pipeline of middleware functions executed in the order they''re registered.',null,'app.use((req, res, next) => {
  console.log(`${req.method} ${req.url}`);
  next();
});','javascript','easy',ARRAY['nodejs','express','middleware']::text[],true,102),
('nodejs','express-apis','nodejs-express-apis-103','What happens if a middleware function never calls next() or sends a response?','The request hangs indefinitely — Express has no way of knowing the middleware is ''done'' other than next() being called or a response method (res.send/res.json/res.end) being invoked, so the client''s connection stays open until it times out on its own, which is a common cause of a server appearing to freeze on certain requests.',null,null,null,'medium',ARRAY['nodejs','express','middleware']::text[],false,103),
('nodejs','express-apis','nodejs-express-apis-104','How does Express''s error-handling middleware differ from regular middleware, and how do you trigger it?','Error-handling middleware is defined with four parameters instead of three — (err, req, res, next) — and Express recognizes this signature specifically to route errors to it. You trigger it either by calling next(err) from any regular middleware/route handler, or by throwing synchronously inside a route handler (Express catches synchronous throws automatically, though async errors need to be passed to next() explicitly in Express 4).',null,'app.use((err, req, res, next) => {
  console.error(err);
  res.status(500).json({ error: ''Internal Server Error'' });
});','javascript','medium',ARRAY['nodejs','express','error-handling']::text[],false,104),
('nodejs','express-apis','nodejs-express-apis-105','Why doesn''t Express 4 automatically catch errors thrown inside an async route handler, and how do you work around it?','Express 4''s error handling relies on synchronous try/catch internally around each handler call — since an async function''s rejection happens later, after that synchronous call has already returned, Express never sees it, leaving it an unhandled rejection. The common workaround is wrapping async handlers in a helper that catches rejections and forwards them to next(err), or using a library like express-async-errors that patches this behavior automatically. Express 5 fixes this natively by awaiting handlers that return a promise.',null,'const asyncHandler = (fn) => (req, res, next) => Promise.resolve(fn(req, res, next)).catch(next);
app.get(''/users/:id'', asyncHandler(async (req, res) => {
  const user = await db.findUser(req.params.id);
  res.json(user);
}));','javascript','hard',ARRAY['nodejs','express','error-handling']::text[],false,105),
('nodejs','express-apis','nodejs-express-apis-106','What''s the difference between app.use() and app.get()/app.post() for registering middleware?','app.use() mounts middleware for all HTTP methods and, if given a path, matches any request path that starts with that prefix. app.get()/app.post()/etc. register handlers scoped to a specific HTTP method and match the path more precisely (by default, exact match unless route parameters or wildcards are used) — they''re really just middleware registration too, but tied to both a method and a specific route pattern.',null,null,null,'easy',ARRAY['nodejs','express','routing']::text[],false,106),
('nodejs','express-apis','nodejs-express-apis-107','How do you define and access route parameters (e.g. /users/:id) in Express?','A colon-prefixed segment in a route path (like /users/:id) defines a named parameter, which Express extracts from the matched URL and makes available on req.params as a string — so a request to /users/42 gives req.params.id === ''42''. Multiple parameters can appear in one route, and Express also supports more advanced patterns like optional parameters and wildcards.',null,'app.get(''/users/:id'', (req, res) => {
  res.json({ userId: req.params.id });
});','javascript','easy',ARRAY['nodejs','express','routing']::text[],false,107),
('nodejs','express-apis','nodejs-express-apis-108','What''s the difference between req.params, req.query, and req.body in Express?','req.params holds values extracted from named URL segments (e.g. /users/:id). req.query holds parsed query-string parameters after the ''?'' (e.g. ?sort=asc). req.body holds the parsed request body (JSON, form data, etc.), which requires body-parsing middleware like express.json() to populate — without it, req.body is undefined even if the client sent a body.',null,'// GET /users/42?sort=asc  with JSON body { "name": "Alex" }
app.use(express.json());
app.post(''/users/:id'', (req, res) => {
  console.log(req.params.id, req.query.sort, req.body.name);
});','javascript','easy',ARRAY['nodejs','express','routing']::text[],true,108),
('nodejs','express-apis','nodejs-express-apis-109','What does express.json() middleware do, and why must it be registered before routes that use req.body?','express.json() parses incoming requests with a Content-Type of application/json, reading the raw request body stream and populating req.body with the resulting parsed object. It must be registered (via app.use()) before any route handlers that rely on req.body, since Express middleware and routes execute in registration order — a route defined before the parser would see req.body as undefined.',null,'app.use(express.json());
app.post(''/api/data'', (req, res) => res.json(req.body));','javascript','easy',ARRAY['nodejs','express','middleware']::text[],false,109),
('nodejs','express-apis','nodejs-express-apis-110','How would you structure Express routes for a REST API across multiple resources (e.g. users, posts) to keep the codebase organized?','A common pattern is using express.Router() to create a separate, self-contained router per resource (userRoutes.js, postRoutes.js), each defining its own routes relative to a mount point, and then mounting each router on the main app with app.use(''/api/users'', userRoutes). This keeps route definitions modular and avoids a single monolithic file listing every endpoint.',null,'// userRoutes.js
const router = express.Router();
router.get(''/'', listUsers);
router.get(''/:id'', getUser);
module.exports = router;
// app.js
app.use(''/api/users'', require(''./userRoutes''));','javascript','medium',ARRAY['nodejs','express','routing']::text[],false,110),
('nodejs','express-apis','nodejs-express-apis-111','How would you implement authentication middleware that protects certain routes but not others in Express?','Write a middleware function that checks for valid credentials (e.g. verifying a JWT from the Authorization header or checking a session), calling next() to allow the request through if valid, or responding with a 401 status and ending the chain if not. Apply it selectively either per-route (app.get(''/admin'', authMiddleware, handler)) or to a whole router/path prefix, rather than globally with app.use() if some routes should stay public.',null,'function requireAuth(req, res, next) {
  const token = req.headers.authorization?.split('' '')[1];
  if (!token || !isValid(token)) return res.status(401).json({ error: ''Unauthorized'' });
  next();
}
app.get(''/admin'', requireAuth, adminHandler);','javascript','medium',ARRAY['nodejs','express','authentication']::text[],false,111),
('nodejs','express-apis','nodejs-express-apis-112','What is CORS, and how do you enable it in an Express application?','CORS (Cross-Origin Resource Sharing) is a browser security mechanism that blocks a web page from making requests to a different origin (domain/port/protocol) unless the server explicitly allows it via response headers like Access-Control-Allow-Origin. In Express, this is typically handled with the cors middleware package (app.use(cors())), which sets the appropriate headers and handles preflight OPTIONS requests automatically, rather than manually setting headers on every response.',null,'const cors = require(''cors'');
app.use(cors({ origin: ''https://example.com'' }));','javascript','medium',ARRAY['nodejs','express','cors']::text[],true,112),
('nodejs','express-apis','nodejs-express-apis-113','How would you validate incoming request data in an Express API, and why shouldn''t you trust client input?','Validation is typically done with a schema library (like Joi, Zod, or express-validator) applied as middleware or at the top of a handler, checking types, required fields, and formats before any business logic runs, responding with a 400 status and details if validation fails. Client input can never be trusted since it may come from a malicious actor, a buggy client, or a stale frontend version — skipping validation risks crashes, injection attacks, or corrupted data reaching the database.',null,'const schema = z.object({ email: z.string().email(), age: z.number().min(0) });
app.post(''/users'', (req, res) => {
  const result = schema.safeParse(req.body);
  if (!result.success) return res.status(400).json(result.error);
  // proceed with result.data
});','javascript','medium',ARRAY['nodejs','express','validation']::text[],false,113),
('nodejs','express-apis','nodejs-express-apis-114','What is the correct order to register a 404 (not found) handler and an error-handling middleware in Express?','Both should be registered last, after all normal routes — a catch-all middleware with no matching route (app.use((req, res) => res.status(404).send(...))) handles requests that didn''t match any defined route, and the four-argument error-handling middleware goes after that, so it catches errors passed via next(err) from anywhere earlier in the chain, including the 404 handler itself if it throws.',null,null,null,'medium',ARRAY['nodejs','express','routing']::text[],false,114),
('nodejs','express-apis','nodejs-express-apis-115','What are appropriate HTTP status codes to return for common REST API scenarios (success, not found, validation error, server error)?','200 OK for a successful GET/PUT, 201 Created for a successful POST that creates a resource, 204 No Content for a successful action with no response body (like a DELETE). 400 Bad Request for invalid input, 401 Unauthorized for missing/invalid auth, 403 Forbidden for valid auth but insufficient permission, 404 Not Found for a missing resource, and 500 Internal Server Error for an unexpected server-side failure.',null,null,null,'easy',ARRAY['nodejs','express','http']::text[],true,115),
('nodejs','express-apis','nodejs-express-apis-116','How would you implement rate limiting on an Express API to prevent abuse?','Use a rate-limiting middleware (like express-rate-limit) that tracks request counts per client (typically by IP or an API key) within a time window, stored in memory for a single instance or in a shared store like Redis for a multi-instance deployment, and responds with a 429 Too Many Requests status once the client exceeds the configured limit within that window.',null,'const rateLimit = require(''express-rate-limit'');
app.use(rateLimit({ windowMs: 15 * 60 * 1000, max: 100 }));','javascript','medium',ARRAY['nodejs','express','security']::text[],false,116),
('nodejs','express-apis','nodejs-express-apis-117','How does Express handle serving static files, and what middleware is used?','express.static(rootDir) is built-in middleware that serves files directly from a given directory based on the request path — e.g. app.use(express.static(''public'')) serves public/style.css at /style.css. It handles content-type detection, caching headers, and range requests automatically, avoiding the need to manually stream each static file yourself.',null,'app.use(express.static(''public''));
// GET /logo.png serves ./public/logo.png','javascript','easy',ARRAY['nodejs','express','static-files']::text[],false,117),
('nodejs','express-apis','nodejs-express-apis-118','What is the purpose of next(''route'') in Express middleware?','Calling next(''route'') skips any remaining middleware and handlers registered for the current route specifically, jumping straight to the next matching route definition — distinct from a plain next(), which just moves to the next middleware in the current route''s own chain. It''s useful when a middleware needs to conditionally ''opt out'' of handling a request entirely and let a different route definition handle it instead.',null,null,null,'hard',ARRAY['nodejs','express','routing']::text[],false,118),
('nodejs','express-apis','nodejs-express-apis-119','How would you structure an Express app to separate routing, business logic (services), and data access (repositories/models)?','A common layered structure has route/controller files that parse the request and call into service functions containing business logic, which in turn call repository/model functions responsible for actual database queries — each layer only depends on the one below it. This separation makes business logic testable independently of HTTP concerns and keeps route handlers thin, focused on request/response translation rather than embedding SQL or complex logic directly.',null,null,null,'medium',ARRAY['nodejs','express','architecture']::text[],false,119),
('nodejs','express-apis','nodejs-express-apis-120','How can you version a REST API built with Express (e.g. /api/v1 vs /api/v2)?','The most common approach is URL-path versioning, mounting separate routers under distinct prefixes like app.use(''/api/v1'', v1Router) and app.use(''/api/v2'', v2Router), letting each version evolve independently. Alternatives include header-based versioning (reading a custom header or the Accept header to pick a handler) or query-parameter versioning, though path versioning is generally the most explicit and cache-friendly.',null,null,null,'medium',ARRAY['nodejs','express','api-design']::text[],false,120),
('nodejs','express-apis','nodejs-express-apis-121','How would you handle file uploads in an Express application?','A multipart/form-data body (used for file uploads) isn''t parsed by express.json() or express.urlencoded(), so you typically use a dedicated middleware like multer, which parses the multipart body, saves uploaded files (to disk or memory, or streams them to cloud storage), and populates req.file / req.files with metadata about each uploaded file.',null,'const multer = require(''multer'');
const upload = multer({ dest: ''uploads/'' });
app.post(''/upload'', upload.single(''avatar''), (req, res) => {
  res.json({ filename: req.file.filename });
});','javascript','medium',ARRAY['nodejs','express','file-upload']::text[],false,121),
('nodejs','express-apis','nodejs-express-apis-122','What security-related HTTP headers should a production Express app typically set, and how is this usually done?','Headers like Strict-Transport-Security, X-Content-Type-Options, X-Frame-Options, and a Content-Security-Policy help protect against common attacks (clickjacking, MIME-sniffing, XSS). Rather than setting each manually, most Express apps use the helmet middleware, which applies a sensible set of these security headers with one line: app.use(helmet()).',null,'const helmet = require(''helmet'');
app.use(helmet());','javascript','medium',ARRAY['nodejs','express','security']::text[],false,122),
('nodejs','express-apis','nodejs-express-apis-123','How would you implement pagination for a GET endpoint that lists a large number of resources?','Accept page and limit (or cursor-based) query parameters, use them to constrain the database query (e.g. LIMIT/OFFSET in SQL, or a cursor comparison for cursor-based pagination), and return both the page of results and metadata like total count or a next-page cursor/link so clients know how to fetch subsequent pages. Cursor-based pagination is generally preferred over offset-based for large or frequently-changing datasets, since offsets can skip or duplicate rows as data changes between requests.',null,'app.get(''/posts'', async (req, res) => {
  const page = parseInt(req.query.page) || 1;
  const limit = parseInt(req.query.limit) || 20;
  const posts = await Post.find().skip((page - 1) * limit).limit(limit);
  res.json({ page, limit, posts });
});','javascript','medium',ARRAY['nodejs','express','api-design']::text[],false,123),
('nodejs','express-apis','nodejs-express-apis-124','What''s the difference between res.send(), res.json(), and res.end() in Express?','res.json(data) serializes data to JSON and sets the Content-Type header to application/json. res.send(data) is more general-purpose — it inspects the argument type and sets an appropriate Content-Type (JSON for objects, text/html for strings), essentially delegating to res.json() internally for objects. res.end() sends the response immediately without any body processing, typically used to end a response that''s already had headers/body set manually (or to close a response with no body at all).',null,null,null,'easy',ARRAY['nodejs','express']::text[],false,124),
('nodejs','express-apis','nodejs-express-apis-125','How would you write integration tests for an Express API without starting a real server on a network port?','Libraries like supertest let you pass the Express app instance directly (rather than a URL) and make requests against it in-process, using Node''s internal request/response handling without binding to an actual TCP port — this makes tests fast and avoids port conflicts, while still exercising the full middleware/routing stack exactly as it would run in production.',null,'const request = require(''supertest'');
const app = require(''../app'');
test(''GET /health returns 200'', async () => {
  const res = await request(app).get(''/health'');
  expect(res.status).toBe(200);
});','javascript','medium',ARRAY['nodejs','express','testing']::text[],false,125),
('nodejs','express-apis','nodejs-express-apis-126','What is idempotency in REST API design, and which HTTP methods are expected to be idempotent?','An idempotent operation produces the same server-side result no matter how many times it''s repeated with the same input — GET, PUT, and DELETE are expected to be idempotent (calling PUT /users/1 with the same body twice leaves the resource in the same state as calling it once), while POST is not, since repeating it typically creates a new resource each time. This matters for safely retrying failed requests without risking duplicate side effects.',null,null,null,'hard',ARRAY['nodejs','express','api-design']::text[],false,126),
('nodejs','express-apis','nodejs-express-apis-127','How would you handle a request whose client disconnects before the server finishes processing it?','You can listen for the ''close'' event on the request or response object, which fires if the underlying connection closes prematurely, and use it to abort in-progress work (like cancelling a database query or a downstream API call) rather than wasting resources computing a response nobody will receive. Not handling this can lead to unnecessary load under high request-cancellation scenarios, like a user rapidly navigating away from a slow-loading page.',null,'app.get(''/report'', (req, res) => {
  const controller = new AbortController();
  req.on(''close'', () => controller.abort());
  runExpensiveQuery({ signal: controller.signal }).then((data) => res.json(data));
});','javascript','hard',ARRAY['nodejs','express','performance']::text[],false,127),
('nodejs','express-apis','nodejs-express-apis-128','What''s the difference between app.all() and registering the same handler on every individual HTTP method?','app.all(path, handler) registers a handler that matches a given path regardless of HTTP method (GET, POST, PUT, etc. all trigger it), which is a concise way to apply logic — like authentication or logging — uniformly across all verbs for a specific route, rather than manually duplicating app.get(path, ...), app.post(path, ...), and so on.',null,'app.all(''/admin/*'', requireAdmin);','javascript','easy',ARRAY['nodejs','express','routing']::text[],false,128),
('nodejs','express-apis','nodejs-express-apis-129','How would you design consistent error response payloads across an Express API?','Define a standard error shape (e.g. { error: { code, message, details } }) and centralize error formatting in the error-handling middleware, having route handlers throw or pass custom error classes (like a NotFoundError or ValidationError with an associated status code) rather than manually formatting the response inline in every handler. This keeps error responses predictable for API consumers and avoids duplicated formatting logic scattered across routes.',null,'class ApiError extends Error {
  constructor(status, message) { super(message); this.status = status; }
}
app.use((err, req, res, next) => {
  const status = err.status || 500;
  res.status(status).json({ error: { message: err.message } });
});','javascript','medium',ARRAY['nodejs','express','error-handling']::text[],false,129),
('nodejs','express-apis','nodejs-express-apis-130','What is the purpose of a health-check endpoint (e.g. GET /health) in a production Express API?','A health-check endpoint returns a lightweight response (often just a 200 status, sometimes with basic diagnostic info like DB connectivity) that load balancers, container orchestrators, and monitoring tools use to determine whether an instance is alive and ready to receive traffic — an unhealthy instance can be automatically restarted or removed from a load balancer''s rotation based on this check failing.',null,'app.get(''/health'', (req, res) => res.status(200).json({ status: ''ok'' }));','javascript','easy',ARRAY['nodejs','express','deployment']::text[],false,130),
('nodejs','express-apis','nodejs-express-apis-131','How would you prevent an Express route handler from directly exposing internal database error messages to API clients?','Catch database/internal errors in the error-handling middleware (or in a try/catch in the handler) and log the full error server-side for debugging, but respond to the client with a generic, sanitized message (and appropriate status code) instead of the raw error — leaking stack traces, SQL fragments, or internal paths to clients can expose implementation details useful to an attacker.',null,'app.use((err, req, res, next) => {
  console.error(err); // full detail logged internally
  res.status(500).json({ error: ''Something went wrong'' }); // sanitized for client
});','javascript','medium',ARRAY['nodejs','express','security']::text[],false,131),
('nodejs','express-apis','nodejs-express-apis-132','What is the role of a reverse proxy (like Nginx) in front of an Express application in production?','A reverse proxy typically handles TLS termination, serves static assets directly, load-balances across multiple Node process instances, and can buffer slow client connections — offloading work Node isn''t especially efficient at (like raw TLS handshakes at scale) and providing a layer for things like rate limiting or request logging outside the application itself. Express''s app.set(''trust proxy'', true) is often needed in this setup so req.ip and req.protocol reflect the original client rather than the proxy.',null,null,null,'medium',ARRAY['nodejs','express','deployment']::text[],false,132),
('nodejs','fundamentals','nodejs-fundamentals-101','What is Node.js, and how does it differ from running JavaScript in a browser?','Node.js is a runtime that executes JavaScript outside the browser, built on Chrome''s V8 engine plus a set of C++ bindings (libuv) that expose OS-level capabilities like the filesystem, networking, and processes. Unlike a browser, it has no DOM, window, or document; instead it provides APIs like fs, http, and process, along with a CommonJS/ESM module system for organizing code across files.',null,null,null,'easy',ARRAY['nodejs','runtime']::text[],true,101),
('nodejs','fundamentals','nodejs-fundamentals-102','What role does V8 play in Node.js, and what does Node add on top of it?','V8 is Google''s open-source JavaScript engine that parses and executes JS code and manages memory/garbage collection; it''s the same engine used in Chrome. Node embeds V8 and adds libuv for the event loop and async I/O, plus built-in modules (fs, http, path, etc.) and a module-loading system, none of which V8 itself provides.',null,null,null,'easy',ARRAY['nodejs','v8']::text[],false,102),
('nodejs','fundamentals','nodejs-fundamentals-103','What is libuv and why is it important to Node.js?','libuv is a C library that provides Node''s event loop, thread pool, and cross-platform abstractions for asynchronous I/O like file access, DNS lookups, and networking. It''s what lets Node perform non-blocking operations on operating systems that don''t natively support async I/O for everything, by offloading blocking work to a background thread pool and notifying the event loop via callbacks when it completes.',null,null,null,'hard',ARRAY['nodejs','libuv']::text[],false,103),
('nodejs','fundamentals','nodejs-fundamentals-104','Is Node.js single-threaded? Explain what that actually means in practice.','Node''s JavaScript execution runs on a single main thread, so only one piece of JS code runs at a time and there''s no shared-memory multithreading of your application logic by default. However, Node itself isn''t single-threaded overall: libuv maintains a background thread pool for certain blocking operations (like some fs and crypto calls), and I/O like networking is handled by the OS kernel asynchronously, so the single JS thread isn''t blocked waiting on them.',null,null,null,'medium',ARRAY['nodejs','concurrency']::text[],true,104),
('nodejs','fundamentals','nodejs-fundamentals-105','What is the global object in Node.js, and how does it differ from window in a browser?','In Node, the global object is called `global` (accessible everywhere without an import), analogous to `window` in browsers, and it holds globally available functions like setTimeout, and objects like process and Buffer. Unlike browser globals, variables declared at the top level of a CommonJS module are scoped to that module, not attached to `global`, because each file is wrapped in its own function during module loading.',null,'console.log(globalThis === global); // true in Node','javascript','easy',ARRAY['nodejs','globals']::text[],false,105),
('nodejs','fundamentals','nodejs-fundamentals-106','What is the process object, and what are some of its commonly used properties or methods?','process is a global object that provides information about, and control over, the currently running Node process. Common members include process.argv (CLI arguments), process.env (environment variables), process.exit() (to terminate the process), process.cwd() (current working directory), and process.on(''uncaughtException''/''exit'') for lifecycle hooks.',null,'console.log(process.argv);
console.log(process.env.NODE_ENV);
process.exit(1);','javascript','easy',ARRAY['nodejs','process']::text[],false,106),
('nodejs','fundamentals','nodejs-fundamentals-107','What is the difference between process.nextTick() and setImmediate()?','process.nextTick() queues a callback to run immediately after the current operation completes, before the event loop continues to any phase — it has its own queue processed even before microtasks like promise callbacks in older Node versions, and fully drains before I/O events are processed. setImmediate() schedules a callback to run in the ''check'' phase of the event loop, after I/O callbacks in the current iteration, so it generally runs later than nextTick but is a better choice for yielding after I/O without starving the loop.',null,null,null,'hard',ARRAY['nodejs','process','event-loop']::text[],false,107),
('nodejs','fundamentals','nodejs-fundamentals-108','How do you read command-line arguments and environment variables in a Node.js script?','Command-line arguments are available in process.argv, an array where the first two entries are the node executable path and the script path, so user-supplied arguments start at index 2. Environment variables are read from process.env, a plain object of string key-value pairs populated from the shell environment (or a .env file loaded via a library like dotenv).',null,'// node script.js --port=3000
const arg = process.argv[2]; // ''--port=3000''
const port = process.env.PORT || 3000;','javascript','easy',ARRAY['nodejs','process']::text[],false,108),
('nodejs','fundamentals','nodejs-fundamentals-109','What is the difference between CommonJS and ES Modules at a conceptual level?','CommonJS is Node''s original, synchronous module system using require() and module.exports, resolved and loaded at runtime. ES Modules (import/export) are the standardized JS module system, statically analyzable (imports are resolved before execution) and loaded asynchronously, which enables features like tree-shaking and top-level await. Node supports both, distinguishing them via file extension (.cjs/.mjs) or the "type" field in package.json.',null,null,null,'medium',ARRAY['nodejs','modules']::text[],true,109),
('nodejs','fundamentals','nodejs-fundamentals-110','What is the Node.js REPL, and what is it useful for?','REPL stands for Read-Eval-Print Loop — it''s the interactive shell you get by running `node` with no arguments, letting you type JavaScript expressions and see results immediately. It''s useful for quickly testing snippets, exploring an API, or debugging small pieces of logic without creating a script file.',null,null,null,'easy',ARRAY['nodejs','cli']::text[],false,110),
('nodejs','fundamentals','nodejs-fundamentals-111','What are Node''s built-in (core) modules, and how do you use one?','Core modules are modules bundled with Node itself, like fs, path, http, os, and crypto, that don''t need to be installed via npm. You load them with require(''fs'') in CommonJS or import fs from ''node:fs'' in ESM; prefixing with ''node:'' makes it explicit that the import refers to a core module rather than a package of the same name.',null,'const fs = require(''fs'');
// or
import fs from ''node:fs'';','javascript','medium',ARRAY['nodejs','modules']::text[],false,111),
('nodejs','fundamentals','nodejs-fundamentals-112','What is the difference between __dirname/__filename and import.meta.url?','__dirname and __filename are CommonJS-only globals giving the absolute path of the current module''s directory and file, respectively — they don''t exist in ES Modules. In ESM, the equivalent information comes from import.meta.url (a file:// URL), which is typically converted to a filesystem path using fileURLToPath from the ''url'' module.',null,'import { fileURLToPath } from ''node:url'';
import { dirname } from ''node:path'';
const __filename = fileURLToPath(import.meta.url);
const __dirname = dirname(__filename);','javascript','medium',ARRAY['nodejs','modules']::text[],false,112),
('nodejs','fundamentals','nodejs-fundamentals-113','What is the purpose of the ''main'' field in package.json?','The "main" field specifies the entry-point file that''s loaded when another package (or Node itself, in some resolution paths) does require(''your-package'') or imports it by name. If omitted, Node defaults to looking for index.js in the package root. Modern packages often also define an "exports" field, which gives finer-grained control over what subpaths are importable and can differ between CommonJS and ESM consumers.',null,null,null,'easy',ARRAY['nodejs','package-json']::text[],false,113),
('nodejs','fundamentals','nodejs-fundamentals-114','How does Node.js determine whether a .js file should be parsed as CommonJS or ESM?','By default, .js files are treated as CommonJS unless the nearest parent package.json has "type": "module", in which case .js files are treated as ES Modules. Regardless of that setting, .cjs files are always CommonJS and .mjs files are always ESM, letting a project mix both module systems explicitly when needed.',null,null,null,'medium',ARRAY['nodejs','modules','package-json']::text[],false,114),
('nodejs','fundamentals','nodejs-fundamentals-115','What is the difference between a Node.js runtime and a JavaScript engine?','A JavaScript engine, like V8, is purely responsible for parsing and executing JS code according to the ECMAScript spec — it has no concept of files, networking, or the operating system. A runtime like Node wraps an engine and adds host-provided APIs (filesystem, networking, timers, process control) and a module system, turning a language into a platform capable of running standalone programs.',null,null,null,'medium',ARRAY['nodejs','v8','runtime']::text[],false,115),
('nodejs','fundamentals','nodejs-fundamentals-116','What is the difference between Node.js and Deno (or Bun), at a high level?','Deno and Bun are newer JavaScript runtimes that address perceived shortcomings of Node: Deno ships with secure-by-default permissions, native TypeScript support, and standardized ESM/URL-based imports without a separate package manager, while Bun focuses on speed by implementing its own JS engine (JavaScriptCore) and bundling a package manager, bundler, and test runner in one binary. Node remains the most widely adopted, with the largest ecosystem and long-term production track record.',null,null,null,'medium',ARRAY['nodejs','runtime']::text[],false,116),
('nodejs','fundamentals','nodejs-fundamentals-117','What is Buffer in Node.js and why does it exist?','Buffer is a global class for handling raw binary data directly in memory, outside V8''s usual string encoding, which is essential for working with things like file contents, network packets, or TCP streams before they''re interpreted as text. It predates JavaScript''s standardized Uint8Array/TypedArray support and today is implemented as a subclass of Uint8Array with extra Node-specific convenience methods.',null,'const buf = Buffer.from(''hello'', ''utf8'');
console.log(buf); // <Buffer 68 65 6c 6c 6f>','javascript','hard',ARRAY['nodejs','buffer']::text[],false,117),
('nodejs','fundamentals','nodejs-fundamentals-118','How do you handle uncaught exceptions and unhandled promise rejections at the process level in Node?','You can listen for process.on(''uncaughtException'', handler) to catch synchronous errors that weren''t caught anywhere else, and process.on(''unhandledRejection'', handler) for promises that rejected without a .catch(). Both are generally treated as last-resort safety nets for logging and a graceful shutdown, not a substitute for proper try/catch and .catch() handling, since the process is often left in an undefined state after an uncaught exception.',null,'process.on(''uncaughtException'', (err) => {
  console.error(''Fatal:'', err);
  process.exit(1);
});','javascript','medium',ARRAY['nodejs','process','error-handling']::text[],true,118),
('nodejs','fundamentals','nodejs-fundamentals-119','What does it mean that Node.js is ''non-blocking'' and ''event-driven''?','Event-driven means Node''s architecture centers around emitting and reacting to events (like a request arriving or a file finishing a read) rather than polling or waiting synchronously. Non-blocking means I/O operations are initiated and then control returns immediately to continue other work, with a callback, promise, or event firing later when the operation completes, instead of the thread sitting idle until the operation finishes.',null,null,null,'easy',ARRAY['nodejs','runtime','event-loop']::text[],false,119),
('nodejs','fundamentals','nodejs-fundamentals-120','What is the EventEmitter class, and why is it central to Node''s core APIs?','EventEmitter is a core class (from the ''events'' module) that implements the publish/subscribe pattern: objects can emit named events, and listeners registered with .on() are invoked synchronously in registration order when that event fires. Many core Node APIs — streams, HTTP servers, child processes — extend EventEmitter internally, making ''listen for an event'' the standard idiom for handling async occurrences throughout Node''s API surface.',null,'const { EventEmitter } = require(''events'');
const emitter = new EventEmitter();
emitter.on(''greet'', (name) => console.log(`Hello ${name}`));
emitter.emit(''greet'', ''world'');','javascript','medium',ARRAY['nodejs','events']::text[],true,120),
('nodejs','fundamentals','nodejs-fundamentals-121','How would you gracefully shut down a Node.js server on SIGTERM?','Listen for the process.on(''SIGTERM'', handler) signal (sent by process managers or container orchestrators like Docker/Kubernetes on shutdown), stop accepting new connections via server.close(), and finish or cancel in-flight work (like closing a database pool) before calling process.exit(). This avoids abruptly dropping active requests when the process is being terminated deliberately.',null,'process.on(''SIGTERM'', () => {
  server.close(() => {
    db.disconnect().then(() => process.exit(0));
  });
});','javascript','medium',ARRAY['nodejs','process']::text[],false,121),
('nodejs','fundamentals','nodejs-fundamentals-122','What is the difference between process.exit() and letting a Node process exit naturally?','process.exit(code) forces immediate termination, potentially cutting off pending I/O, unflushed console output, or ''exit'' event listeners that haven''t finished. Letting a process exit naturally means the event loop simply has no more pending callbacks, timers, or open handles to process, so Node terminates cleanly once nothing remains keeping it alive — generally the safer approach when possible.',null,null,null,'medium',ARRAY['nodejs','process']::text[],false,122),
('nodejs','fundamentals','nodejs-fundamentals-123','What are Node''s LTS (Long-Term Support) releases, and why do they matter for production apps?','Node follows a release schedule where even-numbered major versions become LTS roughly every two years, receiving critical bug fixes and security patches for an extended period (typically 30 months) without introducing breaking changes. Production applications generally target an LTS version rather than the latest ''Current'' release to prioritize stability and predictable upgrade cycles over access to the newest features.',null,null,null,'easy',ARRAY['nodejs','runtime']::text[],false,123),
('nodejs','fundamentals','nodejs-fundamentals-124','How can you check the version of Node.js and npm installed, and why does version matter for a project?','`node -v` and `npm -v` print the installed versions from the command line. Version matters because different Node versions support different JS/ECMAScript features and core API behaviors (e.g., top-level await, certain fetch/test-runner APIs), and mismatches between the version a project was built against and the version it''s deployed on can cause subtle bugs — which is why projects often pin an "engines" field in package.json or use a version manager like nvm.',null,'node -v
npm -v','javascript','easy',ARRAY['nodejs','cli']::text[],false,124),
('nodejs','fundamentals','nodejs-fundamentals-125','What is the purpose of the ''os'' and ''path'' core modules?','The ''os'' module provides information about the operating system Node is running on, such as CPU count, free memory, and platform name, useful for scaling decisions like how many worker processes to spawn. The ''path'' module provides utilities for working with file and directory paths in a cross-platform way (joining, resolving, and parsing paths), avoiding manual string concatenation that would break between Windows and POSIX path separators.',null,'const os = require(''os'');
const path = require(''path'');
console.log(os.cpus().length);
console.log(path.join(''a'', ''b'', ''c.txt''));','javascript','medium',ARRAY['nodejs','modules']::text[],false,125),
('nodejs','fundamentals','nodejs-fundamentals-126','What are Node.js global objects that don''t require an explicit import, and where do they come from?','Globals like console, setTimeout, setInterval, Buffer, process, __dirname (CommonJS only), and module/exports (CommonJS only) are automatically available in every module without requiring an import — some are true JS-engine or web-standard globals, while others (like process and Buffer) are injected by Node''s runtime specifically. This differs from other core modules like fs or http, which must be explicitly required or imported.',null,null,null,'easy',ARRAY['nodejs','globals']::text[],false,126),
('nodejs','fundamentals','nodejs-fundamentals-127','What is the difference between npm''s ''dependencies'' and ''devDependencies'' in package.json?','"dependencies" lists packages required for the application to run in production, and are installed whenever the package is installed as a dependency of another project. "devDependencies" lists packages only needed during development or build (like test frameworks, linters, or bundlers), which are skipped when installing with `npm install --production` or `npm ci --omit=dev`.',null,null,null,'easy',ARRAY['nodejs','package-json','npm']::text[],false,127),
('nodejs','fundamentals','nodejs-fundamentals-128','Why can''t you use top-level await in a CommonJS module, but you can in an ES Module?','CommonJS require() is synchronous by design — module loading assumes each module fully executes to completion before returning control to its caller, which is incompatible with pausing mid-module for an async operation. ES Modules were designed with asynchronous loading in mind from the start, so the spec allows top-level await directly in a .mjs file (or a .js file under "type": "module"), pausing that module''s evaluation without blocking the whole thread.',null,null,null,'hard',ARRAY['nodejs','modules']::text[],false,128),
('nodejs','fundamentals','nodejs-fundamentals-129','What is process.env.NODE_ENV commonly used for, and is it a built-in Node feature?','NODE_ENV is not a special Node.js built-in — it''s simply a widely-adopted convention where developers set an environment variable to ''development'', ''production'', or ''test'' and check it in code (or rely on libraries like Express that use it) to toggle behavior such as verbose logging, error stack traces, or caching. Node itself doesn''t set or read NODE_ENV automatically.',null,'if (process.env.NODE_ENV === ''production'') {
  // disable verbose logging
}','javascript','medium',ARRAY['nodejs','process']::text[],false,129),
('nodejs','fundamentals','nodejs-fundamentals-130','What is module.exports versus exports in CommonJS, and why does confusing them cause bugs?','exports is simply a reference to the same object that module.exports initially points to, provided as a shorthand for adding properties (exports.foo = ...). Reassigning exports = something entirely new breaks the link, since it only repoints the local variable, not module.exports — the object Node actually returns from require() — which is a classic source of a module unexpectedly exporting an empty object.',null,'// Wrong: breaks the reference
exports = { foo: 1 };
// Correct
module.exports = { foo: 1 };
// or
exports.foo = 1;','javascript','hard',ARRAY['nodejs','modules']::text[],false,130),
('nodejs','fundamentals','nodejs-fundamentals-131','What is JIT compilation, and how does V8 use it to make JavaScript execution fast?','Just-In-Time compilation compiles code to machine code at runtime rather than ahead of time, letting the engine optimize based on how code actually behaves as it runs. V8 starts by interpreting bytecode quickly (via Ignition), then profiles hot functions and recompiles them into highly optimized machine code (via TurboFan) based on observed types and shapes, deoptimizing back to the interpreter if assumptions made during optimization turn out to be wrong.',null,null,null,'hard',ARRAY['nodejs','v8']::text[],false,131),
('nodejs','fundamentals','nodejs-fundamentals-132','What are Node.js ''experimental'' flags, and how do they relate to feature stability?','Node ships some features behind an --experimental- flag (or gated by a warning) before they''re considered stable, allowing the API to change or be removed without a major version bump. Production code generally avoids relying on experimental features, or does so cautiously, since their behavior and availability aren''t guaranteed to remain stable across minor Node releases.',null,null,null,'medium',ARRAY['nodejs','runtime']::text[],false,132),
('nodejs','fundamentals','nodejs-fundamentals-133','How would you determine how much memory a running Node.js process is using?','process.memoryUsage() returns an object with figures like rss (total memory allocated for the process), heapTotal and heapUsed (V8''s JS heap), and external (memory used by C++ objects bound to JS, like Buffers) — useful for diagnosing memory growth or leaks. Tools like the built-in inspector or clinic.js can provide deeper heap snapshots for investigating specific leaks.',null,'console.log(process.memoryUsage());
// { rss, heapTotal, heapUsed, external, arrayBuffers }','javascript','medium',ARRAY['nodejs','process','memory']::text[],false,133),
('nodejs','fundamentals','nodejs-fundamentals-134','What is the difference between a Node.js script run directly and one run as an npm script?','Running `node file.js` directly invokes only that file with the shell''s normal PATH. Running it via an npm script (e.g. `npm run start`, defined in package.json''s "scripts") temporarily prepends node_modules/.bin to the PATH, so locally-installed CLI tools (like a bundler or test runner) can be referenced by name without a global install, and npm also passes along certain environment context like npm_package_* variables.',null,null,null,'medium',ARRAY['nodejs','npm','cli']::text[],false,134),
('nodejs','modules-npm','nodejs-modules-npm-101','What is the difference between require() and import in Node.js?','require() is the CommonJS function for synchronously loading a module, returning module.exports and usable anywhere in a script''s execution. import is the ES Module syntax, statically analyzed at parse time (so import statements must be at the top level, not conditional), and loaded asynchronously by the module loader — Node supports both, but you generally can''t mix require() and import syntax within the same file.',null,null,null,'easy',ARRAY['nodejs','modules']::text[],true,101),
('nodejs','modules-npm','nodejs-modules-npm-102','How does Node.js resolve require(''./foo'') versus require(''foo'')?','A path starting with ''./'' or ''../'' is resolved relative to the requiring file''s directory, checking for foo.js, foo.json, foo.node, or a foo/index.js in that order. A bare specifier like ''foo'' (no leading path) is treated as a package name and resolved by walking up the directory tree looking in each ancestor''s node_modules folder until it''s found or the filesystem root is reached.',null,null,null,'medium',ARRAY['nodejs','modules','resolution']::text[],true,102),
('nodejs','modules-npm','nodejs-modules-npm-103','Why does node_modules resolution walk up parent directories, and what problem does that solve?','Node''s algorithm looks in ./node_modules, then ../node_modules, and so on up to the root, which lets nested packages each have their own node_modules without duplicating shared dependencies at every level — a dependency installed at the project root is still resolvable from deep within it. This is also what enables npm''s deduplication/hoisting strategy, where compatible versions of a package are hoisted to a shared ancestor node_modules folder instead of being installed redundantly.',null,null,null,'hard',ARRAY['nodejs','npm','resolution']::text[],false,103),
('nodejs','modules-npm','nodejs-modules-npm-104','Are CommonJS modules cached after being required, and what does that mean in practice?','Yes — Node caches modules by resolved file path the first time they''re required, so subsequent require() calls for the same module return the exact same exported object instead of re-executing the module''s code. This means any mutable state inside a module (like a shared counter or a singleton instance) persists and is shared across every part of the app that requires it.',null,'// counter.js
let count = 0;
module.exports = { increment: () => ++count };
// Every require(''./counter'') call gets the SAME object/state','javascript','medium',ARRAY['nodejs','modules','caching']::text[],true,104),
('nodejs','modules-npm','nodejs-modules-npm-105','What is a circular dependency in Node.js modules, and how does CommonJS handle it?','A circular dependency occurs when module A requires module B, and B (directly or transitively) requires A back. CommonJS handles it by returning whatever partial exports object the first module has built so far at the point the circular require happens, rather than erroring or infinite-looping — which means the second module may see an incomplete version of the first module''s exports if accessed too early, a subtle source of bugs.',null,null,null,'hard',ARRAY['nodejs','modules','circular-dependency']::text[],false,105),
('nodejs','modules-npm','nodejs-modules-npm-106','What are the essential fields typically found in a package.json file?','Common fields include "name" and "version" (identity), "main" (CommonJS entry point) and/or "exports" (fine-grained entry/subpath control), "scripts" (named CLI commands like start/test/build), "dependencies"/"devDependencies" (required packages), "type" (module system default), and "engines" (supported Node version range).',null,null,null,'easy',ARRAY['nodejs','package-json']::text[],false,106),
('nodejs','modules-npm','nodejs-modules-npm-107','What is semantic versioning (semver), and how do npm version ranges like ^1.2.3 and ~1.2.3 differ?','Semver uses MAJOR.MINOR.PATCH, where major bumps signal breaking changes, minor adds backward-compatible features, and patch fixes bugs without changing the API. In package.json, ^1.2.3 allows updates that don''t change the leftmost non-zero digit (so up to but not including 2.0.0), while ~1.2.3 is stricter, only allowing patch-level updates (up to but not including 1.3.0).',null,null,null,'medium',ARRAY['nodejs','npm','semver']::text[],true,107),
('nodejs','modules-npm','nodejs-modules-npm-108','What is the purpose of package-lock.json, and why should it typically be committed to version control?','package-lock.json records the exact resolved version (and integrity hash) of every installed package, including nested dependencies, so that running npm install produces an identical dependency tree on every machine and every CI run, rather than the potentially different versions that would be selected by re-resolving semver ranges fresh each time. Committing it ensures reproducible builds across environments and over time.',null,null,null,'medium',ARRAY['nodejs','npm','package-lock']::text[],true,108),
('nodejs','modules-npm','nodejs-modules-npm-109','What''s the difference between npm install and npm ci?','npm install reads package.json, resolves dependencies (potentially updating package-lock.json if ranges allow newer compatible versions), and can add/remove packages. npm ci instead requires an existing, exact package-lock.json, deletes node_modules first, and installs precisely what the lockfile specifies without modifying it — faster and stricter, which is why it''s the recommended command for CI pipelines and reproducible deploys.',null,null,null,'medium',ARRAY['nodejs','npm']::text[],false,109),
('nodejs','modules-npm','nodejs-modules-npm-110','What are npm ''peerDependencies'', and when would a package use them?','peerDependencies declare that a package expects the consuming project to provide a compatible version of another package itself, rather than bundling its own copy — common for plugins (e.g. a React component library declaring react as a peer) where having two separate copies of the dependency in the tree would cause conflicts or duplicate-instance bugs. npm (v7+) automatically installs peer dependencies unless a conflicting version already exists.',null,null,null,'medium',ARRAY['nodejs','npm','package-json']::text[],false,110),
('nodejs','modules-npm','nodejs-modules-npm-111','What is npx, and how is it different from a locally or globally installed CLI tool?','npx executes a package''s binary without requiring a permanent install — if the package is already in local node_modules/.bin, it runs that; otherwise, it temporarily downloads and runs it (optionally caching it) for that one invocation. It''s useful for one-off tool usage or trying a CLI without cluttering global installs or committing an unnecessary dependency.',null,'npx create-react-app my-app
npx cowsay hello','javascript','easy',ARRAY['nodejs','npm','cli']::text[],false,111),
('nodejs','modules-npm','nodejs-modules-npm-112','How do you publish a package to the npm registry, and what does the package "version" field control during publishing?','After authenticating with npm login and ensuring the package.json is correct, npm publish uploads the package to the registry under its current "name"@"version". npm refuses to publish if that exact version already exists for the package, which is why bumping the version (often via npm version patch/minor/major, which also updates package.json and creates a git tag) is required before every publish.',null,null,null,'medium',ARRAY['nodejs','npm']::text[],false,112),
('nodejs','modules-npm','nodejs-modules-npm-113','What does the "exports" field in package.json control, and why was it introduced?','The "exports" field explicitly defines which subpaths of a package are importable from outside, and can map different files for CommonJS ("require") versus ESM ("import") consumers, or for different runtime conditions. It was introduced to give package authors control over their public API surface (preventing arbitrary deep imports into internal files) and to properly support dual CJS/ESM packages.',null,'// package.json
{
  "exports": {
    ".": {
      "import": "./esm/index.js",
      "require": "./cjs/index.js"
    }
  }
}','javascript','hard',ARRAY['nodejs','package-json','modules']::text[],false,113),
('nodejs','modules-npm','nodejs-modules-npm-114','How would you make a CommonJS module interoperate with a package that only ships ES Modules?','Since require() can''t synchronously load an ESM-only package, the usual workaround is to use a dynamic import() (which returns a Promise) from within the CommonJS module — this works because dynamic import() is available even in CommonJS files, unlike static import statements. Alternatively, migrating the consuming project itself to ESM avoids the interop issue altogether.',null,'// CommonJS file
async function loadEsmOnlyPkg() {
  const mod = await import(''esm-only-package'');
  return mod.default;
}','javascript','hard',ARRAY['nodejs','modules','esm']::text[],false,114),
('nodejs','modules-npm','nodejs-modules-npm-115','What is the difference between a dependency listed with an exact version (''1.2.3'') versus a range (''^1.2.3'') in package.json?','An exact version pins the dependency to that precise release, meaning npm install always resolves it to exactly 1.2.3 regardless of newer compatible releases. A range like ^1.2.3 allows npm to install any version compatible per semver rules (here, up to but excluding 2.0.0), which can pull in newer patch/minor releases automatically — useful for getting bug fixes, but it does introduce some risk of unexpected behavior changes if a dependency doesn''t perfectly follow semver.',null,null,null,'easy',ARRAY['nodejs','npm','semver']::text[],false,115),
('nodejs','modules-npm','nodejs-modules-npm-116','What is ''dependency hoisting'' in npm, and what problem can it cause?','Hoisting means npm tries to install a single shared copy of a dependency at the top level of node_modules when multiple packages need compatible versions of it, rather than duplicating it inside each dependent package''s own node_modules. The downside is ''phantom dependencies'' — code can accidentally require() a hoisted package that isn''t actually declared in its own package.json, working locally but breaking if the hoisting layout changes or in stricter package managers like pnpm.',null,null,null,'hard',ARRAY['nodejs','npm','node-modules']::text[],false,116),
('nodejs','modules-npm','nodejs-modules-npm-117','What is the difference between npm, yarn, and pnpm at a conceptual level?','All three are package managers for the same npm registry and package.json format, but differ in installation strategy: npm and (classic) yarn generally flatten and hoist dependencies into a single node_modules tree, while pnpm uses a content-addressable global store and symlinks packages into a stricter, non-flattened node_modules, preventing phantom dependency access and saving disk space across projects. Yarn also introduced features like workspaces and a lockfile format that npm later adopted similar equivalents of.',null,null,null,'medium',ARRAY['nodejs','npm','package-managers']::text[],false,117),
('nodejs','modules-npm','nodejs-modules-npm-118','What are npm ''workspaces'', and what problem do they solve for monorepos?','Workspaces let a single top-level package.json declare multiple sub-packages (each with their own package.json) that npm installs and links together as one operation — cross-referencing local packages via symlinks instead of publishing them to the registry first. This solves the pain of managing multiple related packages (e.g. a shared UI library and an app that consumes it) in one repository without manual linking or duplicate installs.',null,null,null,'medium',ARRAY['nodejs','npm','monorepo']::text[],false,118),
('nodejs','modules-npm','nodejs-modules-npm-119','How does Node.js decide which version of a dependency to load when multiple versions exist in node_modules?','Node''s module resolution just looks up the directory tree for the nearest node_modules containing the requested package name, without any awareness of semver — so if package A requires lodash@2 and package B (a sibling dependency) requires lodash@4, npm typically nests a separate copy inside whichever package''s own node_modules needs the non-hoisted version, and each require() call resolves to whichever copy is nearest in the directory tree from that requiring file.',null,null,null,'hard',ARRAY['nodejs','modules','npm']::text[],false,119),
('nodejs','modules-npm','nodejs-modules-npm-120','What is the purpose of a .npmignore file (or the "files" field in package.json)?','Both control which files get included when a package is published to npm — .npmignore works like .gitignore, excluding matched files/patterns from the published tarball, while the "files" field is an allowlist specifying exactly which files/folders to include. Using either keeps published packages lean by excluding test files, source maps, CI config, and other files irrelevant to consumers.',null,null,null,'easy',ARRAY['nodejs','npm','package-json']::text[],false,120),
('nodejs','modules-npm','nodejs-modules-npm-121','What does npm audit do, and what are its limitations?','npm audit checks the installed dependency tree against a database of known security vulnerabilities and reports affected packages along with suggested fixes (often via npm audit fix, which updates to patched versions within allowed semver ranges). Its limitations include false positives/noise for vulnerabilities in code paths that aren''t actually reachable by the app, and it can''t catch vulnerabilities that aren''t yet reported in the database.',null,null,null,'medium',ARRAY['nodejs','npm','security']::text[],false,121),
('nodejs','modules-npm','nodejs-modules-npm-122','How do environment-specific configuration values (like API keys) typically get into a Node.js app without being committed to source control?','They''re usually read from process.env, populated either by the hosting platform/CI directly setting environment variables, or locally via a .env file (loaded by a package like dotenv) that''s excluded from git via .gitignore. This keeps secrets out of the codebase while still letting the app read them the same way (process.env.KEY) regardless of environment.',null,'require(''dotenv'').config();
const apiKey = process.env.API_KEY;','javascript','easy',ARRAY['nodejs','npm','configuration']::text[],false,122),
('nodejs','modules-npm','nodejs-modules-npm-123','What is a ''scoped'' npm package (e.g. @babel/core), and why do organizations use scopes?','A scoped package name is prefixed with @scope/, typically an organization or username, which namespaces the package to avoid naming collisions with unrelated packages on the public registry. Organizations use scopes both to group related packages clearly (@babel/core, @babel/preset-env) and to publish private packages restricted to members of that scope.',null,null,null,'easy',ARRAY['nodejs','npm']::text[],false,123),
('nodejs','modules-npm','nodejs-modules-npm-124','What happens if two files import each other using ES Modules — does it behave the same as CommonJS''s circular dependency handling?','Not exactly — ESM uses ''live bindings,'' meaning an imported name is a reference to the exporting module''s binding rather than a snapshot copied at import time, so even with a circular import, accessing a value after both modules have finished initializing generally reflects the correct final value (though accessing it too early, before it''s assigned, throws a ReferenceError due to the temporal dead zone). This differs from CommonJS''s circular handling, which just hands back a possibly-incomplete exports object without any live-binding guarantees.',null,null,null,'hard',ARRAY['nodejs','modules','esm']::text[],false,124),
('nodejs','modules-npm','nodejs-modules-npm-125','What is the difference between ''dependencies'' and ''optionalDependencies'' in package.json?','"dependencies" must install successfully or npm install fails entirely. "optionalDependencies" are attempted but allowed to fail (e.g. a native module that only builds on certain platforms) without failing the whole install — code using an optional dependency needs to handle the case where it wasn''t actually installed, typically with a try/catch around the require() call.',null,null,null,'medium',ARRAY['nodejs','npm','package-json']::text[],false,125),
('nodejs','modules-npm','nodejs-modules-npm-126','How would you check which versions of a package''s dependencies are actually installed versus what''s allowed by package.json?','npm list (or npm ls) prints the resolved dependency tree actually present in node_modules, including transitive dependencies and their exact installed versions, which can be filtered with npm list <package> to trace where a specific version came from. This is distinct from package.json, which only shows the allowed semver ranges, not the concrete versions actually resolved.',null,'npm ls lodash
npm outdated','javascript','easy',ARRAY['nodejs','npm']::text[],false,126),
('nodejs','modules-npm','nodejs-modules-npm-127','What is ''dependency confusion,'' and how does it relate to package naming?','Dependency confusion is a supply-chain attack where an attacker publishes a malicious public package under the same name as an organization''s internal/private package; if the build tooling is misconfigured to prefer the public registry (or falls back to it), it can pull in the attacker''s package instead of the intended private one. Mitigations include scoping internal packages (@company/pkg), configuring registry scoping explicitly, and pre-registering internal package names publicly as empty placeholders.',null,null,null,'hard',ARRAY['nodejs','npm','security']::text[],false,127),
('nodejs','modules-npm','nodejs-modules-npm-128','What does the "engines" field in package.json do, and is it enforced automatically?','"engines" declares which Node (and optionally npm) versions the package is intended to support, e.g. { "node": ">=18" }. By default npm only prints a warning if the current Node version doesn''t satisfy it, rather than blocking the install — strict enforcement requires either setting engine-strict=true in .npmrc or explicit checks in CI.',null,'{
  "engines": { "node": ">=18.0.0" }
}','javascript','medium',ARRAY['nodejs','npm','package-json']::text[],false,128),
('nodejs','modules-npm','nodejs-modules-npm-129','How do dynamic import() expressions differ from static import statements in terms of when and how they run?','A static import statement is hoisted and resolved before any module code runs, always at the top level, and can''t be conditional. Dynamic import(''module-name'') is a function-like expression that returns a Promise, can be called anywhere (inside conditionals, loops, or after some computed path), and is commonly used for lazy-loading a module only when it''s actually needed, or for interop with ESM-only packages from CommonJS.',null,'if (condition) {
  const { heavyFn } = await import(''./heavy-module.js'');
  heavyFn();
}','javascript','medium',ARRAY['nodejs','modules','esm']::text[],false,129),
('nodejs','modules-npm','nodejs-modules-npm-130','What''s the significance of a package having both a ''main'' and a ''module'' field in package.json (outside the newer ''exports'' field)?','This was a common convention before "exports" existed: "main" points to a CommonJS build for Node/bundlers that expect it, while "module" (recognized by bundlers like Webpack/Rollup, though not by Node itself) points to an ES Module build, allowing tree-shaking-capable bundlers to prefer the ESM version while Node''s require() still falls back to "main". It''s a workaround predating standardized dual-package support via "exports".',null,null,null,'hard',ARRAY['nodejs','package-json','modules']::text[],false,130),
('nodejs','modules-npm','nodejs-modules-npm-131','Why might npm install produce a different dependency tree on two different days for the same package.json, if no lockfile is present?','Without a lockfile, npm re-resolves every semver range against whatever the latest compatible versions are in the registry at install time — if any transitive dependency published a new version matching an existing range in between the two installs, the resulting tree (and potentially behavior) can differ, which is exactly the non-determinism that committing package-lock.json is meant to prevent.',null,null,null,'medium',ARRAY['nodejs','npm','package-lock']::text[],false,131),
('nodejs','performance-debugging','nodejs-performance-debugging-101','Why would you use the cluster module in Node.js, and what problem does it solve?','Since a single Node process runs JavaScript on one thread and can''t natively use multiple CPU cores, the cluster module lets you fork multiple worker processes (typically one per CPU core) that all share the same server port, letting the OS distribute incoming connections across them. This improves throughput for CPU-bound or high-concurrency workloads by using all available cores instead of leaving one process to handle everything alone.',null,'const cluster = require(''cluster'');
const os = require(''os'');
if (cluster.isPrimary) {
  os.cpus().forEach(() => cluster.fork());
} else {
  require(''./server'');
}','javascript','medium',ARRAY['nodejs','cluster']::text[],true,101),
('nodejs','performance-debugging','nodejs-performance-debugging-102','How does the cluster module distribute incoming connections across worker processes?','On most platforms, the primary process listens on the shared port and distributes incoming connections to workers using a round-robin scheduling algorithm (the default on non-Windows platforms since Node 4), handing each new connection to the next worker in rotation. Alternatively, worker processes can share the listening socket directly (OS-level load balancing) which was the older/default behavior and remains an option via cluster.schedulingPolicy.',null,null,null,'medium',ARRAY['nodejs','cluster']::text[],false,102),
('nodejs','performance-debugging','nodejs-performance-debugging-103','What is the difference between the cluster module and worker_threads in Node.js?','cluster forks entirely separate OS processes, each with its own memory space and its own event loop, communicating only via IPC (message passing) or a shared listening socket — well-suited to scaling a stateless HTTP server across CPU cores. worker_threads run actual threads within the same process, which can share memory directly via SharedArrayBuffer and have lower overhead to spawn, making them better suited to offloading CPU-intensive computation (like image processing or heavy parsing) without duplicating the whole process.',null,null,null,'medium',ARRAY['nodejs','worker-threads','cluster']::text[],true,103),
('nodejs','performance-debugging','nodejs-performance-debugging-104','When would you choose worker_threads over spawning a child process for CPU-intensive work?','worker_threads are preferable when you need to offload a CPU-bound task (like image resizing, complex data transformation, or hashing) from the main event loop but want lower overhead than a full separate process, and potentially need to share memory efficiently via SharedArrayBuffer/Atomics. A child process is more appropriate when you need process-level isolation (e.g. running an untrusted or crash-prone task, or a completely different executable/language), since a worker thread crash can still be more tightly coupled to the parent than a fully separate process.',null,'const { Worker } = require(''worker_threads'');
const worker = new Worker(''./heavy-task.js'', { workerData: { input: 42 } });
worker.on(''message'', (result) => console.log(result));','javascript','medium',ARRAY['nodejs','worker-threads']::text[],false,104),
('nodejs','performance-debugging','nodejs-performance-debugging-105','How do worker threads communicate with the main thread in Node.js?','Communication happens via message passing using postMessage() and the ''message'' event, similar to Web Workers in browsers — data passed this way is structured-cloned (copied) by default, though SharedArrayBuffer instances can be transferred to allow actual shared memory access between threads for performance-sensitive cases. workerData lets you pass initial data when a worker is created, without needing a separate message round-trip.',null,null,null,'medium',ARRAY['nodejs','worker-threads']::text[],false,105),
('nodejs','performance-debugging','nodejs-performance-debugging-106','What is a memory leak in a Node.js application, and what are some common causes?','A memory leak occurs when memory that''s no longer needed isn''t released because something still holds a reference to it, causing heap usage to grow steadily over time until the process eventually crashes (often with an out-of-memory error) or performance degrades from excessive garbage collection. Common causes include global variables/caches that grow unbounded, event listeners or timers that are never removed/cleared, closures unintentionally retaining large objects, and accumulating references in long-lived collections like arrays or Maps.',null,null,null,'easy',ARRAY['nodejs','memory']::text[],true,106),
('nodejs','performance-debugging','nodejs-performance-debugging-107','How would you take and analyze a heap snapshot to diagnose a memory leak in a Node.js app?','You can generate a heap snapshot using the built-in inspector (node --inspect, then triggering a snapshot from Chrome DevTools'' Memory tab) or programmatically via the v8 module''s writeHeapSnapshot(). Comparing snapshots taken at different points in time — especially after triggering garbage collection — lets you identify object types whose retained count keeps growing, then trace their retainer chains in DevTools to find what''s holding onto them.',null,'const v8 = require(''v8'');
v8.writeHeapSnapshot(''heap.heapsnapshot'');','javascript','hard',ARRAY['nodejs','memory','debugging']::text[],false,107),
('nodejs','performance-debugging','nodejs-performance-debugging-108','How do you attach a debugger to a running Node.js process?','Run the process with node --inspect (or --inspect-brk to pause immediately at the first line) to start a debugging WebSocket server, then connect either through Chrome DevTools (chrome://inspect) or an IDE like VS Code that supports the Node inspector protocol, letting you set breakpoints, step through code, and inspect variables in the running process.',null,'node --inspect-brk index.js','javascript','easy',ARRAY['nodejs','debugging']::text[],false,108),
('nodejs','performance-debugging','nodejs-performance-debugging-109','What is CPU profiling in Node.js, and how would you generate and read a CPU profile?','CPU profiling samples the call stack at regular intervals while your program runs, building a picture of where time is actually spent — useful for finding unexpectedly hot functions. You can generate one with node --prof (producing a v8 log processed by node --prof-process), or interactively via the Profiler tab in Chrome DevTools when connected through --inspect, which shows a flame graph highlighting functions consuming the most CPU time.',null,'node --prof app.js
node --prof-process isolate-*.log > profile.txt','javascript','medium',ARRAY['nodejs','performance','profiling']::text[],false,109),
('nodejs','performance-debugging','nodejs-performance-debugging-110','What is V8''s garbage collector, and how does the generational hypothesis influence its design?','V8''s garbage collector automatically reclaims memory no longer reachable from the program''s roots. It splits the heap into a ''young generation'' (new space, for recently allocated objects) collected frequently with a fast scavenge algorithm, and an ''old generation'' for objects that survive multiple collections, collected less often with a more thorough mark-and-sweep/mark-compact algorithm — based on the generational hypothesis that most objects die young, so it''s efficient to focus frequent, cheap collection effort there.',null,null,null,'hard',ARRAY['nodejs','v8','memory']::text[],false,110),
('nodejs','performance-debugging','nodejs-performance-debugging-111','What does a ''JavaScript heap out of memory'' crash mean, and how would you address it?','It means V8''s heap grew to hit its size limit (by default a few GB, tunable) and the garbage collector couldn''t free enough memory to satisfy an allocation, so the process aborts. Fixes include increasing the heap limit with --max-old-space-size if the workload genuinely needs more memory, but more often it signals an actual leak or an inefficient pattern (like loading an entire huge dataset into memory) that should be fixed by streaming data, paginating, or eliminating retained references, rather than just raising the limit as a band-aid.',null,'node --max-old-space-size=4096 app.js','javascript','medium',ARRAY['nodejs','memory','v8']::text[],false,111),
('nodejs','performance-debugging','nodejs-performance-debugging-112','How would you diagnose why a Node.js server''s response times are degrading under load?','You''d typically start with metrics (CPU usage, event loop lag, memory, GC pauses) to narrow down whether it''s CPU-bound (a hot synchronous function blocking the loop), memory-bound (excessive GC pauses from high memory pressure), or I/O-bound (slow downstream calls or database queries) — tools like clinic.js, 0x (flame graphs), or APM products (like New Relic/Datadog) help visualize where time is actually going, rather than guessing.',null,null,null,'medium',ARRAY['nodejs','performance','debugging']::text[],false,112),
('nodejs','performance-debugging','nodejs-performance-debugging-113','What is ''event loop lag'' (or event loop delay), and why is it a useful metric to monitor in production?','Event loop lag measures how much longer a scheduled callback (like a timer) takes to actually fire compared to its expected delay, which reveals how ''busy'' the event loop is with synchronous work — high lag means requests are queuing up behind blocking code, directly hurting latency for every concurrent request. Node exposes perf_hooks.monitorEventLoopDelay() for measuring this, and many APM tools track it as a key health indicator for Node services.',null,null,null,'medium',ARRAY['nodejs','performance','event-loop']::text[],false,113),
('nodejs','performance-debugging','nodejs-performance-debugging-114','How would you keep a Node.js process running reliably in production, automatically restarting it if it crashes?','Rather than relying on the raw `node` command, production deployments use a process manager — like PM2, systemd, or the container orchestrator''s own restart policy (e.g. Kubernetes'' restartPolicy) — that monitors the process, restarts it automatically on crash, can manage log output, and often supports zero-downtime restarts and clustering built in, none of which the bare Node runtime provides on its own.',null,null,null,'easy',ARRAY['nodejs','deployment','process']::text[],false,114),
('nodejs','performance-debugging','nodejs-performance-debugging-115','What''s a subtle way that a Node.js server can leak memory via Express or a similar framework, related to closures?','A common pattern is a middleware or route handler that captures a large object (like a full request payload, or a reference to a large cache) in a closure that then gets attached to something long-lived — like an event listener registered once at startup, or a timer callback that keeps rescheduling itself — so that object (and everything it references) is never eligible for garbage collection even after the request that created it has long finished.',null,null,null,'hard',ARRAY['nodejs','memory','express']::text[],false,115),
('nodejs','performance-debugging','nodejs-performance-debugging-116','How would you profile and fix a slow synchronous function that''s blocking the Node.js event loop?','First confirm the culprit via CPU profiling (or by measuring event loop lag spiking during that operation), then address it either by optimizing the algorithm itself (e.g. avoiding O(n²) patterns, reducing unnecessary work), breaking the computation into smaller chunks interleaved with setImmediate/setTimeout so the loop gets a chance to process other events, or offloading it entirely to a worker thread if it''s genuinely CPU-heavy and can''t be optimized further.',null,null,null,'hard',ARRAY['nodejs','performance','event-loop']::text[],false,116),
('nodejs','performance-debugging','nodejs-performance-debugging-117','What is graceful shutdown in the context of a clustered/multi-process Node.js deployment, and why does it matter?','Graceful shutdown means a process stops accepting new work, finishes in-flight requests, closes connections/resources cleanly (like database pools), and only then exits — rather than terminating abruptly and dropping active requests or leaving connections in a bad state. In a clustered deployment, this typically involves the primary process signaling workers individually (often on a rolling basis) so the whole fleet doesn''t go down simultaneously during a deploy.',null,null,null,'medium',ARRAY['nodejs','cluster','deployment']::text[],false,117),
('nodejs','performance-debugging','nodejs-performance-debugging-118','What does the --max-old-space-size flag control, and why might you need to adjust it in a containerized environment?','It sets the maximum size (in MB) V8''s old generation heap can grow to before triggering aggressive garbage collection and eventually an out-of-memory crash. In containers, V8''s default heap size historically wasn''t aware of the container''s actual memory limit (only the host''s), so a process could either crash prematurely or, conversely, be OOM-killed by the container runtime before V8''s own limit kicked in — explicitly setting it (or ensuring a recent Node version that detects cgroup limits) helps align V8''s behavior with the container''s actual memory budget.',null,null,null,'hard',ARRAY['nodejs','v8','deployment']::text[],false,118),
('nodejs','performance-debugging','nodejs-performance-debugging-119','How would you use console.time()/console.timeEnd() versus a proper profiler, and when is each appropriate?','console.time(label) / console.timeEnd(label) gives a quick wall-clock duration for a specific block of code, useful for a fast sanity check on how long something takes during development. A proper profiler (CPU profile, flame graph) is needed when you don''t already know where the bottleneck is, since it samples the entire call stack over time and shows relative time spent across many functions, rather than requiring you to manually instrument each suspect.',null,'console.time(''parse'');
const data = JSON.parse(largeString);
console.timeEnd(''parse''); // parse: 42.123ms','javascript','easy',ARRAY['nodejs','debugging','performance']::text[],false,119),
('nodejs','performance-debugging','nodejs-performance-debugging-120','What is the purpose of the node --inspect flag versus the built-in debugger statement?','node --inspect starts the V8 inspector protocol server, allowing an external tool like Chrome DevTools or VS Code to connect and debug the whole running process interactively. The debugger statement, when placed in code, pauses execution at that exact line if a debugger is already attached (similar to a manual breakpoint) — it has no effect at all if nothing is inspecting the process, so it''s typically used together with --inspect rather than as a standalone tool.',null,'function process(data) {
  debugger; // pauses here if a debugger is attached
  return transform(data);
}','javascript','easy',ARRAY['nodejs','debugging']::text[],false,120),
('nodejs','performance-debugging','nodejs-performance-debugging-121','How does inter-process communication (IPC) work between a cluster primary and its workers?','When a worker is forked via cluster.fork() (or child_process.fork()), Node automatically sets up an IPC channel between parent and child, letting them exchange messages with .send()/on(''message'') without manually managing sockets or pipes. This is how the primary process can, for example, broadcast a shutdown signal to all workers, or workers can report their status back to the primary.',null,'// primary
worker.send({ cmd: ''shutdown'' });
// worker
process.on(''message'', (msg) => { if (msg.cmd === ''shutdown'') gracefulExit(); });','javascript','medium',ARRAY['nodejs','cluster','ipc']::text[],false,121),
('nodejs','performance-debugging','nodejs-performance-debugging-122','What''s a downside of using the cluster module for scaling a stateful application (e.g. one relying on in-memory session storage)?','Since each cluster worker is a separate process with its own memory, in-memory state (like a session store, cache, or websocket connection registry) isn''t automatically shared across workers — a request handled by worker A won''t see state stored by worker B, which can cause inconsistent behavior (like session data appearing to randomly disappear) unless that state is moved to a shared external store like Redis, or requests are pinned (''sticky sessions'') to a specific worker.',null,null,null,'medium',ARRAY['nodejs','cluster','architecture']::text[],false,122),
('nodejs','performance-debugging','nodejs-performance-debugging-123','How would you detect and fix an event listener leak (Node''s ''MaxListenersExceededWarning'')?','Node''s EventEmitter warns by default if more than 10 listeners are attached to a single event on one emitter, which usually signals listeners are being added repeatedly (e.g. inside a function called on every request) without ever being removed. The fix is typically to register the listener once at an appropriate scope rather than per-call, or explicitly remove it with .removeListener()/.off() when it''s no longer needed; if a higher limit is genuinely intentional, emitter.setMaxListeners(n) suppresses the warning.',null,'emitter.setMaxListeners(20); // only if genuinely needed, not to silence a real leak','javascript','medium',ARRAY['nodejs','memory','events']::text[],false,123),
('nodejs','performance-debugging','nodejs-performance-debugging-124','What tools or techniques would you use to generate a flame graph for a Node.js application?','The 0x tool wraps your app, records a CPU profile while it runs, and generates an interactive flame graph automatically. Alternatively, Chrome DevTools'' Profiler tab (connected via --inspect) can record a CPU profile and render it as a flame chart directly. Flame graphs are useful for quickly spotting which function call stacks consume disproportionate CPU time, visualized by the width of each stack frame.',null,'npx 0x app.js','javascript','hard',ARRAY['nodejs','profiling','performance']::text[],false,124),
('nodejs','performance-debugging','nodejs-performance-debugging-125','Why is JSON.parse()/JSON.stringify() on very large objects potentially a performance concern in Node.js?','Both are synchronous operations that block the event loop for their entire duration, and for a sufficiently large object/string, that duration can be significant (tens to hundreds of milliseconds or more), freezing the whole process for every concurrent request during that time. For very large payloads, options include streaming JSON parsers, splitting into smaller chunks, or offloading the operation to a worker thread.',null,null,null,'medium',ARRAY['nodejs','performance','event-loop']::text[],false,125),
('nodejs','performance-debugging','nodejs-performance-debugging-126','What is the difference between vertical and horizontal scaling for a Node.js application, and which does clustering help with?','Vertical scaling means giving a single instance more resources (a bigger CPU/more memory), while horizontal scaling means running more instances (processes or machines) and distributing load across them. The cluster module addresses a specific gap in vertical scaling — using multiple cores on a single machine that a single Node process couldn''t otherwise use — while true horizontal scaling across multiple machines still requires an external load balancer in front of multiple deployed instances.',null,null,null,'easy',ARRAY['nodejs','cluster','performance']::text[],false,126),
('nodejs','performance-debugging','nodejs-performance-debugging-127','How would you use async_hooks or the perf_hooks module to trace performance across asynchronous operations?','perf_hooks provides a Performance Timeline API (performance.mark()/performance.measure(), similar to the browser equivalent) for timing arbitrary spans of code, including across await boundaries, and exposes utilities like monitorEventLoopDelay(). async_hooks is a lower-level API for tracking the lifecycle of async resources (their creation, before/after execution, and destruction), which is powerful for building tracing/context-propagation tools but is more complex and has historically carried some performance overhead, so it''s often used indirectly via higher-level libraries (like AsyncLocalStorage, which is itself built on it) rather than directly.',null,'const { performance } = require(''perf_hooks'');
performance.mark(''start'');
await doWork();
performance.mark(''end'');
performance.measure(''doWork'', ''start'', ''end'');','javascript','hard',ARRAY['nodejs','profiling','async']::text[],false,127),
('nodejs','performance-debugging','nodejs-performance-debugging-128','What is a ''zombie'' or orphaned child process in Node.js, and how can it happen?','It happens when a Node process spawns a child process (via child_process.spawn/exec/fork) but exits or crashes without properly waiting for or cleaning up that child, leaving the child running independently — or, in Unix terms, a ''zombie'' process if the child has exited but its exit status was never reaped by the parent. Guarding against it typically means listening for the child''s ''exit'' event, killing children explicitly on the parent''s shutdown handlers, and not ignoring unhandled errors that could cause an unclean parent exit.',null,null,null,'hard',ARRAY['nodejs','process','debugging']::text[],false,128),
('nodejs','performance-debugging','nodejs-performance-debugging-129','How would you decide whether a performance problem should be solved with caching versus with clustering/scaling?','Caching addresses redundant work — if the same expensive computation or database query is repeated for the same input, storing and reusing the result (in memory, or an external store like Redis) avoids paying that cost again, which reduces load without adding infrastructure. Clustering/scaling addresses raw capacity — when the workload is inherently diverse (different inputs each time) or CPU-bound in a way caching can''t help, adding more processes/instances is the appropriate lever instead. In practice, both are often combined, but caching is usually the cheaper first optimization to reach for.',null,null,null,'medium',ARRAY['nodejs','performance','cluster']::text[],false,129),
('nodejs','performance-debugging','nodejs-performance-debugging-130','What is ''warm-up'' latency in Node.js applications, and why might the first few requests after startup be slower?','V8''s JIT compiler starts by interpreting code and only optimizes (''warms up'') functions after observing them run repeatedly with consistent argument types, so the very first executions of any given code path run less optimized than they will after a few iterations. Additionally, lazy-initialized resources (database connection pools, cached configuration, first-time module loading) commonly add extra latency to the first requests specifically, which is why production deployments sometimes include a warm-up phase or health-check delay before routing real traffic to a fresh instance.',null,null,null,'hard',ARRAY['nodejs','v8','performance']::text[],false,130),
('nodejs','performance-debugging','nodejs-performance-debugging-131','How would you find which npm dependency is responsible for a slow application startup time?','You can run the script with node --prof and inspect the processed log for time spent during module initialization, or more simply, wrap require() calls with console.time()/console.timeEnd() around suspect imports to isolate which one is slow — often caused by a package doing heavy synchronous work (like reading large files or compiling something) at import time rather than lazily. Tools like the require-so-slow or clinic.js doctor can also visualize module load time directly.',null,null,null,'medium',ARRAY['nodejs','performance','debugging']::text[],false,131),
('nodejs','performance-debugging','nodejs-performance-debugging-132','What is the difference between a memory leak and simply high (but stable) memory usage in a Node.js process?','High but stable memory usage means the process''s memory grows to a certain level and then plateaus, fluctuating within a consistent range as the garbage collector reclaims and reuses memory normally — this can be totally fine, just reflecting the app''s genuine working set. A true leak shows memory climbing steadily over time without ever returning to a stable baseline, even after forced garbage collection and during periods of low load, indicating something is retaining references that should have been released.',null,null,null,'medium',ARRAY['nodejs','memory']::text[],false,132),
('nodejs','performance-debugging','nodejs-performance-debugging-133','Why is it generally discouraged to call global.gc() to manually trigger garbage collection in production code?','Forcing garbage collection (only available at all when Node is run with the --expose-gc flag) is a blocking, potentially expensive synchronous operation that pauses the event loop, and V8''s own automatic GC heuristics are generally well-tuned to balance memory usage against pause times better than manual intervention would. Manually forcing it in application logic (as opposed to using it as a diagnostic tool during profiling) tends to mask an underlying leak rather than fix it, while also actively hurting latency.',null,null,null,'hard',ARRAY['nodejs','memory','v8']::text[],false,133),
('nodejs','streams-buffers','nodejs-streams-buffers-101','What is a Stream in Node.js, and why are they used instead of loading a whole file into memory?','A Stream is an abstraction for handling data that arrives incrementally, in chunks, rather than all at once — used for reading/writing files, HTTP request/response bodies, and more. Streams let you start processing data before the entire source has been read and keep memory usage bounded, which matters a lot for large files or long-lived connections where loading everything into memory at once would be wasteful or impossible.',null,null,null,'easy',ARRAY['nodejs','streams']::text[],true,101),
('nodejs','streams-buffers','nodejs-streams-buffers-102','What are the four fundamental types of streams in Node.js?','Readable streams produce data you can consume (e.g. fs.createReadStream). Writable streams accept data you write to them (e.g. fs.createWriteStream). Duplex streams are both readable and writable, treated as independent channels (e.g. a TCP socket). Transform streams are a special kind of Duplex where the output is computed from the input as it passes through (e.g. a gzip compressor).',null,null,null,'medium',ARRAY['nodejs','streams']::text[],true,102),
('nodejs','streams-buffers','nodejs-streams-buffers-103','What is the purpose of the .pipe() method on a Readable stream?','.pipe() connects a readable stream''s output directly to a writable stream''s input, automatically handling the reading, writing, and — critically — backpressure (pausing the source when the destination is overwhelmed) without you needing to manually manage ''data'' events and write() calls. It''s the idiomatic way to move data between streams, such as piping a file read stream into an HTTP response.',null,'const fs = require(''fs'');
fs.createReadStream(''input.txt'').pipe(fs.createWriteStream(''output.txt''));','javascript','easy',ARRAY['nodejs','streams']::text[],true,103),
('nodejs','streams-buffers','nodejs-streams-buffers-104','What is backpressure in the context of Node.js streams, and why does it matter?','Backpressure is what happens when a writable destination can''t consume data as fast as a readable source produces it — without handling it, the source would keep pushing data into memory faster than it can be written out, causing unbounded memory growth. Streams handle this by having write() return false when the internal buffer is full, signaling the source to pause until a ''drain'' event fires; .pipe() manages this automatically.',null,null,null,'hard',ARRAY['nodejs','streams','backpressure']::text[],true,104),
('nodejs','streams-buffers','nodejs-streams-buffers-105','What is the difference between ''flowing'' and ''paused'' mode for a Readable stream?','In flowing mode, data is read from the underlying source and pushed to consumers as fast as possible, typically triggered by attaching a ''data'' event listener or calling .pipe(). In paused mode (the default), data must be explicitly pulled by calling .read(), giving the consumer manual control over the pace of consumption — streams start paused and switch to flowing when a ''data'' listener is added, .resume() is called, or .pipe() is used.',null,null,null,'hard',ARRAY['nodejs','streams']::text[],false,105),
('nodejs','streams-buffers','nodejs-streams-buffers-106','How would you create a custom Transform stream, and what''s a typical use case?','You extend the Transform class (or use the transform constructor option) and implement a _transform(chunk, encoding, callback) method that processes each incoming chunk and calls callback(null, transformedChunk) to push output downstream. A typical use case is a line-by-line text processor, a CSV parser, or a stream that uppercases/encrypts/compresses data as it flows through.',null,'const { Transform } = require(''stream'');
const upper = new Transform({
  transform(chunk, encoding, callback) {
    callback(null, chunk.toString().toUpperCase());
  },
});
process.stdin.pipe(upper).pipe(process.stdout);','javascript','hard',ARRAY['nodejs','streams','transform']::text[],false,106),
('nodejs','streams-buffers','nodejs-streams-buffers-107','What is a Buffer in Node.js, and how does it relate to TypedArrays like Uint8Array?','A Buffer represents a fixed-length sequence of raw bytes in memory, outside V8''s usual JS-string encoding, used for handling binary data such as file contents or network payloads. Modern Node implements Buffer as a subclass of JavaScript''s built-in Uint8Array, adding Node-specific convenience methods (like .toString(encoding) and Buffer.concat()) on top of the standard typed array behavior.',null,null,null,'medium',ARRAY['nodejs','buffer']::text[],false,107),
('nodejs','streams-buffers','nodejs-streams-buffers-108','What are the different ways to create a Buffer, and which is generally recommended?','Buffer.from(data) creates a buffer from a string, array, or existing buffer (and is the recommended, safe approach). Buffer.alloc(size) creates a zero-filled buffer of a given size, safe by default but slightly slower. Buffer.allocUnsafe(size) creates an uninitialized buffer faster, but its contents are leftover memory that must be fully overwritten before use, since it could otherwise expose old, potentially sensitive data.',null,'const a = Buffer.from(''hello'');
const b = Buffer.alloc(10);
const c = Buffer.allocUnsafe(10); // must overwrite fully','javascript','medium',ARRAY['nodejs','buffer']::text[],false,108),
('nodejs','streams-buffers','nodejs-streams-buffers-109','How do you convert a Buffer to a string and back, and why does encoding matter?','buffer.toString(encoding) decodes the raw bytes into a string using a given encoding (defaulting to ''utf8''), while Buffer.from(string, encoding) does the reverse. Encoding matters because multi-byte characters (like emoji or non-ASCII text) can be split across chunk boundaries in a stream, or misinterpreted entirely if you decode with the wrong encoding, producing garbled (''mojibake'') output.',null,'const buf = Buffer.from(''café'', ''utf8'');
console.log(buf.toString(''utf8'')); // ''café''
console.log(buf.toString(''hex'')); // raw byte representation','javascript','easy',ARRAY['nodejs','buffer','encoding']::text[],false,109),
('nodejs','streams-buffers','nodejs-streams-buffers-110','What is the difference between fs.createReadStream() and fs.readFile() for reading a large file?','fs.readFile() reads the entire file into memory before invoking its callback with the complete contents, which is simple but can consume large amounts of memory for big files and delays any processing until the whole read completes. fs.createReadStream() reads the file incrementally in chunks, letting you start processing (or piping elsewhere) data as it arrives, with much lower peak memory usage for large files.',null,'const stream = fs.createReadStream(''large-file.txt'', { encoding: ''utf8'' });
stream.on(''data'', (chunk) => process(chunk));
stream.on(''end'', () => console.log(''done''));','javascript','medium',ARRAY['nodejs','streams','fs']::text[],true,110),
('nodejs','streams-buffers','nodejs-streams-buffers-111','How would you use streams to process a large CSV file line by line without loading it fully into memory?','Pipe a fs.createReadStream through a line-splitting mechanism — either the built-in readline module''s createInterface() wrapping the stream, or a Transform stream that buffers partial lines and emits complete ones — so each line can be processed and discarded as it arrives instead of retaining the whole file in memory.',null,'const readline = require(''readline'');
const rl = readline.createInterface({ input: fs.createReadStream(''data.csv'') });
rl.on(''line'', (line) => processRow(line));','javascript','medium',ARRAY['nodejs','streams','fs']::text[],false,111),
('nodejs','streams-buffers','nodejs-streams-buffers-112','What events does a Readable stream emit, and what does each signal?','''data'' fires whenever a chunk of data is available (only in flowing mode). ''end'' fires once all data has been consumed with no more to come. ''error'' fires if something goes wrong during reading. ''close'' fires when the underlying resource has been fully released. Listening for ''error'' is essential — an unhandled stream error event throws and can crash the process.',null,null,null,'medium',ARRAY['nodejs','streams']::text[],false,112),
('nodejs','streams-buffers','nodejs-streams-buffers-113','Why is it important to always attach an ''error'' event listener to a stream?','Streams are EventEmitters, and by default, an ''error'' event with no registered listener throws inside Node''s internals and crashes the process — unlike other events, which are simply ignored if unlistened. Since I/O is inherently failure-prone (missing files, network drops, disk errors), omitting an error handler on a stream is a common and dangerous oversight in production code.',null,'stream.on(''error'', (err) => console.error(''Stream failed:'', err));','javascript','medium',ARRAY['nodejs','streams','error-handling']::text[],true,113),
('nodejs','streams-buffers','nodejs-streams-buffers-114','How would you copy a large file efficiently in Node.js using streams?','Pipe a read stream of the source directly into a write stream of the destination, which handles chunked reading, writing, and backpressure without loading the whole file into memory; alternatively fs.promises.copyFile() offers a simpler, often OS-optimized, one-line alternative for the common case.',null,'fs.createReadStream(''source.dat'').pipe(fs.createWriteStream(''dest.dat''));','javascript','easy',ARRAY['nodejs','streams','fs']::text[],false,114),
('nodejs','streams-buffers','nodejs-streams-buffers-115','What is stream.pipeline(), and why is it preferred over chaining multiple .pipe() calls manually?','stream.pipeline() (from the ''stream'' module) connects a series of streams together like chained .pipe() calls, but additionally forwards errors from any stream in the chain to a single callback (or rejects a returned promise with stream/promises), and automatically cleans up/destroys all streams if any of them errors or the pipeline is aborted. Manually chained .pipe() calls don''t propagate errors between streams or reliably clean up on failure, which can leak file descriptors or leave dangling streams.',null,'const { pipeline } = require(''stream/promises'');
await pipeline(
  fs.createReadStream(''in.txt''),
  zlib.createGzip(),
  fs.createWriteStream(''in.txt.gz'')
);','javascript','hard',ARRAY['nodejs','streams','error-handling']::text[],false,115),
('nodejs','streams-buffers','nodejs-streams-buffers-116','How would you compress a file using gzip while streaming, without buffering the whole thing in memory?','Pipe a read stream through zlib.createGzip() (a Transform stream) and into a write stream — each chunk is compressed as it passes through, so peak memory usage stays proportional to the chunk size rather than the whole file.',null,'const zlib = require(''zlib'');
fs.createReadStream(''file.txt'')
  .pipe(zlib.createGzip())
  .pipe(fs.createWriteStream(''file.txt.gz''));','javascript','medium',ARRAY['nodejs','streams','zlib']::text[],false,116),
('nodejs','streams-buffers','nodejs-streams-buffers-117','What''s the difference between an HTTP request/response object in Node''s http module and a generic stream?','Node''s http.IncomingMessage (the request object) is a Readable stream, and the http.ServerResponse (the response object) is a Writable stream — you consume the request body by reading from it like any other readable stream, and send a response by writing to it, including support for .pipe(). This is why you can, for example, pipe a file directly into a response object to serve it efficiently.',null,'http.createServer((req, res) => {
  fs.createReadStream(''index.html'').pipe(res);
});','javascript','medium',ARRAY['nodejs','streams','http']::text[],false,117),
('nodejs','streams-buffers','nodejs-streams-buffers-118','What does the highWaterMark option control when creating a stream?','highWaterMark sets the size (in bytes, or number of objects in object mode) of the internal buffer a stream uses before it stops reading more data (or before write() starts returning false, signaling backpressure). It''s a tuning knob for the tradeoff between memory usage and throughput — a larger highWaterMark buffers more data ahead of time, potentially improving throughput at the cost of memory.',null,'fs.createReadStream(''file.txt'', { highWaterMark: 64 * 1024 }); // 64KB chunks','javascript','hard',ARRAY['nodejs','streams','backpressure']::text[],false,118),
('nodejs','streams-buffers','nodejs-streams-buffers-119','What are Node''s async iterators for streams, and how do they relate to for await...of?','Since Node 10+, Readable streams implement the async iterator protocol (Symbol.asyncIterator), letting you consume them with for await...of instead of manually attaching ''data''/''end'' listeners — each iteration yields the next chunk and automatically respects backpressure by pausing internal reads between iterations.',null,'for await (const chunk of fs.createReadStream(''file.txt'')) {
  console.log(chunk.length);
}','javascript','medium',ARRAY['nodejs','streams','async']::text[],false,119),
('nodejs','streams-buffers','nodejs-streams-buffers-120','How do you read and write JSON files synchronously and asynchronously in Node.js?','Synchronously: fs.readFileSync(path, ''utf8'') followed by JSON.parse(), and JSON.stringify() followed by fs.writeFileSync(). Asynchronously, the same pattern applies but using fs.promises.readFile / fs.promises.writeFile (or their callback equivalents) so the I/O doesn''t block the event loop — generally preferred in server code, reserving the sync versions for startup scripts or CLI tools.',null,'const data = JSON.parse(await fs.promises.readFile(''config.json'', ''utf8''));
await fs.promises.writeFile(''out.json'', JSON.stringify(data, null, 2));','javascript','easy',ARRAY['nodejs','fs']::text[],false,120),
('nodejs','streams-buffers','nodejs-streams-buffers-121','What is the difference between fs.watch() and fs.watchFile() for monitoring file changes?','fs.watch() uses the operating system''s native file-system notification APIs, so it''s more efficient and near-instant, but its behavior (like whether renames trigger events) can vary between platforms. fs.watchFile() works by polling the file''s stats at an interval, which is more consistent across platforms but less efficient and slower to detect changes, and is generally considered legacy in favor of fs.watch() or third-party libraries like chokidar.',null,null,null,'medium',ARRAY['nodejs','fs']::text[],false,121),
('nodejs','streams-buffers','nodejs-streams-buffers-122','How would you check whether a file or directory exists before performing an operation on it in Node.js?','The recommended approach is to attempt the operation directly (e.g. fs.promises.readFile) and handle the ENOENT error if it doesn''t exist, since checking existence first (with fs.existsSync or fs.access) and then acting on it separately introduces a race condition (the file could be deleted between the check and the actual operation) known as a TOCTOU bug. fs.existsSync is still fine for simple, non-concurrent scripts.',null,'try {
  await fs.promises.access(''file.txt'');
} catch {
  console.log(''does not exist'');
}','javascript','medium',ARRAY['nodejs','fs']::text[],false,122),
('nodejs','streams-buffers','nodejs-streams-buffers-123','What is the difference between appending to a file with fs.appendFile() versus opening a write stream in append mode?','fs.appendFile() opens the file, writes the data, and closes it again for each call — simple for occasional appends but relatively expensive if called very frequently (e.g. logging every request). A write stream opened with { flags: ''a'' } keeps the file descriptor open across many writes, amortizing the open/close overhead, which is more efficient for high-frequency append workloads like log files.',null,'const logStream = fs.createWriteStream(''app.log'', { flags: ''a'' });
logStream.write(''log entry\n'');','javascript','medium',ARRAY['nodejs','fs','streams']::text[],false,123),
('nodejs','streams-buffers','nodejs-streams-buffers-124','How would you handle a Buffer that contains a multi-byte UTF-8 character split across two separate stream chunks?','Naively calling chunk.toString(''utf8'') on each raw chunk independently can produce garbled output where a multi-byte character is split mid-sequence. Node''s StringDecoder class (from the ''string_decoder'' module) is designed for exactly this — it buffers incomplete byte sequences internally and only emits fully-decoded characters, correctly handling characters split across chunk boundaries.',null,'const { StringDecoder } = require(''string_decoder'');
const decoder = new StringDecoder(''utf8'');
let result = '''';
stream.on(''data'', (chunk) => { result += decoder.write(chunk); });','javascript','hard',ARRAY['nodejs','buffer','streams']::text[],false,124),
('nodejs','streams-buffers','nodejs-streams-buffers-125','What does ''object mode'' mean for a Node.js stream, and when would you use it?','By default, streams operate on Buffers or strings. In object mode (set via { objectMode: true }), a stream can instead push and consume arbitrary JavaScript objects, one at a time, rather than raw bytes — useful for building processing pipelines over structured data, like a stream of parsed database rows or JSON records, rather than raw text or binary content.',null,'const { Transform } = require(''stream'');
const toUpperName = new Transform({
  objectMode: true,
  transform(obj, enc, cb) { cb(null, { ...obj, name: obj.name.toUpperCase() }); },
});','javascript','hard',ARRAY['nodejs','streams']::text[],false,125),
('nodejs','streams-buffers','nodejs-streams-buffers-126','What''s a memory-related pitfall of using Buffer.concat() repeatedly inside a loop to accumulate stream data?','Each call to Buffer.concat() allocates a brand-new buffer and copies all the existing bytes into it, so accumulating data by calling concat() on every chunk in a loop is O(n²) in total copying work as the accumulated size grows. It''s much more efficient to collect chunks in an array and call Buffer.concat(chunksArray) once at the end, after all chunks have arrived.',null,'// Inefficient
let buf = Buffer.alloc(0);
stream.on(''data'', (chunk) => { buf = Buffer.concat([buf, chunk]); });
// Efficient
const chunks = [];
stream.on(''data'', (chunk) => chunks.push(chunk));
stream.on(''end'', () => { const buf = Buffer.concat(chunks); });','javascript','hard',ARRAY['nodejs','buffer','performance']::text[],false,126),
('nodejs','streams-buffers','nodejs-streams-buffers-127','How would you limit how much of an uploaded file a server reads, to prevent a client from sending an excessively large payload?','Track the total bytes received as chunks arrive on the request stream, and if the running total exceeds a configured limit, destroy the stream and respond with an error (e.g. HTTP 413 Payload Too Large) instead of continuing to buffer or write the rest — this prevents a malicious or buggy client from exhausting server memory or disk space. Frameworks and body-parsing middleware (like Express''s built-in json/urlencoded parsers) typically expose a configurable size limit that does this for you.',null,null,null,'hard',ARRAY['nodejs','streams','security']::text[],false,127),
('nodejs','streams-buffers','nodejs-streams-buffers-128','What is the difference between fs.rm() and fs.unlink() in Node.js?','fs.unlink() removes a single file (and errors if given a directory). fs.rm() is a more general removal function that can also delete directories, especially with the { recursive: true } option (replacing the older, now-deprecated fs.rmdir({ recursive: true })), giving it functionality closer to a Unix `rm -rf`.',null,'await fs.promises.rm(''some-dir'', { recursive: true, force: true });','javascript','easy',ARRAY['nodejs','fs']::text[],false,128),
('nodejs','streams-buffers','nodejs-streams-buffers-129','How would you stream a large database query result set to an HTTP response without loading all rows into memory first?','Many database drivers expose a streaming/cursor-based query API that returns a Readable stream of rows instead of a single resolved array; piping that (often through a Transform to serialize each row, e.g. as newline-delimited JSON) directly into the HTTP response lets rows be sent to the client as they''re fetched from the database, keeping memory bounded regardless of result set size.',null,null,null,'hard',ARRAY['nodejs','streams','performance']::text[],false,129),
('nodejs','streams-buffers','nodejs-streams-buffers-130','What does stream.destroy() do, and when should you call it explicitly?','destroy() immediately terminates a stream, releasing any underlying resources (like an open file descriptor or socket), emitting a ''close'' event and optionally an ''error'' if given an error argument, without waiting for buffered data to finish flushing. You call it explicitly when you need to abort a stream early — for example, cancelling a file read because the client disconnected, or stopping processing once you''ve found what you needed partway through the data.',null,null,null,'medium',ARRAY['nodejs','streams']::text[],false,130),
('nodejs','streams-buffers','nodejs-streams-buffers-131','Why can working directly with Buffers be more efficient than working with strings when processing binary protocols or file formats?','Strings in JS are immutable and Unicode-aware, so operations on them (especially converting to/from an encoding, or slicing) involve overhead that''s unnecessary for raw binary data that has no meaningful character encoding. Buffers let you read and manipulate specific byte offsets directly (e.g. parsing a binary file header with buf.readUInt32BE(offset)), avoiding repeated encode/decode overhead and letting you interpret bytes with their true binary meaning.',null,'const value = buf.readUInt32BE(0); // read 4 bytes as a big-endian unsigned int','javascript','medium',ARRAY['nodejs','buffer','performance']::text[],false,131)
) as v(language_slug, topic_slug, slug, question, answer, example, code, code_language, difficulty, tags, is_frequently_asked, display_order)
join public.languages l on l.slug = v.language_slug
join public.topics t on t.language_id = l.id and t.slug = v.topic_slug
on conflict (slug) do update set
  question = excluded.question,
  answer = excluded.answer,
  example = excluded.example,
  code = excluded.code,
  code_language = excluded.code_language,
  difficulty = excluded.difficulty,
  tags = excluded.tags,
  is_frequently_asked = excluded.is_frequently_asked,
  display_order = excluded.display_order,
  topic_id = excluded.topic_id,
  language_id = excluded.language_id;
